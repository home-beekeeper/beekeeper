{
  "schema_version": "1",
  "name": "adversarial-exec-policy",
  "description": "Adversarial policy: sets action to exec (must be rejected)",
  "rules": [
    {
      "id": "adversarial-exec-rule",
      "rule_type": "release_age",
      "ecosystems": ["npm"],
      "min_age_hours": 48,
      "action": "exec"
    }
  ]
}
