# .continue-here — Resume handoff (paused 2026-06-12)

**Paused mid-flight:** GSD quick task **260612-f80** "detection-only first responder + reversible
quarantine framework". A fresh session should be able to execute the whole thing from THIS file
without re-reading the prior conversation. Read it top to bottom, then start at **REMAINING STEPS**.

> Honesty rules still apply (CLAUDE.md + memory): SCANNING inventory (Pollen/Bumblebee) stays
> READ-ONLY; quarantine is a NEW reversible-move action; Sentry stays DETECTION-ONLY; the
> destructive **purge** is ALWAYS human-gated via the TUI.

---

## QUICK-TASK INIT STATE (already run — do not re-init)
- quick_id: `260612-f80` ; slug: `detection-only-first-responder-sync-hit-`
- task_dir: `.planning/quick/260612-f80-detection-only-first-responder-sync-hit-`
- models: planner=**opus**, executor=**sonnet**, checker=sonnet, verifier=sonnet
- `branch_name=null`, `USE_WORKTREES=false`, run mode = **--validate** (plan-checker max 2 iters + verifier)
- gsd-tools shim: `/c/Users/Bantu/.claude/gsd-core/bin/gsd-tools.cjs` ; `roadmap_exists=true`
- Re-load config if needed: `node <shim> query init.quick "<desc>"`

## LOCKED DECISIONS (from AskUserQuestion this session)
1. **GIT:** checkpoint the session's uncommitted web redesign as ONE commit on `main`, THEN create a
   feature branch (e.g. `feat/first-responder-quarantine`) for the quick-task work, so the Go feature
   + its clean web-copy update are isolated from the redesign.
   - **Checkpoint INCLUDES:** all `web/**` modified+untracked + prototype HTMLs
     `beekeeper-home-v2.html..beekeeper-home-v5.html` + `web/public/*` favicons (favicon*.png/.ico,
     apple-touch-icon.png, android-chrome-*.png, site.webmanifest, beekeeper-logo.png).
   - **Checkpoint EXCLUDES:** `.claude/` (local screenshot/scratch), `.understand-anything/`,
     `AGENTS.md`, `beekeeper-docs.html`, `beekeeper-tui-prototype.html`,
     `.planning/phases/16-3d-layer/16-PATTERNS.md`, and `.continue-here.md` itself.
   - Use explicit `git add <paths>` (NOT `git add -A`) so scratch dirs are not swept in.
2. **SCOPE = "safe first slice":** implement **A + C1 + B** (below). **DEFER** to follow-on phases:
   **C2** destructive package-manager uninstall (npm/pip/cargo lockfile rewrite); **C3**
   browser-extension + mcp-config quarantine (those two are SCANNED but NOT CATALOGED — they need a
   local denylist trigger + config-file mutation, separate work).

## REFINED FIRST-RESPONDER DESIGN (user's latest, AUTHORITATIVE)
Quarantine acts on the corroboration engine: **catalog SYNC HIT → SCAN FIND → QUARANTINE**. It is
"destructive" but gated by corroboration **>= 2 sources** (bounds false positives). First-responder
behavior is the priority. **CRUCIAL UX:** exactly like a Sentry alert opens the TUI, a quarantine hit
for a compromised cataloged artifact **OPENS THE TUI incident**; the user then MANUALLY clicks
**Purge** (permanent) or **Restore**. So: AUTO **reversible-quarantine** (move to quarantine dir,
fully restorable) fires on corroboration >=2; the DESTRUCTIVE step (purge) is ALWAYS human-gated.
- Planner reconciliation note: current corroboration engine reserves the quarantine *recommendation*
  for **3** sources (1 warn / 2 block / 3 block+quarantine). User wants AUTO reversible-quarantine at
  **>=2** — reconcile by firing auto reversible-quarantine at the 2-source "block" tier (safe because
  reversible + human-gated purge). Make the auto-quarantine threshold a **config knob defaulting to 2**.

## A — sync-hit package rescan + cross-reference (read-only, locally testable)
Extend the `catalog.Watch` onDelta callback (`cmd/beekeeper/main.go` ~454-498; `internal/catalog/watch.go`
~223-264) which TODAY only rescans editor EXTENSIONS, so a catalog delta ALSO runs a Pollen/Bumblebee
package inventory and cross-references installed npm/pip/cargo/etc packages against the updated catalog
index, auditing any installed-package match as a "scan hit" finding. Reuse `internal/scan` +
`internal/policy` corroboration. Read-only matching.

## C1 — generalized reversible quarantine framework
Generalize `internal/quarantine` (`Move/List/Restore/Purge` — TODAY editor-extension ONLY; layout
`quarantineDir/extensions/<id>/` + `beekeeper-manifest.json`; triggered from `internal/watch/handler.go`
~209-221 EDXT-03) into a TYPE-AWARE framework that can quarantine any cataloged-and-installed artifact
that has a filesystem PATH: editor-extension (exists) + language packages via reversible directory move
(e.g. `node_modules/<pkg>`, `site-packages/<pkg>`). On a scan hit with corroboration >=2: auto
reversible-quarantine + write an audit incident + surface it in the TUI for human purge/restore.
Resolving the on-disk path of a flagged language package from a Pollen scan record is NON-TRIVIAL —
call out in plan; if the path is unknown, record a **"pending-quarantine"** TUI incident rather than
guessing. Gate: opt-in config, dry-run-default option, corroboration>=2, every action audited.

## B — catalog→Sentry targeted trace (detection-only, CI-only runtime)
Add a catalog→Sentry **"target list"** shared state: when a scan hit identifies a flagged+installed
artifact, record it (path/name/expected process) into state the Sentry daemon consults so it
focuses/tightens correlation on that process subtree (lower thresholds/faster escalation) and escalates
on cred-read or outbound. **DETECTION-ONLY — NO kill/isolate/network-cut.** Sentry OS taps (Linux
eBPF+fanotify / Windows ETW / macOS eslogger) are **CI-ONLY** on this Windows dev machine:
compile-validate + unit-test the shared target-list logic locally; live behavior CI-only — say so in SUMMARY.

## TESTS + VALIDATE
Go unit/integration tests for A + C1 (locally testable on Windows). For B, unit-test the target-list
logic; OS taps CI-only. Validate: `go test ./...`, `go vet ./...`, `go build ./...`.
- CRLF/Windows gotchas: golden fixtures need `.gitattributes` `text eol=lf`; ipc unix-socket tests
  compile-validate from Windows via `GOOS=linux/darwin go vet ./...` (go build does NOT compile tests).

## THEN: web/docs/blog accuracy update (AFTER Go is green)
- Home: `web/components/home/two-layers.tsx` reactive copy → add A sync-hit rescan + the
  auto-reversible-quarantine-at>=2 + human-gated TUI purge. `web/components/home/dashboard-demo.tsx`
  action row → reconcile to HONEST TUI: for a **catalog-quarantine** incident the real actions are
  **[P] purge / [R] restore** (human-gated); for **Sentry** incidents **acknowledge / full record**.
  `faq.tsx` if relevant.
- Docs: `web/content/docs/security.mdx` + `configuration.mdx` → document `catalog_sync`, the quarantine
  framework, the >=2 auto-quarantine knob, TUI-gated purge, the targeted Sentry trace.
- Blog: `web/content/blog/introducing-beekeeper.mdx`.
- Web honesty rules: **NO em-dashes in visible copy, NO colored left-edge accent stripes**; live-framing
  reversal already applied. Re-run web gate after: `pnpm build` + e2e `gfx`+`home` (13) + unit+postbuild (62).

## RESEARCH FINDINGS — EMBED THESE (do NOT re-investigate)
**[Catalog sync]** default **2h**, clamp **[2h,24h]**, `catalog_sync.interval`
(`internal/config/config.go` ~293-323; untrusted layers can only tighten). Periodic 2h daemon =
refresh mmap index + `enforceSelfQuarantine`, **NO scan**. `catalog.Watch` onDelta = re-Sync +
**EXTENSION rescan only** (`cmd/beekeeper/main.go` ~454-498). 304 = bump freshness only. Reactive flag
= `policy.Evaluate` (`internal/policy/engine.go` ~48) + corroboration (`internal/policy/corroboration.go`
~118-195: 1 warn / 2 block / 3 block+quarantine).

**[Sentry]** 8 rules `internal/sentry/rules.go` (001 cred-file cluster, 002 cred-CLI cluster, 003 first
external outbound, 004 fresh-ext, 005 exfil fusion, 006 agent-CLI cred, 007 generalized exfil fusion,
008 persistence write); scope = monitored descendants (editor+agent-CLI), `isMonitoredDescendant`
~rules.go:214. **DETECTION-ONLY** (no CAP_KILL; seccomp self-kills on privileged syscall
`internal/sentry/linux/privilege.go` ~67-95; `alertToAuditRecord` decision is a LABEL `linux/daemon.go`
~379-412; `QuarantineRec` rules.go ~320 is a rec flag). **NO catalog→Sentry linkage today** (watch
handler has no sentry import). TUI incidents `internal/tui/incidents.go` ~40-50 = acknowledge +
full-record only ([Q]/[I] deliberately removed); `alerts_panel.go` ~113 [Q] is for EDXT hits →
`quarantineAlertMsg` → toast (`model.go` ~158); `quarantine_panel.go` [r]/[p] are fs ops.

**[Bumblebee/Pollen coverage]** catalog ecosystems `internal/catalog/schema.go:22` =
`npm|pypi|go|rubygems|packagist|cargo|editor-extension` (7). Pollen record_types
`internal/scan/scanner.go` ~159-174 = `package|finding|scan_summary|scan_error|scan_status`; package
records cover **npm, editor-extension, browser-extension, mcp-config** (`scanner_test.go` ~246-282
PTEST-04) ⇒ **9 scanned kinds**; **browser-extension + mcp-config are SCANNED but NOT CATALOGED**.
Invocation `scanner.go` ~84-99: `scan` / `scan --profile deep --root <home>`; strictly **READ-ONLY**
(no remove/disable). Quarantine today `internal/quarantine/quarantine.go` = **editor-extension ONLY**.

### Coverage table
| Type | Scanned | Cataloged | Quarantine today | C1 in scope? |
|---|---|---|---|---|
| npm, PyPI, Go, Cargo, RubyGems, Packagist | ✓ | ✓ | ✗ | ✓ reversible dir-move |
| editor-extension | ✓ | ✓ | ✓ (exists) | ✓ (generalize) |
| browser-extension | ✓ | ✗ not cataloged | ✗ | ✗ defer (C3) |
| mcp-config | ✓ | ✗ not cataloged | ✗ | ✗ defer (C3) |

## REMAINING STEPS FOR FRESH SESSION
1. **Git checkpoint + branch** (see LOCKED DECISIONS #1). Use explicit `git add <paths>`.
2. **Spawn gsd-planner** (opus, quick-full/--validate) with THIS file as context →
   `.planning/quick/260612-f80-detection-only-first-responder-sync-hit-/260612-f80-PLAN.md`
   (must_haves + per-task files/action/verify/done; A+C1+B likely needs a few tasks, that's fine).
3. **plan-checker loop** (<=2 iterations).
4. **gsd-executor** (sonnet, MAIN tree — worktrees off) → implement A+C1+B + Go tests, atomic commits,
   `260612-f80-SUMMARY.md`.
5. **verifier**.
6. **web/docs/blog** update + web gate.
7. **STATE.md** "Quick Tasks Completed" row + docs commit per the gsd-quick workflow Step 7-8.

(gsd-quick workflow lives at `$HOME/.claude/gsd-core/workflows/quick.md`; this is a RESUME of SUBCMD=run.)
