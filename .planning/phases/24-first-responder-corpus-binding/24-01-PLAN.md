---
phase: 24-first-responder-corpus-binding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/corpus/reader.go
  - internal/corpus/reader_test.go
  - internal/catalog/local_overlay.go
  - internal/catalog/local_overlay_test.go
  - internal/catalog/multi.go
  - internal/catalog/multi_test.go
autonomous: true
requirements: [FRB-01, FRB-05]
must_haves:
  truths:
    - "ReadMaliciousRecords returns only TrueLabel==\"malicious\" records, latest-per-cluster"
    - "ReadMaliciousRecords returns nil (not error) for a missing corpus file and skips malformed NDJSON lines"
    - "A local catalog overlay entry can be added owner-only and survives a mock catalogs sync"
    - "A local-overlay-only match contributes source_count:1 (warn); overlay + bumblebee contributes source_count:2 (enforce)"
    - "MultiIndex.LookupAll queries the overlay index when one is loaded"
  artifacts:
    - path: "internal/corpus/reader.go"
      provides: "ReadMaliciousRecords(corpusPath) confirmed-malicious adjudication reader"
      contains: "func ReadMaliciousRecords"
    - path: "internal/catalog/local_overlay.go"
      provides: "AddLocalOverlayEntry + LoadLocalOverlay owner-only two-file overlay"
      contains: "func AddLocalOverlayEntry"
    - path: "internal/catalog/multi.go"
      provides: "MultiIndex.Overlay field + NewMultiIndexWithOverlay + LookupAll overlay query"
      contains: "func NewMultiIndexWithOverlay"
  key_links:
    - from: "internal/corpus/reader.go"
      to: "internal/corpus/adjudicator.go"
      via: "reuses clusterKeyOf + maxRecordsToScan (same package, unexported)"
      pattern: "TrueLabel == \"malicious\""
    - from: "internal/catalog/local_overlay.go"
      to: "internal/platform.SetOwnerOnly"
      via: "owner-only enforcement on local-overlay.json + local-overlay.idx"
      pattern: "platform\\.SetOwnerOnly"
    - from: "internal/catalog/multi.go"
      to: "local-overlay.idx"
      via: "NewMultiIndexWithOverlay opens overlay index via OpenIndex"
      pattern: "OpenIndex"
---

<objective>
Build the two "new construction" foundation pieces for First Responder corpus binding: the confirmed-malicious corpus reader (signal source) and the local catalog overlay (owner-only, sync-immune). This is the dependency root for the whole phase — Plans 24-02 and 24-03 consume `ReadMaliciousRecords`, `AddLocalOverlayEntry`, and the extended `MultiIndex`.

Purpose: A confirmed-malicious adjudication must become (a) a readable typed signal that First Responder can act on (FRB-01) and (b) a local-only catalog overlay entry that catches future installs without waiting for catalog lag and survives `catalogs sync` (FRB-05).
Output: `internal/corpus/reader.go` (+ test), `internal/catalog/local_overlay.go` (+ test), an additive `MultiIndex.Overlay` extension in `internal/catalog/multi.go` (+ extended test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-first-responder-corpus-binding/24-RESEARCH.md
@.planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md
@.planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification — they do not exist before this plan):
- `internal/corpus/reader.go` → `func ReadMaliciousRecords(corpusPath string) ([]CorpusRecord, error)`
- `internal/corpus/reader_test.go` (new test file)
- `internal/catalog/local_overlay.go` → `func AddLocalOverlayEntry(catalogDir string, e Entry) error`, `func LoadLocalOverlay(catalogDir string) ([]Entry, error)`
- `internal/catalog/local_overlay_test.go` (new test file)
- `internal/catalog/multi.go` → new field `MultiIndex.Overlay *Index`, new constructor `func NewMultiIndexWithOverlay(bumblebee *Index, osv, socket policy.MultiCatalogLookup, overlayPath string) *MultiIndex`
- New on-disk paths: `<catalogDir>/local-overlay.json`, `<catalogDir>/local-overlay.idx`
- New test cases added to existing `internal/catalog/multi_test.go`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 RED test skeletons for reader + local overlay</name>
  <files>internal/corpus/reader_test.go, internal/catalog/local_overlay_test.go</files>
  <read_first>
    - internal/corpus/adjudicator.go (RunAdjudicationBatch NDJSON scan loop, clusterKeyOf, maxRecordsToScan — the analog for the reader test fixtures)
    - internal/corpus/types.go (CorpusRecord, PushEnvelope, EnvelopeSignature field names: TrueLabel, AdjudicationSource, PushEnvelope.Signature.PackageOrExtensionID, PushEnvelope.Signature.Version, PushEnvelope.SourceCount, PushEnvelope.ConfidenceTier)
    - internal/catalog/index.go (BuildIndex, OpenIndex, writeFileAtomic — the overlay index build analog)
    - internal/catalog/schema.go (Entry struct fields: ID, Name, Ecosystem, Package, Versions, Severity, SourceURL, CatalogSignature, CatalogSource)
    - internal/catalog/sync.go (SyncConditional — confirm it writes ONLY bumblebee.json + bumblebee.idx; the survival test mocks this)
    - .planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md (exact go test -run names this phase must satisfy)
    - .planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md (reader.go + local_overlay.go concrete excerpts)
  </read_first>
  <action>
    Create `internal/corpus/reader_test.go` and `internal/catalog/local_overlay_test.go` as RED skeletons (Wave 0 requirement from 24-VALIDATION.md). Tests must compile and FAIL (referencing not-yet-existing functions is the intended RED state; write the tests against the signatures defined in Task 2/3 so they compile after those functions land, and use `t.Skip` ONLY if a referenced symbol does not yet exist — prefer authoring the function stubs returning a sentinel so tests fail rather than skip).

    In `internal/corpus/reader_test.go` (package corpus) author these test functions with table fixtures built from CorpusRecord literals:
    - `TestReadMaliciousRecords` — write an NDJSON corpus temp file with a mix of TrueLabel values ("malicious", "benign", "unresolved", "policy_correct"); assert ReadMaliciousRecords returns ONLY the malicious ones. Include one record whose latest superseding line flips the label (same ClusterID written twice, last line wins) and assert the latest wins. Include a missing-file case: ReadMaliciousRecords on a non-existent path returns (nil, nil). Include a malformed-line case: an NDJSON file with one garbage line plus one valid malicious line returns the valid record and no error (malformed line skipped). Include a redaction-safety assertion: no secret-shaped field appears in the returned record (the corpus is already redacted at write time; assert the reader does not introduce one).
    - `TestReadMaliciousRecordsLatestPerCluster` — two lines, same ClusterID: first unresolved, second malicious; assert exactly one returned record and it is malicious.

    In `internal/catalog/local_overlay_test.go` (package catalog) author these test functions; align names EXACTLY with 24-VALIDATION.md:
    - `TestLocalOverlaySurvivesSync` — call AddLocalOverlayEntry, then simulate a sync by writing only `bumblebee.json` + `bumblebee.idx` into the same catalogDir (mirror what SyncConditional touches); assert `local-overlay.json` and `local-overlay.idx` are byte-unchanged afterward.
    - `TestLocalOverlayFilePermissions` — after AddLocalOverlayEntry, on non-Windows assert `os.Stat` mode is 0600 for both overlay files; on Windows skip the POSIX mode check with a structured reason ("Windows owner-DACL not a POSIX mode; SetOwnerOnly covers it") since SetOwnerOnly applies a DACL rather than a numeric mode.
    - `TestMultiIndexQueriesOverlay` — build an overlay with one entry (ecosystem "npm", package "@nrwl/nx-console"), construct a MultiIndex via NewMultiIndexWithOverlay with that overlay path and a nil bumblebee, assert LookupAll("npm","@nrwl/nx-console") returns >= 1 match with CatalogSource=="local-overlay".
    - `TestLocalOverlayUnsignedIsWarnTier` — assert the overlay Entry written has CatalogSignature=="" (unsigned → warn-only); document that a single unsigned overlay source yields source_count:1.
    - `TestLocalOverlayPlusBumblebeeIsEnforce` — build both a bumblebee index AND an overlay containing the same package; assert LookupAll returns matches from BOTH "bumblebee" and "local-overlay" CatalogSource names (the corroboration engine later counts these as 2 distinct sources → enforce). This test asserts the two-source shape at the MultiIndex layer; the actual tier derivation is policy-engine territory and is covered by the evaluator gate in 24-03.
    - `TestLocalOverlayIdempotentAdd` — call AddLocalOverlayEntry twice with the same ecosystem+package; assert LoadLocalOverlay returns exactly one entry.

    Run `go test ./internal/corpus/... ./internal/catalog/... -run 'TestReadMaliciousRecords|TestLocalOverlay|TestMultiIndexQueriesOverlay'` and confirm the suite FAILS (RED) because the production functions do not yet exist. Commit the RED skeletons.

    Do NOT inline implementation in this task. Do NOT import internal/tui or internal/check from either test file.
  </action>
  <verify>
    <automated>go vet ./internal/corpus/... ./internal/catalog/... 2>&1 | head -20 (tests compile); then `go test ./internal/corpus/... ./internal/catalog/... -run 'TestReadMaliciousRecords|TestLocalOverlay|TestMultiIndexQueriesOverlay' 2>&1` is expected to FAIL/RED before Tasks 2-3</automated>
  </verify>
  <acceptance_criteria>
    - `internal/corpus/reader_test.go` exists and contains `func TestReadMaliciousRecords` and `func TestReadMaliciousRecordsLatestPerCluster`.
    - `internal/catalog/local_overlay_test.go` exists and contains `TestLocalOverlaySurvivesSync`, `TestLocalOverlayFilePermissions`, `TestMultiIndexQueriesOverlay`, `TestLocalOverlayUnsignedIsWarnTier`, `TestLocalOverlayPlusBumblebeeIsEnforce`, `TestLocalOverlayIdempotentAdd`.
    - Neither test file imports `internal/tui` or `internal/check`.
    - The named tests FAIL (RED) prior to Tasks 2-3 implementation.
  </acceptance_criteria>
  <done>Both test files exist, compile against the planned signatures, and the FRB test functions are present and RED. Committed as `test(24-01): ...`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReadMaliciousRecords — confirmed-malicious corpus reader (FRB-01 signal source)</name>
  <files>internal/corpus/reader.go</files>
  <read_first>
    - internal/corpus/adjudicator.go (RunAdjudicationBatch lines ~253-302: bufio scanner, 1MB Buffer, maxRecordsToScan cap, malformed-line skip via continue, latest-per-cluster collapse via clusterKeyOf, missing-file → nil)
    - internal/corpus/reader_test.go (the RED tests authored in Task 1 — the contract)
    - .planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md (reader.go excerpt — exact scan + collapse + filter pattern)
  </read_first>
  <behavior>
    - Returns nil, nil when corpusPath does not exist (os.IsNotExist) — no corpus yet is not an error.
    - Skips malformed JSON lines (json.Unmarshal error → continue), never aborts the read.
    - Collapses to the latest record per ClusterID using the existing unexported clusterKeyOf helper (last NDJSON line wins).
    - Filters the collapsed set to TrueLabel == "malicious" only; returns that slice.
    - Honors the existing maxRecordsToScan cap (50000) and 1MB scanner buffer to match RunAdjudicationBatch.
  </behavior>
  <action>
    Create `internal/corpus/reader.go` in `package corpus`. Implement `func ReadMaliciousRecords(corpusPath string) ([]CorpusRecord, error)`.

    Imports: stdlib only — `bufio`, `encoding/json`, `fmt`, `os`. NO internal imports (CorpusRecord, clusterKeyOf, maxRecordsToScan all live in the same package). MUST NOT import internal/tui or internal/check (Pitfall 1 / ADJ-01).

    Reuse the same NDJSON scan logic as `RunAdjudicationBatch`: open the file (missing → return nil, nil), bufio.NewScanner with a 1<<20 buffer, cap iterations at `maxRecordsToScan`, skip empty lines, json.Unmarshal each line into a CorpusRecord and `continue` on error (malformed-line skip), accumulate into a slice. After scanning, `f.Close()` explicitly (Windows cannot rename open-for-read files) before further work. Build a `map[string]CorpusRecord` keyed by `clusterKeyOf(rec)` (skip empty keys), last-write-wins. Then iterate the map and append only records where `rec.TrueLabel == "malicious"` to the output. Return the output slice.

    There is NO context parameter — this is a fast read of an already-small drained file. It MUST be callable only from off-hot-path code (runCatalogsSync, RunFirstResponder); never from handler.go (Pitfall 5). Add a doc comment stating this constraint.

    Run the Task-1 reader tests and confirm they pass GREEN. Commit.
  </action>
  <verify>
    <automated>go test ./internal/corpus/... -run 'TestReadMaliciousRecords|TestReadMaliciousRecordsLatestPerCluster' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/corpus/... -run TestReadMaliciousRecords` passes.
    - `go test ./internal/corpus/... -run TestReadMaliciousRecordsLatestPerCluster` passes.
    - `reader.go` imports only stdlib (`bufio`, `encoding/json`, `fmt`, `os`) — verify no `internal/tui`, `internal/check`, or `internal/policy` import via `grep -n 'internal/' internal/corpus/reader.go` returning no matches.
    - Missing-file path returns `(nil, nil)`; malformed-line path returns the valid record with no error (asserted by Task-1 tests).
  </acceptance_criteria>
  <done>ReadMaliciousRecords compiles, the two reader tests pass GREEN, and the file has no forbidden imports. Committed as `feat(24-01): ...`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Local catalog overlay + MultiIndex overlay extension (FRB-05)</name>
  <files>internal/catalog/local_overlay.go, internal/catalog/multi.go, internal/catalog/multi_test.go</files>
  <read_first>
    - internal/catalog/index.go (BuildIndex, OpenIndex, writeFileAtomic — same-package reuse; the overlay rebuilds local-overlay.idx via BuildIndex)
    - internal/catalog/multi.go (existing MultiIndex struct, NewMultiIndex, LookupAll loop with bumblebeeMultiAdapter + Dissented sentinel, Close — the additive extension target)
    - internal/catalog/schema.go (Entry fields)
    - internal/corpus/store.go (NewStoreSink lines ~51-75: MkdirAll 0o700 + OpenFile 0o600 + platform.SetOwnerOnly — the owner-only pattern)
    - internal/catalog/local_overlay_test.go (the RED contract from Task 1)
    - .planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md (local_overlay.go + multi.go concrete excerpts)
  </read_first>
  <behavior>
    - LoadLocalOverlay(catalogDir) reads <catalogDir>/local-overlay.json; missing file → (nil, nil); malformed JSON → error.
    - AddLocalOverlayEntry(catalogDir, e) is idempotent on ecosystem+package (EqualFold) — a duplicate add does not grow the file.
    - AddLocalOverlayEntry writes local-overlay.json atomically (writeFileAtomic), enforces SetOwnerOnly, then rebuilds local-overlay.idx via BuildIndex and enforces SetOwnerOnly on it.
    - AddLocalOverlayEntry caps overlay growth at maxOverlayEntries=1000; at the cap it logs a warning and returns nil (no error).
    - NewMultiIndexWithOverlay opens local-overlay.idx via OpenIndex; on open error the Overlay stays nil (silently degraded — no overlay match is no block, fail-closed-consistent).
    - MultiIndex.LookupAll queries Overlay after Bumblebee with CatalogSource forced to "local-overlay"; no Dissented sentinel for the overlay (it is optional, not a configured source).
    - MultiIndex.Close closes Overlay if non-nil.
  </behavior>
  <action>
    Create `internal/catalog/local_overlay.go` in `package catalog`. Implement:
    - `func LoadLocalOverlay(catalogDir string) ([]Entry, error)` — `filepath.Join(catalogDir, "local-overlay.json")`, os.ReadFile, missing → (nil, nil), json.Unmarshal into []Entry, parse error wrapped.
    - `func AddLocalOverlayEntry(catalogDir string, e Entry) error` — LoadLocalOverlay, idempotency check via `strings.EqualFold` on Ecosystem AND Package (return nil if present), cap guard `const maxOverlayEntries = 1000` (log + return nil at cap), append, marshal, `writeFileAtomic(<catalogDir>/local-overlay.json, data)`, `platform.SetOwnerOnly(jsonPath)`, `BuildIndex(<catalogDir>/local-overlay.idx, entries)`, `platform.SetOwnerOnly(idxPath)`. `writeFileAtomic` and `BuildIndex` are same-package — call directly, no export.

    Imports for local_overlay.go: `encoding/json`, `fmt`, `log`, `os`, `path/filepath`, `strings`, and `github.com/bantuson/beekeeper/internal/platform`. NO internal/tui, NO internal/corpus.

    Modify `internal/catalog/multi.go` additively (non-breaking):
    - Add field `Overlay *Index` to the `MultiIndex` struct (between Bumblebee and OSV).
    - Add `func NewMultiIndexWithOverlay(bumblebee *Index, osv, socket policy.MultiCatalogLookup, overlayPath string) *MultiIndex` — calls existing NewMultiIndex(bumblebee, osv, socket); if overlayPath != "" and `OpenIndex(overlayPath)` succeeds, set m.Overlay; on error leave Overlay nil. Keep existing `NewMultiIndex` UNCHANGED as the thin no-overlay constructor (mirrors the Phase-23 NewMultiSinkWithCorpus pattern; do not break existing callers in catalogs_daemon.go).
    - In `LookupAll`, after the Bumblebee block and before OSV, add an overlay query: if `m.Overlay != nil`, wrap it with the existing `bumblebeeMultiAdapter{idx: m.Overlay}`, call `adapter.LookupAll(ecosystem, pkg)`, and for each returned match set `got[i].CatalogSource = "local-overlay"` before appending. Do NOT emit a Dissented sentinel for the overlay.
    - In `Close`, close `m.Overlay` (if non-nil) in addition to Bumblebee.
    - Add a `TestMultiIndexOverlayLookup` case to `internal/catalog/multi_test.go` if the existing `TestMultiIndexQueriesOverlay` (authored in Task 1's local_overlay_test.go) does not already cover the multi.go-side path — otherwise leave multi_test.go untouched and rely on the Task-1 test. (multi_test.go is listed in files_modified to permit this; modify only if needed.)

    The overlay Entry MUST always carry `CatalogSignature = ""` (Pitfall 3 — unsigned → warn-only) and `CatalogSource = "local-overlay"`. The construction of the Entry from a CorpusRecord happens in 24-03 (buildOverlayEntry); this plan only provides the storage + lookup primitives and tests them with hand-built Entry fixtures.

    Run the Task-1 overlay tests and confirm GREEN. Commit.
  </action>
  <verify>
    <automated>go test ./internal/catalog/... -run 'TestLocalOverlaySurvivesSync|TestLocalOverlayFilePermissions|TestMultiIndexQueriesOverlay|TestLocalOverlayUnsignedIsWarnTier|TestLocalOverlayPlusBumblebeeIsEnforce|TestLocalOverlayIdempotentAdd' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All six overlay/multi tests from Task 1 pass GREEN.
    - `grep -n 'NewMultiIndex(' internal/catalog/multi.go cmd/beekeeper/catalogs_daemon.go` shows the existing NewMultiIndex signature is unchanged (no caller breakage).
    - `grep -n 'CatalogSignature' internal/catalog/local_overlay_test.go` confirms an overlay entry asserts empty CatalogSignature.
    - `grep -rn 'internal/tui\|internal/corpus' internal/catalog/local_overlay.go` returns no matches (no forbidden imports).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>local_overlay.go + the multi.go additive extension compile, all six overlay tests pass, NewMultiIndex is unchanged, and overlay files are owner-only. Committed as `feat(24-01): ...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → StateDir/CatalogDir | An untrusted agent process should not read or write the corpus NDJSON or the local-overlay.* files (both under StateDir; covered by self-protection EvaluateSelfPath). |
| corpus NDJSON → reader | A possibly-corrupted append-only file crosses into ReadMaliciousRecords; malformed input must not crash the reader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-OVR-TAMPER | Tampering | local-overlay.json / local-overlay.idx | mitigate | Both files created via writeFileAtomic + `platform.SetOwnerOnly` (0600 / Windows owner-DACL); they live under CatalogDir inside StateDir, which `EvaluateSelfPath` self-protection already blocks agents from writing. Verified by `TestLocalOverlayFilePermissions`. |
| T-24-OVR-POISON | Tampering (poisoning) | overlay entry for a safe package | mitigate | Overlay entries always have `CatalogSignature=""` → unsigned → warn-only per CTLG-07. A single overlay source yields source_count:1; enforce requires a second independent signed source. Local-only, never fleet-pushed. Verified by `TestLocalOverlayUnsignedIsWarnTier` + `TestLocalOverlayPlusBumblebeeIsEnforce`. |
| T-24-OVR-DOS | Denial of Service | ReadMaliciousRecords on corrupted/huge corpus NDJSON | mitigate | Malformed lines skipped (continue), missing file → nil, scan capped at maxRecordsToScan=50000 with a 1MB line buffer (same bounds as RunAdjudicationBatch). Verified by the malformed-line + missing-file cases in `TestReadMaliciousRecords`. |
| T-24-OVR-RACE | Tampering (integrity) | concurrent catalogs sync writing the overlay | accept | writeFileAtomic for the JSON + BuildIndex's internal atomic rename bound the corruption window to last-writer-wins (same race class accepted for state.json). Documented as a known v1 gap; no file lock added (local-only, no fleet push). |
| T-24-SC | Tampering | go.mod / new dependencies | mitigate | Zero new dependencies — stdlib + existing internal packages only. Verified by `go mod tidy && git diff --exit-code go.mod`. |
</threat_model>

<verification>
- `go test ./internal/corpus/... ./internal/catalog/... -count=1` — all green after Task 3.
- `go build ./...` exits 0; `go vet ./internal/corpus/... ./internal/catalog/...` exits 0.
- `go mod tidy && git diff --exit-code go.mod` — NO CHANGE (zero new deps).
- `grep -rn 'internal/tui\|internal/check' internal/corpus/reader.go internal/catalog/local_overlay.go` — 0 matches (import boundary held).
- Existing NewMultiIndex callers (catalogs_daemon.go) still compile — additive extension only.
</verification>

<success_criteria>
- ReadMaliciousRecords returns only malicious, latest-per-cluster records; nil for missing file; skips malformed lines (FRB-01 signal source).
- AddLocalOverlayEntry writes owner-only local-overlay.json + local-overlay.idx, idempotent, capped; LoadLocalOverlay reads them back (FRB-05 storage).
- The local overlay survives a mock SyncConditional (only bumblebee.* clobbered) (FRB-05 survival).
- MultiIndex.LookupAll returns overlay matches with CatalogSource "local-overlay"; unsigned overlay = source_count:1, overlay+bumblebee = two distinct sources (FRB-05 corroboration shape).
</success_criteria>

<output>
Create `.planning/phases/24-first-responder-corpus-binding/24-01-SUMMARY.md` when done.
</output>
