---
phase: 24-first-responder-corpus-binding
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - internal/watch/firstresponder.go
  - internal/watch/firstresponder_test.go
  - internal/watch/nopurge_test.go
autonomous: true
requirements: [FRB-01, FRB-02, FRB-04]
must_haves:
  truths:
    - "A confirmed-malicious corpus record matched to a local install writes a catalog_quarantine audit record (arms the TUI card)"
    - "A confirmed-malicious corpus record with no local install writes a pending-quarantine audit record"
    - "A corpus record elevates a Sentry watch target ONLY when SourceCount >= CorpusSentryThreshold (default 2); a single-source (watch tier) record does NOT"
    - "The corpus path only ever calls quarantine.MoveTyped (reversible) — never quarantine.Purge"
  artifacts:
    - path: "internal/watch/firstresponder.go"
      provides: "FirstResponderConfig corpus fields + RunFirstResponder corpus loop + parsePackageID helper"
      contains: "CorpusSentryThreshold"
    - path: "internal/watch/firstresponder_test.go"
      provides: "corpus-adjudication test cases (arm card / sentry gate / single-source no-sentry / no-purge / pending)"
      contains: "TestFirstResponderCorpusMaliciousArmsCard"
    - path: "internal/watch/nopurge_test.go"
      provides: "static FRB-02 no-purge gate as a Go test"
      contains: "TestCorpusPathHasNoPurgeCall"
  key_links:
    - from: "internal/watch/firstresponder.go"
      to: "internal/corpus.ReadMaliciousRecords"
      via: "RunFirstResponder reads malicious adjudications when CorpusEnabled"
      pattern: "corpus\\.ReadMaliciousRecords"
    - from: "internal/watch/firstresponder.go"
      to: "internal/sentry.TargetList.AddTarget"
      via: "SourceCount >= CorpusSentryThreshold gate (FRB-04)"
      pattern: "SourceCount >= "
    - from: "internal/watch/firstresponder.go"
      to: "internal/quarantine.MoveTyped"
      via: "reversible quarantine move only — never Purge (FRB-02)"
      pattern: "quarantine\\.MoveTyped"
---

<objective>
Wire the confirmed-malicious corpus signal (from 24-01's `ReadMaliciousRecords`) into the existing First Responder seam: arm the TUI quarantine card (FRB-01), elevate a detection-only Sentry watch gated at corroboration >= threshold (FRB-04), keep restore available and the red/coral semantic preserved by reusing the same reversible quarantine path (FRB-03 groundwork), and NEVER introduce an automatic purge (FRB-02).

Purpose: The single behavioral heart of the phase — a confirmed adjudication must produce exactly the same arming + watch effects the scan-hit path already produces, with the FRB-04 source_count gate, and with a hard guarantee that no purge is reachable from the corpus path.
Output: `internal/watch/firstresponder.go` extended (FirstResponderConfig corpus fields, corpus loop in RunFirstResponder, parsePackageID helper); `internal/watch/firstresponder_test.go` extended with five corpus test cases; `internal/watch/nopurge_test.go` static FRB-02 gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-first-responder-corpus-binding/24-RESEARCH.md
@.planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md
@.planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md
@.planning/phases/24-first-responder-corpus-binding/24-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- `internal/watch/firstresponder.go` → new FirstResponderConfig fields `CorpusPath string`, `CorpusEnabled bool`, `CorpusSentryThreshold int`; new unexported helper `func parsePackageID(id string) (ecosystem, pkg string)`; new corpus-processing loop inside `RunFirstResponder`; new audit helper variant (e.g. `writeCorpusFirstResponderAudit`) if needed for the corpus path
- New test functions in `internal/watch/firstresponder_test.go`: `TestFirstResponderCorpusMaliciousArmsCard`, `TestFirstResponderCorpusSentryGate`, `TestFirstResponderCorpusSingleSourceNoSentry`, `TestFirstResponderCorpusNoPurge`, `TestFirstResponderCorpusPendingQuarantine`
- New test file `internal/watch/nopurge_test.go` → `TestCorpusPathHasNoPurgeCall`
- New audit record reuse: the existing `"catalog_quarantine"` and `"pending-quarantine"` record types are reused for the corpus path (no new record type introduced)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 RED corpus test cases in firstresponder_test.go</name>
  <files>internal/watch/firstresponder_test.go</files>
  <read_first>
    - internal/watch/firstresponder.go (RunFirstResponder body, FirstResponderConfig, the F-4 Sentry gate at lines ~113-124, the MoveTyped + writeFirstResponderAudit "catalog_quarantine" path at lines ~143-167, the "pending-quarantine" path at lines ~137-140, SaveTargets at lines ~171-175, CrossRefFn injection seam)
    - internal/watch/crossref.go (ScanHit fields: Package, Ecosystem, Version, InstalledPath, PathResolved, CorroborationCount, Decision; CrossRefConfig)
    - internal/sentry/targets.go (TargetList, AddTarget, LoadTargets, SaveTargets — DETECTION-ONLY comment)
    - internal/corpus/types.go (CorpusRecord, PushEnvelope.SourceCount, PushEnvelope.ConfidenceTier, PushEnvelope.Signature.PackageOrExtensionID, PushEnvelope.Signature.Version, TrueLabel)
    - internal/corpus/reader.go (ReadMaliciousRecords — the 24-01 signal source)
    - .planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md (exact go test -run names)
    - .planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md (firstresponder.go extension excerpts, parsePackageID excerpt)
  </read_first>
  <action>
    Extend `internal/watch/firstresponder_test.go` with five RED test functions (names aligned EXACTLY with 24-VALIDATION.md). Each test seeds a temp corpus NDJSON file via the same write pattern the existing tests use, injects a fake CrossRefFn returning controlled ScanHit values, runs RunFirstResponder with a FirstResponderConfig that sets CorpusPath + CorpusEnabled=true + CorpusSentryThreshold=2, and asserts on the audit log + sentry-targets.json.

    - `TestFirstResponderCorpusMaliciousArmsCard` — corpus has one malicious enforce-tier record for "npm:@nrwl/nx-console" v17.3.0; fake CrossRefFn returns a matching ScanHit (Ecosystem "npm", Package "@nrwl/nx-console", PathResolved=true, InstalledPath set). Assert the audit log gains a record with RecordType=="catalog_quarantine" referencing the package, AND the quarantine dir gains one entry (the card is armed).
    - `TestFirstResponderCorpusSentryGate` — corpus has one malicious record with SourceCount=2; CorpusSentryThreshold=2. Assert sentry-targets.json contains a target named "@nrwl/nx-console".
    - `TestFirstResponderCorpusSingleSourceNoSentry` — corpus has one malicious record with SourceCount=1 (watch tier); CorpusSentryThreshold=2. Assert sentry-targets.json does NOT contain that package (single-source does not tighten Sentry — FRB-04).
    - `TestFirstResponderCorpusNoPurge` — drive the full corpus path; assert NO purge occurred (the quarantine entry is still present and reversible — i.e. the artifact was MoveTyped'd, not deleted). This is the behavioral half of the FRB-02 guard (the static grep half is a separate verification gate).
    - `TestFirstResponderCorpusPendingQuarantine` — corpus has one malicious record whose package is NOT returned by the fake CrossRefFn (no local install). Assert the audit log gains a RecordType=="pending-quarantine" record and the quarantine dir is empty (nothing to move).

    Author the tests against the Task-2 signatures (CorpusPath/CorpusEnabled/CorpusSentryThreshold fields). Run `go test ./internal/watch/... -run TestFirstResponderCorpus` and confirm RED (fields/loop not implemented yet). Commit RED skeletons.

    Do NOT import internal/tui. The test reads audit records via the existing audit reader helpers already used in firstresponder_test.go.
  </action>
  <verify>
    <automated>go vet ./internal/watch/... 2>&1 | head -20 (compiles); `go test ./internal/watch/... -run TestFirstResponderCorpus 2>&1` expected RED before Task 2</automated>
  </verify>
  <acceptance_criteria>
    - `internal/watch/firstresponder_test.go` contains all five `TestFirstResponderCorpus*` functions named per 24-VALIDATION.md.
    - The test file does not import `internal/tui`.
    - The five tests FAIL (RED) before Task 2.
  </acceptance_criteria>
  <done>Five corpus test cases added, compile against planned signatures, and are RED. Committed as `test(24-02): ...`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend RunFirstResponder with the corpus loop (FRB-01/02/04)</name>
  <files>internal/watch/firstresponder.go</files>
  <read_first>
    - internal/watch/firstresponder.go (full file — the extension target; reuse ecosystemToProcess, writeFirstResponderAudit, the targets Load/Save lifecycle, the MoveTyped + Manifest construction)
    - internal/watch/firstresponder_test.go (the five RED tests from Task 1 — the contract)
    - internal/corpus/reader.go (ReadMaliciousRecords signature from 24-01)
    - internal/corpus/types.go (CorpusRecord / PushEnvelope field access)
    - internal/quarantine/quarantine.go (MoveTyped, Manifest, ArtifactTypeLanguagePackage, ArtifactTypeEditorExtension — confirm Purge is NOT referenced anywhere in the corpus path)
    - .planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md (parsePackageID excerpt + corpus loop shape)
  </read_first>
  <behavior>
    - FirstResponderConfig gains CorpusPath string, CorpusEnabled bool, CorpusSentryThreshold int.
    - When CorpusEnabled && CorpusPath != "", RunFirstResponder calls corpus.ReadMaliciousRecords(cfg.CorpusPath) after the existing CrossReference call; a read error is logged and the scan-hit path is unaffected (fail-closed: corpus failure never breaks scan-hit quarantine).
    - For each malicious record: parse ecosystem+package from PushEnvelope.Signature.PackageOrExtensionID via parsePackageID; match it against the CrossReference ScanHit set by ecosystem+package to resolve InstalledPath.
    - FRB-04 gate: if record.PushEnvelope != nil && record.PushEnvelope.SourceCount >= cfg.CorpusSentryThreshold (default 2 when <= 0), call targets.AddTarget(pkg, installedPath, ecosystemToProcess(ecosystem)). A SourceCount of 1 does NOT add a target.
    - FRB-01 arm: if a matching ScanHit with a resolved path exists, call quarantine.MoveTyped (reversible) and write a "catalog_quarantine" audit record; else write a "pending-quarantine" audit record (no move).
    - FRB-02: the corpus path calls quarantine.MoveTyped ONLY — it MUST NOT call quarantine.Purge anywhere.
    - The corpus loop reuses the same targets Load-at-top / Save-at-bottom lifecycle as the scan-hit loop (one SaveTargets call covers both paths).
  </behavior>
  <action>
    Modify `internal/watch/firstresponder.go`:

    1. Add three fields to `FirstResponderConfig`: `CorpusPath string`, `CorpusEnabled bool`, `CorpusSentryThreshold int` (doc: minimum PushEnvelope.SourceCount to elevate a Sentry watch; default 2 = enforce tier).
    2. Add the import `github.com/bantuson/beekeeper/internal/corpus` to the existing import block (quarantine, sentry, audit, policy already present).
    3. Add an unexported helper `func parsePackageID(id string) (ecosystem, pkg string)` that splits on the FIRST ':' so "npm:@nrwl/nx-console" → ("npm","@nrwl/nx-console") and a colon-less id → ("", id). Scoped npm names (containing '@' and '/') must survive intact.
    4. Inside RunFirstResponder, AFTER the existing scan-hit `for _, hit := range hits` loop and BEFORE the final SaveTargets, add a corpus block guarded by `if cfg.CorpusEnabled && cfg.CorpusPath != ""`:
       - `malicious, rdErr := corpus.ReadMaliciousRecords(cfg.CorpusPath)`; on error `log.Printf(...)` and skip the corpus block (do not return — scan-hit results already computed must persist).
       - Resolve the corpus threshold: `corpusThreshold := cfg.CorpusSentryThreshold; if corpusThreshold <= 0 { corpusThreshold = 2 }`.
       - Build a lookup of the existing `hits` keyed by lowercased ecosystem+"\x00"+package for O(1) install matching.
       - For each malicious record: skip if `rec.PushEnvelope == nil || rec.PushEnvelope.Signature.PackageOrExtensionID == ""`. Parse ecosystem+pkg via parsePackageID. Look up a matching ScanHit to get InstalledPath/PathResolved.
       - FRB-04 Sentry gate: `if targets != nil && rec.PushEnvelope.SourceCount >= corpusThreshold { targets.AddTarget(pkg, installedPath, ecosystemToProcess(ecosystem)) }` (installedPath may be "" when not locally installed — AddTarget tolerates an empty path).
       - FRB-01 arm: if a matching ScanHit with PathResolved && InstalledPath != "": construct a quarantine.Manifest (Publisher=ecosystem, Name=pkg, Version=rec.PushEnvelope.Signature.Version, OriginalPath=installedPath, ArtifactType per ecosystem, Reason="corpus adjudication: confirmed malicious", RuleIDs=["FRSP-02"]), call `quarantine.MoveTyped(cfg.QuarantineDir, installedPath, m)`; on error log + write a "quarantine_error" audit record + continue; on success write a "catalog_quarantine" audit record. Else (no resolved install): write a "pending-quarantine" audit record.
       - Use the existing writeFirstResponderAudit signature where it fits; if the corpus path needs to record package/version/ecosystem that ScanHit does not carry (the pending case has no ScanHit), add a small sibling helper `writeCorpusFirstResponderAudit(auditPath, recordType, ecosystem, pkg, version string)` that mirrors writeFirstResponderAudit's redact-before-write discipline (audit.RedactRecord with DefaultRedactPatterns, RuleID "FRSP-02", RecordType set). Reuse audit.FromDecision/NewWriter exactly as the existing helper does.

    5. Do NOT touch the scan-hit loop behavior. Do NOT call quarantine.Purge anywhere. Do NOT import internal/tui.

    Run the Task-1 corpus tests; iterate to GREEN. Commit.
  </action>
  <verify>
    <automated>go test ./internal/watch/... -run TestFirstResponderCorpus -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/watch/... -run TestFirstResponderCorpusMaliciousArmsCard` passes (catalog_quarantine audit record + armed quarantine entry).
    - `go test ./internal/watch/... -run TestFirstResponderCorpusSentryGate` passes (SourceCount=2 → target added).
    - `go test ./internal/watch/... -run TestFirstResponderCorpusSingleSourceNoSentry` passes (SourceCount=1 → no target).
    - `go test ./internal/watch/... -run TestFirstResponderCorpusPendingQuarantine` passes (no install → pending-quarantine, no move).
    - `go test ./internal/watch/... -run TestFirstResponderCorpusNoPurge` passes (entry quarantined reversibly, not purged).
    - `grep -n 'Purge(' internal/watch/firstresponder.go` returns 0 matches.
    - `grep -n 'internal/tui' internal/watch/firstresponder.go` returns 0 matches.
    - Existing scan-hit tests in firstresponder_test.go still pass: `go test ./internal/watch/... -count=1` exits 0.
  </acceptance_criteria>
  <done>RunFirstResponder processes corpus malicious records, all five corpus tests + existing watch tests pass, no Purge reachable from the corpus path. Committed as `feat(24-02): ...`.</done>
</task>

<task type="auto">
  <name>Task 3: FRB-02 static no-purge gate + full watch-package verification</name>
  <files>internal/watch/nopurge_test.go</files>
  <read_first>
    - internal/watch/firstresponder.go (confirm final state has no Purge reference)
    - internal/corpus/reader.go (confirm no Purge reference)
    - .planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md (FRB-02 grep gate definition)
  </read_first>
  <action>
    Add the FRB-02 static guard as a Go test (so it runs in `go test` and CI, not just an ad-hoc shell grep). Create `internal/watch/nopurge_test.go` (package watch) with `TestCorpusPathHasNoPurgeCall`: it reads the source of `internal/watch/firstresponder.go` and `internal/corpus/reader.go` via `os.ReadFile` and asserts neither contains the substring `quarantine.Purge(` nor a bare `.Purge(` on the corpus code path. Strip comment lines (lines whose trimmed prefix is `//`) before the substring check so a doc-comment mention of "Purge" does not trip the gate (mirror the project's grep-hygiene convention — never count comments).

    Then run the full watch package plus the corpus + catalog packages to confirm no regression from 24-01/24-02:
    - `go test ./internal/watch/... ./internal/corpus/... ./internal/catalog/... -count=1`
    - `go build ./...`
    - `go vet ./internal/watch/...`
    - `go mod tidy && git diff --exit-code go.mod` (zero new deps)

    Commit.
  </action>
  <verify>
    <automated>go test ./internal/watch/... -run TestCorpusPathHasNoPurgeCall -count=1 && go test ./internal/watch/... ./internal/corpus/... ./internal/catalog/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/watch/nopurge_test.go` exists with `TestCorpusPathHasNoPurgeCall`, and it passes (no `.Purge(` outside comments in firstresponder.go or reader.go).
    - `go test ./internal/watch/... ./internal/corpus/... ./internal/catalog/... -count=1` exits 0.
    - `go build ./...` exits 0; `go mod tidy && git diff --exit-code go.mod` reports NO CHANGE.
  </acceptance_criteria>
  <done>The FRB-02 static no-purge gate is a passing Go test, the watch/corpus/catalog suites are green, and no new dependencies were added. Committed as `test(24-02): ...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus adjudication → physical artifact removal | The only path from a corpus signal to artifact removal must be the reversible MoveTyped; irreversible Purge must remain human-keyboard-gated in the TUI, never reachable from the batch/adjudication path. |
| corpus record fields → audit log | Attacker-influenced strings (package id, version, reason) cross into the audit log; they must be redacted before write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-NOPURGE | Tampering / Blast Radius | RunFirstResponder corpus loop | mitigate | The corpus loop calls `quarantine.MoveTyped` (reversible) ONLY; `quarantine.Purge` is never referenced in the corpus code path. Enforced behaviorally by `TestFirstResponderCorpusNoPurge` and statically by `TestCorpusPathHasNoPurgeCall` (FRB-02). |
| T-24-SENTRY-SINGLESRC | Spoofing / Elevation | Sentry watch elevation | mitigate | A Sentry target is added only when `PushEnvelope.SourceCount >= CorpusSentryThreshold` (default 2 = enforce). A single compromised source (SourceCount=1) cannot tighten detection on a legitimate package. The watch is DETECTION-ONLY (no kill/isolate). Verified by `TestFirstResponderCorpusSentryGate` + `TestFirstResponderCorpusSingleSourceNoSentry` (FRB-04). |
| T-24-AUDIT-REDACT | Information Disclosure | corpus audit write | mitigate | The corpus audit helper redacts via `audit.RedactRecord(rec, audit.DefaultRedactPatterns())` before write, matching the existing writeFirstResponderAudit discipline (F-1 lesson). |
| T-24-CORPUS-FAILOPEN | Denial of Service | ReadMaliciousRecords error in RunFirstResponder | accept | A corpus read error is logged and the corpus block is skipped; the already-computed scan-hit quarantine results still persist (the corpus is an additive signal, not the primary gate). Fail-closed on the primary scan-hit path is unchanged. |
| T-24-SC | Tampering | go.mod / new dependencies | mitigate | Zero new dependencies. Verified by `go mod tidy && git diff --exit-code go.mod`. |
</threat_model>

<verification>
- `go test ./internal/watch/... -run TestFirstResponderCorpus -count=1` — all five corpus cases green.
- `go test ./internal/watch/... -run TestCorpusPathHasNoPurgeCall` — FRB-02 static gate green.
- `go test ./internal/watch/... ./internal/corpus/... ./internal/catalog/... -count=1` — no regression.
- `grep -n 'Purge(' internal/watch/firstresponder.go` — 0 matches (manual confirmation of FRB-02).
- `go build ./...` / `go vet ./internal/watch/...` exit 0; `go mod tidy` leaves go.mod unchanged.
</verification>

<success_criteria>
- Confirmed-malicious corpus record + matching local install → catalog_quarantine audit record + armed quarantine entry (FRB-01).
- No matching install → pending-quarantine audit record, no move (FRB-01 pending).
- SourceCount >= 2 → Sentry target added; SourceCount == 1 → not added (FRB-04, detection-only).
- No quarantine.Purge reachable from the corpus path — behavioral + static gates both green (FRB-02).
- Quarantine entries from the corpus path are MoveTyped (reversible), physically identical to scan-hit entries, restorable (FRB-03 groundwork; restore proven in 24-03).
</success_criteria>

<output>
Create `.planning/phases/24-first-responder-corpus-binding/24-02-SUMMARY.md` when done.
</output>
