---
phase: 24-first-responder-corpus-binding
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - cmd/beekeeper/catalogs_daemon.go
  - cmd/beekeeper/catalogs_daemon_test.go
  - internal/quarantine/quarantine_test.go
autonomous: false
requirements: [FRB-01, FRB-02, FRB-03, FRB-04, FRB-05]
must_haves:
  truths:
    - "runCatalogsSync, after RunAdjudicationBatch, runs the first-responder corpus pass and adds a local overlay entry per malicious record — all non-fatal (sync continues on error)"
    - "Restore reverses a corpus-adjudication quarantine entry cleanly (the entry is physically identical to a scan-hit entry)"
    - "The synthetic Nx Console round-trip arms the card, adds the Sentry target, adds the local overlay, never purges, and is restorable — end to end"
  artifacts:
    - path: "cmd/beekeeper/catalogs_daemon.go"
      provides: "runCatalogsSync FRB wiring: firstResponderFn + ReadMaliciousRecords + AddLocalOverlayEntry + buildOverlayEntry"
      contains: "AddLocalOverlayEntry"
    - path: "cmd/beekeeper/catalogs_daemon_test.go"
      provides: "TestRunCatalogsSyncFirstResponder synthetic Nx Console evaluator gate"
      contains: "TestRunCatalogsSyncFirstResponder"
    - path: "internal/quarantine/quarantine_test.go"
      provides: "TestRestoreCorpusQuarantineEntry (FRB-03 reversibility)"
      contains: "TestRestoreCorpusQuarantineEntry"
  key_links:
    - from: "cmd/beekeeper/catalogs_daemon.go"
      to: "internal/watch.firstResponderFn"
      via: "FirstResponderConfig with CorpusPath/CorpusEnabled/CorpusSentryThreshold after the adjudication batch"
      pattern: "firstResponderFn"
    - from: "cmd/beekeeper/catalogs_daemon.go"
      to: "internal/catalog.AddLocalOverlayEntry"
      via: "one overlay entry per malicious corpus record (FRB-05)"
      pattern: "AddLocalOverlayEntry"
    - from: "cmd/beekeeper/catalogs_daemon.go"
      to: "internal/corpus.ReadMaliciousRecords"
      via: "drives the overlay loop"
      pattern: "ReadMaliciousRecords"
---

<objective>
Wire the 24-01 and 24-02 building blocks into the live `runCatalogsSync` command path (the OQ-3 off-hot-path home), prove restore reversibility for a corpus-adjudication quarantine entry (FRB-03), and ship the synthetic Nx Console end-to-end evaluator gate that exercises FRB-01..05 together.

Purpose: This is the integration + acceptance plan. After the adjudication batch already running in runCatalogsSync, the same call now (a) runs the first-responder corpus pass to arm the card + elevate Sentry watch (FRB-01/04), and (b) adds a local catalog overlay entry per confirmed-malicious record so future installs are caught without catalog lag (FRB-05). All three new calls follow the established non-fatal stderr-log pattern. The evaluator gate locks the whole moat loop.
Output: `cmd/beekeeper/catalogs_daemon.go` extended; `cmd/beekeeper/catalogs_daemon_test.go` with the evaluator gate; a restore test for the corpus-quarantine entry shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-first-responder-corpus-binding/24-RESEARCH.md
@.planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md
@.planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md
@.planning/phases/24-first-responder-corpus-binding/24-01-SUMMARY.md
@.planning/phases/24-first-responder-corpus-binding/24-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- `cmd/beekeeper/catalogs_daemon.go` → new unexported helper `func buildOverlayEntry(rec corpus.CorpusRecord) catalog.Entry`; new FRB wiring block inside `runCatalogsSync` after the existing RunAdjudicationBatch call
- `cmd/beekeeper/catalogs_daemon_test.go` → new test `TestRunCatalogsSyncFirstResponder` (the synthetic Nx Console evaluator gate)
- `internal/quarantine/quarantine_test.go` → new test `TestRestoreCorpusQuarantineEntry`
- New import in catalogs_daemon.go: `github.com/home-beekeeper/beekeeper/internal/watch`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FRB-03 restore reversibility for a corpus-quarantine entry</name>
  <files>internal/quarantine/quarantine_test.go</files>
  <read_first>
    - internal/quarantine/quarantine.go (MoveTyped, Restore, Manifest, List — confirm a corpus-path entry is structurally identical to a scan-hit entry; Restore needs no change)
    - internal/quarantine/quarantine_test.go (existing restore test patterns + fixtures)
    - .planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md (TestRestoreCorpusQuarantineEntry row)
  </read_first>
  <behavior>
    - A quarantine entry created via MoveTyped with a corpus-style Manifest (RuleIDs ["FRSP-02"], Reason "corpus adjudication: confirmed malicious") is reversible via quarantine.Restore exactly like a scan-hit entry.
    - Restore returns the artifact to its OriginalPath and removes the quarantine entry.
  </behavior>
  <action>
    Add `TestRestoreCorpusQuarantineEntry` to `internal/quarantine/quarantine_test.go`. Following the existing restore-test fixture pattern: create a temp source artifact, MoveTyped it into a temp quarantine dir using a Manifest that mirrors the corpus path (Publisher "npm", Name "@nrwl/nx-console", Version "17.3.0", ArtifactType language-package, Reason "corpus adjudication: confirmed malicious", RuleIDs ["FRSP-02"]), assert quarantine.List shows one entry, then call quarantine.Restore for that entry id and assert (a) the artifact is back at OriginalPath and (b) the quarantine entry is gone.

    This proves FRB-03 reversibility without any change to quarantine.go — the corpus-adjudication entry is physically identical to a scan-hit entry. Confirm no production code change is required in internal/quarantine; if the test passes against existing Restore, this task is test-only.

    Run the test; commit.
  </action>
  <verify>
    <automated>go test ./internal/quarantine/... -run TestRestoreCorpusQuarantineEntry -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/quarantine/... -run TestRestoreCorpusQuarantineEntry` passes.
    - No production change to `internal/quarantine/quarantine.go` (test-only) — `git diff --stat internal/quarantine/quarantine.go` shows no change.
  </acceptance_criteria>
  <done>Restore is proven reversible for a corpus-quarantine entry shape. Committed as `test(24-03): ...`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the FRB corpus pass into runCatalogsSync (FRB-01/04/05)</name>
  <files>cmd/beekeeper/catalogs_daemon.go</files>
  <read_first>
    - cmd/beekeeper/catalogs_daemon.go (runCatalogsSync lines ~78-123: the existing Phase-23 adjudication batch pass, ResolveCorpusPath, NewMultiIndex wrapper, non-fatal stderr-log pattern, stateDir/dir/stateFile already computed)
    - internal/watch/firstresponder.go (FirstResponderConfig — the corpus fields added in 24-02; firstResponderFn injectable seam)
    - internal/corpus/reader.go (ReadMaliciousRecords from 24-01)
    - internal/corpus/types.go (CorpusRecord / PushEnvelope.Signature.PackageOrExtensionID + Version access for buildOverlayEntry)
    - internal/catalog/local_overlay.go (AddLocalOverlayEntry from 24-01)
    - internal/catalog/schema.go (Entry fields for buildOverlayEntry)
    - .planning/phases/24-first-responder-corpus-binding/24-PATTERNS.md (catalogs_daemon.go wiring excerpt + buildOverlayEntry shape + parsePackageID reuse note)
  </read_first>
  <action>
    Modify `cmd/beekeeper/catalogs_daemon.go`. Inside the existing `if cfg.Corpus.Enabled { ... }` block in runCatalogsSync, AFTER the existing `corpus.RunAdjudicationBatch(...)` call (and its non-fatal error log), add the Phase-24 wiring — every call follows the SAME non-fatal stderr-log pattern (errors logged, sync continues, never returned):

    1. Add import `github.com/home-beekeeper/beekeeper/internal/watch` (catalog + corpus + platform + policy already imported).
    2. Resolve the audit dir/path the first-responder needs (mirror how other commands locate `beekeeper.ndjson` under StateDir/audit; reuse platform helpers — do NOT hardcode). Resolve quarantine dir and sentry-targets path under stateDir via filepath.Join.
    3. FRB-01/04 first-responder pass: call `firstResponderFn(cmd.Context(), watch.FirstResponderConfig{ CorpusPath: corpusPath, CorpusEnabled: cfg.Corpus.Enabled, CorpusSentryThreshold: 2, SentryTargetsPath: <stateDir>/sentry-targets.json, QuarantineDir: <stateDir>/quarantine, AuditPath: <auditDir>/beekeeper.ndjson, IndexPath: <dir>/bumblebee.idx, CacheDir: dir, Enabled: <auto-quarantine enabled>, Threshold: 2 })`. On error: `fmt.Fprintf(os.Stderr, "beekeeper: corpus first-responder: %v\n", frErr)` and continue. Use the `firstResponderFn` injectable seam (not RunFirstResponder directly) so cmd tests can stub it.
    4. FRB-05 overlay pass: `malicious, rdErr := corpus.ReadMaliciousRecords(corpusPath)`; on error log + skip. For each record where `rec.PushEnvelope != nil && rec.PushEnvelope.Signature.PackageOrExtensionID != ""`: call `catalog.AddLocalOverlayEntry(dir, buildOverlayEntry(rec))`; on error log to stderr + continue (non-fatal per record).
    5. Add the unexported helper `func buildOverlayEntry(rec corpus.CorpusRecord) catalog.Entry`: parse ecosystem+pkg from PushEnvelope.Signature.PackageOrExtensionID (split on first ':'); return a catalog.Entry with ID="local-overlay-"+rec.AuditRecord.ClusterID, Name=pkg, Ecosystem=ecosystem, Package=pkg, Versions=[]string{rec.PushEnvelope.Signature.Version}, Severity="critical", SourceURL="", CatalogSignature="" (MUST be empty — unsigned → warn-only per Pitfall 3), CatalogSource="local-overlay".

    Ordering note: keep these three calls AFTER RunAdjudicationBatch and BEFORE the HTTP fetch / interval-gate return, so the FRB pass runs even when the sync is interval-skipped (the moat feedback must not depend on a catalog fetch being due — mirror the existing rationale comment that the batch pass runs before the fetch).

    Do NOT import internal/tui. Do NOT call quarantine.Purge. Keep `internal/policy` untouched.

    Run `go build ./...` and `go vet ./cmd/beekeeper/...`; commit.
  </action>
  <verify>
    <automated>go build ./... && go vet ./cmd/beekeeper/... && go test ./cmd/beekeeper/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'firstResponderFn\|ReadMaliciousRecords\|AddLocalOverlayEntry\|buildOverlayEntry' cmd/beekeeper/catalogs_daemon.go` shows all four wired inside runCatalogsSync.
    - `grep -n 'CatalogSignature' cmd/beekeeper/catalogs_daemon.go` shows buildOverlayEntry sets CatalogSignature to "" (empty — unsigned).
    - `grep -n 'Purge(\|internal/tui' cmd/beekeeper/catalogs_daemon.go` returns 0 matches.
    - The three new calls each have a non-fatal `fmt.Fprintf(os.Stderr, ...)` error branch and never `return` the error from runCatalogsSync.
    - `go build ./...` exits 0; `go vet ./cmd/beekeeper/...` exits 0; the existing cmd/beekeeper tests still pass.
  </acceptance_criteria>
  <done>runCatalogsSync runs the first-responder corpus pass + adds local overlay entries after the adjudication batch, all non-fatal, no Purge, no tui import. Committed as `feat(24-03): ...`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synthetic Nx Console end-to-end evaluator gate (FRB-01..05)</name>
  <files>cmd/beekeeper/catalogs_daemon_test.go</files>
  <read_first>
    - cmd/beekeeper/catalogs_daemon.go (the wired runCatalogsSync from Task 2 + the firstResponderFn seam)
    - cmd/beekeeper/catalogs_daemon_test.go (existing runCatalogsSync test patterns: how cfg/cmd/stateDir/catalogDir temp dirs are set up, how firstResponderFn or scanOnDeltaFn are stubbed)
    - internal/corpus/types.go (CorpusRecord/PushEnvelope/EnvelopeSignature for the fixture)
    - internal/corpus/store.go (AppendCorpusRecordLine — to seed the synthetic corpus NDJSON; or write the NDJSON line directly)
    - internal/sentry/targets.go (LoadTargets — to assert the target was added)
    - internal/quarantine/quarantine.go (List, Restore — to assert armed + reversible)
    - internal/catalog/multi.go (NewMultiIndexWithOverlay, LookupAll — to assert the overlay match)
    - .planning/phases/24-first-responder-corpus-binding/24-VALIDATION.md (the 7-assertion evaluator-gate spec + TestRunCatalogsSyncFirstResponder command)
    - .planning/phases/24-first-responder-corpus-binding/24-RESEARCH.md (Synthetic Nx Console Incident Fixture)
  </read_first>
  <behavior>
    - A confirmed-malicious enforce-tier corpus record for npm:@nrwl/nx-console v17.3.0, fed through runCatalogsSync with a fake CrossReference returning a matching ScanHit, produces: a catalog_quarantine audit record, a Sentry target for the package, one armed quarantine entry, a restorable entry, a local-overlay match, and zero purge calls.
  </behavior>
  <action>
    Add `TestRunCatalogsSyncFirstResponder` to `cmd/beekeeper/catalogs_daemon_test.go`. This is the phase evaluator gate; it must pass before /gsd-verify-work.

    Setup:
    - Temp StateDir + CatalogDir (point platform resolution at them via the existing test env override used by other runCatalogsSync tests; if none exists, set the relevant env vars per the project's StateDir/CatalogDir resolution — see how existing cmd tests isolate these).
    - Enable corpus in cfg (cfg.Corpus.Enabled = true) and resolve the corpus path the same way runCatalogsSync does; seed the synthetic Nx Console CorpusRecord (TrueLabel "malicious", AdjudicationSource "catalog_confirmation", PushEnvelope{Signature{PackageOrExtensionID:"npm:@nrwl/nx-console", Version:"17.3.0"}, TrueLabel:"malicious", ConfidenceTier:"enforce", SourceCount:2, ActionHint: ActionHintWatchAndBlock}) as a single NDJSON line into the corpus file.
    - Stub the first-responder's CrossReference so it returns a matching ScanHit (Ecosystem "npm", Package "@nrwl/nx-console", Version "17.3.0", PathResolved=true, InstalledPath = a temp file that exists so MoveTyped succeeds). Prefer driving the REAL RunFirstResponder (so the integration is genuine) by setting firstResponderFn to a closure that calls watch.RunFirstResponder with a config whose CrossRefFn returns the fake ScanHit; restore the original firstResponderFn in a defer.

    Run runCatalogsSync(cmd, true) and assert the 7 evaluator-gate conditions from 24-VALIDATION.md:
    1. corpus.ReadMaliciousRecords(corpusPath) returns the seeded record.
    2. The audit log contains a record with RecordType "catalog_quarantine" (FRB-01).
    3. sentry.LoadTargets(<stateDir>/sentry-targets.json) contains "@nrwl/nx-console" (FRB-04: SourceCount 2 >= threshold 2).
    4. quarantine.List(<stateDir>/quarantine) contains exactly one entry for the package (FRB-01 armed).
    5. No purge occurred — the entry is still present and reversible (FRB-02). (The static no-purge gate from 24-02 covers the code path; here assert the entry survived, i.e. nothing auto-purged it.)
    6. quarantine.Restore(<stateDir>/quarantine, entryID) succeeds and the artifact returns to its original path (FRB-03).
    7. A MultiIndex opened with NewMultiIndexWithOverlay(nil, nil, nil, <catalogDir>/local-overlay.idx).LookupAll("npm","@nrwl/nx-console") returns >= 1 match with CatalogSource=="local-overlay" (FRB-05).

    Keep the test hermetic (t.TempDir, no network — corpus + overlay are local files; no HTTP fetch needed since --force still runs the corpus pass before the fetch, but stub/avoid the HTTP path or accept a fetch error as non-fatal since the FRB pass runs first). If the HTTP fetch in runCatalogsSync would fail the call's return, set cfg so catalog sync is disabled-but-forced OR assert only on the FRB side effects regardless of the fetch outcome (the FRB pass runs before the fetch and its effects persist even if the later fetch errors).

    Run the gate; iterate to GREEN. Then run the full suite. Commit.
  </action>
  <verify>
    <automated>go test ./cmd/beekeeper/... -run TestRunCatalogsSyncFirstResponder -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/beekeeper/... -run TestRunCatalogsSyncFirstResponder` passes, asserting all 7 evaluator-gate conditions (FRB-01..05 end to end).
    - `go test ./... -count=1` exits 0 (full suite green).
    - `go build ./...` exits 0; `go vet ./...` exits 0.
    - `go mod tidy && git diff --exit-code go.mod` reports NO CHANGE.
    - The test asserts CatalogSource=="local-overlay" for the Nx Console lookup (FRB-05) and that quarantine.Restore succeeds (FRB-03).
  </acceptance_criteria>
  <done>The synthetic Nx Console round-trip passes all 7 FRB assertions, the full suite is green, and no new dependencies were added. Committed as `test(24-03): ...`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the TUI red/coral semantic on the corpus-armed card (FRB-03)</name>
  <files>internal/tui/styles.go (read-only reference — colorRed #f85149 / colorCoral #f0883e), internal/tui/incidents.go (CatalogQuarantineIncidentFromRecord), internal/tui/alerts_panel.go (recordToRow)</files>
  <action>
    This is a blocking human-verify checkpoint — no code is written. The executor automates everything up to this point (Tasks 1-3), then pauses for the maintainer to confirm the FRB-03 manual-only verification flagged in 24-VALIDATION.md: the corpus-armed quarantine card preserves the locked red=attacker / coral=Beekeeper color semantic. Present the what-built + how-to-verify below to the maintainer and wait for the resume signal before finalizing the phase.
  </action>
  <what-built>
    The TUI red=attacker / coral=Beekeeper color semantic on the corpus-armed quarantine card (FRB-03). The corpus path reuses the existing "catalog_quarantine" / "pending-quarantine" audit record types, which the TUI already renders with the locked coral BadgeBlock/BadgeHeld styling — so no new colors are introduced. This checkpoint confirms the visual semantic is preserved on a corpus-adjudication card (the one behavior 24-VALIDATION.md flags as manual-only).
  </what-built>
  <how-to-verify>
    1. Build the binary: `go build -o beekeeper ./cmd/beekeeper` (Windows: `beekeeper.exe`).
    2. Seed a confirmed-malicious corpus record + trigger the first-responder pass (run the steps the maintainer prefers — either run `beekeeper catalogs sync --force` against a StateDir that has the synthetic Nx Console corpus record seeded, or reuse the temp dirs from `TestRunCatalogsSyncFirstResponder`).
    3. Launch the TUI dashboard and open the quarantine / alerts panel.
    4. Confirm the corpus-armed quarantine card uses the EXISTING palette: coral (#f0883e) for the Beekeeper-response badge (HELD / quarantine), red (#f85149) only for attacker-action rows — no new colors, and the [r]estore and [p]urge(irreversible) actions are present (purge still requires the p→y confirmation, never auto-fires).
  </how-to-verify>
  <verify>
    <automated>go build ./cmd/beekeeper/...</automated>
    <human-check>Maintainer confirms on a live TUI that the corpus-armed quarantine card uses only the existing coral/red palette (no new colors) and that [r]estore + [p]urge (p→y gated) actions render.</human-check>
  </verify>
  <resume-signal>Type "approved" if the red/coral semantic is preserved and restore/purge actions render correctly, or describe the visual issue.</resume-signal>
  <done>Maintainer has visually confirmed the red/coral semantic and the restore/purge actions on the corpus-armed card (FRB-03). Phase may finalize.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runCatalogsSync → quarantine / overlay / sentry-targets | The sync command (off the hot path) drives artifact moves, overlay writes, and target additions; a failure in any must not abort the sync and must never escalate to a destructive purge. |
| agent → local-overlay.* / sentry-targets.json / quarantine dir | All written under StateDir; agents must not read/write them (self-protection EvaluateSelfPath + owner-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-SYNC-FAILOPEN | Denial of Service | runCatalogsSync FRB wiring | mitigate | All three new calls (firstResponderFn, ReadMaliciousRecords, AddLocalOverlayEntry) use the established non-fatal stderr-log pattern; an error never returns from runCatalogsSync and never aborts the catalog sync. The FRB pass runs before the HTTP fetch so an offline machine still gets the moat feedback. |
| T-24-NOPURGE-CMD | Tampering / Blast Radius | catalogs_daemon.go | mitigate | The command path adds NO `quarantine.Purge` call; the only artifact removal route remains MoveTyped (reversible). Enforced by the 24-02 static gate (firstresponder.go) and confirmed by `grep -n 'Purge(' cmd/beekeeper/catalogs_daemon.go` returning 0. |
| T-24-OVERLAY-UNSIGNED | Tampering (poisoning) | buildOverlayEntry | mitigate | buildOverlayEntry always sets CatalogSignature="" → unsigned → warn-only (source_count:1); enforce requires a second independent signed source. Verified by the 24-01 unsigned-tier test + the evaluator-gate overlay assertion. |
| T-24-CARD-COLOR | Spoofing (UI) | TUI corpus-armed card | mitigate | The corpus path reuses existing audit record types rendered with the locked coral/red palette — no new colors. Confirmed by the blocking human-verify checkpoint (FRB-03 manual-only verification). |
| T-24-SC | Tampering | go.mod / new dependencies | mitigate | Zero new dependencies. Verified by `go mod tidy && git diff --exit-code go.mod`. |
</threat_model>

<verification>
- `go test ./cmd/beekeeper/... -run TestRunCatalogsSyncFirstResponder -count=1` — the 7-assertion evaluator gate green.
- `go test ./internal/quarantine/... -run TestRestoreCorpusQuarantineEntry` — FRB-03 restore green.
- `go test ./... -count=1` — full suite green.
- `grep -n 'Purge(\|internal/tui' cmd/beekeeper/catalogs_daemon.go` — 0 matches.
- `go build ./...` / `go vet ./...` exit 0; `go mod tidy` leaves go.mod unchanged.
- Human checkpoint: TUI red/coral semantic preserved on the corpus-armed card (FRB-03 manual).
</verification>

<success_criteria>
- runCatalogsSync runs the first-responder corpus pass + adds a local overlay entry per malicious record after the adjudication batch, all non-fatal (FRB-01/04/05 wiring).
- Restore reverses a corpus-adjudication quarantine entry cleanly (FRB-03).
- The synthetic Nx Console round-trip passes all 7 FRB assertions end to end (FRB-01..05).
- No automatic purge anywhere in the command path (FRB-02).
- The TUI red/coral semantic is preserved on the corpus-armed card (FRB-03, human-verified).
</success_criteria>

<output>
Create `.planning/phases/24-first-responder-corpus-binding/24-03-SUMMARY.md` when done.
</output>
