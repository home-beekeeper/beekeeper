---
phase: 25-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/beekeeper/catalogs_daemon_test.go
  - internal/corpus/launch_e2e_test.go
autonomous: true
requirements: [LAUNCH-01, LAUNCH-02]
must_haves:
  truths:
    - "The Nx Console incident produces a corpus record with all four layers (behavior, decision, outcome, context) populated, plus a signed push envelope (LAUNCH-01)"
    - "Each of the eight Sentry patterns (SENTRY-001..008) produces a moat-grade corpus record with all four layers present (LAUNCH-02)"
  artifacts:
    - path: "cmd/beekeeper/catalogs_daemon_test.go"
      provides: "Extended evaluator gate: 7-point FRB round-trip + 4 LAUNCH-01 four-layer/envelope assertions = 11-point gate"
      contains: "EVALUATOR GATE — 11 assertions"
    - path: "internal/corpus/launch_e2e_test.go"
      provides: "TestAllSentryPatternsProduceMoatRecord — table-driven 8-pattern moat-record proof"
      contains: "func TestAllSentryPatternsProduceMoatRecord("
  key_links:
    - from: "cmd/beekeeper/catalogs_daemon_test.go"
      to: "corpus.ReadMaliciousRecords"
      via: "assert four-layer + envelope on returned record"
      pattern: "ReadMaliciousRecords"
    - from: "internal/corpus/launch_e2e_test.go"
      to: "corpus.MapToCorpusRecord"
      via: "map each SentryAlert-derived AuditRecord to a CorpusRecord, assert four layers"
      pattern: "MapToCorpusRecord"
---

<objective>
Prove the two moat-population success criteria of v1.4.0: (1) the Nx Console incident
runs the full trace → record → adjudication → signature → local-feedback loop with all
four corpus layers populated (LAUNCH-01), and (2) all eight Sentry patterns
(SENTRY-001..008) each produce a moat-grade corpus record with all four layers present
(LAUNCH-02).

Purpose: This is the core proof of the v1.4.0 moat — the outcome layer is the
non-retrofittable differentiator, and these two gates assert it is present from the
first write for both the package-install surface (Nx Console / scan) and the behavioral
Sentry surface.

Output: An extended 11-point evaluator gate in `cmd/beekeeper/catalogs_daemon_test.go`
(LAUNCH-01) and a new table-driven `TestAllSentryPatternsProduceMoatRecord` in
`internal/corpus/launch_e2e_test.go` (LAUNCH-02). No production code changes — both are
test-only proofs over code shipped in Phases 22–24.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-launch-readiness/25-RESEARCH.md
@.planning/phases/25-launch-readiness/25-PATTERNS.md
@.planning/phases/25-launch-readiness/25-VALIDATION.md
</context>

<artifacts_this_phase_produces>
The following symbols/headings are CREATED by this phase. Drift verification must
treat them as expected new artifacts (not as orphaned or unreferenced):

- Test function: `TestAllSentryPatternsProduceMoatRecord` (in `internal/corpus/launch_e2e_test.go`)
- Test file: `internal/corpus/launch_e2e_test.go`
- Extended evaluator-gate assertions #8–#11 inside the existing `TestRunCatalogsSyncFirstResponder` (in `cmd/beekeeper/catalogs_daemon_test.go`); the gate comment changes from "7 assertions" to "11 assertions (FRB-01..05 + LAUNCH-01)"
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend TestRunCatalogsSyncFirstResponder to an 11-point gate (LAUNCH-01)</name>
  <files>cmd/beekeeper/catalogs_daemon_test.go</files>
  <read_first>
    - cmd/beekeeper/catalogs_daemon_test.go — the FULL existing `TestRunCatalogsSyncFirstResponder` (lines 69–end), especially the seed block (lines 109–135) and the existing 7-assertion gate (lines 186–end). NOTE: the seed leaves `PushEnvelope.Signature.BehaviorSignatureHash` EMPTY and `Scope` unset (zero-value) — see the action for how to handle this.
    - internal/corpus/types.go — the exact field names: outcome layer `TrueLabel`, `AdjudicationSource`, `WasCorrect *bool`, `ResolvedAt`; context layer `Scope CorpusScope` (zero-value marshals to "org_only"), `CorpusSchemaVersion`; envelope `PushEnvelope.Signature.BehaviorSignatureHash`, `PushEnvelope.ConfidenceTier`, `PushEnvelope.SourceCount`, `PushEnvelope.ActionHint`.
    - .planning/phases/25-launch-readiness/25-PATTERNS.md — § `cmd/beekeeper/catalogs_daemon_test.go (MODIFY — LAUNCH-01)`, the assertions #8–11 code excerpt and the comment-block update note.
    - .planning/phases/25-launch-readiness/25-RESEARCH.md — § LAUNCH-01 "All four layers populated assertion checklist" (lines 141–162) and Pitfall 1.
  </read_first>
  <action>
    Add four numbered assertions (#8, #9, #10, #11) immediately after the existing
    assertion #7 inside `TestRunCatalogsSyncFirstResponder`, operating on the
    `found1`/`malicious[0]` record returned by `corpus.ReadMaliciousRecords`. Update the
    gate banner comment from `EVALUATOR GATE — 7 assertions (FRB-01..05)` to
    `EVALUATOR GATE — 11 assertions (FRB-01..05 + LAUNCH-01)`.

    Assertion #8 (all four layers populated): on the matched malicious record `rec`,
    assert — Behavior layer: at least one of `rec.AuditRecord.SourceSurface` or
    `rec.AuditRecord.ToolName` is non-empty (the seed sets `ToolName="@nrwl/nx-console"`);
    Decision layer: `rec.AuditRecord.Decision == "block"`; Outcome layer:
    `rec.TrueLabel == "malicious"` AND `rec.AdjudicationSource != ""` (seed sets
    `"catalog_confirmation"`); Context layer: `rec.CorpusSchemaVersion == "1.0"` and
    `string(rec.Scope) == "org_only" || rec.Scope == ""` (the in-memory zero value is
    "" and marshals to "org_only" via MarshalJSON — accept either, do NOT require the
    non-empty string on the in-memory field, that is a known seed nuance per types.go).

    Assertion #9 (signature layer): `rec.PushEnvelope != nil`; then assert the envelope
    signature is present. CRITICAL SEED NUANCE: the current seed does NOT set
    `BehaviorSignatureHash`, so it is the empty string. Do NOT assert `len == 64` on the
    raw seed — instead, recompute/populate the signature by calling the production path:
    construct an `AdjudicationResult` consistent with the record and call
    `corpus.BuildPushEnvelope(rec, outcome)`; assert the RETURNED envelope's
    `Signature.BehaviorSignatureHash` is 64-char hex (the production emitter computes it).
    If `BuildPushEnvelope`'s signature input requires fields the seed lacks, instead
    extend the seed (lines 109–135) to set `Signature.BehaviorSignatureHash` to a real
    64-hex value via the production behavior-sig helper, and assert presence on the seed.
    Pick whichever keeps the test honest: the goal is to PROVE a 64-char-hex signature is
    representable for this record, not to assert a placeholder. Document the choice in a
    code comment referencing the seed nuance.

    Assertion #10 (confidence/source): `rec.PushEnvelope.ConfidenceTier == "enforce"` and
    `rec.PushEnvelope.SourceCount == 2` (seed sets both).

    Assertion #11 (action hint): `rec.PushEnvelope.ActionHint == corpus.ActionHintWatchAndBlock`.

    Use the existing `t.Errorf`/`t.Fatalf` numbered-comment style ("[8] ...", "[9] ...").
    Do not restructure the existing 7 assertions; this is purely additive. The full FRB
    7-point round-trip (quarantine card armed, sentry target gated, overlay entry, no
    auto-purge, restore) is the "local feedback" half of LAUNCH-01 and is already proven
    by assertions #1–#7 — #8–#11 add the four-layer + signature proof.
  </action>
  <verify>
    <automated>go test ./cmd/beekeeper/... -run TestRunCatalogsSyncFirstResponder -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/beekeeper/... -run TestRunCatalogsSyncFirstResponder -count=1` exits 0.
    - `cmd/beekeeper/catalogs_daemon_test.go` contains the literal string `EVALUATOR GATE — 11 assertions (FRB-01..05 + LAUNCH-01)`.
    - The test asserts `rec.TrueLabel == "malicious"`, a non-empty `rec.AdjudicationSource`, `rec.PushEnvelope.ConfidenceTier == "enforce"`, `rec.PushEnvelope.SourceCount == 2`, and `rec.PushEnvelope.ActionHint == corpus.ActionHintWatchAndBlock`.
    - The test proves a 64-char-hex `BehaviorSignatureHash` is representable for the record (either via `BuildPushEnvelope` or an extended seed), with a code comment naming the seed-empty-hash nuance.
    - `go vet ./cmd/beekeeper/...` exits 0.
  </acceptance_criteria>
  <done>The existing FRB evaluator gate is extended in-place to 11 assertions that prove the Nx Console corpus record has all four layers populated plus a signed (64-hex) push envelope with enforce-tier / source-count-2 / watch_and_block; the FRB local-feedback half is unchanged and still green.</done>
</task>

<task type="auto">
  <name>Task 2: TestAllSentryPatternsProduceMoatRecord — 8-pattern moat-record table (LAUNCH-02)</name>
  <files>internal/corpus/launch_e2e_test.go</files>
  <read_first>
    - .planning/phases/25-launch-readiness/25-PATTERNS.md — § `internal/corpus/launch_e2e_test.go (NEW — LAUNCH-02)`: the exact import set, the inline process-tree construction (package corpus CANNOT call sentry's package-internal helpers `editorTree`/`emptyInventory`/`defaultCfg`/`noBaseline`), the `MapToCorpusRecord` call pattern, the `[]fieldCheck` four-layer assertion loop, and the envelope-fragment assertion loop.
    - internal/corpus/emitter.go — the exact signature `func MapToCorpusRecord(rec audit.AuditRecord, cfg config.CorpusConfig, repoFingerprint, fleetNodeID string) CorpusRecord` (line 120) and `func BuildPushEnvelope(rec CorpusRecord, outcome AdjudicationResult) (PushEnvelope, error)` (line 205).
    - internal/corpus/types.go — four-layer field names (same as Task 1) and the envelope structure.
    - internal/corpus/emitter_test.go — lines 134–148 (the AuditRecord-with-sentry-fields → MapToCorpusRecord call pattern) and lines 143/168 (CorpusSchemaVersion / ActionHint assertions).
    - internal/corpus/schema_lock_test.go — lines 208–285 (the `fieldCheck` table + loop) and lines 370–396 (envelope fragment + 64-hex BehaviorSignatureHash assertion).
    - internal/sentry/rules.go — the 8 rule IDs and their `SentryAlert` shapes (SENTRY-001 critical cred-file-access … SENTRY-008 high persistence-write); confirm the EXPORTED `sentry.SentryAlert`, `sentry.SentryEvent`, `sentry.EventFileAccess`/`EventProcessCreate`/`EventNetworkConnect`/`EventFileWrite` symbols used to build events from `package corpus`.
    - internal/corpus/store.go — confirm `package corpus` and that this new file declares `package corpus` (white-box).
  </read_first>
  <action>
    Create `internal/corpus/launch_e2e_test.go` declaring `package corpus`. Imports:
    `strings`, `testing`, `time`, plus `github.com/bantuson/beekeeper/internal/audit`,
    `github.com/bantuson/beekeeper/internal/config`, and
    `github.com/bantuson/beekeeper/internal/sentry`. (This import direction is cycle-free:
    `internal/sentry` does NOT import `internal/corpus`.)

    Write `func TestAllSentryPatternsProduceMoatRecord(t *testing.T)` as a table-driven
    test with one row per Sentry pattern: SENTRY-001 through SENTRY-008 (8 rows). Each row
    carries: `name` (e.g. "SENTRY-001 cred-file-access"), `ruleID` (the literal
    "SENTRY-001"..."SENTRY-008"), and the minimal fields needed to construct an
    `audit.AuditRecord` representing that pattern's fired alert as it reaches the corpus
    write path.

    For each row, in `t.Run(tc.name, ...)`:
    1. Build an `audit.AuditRecord` with `RecordType: "policy_decision"`, a unique
       `RecordID`, `Timestamp` (RFC3339), `SourceSurface: "sentry"`,
       `SentryRuleID: tc.ruleID`, `ToolName: tc.ruleID` (action_type proxy for the sentry
       surface), `Decision: "alert"`, `CorroborationCount: 1`,
       `RulesetVersion: "1.0"`, `ClusterID: "launch-02-" + tc.ruleID`,
       and a non-empty `SentryRuleName`/`Reason` (e.g. the severity string).
       (Per RESEARCH.md A2 + the carried critical assumption, LAUNCH-02 "moat-grade" =
       all four layers PRESENT with `TrueLabel="unresolved"` at run-1; it does NOT drive
       a live catalog re-query. Drive the corpus mapper directly from a representative
       AuditRecord per pattern — do NOT attempt to resolve TrueLabel to "malicious".)
    2. Call `corpusRec := MapToCorpusRecord(rec, config.CorpusConfig{Enabled: true}, "launch02-repo-fp", "launch02-node")`.
    3. Assert four layers via a `[]fieldCheck` loop (copy the struct + loop from
       schema_lock_test.go 208–285): Behavior — `SourceSurface == "sentry"`,
       `SentryRuleID == tc.ruleID` (wantAny on a couple of behavior fields is fine);
       Decision — `Decision == "alert"`, and a non-empty `Reason` or `SentryRuleName`;
       Outcome — `TrueLabel == "unresolved"` (the non-retrofittable placeholder IS the
       moat); Context — `string(Scope)` is "org_only" OR "" (zero-value marshals to
       org_only) and `CorpusSchemaVersion == CorpusSchemaVersion`.
    4. Assert envelope: build the envelope via `BuildPushEnvelope(corpusRec, outcome)`
       (construct an `AdjudicationResult` representing an unresolved/initial outcome), OR
       if `MapToCorpusRecord` already populates `corpusRec.PushEnvelope`, assert directly
       on it. Whichever is populated, assert: `PushEnvelope != nil`,
       `ActionHint == ActionHintWatchAndBlock`, `ConfidenceTier` non-empty, and
       `len(PushEnvelope.Signature.BehaviorSignatureHash) == 64` (64-char hex, per the
       SCHEMA-06 gate). Use the envelope-fragment `strings.Contains` loop from
       schema_lock_test.go 370–395 if asserting on marshaled JSON.

    Add a top-of-function comment documenting the carried assumption verbatim:
    `// LAUNCH-02 "moat-grade" = all four layers PRESENT, TrueLabel="unresolved" accepted`
    `// at capture time (RESEARCH.md Assumptions Log A2, MEDIUM risk, maintainer-flagged).`
    `// To require TrueLabel="malicious" for all 8 patterns, drive RunAdjudicationBatch`
    `// with a catalog hit per pattern (heavier — not done here per A2).`

    Do NOT call sentry's package-internal test helpers (`editorTree`, `emptyInventory`,
    `defaultCfg`, `noBaseline`, `hasAlert`) — they live in `package sentry` and are
    unreachable from `package corpus`. The 8 rows construct `audit.AuditRecord` values
    directly (the corpus write path consumes an AuditRecord, not a raw SentryAlert), which
    is exactly the production seam (`NewMultiSinkWithCorpus` → `MapToCorpusRecord`).
  </action>
  <verify>
    <automated>go test ./internal/corpus/... -run TestAllSentryPatternsProduceMoatRecord -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `internal/corpus/launch_e2e_test.go` exists, declares `package corpus`, and contains `func TestAllSentryPatternsProduceMoatRecord(`.
    - The test table has exactly 8 rows, one per `SENTRY-001` … `SENTRY-008` (each ruleID literal appears).
    - `go test ./internal/corpus/... -run TestAllSentryPatternsProduceMoatRecord -count=1 -v` exits 0 and shows 8 passing subtests.
    - Each subtest asserts all four layers (behavior/decision/outcome/context) AND `len(PushEnvelope.Signature.BehaviorSignatureHash) == 64` AND `ActionHint == ActionHintWatchAndBlock`.
    - The file contains the A2 assumption comment naming `TrueLabel="unresolved"` acceptance.
    - `go vet ./internal/corpus/...` exits 0; `go build ./...` exits 0.
  </acceptance_criteria>
  <done>A table-driven test proves each of the eight Sentry patterns maps to a corpus record with all four layers present (TrueLabel="unresolved" placeholder = the moat) plus a 64-hex-signature watch_and_block push envelope; the A2 "moat-grade = present-not-resolved" assumption is documented in-file for maintainer override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-input → corpus mapper | Synthetic AuditRecords / seed records cross into `MapToCorpusRecord` / `BuildPushEnvelope`; a malicious test could try to seed a purge-class intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-INJECT | Tampering | LAUNCH-02 synthetic AuditRecords driving `MapToCorpusRecord`/`BuildPushEnvelope` | mitigate | Tests assert `ActionHint == ActionHintWatchAndBlock` only; `auto_purge` is a compile-time-unrepresentable typed const (SCHEMA-04) and `BuildPushEnvelope` rejects purge-class intent (ENV-02). The existing `FuzzBuildPushEnvelope` (ENV-03, 316k execs/0 escapes) is the standing guard; these tests do not weaken it. |
| T-25-PURGE | Tampering | LAUNCH-01 FRB feedback path | accept | The extended gate asserts NO auto-purge (assertions #4/#5 are unchanged and still prove the quarantine entry survives); the corpus feedback path has `TestCorpusPathHasNoPurgeCall` (Phase 24) as the static guard. No new purge surface is introduced by adding read-only assertions. |
| T-25-SEED | Information Disclosure | Test seed records in test files | accept | Seed records use synthetic identifiers (`@nrwl/nx-console` public CVE-class fixture, `launch02-repo-fp`); no real secrets, repo paths, or PII. HMAC fingerprints in tests use placeholder salts. Low value, test-only. |
</threat_model>

<verification>
Phase-level checks for this plan:
- `go test ./cmd/beekeeper/... -run TestRunCatalogsSyncFirstResponder -count=1` exits 0 (LAUNCH-01).
- `go test ./internal/corpus/... -run TestAllSentryPatternsProduceMoatRecord -count=1` exits 0 (LAUNCH-02).
- `go test ./internal/corpus/... ./cmd/beekeeper/... -count=1` exits 0 (no regression).
- `go vet ./...` exits 0; `go build ./...` exits 0.
- `go mod tidy && git diff --exit-code go.mod` (zero new deps — both tasks use only stdlib + existing internal packages).
- `go test ./internal/policy/... -run TestCorroborationImportsArePure -count=1` exits 0 (purity untouched — neither task imports internal/policy).
</verification>

<success_criteria>
- LAUNCH-01: the Nx Console evaluator gate is an 11-point gate proving trace → record → adjudication → signature → local feedback with all four layers populated.
- LAUNCH-02: all eight Sentry patterns produce a moat-grade (four-layer + signed-envelope) corpus record, with the unresolved-outcome assumption documented for maintainer override.
- Zero new product code, zero new dependencies, internal/policy purity preserved.
</success_criteria>

<output>
Create `.planning/phases/25-launch-readiness/25-01-SUMMARY.md` when done.
</output>
