---
phase: 25-launch-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/check/handler_test.go
  - internal/corpus/store_test.go
autonomous: true
requirements: [LAUNCH-03, LAUNCH-04]
must_haves:
  truths:
    - "beekeeper check p99 stays sub-100ms with the corpus enabled — a deterministic CI gate (not eyeball), proving the corpus loop is off the hot path (LAUNCH-03 perf)"
    - "A disconnected/offline machine still BLOCKS a known-malicious package on the last-synced mmap catalog — fail-closed, not allow (LAUNCH-03 offline)"
    - "No corpus data leaves the machine: internal/corpus/store.go has no net / net/http / os/exec import (LAUNCH-04 no-exfil)"
  artifacts:
    - path: "internal/check/handler_test.go"
      provides: "TestBenchmarkRunCheckGate (deterministic p99 < 100ms gate) + TestOfflineProtective (offline block proof)"
      contains: "func TestBenchmarkRunCheckGate("
    - path: "internal/corpus/store_test.go"
      provides: "TestCorpusStoreHasNoNetworkImports — go/parser AST import-purity gate"
      contains: "func TestCorpusStoreHasNoNetworkImports("
  key_links:
    - from: "internal/check/handler_test.go"
      to: "runCheck"
      via: "N-iteration timed loop with corpus enabled, assert p99 < 100ms"
      pattern: "runCheck"
    - from: "internal/corpus/store_test.go"
      to: "store.go imports"
      via: "go/parser ImportsOnly scan, forbid net/net-http/os-exec"
      pattern: "parser.ParseFile"
---

<objective>
Prove the hot-path budget and the no-exfil guarantee of v1.4.0: (1) a deterministic
gate that `beekeeper check` p99 stays sub-100ms with the corpus write enabled — proving
the corpus loop is off the synchronous hook hot path (LAUNCH-03 perf); (2) an offline
machine still fail-closed BLOCKS a known-malicious package on the last-synced mmap
catalog (LAUNCH-03 offline); and (3) a static AST gate that `internal/corpus/store.go`
has no `net`, `net/http`, or `os/exec` import — the no-corpus-exfil property (LAUNCH-04
verification half).

Purpose: LAUNCH-03 closes the Phase-23 carried-over "p99 eyeball only" item by turning
it into a deterministic CI gate, and proves the offline-protective guarantee that is the
heart of the local-loop value. LAUNCH-04's static import gate is the machine-checkable
half of "no corpus data leaves the machine" (the docs half is in 25-03).

Output: `TestBenchmarkRunCheckGate` + `TestOfflineProtective` added to
`internal/check/handler_test.go`; `TestCorpusStoreHasNoNetworkImports` added to
`internal/corpus/store_test.go`. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-launch-readiness/25-RESEARCH.md
@.planning/phases/25-launch-readiness/25-PATTERNS.md
@.planning/phases/25-launch-readiness/25-VALIDATION.md
</context>

<artifacts_this_phase_produces>
The following symbols are CREATED by this phase. Drift verification must treat them as
expected new artifacts:

- Test function: `TestBenchmarkRunCheckGate` (in `internal/check/handler_test.go`)
- Test function: `TestOfflineProtective` (in `internal/check/handler_test.go`)
- Test function: `TestCorpusStoreHasNoNetworkImports` (in `internal/corpus/store_test.go`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: TestBenchmarkRunCheckGate + TestOfflineProtective (LAUNCH-03)</name>
  <files>internal/check/handler_test.go</files>
  <read_first>
    - internal/check/handler_test.go — the existing `BenchmarkRunCheck` (lines 1462–1511): copy its `cfg` (FailMode closed + `Corpus{Enabled:true, Path: <tempdir>/corpus/...}`), the `buildTestIndexB` index build, the corpus-dir MkdirAll, and the ReadFile (NOT Bash npm-install) stdin string EXACTLY. Also read the existing helpers `buildTestIndex` (lines 22–40), `closedConfig` (line 42), `auditPathIn` (lines 44–47), and a `TestRunCheckBlocks*` / `TestHookHandlerAllow` (lines 49–66) to copy the block-assertion shape.
    - internal/check/handler.go — the `runCheck` / `RunCheck` signatures (confirm parameter order: ctx, stdin, cfg, idxPath, auditPath, stateDir, opener, writer) so the timed loop calls it correctly; `writeCorpusRecord` (line 567) to confirm the corpus write is on the call path when `cfg.Corpus.Enabled`.
    - internal/llamafirewall/latency.go — `LatencyTracker` (lines 13–75): `Record(ms int64)` and `P99() int64` operate in MILLISECONDS (so the 100ms gate is `P99() < 100`). NOTE: RESEARCH.md called this `LTRing`/`internal/check`; the REAL p99 ring type is `llamafirewall.LatencyTracker`. Either use it (import `internal/llamafirewall`, Record elapsed-ms per call, assert `P99() < 100`) OR compute p99 inline from a sorted `[]int64` of per-call elapsed-ms — pick the inline approach if importing llamafirewall into a check test feels heavy; both are acceptable.
    - .planning/phases/25-launch-readiness/25-PATTERNS.md — § `internal/check/handler_test.go (MODIFY — LAUNCH-03)` Analogs A/B/C and the corpus-enabled-config shared pattern.
    - .planning/phases/25-launch-readiness/25-RESEARCH.md — § LAUNCH-03 (Option C recommended), Pitfall 3 (ReadFile-not-Bash), and Open Question 3 (100ms Linux/macOS, 200ms Windows headroom).
  </read_first>
  <action>
    Add two tests to `internal/check/handler_test.go` (package `check`).

    `func TestBenchmarkRunCheckGate(t *testing.T)`: a deterministic latency gate (NOT a
    `Benchmark*` func). If `testing.Short()` return early with `t.Skip("latency gate skipped in -short")`.
    Set up exactly as `BenchmarkRunCheck`: `dir := t.TempDir()`, build the mmap index via
    the test index helper, create `<dir>/corpus/gate-corpus.ndjson` (MkdirAll 0o700), and
    `cfg := config.Config{FailMode: config.FailModeClosed, Corpus: config.CorpusConfig{Enabled: true, Path: corpusPath}}`.
    Run `runCheck` N times (N = 100). For EACH iteration use a fresh
    `strings.NewReader` of the ReadFile tool-input JSON (the exact string from
    `BenchmarkRunCheck` line ~1504 — `{"agent_name":"a","tool_name":"ReadFile","tool_input":{"path":"/bench/fixture/beekeeper-test-file.txt"}}`),
    measure `start := time.Now()` … `elapsedMS := time.Since(start).Milliseconds()` around
    the single `runCheck(...)` call, and record `elapsedMS` into a `[]int64` (or a
    `llamafirewall.LatencyTracker.Record`). Discard runCheck output to `io.Discard`.
    After the loop, compute p99 (sort the slice and take nearest-rank index
    `ceil(0.99*N)-1`, OR call `tracker.P99()`). Assert the p99 is under the budget:
    `budgetMS := int64(100); if runtime.GOOS == "windows" { budgetMS = 200 }` (per Open
    Question 3 — Windows CI runners are 2–3x slower; the development-machine gate is
    100ms). If p99 exceeds the budget, `t.Errorf("LAUNCH-03: runCheck p99 = %dms with corpus enabled; want < %dms (corpus loop must stay off the hot path)", p99, budgetMS)`.
    Add a comment that this proves the corpus write (cfg.Corpus.Enabled=true, real NDJSON
    append each iteration) does not push the hook hot path over budget.

    CRITICAL (Pitfall 3): use the ReadFile tool input, NEVER a Bash npm-install — a Bash
    install triggers the pnpm/bun nudge subprocess (~2–5s on Windows) and makes the gate
    meaningless. The ReadFile input is non-negotiable.

    `func TestOfflineProtective(t *testing.T)`: prove fail-closed block on a disconnected
    machine. `dir := t.TempDir()`; build a catalog mmap index that CONTAINS a
    known-malicious entry (reuse the test index helper that seeds a malicious package —
    inspect `buildTestIndex`/`buildTestIndexB` for how an existing `TestRunCheckBlocks*`
    seeds the malicious entry, and seed the same one, e.g. an `npm` package flagged by ≥2
    sources so corroboration blocks). Configure NO live network catalog sources (the
    default for all tests — tests are offline by construction). Drive `runCheck`/`RunCheck`
    with a stdin tool-input that installs/references that malicious package. Assert the
    returned `res.Decision.Allow == false` (block). Add a comment: "offline = default test
    state (no network sources configured); the mmap index is the last-synced catalog; a
    blocked decision proves the disconnected machine stays protective (fail-closed)."
    If the simplest path is to mirror an existing `TestRunCheckBlocks*` body, do so and
    rename — the offline guarantee IS that existing block path with no network configured.
  </action>
  <verify>
    <automated>go test ./internal/check/... -run "TestBenchmarkRunCheckGate|TestOfflineProtective" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `internal/check/handler_test.go` contains `func TestBenchmarkRunCheckGate(` and `func TestOfflineProtective(`.
    - `go test ./internal/check/... -run "TestBenchmarkRunCheckGate|TestOfflineProtective" -count=1 -v` exits 0 with both tests PASS.
    - `TestBenchmarkRunCheckGate` sets `cfg.Corpus.Enabled = true`, uses the ReadFile (not Bash) tool input, runs 100 iterations, computes p99, and asserts p99 < 100ms (< 200ms on `runtime.GOOS == "windows"`).
    - `TestOfflineProtective` asserts `res.Decision.Allow == false` for a known-malicious package against an mmap index with no network sources configured.
    - `go vet ./internal/check/...` exits 0.
  </acceptance_criteria>
  <done>A deterministic CI gate proves `beekeeper check` p99 stays under the budget (100ms; 200ms Windows) with the corpus write enabled, and an offline-protective test proves a disconnected machine still fail-closed blocks a known-malicious package on the last-synced catalog. The Phase-23 carried-over p99 eyeball item is now a real gate.</done>
</task>

<task type="auto">
  <name>Task 2: TestCorpusStoreHasNoNetworkImports — static no-exfil AST gate (LAUNCH-04)</name>
  <files>internal/corpus/store_test.go</files>
  <read_first>
    - internal/sentry/imports_test.go — the FULL 47-line `TestRulesImportsArePure` AST gate: `os.ReadFile(path)` → `parser.ParseFile(fset, path, src, parser.ImportsOnly)` → range `f.Imports`, strip surrounding quotes from `imp.Path.Value`, check against a `forbidden` map. Copy this structure exactly.
    - internal/corpus/store.go — confirm its CURRENT import set: `bufio, encoding/json, fmt, os, sync, internal/audit, internal/platform` — none of `net`, `net/http`, `os/exec`. This is the file under test.
    - internal/corpus/store_test.go — the existing file (package `corpus`); confirm it already imports `os` so no new import beyond `go/parser`, `go/token` is needed; add the new test function to this file.
    - .planning/phases/25-launch-readiness/25-PATTERNS.md — § `internal/corpus/store_test.go (MODIFY — LAUNCH-04 static import gate)`: the adapted forbidden set and the STORE-03 / LAUNCH-04 error-message guidance.
    - .planning/phases/25-launch-readiness/25-RESEARCH.md — § LAUNCH-04 (the no-exfil verification) and Pitfall 4 (direct-import check on store.go is sufficient; transitive graph is covered by `go vet`/cross-build).
  </read_first>
  <action>
    Add `func TestCorpusStoreHasNoNetworkImports(t *testing.T)` to the existing
    `internal/corpus/store_test.go` (package `corpus`). Add imports `go/parser` and
    `go/token` (and `os` if not already imported). Build a `forbidden := map[string]bool{
    "net": true, "net/http": true, "os/exec": true }`. Read `store.go` via
    `os.ReadFile("store.go")`, parse with
    `parser.ParseFile(token.NewFileSet(), "store.go", src, parser.ImportsOnly)`, and range
    over `f.Imports`: strip the surrounding double-quotes from `imp.Path.Value` (the AST
    keeps them), and if the stripped path is in `forbidden`, call
    `t.Errorf("LAUNCH-04 / STORE-03 violation: internal/corpus/store.go imports forbidden network package %q — the corpus store must never transmit data off-machine (no remote sink wired in v1)", p)`.
    `t.Fatalf` on ReadFile / ParseFile errors. This mirrors `TestRulesImportsArePure`
    exactly but scoped to the single `store.go` file and the network-forbidden set.

    Do NOT use `exec.Command("go", "list", ...)` — the in-process `go/parser` AST approach
    is cheaper, has no subprocess, and is the established codebase pattern
    (`internal/sentry/imports_test.go`). Per Pitfall 4, only `store.go`'s DIRECT imports
    need proving — `StoreSink.Write` is the only corpus-write path; the transitive graph
    is validated by `go vet ./...` and the CI cross-build.
  </action>
  <verify>
    <automated>go test ./internal/corpus/... -run TestCorpusStoreHasNoNetworkImports -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `internal/corpus/store_test.go` contains `func TestCorpusStoreHasNoNetworkImports(`.
    - The test parses `store.go` with `parser.ParseFile(..., parser.ImportsOnly)` and forbids `net`, `net/http`, `os/exec`.
    - `go test ./internal/corpus/... -run TestCorpusStoreHasNoNetworkImports -count=1 -v` exits 0 (PASS — store.go currently has none of the forbidden imports).
    - The error message names both LAUNCH-04 and STORE-03.
    - `go vet ./internal/corpus/...` exits 0.
  </acceptance_criteria>
  <done>A static AST gate proves `internal/corpus/store.go` has no `net`, `net/http`, or `os/exec` import, machine-verifying the "no corpus data leaves the machine" guarantee (LAUNCH-04 verification half / STORE-03), mirroring the established `imports_test.go` purity-gate pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk catalog index → runCheck | The mmap catalog is the last-synced threat intel; offline, it is the sole defense boundary |
| corpus store → (no network) | LAUNCH-04 asserts this boundary is sealed: store.go has no network import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-PERF | Repudiation | `TestBenchmarkRunCheckGate` p99 baseline | mitigate | The gate is set to 100ms (≈4x headroom over the ~25ms observed in Phase 23), 200ms on Windows CI per Open Question 3, with a deterministic 100-iteration measurement — a slow CI runner cannot silently inflate a passing baseline because the budget is fixed, not relative. |
| T-25-EXFIL | Information Disclosure | corpus store network egress | mitigate | `TestCorpusStoreHasNoNetworkImports` statically forbids `net`/`net/http`/`os/exec` in store.go (the only corpus-write path); combined with STORE-03 (owner-only, no remote sink) this proves no corpus data leaves the machine. |
| T-25-OFFLINE-FAILOPEN | Denial of Service / Tampering | offline runCheck decision | mitigate | `TestOfflineProtective` asserts `Decision.Allow == false` (fail-closed BLOCK) for a known-malicious package with no network sources — proving the disconnected machine does not silently fail-open (CLAUDE.md fail-closed-by-default invariant). |
</threat_model>

<verification>
Phase-level checks for this plan:
- `go test ./internal/check/... -run "TestBenchmarkRunCheckGate|TestOfflineProtective" -count=1` exits 0 (LAUNCH-03).
- `go test ./internal/corpus/... -run TestCorpusStoreHasNoNetworkImports -count=1` exits 0 (LAUNCH-04 no-exfil).
- `go test ./internal/check/... ./internal/corpus/... -count=1` exits 0 (no regression).
- `go vet ./...` exits 0; `go build ./...` exits 0.
- `go mod tidy && git diff --exit-code go.mod` (zero new deps).
- `go test ./internal/policy/... -run TestCorroborationImportsArePure -count=1` exits 0 (purity untouched).
</verification>

<success_criteria>
- LAUNCH-03 perf: a deterministic gate proves p99 < 100ms (200ms Windows) with corpus enabled — the carried-over eyeball item is now a CI gate.
- LAUNCH-03 offline: an offline machine fail-closed blocks a known-malicious package on the last-synced catalog.
- LAUNCH-04 (verification half): static AST gate proves store.go has no network/exec import.
- Zero new product code, zero new dependencies, internal/policy purity preserved.
</success_criteria>

<output>
Create `.planning/phases/25-launch-readiness/25-02-SUMMARY.md` when done.
</output>
