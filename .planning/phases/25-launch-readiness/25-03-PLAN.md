---
phase: 25-launch-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/THREAT-MODEL.md
  - cmd/beekeeper/threatmodel_names_test.go
autonomous: false
requirements: [LAUNCH-04]
must_haves:
  truths:
    - "docs/THREAT-MODEL.md has a new §13 stating the corpus is local-first and NAMING all three residual gaps verbatim: SENTRY-008 CI-runner OIDC theft, GitHub API dead-drop exfil, DNS-tunnel ingested-but-undetected (LAUNCH-04 docs)"
    - "A grep tripwire test fails if any of the three residual-gap names is removed from THREAT-MODEL.md"
    - "A maintainer confirms the §13 framing reads honestly (each gap scoped as architectural-mitigation-only), not just present"
  artifacts:
    - path: "docs/THREAT-MODEL.md"
      provides: "§13 Adjudicated Corpus (Local Loop) — v1.4.0, naming the 3 residual gaps + local-first/no-exfil statement"
      contains: "## 13. Adjudicated Corpus (Local Loop)"
    - path: "cmd/beekeeper/threatmodel_names_test.go"
      provides: "TestThreatModelNamesResidualGaps — grep tripwire over the 3 verbatim gap names"
      contains: "func TestThreatModelNamesResidualGaps("
  key_links:
    - from: "cmd/beekeeper/threatmodel_names_test.go"
      to: "docs/THREAT-MODEL.md"
      via: "read file, assert the 3 verbatim residual-gap strings are present"
      pattern: "SENTRY-008 CI-runner OIDC theft"
---

<objective>
Ship the honest-docs half of LAUNCH-04: add a new `## 13. Adjudicated Corpus (Local
Loop) — v1.4.0` section to `docs/THREAT-MODEL.md` that (a) states the corpus is
local-first, append-only, owner-only with no transport in v1, and (b) NAMES the three
residual gaps verbatim: SENTRY-008 CI-runner OIDC theft, GitHub API dead-drop exfil,
and DNS-tunnel ingested-but-undetected. Back the presence requirement with a grep
tripwire test, and gate the framing (not just presence) behind a blocking maintainer
honesty review.

Purpose: LAUNCH-04 treats naming all three gaps as a correctness requirement, not a
courtesy. Understating residual gaps is an information-disclosure / false-confidence
threat. This continues the v1.0.0 through v1.3.0 honesty precedent (every milestone
names its non-defenses). The grep gate proves presence; the human gate proves the
framing is accurate (each gap scoped as architectural-mitigation-only).

Output: a new §13 in `docs/THREAT-MODEL.md` (header "Covers" line updated to include
v1.4.0), a `TestThreatModelNamesResidualGaps` grep tripwire in
`cmd/beekeeper/threatmodel_names_test.go`, and a maintainer sign-off on the framing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-launch-readiness/25-RESEARCH.md
@.planning/phases/25-launch-readiness/25-PATTERNS.md
@.planning/phases/25-launch-readiness/25-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
The following artifacts are CREATED by this phase. Drift verification must treat them as
expected new artifacts:

- Markdown heading: `## 13. Adjudicated Corpus (Local Loop) — v1.4.0` (in `docs/THREAT-MODEL.md`)
- Three named sub-gaps (verbatim): "SENTRY-008 CI-runner OIDC theft", "GitHub API dead-drop exfil", "DNS-tunnel ingested-but-undetected"
- Test function: `TestThreatModelNamesResidualGaps` (in `cmd/beekeeper/threatmodel_names_test.go`)
- Test file: `cmd/beekeeper/threatmodel_names_test.go`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add THREAT-MODEL.md §13 + grep tripwire test (LAUNCH-04 docs)</name>
  <files>docs/THREAT-MODEL.md, cmd/beekeeper/threatmodel_names_test.go</files>
  <read_first>
    - docs/THREAT-MODEL.md — read the header block (lines 1–17: the "**Covers:**" line currently lists v1.0.0 + v1.2.0 + v1.3.0; the "Status"/refresh line), the Table of Contents (lines 27–42; entries §1–§12), and § 8 "Known Gaps and Explicit Non-Defenses" (lines 514–688) so §13 does NOT duplicate the existing DNS-tunnel (line ~681) and SENTRY-008 mentions. §13 reframes them as the v1.4.0 corpus residual-gaps section. Also read § 12 (the existing last numbered section, lines 1135–1216) to copy section style and the closing-line pattern.
    - .planning/REQUIREMENTS.md — LAUNCH-04 (line 63) and the Out-of-Scope rows for the three gaps (lines 106–107): the EXACT verbatim strings to use as sub-headers are "SENTRY-008 CI-runner OIDC theft", "GitHub API dead-drop exfil", "DNS-tunnel ingested-but-undetected". STORE-03 (line 40) for the no-exfil cite.
    - .planning/phases/25-launch-readiness/25-PATTERNS.md — § `docs/THREAT-MODEL.md (MODIFY — LAUNCH-04 docs)`: the full §13 markdown skeleton (3 sub-headers + body) and the "verbatim strings must appear as sub-headers" note.
    - .planning/phases/25-launch-readiness/25-RESEARCH.md — § LAUNCH-04 "What LAUNCH-04 adds to THREAT-MODEL.md" (the three gap descriptions a/b/c) and Pitfall 5 (don't duplicate §8; frame as the corpus residual-gaps section).
    - cmd/beekeeper/catalogs_daemon_test.go — for the test-file package declaration (confirm `package main`), how existing cmd tests locate repo files, and a `t.Fatalf` on read-error pattern to model the grep tripwire on.
  </read_first>
  <action>
    Edit `docs/THREAT-MODEL.md`:
    1. Update the header "**Covers:**" line to append v1.4.0, e.g.
       "... + v1.3.0 (Multi-Harness Hook Enforcement) + v1.4.0 (Adjudicated Corpus, Local Loop)".
       Update the "Status"/refresh line to mention the v1.4.0 refresh.
    2. Add a Table-of-Contents entry for the new section:
       `13. [Adjudicated Corpus (Local Loop) (v1.4.0)](#13-adjudicated-corpus-local-loop--v140)`
       (match the existing TOC anchor style).
    3. Append a new final section `## 13. Adjudicated Corpus (Local Loop) — v1.4.0`. Open
       with a paragraph stating the corpus is local-first, append-only, owner-only
       (0600 / Windows owner-DACL), and that NO corpus data leaves the machine in v1.
       Cite STORE-03 and `TestCorpusStoreHasNoNetworkImports` in
       `internal/corpus/store_test.go` (the static no-`net`/`net/http`/`os/exec` gate from
       plan 25-02). Then add three sub-sections using the EXACT verbatim strings as
       `###` sub-headers:
         - `### SENTRY-008 CI-runner OIDC theft`: tokens stolen from CI runner process
           memory on a machine with no editor/host agent; Beekeeper's `isEditorDescendant`
           ancestry gate cannot observe it. Architectural mitigation only (hardened token
           scoping plus short TTLs at the CI level). Out of host scope.
         - `### GitHub API dead-drop exfil`: a malicious agent that already exfiltrated
           secrets can create a private repo/gist as a dead-drop via the GitHub API; a
           host tool cannot reliably distinguish a legitimate `gh repo create` from a
           malicious one using stolen creds. Out of host scope.
         - `### DNS-tunnel ingested-but-undetected`: DNS query events are captured on
           Linux (eBPF udp_sendmsg/tcp_sendmsg kprobe) and Windows (ETW DNS-Client) but
           `EvaluateEvent`'s `EventDNSQuery` case is an explicit no-op (no correlation rule
           consumes them); DNS-TXT tunneling is an undetected exfil channel until a rule
           is written.
       Each sub-section must be framed as architectural-mitigation-only / out-of-host-scope
       (not "we will fix this") to match the v1.0.0 through v1.3.0 honesty precedent.
       Reframe (do not duplicate) the existing §8 DNS/SENTRY-008 lines: §13 is the coherent
       v1.4.0 corpus residual-gaps section. STYLE: per the project web-style directive, do
       NOT use em-dashes in the §13 prose (use contextual punctuation); the existing §1
       through §12 prose convention applies.

    Create `cmd/beekeeper/threatmodel_names_test.go` (package `main`: confirm the package
    name from `catalogs_daemon_test.go` in the same dir). Add
    `func TestThreatModelNamesResidualGaps(t *testing.T)`: read the repo's
    `docs/THREAT-MODEL.md` via `os.ReadFile`. Resolve the path the same way the existing
    cmd tests locate repo files (check `catalogs_daemon_test.go`); if no precedent exists,
    compute the repo-root from `runtime.Caller(0)` plus `filepath` (the test file is at
    `cmd/beekeeper/`, so the doc is at `../../docs/THREAT-MODEL.md`). Build
    `wantStrings := []string{"SENTRY-008 CI-runner OIDC theft", "GitHub API dead-drop exfil",
    "DNS-tunnel ingested-but-undetected", "## 13. Adjudicated Corpus (Local Loop)"}` and
    for each assert `strings.Contains(string(data), s)`, else
    `t.Errorf("LAUNCH-04: docs/THREAT-MODEL.md must name residual gap %q verbatim (honest-docs correctness requirement)", s)`. `t.Fatalf` on the ReadFile error.
    This is the grep tripwire: removing any of the three names (or the §13 header) turns
    the test red.
  </action>
  <verify>
    <automated>go test ./cmd/beekeeper/... -run TestThreatModelNamesResidualGaps -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `docs/THREAT-MODEL.md` contains the heading `## 13. Adjudicated Corpus (Local Loop)` and the three verbatim sub-headers "SENTRY-008 CI-runner OIDC theft", "GitHub API dead-drop exfil", "DNS-tunnel ingested-but-undetected".
    - The header "**Covers:**" line includes v1.4.0; a TOC entry for §13 exists.
    - §13 states local-first / no-exfil and cites STORE-03 + `TestCorpusStoreHasNoNetworkImports`.
    - §13 prose contains no em-dashes.
    - `cmd/beekeeper/threatmodel_names_test.go` contains `func TestThreatModelNamesResidualGaps(` and asserts all three verbatim strings + the §13 header.
    - `go test ./cmd/beekeeper/... -run TestThreatModelNamesResidualGaps -count=1 -v` exits 0 (PASS).
    - `go vet ./cmd/beekeeper/...` exits 0.
  </acceptance_criteria>
  <done>THREAT-MODEL.md has a v1.4.0 §13 naming all three residual gaps verbatim with local-first/no-exfil framing, and a grep tripwire test fails if any name (or the §13 header) is removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer honesty review of THREAT-MODEL.md §13 framing</name>
  <files>docs/THREAT-MODEL.md</files>
  <action>
    PAUSE for the maintainer. This gate is NOT auto-approvable (`workflow.auto_advance` is
    ignored): editorial honesty cannot be machine-verified, so the grep tripwire in Task 1
    is not sufficient on its own. Present the new §13 of `docs/THREAT-MODEL.md` for review
    using the verification steps below, then WAIT for the maintainer's "approved" (or for
    requested wording changes) before marking the plan complete. If the maintainer requests
    changes, apply them to §13, re-run `TestThreatModelNamesResidualGaps`, and re-present.

    What was built: a new `## 13. Adjudicated Corpus (Local Loop) — v1.4.0` section in
    `docs/THREAT-MODEL.md` that states the corpus is local-first / no-exfil and names the
    three residual gaps verbatim (SENTRY-008 CI-runner OIDC theft, GitHub API dead-drop
    exfil, DNS-tunnel ingested-but-undetected), each scoped as
    architectural-mitigation-only / out-of-host-scope. The grep tripwire test
    (`TestThreatModelNamesResidualGaps`) already proves the three names are PRESENT; this
    checkpoint confirms the FRAMING reads honestly, which a grep cannot verify.

    How to verify (present these to the maintainer):
    1. Open `docs/THREAT-MODEL.md` and read the new §13 (the final section).
    2. Confirm each of the three residual gaps is named AND scoped as
       architectural-mitigation-only / out-of-host-scope, NOT as "will be fixed" or
       understated, consistent with the v1.0.0 through v1.3.0 known-gaps honesty precedent
       (compare tone against the existing §8 "Known Gaps and Explicit Non-Defenses").
    3. Confirm §13 does not OVERCLAIM protection: the local-first / no-exfil statement is
       backed by STORE-03 + the static import gate, and does not imply DNS-tunnel or the
       CI-runner OIDC path is detected.
    4. Confirm the §13 prose contains no em-dashes (project web-style directive) and reads
       consistently with the surrounding sections.
    Per VALIDATION.md "Manual-Only Verifications": editorial honesty cannot be fully
    automated; grep proves presence, the maintainer confirms framing.

    Resume signal: the maintainer types "approved" to accept the §13 framing, or describes
    the wording changes needed.
  </action>
  <verify>
    <human-check>Maintainer reads §13 and types "approved" (framing is honest, scoped as architectural-mitigation-only, no overclaim, no em-dashes), or describes wording changes.</human-check>
  </verify>
  <done>The maintainer has approved the §13 framing (each residual gap named and scoped as architectural-mitigation-only, no overclaim of protection), satisfying the LAUNCH-04 editorial-honesty manual verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator trust | THREAT-MODEL.md sets operator expectations; understated gaps create false confidence (a real, if non-code, attack on the trust boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-DOCS | Information Disclosure (false confidence) | THREAT-MODEL.md §13 residual-gap framing | mitigate | LAUNCH-04 treats naming all three gaps as a correctness requirement: the grep tripwire (`TestThreatModelNamesResidualGaps`) enforces verbatim PRESENCE, and the blocking maintainer checkpoint enforces honest FRAMING (architectural-mitigation-only scoping, no overclaim). |
| T-25-DOCS-OVERCLAIM | Repudiation | §13 local-first / no-exfil statement | mitigate | The no-exfil claim is backed by STORE-03 and the static `TestCorpusStoreHasNoNetworkImports` gate (plan 25-02) cited inline; the claim is machine-checkable, not just prose, preventing an unbacked overclaim. |
</threat_model>

<verification>
Phase-level checks for this plan:
- `go test ./cmd/beekeeper/... -run TestThreatModelNamesResidualGaps -count=1` exits 0 (presence gate).
- Maintainer approves the §13 framing (blocking checkpoint).
- `go test ./cmd/beekeeper/... -count=1` exits 0 (no regression).
- `go vet ./...` exits 0; `go build ./...` exits 0.
- `go mod tidy && git diff --exit-code go.mod` (zero new deps).
- Confirm §13 prose contains no em-dashes (project web-style directive).
</verification>

<success_criteria>
- LAUNCH-04 (docs half): THREAT-MODEL.md §13 names all three residual gaps verbatim with local-first / no-exfil framing, backed by a grep tripwire AND a maintainer honesty sign-off.
- Zero new product code, zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/25-launch-readiness/25-03-SUMMARY.md` when done.
</output>
