---
phase: 04-windows-extension-mcp-coverage-beekeeper-compat-test
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scan/scanner.go
  - internal/scan/scanner_test.go
autonomous: true
requirements: [BKINT-01, PTEST-04]
repo: beekeeper

must_haves:
  truths:
    - "Beekeeper's scan orchestrator invokes `pollen` (not `bumblebee`) via a mockable injectable var; switching back is a one-line lookup-target change"
    - "When `pollen` is not in PATH, beekeeper emits a `pollen_unavailable` scan_status record and the beekeeper-own scan continues (fail-graceful, unchanged behavior)"
    - "The Pollen compatibility test feeds all five record types (npm, editor-extension, browser-extension, mcp, scan_summary) through Scan, asserts scanner_name=pollen, no scan_error, and no double-counting — with zero t.Skip on any OS"
    - "All existing internal/scan tests pass after the bumblebee→pollen rename"
  artifacts:
    - path: "internal/scan/scanner.go"
      provides: "runPollenFn / lookPollenFn / defaultRunPollen injectable seam targeting `pollen`; pollen_unavailable status text"
      contains: "var runPollenFn"
    - path: "internal/scan/scanner_test.go"
      provides: "TestPollenCompatibility (PTEST-04) + TestScanPollenUnavailable; existing tests updated to runPollenFn"
      contains: "func TestPollenCompatibility"
  key_links:
    - from: "internal/scan/scanner.go Scan()"
      to: "runPollenFn(ctx, cfg.Deep)"
      via: "injectable package var (renamed from runBumblebeeFn)"
      pattern: "runPollenFn\\(ctx, cfg.Deep\\)"
    - from: "internal/scan/scanner.go defaultRunPollen"
      to: "exec.LookPath(\"pollen\")"
      via: "lookPollenFn"
      pattern: "exec.LookPath\\(\"pollen\"\\)"
---

<objective>
Swap beekeeper's inventory subprocess boundary from `bumblebee` to `pollen` (BKINT-01) via an in-place rename of the existing injectable-var seam in `internal/scan/scanner.go`, then add the Pollen compatibility test (PTEST-04) that proves all five Pollen record types pass through beekeeper's `Scan` cleanly with correct attribution and no double-counting — running on all three OSes with zero `t.Skip`.

Purpose: BKINT-01 makes beekeeper consume the Windows-compatible fork; PTEST-04 is the integration test the whole milestone exists to enable. The fixture-driven design (no binary spawn) is what drives the Windows inventory-test skip baseline to zero.
Output: Renamed seam in `scanner.go`; `TestPollenCompatibility` + `TestScanPollenUnavailable` in `scanner_test.go`.

Repo: beekeeper (current working directory). Paths are repo-root-relative.
Boundary decision (research-backed, justified): evolve `internal/scan` in place — do NOT create a new `internal/inventory/` package. The injectable-var seam already provides both required properties (replaceability = one-line `exec.LookPath` target change; testability = mock without binary spawn). A new package would add an interface + wrapper + caller updates with zero test benefit; the formal module boundary, if ever needed, belongs to Phase 5's BKINT-02 (go.mod version pin). This matches the Phase-3 RESEARCH conclusion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-CONTEXT.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-RESEARCH.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-VALIDATION.md

<interfaces>
<!-- Current scanner.go seam (VERIFIED from live source). These are the exact identifiers to rename. -->

internal/scan/scanner.go:
  var lookBumblebee = func() (string, error) { return exec.LookPath("bumblebee") }
  var runBumblebeeFn = func(ctx context.Context, deep bool) (<-chan []byte, bool) { return defaultRunBumblebee(ctx, deep) }
  func defaultRunBumblebee(ctx context.Context, deep bool) (<-chan []byte, bool)  // calls lookBumblebee(), exec.CommandContext(bin,"scan"[,"--profile","deep"]), streams stdout NDJSON over a channel
  Scan(ctx, cfg, out):  ch, ok := runBumblebeeFn(ctx, cfg.Deep) ...
    scan_error record: {"record_type":"scan_error","scanner_name":"beekeeper","source":"bumblebee","error":"malformed NDJSON from bumblebee subprocess"}
    scan_status record (else branch): {"record_type":"scan_status","bumblebee_unavailable":true,"scanner_name":"beekeeper"}
    fmt.Errorf("write bumblebee_unavailable status: %w", err)
  Scan signature: func Scan(ctx context.Context, cfg Config, out io.Writer) error  // Config{} with no ExtensionDirs skips beekeeperScan (no beekeeper-own records)

internal/scan/scanner_test.go (existing tests referencing the seam — must be updated):
  TestScanWithBumblebee        (line ~17-44)  uses runBumblebeeFn
  TestScanWindowsShapedRecord  (line ~46-91)  uses runBumblebeeFn
  TestScanBumblebeeUnavailable (line ~93-...)  uses runBumblebeeFn; asserts `"bumblebee_unavailable":true`

NOTE: the `bumblebee` string literals elsewhere in beekeeper (internal/tui, internal/gateway, internal/watch, internal/catalog — e.g. bumblebee.idx, CatalogSource:"bumblebee") refer to the mmap THREAT-INTEL CATALOG source, NOT the pollen scan subprocess. They are OUT OF SCOPE for BKINT-01 — do NOT rename them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the bumblebee→pollen subprocess seam in scanner.go (BKINT-01)</name>
  <read_first>
    - internal/scan/scanner.go (the full file — lines 55-151: the three injectable declarations `lookBumblebee`/`runBumblebeeFn`/`defaultRunBumblebee`, the `Scan` function body with `runBumblebeeFn(ctx, cfg.Deep)`, the scan_error `"source":"bumblebee"` map, the else-branch `"bumblebee_unavailable":true` scan_status map, and the `fmt.Errorf("write bumblebee_unavailable status...")`; also the package doc comment lines 1-5 referencing "Bumblebee CLI")
    - .planning/phases/03-windows-path-representation/03-RESEARCH.md (the BKINT-01 deferral rationale — confirms in-place evolution over a new internal/inventory/ package)
  </read_first>
  <action>
    In `internal/scan/scanner.go`, rename the three injectable identifiers and update the status strings, preserving the exact signatures and channel semantics so existing tests compile with a mechanical rename: `lookBumblebee` → `lookPollenFn` with `exec.LookPath("pollen")` (was `"bumblebee"`); `runBumblebeeFn` → `runPollenFn` (calls `defaultRunPollen`); `defaultRunBumblebee` → `defaultRunPollen` (body identical, just calls `lookPollenFn()` and keeps the `scan` / `--profile deep` args). In `Scan`: change the call site to `runPollenFn(ctx, cfg.Deep)`; in the scan_error map change `"source":"bumblebee"` → `"source":"pollen"` and the error text `"malformed NDJSON from bumblebee subprocess"` → `"malformed NDJSON from pollen subprocess"`; in the else-branch scan_status map change the key `"bumblebee_unavailable"` → `"pollen_unavailable"`; update `fmt.Errorf("write bumblebee_unavailable status: %w", err)` → `"write pollen_unavailable status: %w"`. Update the package/function doc comments that say "Bumblebee CLI" → "Pollen CLI". Do NOT touch `scanOnDeltaFn` (it lives in cmd/beekeeper/main.go and is a separate outer layer — research VERIFIED it does not reference this seam). Do NOT rename any `bumblebee`/`bumblebee.idx`/`CatalogSource:"bumblebee"` literals in internal/tui, internal/gateway, internal/watch, internal/catalog — those are the threat-intel catalog source, out of scope for BKINT-01.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/scan/</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `internal/scan/scanner.go` declares `var lookPollenFn`, `var runPollenFn`, `func defaultRunPollen`; `exec.LookPath("pollen")` is present and `exec.LookPath("bumblebee")` is absent from this file.
    - SOURCE: the scan_status else-branch emits `"pollen_unavailable":true` (the literal `"bumblebee_unavailable"` no longer appears in scanner.go); scan_error `"source"` is `"pollen"`.
    - SOURCE: no `bumblebee` string literals remain in `internal/scan/scanner.go` (verify: `Select-String -Path internal/scan/scanner.go -Pattern bumblebee` returns nothing).
    - SOURCE: `cmd/beekeeper/main.go` `scanOnDeltaFn` is untouched; the catalog-source `bumblebee` literals in tui/gateway/watch/catalog are unchanged.
    - BEHAVIOR: `go build ./...` exits 0 (whole repo still compiles — note scanner_test.go still references the old names until Task 2, so `go test` is deferred to Task 2; `go build ./...` excludes _test.go files and must pass now).
  </acceptance_criteria>
  <done>The internal/scan subprocess seam targets `pollen` with `pollen_unavailable` status; catalog-source bumblebee literals untouched; `go build ./...` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update existing scan tests to runPollenFn + add TestScanPollenUnavailable + TestPollenCompatibility (PTEST-04)</name>
  <read_first>
    - internal/scan/scanner_test.go (the full file — the three existing tests `TestScanWithBumblebee`/`TestScanWindowsShapedRecord`/`TestScanBumblebeeUnavailable` and their `old := runBumblebeeFn` / `runBumblebeeFn = func(...)` injection pattern; the `TestScanBumblebeeUnavailable` assertion on `"bumblebee_unavailable":true`; the imports block: bytes, context, encoding/json, net/http, os, path/filepath, strings, testing, time, catalog)
    - internal/scan/scanner.go (the renamed `runPollenFn` var from Task 1 and the Scan() pass-through behavior + the `pollen_unavailable` status text — the test asserts against these)
    - .planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-RESEARCH.md (Pattern 4 — the TestPollenCompatibility fixture list and assertion shape; Pitfall 6 — pass `Config{}` with no ExtensionDirs so beekeeperScan emits no records and double-counting is measured on Pollen-origin source_file uniqueness only)
  </read_first>
  <behavior>
    - Rename regression: `TestScanWithBumblebee`, `TestScanWindowsShapedRecord`, `TestScanBumblebeeUnavailable` use `runPollenFn` (not the removed `runBumblebeeFn`) and still pass.
    - TestScanPollenUnavailable: with `runPollenFn` injected to return `(nil, false)`, Scan emits a scan_status record containing `"pollen_unavailable":true` and the beekeeper-own scan still runs (no error).
    - TestPollenCompatibility (PTEST-04): inject `runPollenFn` to emit 5 fixture lines — npm package, editor-extension, browser-extension, mcp, scan_summary — each `scanner_name="pollen"` with Windows-shaped backslash+drive-letter paths. After Scan with `Config{}` (no ExtensionDirs):
        - no `"record_type":"scan_error"` appears (all five accepted as valid NDJSON);
        - `scanner_name":"pollen"` appears on the ≥4 non-summary records (attribution preserved);
        - no double-counting: each non-summary record's `source_file` appears exactly once in the output.
    - Zero `t.Skip`: TestPollenCompatibility and TestScanPollenUnavailable have NO t.Skip — fixture-driven, no binary spawn, runs identically on ubuntu/macos/windows.
  </behavior>
  <action>
    In `internal/scan/scanner_test.go`: (a) mechanically update the three existing tests' `old := runBumblebeeFn` / `defer ... runBumblebeeFn = old` / `runBumblebeeFn = func(...)` references to `runPollenFn`; in `TestScanBumblebeeUnavailable` change the assertion `"bumblebee_unavailable":true` → `"pollen_unavailable":true` and rename the function to `TestScanPollenUnavailable` (per VALIDATION.md command `go test ./internal/scan/ -run TestScanPollenUnavailable`). (b) Add `TestPollenCompatibility` following RESEARCH Pattern 4: save+defer-restore `runPollenFn`, inject a func returning a buffered channel of the 5 fixture NDJSON lines (npm / editor-extension / browser-extension / mcp / scan_summary — all `scanner_name":"pollen"`, Windows-shaped `source_file` with `C:\\...` JSON-escaped backslashes), call `Scan(context.Background(), Config{}, &buf)` (Config{} → beekeeperScan skipped, Pitfall 6), then assert: no `"record_type":"scan_error"` substring; `strings.Count(out, "\"scanner_name\":\"pollen\"") >= 4`; for each of the 4 non-summary fixtures, json-marshal its source_file and assert `strings.Count(out, marshaledSourceFile) == 1` (no double-counting). NO `t.Skip` anywhere in either new test.
  </action>
  <verify>
    <automated>go test ./internal/scan/</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `go test ./internal/scan/` passes (all tests green, including the three renamed regression tests).
    - BEHAVIOR: `go test ./internal/scan/ -run TestPollenCompatibility` passes.
    - BEHAVIOR: `go test ./internal/scan/ -run TestScanPollenUnavailable` passes and the test asserts `"pollen_unavailable":true`.
    - SOURCE: `scanner_test.go` contains no `runBumblebeeFn` references (verify: `Select-String -Path internal/scan/scanner_test.go -Pattern runBumblebeeFn` returns nothing).
    - VERIFICATION (PTEST-04 zero-skip): `go test ./internal/scan/ -v 2>&1 | Select-String SKIP` returns no lines (zero skipped inventory tests on any OS).
    - SOURCE: TestPollenCompatibility feeds exactly the five record types and asserts scanner_name=pollen, no scan_error, and source_file uniqueness (no double-counting).
  </acceptance_criteria>
  <done>All internal/scan tests pass with the runPollenFn seam; TestPollenCompatibility + TestScanPollenUnavailable added; zero t.Skip in inventory tests; PTEST-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pollen` subprocess stdout → beekeeper Scan | NDJSON lines from the child process are parsed; malformed lines are already converted to a `scan_error` record (fail-closed on bad input — unchanged behavior). |
| PATH → `exec.LookPath("pollen")` | The resolved binary name comes from the user-controlled PATH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | Attacker plants a fake `pollen` binary earlier in PATH | accept | Identical pre-existing risk to `bumblebee` — the user's PATH is the user's trust domain; the rename introduces no new surface. No mitigation added (would require binary pinning/signature checks out of scope for this phase; tracked conceptually under SDEF-01 self-catalog in Phase 5). |
| T-04-04 | Tampering | Malformed/hostile NDJSON on the subprocess stdout stream | mitigate | Existing fail-closed guard preserved: `json.Unmarshal` probe per line; malformed lines emit a `scan_error` record (`source:"pollen"`) rather than being passed through. TestPollenCompatibility verifies valid records are NOT mis-flagged; the unchanged malformed-line path remains covered by existing behavior. |
</threat_model>

<verification>
- `go build ./...` exits 0 after Task 1 (whole repo compiles; catalog-source bumblebee literals untouched and still valid).
- `go test ./internal/scan/` green after Task 2.
- `go test ./internal/scan/ -v 2>&1 | Select-String SKIP` is empty (PTEST-04 zero-skip baseline).
- `go test ./...` full beekeeper suite green (confirms no cross-package breakage from the rename).
</verification>

<success_criteria>
- BKINT-01: beekeeper invokes `pollen` behind the mockable injectable seam; switching back to upstream is a one-line `exec.LookPath` target change; unit tests need no binary (SC4 boundary).
- PTEST-04: compatibility test passes all five record types through, asserts `scanner_name="pollen"` and no double-counting, runs on all three OSes with zero `t.Skip` (SC4).
- No schema/CLI change; `pollen_unavailable` fail-graceful path preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-02-SUMMARY.md`
</output>
