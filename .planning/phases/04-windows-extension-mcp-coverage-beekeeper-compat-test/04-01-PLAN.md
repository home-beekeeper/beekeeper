---
phase: 04-windows-extension-mcp-coverage-beekeeper-compat-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../pollen/cmd/pollen/roots.go
  - ../pollen/internal/ecosystem/editorext/editorext.go
  - ../pollen/cmd/pollen/roots_windows_test.go
autonomous: true
requirements: [WEXT-01, WEXT-02, WEXT-03]
repo: pollen

must_haves:
  truths:
    - "On Windows, resolveRoots(baseline) returns editor-extension roots for VS Code, VS Code Insiders, Cursor, Windsurf, and VSCodium (both .vscodium and .vscode-oss)"
    - "On Windows, resolveRoots(baseline) returns per-profile browser-extension roots for Chrome, Chromium, Edge, Brave under %LOCALAPPDATA% and the Firefox Profiles parent under %APPDATA%"
    - "On Windows, resolveRoots(baseline) returns MCP config roots for Claude Desktop (%APPDATA%\\Claude), Cline (%APPDATA%\\cline), Cursor (%USERPROFILE%\\.cursor), Windsurf (%USERPROFILE%\\.windsurf), and Gemini CLI (%USERPROFILE%\\.gemini)"
    - "Linux and macOS root enumeration is byte-for-byte unchanged — the differential test stays green"
  artifacts:
    - path: "../pollen/cmd/pollen/roots.go"
      provides: "Filled case windows: blocks in browserExtensionCandidateRoots (chromium + firefox) + Windows MCP entries + unconditional .windsurf MCP root + .vscode-oss editor segment"
      contains: "case \"windows\":"
    - path: "../pollen/internal/ecosystem/editorext/editorext.go"
      provides: ".vscode-oss/extensions added to extensionRootSegments"
      contains: ".vscode-oss/extensions"
    - path: "../pollen/cmd/pollen/roots_windows_test.go"
      provides: "TestWindowsExtensionMCPRoots asserting all editor/browser/MCP roots resolve under a fake %USERPROFILE% tree"
      contains: "func TestWindowsExtensionMCPRoots"
  key_links:
    - from: "../pollen/cmd/pollen/roots.go browserExtensionCandidateRoots case windows:"
      to: "os.Getenv(\"LOCALAPPDATA\") / os.Getenv(\"APPDATA\")"
      via: "env-var-guarded filepath.Join (never the home parameter for Chromium)"
      pattern: "os.Getenv\\(\"LOCALAPPDATA\"\\)"
    - from: "../pollen/cmd/pollen/roots.go baselineHomeCandidates"
      to: "model.RootKindMCPConfig for .windsurf + %APPDATA%\\Claude + %APPDATA%\\cline"
      via: "unconditional add(.windsurf) + case windows: APPDATA adds"
      pattern: "add\\(filepath.Join\\(home, \".windsurf\"\\)"
---

<objective>
Complete Pollen's Windows surface coverage: fill the empty `case "windows":` browser-extension skeletons, add the Windows MCP host-config roots (plus the missing unconditional `.windsurf` MCP root), and add the VSCodium `.vscode-oss` editor segment so all five editors / five browser families / five MCP hosts from PRD §8.2–8.4 resolve on Windows.

Purpose: This is the last gap in Pollen's Windows path coverage (WEXT-01/02/03). Once it lands, `pollen scan` on a Windows machine enumerates every editor-extension dir, browser-extension profile, and MCP config file the milestone requires.
Output: Modified `roots.go` + `editorext.go` in `../pollen`, plus a new Windows-only `TestWindowsExtensionMCPRoots`.

Repo: all work in the SIBLING `../pollen` clone (`C:\Users\Bantu\mzansi-agentive\pollen`). All `files_modified`, `<read_first>`, and commands use `../pollen/...` paths and `cd ../pollen`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-CONTEXT.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-RESEARCH.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-VALIDATION.md

<prd_paths>
<!-- PRD §8.2-8.4 LOCKED path strings — copy verbatim into the resolver tables, do not paraphrase -->

WEXT-01 editor extensions (under %USERPROFILE%):
  VS Code           → .vscode\extensions\          (already wired via .vscode/extensions segment)
  VS Code Insiders  → .vscode-insiders\extensions\ (already wired)
  Cursor            → .cursor\extensions\          (already wired — validates Phase-3 Assumption A1)
  Windsurf          → .windsurf\extensions\        (already wired)
  VSCodium          → .vscode-oss\extensions\      (GAP — PRD wants .vscode-oss; live has only .vscodium; ADD BOTH)

WEXT-02 browser extensions:
  Chrome    → %LOCALAPPDATA%\Google\Chrome\User Data\<Profile>\Extensions\
  Chromium  → %LOCALAPPDATA%\Chromium\User Data\<Profile>\Extensions\
  Edge      → %LOCALAPPDATA%\Microsoft\Edge\User Data\<Profile>\Extensions\
  Brave     → %LOCALAPPDATA%\BraveSoftware\Brave-Browser\User Data\<Profile>\Extensions\
  Firefox   → %APPDATA%\Mozilla\Firefox\Profiles\<profile>\extensions.json  (add the Profiles PARENT only)

WEXT-03 MCP host-config (under %APPDATA% or %USERPROFILE%):
  Claude Desktop → %APPDATA%\Claude\claude_desktop_config.json   (add %APPDATA%\Claude)
  Cline          → %APPDATA%\cline\cline_mcp_settings.json        (add %APPDATA%\cline)
  Cursor MCP     → %USERPROFILE%\.cursor\mcp.json                 (already covered by .cursor root)
  Windsurf MCP   → %USERPROFILE%\.windsurf\mcp.json               (GAP — .codeium/windsurf ≠ .windsurf; ADD .windsurf unconditionally)
  Gemini CLI     → %USERPROFILE%\.gemini\settings.json            (already covered by .gemini root)
</prd_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Windows browser-extension skeletons in browserExtensionCandidateRoots</name>
  <read_first>
    - ../pollen/cmd/pollen/roots.go (lines 510-598 — browserExtensionCandidateRoots; note the two empty `case "windows":` skeletons at the chromiumBases switch ~560-563 and the Firefox switch ~593-596, the shared chromiumProfiles slice at ~530, and the darwin/linux cases as the pattern to mirror)
    - ../pollen/cmd/pollen/roots_windows.go (the Phase-2 env-var-guard pattern: `if v := os.Getenv("LOCALAPPDATA"); v != "" { ... }` — per-variable guards, never the `home` parameter for Chromium)
    - ../pollen/internal/ecosystem/browserext/browserext.go (IsFirefoxExtensionsJSON uses filepath.ToSlash before the Contains check — confirm NO change is needed: the Windows path normalizes to .../Mozilla/Firefox/Profiles/... which already matches; do not add a Windows variant)
  </read_first>
  <action>
    In `../pollen/cmd/pollen/roots.go`, fill the empty `case "windows":` block in the `chromiumBases` switch inside `browserExtensionCandidateRoots`: guard with `if localappdata := os.Getenv("LOCALAPPDATA"); localappdata != "" {`, then set `chromiumBases["chrome"]` = `[filepath.Join(localappdata,"Google","Chrome","User Data")]`, `chromiumBases["chromium"]` = `[...,"Chromium","User Data"]`, `chromiumBases["edge"]` = `[...,"Microsoft","Edge","User Data"]`, `chromiumBases["brave"]` = `[...,"BraveSoftware","Brave-Browser","User Data"]`. The existing per-profile loop (`for base/profile: append filepath.Join(b, prof, "Extensions")`) already runs for all OSes and uses the shared `chromiumProfiles` slice — do NOT duplicate it. Fill the empty `case "windows":` block in the Firefox switch: guard with `if appdata := os.Getenv("APPDATA"); appdata != "" {`, then `roots = append(roots, filepath.Join(appdata,"Mozilla","Firefox","Profiles"))` — the PARENT Profiles dir only (the walker recurses into per-profile subdirs; Pitfall 5). Use `os.Getenv`, never the `home` parameter (Pitfall 4). Do NOT touch IsFirefoxExtensionsJSON or IsChromiumExtensionManifest — they are OS-agnostic (verified). Do NOT hardcode any backslash or `C:\` literal — only `filepath.Join + os.Getenv` (Pitfall 1).
  </action>
  <verify>
    <automated>cd ../pollen && go build ./... && go vet ./cmd/pollen/</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `roots.go` `browserExtensionCandidateRoots` chromium `case "windows":` populates chrome/chromium/edge/brave from `os.Getenv("LOCALAPPDATA")` behind a non-empty guard; Firefox `case "windows":` appends the `%APPDATA%\Mozilla\Firefox\Profiles` parent behind a non-empty `os.Getenv("APPDATA")` guard.
    - SOURCE: no hardcoded `\\` or `C:\` string literals introduced; all paths via `filepath.Join`.
    - SOURCE: `browserext.go` `IsFirefoxExtensionsJSON` is unchanged (research VERIFIED it already handles Windows via filepath.ToSlash).
    - BEHAVIOR: `cd ../pollen && go build ./...` exits 0 and `go vet ./cmd/pollen/` is clean.
  </acceptance_criteria>
  <done>Both Windows browser skeletons are filled with env-var-guarded LOCALAPPDATA/APPDATA paths; build + vet pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Windows MCP roots + unconditional .windsurf + VSCodium .vscode-oss segment</name>
  <read_first>
    - ../pollen/cmd/pollen/roots.go (lines 244-298 — baselineHomeCandidates: the editor `extensionRootSegments` loop ~245-256 with `.vscodium/extensions` present and `.vscode-oss/extensions` ABSENT; the cross-platform MCP `add(...)` block ~264-268 which has .cursor/.codeium-windsurf/.claude/.codex/.gemini but NOT .windsurf; the MCP `switch runtime.GOOS` at ~269-276 with darwin/linux only)
    - ../pollen/internal/ecosystem/editorext/editorext.go (lines 38-47 — extensionRootSegments package var with `.vscodium/extensions`; lines 124-134 — hostFromExtRoot maps `/.vscodium` → "vscodium" and falls through to "vscode" otherwise)
    - ../pollen/internal/ecosystem/mcp/mcp.go (IsKnownMCPConfig + IsGeminiSettingsJSON — confirm they already dispatch on basename, so no scanner change is needed for the new roots)
  </read_first>
  <action>
    In `../pollen/cmd/pollen/roots.go` `baselineHomeCandidates`: (a) add `".vscode-oss/extensions"` to the editor-extension `seg` slice (KEEP `.vscodium/extensions` for upstream parity — per research recommendation, add BOTH so both VSCodium install variants resolve); (b) add one unconditional MCP root `add(filepath.Join(home, ".windsurf"), model.RootKindMCPConfig)` to the cross-platform block (Pitfall 2 — `.codeium/windsurf` does NOT cover `%USERPROFILE%\.windsurf\mcp.json`; benefits all OSes); (c) add a `case "windows":` to the MCP `switch runtime.GOOS` that, guarded by `if appdata := os.Getenv("APPDATA"); appdata != "" {`, calls `add(filepath.Join(appdata,"Claude"), model.RootKindMCPConfig)` and `add(filepath.Join(appdata,"cline"), model.RootKindMCPConfig)` (Pitfall 3 — env-var guard required). In `../pollen/internal/ecosystem/editorext/editorext.go`: add `".vscode-oss/extensions"` to the `extensionRootSegments` package var (KEEP `.vscodium/extensions`) so `IsExtensionPackageJSON` matches a `.vscode-oss\extensions\<id>\package.json` fixture (Pitfall 1 / VSCodium gap). Do NOT modify `hostFromExtRoot` — a `.vscode-oss` root resolving to host "vscode" is acceptable (record still emitted). Cursor/Windsurf/Gemini MCP need no Windows-case additions — they are covered by the unconditional `.cursor`/`.windsurf`/`.gemini` dotfile roots since `os.UserHomeDir()` returns `%USERPROFILE%` on Windows (A4).
  </action>
  <verify>
    <automated>cd ../pollen && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `roots.go` editor `seg` slice contains BOTH `.vscodium/extensions` and `.vscode-oss/extensions`.
    - SOURCE: `roots.go` cross-platform MCP block contains `add(filepath.Join(home, ".windsurf"), model.RootKindMCPConfig)`.
    - SOURCE: `roots.go` MCP `switch runtime.GOOS` has a `case "windows":` adding `%APPDATA%\Claude` and `%APPDATA%\cline` behind a non-empty `os.Getenv("APPDATA")` guard.
    - SOURCE: `editorext.go` `extensionRootSegments` contains BOTH `.vscodium/extensions` and `.vscode-oss/extensions`; `hostFromExtRoot` unchanged.
    - BEHAVIOR: `cd ../pollen && go build ./... && go vet ./...` exit 0.
  </acceptance_criteria>
  <done>Windows MCP roots added with env-var guard; .windsurf MCP root added unconditionally; .vscode-oss segment added to both lists; build + vet pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestWindowsExtensionMCPRoots — Windows-only fixture test for WEXT-01/02/03 root discovery</name>
  <read_first>
    - ../pollen/cmd/pollen/roots_windows_test.go (the existing `TestWindowsBaselineRoots` — the canonical pattern: `t.TempDir()`, `t.Setenv("USERPROFILE"/"APPDATA"/"LOCALAPPDATA"/"ProgramFiles")`, an inline `mustMkdir` helper, then `resolveRoots(model.ProfileBaseline, ...)` and path-based assertions; `//go:build windows` header)
    - ../pollen/cmd/pollen/roots.go (the resolveRoots signature + filterExistingRoots — only roots whose dir exists are returned, so the test must mkdir each expected root before asserting it appears)
    - ../pollen/internal/model (RootKind constants: RootKindEditorExtension, RootKindBrowserExtension, RootKindMCPConfig — referenced by the assertion)
  </read_first>
  <behavior>
    - Editor (WEXT-01): after planting `.vscode/extensions`, `.vscode-insiders/extensions`, `.cursor/extensions`, `.windsurf/extensions`, `.vscode-oss/extensions` under the fake USERPROFILE, resolveRoots(baseline) returns a root for each of the five.
    - Browser (WEXT-02): after planting `%LOCALAPPDATA%\Google\Chrome\User Data\Default\Extensions` (and at least one more Chromium family, e.g. Brave) and `%APPDATA%\Mozilla\Firefox\Profiles\<fake-profile>`, resolveRoots(baseline) returns the corresponding browser-extension roots.
    - MCP (WEXT-03): after planting `%APPDATA%\Claude`, `%APPDATA%\cline`, `%USERPROFILE%\.cursor`, `%USERPROFILE%\.windsurf`, `%USERPROFILE%\.gemini`, resolveRoots(baseline) returns an MCP-config root for each of the five hosts.
    - Isolation: env set via t.Setenv(USERPROFILE/APPDATA/LOCALAPPDATA/ProgramFiles) ONLY — never HOME (Pitfall 7 / Phase-2 Pitfall 5).
  </behavior>
  <action>
    Add `TestWindowsExtensionMCPRoots` to `../pollen/cmd/pollen/roots_windows_test.go` (file already has the `//go:build windows` header — keep it). Follow the `TestWindowsBaselineRoots` structure exactly: create a `t.TempDir()`, derive `appdata`/`localappdata` under it, `t.Setenv` all four Windows env vars (USERPROFILE/APPDATA/LOCALAPPDATA/ProgramFiles — never HOME), define an inline `mustMkdir` helper (do NOT redefine the package-level helper if one exists — use a local closure as TestWindowsBaselineRoots does), mkdir every editor/browser/MCP directory listed in <behavior>, call `resolveRoots(model.ProfileBaseline, nil, rootsOpts{})` (match the exact signature used by TestWindowsBaselineRoots), and assert (by RootKind + path substring) that each of the five editor roots, the planted browser roots, and the five MCP roots appears in the returned slice. Use path-substring assertions tolerant of separators (e.g. compare against `filepath.Join`-built expected paths). Assert at least one Chromium-family `...\User Data\Default\Extensions` root and the Firefox `...\Mozilla\Firefox\Profiles` parent are present. NO `t.Skip` anywhere — the build tag already restricts this to Windows CI.
  </action>
  <verify>
    <automated>cd ../pollen && go test ./cmd/pollen/ -run TestWindowsExtensionMCPRoots</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `cd ../pollen && go test ./cmd/pollen/ -run TestWindowsExtensionMCPRoots` passes on Windows (the dev machine is Windows 11, so this runs locally).
    - SOURCE: the test sets USERPROFILE, APPDATA, LOCALAPPDATA, ProgramFiles via `t.Setenv` and never references HOME.
    - SOURCE: the test contains zero `t.Skip` calls.
    - BEHAVIOR: assertions cover all five editors, ≥2 browser roots (≥1 Chromium-family Extensions dir + Firefox Profiles parent), and all five MCP hosts (Claude, Cline, Cursor, Windsurf, Gemini).
    - REGRESSION: `cd ../pollen && go test ./cmd/pollen/ -run 'TestDifferential|TestParityAllEcosystems'` still passes (Linux/macOS bytes unchanged — though TestDifferential is CI-gated; TestParityAllEcosystems runs locally where applicable).
  </acceptance_criteria>
  <done>TestWindowsExtensionMCPRoots passes on Windows asserting all WEXT-01/02/03 root discovery; zero t.Skip; differential/parity regression tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS filesystem → Pollen path enumerator | Untrusted directory tree under %LOCALAPPDATA%/%APPDATA%/%USERPROFILE% is read (existence-checked) to build the candidate root list. Read-only; no execution. |
| Environment variables → path construction | LOCALAPPDATA/APPDATA values are joined into candidate paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Windows junction point under `%LOCALAPPDATA%\Google\Chrome\User Data\` redirecting enumeration to a sensitive area | accept | Carried forward from Phase-2 T-02-02 (accepted, bounded): worst case is double-counting; `filterExistingRoots` uses `os.Stat` to confirm is-dir; read-only enumeration, no content exfiltration. Phase-2 open research flag (junction behavior under %LOCALAPPDATA%) tracked, validated on Windows CI. |
| T-04-02 | Elevation of Privilege | Unset `LOCALAPPDATA`/`APPDATA` producing a relative path via `filepath.Join("", ...)` walked from CWD | mitigate | Per-variable env-var guard `if v := os.Getenv("LOCALAPPDATA"); v != "" { ... }` (Phase-2 pattern) — a candidate is only added when the env var is non-empty, so no relative-path enumeration can occur. Enforced in both Task 1 and Task 2. |
</threat_model>

<verification>
- `cd ../pollen && go build ./...` exits 0 (all three GOOS compile — the Windows additions are env-var-guarded inside existing `case "windows":` blocks, no new build-tagged non-test files).
- `cd ../pollen && go vet ./...` clean.
- `cd ../pollen && go test ./cmd/pollen/ -run TestWindowsExtensionMCPRoots` passes on the Windows dev machine.
- `cd ../pollen && go test ./...` full suite green.
- Differential regression (`TestDifferential`) confirmed via CI (Linux/macOS); locally `TestParityAllEcosystems` green.
</verification>

<success_criteria>
- WEXT-01: all five editor-extension roots (VS Code, Insiders, Cursor, Windsurf, VSCodium via both `.vscodium` and `.vscode-oss`) resolve on Windows (SC1).
- WEXT-02: Chrome/Chromium/Edge/Brave per-profile + Firefox Profiles roots resolve on Windows (SC2).
- WEXT-03: Claude Desktop, Cline, Cursor, Windsurf, Gemini MCP config roots resolve on Windows (SC3).
- No Linux/macOS regression; no schema/CLI change (schema_version stays 0.1.0).
</success_criteria>

<output>
After completion, create `.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-01-SUMMARY.md`
</output>
