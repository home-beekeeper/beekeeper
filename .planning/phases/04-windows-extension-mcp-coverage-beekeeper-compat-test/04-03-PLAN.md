---
phase: 04-windows-extension-mcp-coverage-beekeeper-compat-test
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ../pollen/VERSION
  - ../pollen/CHANGES.md
autonomous: true
requirements: [WEXT-01, WEXT-02, WEXT-03]
repo: pollen

must_haves:
  truths:
    - "../pollen/VERSION reads 0.1.1-pollen.4"
    - "../pollen/CHANGES.md has a new dated v0.1.1-pollen.4 section documenting the WEXT-01/02/03 deltas, marked 'prepared, not yet tagged' with the D-06 deferral note"
    - "No git tag, no cosign signing, no push is performed — the signed release is batched to M2 close"
  artifacts:
    - path: "../pollen/VERSION"
      provides: "Milestone version string for the Phase 4 release"
      contains: "0.1.1-pollen.4"
    - path: "../pollen/CHANGES.md"
      provides: "Dated delta section for the WEXT extension/MCP coverage work"
      contains: "v0.1.1-pollen.4"
  key_links:
    - from: "../pollen/CHANGES.md v0.1.1-pollen.4 section"
      to: "the WEXT-01/02/03 roots.go + editorext.go changes from plan 04-01"
      via: "Modified-files list naming roots.go and editorext.go"
      pattern: "roots.go"
---

<objective>
Prepare the `v0.1.1-pollen.4` release LOCALLY in the `../pollen` repo: bump `VERSION` to `0.1.1-pollen.4` and append a dated CHANGES.md section documenting the Phase 4 WEXT-01/02/03 extension/browser/MCP coverage deltas. Following the locked D-06 maintainer decision (and the Phase 2/3 precedent), the SIGNED git tag + cosign + push are DEFERRED to milestone (M2) close — this plan does NOT tag, sign, or push.

Purpose: keeps the release metadata in lockstep with the code (mirrors plans 02-04 and 03-03) so the batched M2-close signed release has accurate VERSION + CHANGES already committed.
Output: updated `../pollen/VERSION` and `../pollen/CHANGES.md`.

Repo: the SIBLING `../pollen` clone. Commands use `cd ../pollen`.
This depends on plan 04-01 having landed (the CHANGES section describes the code that plan produces).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-CONTEXT.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump VERSION and append the v0.1.1-pollen.4 CHANGES.md section (release-prep only — no tag)</name>
  <read_first>
    - ../pollen/VERSION (currently `0.1.1-pollen.3` — single-line version string)
    - ../pollen/CHANGES.md (the existing structure: a dated `## v0.1.1-pollen.N (date) — title` heading, a "Status: prepared, not yet tagged" blockquote with the D-06 deferral note referencing batched signed releases, then `### Modified` / `### Added` subsections. Mirror the v0.1.1-pollen.3 section format exactly.)
    - .planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-01-PLAN.md (the exact files + deltas to credit: roots.go Windows browser skeletons + Windows MCP roots + unconditional .windsurf + .vscode-oss segment; editorext.go .vscode-oss segment; roots_windows_test.go TestWindowsExtensionMCPRoots)
  </read_first>
  <action>
    Set `../pollen/VERSION` to exactly `0.1.1-pollen.4` (single line, no trailing prose). Prepend a new dated section to `../pollen/CHANGES.md` ABOVE the v0.1.1-pollen.3 section, titled `## v0.1.1-pollen.4 (2026-06-02) — Windows extension & MCP coverage`. Include the same "prepared, not yet tagged" status blockquote used by the v0.1.1-pollen.3 section, stating the signed `v0.1.1-pollen.4` git tag + Sigstore signing + CycloneDX SBOM are DEFERRED to M2 close per D-06, batched together with the pending pollen.2/pollen.3 signed releases; point to beekeeper STATE.md Deferred Items for the pending tag/verify commands. State that satisfies WEXT-01 (5 editor-extension roots incl. VSCodium via both .vscodium and .vscode-oss), WEXT-02 (Chrome/Chromium/Edge/Brave per-profile + Firefox Profiles on Windows), WEXT-03 (Claude Desktop + Cline via %APPDATA%, Cursor/Windsurf/Gemini via %USERPROFILE% dotfiles, plus the new unconditional .windsurf MCP root), and that schema_version stays 0.1.0 and the Linux/macOS differential stays byte-identical (Windows-only code paths). Add a `### Modified` subsection listing `cmd/pollen/roots.go` (browser skeletons filled, Windows MCP case added, .windsurf MCP root, .vscode-oss editor segment), `internal/ecosystem/editorext/extensionRootSegments` (.vscode-oss added), and an `### Added` subsection listing `cmd/pollen/roots_windows_test.go` (TestWindowsExtensionMCPRoots). Do NOT run `git tag`, `cosign`, `git push`, or `goreleaser` — D-06 is locked; this is local prep only.
  </action>
  <verify>
    <automated>cd ../pollen && go build ./... && grep -q "0.1.1-pollen.4" VERSION && grep -q "v0.1.1-pollen.4" CHANGES.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `../pollen/VERSION` contains exactly `0.1.1-pollen.4`.
    - SOURCE: `../pollen/CHANGES.md` has a new `## v0.1.1-pollen.4` dated section above the pollen.3 section, with the D-06 "prepared, not yet tagged / deferred to M2 close" blockquote and a Modified/Added file list naming roots.go and editorext.go.
    - BEHAVIOR: `cd ../pollen && go build ./...` exits 0 (VERSION/CHANGES are metadata — build unaffected; this is a sanity gate).
    - NEGATIVE: no new git tag was created in `../pollen` (verify: `cd ../pollen && git tag --list v0.1.1-pollen.4` returns empty); no push performed.
  </acceptance_criteria>
  <done>VERSION reads 0.1.1-pollen.4; CHANGES.md documents the WEXT deltas with the D-06 deferral note; no tag/sign/push performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Release-metadata edit only — no executable surface, no network, no new input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Unsigned local VERSION/CHANGES edit could drift from the eventual signed tag content | accept | Bounded and intentional: D-06 batches the signed tag to M2 close; the M2-close cut re-verifies VERSION + CHANGES before tagging and cosign-signs the artifact. No signing obligation is satisfied here, so no signature can be forged by this step. Documented as a tracked deferral in STATE.md Deferred Items. |
</threat_model>

<verification>
- `../pollen/VERSION` == `0.1.1-pollen.4`.
- `../pollen/CHANGES.md` v0.1.1-pollen.4 section present with deferral note + Modified/Added lists.
- `cd ../pollen && go build ./...` exits 0.
- No `v0.1.1-pollen.4` git tag exists; nothing pushed (D-06 deferral honored).
</verification>

<success_criteria>
- Release prepared locally: VERSION bumped + CHANGES.md WEXT section, mirroring plans 02-04 / 03-03.
- Signed tag intentionally DEFERRED to M2 close per D-06 — SC5 is satisfied by code-complete + prepared release; the tag is a tracked deferral, NOT a phase failure.
</success_criteria>

<output>
After completion, create `.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-03-SUMMARY.md`
</output>
