---
phase: 22-schema-envelope-lock
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - internal/corpus/types.go
  - internal/corpus/action_hint.go
  - internal/corpus/scope.go
  - internal/corpus/schema_version.go
  - internal/corpus/corpus_test.go
autonomous: true
requirements: [SCHEMA-01, SCHEMA-03, SCHEMA-04, SCOPE-01, SCOPE-02]
must_haves:
  truths:
    - "CorpusRecord embeds audit.AuditRecord via UNNAMED embedding (not json:\",inline\") so AuditRecord fields promote to the top-level JSON object"
    - "CorpusRecord carries the outcome layer (TrueLabel always present = unresolved initially, WasCorrect *bool nil = unresolved) and the context layer (cluster_id, baseline_deviation, repo_fingerprint, fleet_node_id, scope)"
    - "PushEnvelope freezes the wire format with a zero-value SigningBlock in v1 and round-trips through JSON"
    - "ActionHint is a typed string with the SINGLE constant ActionHintWatchAndBlock; no auto_purge constant exists anywhere in internal/corpus"
    - "CorpusRecord{} zero-value serializes scope as org_only via CorpusScope.MarshalJSON"
    - "PromoteScope always returns a non-nil error in v1 and leaves scope unchanged"
  artifacts:
    - path: "internal/corpus/types.go"
      provides: "CorpusRecord (embeds AuditRecord) + PushEnvelope + EnvelopeSignature + IOCBlock + SigningBlock"
      contains: "audit.AuditRecord"
    - path: "internal/corpus/action_hint.go"
      provides: "type ActionHint string + ActionHintWatchAndBlock constant (compile-time auto_purge guard)"
      contains: "ActionHintWatchAndBlock"
    - path: "internal/corpus/scope.go"
      provides: "type CorpusScope + MarshalJSON zero-value guard + PromoteScope always-error stub"
      contains: "func PromoteScope"
    - path: "internal/corpus/schema_version.go"
      provides: "CorpusSchemaVersion constant"
      contains: "CorpusSchemaVersion"
  key_links:
    - from: "internal/corpus/types.go"
      to: "audit.AuditRecord"
      via: "unnamed embedding (promotion), NOT json:\",inline\""
      pattern: "audit\\.AuditRecord"
    - from: "internal/corpus/types.go"
      to: "ActionHint"
      via: "PushEnvelope.ActionHint field typed ActionHint not string"
      pattern: "ActionHint\\s+ActionHint"
    - from: "internal/corpus/types.go"
      to: "CorpusScope"
      via: "CorpusRecord.Scope and PushEnvelope.Scope typed CorpusScope"
      pattern: "Scope\\s+CorpusScope"
---

<objective>
Create the `internal/corpus` package as a PURE package (types + a compile-time guard + pure validation; no I/O, no goroutines, no side effects per CLAUDE.md) holding the frozen four-layer `CorpusRecord` and the frozen `PushEnvelope` wire format, plus the `auto_purge`-unrepresentable guard and the scope zero-value guarantee.

Purpose: Freeze the non-retrofittable schema and envelope as Go types before any record is written. Bakes the blast-radius guard (auto_purge unrepresentable) and the scope-leakage guard (zero-value resolves to org_only) into the type system so future code cannot regress them.

Output: Four new production files in `internal/corpus/` + a unit test file. Depends on Plan 01 having added `SourceSurface`/`ClusterID`/`RulesetVersion` to `audit.AuditRecord` (the embed promotes them).

PHASE BOUNDARY: Types + guards ONLY. No store, no emitter, no BuildPushEnvelope builder (that is Phase 23 ENV-02), no HMAC population of repo_fingerprint/fleet_node_id (Phase 23 STORE-05 — fields + non-reversibility contract defined here only), no behavior_sig/cluster_id functions (Plan 03), no schema-lock gate (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-schema-envelope-lock/22-CONTEXT.md
@.planning/phases/22-schema-envelope-lock/22-RESEARCH.md
@.planning/phases/22-schema-envelope-lock/22-VALIDATION.md
@CLAUDE.md
@internal/audit/types.go
</context>

<artifacts_this_phase_produces>
This plan (22-02) produces, all in package `internal/corpus` (module path `github.com/bantuson/beekeeper/internal/corpus`):
- `internal/corpus/types.go`:
  - `type CorpusRecord struct` — unnamed-embeds `audit.AuditRecord`; outcome layer (`TrueLabel string json:"true_label"` NOT omitempty, `AdjudicationSource string json:"adjudication_source,omitempty"`, `WasCorrect *bool json:"was_correct,omitempty"`, `ResolvedAt string json:"resolved_at,omitempty"`); context layer (`BaselineDeviation`, `RepoFingerprint`, `FleetNodeID` all `omitempty`; `Scope CorpusScope json:"scope"` always present); `CorpusSchemaVersion string json:"corpus_schema_version"`; `PushEnvelope *PushEnvelope json:"push_envelope,omitempty"`.
  - `type PushEnvelope struct` — `Signature EnvelopeSignature`, `TrueLabel string`, `ConfidenceTier string`, `SourceCount int`, `Scope CorpusScope`, `ActionHint ActionHint`, `Signing *SigningBlock json:"signing,omitempty"`.
  - `type EnvelopeSignature struct` — `PackageOrExtensionID`, `Version`, `BehaviorSignatureHash`, `IOCs IOCBlock json:"iocs,omitempty"`.
  - `type IOCBlock struct` — `Domains []string`, `DNSTunnelPattern string`, `DeadDropPattern string` (all omitempty).
  - `type SigningBlock struct` — `Issuer`, `Signature`, `IssuedAt`, `Nonce`.
- `internal/corpus/action_hint.go`: `type ActionHint string`; `const ActionHintWatchAndBlock ActionHint = "watch_and_block"`.
- `internal/corpus/scope.go`: `type CorpusScope string`; `const ScopeOrgOnly`, `ScopeCommunityShareable`; `func (CorpusScope) MarshalJSON`; `func PromoteScope(r *CorpusRecord) error` (always-error stub).
- `internal/corpus/schema_version.go`: `const CorpusSchemaVersion = "1.0"`.
- `internal/corpus/corpus_test.go`: `TestCorpusRecordSchema`, `TestPushEnvelopeRoundTrip`, `TestScopeZeroValue`, `TestPromoteScopeReturnsErrorInV1`, `TestActionHintTypeSafety`.

(Behavior-signature + cluster-id functions and the schema-lock gate test are produced by Plan 03.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the ActionHint compile-time guard, CorpusScope, and the schema-version constant</name>
  <read_first>
    - .planning/phases/22-schema-envelope-lock/22-RESEARCH.md Finding 2 (Option A typed-string guard, the full rationale + the "why this is genuinely compile-time-safe" analysis) and Finding 5 (CorpusScope MarshalJSON + PromoteScope stub) and the Code Examples section (the exact ActionHint / CorpusScope / PromoteScope sketches)
    - internal/config/config.go (the FailModeClosed/FailModeOpen typed-constant precedent referenced in research Pattern 1)
    - .planning/phases/22-schema-envelope-lock/22-CONTEXT.md §Decisions (auto_purge must be unrepresentable; PromoteScope always errors; zero-value resolves org_only)
  </read_first>
  <behavior>
    - ActionHintWatchAndBlock has the string value "watch_and_block".
    - No constant named auto_purge (or value "auto_purge") exists anywhere in internal/corpus.
    - CorpusScope("").MarshalJSON() returns the bytes `"org_only"`.
    - CorpusScope("community_shareable").MarshalJSON() returns `"community_shareable"`.
    - PromoteScope(&CorpusRecord{}) returns a non-nil error and does not change r.Scope.
    - CorpusSchemaVersion == "1.0".
  </behavior>
  <action>
    Create `internal/corpus/action_hint.go` (package `corpus`): declare `type ActionHint string` and the SINGLE package-level constant `const ActionHintWatchAndBlock ActionHint = "watch_and_block"`. Add a doc comment stating this is the sole fleet-pushable action hint and that auto_purge is intentionally undefined (unrepresentable without an explicit unsafe conversion) — this is the SCHEMA-04 compile-time blast-radius guard. Do NOT define any auto_purge constant, variable, or string literal anywhere in the package.
    Create `internal/corpus/scope.go` (package `corpus`, imports `encoding/json` and `errors`): declare `type CorpusScope string`; constants `ScopeOrgOnly CorpusScope = "org_only"` and `ScopeCommunityShareable CorpusScope = "community_shareable"`; a value-receiver `func (s CorpusScope) MarshalJSON() ([]byte, error)` that returns `[]byte(\`"org_only"\`)` when `s == ""` and otherwise `json.Marshal(string(s))` (SCOPE-01 zero-value guard); and `func PromoteScope(r *CorpusRecord) error` that ALWAYS returns a non-nil error (message noting promotion requires the v2.0 anonymization gate) and mutates nothing (SCOPE-02). PromoteScope references the `*CorpusRecord` type defined in Task 2's types.go in the same package — both land in this plan, so the package compiles as a unit.
    Create `internal/corpus/schema_version.go` (package `corpus`): `const CorpusSchemaVersion = "1.0"` with a doc comment that bumping it is a breaking schema change.
  </action>
  <verify>
    <automated>go build ./internal/corpus/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "ActionHintWatchAndBlock ActionHint = \"watch_and_block\"" internal/corpus/` returns a match.
    - `grep -rc "auto_purge" internal/corpus/` returns 0 (no auto_purge token anywhere in the package).
    - `grep -n "func (s CorpusScope) MarshalJSON" internal/corpus/scope.go` returns a match; the body returns `"org_only"` for the empty string.
    - `grep -n "func PromoteScope" internal/corpus/scope.go` returns a match; the body returns a non-nil error unconditionally (no path returns nil).
    - `grep -n "CorpusSchemaVersion = \"1.0\"" internal/corpus/schema_version.go` returns a match.
    - `go build ./internal/corpus/...` exits 0 (after Task 2's types.go lands; if building this file alone before types.go, expect a reference error — build the package as a unit at task end).
  </acceptance_criteria>
  <done>ActionHint typed guard, CorpusScope with MarshalJSON + always-error PromoteScope, and the schema-version constant exist; no auto_purge token in the package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define CorpusRecord (embeds AuditRecord) + PushEnvelope wire format</name>
  <read_first>
    - internal/audit/types.go (the AuditRecord struct to embed — confirm it now carries SourceSurface/ClusterID/RulesetVersion from Plan 01; confirm it has NO scope/true_label/cluster_id-collision field per the research placement matrix)
    - .planning/phases/22-schema-envelope-lock/22-RESEARCH.md Finding 1 (unnamed embedding, NOT json:",inline"; the field placement matrix), Pattern 2 (the full CorpusRecord embedding sketch), Pitfall 1 (json:",inline" is invalid Go), Pitfall 5 (TrueLabel NOT omitempty, WasCorrect *bool), and the Code Examples (full CorpusRecord + PushEnvelope + EnvelopeSignature + IOCBlock + SigningBlock sketches)
    - beekeeper-corpus-milestone-prd.md §3.1 (four-layer field list) and §3.5 (envelope field list incl. signing block)
  </read_first>
  <behavior>
    - A CorpusRecord with audit.AuditRecord{SourceSurface:"sentry"} embedded serializes source_surface at the TOP LEVEL of the JSON object (promoted), NOT nested under an "AuditRecord" key.
    - CorpusRecord{}.TrueLabel can be set to "unresolved" and that key is ALWAYS present in JSON (true_label has no omitempty); WasCorrect is *bool and is absent from JSON when nil (omitempty).
    - A PushEnvelope marshals and unmarshals round-trip with all fields present: signature{package_or_extension_id, version, behavior_signature_hash, iocs}, true_label, confidence_tier, source_count, scope, action_hint, signing.
    - A PushEnvelope with Signing nil omits the signing key (omitempty) — the v1 zero-value transport-less shape.
    - PushEnvelope.ActionHint is typed ActionHint (assigning a bare string literal to it is a compile error); PushEnvelope.Scope and CorpusRecord.Scope are typed CorpusScope.
  </behavior>
  <action>
    Create `internal/corpus/types.go` (package `corpus`, import `github.com/bantuson/beekeeper/internal/audit`). Define `type CorpusRecord struct` whose FIRST field is the UNNAMED embedded `audit.AuditRecord` (no field name, no json tag — promotion; per Pitfall 1 do NOT use `json:",inline"` which is invalid stdlib Go). Then the outcome layer: `TrueLabel string json:"true_label"` (NOT omitempty — always present, "unresolved" initially per Pitfall 5), `AdjudicationSource string json:"adjudication_source,omitempty"`, `WasCorrect *bool json:"was_correct,omitempty"` (nil = unresolved), `ResolvedAt string json:"resolved_at,omitempty"`. Then the context layer: `BaselineDeviation string json:"baseline_deviation,omitempty"`, `RepoFingerprint string json:"repo_fingerprint,omitempty"` (doc: HMAC-SHA256 hex, non-reversible, populated Phase 23 STORE-05), `FleetNodeID string json:"fleet_node_id,omitempty"` (same contract), `Scope CorpusScope json:"scope"` (always present; zero-value resolves org_only via MarshalJSON). Then `CorpusSchemaVersion string json:"corpus_schema_version"` and `PushEnvelope *PushEnvelope json:"push_envelope,omitempty"` (nil until Phase 23 emitter). Do NOT redeclare source_surface/cluster_id/ruleset_version on CorpusRecord — they are promoted from the embedded AuditRecord. Document the non-reversibility contract for the fingerprint fields in comments (the HMAC population is Phase 23).
    In the same file define the frozen envelope types: `type PushEnvelope struct` with `Signature EnvelopeSignature json:"signature"`, `TrueLabel string json:"true_label"`, `ConfidenceTier string json:"confidence_tier"` (watch|enforce), `SourceCount int json:"source_count"`, `Scope CorpusScope json:"scope"`, `ActionHint ActionHint json:"action_hint"` (typed ActionHint, NOT string — the SCHEMA-04 guard at the field level), `Signing *SigningBlock json:"signing,omitempty"` (nil in v1). `type EnvelopeSignature struct` with `PackageOrExtensionID string json:"package_or_extension_id"`, `Version string json:"version"`, `BehaviorSignatureHash string json:"behavior_signature_hash"`, `IOCs IOCBlock json:"iocs,omitempty"`. `type IOCBlock struct` with `Domains []string json:"domains,omitempty"`, `DNSTunnelPattern string json:"dns_tunnel_pattern,omitempty"`, `DeadDropPattern string json:"dead_drop_pattern,omitempty"`. `type SigningBlock struct` with `Issuer string json:"issuer"`, `Signature string json:"signature"`, `IssuedAt string json:"issued_at"`, `Nonce string json:"nonce"`. Add a NewCorpusRecord-style constructor is OPTIONAL discretion; if added, it sets Scope:ScopeOrgOnly, TrueLabel:"unresolved", CorpusSchemaVersion:CorpusSchemaVersion (the MarshalJSON guard already covers zero-value construction, so the constructor is belt-and-suspenders, not the SCOPE-01 mechanism). Do NOT add a BuildPushEnvelope builder (Phase 23). Do NOT add behavior_sig/cluster_id functions (Plan 03).
  </action>
  <verify>
    <automated>go build ./internal/corpus/... && go vet ./internal/corpus/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "audit.AuditRecord" internal/corpus/types.go` returns a match on a line with NO field name and NO json tag (unnamed embedding).
    - `grep -c "json:\",inline\"" internal/corpus/types.go` returns 0 (the invalid tag is not used).
    - `grep -n "TrueLabel string \`json:\"true_label\"\`" internal/corpus/types.go` returns a match WITHOUT omitempty.
    - `grep -n "WasCorrect \*bool" internal/corpus/types.go` returns a match.
    - `grep -n "ActionHint ActionHint" internal/corpus/types.go` returns a match (field typed, not string).
    - `grep -n "Scope CorpusScope" internal/corpus/types.go` returns matches on both CorpusRecord and PushEnvelope.
    - `go build ./internal/corpus/...` and `go vet ./internal/corpus/...` both exit 0.
  </acceptance_criteria>
  <done>CorpusRecord embeds AuditRecord by promotion with the outcome+context layers; the PushEnvelope wire format is frozen with a zero-value SigningBlock; the package builds and vets clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests — embedding promotion, envelope round-trip, scope zero-value, PromoteScope error, ActionHint type safety</name>
  <read_first>
    - internal/corpus/types.go and internal/corpus/scope.go and internal/corpus/action_hint.go (the types under test — written in Tasks 1 and 2)
    - .planning/phases/22-schema-envelope-lock/22-VALIDATION.md (the per-requirement test names: TestCorpusRecordSchema, TestPushEnvelopeRoundTrip, TestScopeZeroValue, TestPromoteScopeReturnsErrorInV1)
    - internal/audit/types.go (AuditRecord fields to set in the embedding-promotion assertion)
  </read_first>
  <behavior>
    - TestCorpusRecordSchema: marshal a CorpusRecord whose embedded AuditRecord has SourceSurface:"sentry"; assert the JSON contains top-level "source_surface":"sentry" and NOT a nested "AuditRecord" object; assert "true_label" key is present even when WasCorrect is nil; assert "was_correct" is absent when WasCorrect is nil.
    - TestPushEnvelopeRoundTrip: marshal a fully-populated PushEnvelope (Signing nil), unmarshal it back, assert all fields equal and "signing" key is absent; then a variant with a zero-value &SigningBlock{} round-trips with the signing key present.
    - TestScopeZeroValue: marshal CorpusRecord{} (zero value) and assert the JSON contains "scope":"org_only"; assert CorpusScope("").MarshalJSON() yields `"org_only"`.
    - TestPromoteScopeReturnsErrorInV1: rec := &CorpusRecord{Scope: ScopeOrgOnly}; err := PromoteScope(rec); assert err != nil AND rec.Scope == ScopeOrgOnly (unchanged).
    - TestActionHintTypeSafety: assert ActionHintWatchAndBlock == ActionHint("watch_and_block") and that it is the value assigned to a PushEnvelope.ActionHint round-tripping as "watch_and_block".
  </behavior>
  <action>
    Create `internal/corpus/corpus_test.go` (package `corpus`, imports `encoding/json`, `testing`, and `github.com/bantuson/beekeeper/internal/audit`). Implement the five tests named in the behavior block. For TestCorpusRecordSchema build the CorpusRecord by setting its embedded `AuditRecord` field (e.g. `rec.AuditRecord.SourceSurface = "sentry"`), marshal with `encoding/json`, and assert on the raw JSON string (string-contains checks for `"source_surface":"sentry"` and absence of `"AuditRecord"`). For TestScopeZeroValue marshal `CorpusRecord{}` and string-contain `"scope":"org_only"`. For TestPromoteScopeReturnsErrorInV1 assert the error is non-nil and Scope is unchanged. Keep the tests pure (no filesystem, no network). Do NOT add the schema-lock gate test or the nx_console_trace fixture here (Plan 03 owns SCHEMA-06).
  </action>
  <verify>
    <automated>go test ./internal/corpus/... -run "TestCorpusRecordSchema|TestPushEnvelopeRoundTrip|TestScopeZeroValue|TestPromoteScopeReturnsErrorInV1|TestActionHintTypeSafety"</automated>
  </verify>
  <acceptance_criteria>
    - All five test functions exist in `internal/corpus/corpus_test.go` (grep each name).
    - `go test ./internal/corpus/... -run "TestCorpusRecordSchema|TestPushEnvelopeRoundTrip|TestScopeZeroValue|TestPromoteScopeReturnsErrorInV1|TestActionHintTypeSafety"` exits 0.
    - The TestCorpusRecordSchema assertions include both a presence check for `"source_surface":"sentry"` and an absence check for `"AuditRecord"` (verifiable by grep of the test).
    - The TestScopeZeroValue body marshals a zero-value `CorpusRecord{}` (grep `CorpusRecord{}` inside the test).
  </acceptance_criteria>
  <done>The five corpus unit tests pass, proving promotion (no nesting), envelope round-trip, scope zero-value guard, PromoteScope always-error, and ActionHint type safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributor → PushEnvelope.ActionHint | A code change could try to introduce a destructive fleet action; the type system must reject it at compile time |
| uninitialized CorpusRecord → corpus/JSON consumer | A zero-value record must not leak as community_shareable; serialization is the boundary |
| Phase 23 emitter → repo_fingerprint/fleet_node_id fields | The fields are defined now with a non-reversibility contract; the values are HMAC'd in Phase 23 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering / Elevation | PushEnvelope.ActionHint | mitigate | `type ActionHint string` with the SINGLE constant `ActionHintWatchAndBlock`; no auto_purge constant exists (grep returns 0). Assigning a bare/dynamic string to the typed field is a compile error; the only well-typed value is watch_and_block. This is the compile-time blast-radius guard (SCHEMA-04, research Finding 2 / PITFALLS #5). |
| T-22-06 | Information Disclosure | CorpusRecord.Scope zero-value | mitigate | `CorpusScope.MarshalJSON` maps `""` → `"org_only"`; a zero-value `CorpusRecord{}` can never serialize as community_shareable (SCOPE-01, PITFALLS #4). |
| T-22-07 | Elevation | PromoteScope | mitigate | `PromoteScope` ALWAYS returns a non-nil error in v1 and mutates nothing; it is the only path that changes scope, so automatic promotion is impossible (SCOPE-02). |
| T-22-08 | Information Disclosure | repo_fingerprint / fleet_node_id | mitigate | Fields documented as HMAC-SHA256 non-reversible; Phase 22 defines the fields + contract only, the per-install-secret HMAC population is Phase 23 STORE-05 (no plain-hash dictionary-attack vector is introduced here). |
| T-22-09 | Tampering (data integrity) | outcome-layer placeholders | mitigate | `TrueLabel` is NOT omitempty (always present, "unresolved" initially) and `WasCorrect` is `*bool` (nil = unresolved, distinct from false); the moat fields exist from the first write (PITFALLS #1). |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; `internal/corpus` imports only stdlib + `internal/audit`. No supply-chain surface added. |
</threat_model>

<verification>
- `go build ./internal/corpus/...` and `go vet ./internal/corpus/...` succeed.
- `grep -rc "auto_purge" internal/corpus/` returns 0.
- `go test ./internal/corpus/...` (Plan 02 tests) is green.
- `go mod tidy && git diff --exit-code go.mod` shows no change.
- `internal/corpus` is pure: no `os`, `net`, goroutine, or filesystem imports (grep the package's import blocks).
</verification>

<success_criteria>
- `CorpusRecord` embeds `audit.AuditRecord` by promotion (no nested object, no `json:",inline"`) and carries the outcome + context layers with `TrueLabel` always present and `WasCorrect *bool`.
- `PushEnvelope` wire format frozen with a zero-value `SigningBlock` in v1; round-trips through JSON.
- `ActionHint` is a typed string with the single `ActionHintWatchAndBlock` constant; no `auto_purge` token in the package.
- `CorpusRecord{}` serializes `scope` as `org_only`; `PromoteScope` always errors.
</success_criteria>

<output>
Create `.planning/phases/22-schema-envelope-lock/22-02-SUMMARY.md` when done.
</output>
