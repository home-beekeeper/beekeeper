---
phase: 02-policy-engine-multi-source-catalogs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/policy/path.go
  - internal/policy/path_test.go
  - internal/policy/credentials.go
  - internal/policy/credentials_test.go
autonomous: true
requirements: [PLCY-04, PLCY-08]
must_haves:
  truths:
    - "A tool call targeting ~/.ssh/ (resolved) is blocked by the sensitive path policy"
    - "A tool call targeting a .env file anywhere in the tree is blocked"
    - "A path explicitly allowlisted overrides the blocklist"
    - "Tool output containing an AWS access key, JWT, or Bearer token is redacted before return"
    - "The credential filter reports which credential types were detected"
    - "Both new policy files are pure — no os/net/io/sync/time/context imports"
  artifacts:
    - path: "internal/policy/path.go"
      provides: "EvaluatePath pure sensitive-path policy"
      contains: "func EvaluatePath"
    - path: "internal/policy/credentials.go"
      provides: "FilterCredentials pure output redaction"
      contains: "func FilterCredentials"
  key_links:
    - from: "internal/policy/path.go"
      to: "Decision"
      via: "EvaluatePath returns a block Decision with the matched pattern"
      pattern: "ruleSensitivePath"
    - from: "internal/policy/credentials.go"
      to: "CredentialFilterResult"
      via: "package-level compiled regexps drive redaction"
      pattern: "regexp.MustCompile"
---

<objective>
Add two pure, side-effect-free policy functions: sensitive-path blocking (PLCY-04) and output credential redaction (PLCY-08). Both live in `internal/policy` and follow the same purity contract as the Phase 1 engine.

Purpose: These two policies are independent of catalog corroboration and of each other — they have no shared files with any other Wave 1 plan, so they run in parallel. Path matching protects credential stores from agent reads; credential filtering scrubs secrets out of tool outputs before they re-enter agent context.

Output: `internal/policy/path.go` + test, `internal/policy/credentials.go` + test. Path normalization (`~` resolution, Windows path handling) is the CALLER's job (the platform/IO layer in Wave 3); these functions receive pre-resolved inputs to stay pure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-policy-engine-multi-source-catalogs/02-CONTEXT.md
@.planning/phases/02-policy-engine-multi-source-catalogs/02-PATTERNS.md

<interfaces>
From internal/policy/engine.go (purity model — these files follow it):
```go
// package policy; engine.go imports only "strings" + internal/catalog.
// Forbidden imports (enforced by AST test): os, net, net/http, io, sync, time, context.
// "strings", "regexp", "sort", "path", "math" are ALL pure and permitted.
```
The `Decision` struct and rule-ID constant pattern are defined in types.go / engine.go. New rule constants for THIS plan: `ruleSensitivePath = "sensitive-path-policy"`, `ruleCredentialOutput = "credential-output-filter"`. Add these constants in the file that uses them (path.go / credentials.go) to avoid touching engine.go (owned by Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sensitive path policy in path.go (TDD)</name>
  <files>internal/policy/path.go, internal/policy/path_test.go</files>
  <read_first>
    - internal/policy/engine.go (purity model + Decision construction pattern)
    - .planning/phases/02-policy-engine-multi-source-catalogs/02-PATTERNS.md (lines 260–282, path.go signature pattern)
    - .planning/phases/02-policy-engine-multi-source-catalogs/02-CONTEXT.md (Sensitive Path Policy — PLCY-04, the full default blocklist)
    - internal/policy/engine_test.go (TestEngineImportsArePure pattern to replicate)
  </read_first>
  <behavior>
    - Test: resolved path "/home/u/.ssh/id_rsa" with default blocklist → Level "block", Allow false, reason names the matched pattern
    - Test: resolved path "/home/u/project/.env" → block (glob .env anywhere)
    - Test: resolved path "/home/u/project/.env.local" → block
    - Test: resolved path "/home/u/project/src/main.go" → Level "allow", Allow true
    - Test: path "/home/u/.aws/credentials" present in AllowPatterns → allow (allowlist overrides blocklist)
    - Test: Windows-style resolved path "C:\\Users\\u\\.ssh\\id_rsa" with prefix pattern → block (caller already normalized separators; function matches on the resolved string verbatim)
    - Test: TestPathImportsArePure — path.go imports none of os/net/net/http/io/sync/time/context
  </behavior>
  <action>
    Create `path.go` in package policy. Define `const ruleSensitivePath = "sensitive-path-policy"`. Define `type SensitivePathConfig struct { BlockPatterns []string; AllowPatterns []string }` and `func DefaultSensitivePaths() SensitivePathConfig` returning the PLCY-04 default blocklist as path FRAGMENTS the caller will have resolved against home (use forward-slash canonical fragments; the caller normalizes `~` and OS separators before calling): `/.ssh/`, `/.aws/`, `/.gnupg/`, `/.config/Claude/`, `/.config/op/`, `/.config/gh/`, `/.netrc`, `/.npmrc`, `/.pypirc`, `/.cargo/credentials.toml`, and the basename globs `.env`, `.env.local`, and `.env.*` (handled as a suffix/basename rule). Document that "all MCP config files enumerated by Bumblebee" is sourced by the caller and appended to BlockPatterns at wiring time (Plan 08) — not hardcoded here.

    Implement `func EvaluatePath(resolvedPath string, cfg SensitivePathConfig) Decision`. Match semantics: if `resolvedPath` matches any AllowPattern (prefix or exact) → return allow Decision (reason "explicitly allowlisted"). Otherwise, for each BlockPattern: a fragment containing "/" matches via `strings.Contains(resolvedPath, pattern)`; a basename glob like ".env" or ".env.*" matches when the final path segment equals ".env"/".env.local" or has prefix ".env." — extract the last segment by splitting on both "/" and "\\" using `strings.LastIndexAny`. On a block match return `Decision{Allow:false, Level:"block", Reason:"sensitive path blocked: "+pattern, RuleIDs:[]string{ruleSensitivePath}}`. No match → allow Decision (reason "no sensitive path match"). Use only `strings` (pure).

    Add `path_test.go` (package policy, white-box) with the table tests and `TestPathImportsArePure`.
  </action>
  <verify>
    <automated>go test ./internal/policy/... -run TestPath -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/policy/... -run TestPath -count=1` exits 0
    - `TestPathImportsArePure` passes (no os/net/net/http/io/sync/time/context)
    - `.ssh/`, `.aws/`, `.env`, `.env.local` cases all block; allowlist overrides; clean source file allows
    - `path.go` defines `func EvaluatePath(resolvedPath string, cfg SensitivePathConfig) Decision` and `func DefaultSensitivePaths() SensitivePathConfig`
    - block Decision Reason contains the matched pattern string
  </acceptance_criteria>
  <done>EvaluatePath is a pure function with documented default blocklist and full table coverage; caller owns path resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Output credential filtering in credentials.go (TDD)</name>
  <files>internal/policy/credentials.go, internal/policy/credentials_test.go</files>
  <read_first>
    - internal/policy/engine.go (purity model)
    - .planning/phases/02-policy-engine-multi-source-catalogs/02-PATTERNS.md (lines 373–403, credentials.go signature + package-level regexp pattern)
    - .planning/phases/02-policy-engine-multi-source-catalogs/02-CONTEXT.md (Output Credential Filtering — PLCY-08, default redaction patterns)
  </read_first>
  <behavior>
    - Test: output "AKIAIOSFODNN7EXAMPLE" → Redacted contains "[REDACTED:aws-access-key]", DetectedTypes contains "aws-access-key", ContainsCredentials true
    - Test: a JWT "eyJhbGciOi....x.y" → redacted, DetectedTypes contains "jwt"
    - Test: "Authorization: Bearer abc123def456..." → Bearer value redacted, DetectedTypes contains "bearer"
    - Test: GitHub token "ghp_" + 36 chars → DetectedTypes contains "github-token"
    - Test: npm token "npm_" + 36 chars → DetectedTypes contains "npm-token"
    - Test: OpenAI key "sk-" + 40 chars → DetectedTypes contains "openai-key"
    - Test: clean output "hello world" → Redacted == input, ContainsCredentials false, DetectedTypes empty
    - Test: additional regex pattern supplied via config redacts a custom token
    - Test: TestCredentialsImportsArePure — credentials.go imports none of os/net/net/http/io/sync/time/context
  </behavior>
  <action>
    Create `credentials.go` in package policy. Define `const ruleCredentialOutput = "credential-output-filter"`. Define `type CredentialFilterResult struct { Redacted string; DetectedTypes []string; ContainsCredentials bool }` and `type CredentialFilterConfig struct { AdditionalPatterns []string }`.

    Declare package-level compiled regexps in a slice `var credPatterns = []credPattern{...}` where `type credPattern struct { name string; re *regexp.Regexp }`. Compile once (package init via var, NOT per call). Patterns: aws-access-key `AKIA[0-9A-Z]{16}`, jwt `eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+`, bearer `(?i)Bearer\s+[A-Za-z0-9._-]+`, github-token `gh[pousr]_[A-Za-z0-9]{36,}`, npm-token `npm_[A-Za-z0-9]{36}`, openai-key `sk-[A-Za-z0-9]{20,}`.

    Implement `func FilterCredentials(output string, cfg CredentialFilterConfig) CredentialFilterResult`. For each pattern (built-in then `cfg.AdditionalPatterns` compiled with name "custom"): replace all matches with `[REDACTED:<name>]`, and if any replacement happened append `name` to DetectedTypes (deduped). For bearer, redact only the token portion is acceptable but redacting the whole "Bearer X" match with "[REDACTED:bearer]" is the simpler correct behavior — do the whole-match replacement. Set `ContainsCredentials = len(DetectedTypes) > 0`. Use only `regexp` and `strings` (both pure). Document that compiling `AdditionalPatterns` per call is acceptable because the config set is small and caller-controlled; built-ins stay package-level.

    Add `credentials_test.go` (package policy, white-box) with the table tests and `TestCredentialsImportsArePure`.
  </action>
  <verify>
    <automated>go test ./internal/policy/... -run TestCredential -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/policy/... -run TestCredential -count=1` exits 0
    - `TestCredentialsImportsArePure` passes (no os/net/net/http/io/sync/time/context)
    - AWS key, JWT, Bearer, GitHub, npm, and OpenAI patterns each detected and redacted with `[REDACTED:<type>]`
    - clean input returns Redacted == input and ContainsCredentials false
    - built-in regexps are package-level vars (compiled once), not compiled inside FilterCredentials
    - `credentials.go` defines `func FilterCredentials(output string, cfg CredentialFilterConfig) CredentialFilterResult`
  </acceptance_criteria>
  <done>FilterCredentials redacts all default credential classes plus custom patterns; purity test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → file target | Agent-supplied path may target developer credential stores |
| tool output → agent context | Tool output may carry leaked secrets back into the model context (exfil/poisoning vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | EvaluatePath blocklist coverage | mitigate | Default blocklist covers ssh/aws/gnupg/gh/op/netrc/npmrc/pypirc/cargo-creds/.env globs; user can extend via config |
| T-02-02-02 | Information Disclosure | FilterCredentials redaction | mitigate | Six default credential classes redacted in tool output before it re-enters agent context; ContainsCredentials flag lets the audit record note presence without storing the secret |
| T-02-02-03 | Tampering | path matching bypass via separators | mitigate | Last-segment extraction splits on both "/" and "\\" (Windows); caller normalizes before calling. Documented caller contract |
| T-02-02-04 | Denial of Service | regex catastrophic backtracking | accept | Patterns are anchored character-class repeats with no nested quantifiers; Go RE2 engine is linear-time by construction (no backtracking) |
</threat_model>

<verification>
- `go test ./internal/policy/... -run 'TestPath|TestCredential' -count=1` exits 0
- `go vet ./internal/policy/...` exits 0
- Both purity tests green
</verification>

<success_criteria>
- PLCY-04 sensitive-path policy blocks the documented default credential-store paths and .env globs; allowlist overrides
- PLCY-08 output credential filtering redacts six default credential classes plus custom patterns
- Both files pure (no forbidden imports)
</success_criteria>

<output>
After completion, create `.planning/phases/02-policy-engine-multi-source-catalogs/02-02-SUMMARY.md` documenting EvaluatePath and FilterCredentials signatures, the default blocklist/redaction sets, and the caller-owned path-resolution contract.
</output>
