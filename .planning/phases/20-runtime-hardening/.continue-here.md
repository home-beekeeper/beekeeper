---
context: phase
phase: 20-runtime-hardening
plan: 20-02
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-06-10T11:02:04Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through this session's execution. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **HF gated-model human gate (20-02 Task 3)** — accepting the Llama-Prompt-Guard-2 license + `huggingface-cli login` is a human-only web action. Claude cannot complete it. → Do ALL of 20-02's code automation first; only the final live-bootstrap verify + e2e are gated. Present the checkpoint; never fake the bootstrap.
- [ ] CONSTRAINT: **SDK phase-resolver corrupts STATE frontmatter** — do NOT run `state.*` / `phase.complete` / `verify-work` SDK verbs against bare phase numbers. STATE.md / ROADMAP.md are hand-managed this whole phase. The slug `20-runtime-hardening` is otherwise resolver-safe.
- [ ] CONSTRAINT: **No subagents for Go/web execution** — executed inline on `main` (use_worktrees=false serializes anyway; pnpm web steps need tools subagents lack). Continue inline.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Acceptance grep counts comments | Task-3 AC `grep -c "/ru SYSTEM"==0` and `grep "role=\"user\""==0` failed because explanatory COMMENTS/docstrings contained the forbidden literal. | advisory | Reword comments so forbidden literal strings never appear in the file, not just the code. |
| `&&`-chaining greps | `grep -c X && next` aborts the chain when grep finds 0 matches (exit 1), silently skipping later commands. | advisory | Use `;` separators or run grep checks individually. |
| SDK state verbs | See blocking constraint above. | blocking | Hand-edit STATE.md/ROADMAP.md; do not call state.*/phase.complete. |

<current_state>
Phase 20 (Runtime Hardening II) executing inline on `main`. **4/6 plans complete** (20-01 CSYNC, 20-03 SENT-W1, 20-04 SENT-W2 — all committed, all gates green). **20-02 (LlamaFirewall) Task 1 of 4 complete** (commit `d306f19`): the critical security fix landed — silent fail-open closed, cloud-exfil AlignmentCheck removed, sidecar embedded + InstallSidecar. Paused at the start of 20-02 Task 2 (IPC TCP rework). Working tree clean; everything committed atomically.

User chose **"Finish all autonomous work"** (20-02 Tasks 2+4 + Task-3 automation → 20-05 → 20-06, stopping only at the 20-02 HF-license human gate) immediately before invoking /gsd-pause-work.
</current_state>

<completed_work>

**Plans complete (committed):**
- 20-01 Tier 1 CSYNC (CSYNC-01..06): `0540e52` `482e8b9` `6280a5f` `8415c73` + SUMMARY `51d8352`
- 20-03 Tier 3 W1 Sentry rules (SENT-01..04): `0e6b5f1` `3e5b3a2` + SUMMARY `fa7ed71`
- 20-04 Tier 3 W2 file-write (SENT-05..08): `f285ecf` `c5c05db` `fc5a781` `03dcaec` + SUMMARY `ac424ca`

**20-02 (LlamaFirewall) — Task 1 of 4 DONE (`d306f19`):**
- Moved `sidecar/` → `internal/llamafirewall/assets/` + `//go:embed` + `InstallSidecar` (sha stamp / hash-skip).
- FIXED the silent fail-open: scanners built ONCE (PROMPT_GUARD/USER + CODE_SHIELD/ASSISTANT), `lf.scan(UserMessage(content=...))` (no bogus role kwarg), real CodeShield, ANY exception → `error` sentinel; `Supervisor.Scan` maps `result=error`/`Error!=""` → fail-closed per FailMode.
- Removed AlignmentCheck (Together AI cloud) EVERYWHERE: proto (ScanAlignment/ResultHijacked), integration, config, layered (both variants), supervisor field, handler block, main.go wiring. Added `ResultError`.
- `requirements.txt` pins the real `llamafirewall==1.0.3` (not the `llama-firewall` squat). The embedded Python is in FINAL form (already AF_INET + per-launch token).
</completed_work>

<remaining_work>

**20-02 Task 2 (IPC TCP+token) — START HERE:**
- Collapse `client.go` + `client_windows.go` → ONE TCP `Dial(addr, token, timeout)` (delete `client_windows.go`; remove the `//go:build linux||darwin` tag); `Client.Scan` sets `req.Token`. `ScanRequest.Token` already exists.
- `supervisor.go`: pick a free 127.0.0.1 port (listen :0 then close) + crypto/rand token; inject `BEEKEEPER_LLMF_PORT`/`BEEKEEPER_LLMF_TOKEN`/`HF_HOME=<stateDir>/llamafirewall/hf` via `cmd.Env` in BOTH Start and relaunch; replace the `os.Stat(SockPath)` readiness with a TCP dial-retry loop; `NewSupervisor(cfg, sidecarPath)` (drop sockPath, drop SockPath field → port/addr); `persistState` also writes port + token.
- `main.go`: fix `$HOME` → `platform.ConfigPath()`/`platform.StateDir()` at ~1645/1664/1684/1712; the `audit-record` dial (~1788) reads port+token from state.json and `Dial(127.0.0.1:port, token, ...)`; the gateway wiring (~1314) uses `NewSupervisor(llmfCfg, SidecarScriptPath(stateDir))`.
- **Rework tests:** `supervisor_failclosed_test.go` (4 `NewSupervisor(cfg,"test.sock","fake_sidecar.py")` → `NewSupervisor(cfg,"fake_sidecar.py")`; delete the `sup.SockPath` assertion at lines 62-64). `supervisor_test.go` (//go:build linux): rewrite `startMockSidecar`/`startCrashingSidecar` from unix `net.Listen("unix",...)` to TCP `net.Listen("tcp","127.0.0.1:0")`, return addr; all `Dial(sockPath, time.Second)` → `Dial(addr, token, time.Second)`; all `NewSupervisor(cfg, sock, sidecar)` → `NewSupervisor(cfg, sidecar)`.
- Verify: `go build ./...` + 3 GOOS + `go test ./internal/llamafirewall/ ./internal/config/ ./internal/check/` + `GOOS=linux go vet`.

**20-02 Task 3 (auto, then BLOCKING human gate):** add `beekeeper llamafirewall install` (venv under `StateDir/llamafirewall/venv` + `pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu` + pre-pull `meta-llama/Llama-Prompt-Guard-2-22M` into `HF_HOME`; print the Llama-license + `huggingface-cli login` requirement); supervisor `PythonPath` → venv interpreter when present; soften `web/components/home/feature-cards.tsx` (~:52) + `web/content/docs/security.mdx` (opt-in/experimental/gated-model; raw `var(--amber)`/`var(--teal)` tokens); `cd web && pnpm build`. **THEN present the human-verify checkpoint** (HF license + venv bootstrap + StateDir status + softened claims).

**20-02 Task 4 (auto):** `e2e_test.go` `//go:build e2e` gated by `BEEKEEPER_LLMF_E2E=1` (benign-allow/injection/codeshield-unsafe/crash-fail-closed; `t.Skip` otherwise — CI-only); `TestSupervisorRestartOnCrash` (NOT e2e-gated) using the existing unused `startCrashingSidecar`; `security.mdx` posture accurate. Verify `go build -tags e2e ./internal/llamafirewall/` + `go test -run TestSupervisorRestartOnCrash`.

**20-05 Tier 3 W3 honesty (SENT-09/10) — UNBLOCKED, independent of 20-02:** PROJECT.md + `docs/THREAT-MODEL.md` §8 corrections (remove stale SENTRY-004/005-don't-fire + fanotify-no-drops; add editor+agent scope / detection-only / residual gaps per `analysis/sentry-coverage-2026-06.md` §6); home `how-it-works.tsx`/`feature-cards.tsx`/`honesty-callout.tsx` honesty edits (raw theme tokens); synthetic `rules_test.go` 006/007/008 + watchlist; gate = `pnpm build` + seo/home/gfx/accuracy specs. NOTE: `feature-cards.tsx` is touched by BOTH 20-02 Task 3 and 20-05 — sequence to compose.

**20-06 Tier 3 W4 DNS (SENT-11) — OPTIONAL stretch:** droppable. EventDNSQuery + Linux kprobe bpf2go (fail-closed loader stub, never committed/runtime-compiled) + Windows ETW DNS-Client ID 3006. macOS deferred.
</remaining_work>

<decisions_made>

- Inline-on-main execution, per-task atomic commits, hand-managed STATE/ROADMAP (SDK-resolver caveat).
- Embedded Python written in final AF_INET+token form during Task 1 (avoids a double rewrite; Go IPC catches up in Task 2).
- Windows Kernel-File: kept 12=Create(open) as access alongside 15=Read (avoids narrowing detection); only 14=Close excluded.
- macOS create stays EventFileAccess (path union fixed); only write/rename → EventFileWrite. Subscription is in `eslogger.go`, not the plan's (nonexistent) `collector.go`.
- ETW write/rename EventData key is UNVERIFIED (no live capture) — uses the FileName→FilePath fallback; flagged for CI/live validation (CLAUDE.md Phase-7 ETW flag).
</decisions_made>

<blockers>
- **20-02 Task 3 HF gated-model license** (human_action): accept the Llama-Prompt-Guard-2 license + `huggingface-cli login`; Claude cannot. Workaround: complete all code automation first; only the live-bootstrap verify is gated.
</blockers>

## Required Reading (in order)
1. `C:\Users\Bantu\.claude\projects\C--Users-Bantu-mzansi-agentive-beekeeper\memory\phase20-execution-progress.md` — exhaustive per-task state + full commit map (the authoritative resume reference).
2. `.planning/HANDOFF.json` — structured state for /gsd-resume-work.
3. `.planning/phases/20-runtime-hardening/20-02-PLAN.md` — the LlamaFirewall plan (Tasks 2-4 + the Task-3 human checkpoint contract).
4. `.planning/phases/20-runtime-hardening/20-RESEARCH.md` (Tier-2 facts) + `20-05-PLAN.md` / `20-06-PLAN.md`.

## Infrastructure State
- Working tree clean; all Phase 20 work committed. Go 1.25.0 windows/amd64. `x/sys v0.44.0` has all needed fanotify/handle symbols (already verified).
- No background processes running. No venv bootstrapped (that is 20-02 Task 3's human-gated step).

<context>
This was a long single-session execution of Phase 20. The work has been methodical: every task ran its acceptance-criteria gate (source greps + builds + cross-OS + tests + vet) before an atomic commit, then a SUMMARY + ROADMAP update per plan. The remaining 20-02 is the hardest (cross-language + a large linux-gated test rework + a human gate). 20-05 is the safest unblocked next step if you'd rather defer the LlamaFirewall IPC rework. ROADMAP progress row currently reads 4/6.
</context>

<next_action>
Start with: resume **20-02 Task 2** — collapse `internal/llamafirewall/client.go` + `client_windows.go` into one TCP `Dial(addr, token, timeout)`, then rework `supervisor.go` (free port + crypto/rand token + HF_HOME via cmd.Env + TCP dial-retry readiness + persist port/token) and `main.go` ($HOME→platform paths + audit-record dial + 2-arg NewSupervisor), then fix the two supervisor test files to the new signatures. (Alternatively, if deferring 20-02: jump to the unblocked **20-05**.) Re-read `memory/phase20-execution-progress.md` first.
</next_action>
