---
context: phase
phase: 20-runtime-hardening
task: "20-02 LlamaFirewall live bootstrap (the LAST v1.3.0 item)"
status: blocked
last_updated: 2026-06-11T11:16:08.025Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **`requirements.txt` is `//go:embed`-ed in the binary.** Fixing the source asset alone does NOTHING until `go install` rebuilds. The user's install error still shows `transformers==4.44.2` because their installed binary embeds the OLD pin. The source IS fixed (commit `61c77b9`, `transformers>=4.51.3`). → Do NOT re-edit the source thinking it's unfixed; the fix is in. The action is to **rebuild** (`go install ./cmd/beekeeper`).

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| "Edited the source, expected it to take effect" | `requirements.txt` is embedded; only a rebuilt binary deploys it. `InstallSidecar` also hash-skips re-deploy when embedded content is unchanged. | blocking | Always `go install ./cmd/beekeeper` after touching `internal/llamafirewall/assets/*`, then re-run the install. |
| "Assumed gated-model deps were tested" | The LlamaFirewall deps were never install-validated (license-gated) — the Tier-C register's PENDING status was load-bearing. | advisory | Expect more untested-deps friction (possible torch/tokenizers conflict). Iterate on pins as pip surfaces them; the local pip resolve was NOT run this session. |

<current_state>
Phase 21 (Full-System Validation, VAL-01..08) is **COMPLETE & verified 2026-06-11**. ALL v1.3.0 phases (10–21) are code-complete. The ONLY remaining milestone item is this — the **Phase-20 20-02 LlamaFirewall HF-license live bootstrap** — then `/gsd-complete-milestone v1.3.0`.

The user accepted the Llama license and is running `beekeeper llamafirewall install`, which fails:
`ResolutionImpossible: transformers==4.44.2 vs llamafirewall 1.0.3 needs transformers>=4.51.3`.
The SOURCE asset is already fixed (commit `61c77b9`); the user's **installed binary still embeds the old pin** — they have not rebuilt yet.
</current_state>

<completed_work>
- Phase 21 planned + executed + finalized (VAL-01..08, maintainer-approved) — `f111d25` + the 18 `(21*)` commits.
- LlamaFirewall HF-token guidance given (permissions, storage, git-credential — see decisions).
- `fix(llamafirewall)`: `internal/llamafirewall/assets/requirements.txt` → `transformers>=4.51.3` — `61c77b9`.
</completed_work>

<remaining_work>
1. **Unblock install (do first):** `go install ./cmd/beekeeper` → re-run `beekeeper llamafirewall install`. (Alt without rebuild: manually edit `%APPDATA%\beekeeper\llamafirewall\requirements.txt` to `transformers>=4.51.3`; the hash-skip won't clobber it on the same binary.) Watch for a SECOND pip conflict (torch/tokenizers) — bump + rebuild if it appears.
2. **Token:** `huggingface-cli login` / set `HF_TOKEN` (read-only token), stored at `%APPDATA%\beekeeper\llamafirewall\hf\token`. Decline the git-credential prompt.
3. **Verify:** `BEEKEEPER_LLMF_E2E=1 go test -tags e2e -run TestLlamaFirewallE2E ./internal/llamafirewall/` → tick the gated-model row in `docs/validation-register.md`.
4. **Docs follow-up:** land the per-operator clarification (drafted sentence below) into `web/content/docs/security.mdx` (Gated-model bullet) + a short mirror in `cli-reference.mdx`; then `pnpm build` + the accuracy spec to keep the web gate green. Confirm-before-edit (public site).
5. `/gsd-complete-milestone v1.3.0`.
</remaining_work>

<decisions_made>
- **HF token = read-only** (fine-grained "read access to public gated repos you can access", or classic Read). No write/admin/inference/billing.
- **Token location = `%APPDATA%\beekeeper\llamafirewall\hf\token`** (`HFHome(StateDir)`). Reliable (install pins HF_HOME there → `huggingface_hub` reads `$HF_HOME/token`) AND beekeeper-self-protected (StateDir ∈ `ReadWritePrefixes` → `EvaluateSelfPath` blocks agent reads, verified `internal/policy/selfpath.go:20`). `HF_TOKEN` env var is reliable but NOT SPATH-protectable (env isn't a file path) — prefer the file-under-StateDir for the "beekeeper blocks agent reads" goal.
- **Decline the git-credential prompt** — `snapshot_download` uses the HF HTTP API, not `git clone`.
- **transformers pin tracks llamafirewall's floor** (`>=4.51.3`), not an independent stale hard-pin.
</decisions_made>

<blockers>
- **Install ResolutionImpossible** (transformers 4.44.2 vs >=4.51.3): root-caused + source-fixed (`61c77b9`); needs a binary REBUILD to take effect. See BLOCKING CONSTRAINTS.
- **Possible follow-on pip conflict** (torch/tokenizers vs transformers>=4.51.3): untested — the local pip resolve was not run (downloads ~hundreds of MB). Iterate if it appears.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state for `/gsd-resume-work`.
2. `internal/llamafirewall/assets/requirements.txt` — the fixed pins (`transformers>=4.51.3`).
3. `cmd/beekeeper/main.go` (~L1775–1850) — the `llamafirewall install` flow (venv → pip → `snapshot_download` with pinned HF_HOME).
4. `internal/llamafirewall/sidecar_assets.go` — `InstallSidecar` hash-skip (won't re-deploy unchanged embedded content).
5. `docs/validation-register.md` (gated-model row, ~L171–177) — the PENDING Tier-C item to sign off after the e2e passes.

## Infrastructure State
- Python **3.12.7** (in range for torch 2.4.1 cp312 + llamafirewall 3.10+). The venv at `%APPDATA%\beekeeper\llamafirewall\venv` is partially built (pip failed at the resolve step).
- No background processes left running by the agent.
- Working tree clean except a pre-existing untracked maintainer artifact `beekeeper-home-v2.html` (not part of this work).

## Docs-clarity follow-up — drafted sentence (not yet landed)
Append to the **Gated model** bullet in `web/content/docs/security.mdx`:
> This is **per-operator and one-time per machine**, not something Beekeeper can do once for everyone: Meta's Llama license must be accepted by each user individually and the gated weights cannot be redistributed, so Beekeeper ships no model and bundles no token. Your Hugging Face token stays on your machine — Beekeeper never sees or transmits it — and the model cache lives under your state directory.

<context>
We finished Phase 21 cleanly, then pivoted to the user's real-world LlamaFirewall bring-up (the human gate that closes the last milestone item). The install surfaced a genuine shipped bug (stale transformers pin) — fixed at source but needs a rebuild. The user also asked good product questions (per-user token provisioning, why-not-bundleable) that exposed a docs-clarity gap worth closing before milestone close. Nothing is broken in the Go core; this is opt-in/experimental sidecar bring-up + a doc polish.
</context>

<next_action>
Start with: `go install ./cmd/beekeeper` (embeds the fixed requirements.txt from `61c77b9`), then re-run `beekeeper llamafirewall install`. If a second pip conflict appears, bump that pin in `internal/llamafirewall/assets/requirements.txt` and rebuild again.
</next_action>
