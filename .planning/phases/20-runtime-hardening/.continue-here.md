---
context: phase
phase: 20-runtime-hardening
plan: 20-02
task: 3
total_tasks: 4
status: human_gate_only
last_updated: 2026-06-10T14:30:00Z
---

# Phase 20 — ALL CODE COMPLETE; only a human gate remains

> Updated 2026-06-10. All 6 plans (20-01..20-06) are CODE-complete and committed
> on `main`, every gate green. There is NO autonomous code left in Phase 20. The
> single remaining item is a human-only checkpoint.

## The only remaining item: 20-02 Task 3 human verification (BLOCKING, human-only)

Accepting the gated model license is a web action Claude cannot perform. To close
Phase 20's last item:

1. Accept the **Llama-Prompt-Guard-2** license at
   https://huggingface.co/meta-llama/Llama-Prompt-Guard-2-22M
2. `huggingface-cli login` with an HF token that has accepted the license.
3. `beekeeper llamafirewall install` — confirm it creates the venv under the
   StateDir (`%APPDATA%/beekeeper/llamafirewall/venv` on Windows), installs deps
   from the CPU torch index (no CUDA wheels), and pre-pulls the 22M model into
   `<StateDir>/llamafirewall/hf`.
4. `beekeeper llamafirewall status` — confirm it reports state from the StateDir
   (not `$HOME`).
5. In `web/`: `pnpm build`, serve, and confirm on localhost:3000 that the
   LlamaFirewall claim reads opt-in/experimental + gated-model (not "always on").

Contract + acceptance: `.planning/phases/20-runtime-hardening/20-02-PLAN.md`
Task 3. Code status + commit map: `20-02-SUMMARY.md`.

## Phase 20 plan status (all committed on main)

- 20-01 CSYNC ✅ · 20-03 SENT-W1 ✅ · 20-04 SENT-W2 ✅ · 20-05 honesty+tests ✅ · 20-06 DNS stretch ✅
- 20-02 LlamaFirewall: Tasks 1–4 CODE ✅ (commits d306f19 / b116c5b / 923c4de / f36546d); Task 3 human verify pending.

## CI-validated (NOT locally verifiable, by design)

- LLMF real-sidecar e2e (`//go:build e2e`, `BEEKEEPER_LLMF_E2E=1`) — needs the
  accepted gated model; CI Linux job.
- eBPF DNS QNAME parse + `msg_iter.iov` (CI kernel matrix 5.4/5.15).
- Windows ETW DNS-Client manifest-provider enable (windows-latest CI).

## After the human gate

`/gsd-plan-phase 19` (Test Suite & CI, QA-01/02) — the last v1.3.0 phase. Then
`/gsd-complete-milestone v1.3.0`.

## Tracking caveats (still apply)

- Hand-managed STATE.md / ROADMAP.md (the SDK phase-resolver corrupts frontmatter
  on bare phase numbers; do NOT run `state.*` / `phase.complete` against "20").
- Phase executed inline on `main` (no subagents — they lack node/pnpm).
