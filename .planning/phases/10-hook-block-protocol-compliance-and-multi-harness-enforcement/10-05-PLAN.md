---
phase: 10-hook-block-protocol-compliance-and-multi-harness-enforcement
plan: 05
type: execute
wave: 4
depends_on: ["10-01", "10-04"]
files_modified:
  - internal/hooks/hermes.go
  - internal/hooks/cline.go
  - internal/hooks/cline_windows.go
  - internal/hooks/opencode_plugin.go
  - internal/hooks/hooks.go
  - internal/hooks/hooks_test.go
  - internal/hooks/cline_test.go
autonomous: true
requirements: [HPC-02, HPC-03, HPC-05]
must_haves:
  truths:
    - "The Hermes installer wires a pre_tool_call hook whose deny path is JSON-only (exit codes ignored by Hermes); RenderDeny(HarnessHermes) emits {\"action\":\"block\",\"message\":...} with a non-empty message"
    - "The Cline installer writes an executable PreToolUse script (macOS/Linux only, build-tagged off Windows) that shells to 'beekeeper check --hook cline'"
    - "The OpenCode installer ships a JS plugin that shells to 'beekeeper check --hook opencode' and throws on deny"
    - "hooks install --target {hermes,cline,opencode} dispatches to the right installer"
  artifacts:
    - path: "internal/hooks/hermes.go"
      provides: "Hermes installer (~/.hermes/config.yaml pre_tool_call; fail-open → JSON-only block path)"
      contains: "beekeeper check --hook hermes"
    - path: "internal/hooks/cline.go"
      provides: "Cline installer (executable PreToolUse script, macOS/Linux only)"
      contains: "beekeeper check --hook cline"
    - path: "internal/hooks/opencode_plugin.go"
      provides: "OpenCode JS plugin installer (tool.execute.before → throw)"
      contains: "beekeeper check --hook opencode"
  key_links:
    - from: "internal/hooks/hooks.go InstallTo"
      to: "installHermes/installCline/installOpenCodePlugin"
      via: "switch target dispatch"
      pattern: "TargetHermes"
    - from: "internal/hooks/opencode_plugin.go plugin template"
      to: "beekeeper check --hook opencode"
      via: "child_process shelling + throw on non-zero"
      pattern: "tool.execute.before"
---

<objective>
Implement the three caveated long-tail harnesses: Hermes (fail-OPEN — exit codes ignored, so the
block is carried ONLY by the JSON), Cline (executable PreToolUse script, macOS/Linux only — no
Windows), and OpenCode (a JS plugin that shells to `beekeeper check --hook opencode` and throws on
deny). Each wires the correct --hook command so the Plan 01 RenderDeny adapter produces the right
signal.

Purpose: HPC-02 + HPC-03 + part of HPC-05 (these are the Tier-2 caveated harnesses). Depends on
Plan 04 because it shares hooks.go (dispatch) and hooks_test.go.
Output: hermes.go, cline.go (+ cline_windows.go stub), opencode_plugin.go, dispatch in hooks.go,
contract-shape tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hermes installer (fail-open harness, JSON-only block path)</name>
  <files>internal/hooks/hermes.go, internal/hooks/hooks.go</files>
  <read_first>
    - internal/hooks/codex.go (the config-file install/uninstall analog; Hermes config is YAML not JSON)
    - internal/check/deny_render.go (the HarnessHermes branch from Plan 01: ExitCode 0 + {"action":"block","message":...} non-empty message — confirms the installer must rely on JSON, not exit code)
    - internal/hooks/hooks.go (path helpers + dispatch)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md (matrix row 10 Hermes: ~/.hermes/config.yaml, pre_tool_call event, fail-OPEN, non-zero exit / timeout / bad-JSON does NOT block → MUST emit block-JSON; §3 family G)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (hermes.go lines 531-543)
  </read_first>
  <action>
    Create internal/hooks/hermes.go. hermesConfigPath(homeDir)=homeDir+"/.hermes/config.yaml".
    hermesPreCommand="beekeeper check --hook hermes". Hermes config is YAML and the repo has NO YAML
    writer dependency — do NOT add gopkg.in/yaml.v3 (CLAUDE.md: no new deps). Instead patch config.yaml
    with a careful, idempotent string/section edit: ensure a `hooks:` mapping with a `pre_tool_call:`
    entry invoking the hermesPreCommand exists (read existing; if the pre_tool_call beekeeper command is
    already present, no-op; otherwise append the hook block preserving existing content). Back up
    config.yaml before writing (backupSettings) + MkdirAll ~/.hermes. installHermes(configPath, dryRun, out)
    and uninstallHermes(configPath, dryRun, out) (remove the beekeeper pre_tool_call line/block, preserve
    others). Document in a comment: Hermes IGNORES exit codes, so the actual deny is carried by
    RenderDeny(HarnessHermes)'s {"action":"block","message":...} with a guaranteed non-empty message — a
    missing/empty message = silent allow, which is why Plan 01 substitutes a default reason. Add
    TargetHermes="hermes" in hooks.go + path helper + InstallTo/UninstallTo dispatch + fileTargets/allTargets
    + error strings.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/hooks/</automated>
  </verify>
  <acceptance_criteria>
    - internal/hooks/hermes.go contains `beekeeper check --hook hermes` and writes a `pre_tool_call` entry into config.yaml
    - internal/hooks/hermes.go adds NO new module import (no gopkg.in/yaml.v3) — the YAML edit is an in-repo string patch
    - internal/hooks/hooks.go contains TargetHermes + dispatch in both switches
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Hermes installer wires the pre_tool_call --hook command via an idempotent YAML string patch with no new dependency; the JSON-only block path is documented and delegated to RenderDeny.</done>
</task>

<task type="auto">
  <name>Task 2: Cline installer (executable PreToolUse script, macOS/Linux only)</name>
  <files>internal/hooks/cline.go, internal/hooks/cline_windows.go, internal/hooks/hooks.go</files>
  <read_first>
    - internal/hooks/cursor.go (install/uninstall shape analog — but Cline writes an executable file, not JSON)
    - internal/hooks/hooks.go (backupSettings line 139, writeFileAtomic line 162, dispatch)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md (matrix row 4 Cline: hook = executable file named PreToolUse (no ext) in .clinerules/hooks/ or ~/Documents/Cline/Rules/Hooks/; deny via {"cancel":true,"errorMessage"} exit 0 OR exit 2; macOS/Linux ONLY — no Windows)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (cline.go lines 505-527 — the //go:build !windows tag + 0o755 executable + #!/bin/sh script)
  </read_first>
  <action>
    Create internal/hooks/cline.go with a `//go:build !windows` build constraint at the very top (Cline
    hooks are macOS/Linux only — RESEARCH row 4). clineHooksDir(homeDir) returns the global
    ~/Documents/Cline/Rules/Hooks/ path (document the project-local .clinerules/hooks/ alternative in a
    comment). clinePreCommand="beekeeper check --hook cline". installCline(hooksDir, dryRun, out) writes an
    executable file named "PreToolUse" (NO extension) containing a "#!/bin/sh\n" shebang line that execs
    `beekeeper check --hook cline` (passing stdin through). Write with mode 0o755 (executable); MkdirAll the
    hooks dir; back up an existing PreToolUse file first (backupSettings) and only overwrite if it is the
    beekeeper script (idempotent — if the file already contains clinePreCommand, no-op; if it is a foreign
    script, back it up and report, do not silently destroy). uninstallCline removes ONLY the beekeeper
    PreToolUse script (verify it contains clinePreCommand before removing). Because the file is
    build-tagged off Windows, add a companion `//go:build windows` stub file cline_windows.go whose
    installCline/uninstallCline return a clear error: "cline hooks are macOS/Linux only (not supported on
    Windows)". Add TargetCline="cline" in hooks.go + InstallTo/UninstallTo dispatch + allTargets (NOT
    fileTargets if fileTargets implies a settings.json write — it writes an executable; put it where the
    dispatch expects, and document). Update error strings.
  </action>
  <verify>
    <automated>go build ./... && GOOS=windows go build ./internal/hooks/ && GOOS=linux go build ./internal/hooks/</automated>
  </verify>
  <acceptance_criteria>
    - internal/hooks/cline.go begins with `//go:build !windows` and writes an executable "PreToolUse" file (mode 0o755) containing `beekeeper check --hook cline`
    - internal/hooks/cline_windows.go begins with `//go:build windows` and installCline returns a "macOS/Linux only" error on Windows
    - `GOOS=windows go build ./internal/hooks/` AND `GOOS=linux go build ./internal/hooks/` both exit 0
    - internal/hooks/hooks.go contains TargetCline + dispatch in both switches
  </acceptance_criteria>
  <done>Cline installs an executable PreToolUse script wiring --hook cline on macOS/Linux and returns a clear unsupported error on Windows, both compiling cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: OpenCode JS plugin installer + dispatch + contract-shape tests for all three</name>
  <files>internal/hooks/opencode_plugin.go, internal/hooks/hooks.go, internal/hooks/hooks_test.go, internal/hooks/cline_test.go</files>
  <read_first>
    - internal/hooks/gateway_targets.go (printOpenCodeGuide lines 57-89 — the existing OpenCode gateway guide that this UPGRADES to a real plugin installer; the printGuide fmt.Fprintf pattern)
    - internal/hooks/hooks.go (TargetOpenCode already exists in gatewayTargets line 30 — this plan moves it to a plugin installer; the InstallTo case line 78 currently routes opencode to printGatewayGuide)
    - internal/hooks/hooks_test.go (readJSON + per-harness test idioms; TestInstallGatewayTarget for the printed-guide assertion shape)
    - internal/hooks/hermes.go + cline.go (Tasks 1-2 — the installers these tests cover)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md (matrix row 12 OpenCode: PLUGIN, tool.execute.before JS/TS hook, throw new Error(...) — ship a plugin in ~/.config/opencode/plugins/ that shells to beekeeper check and throws; caveat: does NOT catch subagent task calls #5894 or MCP #2319)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (opencode_plugin.go lines 547-560)
  </read_first>
  <action>
    Create internal/hooks/opencode_plugin.go. openCodePluginDir(homeDir)=homeDir+"/.config/opencode/plugins".
    installOpenCodePlugin(pluginDir, dryRun, out) writes a file beekeeper.js into pluginDir whose content is
    a const openCodePluginTemplate JS string implementing the `tool.execute.before` hook: it shells out
    (child_process / Bun spawn as appropriate for OpenCode's runtime) to `beekeeper check --hook opencode`
    feeding the tool call as stdin, and on a non-zero exit (or a deny signal) `throw new Error(...)` with the
    reason so OpenCode aborts the tool. MkdirAll the plugin dir; back up an existing beekeeper.js; idempotent
    (if present + unchanged, report no-op). After writing, print a CAVEAT to out: the plugin does NOT catch
    subagent `task` calls (#5894) or (historically) MCP calls (#2319) — those route through the MCP gateway.
    uninstallOpenCodePlugin removes beekeeper.js. In hooks.go: REMOVE TargetOpenCode from the gatewayTargets
    map and re-route its InstallTo/UninstallTo case from printGatewayGuide to installOpenCodePlugin/uninstall;
    keep TargetOpenCode const. Then extend internal/hooks/hooks_test.go: TestInstallHermes (config.yaml has
    a pre_tool_call beekeeper entry; idempotent; uninstall removes it; foreign content preserved) and
    TestInstallOpenCodePlugin (beekeeper.js written under plugins/, contains "tool.execute.before" and
    "beekeeper check --hook opencode" and a throw; idempotent; uninstall removes it). Create a SEPARATE
    file internal/hooks/cline_test.go with `//go:build !windows` containing TestInstallCline (assert the
    PreToolUse file is executable mode 0o755 and contains the --hook cline command; idempotent; uninstall;
    foreign script preserved) — keep it build-tagged so it does not compile on Windows. Add the three
    targets to TestInstallDispatch coverage in hooks_test.go (guard the cline case with runtime.GOOS so the
    dispatch test still passes on Windows where installCline returns the unsupported error).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/hooks/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/hooks/opencode_plugin.go writes beekeeper.js containing `tool.execute.before`, `beekeeper check --hook opencode`, and a `throw`
    - internal/hooks/hooks.go no longer maps TargetOpenCode in gatewayTargets and routes it to installOpenCodePlugin
    - `go test ./internal/hooks/ -run 'TestInstallHermes|TestInstallOpenCodePlugin' -count=1` exits 0
    - internal/hooks/cline_test.go begins with `//go:build !windows` and asserts the PreToolUse file mode is 0o755 + contains the --hook cline command
    - `go test ./internal/hooks/ -count=1` exits 0 (no regression)
  </acceptance_criteria>
  <done>OpenCode ships a real throw-on-deny plugin (replacing the gateway guide); Hermes/Cline/OpenCode have contract-shape tests; all three dispatch by target name.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New in this plan: `internal/hooks/hermes.go` (`installHermes`/`uninstallHermes`/`hermesPreCommand`/
`hermesConfigPath`), `cline.go` + `cline_windows.go` (`installCline`/`uninstallCline`/`clinePreCommand`/
`clineHooksDir` + windows stub), `opencode_plugin.go` (`installOpenCodePlugin`/`uninstallOpenCodePlugin`/
`openCodePluginDir`/`openCodePluginTemplate`); the consts `TargetHermes`, `TargetCline`; test funcs
`TestInstallHermes`, `TestInstallOpenCodePlugin`, and `TestInstallCline` (in build-tagged
`cline_test.go`). Modified existing: `TargetOpenCode` moves from gatewayTargets to a plugin installer.
The full phase-wide symbol inventory lives in 10-01-PLAN.md's `<artifacts_this_phase_produces>`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes → JSON deny | Hermes ignores exit codes; the block exists ONLY if the JSON has a non-empty message |
| Cline → executable hook | Cline runs an executable file; on Windows it does not run at all |
| OpenCode plugin → beekeeper check | The plugin must throw on deny; subagent/MCP calls bypass it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Tampering (Hermes silent allow) | Hermes empty message | mitigate | RenderDeny(HarnessHermes) guarantees a non-empty message (substitutes a default when reason is empty); the Plan 01 hermes test asserts ExitCode 0 + non-empty {"action":"block"}; the installer wires the JSON-emitting command |
| T-10-18 | Information Disclosure (overclaim Windows) | Cline on Windows | mitigate | Cline is build-tagged !windows with a windows stub that returns a clear "macOS/Linux only" error; the support matrix (Plan 06) documents the caveat so users do not assume Windows protection |
| T-10-19 | Elevation (subagent bypass) | OpenCode plugin | accept | The plugin cannot catch subagent task (#5894) / MCP (#2319) calls — an upstream limitation; documented as a caveat in the install output AND the Plan 06 support matrix; MCP calls route through the gateway (Plan 06) |
| T-10-20 | Spoofing (destroy foreign Cline hook) | Cline file overwrite | mitigate | installCline backs up and refuses to silently destroy a foreign PreToolUse script (only overwrites if it already contains clinePreCommand); uninstall verifies the beekeeper marker before removing |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs; the OpenCode plugin is a shipped JS template string, not an installed npm package; Hermes YAML + Cline script are in-repo writes with no new Go module dependency |
</threat_model>

<verification>
- `go build ./...`, `GOOS=windows go build ./internal/hooks/`, `GOOS=linux go build ./internal/hooks/` all clean.
- `go test ./internal/hooks/ -count=1` green (Hermes + OpenCode tests on all OSes; Cline test on non-Windows).
</verification>

<success_criteria>
- Hermes installer wires the pre_tool_call --hook command via an idempotent YAML string patch (no new dep); the deny is carried by RenderDeny's non-empty block JSON.
- Cline installs an executable PreToolUse script on macOS/Linux and returns a clear unsupported error on Windows.
- OpenCode ships a throw-on-deny JS plugin (replacing the gateway guide) with the subagent/MCP-bypass caveat printed.
- All three dispatch by target name and have contract-shape tests.
</success_criteria>

<output>
Create `.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-05-SUMMARY.md` when done.
</output>
