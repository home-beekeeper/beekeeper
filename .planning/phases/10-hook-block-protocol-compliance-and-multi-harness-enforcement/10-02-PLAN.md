---
phase: 10-hook-block-protocol-compliance-and-multi-harness-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: []
autonomous: false
requirements: [HPC-04]
must_haves:
  truths:
    - "After installing the fixed hook on Claude Code, a canary credential-read tool call is DENIED end-to-end (tool never executes), proven live"
    - "The audit log shows a PreToolUse policy_decision=block with NO following PostToolUse tool_result for the blocked call"
    - "~/.claude/settings.json is restored byte-exact after the test (no residue)"
  artifacts:
    - path: ".planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-02-SUMMARY.md"
      provides: "Recorded live-proof result (pass/fail + audit-log smoking-gun evidence)"
  key_links:
    - from: "beekeeper check --hook claude-code"
      to: "Claude Code PreToolUse honoring exit 2"
      via: "installed settings.json hook"
      pattern: "claude-code"
---

<objective>
Re-prove the bug is fixed on the ONLY locally-installed harness: install the corrected hook
via `beekeeper hooks install --target claude-code`, attempt a canary credential read in a
fresh Claude Code session, and confirm it is ACTUALLY denied (tool never executes) — not just
audited. This is the live, manual, Claude-Code-only verification (CI cannot run a real harness).

Purpose: HPC-04 — the headline guarantee ("a hijacked agent cannot act") is only credible if
the live end-to-end block is observed, not just unit-tested. This closes the exact gap the
2026-06-05 dogfood exposed.
Output: a recorded pass/fail with the audit-log smoking-gun, plus byte-exact settings restore.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-LIVE-TEST-RESUME.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the --hook binary onto PATH and pre-flight the adapter (no live settings touched yet)</name>
  <files>C:\Users\Bantu\go\bin\beekeeper.exe (PATH binary, rebuilt via go install)</files>
  <read_first>
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-LIVE-TEST-RESUME.md (steps 1-3 — the machine state: binary at C:\Users\Bantu\go\bin\beekeeper.exe, catalog at %APPDATA%\beekeeper\catalogs\bumblebee.idx, the canary-safe credential path)
    - internal/check/deny_render.go (the RenderDeny contract delivered by Plan 01 — confirms exit 2 on a credential block)
  </read_first>
  <action>
    Run `go install ./cmd/beekeeper` to refresh the PATH binary (C:\Users\Bantu\go\bin\beekeeper.exe)
    with the Plan 01 --hook build. Confirm the catalog index exists at
    %APPDATA%\beekeeper\catalogs\bumblebee.idx (run `beekeeper catalogs sync` only if absent — without a
    catalog, fail-closed blocks everything and the test is meaningless). Pre-flight WITHOUT touching live
    settings: pipe an allow case — a Read of ./README.md — to `beekeeper check --hook claude-code` and
    confirm it exits 0. Then pipe a canary credential-read tool call (a Bash tool_input reading a
    NONEXISTENT canary path such as ~/.ssh/id_rsa-beekeeper-canary-DOES-NOT-EXIST, NEVER the real key)
    and confirm it now exits 2 (was 1 before the fix) and writes hookSpecificOutput deny JSON to stdout.
    Record both exit codes in the SUMMARY. This validates the adapter end-to-end at the CLI level before
    any live-settings mutation.
  </action>
  <verify>
    <automated>go build ./cmd/beekeeper/ && go install ./cmd/beekeeper</automated>
    <human-check>The canary credential read piped to `beekeeper check --hook claude-code` exits 2 (not 1) and prints "permissionDecision":"deny"; the README read exits 0.</human-check>
  </verify>
  <acceptance_criteria>
    - `go install ./cmd/beekeeper` exits 0 and the PATH beekeeper reports the new build (`beekeeper version` or `beekeeper check --help` lists the --hook flag)
    - Piping a README Read to `beekeeper check --hook claude-code` exits 0
    - Piping a canary credential read to `beekeeper check --hook claude-code` exits 2 and stdout contains `"permissionDecision":"deny"`
  </acceptance_criteria>
  <done>The refreshed PATH binary emits exit 2 + deny JSON on a canary credential read and exit 0 on a benign read, recorded in the SUMMARY — before any live settings change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Claude Code end-to-end block re-proof (HPC-04) with byte-exact settings restore</name>
  <files>~/.claude/settings.json (installed + restored byte-exact), %APPDATA%\beekeeper\audit\beekeeper.ndjson (read-only cross-check)</files>
  <read_first>
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-LIVE-TEST-RESUME.md (steps 4-10 — backup, install, RESTART, canary, smoking-gun audit cross-check, cleanup; the full canary-safe procedure)
  </read_first>
  <action>
    This is a human-verified checkpoint: the executor automates the install/uninstall via
    `beekeeper hooks install --target claude-code` and `beekeeper hooks uninstall --target claude-code`,
    and the human performs the in-Claude-Code canary + observes the result. Follow the
    10-LIVE-TEST-RESUME.md steps 4-10 exactly (backup byte-exact, install, RESTART Claude Code, run the
    canary credential read against a NONEXISTENT path, cross-check the audit log, repeat for ~/.aws,
    uninstall, restore byte-exact, remove backups). Record the pass/fail + the audit-log smoking-gun in
    the SUMMARY. See <what-built>/<how-to-verify>/<resume-signal> below for the operator steps.
  </action>
  <what-built>
    Plan 01 delivered the exit-2 `--hook claude-code` adapter + the corrected Claude installer command.
    Task 1 refreshed the PATH binary and pre-flighted the adapter (exit 2 on a canary credential read).
    This checkpoint performs the live, in-Claude-Code re-proof that the hook now actually BLOCKS.
  </what-built>
  <how-to-verify>
    1. Back up settings byte-exact: copy ~/.claude/settings.json (3837 bytes baseline) to a temp path.
    2. Run `beekeeper hooks install --target claude-code`. Confirm GSD hooks preserved (PreToolUse
       beekeeper entry now wires `beekeeper check --hook claude-code`; existing hooks untouched).
    3. RESTART Claude Code (hooks load at session start — the canonical proof uses a fresh session).
    4. In the new session, via the Bash tool attempt the canary credential read against a NONEXISTENT
       path: `echo "RAN: $(cat ~/.ssh/id_rsa-beekeeper-canary-DOES-NOT-EXIST 2>&1)"`.
       - PASS (fix works): the Bash tool call is DENIED by the PreToolUse hook — you NEVER see "RAN:".
       - FAIL (still broken): you see `RAN: cat: ...: No such file` (the tool ran).
       NEVER `cat ~/.ssh/id_rsa` (the real key may exist). Always use a canary suffix.
    5. Smoking-gun cross-check in %APPDATA%\beekeeper\audit\beekeeper.ndjson: after the canary, a
       PreToolUse `"policy_decision":"block"` with NO following PostToolUse `tool_result` for that call =
       the tool did NOT run (fix confirmed). Before the fix, the block was followed by a tool_result.
    6. Repeat for a Read tool on a canary ~/.aws/credentials-canary (a different extraction path).
    7. CLEANUP (mandatory): `beekeeper hooks uninstall --target claude-code`, then restore settings
       byte-exact from the backup and verify with `cmp` (or fc on Windows); remove
       *.beekeeper-backup-* files. Confirm the PreToolUse count returns to baseline.
  </how-to-verify>
  <verify>
    <human-check>Canary credential read is DENIED in a fresh Claude Code session (no "RAN:" line); audit log shows the block with NO following tool_result; settings restored byte-exact (cmp/fc clean).</human-check>
  </verify>
  <resume-signal>Type "approved" with the result (DENIED end-to-end, or describe what ran), and confirm settings restored byte-exact.</resume-signal>
  <done>A canary credential read is blocked live in Claude Code (no tool_result after the block) and settings are restored byte-exact with backups removed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces NO new code symbols — it is a live-verification + a recorded result
(`10-02-SUMMARY.md`). It consumes the Plan 01 artifacts (`RenderDeny`, the `--hook` flag, the
corrected `claudePreCommand`). The only filesystem effects are transient (install then byte-exact
restore of `~/.claude/settings.json`).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → installed hook | The live agent invokes the installed `beekeeper check --hook claude-code`; whether it honors exit 2 is the thing under test |
| test → ~/.claude/settings.json | The test mutates the developer's live Claude settings; it must restore them byte-exact |
| canary → real credentials | The canary must NEVER touch a real key (use a nonexistent suffix) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Information Disclosure (leak real key) | live canary command | mitigate | The canary reads ONLY nonexistent suffixed paths (id_rsa-beekeeper-canary-DOES-NOT-EXIST, credentials-canary) — never the real key. PASS is "no RAN: line"; observing the file contents is never the success signal |
| T-10-07 | Tampering (corrupt user settings) | install/uninstall on live settings | mitigate | Byte-exact backup before install + cmp/fc verification after restore + backup-file removal; the installer's merge-not-clobber path already preserves the 5 GSD hooks |
| T-10-08 | Repudiation (unproven claim) | "block works" assertion | mitigate | The audit-log smoking-gun (block record with NO following tool_result) is the objective evidence recorded in the SUMMARY, not just the absence of a printed line |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs; `go install` builds the already-audited local module only |
</threat_model>

<verification>
- `go install ./cmd/beekeeper` succeeds; the PATH binary exposes `--hook`.
- Live: canary credential read is DENIED in a fresh Claude Code session (no "RAN:" line).
- Audit cross-check: PreToolUse block with NO following PostToolUse tool_result for the canary call.
- Settings restored byte-exact (cmp/fc clean); backup files removed.
</verification>

<success_criteria>
- On Claude Code, a canary credential-read tool call is BLOCKED live end-to-end (tool never executes), proven via the absence of a tool_result audit record after the block.
- The live test leaves ~/.claude/settings.json byte-exact and removes all backup residue.
</success_criteria>

<output>
Create `.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-02-SUMMARY.md` when done, recording the pass/fail and the audit-log smoking-gun evidence.
</output>
