---
phase: 05-contribution-back-milestone-close
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/catalog/selfcatalog_test.go
  - internal/check/selftest.go
  - internal/check/corpus/fixtures.json
autonomous: true
requirements: [SDEF-01]
must_haves:
  truths:
    - "selfCatalogAdapter.LookupAll(\"beekeeper\", \"pollen\") returns a CatalogMatch for a known-bad Pollen version"
    - "beekeeper selftest passes with the extended pollen-self fixture (PASS count increases by one, zero failures)"
    - "The selftest fixture uses a non-production version string so a real pollen v0.1.1-pollen.N never trips a false self-quarantine"
  artifacts:
    - path: "internal/catalog/selfcatalog_test.go"
      provides: "TestSelfCatalogAdapter_PollenEntries asserting the pollen discriminator"
      contains: "TestSelfCatalogAdapter_PollenEntries"
    - path: "internal/check/selftest.go"
      provides: "selftestEntries extended with a beekeeper/pollen catalog.Entry"
      contains: "Package:      \"pollen\""
    - path: "internal/check/corpus/fixtures.json"
      provides: "pollen-self selftest fixture (warn/catalog-match) with non-production version"
      contains: "pollen"
  key_links:
    - from: "internal/check/corpus/fixtures.json pollen fixture"
      to: "internal/check/selftest.go selftestEntries"
      via: "ecosystem=beekeeper package=pollen version match builds a catalog hit"
      pattern: "\"package\":\\s*\"pollen\""
    - from: "internal/catalog/selfcatalog_test.go"
      to: "selfCatalogAdapter.LookupAll"
      via: "selfCatalogEntry{Ecosystem:beekeeper, Package:pollen} discriminator"
      pattern: "LookupAll\\(\"beekeeper\", \"pollen\"\\)"
---

<objective>
Add `pollen-self` entries to the unified `beekeeper-self` catalog so a compromised Pollen release is detectable by beekeeper's own self-quarantine mechanism (SDEF-01, D-9). Extend the self-catalog adapter test, the selftest entry table, and the embedded corpus so `beekeeper selftest` stays green with the extended catalog.

Purpose: SDEF-01 is the recursive self-defense across the dependency boundary — beekeeper protects against a tampered Pollen binary the same way it protects against a tampered beekeeper binary. This is the Tampering mitigation for "compromised Pollen release."

Output: One new self-catalog adapter test, one extended selftest entry, one corpus fixture, and an optional feed fixture — all using the unified `beekeeper-self` catalog (NOT a separate catalog; SELF-02 is v2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contribution-back-milestone-close/05-CONTEXT.md
@.planning/phases/05-contribution-back-milestone-close/05-RESEARCH.md

<interfaces>
<!-- Extracted from internal/catalog/selfcatalog.go and internal/check/selftest.go. -->

selfCatalogEntry (feed schema, internal/catalog/selfcatalog.go):
```go
type selfCatalogEntry struct {
    ID            string   `json:"id"`
    Name          string   `json:"name"`
    Ecosystem     string   `json:"ecosystem"`
    Package       string   `json:"package"`
    Versions      []string `json:"versions"`
    Severity      string   `json:"severity"`
    CatalogSource string   `json:"catalog_source"`
}
```

selfCatalogAdapter.LookupAll only matches `ecosystem == "beekeeper"`, then filters by `Package` (empty pkg matches all):
```go
func (a *selfCatalogAdapter) LookupAll(ecosystem, pkg string) []policy.CatalogMatch {
    if ecosystem != "beekeeper" { return nil }
    // for each entry: if e.Package != pkg && pkg != "" { continue }
    //   per version → policy.CatalogMatch{CatalogSource:"beekeeper-self", EntryID:e.ID, Ecosystem:"beekeeper", Package:e.Package, Version:v, Severity:e.Severity, Signed:true, CatalogVersion:"beekeeper-self"}
}
```

selftestEntries (mmap schema — catalog.Entry, NOT selfCatalogEntry; internal/check/selftest.go):
```go
var selftestEntries = []catalog.Entry{
    {ID, Name, Ecosystem, Package, Versions []string, Severity, CatalogSource string}, ...
}
```
RunSelftest builds an mmap index from selftestEntries, evaluates each corpus fixture via policy.Evaluate, and counts pass/fail. A warn fixture needs a matching selftestEntry (ecosystem+package+version) to produce a catalog hit.

corpus fixture shape (internal/check/corpus/fixtures.json):
```json
{
  "name": "...",
  "tool_call": {"agent_name":"...","tool_name":"Install","tool_input":{"ecosystem":"...","package":"...","version":"..."}},
  "expect_level": "warn",
  "expect_allow": true,
  "expect_catalog_match": true
}
```

Test helper available in selfcatalog_test.go: `signFeedEntries(t, entries)` returns a base64 Ed25519 signature over the entries; `serveFeed`, `readFixtureSelfCatalog`, `SelfCatalogPublicKey`.
</interfaces>

<read_first>
- internal/catalog/selfcatalog.go — `selfCatalogEntry` schema, `selfCatalogAdapter.LookupAll` ecosystem/package discriminator (lines 415-446), `selfFeed`
- internal/catalog/selfcatalog_test.go — `TestSelfCatalogAdapter_LookupAll` (the exact analog to mirror, lines 355-397); `signFeedEntries`, `serveFeed`, `readFixtureSelfCatalog` helpers
- internal/catalog/testdata/selfcatalog_match.json — the existing feed fixture shape to mirror for the optional pollen feed fixture
- internal/check/selftest.go — `selftestEntries` (catalog.Entry slice, lines 29-48), `RunSelftest` flow, `fixtureMatches`
- internal/check/corpus/fixtures.json — existing warn/catalog-match fixtures to mirror
- .planning/phases/05-contribution-back-milestone-close/05-RESEARCH.md — RQ-4 (SDEF-01 entry schema), Pitfall 5 (catalog.Entry vs selfCatalogEntry), Open Question 2 (use a non-production version string), Code Examples Patterns 2 and 4
  </read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pollen-self adapter test + optional feed fixture</name>
  <files>internal/catalog/selfcatalog_test.go, internal/catalog/testdata/selfcatalog_match_pollen.json</files>
  <read_first>
internal/catalog/selfcatalog_test.go (TestSelfCatalogAdapter_LookupAll), internal/catalog/selfcatalog.go (selfCatalogAdapter.LookupAll, selfFeed), internal/catalog/testdata/selfcatalog_match.json
  </read_first>
  <behavior>
    - Test 1: adapter built from a selfCatalogEntry{Ecosystem:"beekeeper", Package:"pollen", Versions:["pollen-test-v0.0.1"]} returns exactly one match for LookupAll("beekeeper","pollen")
    - Test 2: the returned match has CatalogSource "beekeeper-self", Package "pollen", Signed true, Severity "critical"
    - Test 3: LookupAll("beekeeper","beekeeper") against the same pollen-only adapter returns nil (package discriminator excludes pollen)
    - Test 4: LookupAll("npm","pollen") returns nil (non-beekeeper ecosystem)
  </behavior>
  <action>
Add `TestSelfCatalogAdapter_PollenEntries` to selfcatalog_test.go (package `catalog`, same as the existing adapter test). Construct `adapter := &selfCatalogAdapter{entries: []selfCatalogEntry{{ID:"pollen-self-2026-001", Name:"pollen (hypothetical compromised pollen release)", Ecosystem:"beekeeper", Package:"pollen", Versions:[]string{"pollen-test-v0.0.1"}, Severity:"critical", CatalogSource:"beekeeper-self"}}}`. Assert the four behavior cases above, mirroring the assertion style of `TestSelfCatalogAdapter_LookupAll`. The test name MUST be exactly `TestSelfCatalogAdapter_PollenEntries` (VALIDATION.md runs `-run TestSelfCatalogAdapter_PollenEntries`).

Use the NON-production version string `pollen-test-v0.0.1` (per 05-RESEARCH Open Question 2) so the entry can never collide with a real shipped pollen version (`v0.1.1-pollen.N`) and trigger a false quarantine.

Optionally (extends existing fixture coverage, mirror selfcatalog_match.json): create `internal/catalog/testdata/selfcatalog_match_pollen.json` — a `selfFeed` JSON with `schema_version:"1"`, one entry `{id:"pollen-self-2026-001", ecosystem:"beekeeper", package:"pollen", versions:["pollen-test-v0.0.1"], severity:"critical", catalog_source:"beekeeper-self"}`, and a valid `catalog_signature`. The signature must be produced with the test private key; if you cannot statically compute it, prefer building the feed in-test via `signFeedEntries(t, entries)` (as `TestSelfCatalog_CustomKeyVerifiesAndEmbeddedFails` does) instead of a static fixture file. Do not ship an unsigned or wrongly-signed fixture — `parseAndVerifySelfFeed` fails closed on bad signatures.
  </action>
  <verify>
    <automated>go test ./internal/catalog/ -run TestSelfCatalogAdapter_PollenEntries -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/catalog/ -run TestSelfCatalogAdapter_PollenEntries -count=1` passes
    - Full catalog package green: `go test ./internal/catalog/ -count=1` (no regression to existing self-catalog tests)
    - The pollen entry version string is non-production (`pollen-test-v0.0.1`), not any `v0.1.1-pollen.N`
    - If a static feed fixture was added, `go test ./internal/catalog/ -count=1` still verifies its signature successfully (no fail-closed)
  </acceptance_criteria>
  <done>TestSelfCatalogAdapter_PollenEntries proves the pollen discriminator works against the unified beekeeper-self adapter; catalog package green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend selftestEntries + corpus fixture for pollen-self; beekeeper selftest stays green</name>
  <files>internal/check/selftest.go, internal/check/corpus/fixtures.json</files>
  <read_first>
internal/check/selftest.go (selftestEntries, RunSelftest, fixtureMatches), internal/check/corpus/fixtures.json (existing warn/catalog-match fixtures)
  </read_first>
  <action>
Add one entry to `selftestEntries` in selftest.go (the `catalog.Entry` mmap schema — NOT selfCatalogEntry, per Pitfall 5):
`{ID:"pollen-self-2026-001", Name:"pollen (hypothetical compromised pollen release)", Ecosystem:"beekeeper", Package:"pollen", Versions:[]string{"pollen-test-v0.0.1"}, Severity:"critical", CatalogSource:"beekeeper-self"}`.

Add one fixture to corpus/fixtures.json mirroring the existing direct-shape warn fixtures: a `tool_call` with `tool_name:"Install"`, `tool_input:{ecosystem:"beekeeper", package:"pollen", version:"pollen-test-v0.0.1"}`, `expect_level:"warn"`, `expect_allow:true`, `expect_catalog_match:true`. (A single unsigned bumblebee-adapter source yields warn, not block — consistent with the existing nrwl/shai-hulud fixtures which are also `warn`. Do NOT set `check_provenance` unless you assert the exact corroboration count the engine produces.)

Use the same non-production version `pollen-test-v0.0.1` end-to-end so `beekeeper selftest` exercises the pollen-self detection path without any risk of matching a real shipped pollen version. The PASS count increases by exactly one; no existing fixture regresses.

Confirm the fixture name is descriptive (e.g. `"pollen-self: hypothetical compromised pollen release routes through beekeeper-self catalog"`).
  </action>
  <verify>
    <automated>go test ./internal/check/ -run TestRunSelftest -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/check/ -run TestRunSelftest -count=1` passes (FAIL count is zero; new fixture passes)
    - Full check package green: `go test ./internal/check/ -count=1`
    - The new selftestEntry and corpus fixture both use `pollen-test-v0.0.1` (grep confirms no `v0.1.1-pollen` in the new selftest fixture)
    - `go vet ./internal/check/` clean
  </acceptance_criteria>
  <done>selftestEntries + corpus extended with a pollen-self warn fixture; beekeeper selftest green with one additional pass; non-production version avoids false quarantine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| beekeeper-self feed (HTTP) → self-catalog verifier | Untrusted remote feed bytes cross into beekeeper; Ed25519 signature is the trust anchor |
| pollen binary release → beekeeper consumer | A compromised pollen release is an upstream-of-beekeeper supply-chain risk across the subprocess boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (compromised Pollen release) | internal/catalog/selfcatalog.go selfCatalogAdapter | mitigate | pollen-self entries (`ecosystem:beekeeper, package:pollen`) make a known-bad Pollen version detectable by beekeeper's self-quarantine; `TestSelfCatalogAdapter_PollenEntries` and the selftest corpus fixture prove the detection path. |
| T-05-05 | Tampering (forged/false-quarantine feed) | parseAndVerifySelfFeed | mitigate | Ed25519 feed signature verified before evaluation; fails closed (SelfCatalogFailClosed/errIntegrity) on a bad signature. Any pollen-self feed fixture must be correctly signed or built via signFeedEntries. |
| T-05-06 | Spoofing (false self-quarantine of a valid release) | selftestEntries / corpus version string | mitigate | Use a non-production version `pollen-test-v0.0.1` for all selftest/adapter fixtures so a real `v0.1.1-pollen.N` is never matched by the in-binary test corpus; real compromised-version strings live only in the externally hosted, signed feed. |
</threat_model>

<verification>
- `go test ./internal/catalog/ -run TestSelfCatalogAdapter_PollenEntries -count=1` passes
- `go test ./internal/check/ -run TestRunSelftest -count=1` passes (zero failures)
- `go test ./internal/catalog/ ./internal/check/ -count=1` green
- `go vet ./internal/catalog/ ./internal/check/` clean
- No fixture uses a real `v0.1.1-pollen.N` version string
</verification>

<success_criteria>
- SC5 (SDEF-01): `beekeeper-self` catalog contains `pollen-self` entries so a known-bad Pollen release is detectable by beekeeper's self-quarantine mechanism; `beekeeper selftest` passes with the extended catalog.
</success_criteria>

<output>
After completion, create `.planning/phases/05-contribution-back-milestone-close/05-02-SUMMARY.md`
</output>
