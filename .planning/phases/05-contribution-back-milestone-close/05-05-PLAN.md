---
phase: 05-contribution-back-milestone-close
plan: "05"
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements: [BKINT-02, SYNC-01, PTEST-05, SDEF-01]
must_haves:
  truths:
    - "Both home-beekeeper/pollen and home-beekeeper/beekeeper are pushed to GitHub"
    - "Four signed tags exist on GitHub: v0.1.1-pollen.2, .3, .4, .5 — each cosign-verified against the capital-B Bantuson OIDC identity"
    - "beekeeper CI is green on ubuntu/macos/windows with the Pollen install step succeeding and zero Windows inventory t.Skip"
    - "The Windows honeypot E2E (TestHoneypotExfilFusion) fires SENTRY-005 on the windows-latest runner"
  artifacts:
    - path: ".planning/phases/05-contribution-back-milestone-close/05-05-SUMMARY.md"
      provides: "Record of the release cut: tag hashes, cosign verify output, CI run links"
      contains: "v0.1.1-pollen.5"
  key_links:
    - from: "pollen tag push (v*)"
      to: "release.yml signed-release pipeline"
      via: "cosign keyless OIDC + CycloneDX SBOM + SLSA L3"
      pattern: "v0\\.1\\.1-pollen\\.[2345]"
    - from: "beekeeper CI Install Pollen step"
      to: "the published pollen module"
      via: "go install resolves once Pollen is pushed + tagged"
      pattern: "go install github.com/home-beekeeper/pollen"
---

<objective>
Cut the milestone-close release: push both repos to GitHub, cut the four signed tags (pollen.2/.3/.4/.5) in order via the existing cosign keyless / SLSA L3 pipeline, cosign-verify each, and confirm beekeeper CI is green on all three OSes (including the Windows honeypot E2E and zero inventory skips). This is the D-5 CHECKPOINT plan — the executor does NO local code changes; it presents the runbook and pauses for the maintainer to perform the auth-gated, irreversible GitHub-facing actions.

Purpose: SC6 (the milestone-complete tag) plus the CI-green confirmation of BKINT-02, PTEST-05, and SDEF-01. These steps (`gh repo create`, `git push`, tag pushes that trigger signing, `cosign verify`) are auth-gated and irreversible, so per D-5 they are performed/approved by the maintainer with their own credentials — never run autonomously by the executor.

Output: Four cosign-verified GitHub releases, both repos pushed, green 3-OS CI, and a SUMMARY recording the tag hashes + verification output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contribution-back-milestone-close/05-CONTEXT.md
@.planning/phases/05-contribution-back-milestone-close/05-RESEARCH.md
@docs/release-runbook.md
@.planning/phases/02-windows-root-resolver/02-04-SUMMARY.md
@.planning/phases/03-windows-path-representation/03-03-SUMMARY.md
@.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-03-SUMMARY.md
</context>

<read_first>
- docs/release-runbook.md — the exact, ordered, copy-paste procedure authored in plan 04. This plan EXECUTES it via the checkpoint hand-off. Do not re-derive commands; follow the runbook.
- .planning/phases/05-contribution-back-milestone-close/05-RESEARCH.md — RQ-6 (release pipeline mechanics, runbook), Pitfall 2 (tag order), Pitfall 3 (push before beekeeper CI), Pitfall 4 (capital-B Bantuson cosign identity)
- .planning/STATE.md Deferred Items — the pollen.2/.3/.4 deferred-tag obligations this plan discharges
</read_first>

<checkpoint_note>
This plan is `autonomous: false`. There are no autonomous local-prep tasks left (plans 01-04 did all local work). The executor's role here is to:
1. Confirm all prerequisites (plans 01-04 committed; runbook present; pollen.5 VERSION/CHANGES/UPSTREAM.md committed) by reading state — read-only checks only.
2. Present the runbook to the maintainer and PAUSE at the blocking checkpoint.
3. After the maintainer completes the GitHub-facing steps and reports results, run the read-only verification (cosign verify output review, CI status review via `gh run list`/`gh run watch` if the maintainer authorizes) and record them in the SUMMARY.
The executor MUST NOT run `gh repo create`, `git push`, `git tag ... push`, or `cosign sign`/`cosign verify-blob` itself — those are the maintainer's auth-gated actions.
</checkpoint_note>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight read-only verification (prerequisites for the release)</name>
  <files></files>
  <read_first>
docs/release-runbook.md, ../pollen/VERSION, ../pollen/UPSTREAM.md
  </read_first>
  <action>
Read-only pre-flight checks confirming the local prep from plans 01-04 is complete before the maintainer cuts the release. Do NOT modify anything. Confirm:
- `../pollen/VERSION` reads `0.1.1-pollen.5` and `../pollen/CHANGES.md` has a `## v0.1.1-pollen.5` section (plan 03).
- `../pollen/UPSTREAM.md` has the `## Contribution-back status` section + the version-history rows (plan 03).
- `.github/workflows/ci.yml` has the `go install github.com/home-beekeeper/pollen/cmd/pollen@v0.1.1-pollen.4` step and `internal/scan/pollen_version.go` exists (plan 04).
- `docs/release-runbook.md` exists with the 7-step procedure (plan 04).
- The PTEST-05 and SDEF-01 tests pass locally on the Windows dev host: `go test ./internal/sentry/ ./internal/sentry/windows/ ./internal/catalog/ ./internal/check/ -count=1`.
- Confirm the four tag target commits exist in `../pollen`: `git -C ../pollen log --oneline | Select-String "c94b271|19695e3|b906404"` and the pollen.5 target is the current pollen HEAD after plan 03.

If any check fails, STOP and report which prerequisite plan is incomplete — do not proceed to the checkpoint.
  </action>
  <verify>
    <automated>go test ./internal/sentry/ ./internal/sentry/windows/ ./internal/catalog/ ./internal/check/ -count=1 ; Select-String -Path docs/release-runbook.md -Pattern "cosign verify-blob" -Quiet ; Select-String -Path .github/workflows/ci.yml -Pattern "go install github.com/home-beekeeper/pollen/cmd/pollen" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - All four local-prep deliverables confirmed present (pollen.5 VERSION/CHANGES, UPSTREAM.md contribution-back section, CI install step + PinnedPollenVersion, release-runbook.md)
    - `go test ./internal/sentry/ ./internal/sentry/windows/ ./internal/catalog/ ./internal/check/ -count=1` passes on the Windows dev host
    - No file is modified by this task (read-only)
  </acceptance_criteria>
  <done>All release prerequisites confirmed; the four tag target commits are present; honeypot + pollen-self tests green locally. Ready for the maintainer checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CHECKPOINT — maintainer cuts the four signed tags + pushes both repos (D-5)</name>
  <action>
Maintainer-executed, auth-gated hand-off. The executor presents the runbook and PAUSES; the maintainer performs every GitHub-facing step (repo create, push, tag, cosign verify) with their own `gh`/cosign credentials per the what-built / how-to-verify blocks below. The executor MUST NOT run `gh repo create`, `git push`, `git tag ... push`, `cosign sign`, or `cosign verify-blob` itself — those are the maintainer's auth-gated, irreversible actions (D-5 two-person-rule discipline).
  </action>
  <what-built>
Plans 01-04 produced, autonomously and locally: the PTEST-05 isSensitivePath fix + honeypot test, the SDEF-01 pollen-self catalog entries + selftest fixture, the SYNC-01/SYNC-02 UPSTREAM.md delta, the pollen.5 VERSION/CHANGES bump, the BKINT-02 CI Pollen-install step + PinnedPollenVersion const, and the complete copy-paste release runbook at `docs/release-runbook.md`. Everything is committed locally in the two repos. Nothing has been pushed; no tags exist yet.
  </what-built>
  <how-to-verify>
The remaining steps are auth-gated and irreversible, so they are yours to perform with your own `gh`/cosign credentials. Follow `docs/release-runbook.md` exactly, in order:

1. Create/confirm the beekeeper GitHub remote (`gh repo create home-beekeeper/beekeeper --public --source=. --push`, lowercase bantuson).
2. Push pollen main; wait for the 3-OS pollen CI to go green (`gh -R home-beekeeper/pollen run watch`).
3. Cut the four signed tags IN ORDER — confirm each commit hash with `git -C ../pollen log --oneline` first:
   - v0.1.1-pollen.2 -> c94b271
   - v0.1.1-pollen.3 -> 19695e3
   - v0.1.1-pollen.4 -> b906404 (RECOMMENDED — includes the .vscode-oss/VSCodium WEXT fix; the runbook notes the alternative a9db7b3 if you prefer to exclude that fix)
   - v0.1.1-pollen.5 -> current pollen HEAD (after plan 03's commits)
   Push each tag; each push triggers release.yml (cosign keyless OIDC + CycloneDX SBOM + SLSA L3). Wait for each release job.
4. For EACH of the four releases, download `checksums.txt` + `checksums.txt.sigstore.json` and run the cosign verify command from the runbook — CRITICAL: the identity regexp uses capital-B `^https://github\.com/home-beekeeper/pollen/`. Expected output: `Verified OK`.
5. Push beekeeper main (AFTER the pollen tags exist, so the `go install ...@v0.1.1-pollen.4` CI step resolves). Wait for the beekeeper 3-OS matrix to go green.
6. Confirm on the windows-latest beekeeper run: the Install Pollen step succeeded, `TestHoneypotExfilFusion` passed, and there are zero inventory `t.Skip`.

Report back: the four tag commit hashes you used, the four `cosign verify-blob` outputs (each should be `Verified OK`), and the beekeeper CI run URL/status for all three OSes.
  </how-to-verify>
  <resume-signal>Paste the four `Verified OK` cosign outputs + the beekeeper 3-OS CI status (green/red per OS), or describe any failure (e.g. cosign identity mismatch, go install resolution error, honeypot test failure).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-release verification + tracking update</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
.planning/STATE.md (Deferred Items table, Phase Summary), .planning/ROADMAP.md (Phase 5 section + Progress table)
  </read_first>
  <action>
After the maintainer reports the checkpoint results, verify and record (read-only review of the maintainer-supplied outputs, plus optional `gh run list`/`gh -R home-beekeeper/pollen release list` if authorized):
- All four `cosign verify-blob` outputs were `Verified OK` (capital-B Bantuson identity).
- beekeeper CI is green on ubuntu/macos/windows; the Install Pollen step succeeded; `TestHoneypotExfilFusion` passed on windows-latest; zero inventory `t.Skip`.
- All four GitHub Releases carry a cosign signature + CycloneDX SBOM.

Then update tracking (these edits ARE autonomous — they are beekeeper planning docs, not GitHub-facing):
- STATE.md Deferred Items: mark the pollen.2/.3/.4 deferred signed-tag rows as DISCHARGED (cut at M2 close, this phase), and remove them from "outstanding obligations" framing.
- STATE.md Phase Summary table: Phase 5 status -> Complete; tag `v0.1.1-pollen.5` cut + signed.
- ROADMAP.md Phase 5: mark SC1/SC3/SC4/SC5/SC6 satisfied, SC2 deferred-by-decision (D-2); flip the Phase 5 row to complete in the Progress table.
- Per MEMORY note: phase.complete leaves stale bold v1.1.0 ROADMAP rows + STATE body — fix those manually here (Phases 2/3/4 release rows now say "signed release cut at M2 close" not "deferred").

If the maintainer reported any failure, record it as a blocker and do NOT mark the phase complete.
  </action>
  <verify>
    <automated>Select-String -Path .planning/ROADMAP.md -Pattern "v0.1.1-pollen.5" -Quiet ; Select-String -Path .planning/STATE.md -Pattern "Phase 5" -Quiet ; Select-String -Path .planning/STATE.md -Pattern "DISCHARGED" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md Deferred Items shows pollen.2/.3/.4 signed tags as discharged (cut this phase); Phase 5 marked complete
    - ROADMAP.md Phase 5 reflects SC1/SC3/SC4/SC5/SC6 satisfied + SC2 deferred (D-2); Progress table Phase 5 row complete
    - The four cosign `Verified OK` outputs and the 3-OS CI-green status are recorded in 05-05-SUMMARY.md
    - If any release/CI step failed, the phase is NOT marked complete and the failure is recorded as a blocker
  </acceptance_criteria>
  <done>Release verified (4x Verified OK, 3-OS CI green, honeypot fired on Windows, zero skips); tracking docs updated; milestone v1.1.0 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer credentials -> GitHub | Auth-gated push/tag/cosign cross from the maintainer's machine to GitHub; irreversible outward actions |
| GitHub Actions OIDC -> Sigstore | The release pipeline mints a keyless cosign certificate bound to the Bantuson repo identity |
| published release -> downstream consumers (beekeeper CI, future users) | A signed, SLSA-attested release is the integrity anchor for everything that installs Pollen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering (release artifact integrity) | the four signed tags | mitigate | Each tag push triggers cosign keyless OIDC signing + CycloneDX SBOM + SLSA L3 provenance; the maintainer cosign-verifies every release (`Verified OK`) before the milestone is declared complete. |
| T-05-14 | Spoofing (wrong signing identity) | cosign verify identity | mitigate | Verification pins the OIDC identity regexp to capital-B `^https://github\.com/home-beekeeper/pollen/` (Pitfall 4); a lowercase or mismatched identity fails verification and blocks completion. |
| T-05-15 | Elevation (autonomous irreversible action) | the release checkpoint | mitigate | D-5 two-person-rule discipline: the executor never runs push/tag/sign; the maintainer performs and approves every GitHub-facing step with their own credentials. The plan is autonomous:false with a blocking checkpoint. |
| T-05-16 | Repudiation (untracked release) | STATE.md / ROADMAP.md | mitigate | Post-release, the four tag hashes + cosign outputs + CI run links are recorded in 05-05-SUMMARY.md and the deferred-release obligations are explicitly marked discharged. |
</threat_model>

<verification>
- Pre-flight (read-only): all plan 01-04 deliverables present; honeypot + pollen-self tests green locally
- Checkpoint (maintainer): both repos pushed; four signed tags cut in order; four `cosign verify-blob` -> `Verified OK` (capital-B identity); beekeeper 3-OS CI green; honeypot fired on windows-latest; zero inventory skips
- Post-release: STATE.md/ROADMAP.md updated; deferred pollen.2/.3/.4 tags marked discharged; 05-05-SUMMARY.md records tag hashes + cosign outputs + CI status
</verification>

<success_criteria>
- SC6: `v0.1.1-pollen.5` tagged + signed — the milestone-complete tag, cut together with the discharged pollen.2/.3/.4 tags.
- SC3 (BKINT-02) confirmed: beekeeper CI green on 3 OSes with the Pollen install step and zero Windows inventory skips.
- SC4 (PTEST-05) confirmed on CI: TestHoneypotExfilFusion fires SENTRY-005 on windows-latest.
- SC5 (SDEF-01) confirmed on CI: beekeeper selftest green with the pollen-self catalog.
- SC1 (SYNC-01) shipped; SC2 (SYNC-02) deferred-by-documentation (D-2). Milestone v1.1.0 "Pollen" closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-contribution-back-milestone-close/05-05-SUMMARY.md`
</output>
