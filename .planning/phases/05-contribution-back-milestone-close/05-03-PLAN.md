---
phase: 05-contribution-back-milestone-close
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - ../pollen/UPSTREAM.md
  - ../pollen/VERSION
  - ../pollen/CHANGES.md
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "UPSTREAM.md documents a repeatable 8-step sync workflow a second maintainer could follow cold, with real commands"
    - "UPSTREAM.md version-history table has rows for pollen.2, pollen.3, pollen.4, and pollen.5"
    - "UPSTREAM.md records the prepared Windows patch set and the contribution-back-deferred rationale (SYNC-02 satisfied-by-documented-deferral; NO upstream PR opened)"
    - "../pollen/VERSION reads 0.1.1-pollen.5 and CHANGES.md has a v0.1.1-pollen.5 milestone-close section"
  artifacts:
    - path: "../pollen/UPSTREAM.md"
      provides: "Sync workflow + version history + contribution-back-deferred section"
      contains: "Contribution-back status"
    - path: "../pollen/VERSION"
      provides: "Milestone-close version string"
      contains: "0.1.1-pollen.5"
    - path: "../pollen/CHANGES.md"
      provides: "v0.1.1-pollen.5 changelog section"
      contains: "v0.1.1-pollen.5"
  key_links:
    - from: "../pollen/UPSTREAM.md version history"
      to: "the four signed tags cut in plan 05"
      via: "rows pollen.2/3/4/5 with pinned upstream commit and summary"
      pattern: "v0\\.1\\.1-pollen\\.[2345]"
    - from: "../pollen/UPSTREAM.md Contribution-back status"
      to: "SYNC-02 deferral (D-2)"
      via: "prepared patch set commit range + upstream #3/#4 rationale"
      pattern: "deferred"
---

<objective>
Extend `../pollen/UPSTREAM.md` with the repeatable sync workflow version history and the contribution-back-deferred rationale (SYNC-01 + SYNC-02-by-documented-deferral, D-6/D-2), and bump `../pollen/VERSION` + `CHANGES.md` to `0.1.1-pollen.5` (the milestone-close release prep). All work is LOCAL and committed in the `../pollen` sibling repo — no push, no tag (the signed-tag cut is plan 05's checkpoint).

Purpose: SYNC-01 makes the fork self-sufficient — a second maintainer can absorb upstream cold. SYNC-02 is descoped (D-2): no PRs against perplexityai/bumblebee; instead UPSTREAM.md records the prepared Windows patch set and why contribution-back is deferred (upstream already has ignored Windows PRs #3/#4; their maintainers plan their own update). The pollen.5 VERSION/CHANGES prep is the last release-metadata step before the batched tag cut.

Output: An extended UPSTREAM.md, VERSION=0.1.1-pollen.5, a new CHANGES.md section — all committed in `../pollen`. NO `gh pr create`, NO `git push`, NO `git tag` (those are plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contribution-back-milestone-close/05-CONTEXT.md
@.planning/phases/05-contribution-back-milestone-close/05-RESEARCH.md
</context>

<read_first>
- ../pollen/UPSTREAM.md — the EXISTING sync doc to extend (fork metadata, 8-step sync workflow, version history with only the pollen.1 row). Do NOT recreate it — append/extend.
- ../pollen/VERSION — currently `0.1.1-pollen.4`; bump to `0.1.1-pollen.5`
- ../pollen/CHANGES.md — newest-first changelog; existing pollen.2/3/4 sections show the "prepared, not yet tagged" convention to mirror
- .planning/phases/05-contribution-back-milestone-close/05-RESEARCH.md — RQ-5 (UPSTREAM.md delta: version-history rows, contribution-back note, prepared patch set commit range `2c202ef..b906404`), Pitfall 6 (pollen.5 commit must include the UPSTREAM.md delta before tagging)
- .planning/phases/02-windows-root-resolver/02-04-SUMMARY.md and .planning/phases/03-windows-path-representation/03-03-SUMMARY.md — the established "prepared, not yet tagged" CHANGES.md wording and the deferred-release precedent
</read_first>

<cross_repo_note>
All edits and commits in this plan happen in the sibling `../pollen` repo (`C:\Users\Bantu\mzansi-agentive\pollen`). Commit there with `git -C ../pollen ...` (or `cd ../pollen && git ...`). Do NOT stage `../pollen` paths into the beekeeper repo. The upstream pinned commit is unchanged this release (`c24089804ee66ece4bec6f14638cb98985389cdb`) — pollen.2/3/4/5 are Windows-addition releases, not upstream-absorption releases.
</cross_repo_note>

<tasks>

<task type="auto">
  <name>Task 1: UPSTREAM.md — version history rows + contribution-back-deferred section</name>
  <files>../pollen/UPSTREAM.md</files>
  <read_first>
../pollen/UPSTREAM.md (existing 8-step workflow + version history table), 05-RESEARCH.md RQ-5
  </read_first>
  <action>
Extend (do NOT recreate) `../pollen/UPSTREAM.md`:

1. Version history table: add four rows below the existing pollen.1 row — pollen.2, pollen.3, pollen.4, pollen.5. All four use the SAME pinned upstream commit `c24089804ee66ece4bec6f14638cb98985389cdb` (no upstream sync occurred; these are Windows-addition releases). Date absorbed = the release date; Summary per row: pollen.2 "Windows root resolver (WRES-01/02, PTEST-01)", pollen.3 "Windows path representation (WPATH-01/02)", pollen.4 "Windows extension & MCP coverage (WEXT-01/02/03)", pollen.5 "Milestone close — UPSTREAM sync doc (SYNC-01), beekeeper CI pin (BKINT-02), Windows honeypot (PTEST-05), pollen-self catalog (SDEF-01)".

2. New `## Contribution-back status` section documenting (per D-2):
   - The Windows additions are prepared as a patch set available in this repo across commits `2c202ef..b906404` (the Phase-2/3/4 Windows commits: root resolver, path representation, extension/MCP coverage). Name the requirement groups WRES-01/02, WPATH-01/02, WEXT-01/02/03.
   - Upstream `perplexityai/bumblebee` already has open Windows-support PRs (#3, #4) that are being ignored while other PRs get feedback/merged; a commenter was told the maintainers plan their own Windows update.
   - Therefore contribution-back is DEFERRED to a future milestone (SYNC-02 / roadmap SC2 explicitly deferred per maintainer decision D-2). The intent is documented and re-openable: if upstream signals readiness, the prepared patch set can be submitted as upstream-shaped PRs (root resolver as a build-tag pattern like upstream PR #4, path representation, extension/MCP coverage), each referencing the equivalent Pollen tag and linking parity-test output as evidence.
   - Do NOT open any PR. This section IS the SYNC-02 deliverable (satisfied-by-documented-deferral).

3. Confirm the existing 8-step sync workflow (fetch upstream → diff-review → run upstream tests on Linux+macOS → cherry-pick preserving Windows paths → re-run 3-OS CI → re-run differential → update pinned commit + CHANGES.md → tag pollen.N) is present and concrete (real commands, not prose). If any step is prose-only, tighten it to a runnable command so a second maintainer can follow it cold (SC1). The workflow already exists per the read — verify and, if needed, add a worked example invocation.

Commit in pollen: `cd ../pollen && git add UPSTREAM.md && git commit` with message `docs(05-03): UPSTREAM.md sync version history + contribution-back-deferred (SYNC-01, SYNC-02 D-2)`.
  </action>
  <verify>
    <automated>cd ../pollen && git -C . grep -l "Contribution-back status" UPSTREAM.md && Select-String -Path UPSTREAM.md -Pattern "v0.1.1-pollen.5" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - UPSTREAM.md contains a `## Contribution-back status` section naming the deferral (D-2), upstream #3/#4, and the prepared patch-set commit range
    - UPSTREAM.md version-history table has rows for pollen.2, pollen.3, pollen.4, pollen.5 (all pinned to `c24089804ee66ece4bec6f14638cb98985389cdb`)
    - The 8-step sync workflow is present with runnable commands (second-maintainer-followable, SC1)
    - NO `gh pr create` was run; no PR exists against perplexityai/bumblebee (D-2)
    - Change is committed in `../pollen` only (beekeeper `git status` shows no `../pollen` paths)
  </acceptance_criteria>
  <done>UPSTREAM.md extended with version history + contribution-back-deferred rationale; SYNC-01 and SYNC-02-by-documented-deferral satisfied; committed in pollen.</done>
</task>

<task type="auto">
  <name>Task 2: pollen.5 VERSION bump + CHANGES.md milestone-close section</name>
  <files>../pollen/VERSION, ../pollen/CHANGES.md</files>
  <read_first>
../pollen/VERSION, ../pollen/CHANGES.md (existing pollen.2/3/4 "prepared, not yet tagged" sections), 02-04-SUMMARY.md / 03-03-SUMMARY.md (wording precedent)
  </read_first>
  <action>
Bump `../pollen/VERSION` from `0.1.1-pollen.4` to `0.1.1-pollen.5` (single line).

Prepend a `## v0.1.1-pollen.5 (<release date>) — Milestone close` section to CHANGES.md (newest-first ordering, above the pollen.4 section), mirroring the existing prepared-section convention. Content:
- A status blockquote noting this is the milestone-complete tag, cut together with the deferred pollen.2/3/4 tags at M2 close (cross-reference beekeeper STATE.md Deferred Items + the release runbook).
- Summary: SYNC-01 (UPSTREAM.md sync workflow + version history), SYNC-02 deferred-by-documentation (D-2 — no upstream PRs), and the cross-repo beekeeper deliverables that close the milestone (BKINT-02 CI Pollen pin, PTEST-05 Windows honeypot, SDEF-01 pollen-self catalog).
- schema_version stays `0.1.0` (behavioral fork, not protocol fork).
- Note: no Pollen source-code change in this release beyond the UPSTREAM.md/VERSION/CHANGES metadata — the milestone-close work landed in beekeeper.

Commit in pollen: `cd ../pollen && git add VERSION CHANGES.md && git commit` with message `release(05-03): prepare v0.1.1-pollen.5 — milestone close (tag/sign in plan 05)`. Do NOT create a git tag and do NOT push.
  </action>
  <verify>
    <automated>cd ../pollen && Select-String -Path VERSION -Pattern "0.1.1-pollen.5" -Quiet && Select-String -Path CHANGES.md -Pattern "v0.1.1-pollen.5" -Quiet && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `../pollen/VERSION` reads `0.1.1-pollen.5`
    - `../pollen/CHANGES.md` has a `## v0.1.1-pollen.5` section above the pollen.4 section
    - `cd ../pollen && go build ./...` exits 0 (VERSION is consumable)
    - `git -C ../pollen tag --list 'v0.1.1-pollen.5'` is empty (no tag created — that is plan 05)
    - Committed in `../pollen` only
  </acceptance_criteria>
  <done>VERSION=0.1.1-pollen.5, CHANGES.md has the milestone-close section, pollen builds; no tag, no push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream perplexityai/bumblebee → pollen fork | Untrusted upstream commits cross into the fork only via the documented diff-review sync workflow (two-person-rule gate) |
| documentation → second maintainer | UPSTREAM.md is the operational contract; an incomplete workflow could lead a maintainer to absorb upstream without the differential gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering (malicious upstream absorption) | UPSTREAM.md sync workflow | mitigate | The documented 8-step workflow makes diff-review (step 2) and the byte-for-byte differential re-run (step 6) mandatory gates before any upstream code is absorbed — the two-person-rule discipline from PRD §12.2. |
| T-05-08 | Repudiation (undocumented provenance) | UPSTREAM.md version history + pinned commit | mitigate | Version-history table records every pollen.N release against its pinned 40-char upstream SHA; the prepared-patch-set commit range is named so contribution-back provenance is auditable later. |
| T-05-09 | Information Disclosure | Contribution-back section | accept | The section references public upstream PR numbers and public commit hashes only; no secrets. Low risk. |
</threat_model>

<verification>
- UPSTREAM.md contains `## Contribution-back status` + version-history rows pollen.2/3/4/5 + the runnable 8-step workflow
- `../pollen/VERSION` = `0.1.1-pollen.5`; CHANGES.md has the `## v0.1.1-pollen.5` section
- `cd ../pollen && go build ./...` exits 0
- No tag, no push, no PR (plan 05 owns those); beekeeper repo untouched by this plan
</verification>

<success_criteria>
- SC1 (SYNC-01): UPSTREAM.md documents a repeatable, second-maintainer-followable sync workflow with version history.
- SC2 relaxed (SYNC-02, D-2): no upstream PR; the prepared Windows patch set + contribution-back-deferred rationale are documented in UPSTREAM.md (satisfied-by-documented-deferral).
- pollen.5 release metadata prepared (VERSION + CHANGES) — the local prep the plan-05 checkpoint tags.
</success_criteria>

<output>
After completion, create `.planning/phases/05-contribution-back-milestone-close/05-03-SUMMARY.md`
</output>
