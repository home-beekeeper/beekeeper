---
phase: 12-design-system
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - web/app/providers.tsx
  - web/lib/reduced-motion.tsx
autonomous: true
requirements: [DSYS-02, DSYS-03]
user_setup: []

must_haves:
  truths:
    - "A ThemeProvider (next-themes, class strategy, storageKey bk-theme) is established as the outermost theme context"
    - "A ReducedMotionProvider exposes a useReducedMotion() hook and writes data-reduced-motion to <html>"
    - "providers.tsx carries the marked Phase 13 RootProvider insertion point so docs can wire in cleanly"
  artifacts:
    - path: "web/app/providers.tsx"
      provides: "Providers wrapper — ThemeProvider + ReducedMotionProvider"
      exports: ["Providers"]
      contains: 'storageKey="bk-theme"'
    - path: "web/lib/reduced-motion.tsx"
      provides: "ReducedMotionProvider + useReducedMotion() hook (DSYS-03 + Phase 16 R3F contract)"
      exports: ["ReducedMotionProvider", "useReducedMotion"]
  key_links:
    - from: "web/app/providers.tsx"
      to: "web/lib/reduced-motion.tsx"
      via: "import { ReducedMotionProvider } from \"@/lib/reduced-motion\""
      pattern: "@/lib/reduced-motion"
    - from: "web/app/providers.tsx"
      to: "next-themes"
      via: "import { ThemeProvider } from \"next-themes\""
      pattern: "from \"next-themes\""
---

<objective>
Create the two React context providers the design system needs: the next-themes ThemeProvider
(theme toggle + persistence + no-FOUC wiring, DSYS-02) and the ReducedMotionProvider with its
useReducedMotion() hook and data-reduced-motion attribute (site-wide motion gate, DSYS-03). Both
are bundled into web/app/providers.tsx which layout.tsx (Plan 03) imports and wraps around the app.

Purpose: providers.tsx and reduced-motion.tsx must exist BEFORE Plan 03 rewrites layout.tsx,
because layout.tsx imports Providers (and Providers imports ReducedMotionProvider) — a missing
module would fail the build. This plan only needs next-themes (installed by Plan 01); it shares no
files with Plan 01, so it is a clean Wave 2 step. The useReducedMotion() boolean signature is a
LOCKED contract consumed by Phase 16's Hero3D — do not change it.

Output: web/app/providers.tsx, web/lib/reduced-motion.tsx. Addresses DSYS-02 (ThemeProvider) and
DSYS-03 (ReducedMotionProvider + hook + data attribute).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system/12-UI-SPEC.md
@.planning/phases/12-design-system/12-RESEARCH.md
@.planning/phases/12-design-system/12-PATTERNS.md
</context>

<artifacts_produced>
New files / symbols this plan creates (convergence source-grounding allowlist):
- web/app/providers.tsx — exports `Providers` (ThemeProvider + ReducedMotionProvider wrapper)
- web/lib/reduced-motion.tsx — exports `ReducedMotionProvider`, `useReducedMotion`
- localStorage key: `bk-theme`
- DOM attribute: `data-reduced-motion` on `<html>` (set to "true"/"false")
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ReducedMotionProvider + useReducedMotion hook</name>
  <files>web/lib/reduced-motion.tsx</files>
  <read_first>
    - .planning/phases/12-design-system/12-UI-SPEC.md (§ReducedMotionProvider Contract, lines 699–790 — VERBATIM implementation contract; §Phase 16 consumption contract; the data-reduced-motion attribute)
    - .planning/phases/12-design-system/12-PATTERNS.md (§web/lib/reduced-motion.tsx, lines 290–345 — copy exactly; "use client" placement note)
    - web/tsconfig.json (confirms @/* → ./* alias so @/lib/reduced-motion resolves to web/lib/reduced-motion.tsx)
  </read_first>
  <action>
    Create web/lib/reduced-motion.tsx exactly per the UI-SPEC §ReducedMotionProvider Contract and
    the PATTERNS verbatim block. First line is the "use client" directive (no imports before it).
    Define a ReducedMotionContext via createContext with shape { prefersReducedMotion: boolean },
    default false. Export ReducedMotionProvider: useState(false); in a useEffect, read
    window.matchMedia("(prefers-reduced-motion: reduce)"), set state from mq.matches, and write
    document.documentElement.dataset.reducedMotion = mq.matches ? "true" : "false" (this is the
    data-reduced-motion attribute CSS-only consumers read). Register an mq "change" listener that
    updates both state and the dataset, and return its removeEventListener cleanup. Export
    useReducedMotion(): boolean returning useContext(ReducedMotionContext).prefersReducedMotion. The
    hook signature `(): boolean` is the LOCKED Phase 16 contract — do not parameterize it. Do NOT add
    a test runner (Phase 19 owns that).
  </action>
  <verify>
    <automated>cd web; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `web/lib/reduced-motion.tsx` exists; first non-blank line is `"use client";`.
    - `grep -E "export function ReducedMotionProvider" web/lib/reduced-motion.tsx` matches.
    - `grep -E "export function useReducedMotion\(\): boolean" web/lib/reduced-motion.tsx` matches (locked signature).
    - `grep -E "prefers-reduced-motion: reduce" web/lib/reduced-motion.tsx` matches (matchMedia query present).
    - `grep -E "reducedMotion" web/lib/reduced-motion.tsx` matches (data-reduced-motion / dataset write present).
    - `cd web && pnpm build` exits 0 (TypeScript compiles the new module).
  </acceptance_criteria>
  <done>reduced-motion.tsx exports ReducedMotionProvider and useReducedMotion(): boolean, syncs data-reduced-motion on <html>, build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Providers wrapper (ThemeProvider + ReducedMotionProvider)</name>
  <files>web/app/providers.tsx</files>
  <read_first>
    - web/lib/reduced-motion.tsx (created in Task 1 — providers.tsx imports ReducedMotionProvider from it)
    - .planning/phases/12-design-system/12-UI-SPEC.md (§Theme Toggle: No-FOUC Contract, lines 609–695; §Nesting in Providers, lines 779–790)
    - .planning/phases/12-design-system/12-RESEARCH.md (§Pattern 3, lines 253–262; §Pattern 4 Fumadocs deferral, lines 263–293; §providers.tsx code example, lines 503–525; Pitfall 3, lines 372–380)
    - .planning/phases/12-design-system/12-PATTERNS.md (§web/app/providers.tsx, lines 247–287)
  </read_first>
  <action>
    Create web/app/providers.tsx with the "use client" directive first. Import ThemeProvider from
    next-themes and ReducedMotionProvider from @/lib/reduced-motion. Export a Providers({ children })
    component that renders ThemeProvider with attribute="class", defaultTheme="system",
    enableSystem={true}, storageKey="bk-theme" (the localStorage key — exact string), and
    disableTransitionOnChange (prevents transition jank on the .dark↔.light swap). Inside it, wrap
    children with ReducedMotionProvider. Leave the Phase 13 handoff markers as literal comments
    inside ReducedMotionProvider, around {children}: one comment reading that Phase 13 inserts
    `<RootProvider theme={{ enabled: false }}>` (the theme={{enabled:false}} is critical to avoid the
    double-next-themes conflict of Pitfall 3) and a matching closing-comment. Do NOT install or import
    Fumadocs here — Phase 13 owns RootProvider. ThemeProvider must be the OUTERMOST theme context so
    Phase 13's RootProvider can consume it.
  </action>
  <verify>
    <automated>cd web; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `web/app/providers.tsx` exists; first non-blank line is `"use client";`.
    - `grep -E "export function Providers" web/app/providers.tsx` matches.
    - `grep 'storageKey="bk-theme"' web/app/providers.tsx` matches (exact persistence key).
    - `grep -E "attribute=\"class\"" web/app/providers.tsx` matches.
    - `grep -E "disableTransitionOnChange" web/app/providers.tsx` matches.
    - `grep "@/lib/reduced-motion" web/app/providers.tsx` matches (ReducedMotionProvider wired).
    - `grep -E "RootProvider" web/app/providers.tsx` matches inside a comment (Phase 13 insertion marker preserved).
    - `cd web && pnpm build` exits 0; `cd web && pnpm lint` clean.
  </acceptance_criteria>
  <done>providers.tsx exports Providers wrapping ThemeProvider (class strategy, bk-theme, disableTransitionOnChange) around ReducedMotionProvider, with the Phase 13 RootProvider marker; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage ↔ client | next-themes persists the theme choice in `localStorage["bk-theme"]` (client-only) |
| OS media query → DOM | prefers-reduced-motion read via window.matchMedia, written to a data attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | next-themes localStorage value | accept | localStorage is client-side only; no server state, no SSR cookie (static export), no XSS surface in the theme value itself (next-themes writes a class, not eval'd markup). RESEARCH §Security Domain confirms low/no surface. |
| T-12-04 | Tampering | next-themes inline blocking script vs future CSP | accept | The inline no-FOUC script cannot use nonce-based CSP without custom work. Phase 12 is static export to Cloudflare Pages with NO CSP headers configured — explicitly deferred to Phase 17 (SEO & Headers) per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `cd web && pnpm build` exits 0 after both tasks.
- `cd web && pnpm lint` clean.
- reduced-motion.tsx exports ReducedMotionProvider + useReducedMotion(): boolean; writes data-reduced-motion.
- providers.tsx exports Providers; storageKey "bk-theme"; class strategy; disableTransitionOnChange; Phase 13 RootProvider marker present.
</verification>

<success_criteria>
The two providers exist and compile: ThemeProvider (DSYS-02 wiring, persistence via bk-theme, FOUC
prevention scaffolding) and ReducedMotionProvider with the locked useReducedMotion() hook and
data-reduced-motion attribute (DSYS-03). Phase 13 RootProvider insertion point is marked. Layout
wiring + the runtime smoke verification happen in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/12-design-system/12-02-SUMMARY.md` when done.
</output>
