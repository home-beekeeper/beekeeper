---
phase: 12-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/components.json
  - web/lib/utils.ts
  - web/components/ui/button.tsx
  - web/components/ui/badge.tsx
  - web/components/ui/separator.tsx
  - web/components/ui/tooltip.tsx
  - web/package.json
  - web/pnpm-lock.yaml
  - pnpm-lock.yaml
autonomous: false
requirements: [DSYS-01]
user_setup: []

must_haves:
  truths:
    - "shadcn CLI initialized in web/ with new-york style and a Tailwind-v4 (config-less) components.json"
    - "The four base shadcn components (button, badge, separator, tooltip) exist and import cn() from @/lib/utils"
    - "next-themes and the shadcn runtime deps are installed without running untrusted lifecycle scripts"
  artifacts:
    - path: "web/components.json"
      provides: "shadcn config — new-york, zinc base, tailwind.config empty string, no third-party registries"
      contains: '"config": ""'
    - path: "web/lib/utils.ts"
      provides: "cn() classname helper (clsx + tailwind-merge)"
      exports: ["cn"]
    - path: "web/components/ui/button.tsx"
      provides: "Button component (CTA, copy, theme toggle base)"
    - path: "web/components/ui/badge.tsx"
      provides: "Badge component (severity badges, version pills)"
    - path: "web/components/ui/separator.tsx"
      provides: "Separator component (dividers)"
    - path: "web/components/ui/tooltip.tsx"
      provides: "Tooltip component (hover hints)"
  key_links:
    - from: "web/components/ui/button.tsx"
      to: "web/lib/utils.ts"
      via: "import { cn } from \"@/lib/utils\""
      pattern: "from \"@/lib/utils\""
    - from: "web/components.json"
      to: "web/app/globals.css"
      via: "tailwind.css path points at app/globals.css"
      pattern: "globals.css"
---

<objective>
Establish the shadcn/ui foundation inside web/: run the shadcn CLI init (new-york style,
zinc base, Tailwind v4 CSS-first), add the four base components (button, badge, separator,
tooltip), and install next-themes. This produces components.json, lib/utils.ts, and the
components/ui/*.tsx files that Plan 02 (providers) and Plan 03 (tokens/fonts) depend on.

Purpose: shadcn init and add must run BEFORE the hand-authored globals.css token work,
because the CLI OVERWRITES web/app/globals.css with OKLCH zinc defaults (RESEARCH Pitfall 1).
This plan deliberately leaves globals.css in the CLI-generated state; Plan 03 replaces it
with the canonical Beekeeper order. Sequencing is the whole point of isolating this plan.

Output: components.json, lib/utils.ts, four components/ui/*.tsx, next-themes + shadcn deps in
package.json. Addresses DSYS-01 (single design-system source; shadcn registry foundation).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system/12-UI-SPEC.md
@.planning/phases/12-design-system/12-RESEARCH.md
@.planning/phases/12-design-system/12-PATTERNS.md
</context>

<artifacts_produced>
New files / symbols this plan creates (convergence source-grounding allowlist):
- web/components.json — shadcn config (CLI-generated; style new-york, baseColor zinc, tailwind.config "")
- web/lib/utils.ts — exports `cn`
- web/components/ui/button.tsx — exports `Button`, `buttonVariants`
- web/components/ui/badge.tsx — exports `Badge`, `badgeVariants`
- web/components/ui/separator.tsx — exports `Separator`
- web/components/ui/tooltip.tsx — exports `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`
- package.json additions: next-themes, tw-animate-css, clsx, tailwind-merge, class-variance-authority,
  lucide-react, @radix-ui/react-slot, @radix-ui/react-tooltip, @radix-ui/react-separator
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for next-themes + shadcn runtime deps</name>
  <read_first>
    - .planning/phases/12-design-system/12-RESEARCH.md (§Package Legitimacy Audit, lines 104–121; §Standard Stack, lines 49–100)
  </read_first>
  <what-built>
    Nothing automated yet — this is the pre-install legitimacy gate. RESEARCH §Package Legitimacy
    Audit ran with slopcheck DENIED by sandbox policy, so every package is tagged [ASSUMED]. The
    packages about to be installed by Tasks 2–3 are: next-themes@0.4.6, tw-animate-css@1.4.0,
    clsx@2.1.1, tailwind-merge@3.6.0, class-variance-authority@0.7.1, lucide-react@1.17.0,
    @radix-ui/react-slot@1.2.5, @radix-ui/react-tooltip@1.2.9, @radix-ui/react-separator@1.1.9,
    and the shadcn@4.10.0 CLI (run via pnpm dlx). RESEARCH marks all as APPROVED (long-lived,
    high-download shadcn-ecosystem libraries) but the verdict is maintainer-familiarity, not slopcheck.
  </what-built>
  <how-to-verify>
    1. Review the RESEARCH §Package Legitimacy Audit table — all rows are the standard shadcn/ui
       ecosystem (next-themes by pacocoursey ~3M/wk; clsx/tailwind-merge/cva industry standards;
       @radix-ui/* the shadcn foundation; lucide-react the new-york default icon set).
    2. Spot-check any package you do not already trust on npmjs.com/package/<name> (publisher,
       weekly downloads, repo link, no suspicious postinstall).
    3. Confirm none are [SLOP]/[SUS] (RESEARCH reports zero of each).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the installs, or name any package to reject (a rejected package blocks its consumer task).</resume-signal>
  <acceptance_criteria>
    - The maintainer has explicitly approved (or rejected specific) packages before any install runs.
    - This task is NEVER auto-approved (workflow.auto_advance is ignored for package-legitimacy checkpoints).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: shadcn init in web/ (new-york, zinc, CSS-first)</name>
  <files>web/components.json, web/lib/utils.ts, web/package.json, web/pnpm-lock.yaml, pnpm-lock.yaml</files>
  <read_first>
    - web/components.json (confirm it does NOT yet exist — this task creates it)
    - web/biome.json (noUnknownAtRules already off; confirms no Biome retuning needed)
    - .planning/phases/12-design-system/12-UI-SPEC.md (§shadcn/ui Configuration, lines 52–95 — target components.json)
    - .planning/phases/12-design-system/12-RESEARCH.md (§Pattern 2, lines 205–251; Pitfall 1, lines 352–360; Pitfall 5, lines 392–399; §Installation, lines 75–88)
    - .planning/phases/12-design-system/12-PATTERNS.md (§web/components.json, lines 372–415; §web/lib/utils.ts, lines 348–369)
  </read_first>
  <action>
    From INSIDE web/ (cd web first — never the repo root, which has pnpm-workspace.yaml and would
    trigger a monorepo init per RESEARCH Pitfall 5), run shadcn init via pnpm dlx using the shadcn
    package (NOT the renamed-away shadcn-ui), with style new-york, base-color zinc, css-variables yes,
    and WITHOUT the --monorepo flag. The non-interactive flag form per RESEARCH §Installation is
    `pnpm dlx shadcn@latest init --style new-york --base-color zinc --css-variables yes`. This
    generates web/components.json, creates web/lib/utils.ts (cn = twMerge(clsx(inputs))), and installs
    tw-animate-css, clsx, tailwind-merge, class-variance-authority, lucide-react. Verify the generated
    components.json matches UI-SPEC §shadcn/ui Configuration: style "new-york", rsc true, tsx true,
    tailwind.config "" (empty string — Tailwind v4 CSS-first, NO tailwind.config.js), tailwind.css
    "app/globals.css", baseColor "zinc", cssVariables true, aliases mapping @/components, @/lib/utils,
    @/components/ui, @/lib, @/hooks, iconLibrary "lucide", and NO top-level "registries" key. Verify
    lib/utils.ts matches the PATTERNS cn() example exactly. EXPECTED SIDE EFFECT: the CLI rewrites
    web/app/globals.css with OKLCH zinc defaults — leave it; Plan 03 overwrites it with the canonical
    Beekeeper order. Do NOT touch globals.css in this plan. The authoritative lockfile is the
    repo-root pnpm-lock.yaml (workspace mode, per Phase 11 decision); if the CLI writes a member
    web/pnpm-lock.yaml, delete it after install so the root lockfile stays authoritative.
    If pnpm install hits the build-approval gate, honor the Phase 11 DENY posture (sharp:false,
    ignoredBuiltDependencies unrs-resolver) — do NOT approve new lifecycle scripts.
  </action>
  <verify>
    <automated>cd web; pnpm exec node -e "const c=require('./components.json'); if(c.style!=='new-york')throw new Error('style'); if(c.tailwind.config!=='')throw new Error('config not empty'); if(c.tailwind.baseColor!=='zinc')throw new Error('baseColor'); if(c.registries)throw new Error('third-party registries present'); console.log('components.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/components.json` exists; `grep '"config": ""' web/components.json` matches (Tailwind v4 config-less).
    - `web/components.json` has NO `registries` key: `grep -c '"registries"' web/components.json` returns 0.
    - `grep '"style": "new-york"' web/components.json` matches; `grep '"baseColor": "zinc"' web/components.json` matches.
    - `web/lib/utils.ts` exists and `grep -E "export function cn" web/lib/utils.ts` matches.
    - `grep -E "from \"clsx\"|from \"tailwind-merge\"" web/lib/utils.ts` matches both imports.
    - No `web/pnpm-lock.yaml` member lockfile remains (repo-root lockfile is authoritative).
    - `cd web && pnpm build` exits 0 (CLI-generated globals.css still valid at this stage).
  </acceptance_criteria>
  <done>components.json is the new-york, zinc, config-less, registry-clean shadcn config; lib/utils.ts exports cn(); base shadcn deps installed; build still green.</done>
</task>

<task type="auto">
  <name>Task 3: Add base components + install next-themes</name>
  <files>web/components/ui/button.tsx, web/components/ui/badge.tsx, web/components/ui/separator.tsx, web/components/ui/tooltip.tsx, web/package.json, web/pnpm-lock.yaml, pnpm-lock.yaml</files>
  <read_first>
    - web/components.json (created in Task 2 — drives shadcn add output paths)
    - .planning/phases/12-design-system/12-UI-SPEC.md (§shadcn Base Component Set, lines 856–894 — the four installs + rationale + registry safety)
    - .planning/phases/12-design-system/12-RESEARCH.md (§Installation, lines 75–88; §Standard Stack Radix deps, lines 59–68)
    - .planning/phases/12-design-system/12-PATTERNS.md (§button/badge/separator/tooltip, lines 419–482 — token-usage expectations)
  </read_first>
  <action>
    From INSIDE web/, add the four base components from the OFFICIAL shadcn registry only:
    `pnpm dlx shadcn@latest add button`, then `add badge`, `add separator`, `add tooltip`. The CLI
    pulls each component's Radix dependency automatically (@radix-ui/react-slot for button asChild,
    @radix-ui/react-tooltip, @radix-ui/react-separator) — do NOT add any third-party registry. Each
    generated file uses cva for variants, cn() from @/lib/utils, and the React-19 data-slot pattern
    (no forwardRef). Then install next-themes as the only manually-added runtime dep this phase:
    `pnpm add next-themes`. Per UI-SPEC, button variant=default uses bg-primary (→ amber once Plan 03
    tokens land), variant=destructive uses bg-destructive (→ red), focus ring uses ring-ring (→ teal);
    these resolve correctly only after Plan 03 — that is expected. Keep the repo-root pnpm-lock.yaml
    authoritative; delete any web/pnpm-lock.yaml member lockfile the CLI writes. Honor the Phase 11
    build-approval DENY posture on install — approve no new lifecycle scripts.
  </action>
  <verify>
    <automated>cd web; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist: button.tsx, badge.tsx, separator.tsx, tooltip.tsx under web/components/ui/.
    - Each imports cn: `grep -l "@/lib/utils" web/components/ui/button.tsx web/components/ui/badge.tsx web/components/ui/separator.tsx web/components/ui/tooltip.tsx` lists all four.
    - `grep -E '"next-themes"' web/package.json` matches (next-themes installed).
    - No third-party registry added: `grep -c '"registries"' web/components.json` still returns 0.
    - `cd web && pnpm build` exits 0.
    - `cd web && pnpm lint` (Biome) reports no errors on the new files.
  </acceptance_criteria>
  <done>button/badge/separator/tooltip exist, import cn via @/lib/utils, build is green, next-themes installed, registry remains official-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → web/node_modules | shadcn CLI + runtime deps pulled at install time (supply-chain surface) |
| shadcn registry → components/ui | component source pulled by `shadcn add` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | shadcn `add` registry pull | mitigate | Official registry only (ui.shadcn.com); components.json has NO `registries` key (verified in Task 2/3 acceptance). |
| T-12-SC | Tampering | npm/pnpm installs (next-themes + shadcn deps) | mitigate | Task 1 blocking-human package-legitimacy checkpoint (all RESEARCH-tagged [ASSUMED]); Phase 11 build-approval DENY posture honored — no untrusted lifecycle scripts run on install. |
| T-12-02 | Tampering | repo-root vs member pnpm-lock.yaml | accept | Workspace-mode root lockfile is authoritative (Phase 11 decision); member lockfile deleted if written. Low risk — local-only, no remote publish this phase. |
</threat_model>

<verification>
- `cd web && pnpm build` exits 0 after Task 3.
- `cd web && pnpm lint` clean.
- components.json: new-york, zinc, `config: ""`, no `registries`.
- lib/utils.ts exports cn(); all four ui components import it.
- next-themes present in package.json.
</verification>

<success_criteria>
shadcn foundation is in place: components.json (registry-clean, config-less), cn() helper, four
base components, and next-themes installed. Build green. globals.css intentionally left in CLI
state for Plan 03 to overwrite. Partially advances DSYS-01.
</success_criteria>

<output>
Create `.planning/phases/12-design-system/12-01-SUMMARY.md` when done.
</output>
