---
plan: 10-01
phase: 10
wave: 1
depends_on: []
autonomous: true
requirements: [PLCY-01, PLCY-07, CTLG-09, CODE-01, CODE-06, INTG-03, INTG-04, LLMF-01, LLMF-02, LLMF-03, LLMF-04, LLMF-05, LLMF-06]
files_modified:
  - internal/check/handler.go
  - internal/check/handler_test.go
  - internal/policyloader/test.go
  - internal/gateway/gateway.go
  - internal/gateway/proxy.go
  - internal/gateway/gateway_test.go
  - internal/watch/handler.go
  - internal/scan/scanner.go
  - internal/llamafirewall/supervisor.go
  - internal/llamafirewall/latency.go
  - cmd/beekeeper/main.go
---

# Plan 10-01 — Cross-phase integration closure

<objective>
Close the four cross-phase wiring blockers + two warnings from `.planning/v1.0.0-MILESTONE-AUDIT.md`. No new features — connect existing, tested components into the runtime path so shipped requirements work end-to-end. `internal/policy` stays pure; fail-closed preserved; native + Windows builds + full test suite stay green.
</objective>

<tasks>

<task id="10-01-01" type="execute">
<title>INT-BLOCK-2: apply policy-file corroboration_threshold in live `beekeeper check`</title>
<read_first>
- internal/check/handler.go (runCheck — the policy.Evaluate call ~line 225 and the false comment ~line 237; the LoadPolicyDir/ApplyPolicyOverlay block ~244-253)
- internal/policyloader/test.go (thresholdsFromPolicyFile ~line 28 — unexported)
- internal/policyloader/enforce.go (ApplyPolicyOverlay — skips corroboration_threshold by design)
- internal/policy/engine.go (Evaluate signature + CorroborationThresholds)
</read_first>
<action>
Export a policyloader helper (e.g. ThresholdsFromPolicyFiles([]PolicyFile) policy.CorroborationThresholds, generalizing the existing unexported thresholdsFromPolicyFile to merge across all loaded files; later files/rules override earlier per the existing precedence). In runCheck, after LoadPolicyDir, derive thresholds from the loaded policy files and pass them to policy.Evaluate INSTEAD of the unconditional policy.DefaultCorroborationThresholds(); fall back to defaults when no policy file sets a threshold. Update RunPolicyTest to use the same exported helper so dry-run and live stay identical. Delete/correct the false handler.go comment claiming thresholds already flowed through.
</action>
<acceptance_criteria>
- A new handler_test.go case proves a policies-dir file with a corroboration_threshold rule (e.g. block_at:1) changes a live runCheck decision (a single-source match that is warn under defaults becomes block)
- `policy test` and live `check` produce the SAME decision for the same policy file + tool call + catalog (parity test)
- internal/policy unchanged (git diff shows no internal/policy/* change)
- `go test ./internal/check/... ./internal/policyloader/... -count=1` exits 0
</acceptance_criteria>
</task>

<task id="10-01-02" type="execute">
<title>INT-BLOCK-3 + overlay: gateway multi-source corroboration + policy overlay</title>
<read_first>
- internal/gateway/gateway.go (~line 75 NewMultiIndex(bbIdx, nil, nil); Config struct with CacheDir/SocketToken)
- internal/check/handler.go (~lines 194-213 — the OSV + Socket adapter construction to mirror; ~244-253 overlay application)
- internal/gateway/proxy.go (handleToolCall / applyPolicy — where Evaluate is called)
- internal/policyloader/enforce.go + loader.go (LoadPolicyDir, ApplyPolicyOverlay)
</read_first>
<action>
In gateway.go Start, construct catalog.OSVAdapter and catalog.SocketAdapter from Config.CacheDir and Config.SocketToken (mirror handler.go:194-213) and pass them to catalog.NewMultiIndex(bbIdx, osvAdapter, socketAdapter). In the gateway policy path (proxy.go applyPolicy), after Evaluate, load policies via policyloader.LoadPolicyDir and apply policyloader.ApplyPolicyOverlay (mirror handler.go), and pass policy-file-derived thresholds to Evaluate (reuse the 10-01-01 helper). Use the same threshold-derivation so gateway matches check.
</action>
<acceptance_criteria>
- gateway.go no longer passes nil,nil to NewMultiIndex; OSV + Socket adapters constructed from Config
- A gateway_test.go case proves a 2-source corroboration match produces a block decision via the gateway path (was warn-only before)
- A gateway_test.go case proves a package_allowlist/sensitive_path policy file affects a gateway decision
- `go test ./internal/gateway/... -count=1` exits 0; `go build ./...` + `GOOS=windows go build ./...` green
</acceptance_criteria>
</task>

<task id="10-01-03" type="execute">
<title>INT-WARN-1: apply policy overlay in watch and scan decision paths</title>
<read_first>
- internal/watch/handler.go (~line 132 policy.Evaluate direct call)
- internal/scan/scanner.go (~line 239 policy.Evaluate direct call)
- internal/check/handler.go (overlay reference ~244-253)
- internal/policyloader/loader.go + enforce.go
</read_first>
<action>
In watch/handler.go and scan/scanner.go, after the existing policy.Evaluate call, load policies via policyloader.LoadPolicyDir (from the standard ~/.beekeeper/policies dir, resolved via platform) and apply policyloader.ApplyPolicyOverlay to the decision, plus pass policy-file-derived thresholds to Evaluate (reuse the 10-01-01 helper). Mirror the handler.go pattern. Missing policies dir = no-op (LoadPolicyDir returns empty); a malformed file is skipped with a warning, not fatal.
</action>
<acceptance_criteria>
- watch and scan decision paths call policyloader.LoadPolicyDir + ApplyPolicyOverlay (grep confirms)
- A test (watch and/or scan) proves a policy-file rule affects the decision
- `go test ./internal/watch/... ./internal/scan/... -count=1` exits 0
</acceptance_criteria>
</task>

<task id="10-01-04" type="execute">
<title>INT-BLOCK-1 + INT-WARN-2: LlamaFirewall supervisor lifecycle + scan wiring</title>
<read_first>
- internal/llamafirewall/supervisor.go (NewSupervisor, Start, Scan, IsDegraded, sockPath model)
- internal/llamafirewall/client.go (how a client connects to a running sidecar socket)
- internal/check/handler.go (RunAuditRecordWithLLMF — signature: what scanner/client it expects)
- internal/gateway/policy.go (GatewayScanner interface + ScanProxiedResponse) + internal/gateway/proxy.go (forwardWithWarningInjection)
- cmd/beekeeper/main.go (newAuditRecordCmd ~1444 calls RunAuditRecord; newGatewayCmd; newLlamaFirewallCmd enable/disable/status)
- .planning/phases/06-llamafirewall-audit-sinks/06-05-SUMMARY.md (the deferral note)
</read_first>
<action>
Wire the LlamaFirewall sidecar into the runtime, gated on config.LlamaFirewall.Enabled (stays disabled by default). Pin this lifecycle: the long-lived gateway daemon starts/supervises the sidecar when enabled (NewSupervisor + Start) and the gateway proxy passes the supervisor as the GatewayScanner so forwardWithWarningInjection calls ScanProxiedResponse on proxied responses. Add a standalone long-lived path so the sidecar can run for one-shot commands (either a `beekeeper llamafirewall serve`/run subcommand that starts and supervises the sidecar, or document that LLMF scanning for `audit-record` requires the gateway/serve to be running). Route newAuditRecordCmd to check.RunAuditRecordWithLLMF when enabled, connecting via a client to the running sidecar socket; on unreachable sidecar, honor fail_mode (fail-closed default). Ensure no orphaned exports remain (ScanProxiedResponse, RunAuditRecordWithLLMF must have production callers). If the sidecar lifecycle host raises a genuine design choice you cannot resolve from the existing code, return a CHECKPOINT rather than guessing.
</action>
<acceptance_criteria>
- grep shows production (non-test) callsites for llamafirewall.NewSupervisor, gateway ScanProxiedResponse, and check.RunAuditRecordWithLLMF
- With LLMF disabled (default), behavior is unchanged and all existing tests pass
- A test proves the gateway proxy invokes the scanner when a supervisor/scanner is present (injection → redacted/warned payload), and that an unreachable sidecar under fail_mode=closed blocks
- `go build ./...`, `GOOS=windows go build ./...`, `go test ./internal/llamafirewall/... ./internal/gateway/... ./internal/check/... ./cmd/... -count=1` all exit 0
</acceptance_criteria>
</task>

<task id="10-01-05" type="execute">
<title>INT-BLOCK-4: populate GlobalLatencyTracker so diag reports sidecar latency</title>
<read_first>
- internal/llamafirewall/latency.go (GlobalLatencyTracker ~line 114; LatencyTracker.Record/P95)
- internal/llamafirewall/supervisor.go (Scan — records into per-instance s.latency ~line 297)
- internal/check/diag.go (~line 92 reads GlobalLatencyTracker.P95())
</read_first>
<action>
In Supervisor.Scan, after recording into the per-instance tracker, also record the same elapsed-ms sample into llamafirewall.GlobalLatencyTracker so check.CollectDiag reports a real sidecar p95. (Keep the per-instance tracker for supervisor-local use.) Confirm CollectDiag reads the global tracker.
</action>
<acceptance_criteria>
- A latency_test.go (or supervisor test) proves that after Supervisor.Scan records N samples, llamafirewall.GlobalLatencyTracker.P95() is non-zero
- grep shows GlobalLatencyTracker.Record(...) has a production callsite
- `go test ./internal/llamafirewall/... ./internal/check/... -count=1` exits 0
</acceptance_criteria>
</task>

</tasks>

<threat_model>
- T-10-01 (Tampering): gateway now enforces corroboration like the hook handler — closes a privilege-asymmetry where a gateway-routed agent got weaker enforcement (INT-BLOCK-3). No new attack surface; reuses the vetted adapter construction.
- T-10-02 (Spoofing/DoS): policy-file threshold rules now affect live enforcement (INT-BLOCK-2); a malicious local policy that RAISES thresholds to weaken enforcement is bounded by the same validation as dry-run, and policy files are the user's own version-controlled trust decision (documented in THREAT-MODEL.md §9).
- T-10-03 (Availability): LLMF wiring stays fail-closed on sidecar unavailability (LLMF-05); disabled-by-default unchanged. A one-shot command with no reachable sidecar fails per fail_mode, never silently allows.
</threat_model>

<must_haves>
truths:
  - "A corroboration_threshold policy rule changes a live `beekeeper check` decision, identical to `policy test` (live/dry-run parity)"
  - "The MCP gateway produces a 2-source corroboration block (Bumblebee+OSV+Socket), matching the hook handler"
  - "policy overlay (package_allowlist/sensitive_path) is applied in gateway, watch, and scan — not only check"
  - "With LLMF enabled the supervisor is started and the gateway proxy + audit-record invoke the scan path; no orphaned LLMF exports; disabled-by-default behavior unchanged; fail-closed on unreachable sidecar"
  - "beekeeper diag reports a non-zero LlamaFirewall sidecar p95 after real sidecar invocations"
  - "internal/policy remains pure; native + Windows builds + full `go test ./...` stay green"
artifacts:
  - "Updated handler.go/gateway/watch/scan/supervisor wiring with production callers for previously-orphaned exports"
</must_haves>
