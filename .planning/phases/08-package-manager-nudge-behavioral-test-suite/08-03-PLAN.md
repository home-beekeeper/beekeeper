---
phase: 08-package-manager-nudge-behavioral-test-suite
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/nudge/evaluate.go
  - internal/nudge/config.go
  - internal/nudge/reasons.go
  - internal/nudge/rewrite.go
  - internal/nudge/version.go
  - internal/nudge/evaluate_test.go
  - internal/nudge/rewrite_test.go
  - internal/nudge/version_test.go
  - internal/nudge/config_test.go
autonomous: true
requirements: [NUDGE-02, NUDGE-03, NUDGE-04, NUDGE-05, NUDGE-06, NUDGE-08, BTEST-01]
must_haves:
  truths:
    - "nudge.Evaluate is a PURE decision over a caller-resolved PMState (no exec/os/time/sync) — TestNudgeEvaluateImportsArePure passes"
    - "Soft mode (default) returns Advise + proceeds (exit 0); hard mode returns Rewrite; requireHardened with no hardened PM returns Block"
    - "sudo install commands are parsed + logged but NEVER rewritten"
    - "Reason codes are a closed enum; every Evaluate decision carries a valid reason code + Level for mergeDecisions"
    - "Every Decision exposes a nudge_action from the closed §9 enum advise|proceed|rewrite|block (distinct from the allow|warn|block Level) so §9 semantics are preserved through the audit record"
    - "PRD §10 acceptance criteria 1-10 pass as table-driven tests against nudge.Evaluate"
    - "nudge.ConfigFrom maps loose primitive config values (enabled, mode, preferred, checkScanner, version floors, drift enabled/interval) into a nudge.Config WITHOUT importing internal/config (no cycle) — it is the SINGLE config mapper the Wave-4 consumers (Plans 06/08) call (BLOCKER 1 closed)"
    - "nudge.IsMajorDrift is EXPORTED so package gateway (Plan 06 drift.go) can call it across the package boundary at compile time (BLOCKER 2 closed)"
  artifacts:
    - path: "internal/nudge/evaluate.go"
      provides: "Pure nudge decision; Action/PMState/Decision types; Evaluate func; ActionString (closed §9 enum)"
      contains: "func Evaluate"
    - path: "internal/nudge/config.go"
      provides: "Config + DefaultConfig (1440 hardening baseline, Node 24 recommended, floors pnpm 11/bun 1.3/node 22) + ConfigFrom (the single config→nudge.Config mapper, no internal/config import)"
      contains: "func ConfigFrom"
    - path: "internal/nudge/reasons.go"
      provides: "Closed reason-code enum (PRD §9)"
      contains: "Reason"
    - path: "internal/nudge/rewrite.go"
      provides: "Pure npm→pnpm/bun verb mapping"
    - path: "internal/nudge/version.go"
      provides: "Pure major.minor floor compare + drift detection; EXPORTED IsMajorDrift wrapper callable from package gateway"
      contains: "func IsMajorDrift"
    - path: "internal/nudge/evaluate_test.go"
      provides: "Table-driven PRD §10 1-10 + TestNudgeEvaluateImportsArePure"
      contains: "TestNudgeEvaluateImportsArePure"
    - path: "internal/nudge/config_test.go"
      provides: "ConfigFrom mapping unit test (TestConfigFrom)"
      contains: "TestConfigFrom"
  key_links:
    - from: "internal/nudge/evaluate.go"
      to: "internal/pkgparse.ParsedCommand"
      via: "Evaluate(cmd pkgparse.ParsedCommand, state PMState, cfg Config)"
      pattern: "pkgparse\\.ParsedCommand"
    - from: "internal/nudge/config.go ConfigFrom"
      to: "nudge.Config"
      via: "primitive args (no internal/config import) → nudge.Config; called by Plans 06/08"
      pattern: "func ConfigFrom"
    - from: "internal/nudge/version.go IsMajorDrift"
      to: "internal/gateway/drift.go (Plan 06)"
      via: "exported wrapper over the private isMajorDrift, callable across the package boundary"
      pattern: "func IsMajorDrift"
---

<objective>
Build the PURE heart of the nudge feature in a new `internal/nudge/` package: the decision
function `Evaluate(cmd pkgparse.ParsedCommand, state PMState, cfg Config) Decision`, the closed
reason-code enum, the config + defaults (with Flag 5 corrections baked in), the pure command
rewriter, and the pure version/floor compare. This mirrors the locked `policy.EvaluateReleaseAge`
pattern exactly: a pure decision over a caller-resolved input struct. ALL detection I/O is deferred
to Plan 04's `detect.go` adapter — this plan touches NO I/O. The table-driven test suite here
satisfies PRD §10 acceptance criteria 1-10 (BTEST-01).

This plan also defines the closed §9 `nudge_action` enum (`advise|proceed|rewrite|block`) via an
`ActionString(Action) string` mapper, so the audit record (Plan 05 field, Plan 06 wiring) can carry
the PRD §9 `decision` semantics WITHOUT colliding with the repo's existing `allow|warn|block`
audit `Decision` field. The two are deliberately separate: `Level` drives mergeDecisions/exit code;
`nudge_action` preserves PRD §9 wording for forensic conformance (resolves BLOCKER 2 / §10-14 root cause).

This plan ALSO produces the two exports the Wave-4 consumers (Plans 06/08) compile against — they
were previously consumed but produced by no plan (re-verification BLOCKER 1/BLOCKER 2):
1. `nudge.ConfigFrom(...)` — the SINGLE config→`nudge.Config` mapper. `config.NudgeConfig` (Plan 05)
   and `nudge.Config` are deliberately separate structs (config must not import nudge, nudge must not
   import config — import-cycle avoidance), so a mapper is mandatory. `ConfigFrom` takes the PRIMITIVE
   field values and returns a `nudge.Config`, so check/gateway/shim/CLI all call ONE mapper (no Flag-4
   drift hazard). Lives in `internal/nudge/config.go` and imports NO internal/config (BLOCKER 1).
2. `nudge.IsMajorDrift(latest, floor string) bool` — an EXPORTED wrapper over the package-private
   `isMajorDrift`, so package `gateway` (Plan 06 `drift.go`) can call it across the package boundary.
   The private lowercase form is uncallable from `gateway` and re-blocks §10-15 at compile time
   (BLOCKER 2).

Purpose: This is the CLAUDE.md purity discipline applied to nudge — `internal/policy` is the pure
library; nudge gets its own pure decision in its own package (own Action/PMState/reason enum) so the
locked library stays lean, enforced by `TestNudgeEvaluateImportsArePure`.
Output: `internal/nudge/{evaluate,config,reasons,rewrite,version}.go` + their tests — pure, table-driven —
plus `ConfigFrom` (config.go) and exported `IsMajorDrift` (version.go) so the Wave-4 consumers compile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-package-manager-nudge-behavioral-test-suite/08-RESEARCH.md
@.planning/phases/08-package-manager-nudge-behavioral-test-suite/08-PATTERNS.md
@.planning/specs/NUDGE-PRD.md

<interfaces>
<!-- From Plan 01: -->
internal/pkgparse:
  type ParsedCommand struct { Raw, Manager, Ecosystem, Verb, Package, Version string; IsInstall, IsExec, Sudo, Unpinned bool }

<!-- From Plan 05 (the SOURCE struct ConfigFrom maps FROM — but ConfigFrom takes PRIMITIVES, NOT this
     type, so internal/nudge does NOT import internal/config; the caller in Plan 06/08 destructures
     cfg.Nudge into the primitive args): -->
internal/config:
  type NudgeConfig struct { Enabled bool; Mode string; RequireHardened bool; Preferred string; CheckSocketScanner bool; MajorDriftCheck struct{Enabled bool; Interval string}; VersionFloors struct{Pnpm, Bun, Node string} }

<!-- The locked analog to mirror (internal/policy/release_age.go): -->
  func EvaluateReleaseAge(input ReleaseAgeInput, cfg ReleaseAgeConfig) Decision  // PURE, imports only fmt/strings
  func DefaultReleaseAgeConfig() ReleaseAgeConfig { return {DefaultMinutes:1440, ...} }  // note 1440 baseline
  // Decision struct shape: Allow bool; Level string ("allow"/"warn"/"block"); Reason string; RuleIDs []string

<!-- The purity test to copy VERBATIM: internal/policy/release_age_test.go:141-174 (TestReleaseAgeImportsArePure) -->

<!-- The repo's audit Decision field is "allow|warn|block" (internal/audit/types.go line 29 — VERIFIED).
     PRD §9 demands a "decision" enum of advise|proceed|rewrite|block. These are DIFFERENT vocabularies.
     Resolution (BLOCKER 2): nudge keeps Level=allow|warn|block for mergeDecisions/exit, and exposes a
     SEPARATE nudge_action (advise|proceed|rewrite|block) via ActionString. Plan 05 adds a NudgeAction
     audit field; the existing Decision field is left untouched. -->

<!-- Nudge public API (PRD §3.2, adjusted for purity per editor's note): -->
  type Action int  // Proceed | Advise | Rewrite | Block
  func ActionString(a Action) string  // "proceed"|"advise"|"rewrite"|"block" — the closed §9 enum
  type PMState struct { NpmInstalled bool; NpmVersion string; PnpmInstalled bool; PnpmVersion string; PnpmHardened bool; BunInstalled bool; BunVersion string; BunScannerOK bool; NodeVersion string }
  type Decision struct { Action Action; Reason string; Original string; Rewritten string; Detected PMState; Level string; AuditFields map[string]any }
  func Evaluate(cmd pkgparse.ParsedCommand, state PMState, cfg Config) Decision   // PURE
  func ConfigFrom(enabled bool, mode, preferred string, checkScanner bool, floorPnpm, floorBun, floorNode string, driftEnabled bool, driftInterval string) Config  // single config mapper (no internal/config import)
  func IsMajorDrift(latest, floor string) bool  // EXPORTED wrapper over isMajorDrift, callable from package gateway
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reasons.go (closed enum) + config.go (defaults, Flag 5) + version.go (floor compare + EXPORTED IsMajorDrift)</name>
  <read_first>
    - internal/policy/release_age.go (DefaultReleaseAgeConfig lines 36-45 — 1440 default; threshold-compare shape)
    - internal/policyloader/validate.go (lines 14-28 — legalRuleTypes/legalActions closed-enum map pattern)
    - .planning/specs/NUDGE-PRD.md (§5 config block + §5.1 defaults; §7 version matrix + §7.1 drift; §9 reason_code closed enum)
    - .planning/phases/08-package-manager-nudge-behavioral-test-suite/08-RESEARCH.md (Flag 5 — 1440 baseline + Node 24 recommended / floor stays 22)
  </read_first>
  <behavior>
    - DefaultConfig() returns Enabled:true, Mode:"soft", RequireHardened:false, Preferred:"pnpm", CheckSocketScanner:true, MajorDriftCheck{Enabled:true, Interval 168h-equivalent}, VersionFloors{Pnpm:"11.0.0", Bun:"1.3.0", Node:"22.0.0"}
    - meetsFloor("11.5.1","11.0.0") → true; meetsFloor("10.9.0","11.0.0") → false; meetsFloor("1.3.14","1.3.0") → true; meetsFloor("21.0.0","22.0.0") → false (major.minor compare)
    - isMajorDrift("12.0.0","11.0.0") → true (§10-15); isMajorDrift("11.5.1","11.0.0") → false
    - IsMajorDrift (exported) returns EXACTLY what isMajorDrift returns for the same args (it is a thin wrapper/alias) — IsMajorDrift("12.0.0","11.0.0") → true, IsMajorDrift("11.5.1","11.0.0") → false; it is callable from another package
    - reason codes are a closed set: pnpm-available-soft, pnpm-hard-rewrite, bun-available-no-scanner, bun-available-soft, no-hardened-pm, node-incompatible-with-pnpm-11, sudo-passthrough, no-arg-install-soft, not-applicable — IsValidReason(code) true for these, false otherwise
    - hardening-weakness baseline constant = 1440 (NOT 60) for minimumReleaseAge comparison (Flag 5)
  </behavior>
  <action>
    Create `internal/nudge/reasons.go`: a closed reason-code enum (typed string consts) covering every reason_code in PRD §9 plus the criteria-specific ones — `ReasonPnpmAvailableSoft`="pnpm-available-soft", `ReasonPnpmHardRewrite`="pnpm-hard-rewrite", `ReasonBunAvailableNoScanner`="bun-available-no-scanner", `ReasonBunAvailableSoft`="bun-available-soft", `ReasonNoHardenedPM`="no-hardened-pm", `ReasonNodeIncompatiblePnpm11`="node-incompatible-with-pnpm-11", `ReasonSudoPassthrough`="sudo-passthrough", `ReasonNoArgInstallSoft`="no-arg-install-soft" (§10-8 softer code), `ReasonNotApplicable`="not-applicable". Add a `map[string]bool` set + `IsValidReason(string) bool` (mirror legalRuleTypes from policyloader/validate.go). Create `internal/nudge/config.go`: `Config` struct mirroring PRD §5 (Enabled, Mode, RequireHardened, Preferred, CheckSocketScanner, MajorDriftCheck{Enabled, Interval}, VersionFloors{Pnpm, Bun, Node}) and `DefaultConfig()` returning PRD §5.1 defaults. Add a documented package const `minimumReleaseAgeWeaknessBaseline = 1440` and a doc comment that this is the Flag 5 correction (NOT 60) used only for hardening-weakness detection — Beekeeper does NOT itself set minimumReleaseAge (Out-of-Scope). Create `internal/nudge/version.go` (PURE, imports only `strings`/`strconv`-equivalent — NO time/regex): `meetsFloor(have, floor string) bool` doing a major.minor int compare (per research A3, floors are major.minor only); `isMajorDrift(latest, floor string) bool` true when latest major > floor major (§10-15); AND an EXPORTED `func IsMajorDrift(latest, floor string) bool` that is a thin wrapper returning `isMajorDrift(latest, floor)` — exported SO package `gateway` (Plan 06 `drift.go`) can call it across the package boundary (BLOCKER 2; the lowercase form alone is uncallable from another package and re-blocks §10-15 at compile time). Doc-comment IsMajorDrift as the exported drift predicate for cross-package callers. Do NOT add a semver dependency.
  </action>
  <verify>
    <automated>go build ./internal/nudge/... ; go test ./internal/nudge/... -run "TestVersion|TestReasons|TestConfig"</automated>
  </verify>
  <done>reasons.go, config.go, version.go compile; DefaultConfig returns PRD §5.1 defaults with floors 11.0.0/1.3.0/22.0.0; the 1440 weakness baseline const is present and documented; meetsFloor/isMajorDrift behave per <behavior>; the EXPORTED IsMajorDrift wrapper is present and returns identically to isMajorDrift (callable from another package).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rewrite.go (pure npm→pnpm/bun verb mapping) + rewrite_test.go</name>
  <read_first>
    - internal/pkgparse/pkgparse.go (ParsedCommand fields — Manager/Verb/Package/Version drive the rewrite)
    - .planning/specs/NUDGE-PRD.md (§6.4 verb mapping; §10 criteria 2 [pnpm add foo], 9 [npx→dlx], 10 [sudo never rewrite])
    - .planning/phases/08-package-manager-nudge-behavioral-test-suite/08-PATTERNS.md ("internal/nudge/rewrite.go" analog — pure string transform)
  </read_first>
  <behavior>
    - rewriteToPnpm(parse of "npm install foo") → "pnpm add foo"
    - rewriteToPnpm(parse of "npm install foo@5.4.0") → "pnpm add foo@5.4.0"
    - rewriteToPnpm(parse of "npm install") (no args) → "pnpm install" (no-arg form, NUDGE-03)
    - rewriteToPnpm(parse of "npx create-app") → "pnpm dlx create-app" (§10-9 / NUDGE-03)
    - rewriteToBun(parse of "npm install foo") → "bun add foo"; rewriteToBun(npx form) → "bun x create-app"
    - A parse with Sudo=true is NEVER rewritten (the caller in Evaluate guards this; rewrite funcs may assume Sudo already filtered, but a defensive test asserts Evaluate path)
  </behavior>
  <action>
    Create `internal/nudge/rewrite.go` (PURE, imports only `strings`): `rewriteToPnpm(cmd pkgparse.ParsedCommand) string` and `rewriteToBun(cmd pkgparse.ParsedCommand) string`. Verb mapping: install/i/add → pnpm "add" (bun "add"); no-arg install → pnpm "install"/bun "install"; npx / IsExec → pnpm "dlx" / bun "x" (NUDGE-03, §10-9). Append `Package` and, when `Version != ""`, `@Version` exactly as parsed. Operate ONLY on the parsed package/version tokens — never re-emit shell metacharacters or a `sudo` prefix (threat T-08-08). Create `internal/nudge/rewrite_test.go`: table-driven over the <behavior> cases for both pnpm and bun targets.
  </action>
  <verify>
    <automated>go test ./internal/nudge/... -run TestRewrite</automated>
  </verify>
  <done>rewrite.go compiles pure; rewriteToPnpm/rewriteToBun produce correct verb mappings incl. no-arg and npx→dlx/x forms; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: evaluate.go (PURE decision + ActionString §9 enum) + evaluate_test.go (PRD §10 1-10) + purity test</name>
  <read_first>
    - internal/policy/release_age.go (lines 61-109 — THE pure-decision-over-input pattern to mirror exactly)
    - internal/policy/release_age_test.go (lines 141-174 — TestReleaseAgeImportsArePure to copy verbatim → TestNudgeEvaluateImportsArePure)
    - internal/nudge/reasons.go, config.go, version.go, rewrite.go (the types/helpers from Tasks 1-2)
    - internal/audit/types.go (line 29 — confirm the existing Decision field is "allow|warn|block"; nudge_action is the SEPARATE §9 vocabulary)
    - .planning/specs/NUDGE-PRD.md (§4 decision flow; §9 audit record schema — the decision enum advise|proceed|rewrite|block; §10 acceptance criteria 1-10)
    - .planning/phases/08-package-manager-nudge-behavioral-test-suite/08-RESEARCH.md (Code Examples — pure nudge decision skeleton; Pattern 2 — Level mapping A1)
  </read_first>
  <behavior>
    - §10-1: pnpm>=11 installed, mode soft, "npm install foo" → Action Advise, Reason pnpm-available-soft, Level "warn", Allow true (exit 0); ActionString(Advise)=="advise"
    - §10-2: same setup, mode hard → Action Rewrite, Rewritten "pnpm add foo", Level "warn", Allow true; ActionString(Rewrite)=="rewrite"
    - §10-3: no hardened PM, requireHardened false → Action Proceed, Reason no-hardened-pm, Level "allow"; ActionString(Proceed)=="proceed"
    - §10-4: no hardened PM, requireHardened true → Action Block, Reason no-hardened-pm, Level "block", Allow false; ActionString(Block)=="block"
    - §10-5: bun>=1.3 installed, scanner absent (BunScannerOK false) → Action Advise, Reason bun-available-no-scanner
    - §10-6: pnpm 11 installed but NodeVersion<22 → Action Advise, Reason node-incompatible-with-pnpm-11
    - §10-7: non-install (cmd.IsInstall false) → Action Proceed, Reason not-applicable (no detection, no nudge)
    - §10-8: no-arg "npm install" → Action Advise with softer Reason no-arg-install-soft
    - §10-9: npx form parsed as install+execute → still nudged (Advise/Rewrite per mode)
    - §10-10: cmd.Sudo true → Action Advise (logged), Reason sudo-passthrough, NEVER Rewrite (even in hard mode)
    - cfg.Enabled false → Action Proceed, Reason not-applicable (NUDGE-08 layered disable)
    - Every returned Decision has a valid reason code (IsValidReason) and a Level for mergeDecisions: Advise→"warn", Rewrite→"warn", Block→"block", Proceed→"allow" (A1)
    - AuditFields["nudge_action"] == ActionString(d.Action) (closed §9 enum) for every decision; AuditFields["decision"] is NOT set by Evaluate (the audit Decision/Level mapping happens in the Plan 06 adapter) — Evaluate owns nudge_action + reason_code, not the allow|warn|block decision string
    - ActionString returns exactly one of "proceed"|"advise"|"rewrite"|"block" for the four Action consts and "proceed" (safe default) for any out-of-range value
  </behavior>
  <action>
    Create `internal/nudge/evaluate.go` (PURE — imports only `fmt`/`strings` + the pure pkgparse/nudge types; NO os/exec/time/sync/context/io/net): define `type Action int` const (Proceed, Advise, Rewrite, Block), `func ActionString(a Action) string` returning the closed §9 enum ("proceed"/"advise"/"rewrite"/"block"; default "proceed" — never panic on an out-of-range int), `type PMState struct{...}` (NpmInstalled, NpmVersion, PnpmInstalled, PnpmVersion, PnpmHardened, BunInstalled, BunVersion, BunScannerOK, NodeVersion), and `type Decision struct{Action Action; Reason string; Original string; Rewritten string; Detected PMState; Level string; AuditFields map[string]any}`. Implement `Evaluate(cmd pkgparse.ParsedCommand, state PMState, cfg Config) Decision` following PRD §4 decision flow and the release_age.go skeleton: (1) if !cfg.Enabled || !cmd.IsInstall → Proceed/not-applicable; (2) if cmd.Sudo → Advise/sudo-passthrough, NEVER Rewrite (§10-10); (3) if pnpm installed && hardened: if NodeVersion below floor via meetsFloor(state.NodeVersion, cfg.VersionFloors.Node) false → Advise/node-incompatible-with-pnpm-11 (§10-6); else if cfg.Mode=="hard" → Rewrite with rewriteToPnpm(cmd) (§10-2); else Advise/pnpm-available-soft (§10-1) with no-arg-install-soft reason when cmd.Package=="" (§10-8); (4) else if bun installed && meetsFloor(bun): if !state.BunScannerOK → Advise/bun-available-no-scanner (§10-5) else hard/soft branch like pnpm using rewriteToBun; (5) else if cfg.RequireHardened → Block/no-hardened-pm (§10-4) else Proceed/no-hardened-pm (§10-3). Set `Level` per A1 mapping (Advise/Rewrite→"warn", Block→"block", Proceed→"allow"), set `Original`=cmd.Raw, and populate `AuditFields` with original_command, reason_code, rewritten_command (when present), nudge_action (=ActionString(d.Action), the closed §9 enum), and a flattened pm_state. Do NOT set AuditFields["decision"] here — the allow|warn|block audit Decision field is mapped from Level by the Plan 06 adapter; Evaluate owns nudge_action so §9 semantics are not lost. honor `cfg.Preferred` when BOTH pnpm and bun are hardened (default pnpm). Create `internal/nudge/evaluate_test.go`: table-driven over §10 criteria 1-10 (each row: PMState + Config + parsed command → want Action/Reason/Level/Allow AND want ActionString(Action) from the closed §9 enum AND AuditFields["nudge_action"] set correctly); ADD `TestNudgeEvaluateImportsArePure` (copy release_age_test.go verbatim, srcPath "evaluate.go", forbidden {os, net, net/http, io, sync, time, context, os/exec}).
  </action>
  <verify>
    <automated>go test ./internal/nudge/...</automated>
  </verify>
  <done>`go test ./internal/nudge/...` exits 0; all §10 criteria 1-10 pass as table rows; ActionString maps the four Actions to the closed §9 enum advise|proceed|rewrite|block; AuditFields carries nudge_action + reason_code (not the allow|warn|block decision); TestNudgeEvaluateImportsArePure passes (evaluate.go imports no I/O package); sudo never rewrites; every Decision carries a valid reason code + Level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: config.go ConfigFrom — the single config→nudge.Config mapper (BLOCKER 1) + config_test.go</name>
  <read_first>
    - internal/nudge/config.go (Config + DefaultConfig from Task 1 — ConfigFrom returns this type)
    - internal/config/config.go (Plan 05 NudgeConfig fields — these are the PRIMITIVE values the CALLER destructures and passes to ConfigFrom; internal/nudge must NOT import internal/config)
    - internal/nudge/version.go (meetsFloor/major.minor parse shape — ConfigFrom may pass floor strings through verbatim)
    - .planning/specs/NUDGE-PRD.md (§5 config block — the fields ConfigFrom maps)
    - .planning/phases/08-package-manager-nudge-behavioral-test-suite/08-PATTERNS.md ("internal/nudge/config.go" analog — config defaults; "Closed-enum pattern" — mapper stays pure)
  </read_first>
  <behavior>
    - ConfigFrom(true, "soft", "pnpm", true, "11.0.0", "1.3.0", "22.0.0", true, "168h") → a nudge.Config with Enabled true, Mode "soft", Preferred "pnpm", CheckSocketScanner true, VersionFloors{Pnpm:"11.0.0", Bun:"1.3.0", Node:"22.0.0"}, MajorDriftCheck{Enabled:true, Interval:"168h"}
    - ConfigFrom(false, ...) → returned Config.Enabled == false (a layered disable maps through faithfully — NUDGE-08)
    - ConfigFrom with EMPTY floor strings ("","","") → the returned Config falls back to DefaultConfig() floors (11.0.0/1.3.0/22.0.0) so an under-specified config never yields an empty floor that breaks meetsFloor (documented fallback)
    - ConfigFrom with an empty Mode → falls back to DefaultConfig().Mode ("soft"); empty Preferred → "pnpm"; empty Interval → "168h"
    - ConfigFrom imports NO internal/config — `go build ./internal/nudge/...` has no import-cycle error (TestNudgeEvaluateImportsArePure unaffected; config.go imports only stdlib)
  </behavior>
  <action>
    Add `func ConfigFrom(enabled bool, mode, preferred string, checkScanner bool, floorPnpm, floorBun, floorNode string, driftEnabled bool, driftInterval string) Config` to `internal/nudge/config.go`. This is the SINGLE config mapper consumed by Plans 06 and 08 (check/gateway/shim/CLI) — it takes the PRIMITIVE field values that the caller destructures out of `config.NudgeConfig`, so `internal/nudge` does NOT import `internal/config` (no cycle — config's consumers import config; nudge stays import-free of config; this is the Flag 4 single-mapper rule). Build a `nudge.Config` from the args, starting from `DefaultConfig()` and overriding only the supplied primitives; apply documented fallbacks for empties: empty `mode`→DefaultConfig().Mode, empty `preferred`→DefaultConfig().Preferred, empty floor string→the corresponding DefaultConfig() floor, empty `driftInterval`→DefaultConfig().MajorDriftCheck.Interval (so an under-specified loose config never produces an empty floor that would break meetsFloor). Pass `enabled`, `checkScanner`, `driftEnabled` through verbatim. Keep ConfigFrom PURE (no I/O; imports stay stdlib-only so config.go does not break the package's purity expectations). Create `internal/nudge/config_test.go` with `TestConfigFrom`: table-driven over the <behavior> rows asserting the full-field mapping, the layered-disable passthrough, and the empty-floor/empty-mode/empty-interval fallbacks to DefaultConfig values.
  </action>
  <verify>
    <automated>go build ./internal/nudge/... ; go test ./internal/nudge/... -run TestConfigFrom</automated>
  </verify>
  <done>nudge.ConfigFrom exists in config.go and maps the primitive config values into a nudge.Config without importing internal/config (no cycle); layered disable (enabled false) maps through; empty floor/mode/preferred/interval fall back to DefaultConfig values; TestConfigFrom green. Plans 06/08 can now reference nudge.ConfigFrom as already-built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed command → nudge.Evaluate | Attacker-influenced parsed tokens reach the decision; the rewriter may emit a new command string |
| loose config primitives → nudge.ConfigFrom | Caller-supplied config values (mode/preferred/floors) are mapped into the decision config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Elevation/Tampering | evaluate.go (pure) | mitigate | Evaluate imports NO I/O (TestNudgeEvaluateImportsArePure) — it cannot exec, read files, or read the clock. All detection is the caller's resolved PMState. |
| T-08-07 | Tampering (sudo escalation) | sudo branch | mitigate | cmd.Sudo → Advise only, NEVER Rewrite (§10-10) — Beekeeper never rewrites a privileged command into a privileged pnpm/bun invocation. Explicit table test. |
| T-08-08 | Tampering (metacharacter injection in rewrite) | rewrite.go | mitigate | Rewrite operates only on parsed Package/Version tokens, never re-emits shell metacharacters or sudo; the rewritten string is advisory/audit only — Beekeeper does not execute it. |
| T-08-09 | DoS (fail-bypass) | detection-fail contract | accept | Detection failure is fail-OPEN by design (PRD §10-12): a slow/absent PM yields PMState "not installed" → Proceed, never blocks the agent. Documented as the soft-nudge contract, distinct from catalog/path fail-closed. (Detection itself is Plan 04.) |
| T-08-09b | Repudiation (audit vocabulary mismatch) | nudge_action vs Decision | mitigate | The closed §9 enum (advise|proceed|rewrite|block) is preserved verbatim in AuditFields["nudge_action"] via ActionString — a soft advisory records nudge_action "advise" even though its Level/Decision is "warn", so the forensic record matches PRD §9 (BLOCKER 2 root cause). |
| T-08-09c | Tampering (config under-spec weakens floors) | ConfigFrom fallbacks | mitigate | ConfigFrom never returns an empty version floor: an empty floor string falls back to the secure DefaultConfig() floor (11.0.0/1.3.0/22.0.0), so an under-specified loose config cannot silently disable the floor compare (meetsFloor) — the mapper fails toward the documented defaults, not toward "no floor". |
</threat_model>

<verification>
- `go test ./internal/nudge/...` green — §10 criteria 1-10 covered (BTEST-01).
- TestNudgeEvaluateImportsArePure proves evaluate.go is pure.
- ActionString maps the four Actions to the closed §9 enum; AuditFields["nudge_action"] is set on every decision.
- DefaultConfig reflects Flag 5: floors 11.0.0/1.3.0/22.0.0; hardening-weakness baseline 1440; Node 24 recommended in guidance text/comment.
- nudge.ConfigFrom maps loose config primitives into a nudge.Config without importing internal/config (BLOCKER 1); TestConfigFrom green.
- nudge.IsMajorDrift is exported and callable from another package (BLOCKER 2 — the Plan 06 gateway drift check links against it).
</verification>

<success_criteria>
- nudge.Evaluate is pure and decides Advise/Rewrite/Proceed/Block per PRD §4 (NUDGE-02/03/04).
- Reason codes are a closed enum (NUDGE-06 foundation); AuditFields carry the §9 schema fields incl. the §9 nudge_action enum.
- sudo passthrough never rewrites (§10-10); no-arg install gets a softer reason (§10-8).
- Level mapping keeps soft mode at exit 0 (Advise/Rewrite→"warn") and only Block is restrictive (A1).
- nudge.ConfigFrom (the single config mapper) and the exported nudge.IsMajorDrift exist so the Wave-4 consumers (Plans 06/08) compile against them (BLOCKER 1 + BLOCKER 2 closed); NUDGE-08 layered disable maps faithfully through ConfigFrom.
</success_criteria>

<output>
After completion, create `.planning/phases/08-package-manager-nudge-behavioral-test-suite/08-03-SUMMARY.md`
</output>
</content>
</invoke>
