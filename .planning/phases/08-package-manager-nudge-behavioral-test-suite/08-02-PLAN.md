---
phase: 08-package-manager-nudge-behavioral-test-suite
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/policy/engine.go
  - internal/policy/engine_test.go
  - internal/policyloader/enforce.go
autonomous: true
requirements: [NUDGE-01]
must_haves:
  truths:
    - "internal/policy/engine.go uses pkgparse.Parse for install-command extraction; the local installPrefixes/extractFromCommand copy is gone"
    - "internal/policyloader/enforce.go uses pkgparse.Parse; the duplicate installPrefixesOverlay copy is gone"
    - "Existing engine_test.go and enforce_test.go pass unchanged (byte-identical behavior regression net)"
    - "A pnpm/bun install of a known-malicious package now resolves ecosystem \"npm\" and is catalog-matched (F3 closed)"
  artifacts:
    - path: "internal/policy/engine.go"
      provides: "Catalog engine routing install extraction through pkgparse"
      contains: "pkgparse."
    - path: "internal/policyloader/enforce.go"
      provides: "Overlay routing install extraction through pkgparse"
      contains: "pkgparse."
    - path: "internal/policy/engine_test.go"
      provides: "New pnpm-add catalog-match regression case (SC1/F3) added to the existing engine regression net"
      contains: "pnpm add"
  key_links:
    - from: "internal/policy/engine.go"
      to: "internal/pkgparse"
      via: "extract() delegates command-shape parsing to pkgparse.Parse"
      pattern: "pkgparse\\.Parse"
    - from: "internal/policyloader/enforce.go"
      to: "internal/pkgparse"
      via: "extractEcoPackageFromCommand delegates to pkgparse.Parse"
      pattern: "pkgparse\\.Parse"
---

<objective>
Refactor the TWO existing duplicate install-command parser copies to consume the new
`internal/pkgparse.Parse` (Plan 01), and KEEP each package's existing test suite green as the
byte-identical regression net (research A5). Because `pkgparse` now maps pnpm/bun/yarn install
verbs to ecosystem "npm", routing both consumers through it automatically gives F3 catalog
matching to BOTH the engine path (`policy.Evaluate` → `LookupAll`) and the overlay path — closing
SC1 with no third copy.

Purpose: This is the Flag 4 EXTRACT decision made concrete. The two copies (engine.go's
`installPrefixes` and enforce.go's `installPrefixesOverlay`, the latter literally commented
"duplicated here") must stay in lockstep for the F3 fix; collapsing them to one source eliminates
the drift hazard.
Output: `internal/policy/engine.go` + `internal/policyloader/enforce.go` route through pkgparse;
their existing tests pass unchanged; a new engine_test case proves pnpm/bun catalog matching.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-package-manager-nudge-behavioral-test-suite/08-RESEARCH.md
@.planning/phases/08-package-manager-nudge-behavioral-test-suite/08-PATTERNS.md

<interfaces>
<!-- From Plan 01 (already built): -->
internal/pkgparse:
  type ParsedCommand struct { Raw, Manager, Ecosystem, Verb, Package, Version string; IsInstall, IsExec, Sudo, Unpinned bool }
  func Parse(cmd string) (ParsedCommand, bool)  // ok=false when not install/exec; pnpm/bun/yarn → Ecosystem "npm"

<!-- The two existing copies to remove (from the live codebase): -->
internal/policy/engine.go:
  - extract(input map[string]any) (ecosystem, pkg, version string, ok bool) — lines 227-248. Direct shape (ecosystem/package keys) and editor-extension path STAY; only the command-shape fall-through `extractFromCommand(cmd)` (line 244) routes to pkgparse.
  - installPrefixes (34-47), extractFromCommand (327-348), firstPackageToken (352-360), splitVersion (364-370) — REMOVE the install-prefix table + extractFromCommand; firstPackageToken/splitVersion/normalize remain ONLY if still used by the retained editor-extension path (extractExtensionInstall/extractAllExtensionInstalls use firstPackageToken + splitVersion + normalize — KEEP those helpers).

internal/policyloader/enforce.go:
  - extractEcoPackageFromCommand(cmd) (eco, pkg string) — lines 256-275. Direct-shape branch (222-227) STAYS; the command branch (line 231) routes to pkgparse.
  - installPrefixesOverlay (240-253), firstNonFlagToken, stripVersionSuffix — REMOVE (now provided by pkgparse).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route internal/policy/engine.go command extraction through pkgparse</name>
  <read_first>
    - internal/policy/engine.go (full file — note: the editor-extension path at lines 90-116, 265-322 MUST be preserved; only the generic install-prefix path changes)
    - internal/pkgparse/pkgparse.go (the Parse contract from Plan 01)
    - internal/policy/engine_test.go (the regression net — must still pass)
    - .planning/phases/08-package-manager-nudge-behavioral-test-suite/08-PATTERNS.md ("internal/policy/engine.go ... (EDIT) — Flag 4 consumers")
  </read_first>
  <action>
    In `internal/policy/engine.go`: in `extract`, replace the command-shape fall-through `return extractFromCommand(cmd)` (line 244) with a call to `pkgparse.Parse(cmd)` — when ok and `parsed.IsInstall`, return `(parsed.Ecosystem, parsed.Package, parsed.Version, true)`; otherwise return `("","","",false)`. Import `github.com/bantuson/beekeeper/internal/pkgparse`. DELETE the local `installPrefixes` var (34-47) and the `extractFromCommand` func (327-348). KEEP `firstPackageToken`, `splitVersion`, `normalize` IF they are still referenced by the retained editor-extension helpers (`extractExtensionInstall`, `extractAllExtensionInstalls`) — they are, so leave them. Do NOT touch the editor-extension code path (it has its own pattern table `editorInstallPatterns` and is out of pkgparse scope). The Package returned to the engine is already normalized (lowercase/trim) by pkgparse, matching the prior `normalize` behavior so catalog index keys still match. Because pkgparse maps pnpm/bun/yarn → "npm", `LookupAll("npm", pkg)` now matches those installs (F3/SC1) with no further change here.
  </action>
  <verify>
    <automated>go test ./internal/policy/...</automated>
  </verify>
  <done>engine.go compiles, imports pkgparse, no local installPrefixes/extractFromCommand; `go test ./internal/policy/...` passes unchanged (regression net green).</done>
</task>

<task type="auto">
  <name>Task 2: Route enforce.go through pkgparse + add pnpm/bun catalog-match regression case</name>
  <read_first>
    - internal/policyloader/enforce.go (lines 215-275 — extractEcoPackage / extractEcoPackageFromCommand / installPrefixesOverlay / firstNonFlagToken / stripVersionSuffix)
    - internal/pkgparse/pkgparse.go (the Parse contract)
    - internal/policyloader/enforce_test.go (the regression net — must still pass; run, not edited)
    - internal/policy/engine_test.go (where the new pnpm/bun catalog-match case is added — in this plan's files_modified)
  </read_first>
  <action>
    In `internal/policyloader/enforce.go`: replace the body of `extractEcoPackageFromCommand` with a call to `pkgparse.Parse(cmd)` — on ok && `parsed.IsInstall` return `(parsed.Ecosystem, parsed.Package)`, else `("","")`. Import `github.com/bantuson/beekeeper/internal/pkgparse`. DELETE the duplicate `installPrefixesOverlay` table (240-253), `firstNonFlagToken`, and `stripVersionSuffix` (now provided by pkgparse). Leave the direct-shape branch (explicit ecosystem/package keys, lines 222-227) untouched. Then in `internal/policy/engine_test.go` ADD one table case proving F3/SC1: a ToolCall with `command:"pnpm add evil-pkg"` against a MultiCatalogLookup fake keyed `"npm::evil-pkg"` returns a catalog-match decision (not "no package identified"/"no catalog match"). Mirror the existing engine_test fake-index construction.
  </action>
  <verify>
    <automated>go test ./internal/policy/... ./internal/policyloader/...</automated>
  </verify>
  <done>enforce.go compiles, imports pkgparse, no duplicate table; both `internal/policy` and `internal/policyloader` suites pass; the new pnpm-add case in engine_test.go proves catalog matching of a pnpm install (F3 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent command string → engine/overlay extraction | Attacker-influenced install command reaches both enforcement paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering (F3 bypass) | engine.go / enforce.go extraction | mitigate | Both paths now key pnpm/bun/yarn installs to ecosystem "npm" via the single pkgparse source — a malicious package installed with pnpm/bun can no longer evade catalog corroboration (SC1). New engine_test case is the proof. |
| T-08-05 | Tampering (parser drift) | two duplicate copies | mitigate | Collapsing two copies to one (pkgparse) removes the drift surface where catalog-match and overlay views of "what is being installed" could disagree. Existing engine_test/enforce_test are the byte-identical regression net (A5). |
</threat_model>

<verification>
- `go test ./internal/policy/... ./internal/policyloader/...` green (existing tests UNCHANGED — proves byte-identical refactor).
- `go build ./...` compiles (both consumers wired to pkgparse).
- New engine_test case: `pnpm add evil-pkg` → catalog match under key `npm::evil-pkg` (F3/SC1).
</verification>

<success_criteria>
- No duplicate install-prefix table remains in the repo (only pkgparse holds it).
- F3 is closed at the catalog layer: pnpm/bun/yarn installs are catalog-matched via ecosystem "npm" (NUDGE-01 / SC1).
- Regression nets (engine_test, enforce_test) pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/08-package-manager-nudge-behavioral-test-suite/08-02-SUMMARY.md`
</output>
</content>
