---
phase: 15-marketing-home
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - web/components/home/harness-matrix.tsx
  - web/components/home/honesty-callout.tsx
  - web/app/page.tsx
  - web/tests/home.spec.ts
autonomous: true
requirements: [HOME-04, HOME-05]
must_haves:
  truths:
    - "The page shows all 15 harnesses with honest tier labels and live-verification caveats, linking to the integration docs"
    - "The page surfaces an honesty / known-gaps callout linking to the security-posture docs (no overclaiming)"
    - "The final page renders all required sections in order and builds to out/index.html"
    - "The hero is fully visible above the fold at 1280px and the page renders correctly in BOTH light and dark themes"
  artifacts:
    - path: "web/components/home/harness-matrix.tsx"
      provides: "15-harness support matrix with tier labels + caveats + integration-docs link (HOME-04)"
      min_lines: 50
    - path: "web/components/home/honesty-callout.tsx"
      provides: "Honesty / known-gaps callout linking to security-posture docs (HOME-05)"
      min_lines: 30
    - path: "web/tests/home.spec.ts"
      provides: "Playwright both-theme + above-the-fold assertions against out/index.html"
      min_lines: 30
  key_links:
    - from: "web/app/page.tsx"
      to: "web/components/home/harness-matrix.tsx"
      via: "import + render inside main"
      pattern: "HarnessMatrix"
    - from: "web/app/page.tsx"
      to: "web/components/home/honesty-callout.tsx"
      via: "import + render inside main"
      pattern: "HonestyCallout"
    - from: "web/components/home/harness-matrix.tsx"
      to: "/docs/integration"
      via: "link to integration docs"
      pattern: "/docs/integration"
    - from: "web/components/home/honesty-callout.tsx"
      to: "/docs/security"
      via: "link to security-posture docs"
      pattern: "/docs/security"
---

<objective>
Add the two sections the mockup LACKS (D-04 reconciliation): the 15-harness support matrix
(HOME-04, sourced from docs/harness-support-matrix.md) and the honesty / known-gaps callout
(HOME-05, sourced from docs/THREAT-MODEL.md §8). Assemble the final page in its required order,
gate the production build, and prove both-theme correctness + above-the-fold layout at 1280px
with a Playwright-on-static-export test (the project's established verification pattern).

Purpose: HOME-04 and HOME-05 are the two requirements the mockup does not cover; per D-04 they
must be ADDED, styled consistently. They are the highest-trust content on the page — a
security product's matrix and known-gaps must be accurate (D-03). This wave also runs the
phase-complete build + visual gate.
Output: 2 new section components + a Playwright spec, final page.tsx assembly, build green,
both themes verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-marketing-home/15-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@beekeeper-docs.html
@web/app/globals.css
@docs/harness-support-matrix.md
@docs/THREAT-MODEL.md
@README.md
@web/components/changelog/breaking-change-callout.tsx
@.planning/phases/15-marketing-home/15-01-PLAN.md
@.planning/phases/15-marketing-home/15-02-PLAN.md
</context>

<design_source_notes>
The mockup (beekeeper-docs.html) has NO harness matrix and NO known-gaps callout. Per D-04 the
executor ADDS both, styled consistently with the mockup + Phase-12 design system. The mockup's
"self-defense" callout (lines 1022-1065) is positive-claims marketing, NOT the honest known-gaps
content HOME-05 requires — this wave builds the honesty callout from docs/THREAT-MODEL.md §8
instead. The harness matrix reuses the mockup's table/grid visual language (the .threats /
.feature card styling) so it reads as part of the same page.

TOKEN RULE: raw theme tokens only (var(--bg/--surface/--surface-2/--border/--border-strong/
--fg/--fg-strong/--dim/--dimmer/--amber/--teal/--coral/--red/--green)). NEVER --color-bk-* for
color. The honesty callout MAY follow the breaking-change-callout.tsx pattern (border-left +
color-mix(in srgb, var(--coral) 10%, transparent)) for a dual-theme-safe accent. Reuse the
Wave-1 Section/SectionHead primitives.

DECISION TRACEABILITY: implements D-04 (add the missing HOME-04 + HOME-05 sections), D-03
(content accuracy — matrix tiers + known gaps must be true to the shipped binary), and the
deferred-deploy note from D-02 (SITE-03 is NOT a must_have; this wave verifies the BUILT page,
not a live URL).
</design_source_notes>

<harness_matrix_source>
HOME-04 content comes verbatim from docs/harness-support-matrix.md. All 15 harnesses, grouped
by tier, with honest caveats:

- **Tier 1 — full hook-block (exit 2 + per-harness deny JSON):** Claude Code (LIVE-VERIFIED —
  the only locally live-verified harness, HPC-04), Codex, Cursor, Augment, CodeBuddy, Qwen Code,
  Gemini CLI, Copilot, Antigravity, Windsurf. (The non-Claude-Code Tier-1 harnesses are
  "documented contract, unverified locally" — label them as such, do NOT imply they are live-tested.)
- **Tier 2 — hook-block with caveats:** Hermes (structurally fail-OPEN — exit codes ignored,
  JSON-only block), Cline (macOS/Linux ONLY, no Windows), OpenCode (JS plugin; misses subagent
  task calls / historically MCP).
- **Tier 3 — MCP gateway only (NATIVE TOOLS UNGUARDED):** Kilo, Trae.

The matrix MUST surface: (1) that only Claude Code is locally live-verified and the other 14 are
documented-contract; (2) the Tier-3 native-tool-unguarded caveat; (3) the Hermes fail-OPEN caveat.
Link to the integration docs at /docs/integration (the resolving docs section). Count = 15 exactly.
</harness_matrix_source>

<honesty_callout_source>
HOME-05 content comes from docs/THREAT-MODEL.md §8 "Known Gaps and Explicit Non-Defenses" + the
known-gaps named in CONTEXT D-03. Surface at least these four, accurately:

1. **Hermes fail-open** — Hermes ignores exit codes; a block rests entirely on stdout JSON; any
   timeout/crash/non-JSON makes Hermes allow the tool (THREAT-MODEL §8 "Hermes Is a Structurally
   Fail-OPEN Harness").
2. **Tier-3 native tools unguarded** — Kilo/Trae have no pre-exec hook; only MCP-routed tools are
   intercepted; native Bash/file/shell calls bypass Beekeeper (THREAT-MODEL §8 "Tier-3 Harnesses").
3. **`release_age` / lifecycle-script allowlist unenforced in v1.3.0** — these policy rule types
   are accepted in policy files but NOT enforced by the engine (THREAT-MODEL "Overlay Limitations").
4. **Gateway `--bind 0.0.0.0` caveat** — binding to a non-loopback interface exposes the proxy over
   plaintext HTTP with only a bearer token; the promised `allow_remote_gateway` gate is not
   implemented (THREAT-MODEL §8 "Gateway Remote-Bind Exposure").

Frame this as the project's stated posture: "Don't trust us; verify us." Link to /docs/security
(the security-posture / known-gaps docs page). NO overclaiming — this is the honesty section, so
it states limitations plainly. Do NOT invent gaps not in the threat model.
</honesty_callout_source>

<tasks>

<task type="auto">
  <name>Task 1: 15-harness support matrix (HOME-04)</name>
  <read_first>
    - docs/harness-support-matrix.md (the authoritative 15-harness tier table, tier definitions, and honesty notes) — the data source
    - the <harness_matrix_source> block in THIS plan — the grouped list + required caveats
    - beekeeper-docs.html (.threats table styling lines 402-470 and .feature card styling lines 367-400) — visual language to match
    - web/components/home/section.tsx (Section + SectionHead from Wave 1)
    - web/components/ui/badge.tsx (shadcn Badge available for tier labels)
    - web/app/globals.css (raw theme tokens)
  </read_first>
  <files>web/components/home/harness-matrix.tsx</files>
  <action>
    Create harness-matrix.tsx as a SERVER component exporting `HarnessMatrix`. SectionHead with
    an amber (or teal) eyebrow ("Harness support"), a title, and a lede stating Beekeeper supports
    15 agent harnesses across three tiers. Render the 15 harnesses grouped into three tier blocks
    (Tier 1 / Tier 2 / Tier 3) using the mockup's table or card visual language. For each harness
    show its name and a short caveat. Use tier badges (Tier 1 amber/green, Tier 2 coral, Tier 3 red)
    via the shadcn Badge or styled spans with raw tokens.

    REQUIRED honesty surfacing (HOME-04 caveats, from docs/harness-support-matrix.md):
    - Claude Code is the ONLY locally live-verified harness — label it distinctly (e.g. a
      "live-verified" marker); label the other nine Tier-1 harnesses "documented contract".
    - Tier 3 (Kilo, Trae): mark "native tools UNGUARDED — MCP gateway only".
    - Tier 2 Hermes: mark "fail-OPEN"; Cline: "macOS/Linux only"; OpenCode: "plugin; subagent gap".

    Include a link to the integration docs at /docs/integration (resolves under the Phase-13 docs
    skeleton). Codify the data exactly from the source doc — all 15 names, accurate tiers. Do NOT
    add a harness not in the matrix, and do NOT upgrade any tier or drop a caveat.

    No animation; static content. Both-theme safe via raw tokens.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec tsc --noEmit` exits 0.
    - harness-matrix.tsx contains all 15 harness names: "Claude Code", "Codex", "Cursor", "Augment", "CodeBuddy", "Qwen", "Gemini", "Copilot", "Antigravity", "Windsurf", "Hermes", "Cline", "OpenCode", "Kilo", "Trae".
    - It contains the strings "Tier 1", "Tier 2", "Tier 3", a live-verified marker for Claude Code (e.g. "live-verified" or "Live-verified"), the Tier-3 "UNGUARDED" caveat, and the Hermes "fail-OPEN" caveat.
    - It links to "/docs/integration".
    - No `--color-bk-` color usage (grep, excluding comment lines).
  </acceptance_criteria>
  <done>HarnessMatrix renders all 15 harnesses grouped by tier with honest caveats (Claude-Code-only live-verified, Tier-3 unguarded, Hermes fail-OPEN), links to /docs/integration, and is dual-theme safe.</done>
</task>

<task type="auto">
  <name>Task 2: Honesty / known-gaps callout (HOME-05)</name>
  <read_first>
    - docs/THREAT-MODEL.md §8 "Known Gaps and Explicit Non-Defenses" (lines 513-642) — the data source
    - the <honesty_callout_source> block in THIS plan — the four required gaps + framing
    - web/components/changelog/breaking-change-callout.tsx (the dual-theme-safe callout pattern: border-left + color-mix accent, raw var(--red)/var(--coral)) — REUSE this pattern
    - web/components/home/section.tsx (Section + SectionHead)
    - web/app/globals.css (raw theme tokens)
  </read_first>
  <files>web/components/home/honesty-callout.tsx</files>
  <action>
    Create honesty-callout.tsx as a SERVER component exporting `HonestyCallout`. SectionHead with
    a CORAL eyebrow (e.g. "Built honest" / "Known gaps"), a title (e.g. "We tell you what it does
    NOT do."), and a lede stating that for a security product, honest limitations are the point —
    "Don't trust us; verify us." Then render the four known gaps from <honesty_callout_source> as
    a list of items (reuse the breaking-change-callout.tsx visual pattern — a bordered callout with
    a color-mix accent so it is dual-theme safe; coral or amber accent, NOT the red breaking-change
    red so it reads as "honest disclosure" not "error"):

    1. Hermes is structurally fail-OPEN (exit codes ignored; block rests on stdout JSON).
    2. Tier-3 harnesses (Kilo, Trae): native tools are UNGUARDED — only MCP-routed tools intercepted.
    3. `release_age` and lifecycle-script allowlist policy rules are NOT enforced in v1.3.0.
    4. Gateway `--bind 0.0.0.0` exposes the proxy over plaintext HTTP; the `allow_remote_gateway`
       gate promised in help text is not implemented.

    Each item: a short honest sentence drawn from THREAT-MODEL.md §8 (do NOT soften or omit the
    caveat). End with a prominent link to /docs/security (the co-located security-posture +
    known-gaps docs). NO overclaiming, NO invented gaps. This is the HOME-05 honesty/known-gaps
    callout — its accuracy is the whole point.
  </action>
  <verify>
    <automated>cd web && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec tsc --noEmit` exits 0.
    - honesty-callout.tsx references all four gaps: contains "Hermes" + "fail-OPEN" (case-insensitive), "UNGUARDED" (Tier-3), "release_age", and "0.0.0.0" (the gateway bind caveat).
    - It links to "/docs/security".
    - It does NOT reuse a green/"all clear" framing for these items (this is the limitations section).
    - No `--color-bk-` color usage (grep, excluding comment lines).
  </acceptance_criteria>
  <done>HonestyCallout surfaces the four documented known gaps accurately (Hermes fail-open, Tier-3 unguarded, release_age unenforced, --bind 0.0.0.0 caveat), links to /docs/security, uses a dual-theme-safe accent, and adds no invented claims.</done>
</task>

<task type="auto">
  <name>Task 3: Final page assembly + build gate + both-theme Playwright verification (HOME-04, HOME-05, phase-complete gate)</name>
  <read_first>
    - web/app/page.tsx (as left by Wave 2: SiteHeader + main{Hero, FeatureCards, OriginStory, HowItWorks} + SiteFooter)
    - .planning/phases/13-docs-content-pipeline/13-03-PLAN.md or 13-03-SUMMARY.md (the established Playwright-on-static-export pattern: Python Playwright 1.57.0 + cached chromium, serve out/ then assert) — match the project's existing approach for the both-theme + above-the-fold proof
    - web/next.config.mjs (trailingSlash:true → home emits to out/index.html at root; serve out/ to test)
    - web/package.json (the `pnpm dlx serve out` start script; build script)
    - the harness-matrix.tsx and honesty-callout.tsx from Tasks 1-2
  </read_first>
  <files>web/app/page.tsx, web/tests/home.spec.ts</files>
  <action>
    (A) Final assembly: edit web/app/page.tsx so `<main id="main-content">` renders the full
    section order: `<Hero />`, `<FeatureCards />`, `<OriginStory />`, `<HowItWorks />`,
    `<HarnessMatrix />`, `<HonestyCallout />`. (Optionally a closing CTA banner from the mockup
    lines 1189-1201 may be added before SiteFooter, but it is not required — HOME-01..05 are the
    gate.) Import the two new components. This is the canonical final ordering.

    (B) Build gate: run `pnpm build` and assert out/index.html contains the HOME-04 + HOME-05
    markers (all 15 harnesses + the four known gaps + the two doc links).

    (C) Both-theme + above-the-fold Playwright proof: create web/tests/home.spec.ts following the
    project's established Playwright-on-static-export pattern used in Phase 13/14 (serve the built
    out/ directory, navigate to "/"). The test MUST assert:
      - The hero headline ("autonomous coding agents"), the install chip command, and the
        "Read the docs" link are visible within a 1280x800 viewport WITHOUT scrolling (above the
        fold at 1280px — HOME-01 SC-1). Assert the install chip and the Read-the-docs link have a
        boundingBox whose y+height is within the 800px viewport height.
      - The page renders in DARK theme (default) and in LIGHT theme (toggle .light on
        documentElement or set the bk-theme storage to light and reload) with the body
        background actually changing between themes (read computed background-color in each
        theme and assert they differ) — this is the dual-theme proof that catches the
        --color-bk-* freeze.
      - All 15 harness names and the four known-gaps markers are present in the rendered DOM.
    Use whatever runner the repo already uses for these Python/Playwright static-export checks
    (do NOT introduce a new JS Playwright dependency if the project standard is Python Playwright —
    match Phase 13/14). If the established pattern is a Python script rather than a .spec.ts,
    create the equivalent script at web/tests/home_spec.py instead and update files_modified
    accordingly in the SUMMARY — the REQUIREMENT is a both-theme + above-the-fold automated proof,
    matching the existing project pattern, not a specific filename.
  </action>
  <verify>
    <automated>cd web && pnpm build && grep -q "Claude Code" out/index.html && grep -q "Kilo" out/index.html && grep -q "Trae" out/index.html && grep -q "Hermes" out/index.html && grep -q "release_age" out/index.html && grep -q "0.0.0.0" out/index.html && grep -q "/docs/integration" out/index.html && grep -q "/docs/security" out/index.html</automated>
    <human-check>Run the both-theme + above-the-fold Playwright proof per the Phase-13/14 pattern; confirm it passes (hero above fold @1280px; body background differs between light/dark; 15 harnesses + 4 gaps present).</human-check>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm build` exits 0 and emits out/index.html.
    - out/index.html contains "Claude Code", "Kilo", "Trae", "Hermes" (harness matrix present), "release_age" and "0.0.0.0" (known gaps present), and both "/docs/integration" and "/docs/security" links.
    - page.tsx renders, in order inside main: Hero, FeatureCards, OriginStory, HowItWorks, HarnessMatrix, HonestyCallout.
    - The Playwright (or project-standard Python Playwright) proof passes: hero headline + install chip + Read-the-docs link are above the fold at 1280x800; body background-color differs between dark and light themes; all 15 harness names render.
    - No `--color-bk-` color usage in page.tsx or the two new components (grep, excluding comment lines).
  </acceptance_criteria>
  <done>Final page renders all six sections in order; `pnpm build` emits out/index.html with the full HOME-04 matrix + HOME-05 gaps + doc links; the both-theme + above-the-fold Playwright proof passes. HOME-01..05 all satisfied; SITE-03 (live deploy) remains deferred per D-02.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New files created by THIS plan (Wave 3):
- `web/components/home/harness-matrix.tsx` — 15-harness support matrix (server, HOME-04)
- `web/components/home/honesty-callout.tsx` — known-gaps honesty callout (server, HOME-05)
- `web/tests/home.spec.ts` (or `web/tests/home_spec.py` if matching the project's Python-Playwright standard) — both-theme + above-the-fold automated proof

Modified:
- `web/app/page.tsx` — final assembly: Hero + FeatureCards + OriginStory + HowItWorks + HarnessMatrix + HonestyCallout

Deferred (NOT produced this phase, per D-02): the live Vercel deploy + public URL (SITE-03 /
ROADMAP SC-6). The out/ directory remains Vercel-deployable (static export retained); the deploy
steps are noted but not executed — pending repo push (bantuson/beekeeper) and Vercel setup.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content → static HTML | Matrix + known-gaps copy authored in-repo from docs/harness-support-matrix.md + docs/THREAT-MODEL.md, emitted as static text |
| page → internal doc links | Matrix → /docs/integration; honesty callout → /docs/security (both resolve under the Phase-13 docs skeleton) |
| test → served out/ | Playwright serves the built out/ locally and asserts rendered DOM — no external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering (content integrity) | harness-matrix.tsx tiers/caveats | mitigate | Per D-03 the matrix is codified verbatim from docs/harness-support-matrix.md: only Claude Code marked live-verified, Tier-3 UNGUARDED caveat, Hermes fail-OPEN caveat all required by grep gate. No tier upgrade, no dropped caveat. A security product overclaiming its coverage is an integrity defect. |
| T-15-10 | Tampering (content integrity) | honesty-callout.tsx known gaps | mitigate | The four gaps are drawn from THREAT-MODEL.md §8 and asserted present (Hermes fail-open, Tier-3 unguarded, release_age unenforced, --bind 0.0.0.0). No softening, no invented gaps — accuracy is the section's purpose. |
| T-15-11 | Spoofing / link safety | matrix + footer external links | mitigate | All section links are internal (/docs/*). If any external link (e.g. a future GitHub URL) is added, it MUST carry rel="noopener noreferrer" on target=_blank — but this phase ships only internal links, so no _blank/noopener surface is introduced. |
| T-15-12 | Tampering (XSS) | both new components | mitigate | No dangerouslySetInnerHTML; all content is JSX-escaped React. |
| T-15-13 | Repudiation / false confidence | honesty-callout omission risk | mitigate | The Playwright + grep gates fail the build if any of the four documented gaps is missing, so the page cannot silently drop a known limitation. |
| T-15-SC | Tampering (supply chain) | npm installs | accept | Zero new runtime dependencies. The Playwright test uses the project's already-established Python-Playwright (1.57.0 + cached chromium) tooling from Phase 13/14 — no new npm package added. No legitimacy gate needed. |
</threat_model>

<verification>
- `cd web && pnpm exec tsc --noEmit` exits 0 for each new component.
- `cd web && pnpm build` exits 0 and emits out/index.html.
- Grep gates on out/index.html: 15 harness names (sampled: Claude Code, Kilo, Trae, Hermes), four known-gaps markers (release_age, 0.0.0.0, Hermes fail-OPEN, Tier-3 UNGUARDED), and both /docs/integration + /docs/security links.
- Playwright both-theme proof: hero above the fold at 1280x800; body background differs dark vs light (catches the --color-bk-* freeze); all 15 harnesses render.
- No --color-bk-* color usage anywhere in the home components.
</verification>

<success_criteria>
HOME-04 satisfied: all 15 harnesses shown with honest tier labels + live-verification caveats,
linking to /docs/integration. HOME-05 satisfied: an honesty / known-gaps callout surfaces the
four documented gaps and links to /docs/security, with no overclaiming. The final page renders
all six sections in order, builds to out/index.html, and is proven correct in both light and
dark themes with the hero above the fold at 1280px. HOME-01..05 are all satisfied. SITE-03 (the
live Vercel deploy) remains deferred per D-02 — the page is build-verified locally, not against a
live URL.
</success_criteria>

<output>
Create `.planning/phases/15-marketing-home/15-03-SUMMARY.md` when done.
</output>
