# Phase 31 — Test, Coverage & E2E + CI Matrix (PLAN)

Milestone v1.5.0 "Install Posture" (ships as v1.1.0). Reqs: IPST/IPVIEW/IPOVR test coverage, REL-01 (finalize). **Gate 2 (maintainer signs the release tag) after this phase.**
Hand-managed (summary-style ROADMAP). Go repo only (`feat/install-posture-foundation`, SSH-signed).

## Baseline (from the Phase 31 coverage scout — install posture is ALREADY well-tested)
- Rules (`internal/policy/{release_age,lifecycle,remote_source}_test.go`): ~6/6/4 tests each + import-purity contracts. Fail-soft-on-unknown covered.
- Adapter (`internal/check/posture_adapter_test.go`): 25 unit tests across every decision branch; warnUnknown not lifted by block.
- Live path (`internal/check/posture_integration_test.go`): 9 `RunCheck` tests incl. the T-09-31 security gates (allow-always + block-mode CANNOT downgrade a catalog malware block).
- Overrides (`cmd/beekeeper/posture_override_test.go`): allow_once / allow_always / enforce_block each assert the DISTINCT `posture_override` record + config persistence.
- View read-only IPVIEW-02 (`cmd/beekeeper/posture_cmd_test.go:TestPostureCmd_ReadOnlyGuarantee`): sha256 byte-for-byte unchanged over .npmrc/pnpm-workspace.yaml/bunfig.toml. `TestPostureCmd_Output_NpmAndPnpm` already covers the npm+pnpm view (criterion 3 view = DONE).
- SENTRY-009 (`internal/sentry/install_observe_test.go`): observe-only, QuarantineRec=false, record_type sentry_install_observed, ancestry-gated, non-install ignored.
- Coverage gate: package-level linkage already accounts for every posture file; NO allowlist entry needed.
- CI (`.github/workflows/ci.yml`): 3×GOOS test+race, vet, mod-verify, fuzz×4, coverage-floor. Gap: `-tags e2e` is NOT run in CI.

## Gaps to close (this phase)

### 31-01 — Unit + live-path gap tests (Go)
1. `internal/policy/release_age_test.go`: `TestReleaseAgeExactly24hBoundary` — 1440 min exactly → allow (boundary not block).
2. `internal/policy/lifecycle_test.go`: extend to assert each lifecycle script type fires (preinstall, prepare, install, postuninstall), not just postinstall (table case).
3. `internal/check/posture_adapter_test.go`: `TestPostureFetchTimeout` — stub the publish-age fetcher to exceed `postureFetchTimeout` (2500ms) → fail-soft WARN even under block mode (registry-error path is tested; add the explicit timeout case).
4. `internal/check/posture_integration_test.go` (live `RunCheck`): `TestRunCheckPostureBlockModeBlocksLifecycle` (lifecycle rule opted to block + postinstall present → block/exit 1) and `TestRunCheckPostureBlockModeBlocksRemoteSource` (git-remote rule opted to block + git spec → block). These extend block-mode coverage beyond release-age at the live layer.
5. `cmd/beekeeper/posture_override_test.go`: add
   - `TestPostureEnforceWarnWritesRecord` — `posture enforce <rule> --warn` writes `enforce_warn` + sets PostureActionWarn.
   - `TestPostureAllowAlwaysEcosystemScoped` — `--ecosystem npm` scopes the exemption (npm allowed, pypi still warns).
   - `TestPostureAllowAlwaysConfigPersistence` — after allow-always, reload config from disk → entry survives.
   - CLI-level tighten-only sanity: confirm `internal/config/layered_posture_test.go` covers untrusted block->warn refusal; if no CLI-surface assertion exists, add a thin one. (Do NOT duplicate if the config-layer test is authoritative; note the decision in the SUMMARY.)
6. (Optional/MEDIUM if time) `internal/sentry/install_observe_test.go`: `TestSENTRY009NestedSpawns` (agent→bash→npm install still observed); scoped-package name.

### 31-02 — E2E + CI wiring + coverage report (REL-01 finalize)
7. `internal/check/e2e_test.go` (`//go:build e2e`, real binary): add posture sub-cases to `TestE2ELiveBinary` (or a sibling):
   - **agent-install-blocked/warned-at-hook-with-reason+audit**: fresh-package `npm install` via `beekeeper check` with the posture publish-age stubbed <24h (use the same stub seam the integration tests use, or a fixture catalog) → default mode warns (exit 0) with a posture reason in the audit record; under a block-mode `--config` → blocks (exit 1) with reason + audit `decision:"block"`.
   - **human-install-observed-not-blocked**: assert the SENTRY-009 observe path records `sentry_install_observed`/`decision:"observe"` and the hook does NOT block a non-agent install (the hook only sees agent tool calls; the boundary statement says Sentry observes, never prevents). If a live daemon tap is infeasible cross-platform (it is — daemon e2e is CI/Linux-only), assert this at the EvaluateEvent integration level + reference the existing SENTRY-009 tests; name the daemon-level cross-platform check as a documented CI-only gap.
8. `.github/workflows/ci.yml`: add an `e2e` job (ubuntu) that builds the binary and runs `go test -tags e2e -run TestE2ELiveBinary ./internal/check/...` so the posture + existing SPATH/CORR/HOOK E2E actually run in CI; wire it into the release-gate `needs` (mirror the fuzz-sentry→release-gate pattern from Phase 21). Statically authored + locally build-verified (live at first push per D-05).
9. Coverage report — `docs/validation-register.md` (extend) or a new `docs/posture-validation.md`: enumerate the install-posture rules/branches/override paths with their covering tests, and NAME the deliberate gaps honestly (SENTRY-009 per-OS daemon taps are CI/Linux-only; DNS correlation out of scope; shim machine-wide enforcement is roadmap). This is the criterion-4 "coverage report with deliberate gaps named".
10. REL-01 finalize: confirm the version path is v1.1.0 (binary version is ldflags-injected at tag/build time; nothing hardcoded to bump in Go beyond what Gate 2 sets) and that the web changelog v1.1.0 entry exists (done in 30-02). Cross-check no doc claims a roadmap item as shipped.

## Verification (orchestrator)
- `go build ./... && go vet ./... && go test ./...` green; `go test -tags fuzz ...` seeds clean (unchanged); `go test -tags e2e -run TestE2ELiveBinary ./internal/check/...` green locally (Windows: the posture + SPATH/CORR/HOOK cases run; daemon-tap cases are CI-only and skip with a structured reason).
- `BEEKEEPER_COVERPROFILE=coverage.out go test ./internal/coveragegate -run TestCoverageFloor` green (no posture file unaccounted).
- `GOOS=linux go vet ./...` + `GOOS=darwin go vet ./...` (cross-compile sanity for the build-tagged sentry/e2e files).
- New tests are em-dash-free; assertions are behavior-asserting (decision + reason + audit), not just non-panic.
- Update STATE (Phase 31 → complete) and ROADMAP; then prepare the v1.1.0 release notes for **Gate 2** (maintainer signs the tag).
