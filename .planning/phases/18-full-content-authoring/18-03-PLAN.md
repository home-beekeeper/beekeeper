---
phase: 18-full-content-authoring
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - web/content/docs/configuration/index.mdx
  - web/content/docs/cli-reference/index.mdx
autonomous: true
requirements: [DOCS-04, DOCS-07, DOCS-09]
must_haves:
  truths:
    - "Configuration docs describe layered config, policy-as-code, sensitive paths, and BOTH soft/hard nudge modes AND blocking behavior accurate to the real ValidateNudgeConfig (SC-3 support)"
    - "release_age and lifecycle_script_allowlist are labeled unenforced via <UnenforcedCallout> in configuration"
    - "CLI reference documents every shipped subcommand and flag (SC-4) with no phantom commands"
  artifacts:
    - path: "web/content/docs/configuration/index.mdx"
      provides: "Layered config, fail_mode, policy-as-code, DefaultSensitivePaths, nudge config (soft/hard + require_hardened blocking), unenforced-feature labels"
      contains: "<UnenforcedCallout"
      min_lines: 70
    - path: "web/content/docs/cli-reference/index.mdx"
      provides: "Exhaustive subcommand tree (H2-per-command, H3-per-subcommand) for all shipped commands; plumbing grouped"
      contains: "source_doc:"
      min_lines: 120
  key_links:
    - from: "web/content/docs/configuration/index.mdx"
      to: "UnenforcedCallout"
      via: "MDX component for release_age / lifecycle_script_allowlist"
      pattern: "<UnenforcedCallout"
---

<objective>
Author the Configuration (DOCS-04) and CLI Reference (DOCS-07) docs sections, accurate to the shipped binary, with `source_doc:` frontmatter and `<UnenforcedCallout>` labels (DOCS-09).

Purpose: SC-3 support (configuration with copyable examples) and SC-4 (CLI reference covers all subcommands and flags). These share `cmd/beekeeper/main.go` + `config.go` as authoritative sources (D-04 — authored from the real cobra definitions). Configuration must accurately document the nudge validator surface (OQ-1: `config set nudge.mode` only accepts soft|hard; `require_hardened: true` is the documented blocking path — do NOT present `mode: "block"` as user-settable). CLI reference must NOT document non-existent commands (`baseline`/`notify` are internal packages per OQ-2/OQ-3; `check --input` does not exist).
Output: expanded `configuration/index.mdx` (with UnenforcedCallout) and `cli-reference/index.mdx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Execution runs INLINE on main. Wave 2 — depends on 18-01 (UnenforcedCallout must exist + be registered before configuration uses it). Shares no files with 18-02/18-04/18-05; may run in parallel.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-full-content-authoring/18-CONTEXT.md
@.planning/phases/18-full-content-authoring/18-RESEARCH.md
@.planning/phases/18-full-content-authoring/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Configuration docs (DOCS-04) with UnenforcedCallout</name>
  <files>web/content/docs/configuration/index.mdx</files>
  <read_first>
    - web/content/docs/configuration/index.mdx (current 20-line stub — preserve title/description; it already gets the Windows %APPDATA% state dir right — KEEP that)
    - docs/nudge.md (the AUTHORITATIVE nudge config block, modes, version_floors, minimumReleaseAge=1440 baseline)
    - cmd/beekeeper/config.go (MANDATORY: verify the EXACT ValidateNudgeConfig + applyNudgeKey surface before writing — confirm `config set nudge.mode` accepts only soft|hard and that the 5 settable keys are nudge.enabled/mode/require_hardened/preferred/check_socket_scanner; do NOT trust memory, read the real validator per OQ-1 and §8.5)
    - CLAUDE.md (layered config merge order, fail_closed default, DefaultSensitivePaths, self-host catalog)
    - docs/THREAT-MODEL.md §8 (project `.beekeeper/config.json` lowest-trust layer + fail_mode:open relax risk — the point-of-use caveat) and §9 (release_age / lifecycle_script_allowlist unenforced)
    - 18-RESEARCH.md §1 Section 3 (Configuration outline + key facts) and §3 (enforced/unenforced table) and §5.2 (UnenforcedCallout usage)
    - web/components/docs/unenforced-callout.tsx (the component created in 18-01 — confirm the `feature` prop name and usage)
  </read_first>
  <action>
    Expand configuration/index.mdx body per 18-RESEARCH.md §1 Section 3 outline: (1) Config file location (`~/.beekeeper/config.json`, Windows `%APPDATA%\beekeeper\config.json` — keep the stub's correct Windows path); (2) Layered config merge: system → user → project (`.beekeeper/config.json` in working dir) → `BEEKEEPER_*` env vars; (3) Fail mode: `fail_closed` default true, `fail_mode: open` explicit opt-in (reduces security); (4) Policy-as-code: `~/.beekeeper/policies/*.json` + reference policy validate/test/list; (5) Sensitive paths (DefaultSensitivePaths: ~/.ssh, ~/.aws, ~/.cargo/credentials, .env globs, editor MCP config dirs); (6) Package-manager nudge — document soft (advise+proceed) and hard (rewrite, still advisory) modes accurately, AND the blocking behavior via `require_hardened: true` (deny npm/yarn when no hardened PM). Per OQ-1/§8.5: `config set nudge.mode` accepts ONLY soft|hard; describe the first-install supply-chain enforcement as "Beekeeper sets blocking enforcement on first hook install" WITHOUT presenting `mode: "block"` as a user-settable `config set` value. List the 5 settable keys; show the default nudge config JSON block; (7) `beekeeper config set nudge.<key> <value>` is the only config set surface.
    Wrap `release_age` / `minimumReleaseAge` AND `lifecycle_script_allowlist` mentions in `<UnenforcedCallout feature="...">` per §5.2 (these are informational-only in v1.3.0). Add the project-config point-of-use caveat from §1 Section 3 (fail_mode:open in a project file relaxes all fail-closed nets — security-relevant).
    Frontmatter: keep title + description, ADD `source_doc: "docs/nudge.md, docs/THREAT-MODEL.md, CLAUDE.md, cmd/beekeeper/config.go"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - configuration/index.mdx frontmatter contains `source_doc:` and all four referenced paths exist on disk
    - `grep -c "<UnenforcedCallout" web/content/docs/configuration/index.mdx` returns >= 1 (release_age / lifecycle_script_allowlist labeled) — AC-2 passes
    - The page documents `require_hardened` as the blocking path: `grep -c "require_hardened" web/content/docs/configuration/index.mdx` returns >= 1
    - The page does NOT present `mode: "block"` as a `config set` value: `grep -c "config set nudge.mode block" web/content/docs/configuration/index.mdx` returns 0
    - Layered config + fail_mode documented: case-insensitive grep for "fail_closed" returns >= 1 and "BEEKEEPER_" returns >= 1
    - accuracy_spec.py exits 0 for AC-2 over this file (release_age mentioned WITH a label)
  </acceptance_criteria>
  <done>configuration/index.mdx documents layered config, policy-as-code, sensitive paths, soft/hard nudge + require_hardened blocking accurate to the real validator, labels release_age/lifecycle via UnenforcedCallout, and carries valid source_doc:; AC-1/AC-2/AC-3 pass for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Author CLI Reference (DOCS-07)</name>
  <files>web/content/docs/cli-reference/index.mdx</files>
  <read_first>
    - web/content/docs/cli-reference/index.mdx (current 28-line stub — preserve title/description; it documents only 3 commands and a phantom `check --input` flag — FIX per §8.1)
    - 18-RESEARCH.md §2 (the FULL CLI command tree — the exhaustive, source-verified inventory of every subcommand and flag; this is the authoring spine for this task)
    - cmd/beekeeper/main.go, cmd/beekeeper/config.go, cmd/beekeeper/policy.go, cmd/beekeeper/nudge.go, cmd/beekeeper/diag.go, cmd/beekeeper/protect_linux.go (spot-verify any command/flag whose exact form is uncertain against the real cobra definitions, per D-04 — §2 is source-verified but confirm anything ambiguous)
    - 18-RESEARCH.md §8.7 (Sentry platform scope — protect install/uninstall/status is Linux/systemd only) and OQ-2/OQ-3 (no baseline/notify subcommands — do NOT invent them)
    - web/content/changelog/v1.3.0/index.mdx (MDX heading/fenced-code voice analog)
  </read_first>
  <action>
    Rewrite cli-reference/index.mdx as a single exhaustive page using H2-per-top-level-command and H3-per-subcommand (18-RESEARCH.md §5.3 structure). Document EVERY user-facing command and flag from §2: version, init (`--yes`/`--no-editors`), check (`--hook`/`--tool`/`--args`; exit codes 0/1/2; input is stdin — NO `--input`), catalogs (sync/watch/verify --source/diff), audit (tail --no-follow / query --since/--agent/--tool/--decision/--limit / export --format ndjson|csv|otlp), selftest, watch, scan (`--deep`), quarantine (list/restore/purge --yes), hooks (install --target/--dry-run/--force, uninstall --target/--dry-run; target values list), gateway (--port/--upstream/--bind/--allow-remote, token, status), shim (install/uninstall/status), protect (install/uninstall/status — Linux/systemd only per §8.7), sentry (+ rules list/enable/disable — Linux only), llamafirewall (enable/disable/status), dashboard (`--admin`), policy (validate/test --tool-call/list), diag, nudge (status/check/audit --since), config (set <key> <value> with the 5 supported keys).
    Group the plumbing/internal commands (audit-record PostToolUse handler; raw `sentry` daemon; advanced shim) under a clearly-labeled "Internal / Plumbing Commands" section (§2) rather than giving them equal weight — Claude's-Discretion grouping is allowed (CONTEXT.md). Do NOT document `beekeeper baseline` or `beekeeper notify` (internal packages, not subcommands — OQ-2/OQ-3). Do NOT include `beekeeper check --input` (phantom — §8.1).
    Frontmatter: keep title + description, ADD `source_doc: "cmd/beekeeper/main.go, cmd/beekeeper/config.go, cmd/beekeeper/policy.go, cmd/beekeeper/nudge.go, cmd/beekeeper/diag.go, cmd/beekeeper/protect_linux.go"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - cli-reference/index.mdx frontmatter contains `source_doc:` and all six referenced cmd/ paths exist on disk
    - The page documents the full command set: case-insensitive grep returns >= 1 for each of "beekeeper check", "beekeeper catalogs", "beekeeper config", "beekeeper hooks", "beekeeper gateway", "beekeeper scan", "beekeeper audit", "beekeeper policy", "beekeeper quarantine", "beekeeper sentry", "beekeeper tui" OR "beekeeper dashboard", "beekeeper version", "beekeeper protect", "beekeeper llamafirewall"
    - No phantom commands: `grep -c "beekeeper check --input" web/content/docs/cli-reference/index.mdx` returns 0; `grep -c "beekeeper baseline" web/content/docs/cli-reference/index.mdx` returns 0; `grep -c "beekeeper notify" web/content/docs/cli-reference/index.mdx` returns 0
    - protect/sentry are labeled Linux/systemd-only: case-insensitive grep for "systemd" returns >= 1
    - accuracy_spec.py AC-3 finds no phantom command in this file
  </acceptance_criteria>
  <done>cli-reference/index.mdx exhaustively documents every shipped subcommand and flag from §2 with H2/H3 hierarchy, groups plumbing commands, omits phantom/internal commands, and carries valid source_doc:; AC-1/AC-3 pass for this file.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes two expanded docs sections:
- `web/content/docs/configuration/index.mdx` (DOCS-04) — first consumer of `<UnenforcedCallout>` (DOCS-09 unenforced labels)
- `web/content/docs/cli-reference/index.mdx` (DOCS-07) — exhaustive command tree
Both carry `source_doc:` frontmatter (DOCS-09).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → reader | A reader configures Beekeeper / runs a command trusting the doc reflects the shipped validator + CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure (false trust) | configuration unenforced features | mitigate | `<UnenforcedCallout>` on release_age / lifecycle_script_allowlist; AC-2 fails the build-gate review if a mention lacks the label |
| T-18-08 | Spoofing (invalid config value) | configuration nudge.mode | mitigate | Document `config set nudge.mode` as soft|hard only + `require_hardened` for blocking, verified against the real ValidateNudgeConfig (OQ-1); never present `mode: "block"` as settable |
| T-18-09 | Spoofing (phantom command) | cli-reference | mitigate | AC-3 + acceptance greps reject `check --input` / `baseline` / `notify`; the tree is authored from §2 (source-verified) |
| T-18-10 | Information Disclosure (false trust) | configuration project-config risk | mitigate | Point-of-use caveat that a project `.beekeeper/config.json` with `fail_mode:open` relaxes fail-closed (THREAT-MODEL §8) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (prose only). No legitimacy gate required. |
</threat_model>

<verification>
- `cd web && python tests/accuracy_spec.py` passes AC-1/AC-2/AC-3 for both files.
- Wave gate: `cd web && pnpm build` exits 0 (configuration uses the registered UnenforcedCallout — proves the component renders in MDX).
</verification>

<success_criteria>
- configuration documents layered config + soft/hard nudge + require_hardened blocking accurately, labels unenforced features via UnenforcedCallout.
- cli-reference documents every shipped subcommand/flag, no phantom/internal commands.
- Both carry valid source_doc:; accuracy_spec.py AC-1/AC-2/AC-3 green for these files.
</success_criteria>

<output>
Create `.planning/phases/18-full-content-authoring/18-03-SUMMARY.md` when done.
</output>
