---
phase: 18-full-content-authoring
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - web/content/docs/security/index.mdx
  - web/content/docs/integration/index.mdx
autonomous: true
requirements: [DOCS-05, DOCS-06, DOCS-09]
must_haves:
  truths:
    - "The security page co-locates posture AND known gaps on the same page: Hermes fail-open, Tier-3 unguarded, release_age/lifecycle unenforced, --bind 0.0.0.0 caveat (SC-3)"
    - "Every integration guide carries honest caveats at point-of-use: Hermes fail-open in the Hermes section, Kilo/Trae UNGUARDED in their sections, Claude-Code-only live-verified in the intro (SC-4)"
    - "Both pages carry source_doc: frontmatter and pass AC-1/AC-2/AC-3"
  artifacts:
    - path: "web/content/docs/security/index.mdx"
      provides: "Corroboration model, fail-closed, SPATH, self-protection, build hardening + co-located known gaps"
      contains: "source_doc:"
      min_lines: 90
    - path: "web/content/docs/integration/index.mdx"
      provides: "Per-harness Tier1→2→3→gateway guidance with point-of-use caveats; correct --target flag"
      contains: "source_doc:"
      min_lines: 90
  key_links:
    - from: "web/content/docs/integration/index.mdx"
      to: "Hermes fail-open caveat"
      via: "point-of-use caveat in the Hermes section"
      pattern: "fail-?open"
---

<objective>
Author the Security posture + known gaps (DOCS-05) and Integration guides (DOCS-06) docs sections — the accuracy spine of Phase 18 — with `source_doc:` frontmatter (DOCS-09).

Purpose: SC-3 (security posture AND known gaps co-located) and SC-4 (integration caveats at point-of-use). These are the highest-stakes accuracy sections: per D-02/D-03, every gap must live on the same page as the posture claim (security), and every caveat must live where the user reads it (integration). Per D-04, where THREAT-MODEL.md and any marketing claim disagree, the threat model wins. These two pages are the primary target of the human accuracy review in plan 18-06.
Output: expanded `security/index.mdx` and `integration/index.mdx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Execution runs INLINE on main. Wave 2 — depends on 18-01. Shares no files with 18-02/18-03/18-05; may run in parallel.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-full-content-authoring/18-CONTEXT.md
@.planning/phases/18-full-content-authoring/18-RESEARCH.md
@.planning/phases/18-full-content-authoring/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Security posture + co-located known gaps (DOCS-05)</name>
  <files>web/content/docs/security/index.mdx</files>
  <read_first>
    - web/content/docs/security/index.mdx (current 20-line stub — preserve title/description; the corroboration claim is accurate but incomplete per §8.1)
    - docs/THREAT-MODEL.md (the ENTIRE authoritative spine — read the whole document: corroboration model, fail-closed, SPATH, self-protection, build hardening, AND §8 known gaps, §9 enforced/unenforced, §10 exit-1→2 history, §11 TM-B-02)
    - docs/harness-support-matrix.md (Hermes fail-open, Tier-3 unguarded, Tier-1-testable=Claude-Code-only — for the gaps list)
    - CLAUDE.md (corroboration thresholds 1→warn/2→block/3→quarantine, fail-closed default, self-defense non-negotiables)
    - 18-RESEARCH.md §1 Section 4 (the full posture + co-located known-gaps outline — 12 gaps enumerated), §3 (enforced/unenforced table), §4 (all 5 honesty caveats with exact wording basis)
    - web/content/changelog/v1.3.0/index.mdx (link target for the exit-1→2 history — link, don't duplicate the BreakingChangeCallout)
  </read_first>
  <action>
    Expand security/index.mdx per 18-RESEARCH.md §1 Section 4, with posture AND known gaps CO-LOCATED on the same page (DOCS-05 / D-03 — gaps must NOT be hidden on a separate page).
    POSTURE: (1) Corroboration model (1 source→warn, 2→block, 3+→block+quarantine; per-severity thresholds); (2) Fail-closed defaults across all paths (hook, gateway, Sentry); (3) SPATH (blocks agent reads of ~/.ssh, ~/.aws, .env, editor MCP dirs; Windows ADS/trailing-dot normalization); (4) Self-protection (agent cannot read/write StateDir, overwrite binary, remove hook entry — content-aware); (5) beekeeper-self catalog (startup + post-sync self-quarantine check, separate Ed25519 key); (6) Build hardening (reproducible builds, Sigstore/cosign, SLSA L3, CycloneDX SBOM); (7) Policy-as-code escape hatch is forensically visible in audit.
    KNOWN GAPS (co-located, in a clearly-labeled section on the SAME page): Hermes fail-OPEN; Tier-3 (Kilo/Trae) native tools UNGUARDED; Tier-1 testable = Claude Code only (14 documented but not live-verified); release_age / lifecycle_script_allowlist unenforced (wrap in `<UnenforcedCallout>`); `--bind 0.0.0.0` exposes plain HTTP + `allow_remote_gateway` gate NOT implemented; project `.beekeeper/config.json` can relax fail-closed; Windsurf fail-open on non-2 exit; OpenCode subagent task gap; package-parse evasion (&&, env-var prefixes, unlisted PMs); TM-B-02 Bumblebee presence-check not Ed25519; fanotify mmap gap (Linux); Windows Sentry missing PPID. Use the EXACT wording basis from §4 where caveats overlap. For exit-1→2 history, LINK to /changelog/v1.3.0/ (do not duplicate the callout).
    Frontmatter: keep title + description, ADD `source_doc: "docs/THREAT-MODEL.md, docs/harness-support-matrix.md, CLAUDE.md"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - security/index.mdx frontmatter contains `source_doc:` and all three referenced paths exist on disk
    - All four SC-3 gaps present on the page: case-insensitive grep returns >= 1 for each of "fail-open" (Hermes), "UNGUARDED" or "unguarded" (Tier-3), "release_age", and "0.0.0.0" (--bind caveat)
    - release_age/lifecycle wrapped with a label: `grep -c "<UnenforcedCallout" web/content/docs/security/index.mdx` returns >= 1 (AC-2 passes)
    - Corroboration thresholds present: grep for "quarantine" returns >= 1 and the page references the corroboration model
    - The exit-1→2 history links to the changelog rather than duplicating: grep for "/changelog/v1.3.0" returns >= 1
    - accuracy_spec.py AC-1/AC-2/AC-3 pass for this file
  </acceptance_criteria>
  <done>security/index.mdx presents the full posture AND co-locates all known gaps (Hermes fail-open, Tier-3 unguarded, unenforced features labeled via UnenforcedCallout, --bind 0.0.0.0) on the same page, links exit-1→2 history to the changelog, and carries valid source_doc:.</done>
</task>

<task type="auto">
  <name>Task 2: Author Integration guides (DOCS-06) with point-of-use caveats</name>
  <files>web/content/docs/integration/index.mdx</files>
  <read_first>
    - web/content/docs/integration/index.mdx (current 26-line stub — preserve title/description; FIX the `--hook` → `--target` error per §8.1)
    - docs/harness-support-matrix.md (the AUTHORITATIVE 15-harness tier table + per-harness caveats — Tier 1 testable=Claude Code; Tier 1 documented = 9 others; Tier 2 = Hermes/Cline/OpenCode; Tier 3 = Kilo/Trae UNGUARDED)
    - docs/THREAT-MODEL.md §8 and §10 (Hermes fail-open structural detail; gateway --bind exposure + missing allow_remote_gateway gate)
    - cmd/beekeeper/main.go (gateway wiring: default port 7837, default bind 127.0.0.1, flags --upstream/--port/--bind/--allow-remote; the Tier-1 hooks-install target value list line ~1229)
    - 18-RESEARCH.md §1 Section 5 (Integration outline + key facts + the three mandatory point-of-use caveats) and §4.1/§4.2/§4.3/§4.4 (exact caveat wording basis for Hermes, Tier-3, Claude-Code-only, --bind)
  </read_first>
  <action>
    Expand integration/index.mdx per 18-RESEARCH.md §1 Section 5, with honest caveats AT POINT-OF-USE (D-03 — caveat lives in the harness's own section, not a footnote). Order Tier 1 → Tier 2 → Tier 3 → MCP gateway.
    INTRO: 15 harnesses, 3 tiers, honest ceiling + the Claude-Code-only-live-verified caveat (§4.3) right in the intro/table.
    TIER 1 full walkthrough (Claude Code): `beekeeper hooks install --target claude-code` (use `--target`, NOT `--hook` — fix the stub); reload behavior; live-verified.
    TIER 1 documented (9 others — Codex, Cursor, Augment, CodeBuddy, Qwen, Gemini CLI, Copilot, Antigravity, Windsurf): per-harness install command + known caveat (Cursor needs failClosed:true; Windsurf fail-open on non-2 exit).
    TIER 2 with caveats: Hermes (fail-OPEN — put the §4.1 caveat IN the Hermes section: exit codes ignored, block depends on stdout JSON, prefer the MCP gateway), Cline (macOS/Linux only — Windows returns an explicit error), OpenCode (subagent task plugin gap).
    TIER 3 gateway-only: Kilo, Trae — put the §4.2 UNGUARDED caveat IN their sections (native Bash/file/shell tools UNGUARDED; only MCP tools via the gateway are intercepted; upstream limitation).
    MCP GATEWAY: `beekeeper gateway --upstream <url> --port 7837` (default bind 127.0.0.1); the §4.4 `--bind 0.0.0.0` caveat IN the gateway section (plain HTTP, cleartext token, allow_remote_gateway gate NOT implemented); `gateway token` / `gateway status`; bearer auth.
    Frontmatter: keep title + description, ADD `source_doc: "docs/harness-support-matrix.md, docs/THREAT-MODEL.md, cmd/beekeeper/main.go"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - integration/index.mdx frontmatter contains `source_doc:` and all three referenced paths exist on disk
    - Correct flag: `grep -c "hooks install --target" web/content/docs/integration/index.mdx` returns >= 1 and `grep -c "hooks install --hook" web/content/docs/integration/index.mdx` returns 0
    - Point-of-use caveats present: case-insensitive grep returns >= 1 for each of "fail-open" (Hermes), "UNGUARDED" or "unguarded" (Kilo/Trae), "live-verified" (Claude-Code-only intro), and "0.0.0.0" (gateway --bind)
    - Gateway documented with real defaults: grep for "7837" returns >= 1 and "127.0.0.1" returns >= 1
    - accuracy_spec.py AC-1/AC-3 pass for this file
  </acceptance_criteria>
  <done>integration/index.mdx provides Tier1→2→3→gateway guidance with the Hermes fail-open, Kilo/Trae UNGUARDED, Claude-Code-only, and --bind 0.0.0.0 caveats each at their point-of-use, uses `hooks install --target`, and carries valid source_doc:.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes the two highest-stakes docs sections:
- `web/content/docs/security/index.mdx` (DOCS-05) — posture + co-located known gaps, UnenforcedCallout label
- `web/content/docs/integration/index.mdx` (DOCS-06) — per-harness point-of-use caveats
Both carry `source_doc:` frontmatter (DOCS-09); both are the primary target of the 18-06 human accuracy review.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → reader | A reader chooses a harness / trusts the security posture for a real security decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Information Disclosure (false trust) | security posture without gaps | mitigate | DOCS-05 co-location requirement — gaps live on the SAME page as posture; acceptance greps assert all four SC-3 gaps are present, preventing a posture-only page that hides limitations |
| T-18-12 | Information Disclosure (false trust) | integration Hermes/Tier-3 | mitigate | Point-of-use caveats (D-03): Hermes fail-open in the Hermes section, Kilo/Trae UNGUARDED in their sections; greps enforce presence so a reader cannot assume guarded coverage |
| T-18-13 | Spoofing (wrong flag) | integration install commands | mitigate | `hooks install --target` enforced; AC-3 + grep reject `--hook` (the check flag) being used for install |
| T-18-14 | Elevation/Exposure (remote bind) | integration gateway --bind | mitigate | The §4.4 caveat documents that `--bind 0.0.0.0` exposes plain HTTP and the `allow_remote_gateway` gate is NOT implemented — must not overclaim a gate that doesn't exist (§8.6) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (prose only). No legitimacy gate required. |
</threat_model>

<verification>
- `cd web && python tests/accuracy_spec.py` passes AC-1/AC-2/AC-3 for both files.
- Wave gate: `cd web && pnpm build` exits 0 (security uses the registered UnenforcedCallout).
- These two pages are read side-by-side with docs/THREAT-MODEL.md in plan 18-06 (AC-5 human review).
</verification>

<success_criteria>
- security co-locates posture AND all known gaps (Hermes/Tier-3/unenforced/--bind) on one page.
- integration carries the four mandatory point-of-use caveats and uses `hooks install --target`.
- Both carry valid source_doc:; accuracy_spec.py AC-1/AC-2/AC-3 green for these files.
</success_criteria>

<output>
Create `.planning/phases/18-full-content-authoring/18-04-SUMMARY.md` when done.
</output>
