---
phase: 19-test-suite-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/vitest.config.mts
  - web/tests/unit/utils.test.ts
  - web/tests/unit/reduced-motion.test.tsx
  - web/tests/unit/install-chip.test.tsx
  - web/tests/unit/metadata.test.ts
  - web/tests/unit/accuracy.test.ts
  - web/biome.json
autonomous: true
requirements: [QA-01]
user_setup: []

must_haves:
  truths:
    - "pnpm --filter web install --frozen-lockfile completes without modifying any Go-module file (go.mod/go.sum)"
    - "pnpm --filter web test runs Vitest and all pre-build unit tests pass"
    - "pnpm --filter web typecheck (tsc --noEmit) exits 0 with the new config + test files present"
    - "pnpm --filter web lint (biome check) exits 0 over the new test files"
    - "accuracy.test.ts reproduces accuracy_spec.py coverage (AC-1/AC-2/AC-3) with the constant arrays copied verbatim"
  artifacts:
    - path: "web/vitest.config.mts"
      provides: "Vitest config: jsdom default, tsconfigPaths() before react(), globals:false, accuracy=node env"
      contains: "tsconfigPaths"
    - path: "web/tests/unit/utils.test.ts"
      provides: "cn() unit test"
    - path: "web/tests/unit/reduced-motion.test.tsx"
      provides: "useReducedMotion hook test (matchMedia mocked)"
    - path: "web/tests/unit/install-chip.test.tsx"
      provides: "InstallChip render + accessible-label test"
    - path: "web/tests/unit/metadata.test.ts"
      provides: "BASE_URL / SITE_NAME constant test"
    - path: "web/tests/unit/accuracy.test.ts"
      provides: "port of accuracy_spec.py (source-MDX walk, pre-build)"
      contains: "PHANTOM_COMMANDS"
    - path: "web/package.json"
      provides: "typecheck/test/test:watch/test:postbuild/test:e2e scripts + pinned test devDependencies"
      contains: "vitest"
  key_links:
    - from: "web/vitest.config.mts"
      to: "web/tsconfig.json paths (@/*, collections/*)"
      via: "vite-tsconfig-paths plugin"
      pattern: "tsconfigPaths\\(\\)"
    - from: "web/tests/unit/accuracy.test.ts"
      to: "web/content/docs/*.mdx"
      via: "fs readdirSync recursive walk"
      pattern: "readdirSync"
---

<objective>
Install the JS-native unit test toolchain (Vitest 4.1.8 + jsdom + @vitejs/plugin-react + vite-tsconfig-paths + @testing-library/react/dom) into the `web/` workspace, add the missing npm scripts, write the Vitest config, and author all PRE-BUILD unit tests: the `cn()` util, the `useReducedMotion` hook, `InstallChip`, the `lib/metadata` constants, and the file-walk port of `accuracy_spec.py`. No `pnpm build` is needed for any of these tests — they read source files or render client components only.

Purpose: QA-01 names Vitest as the unit-test runner gating merges. This plan establishes the runner + config + the pre-build unit surface so Wave 2 (E2E + post-build SEO) and Wave 3 (CI gate + .py retirement) have a working `pnpm test`. The `accuracy_spec.py` port is the first parity-critical port — its constant arrays must be copied verbatim so no coverage is lost when the `.py` file is retired in Wave 3.

Output: `web/vitest.config.mts`, five unit test files under `web/tests/unit/`, the new package.json scripts + pinned devDependencies, and (only if `pnpm lint` actually fails) a point-of-use biome-ignore.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

NOTE: D-05 — execution runs INLINE on main (subagents lack node/pnpm/playwright). The orchestrator is the executor; per-task atomic commits; STATE/ROADMAP/REQUIREMENTS tracking is hand-managed.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-test-suite-ci/19-CONTEXT.md
@.planning/phases/19-test-suite-ci/19-RESEARCH.md
@.planning/phases/19-test-suite-ci/19-PATTERNS.md
@.planning/phases/19-test-suite-ci/19-VALIDATION.md

# Units under test (read for exact signatures/exports before writing each test):
@web/lib/utils.ts
@web/lib/reduced-motion.tsx
@web/lib/metadata.ts
@web/components/home/install-chip.tsx

# Port source-of-truth (behavioral spec for accuracy.test.ts):
@web/tests/accuracy_spec.py

# Config files modified:
@web/package.json
@web/tsconfig.json
@web/biome.json
</context>

<artifacts_this_phase_produces>
This Wave-1 plan introduces (the rest of the phase artifacts are in 19-02 / 19-03):

NEW files:
- `web/vitest.config.mts` — Vitest config
- `web/tests/unit/utils.test.ts`
- `web/tests/unit/reduced-motion.test.tsx`
- `web/tests/unit/install-chip.test.tsx`
- `web/tests/unit/metadata.test.ts`
- `web/tests/unit/accuracy.test.ts`

NEW package.json scripts: `typecheck` (`tsc --noEmit`), `test` (`vitest run`), `test:watch` (`vitest`), `test:postbuild` (`vitest run tests/postbuild`), `test:e2e` (`playwright test`).

NEW devDependencies (pinned, no postinstall scripts): `vitest@4.1.8`, `vite@8.0.16`, `@vitejs/plugin-react@6.0.2`, `jsdom@29.1.1`, `vite-tsconfig-paths@6.1.1`, `@testing-library/react@16.3.2`, `@testing-library/dom@10.4.1`, `@playwright/test@1.57.0` (installed here once; used by 19-02).

MODIFIED: `web/biome.json` — only if `pnpm lint` fails on a test file (point-of-use ignore); otherwise unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Package-legitimacy gate + install pinned test devDependencies (D-06)</name>
  <files>web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/19-test-suite-ci/19-RESEARCH.md §Package Legitimacy Audit (the verification table) and §Standard Stack (the pinned version list)
    - web/package.json (current devDependencies + packageManager pnpm@11.1.3)
    - .planning/STATE.md "Recent decisions from Phase 11" (pnpm workspace = repo-root pnpm-lock.yaml is authoritative; web file deleted; allowBuilds DENY for sharp/unrs-resolver)
  </read_first>
  <action>
    Apply the package-legitimacy gate (D-06, Phase 12/16 precedent) to the eight new devDependencies, then install them workspace-isolated.

    The legitimacy gate is already satisfied by 19-RESEARCH §Package Legitimacy Audit: all eight packages (vitest, vite, @vitejs/plugin-react, jsdom, vite-tsconfig-paths, @testing-library/react, @testing-library/dom, @playwright/test) are Approved/VERIFIED on the npm registry with multi-year histories and official GitHub repos, and NONE has a postinstall script. There are NO [ASSUMED]/[SUS]/[SLOP] packages in this set — per the planner legitimacy protocol a blocking-human checkpoint is required ONLY for [ASSUMED]/[SUS] packages, so this stays autonomous: cite the RESEARCH table rows as the disposition of record before installing. This is the npm ecosystem (NOT Go) — slopcheck targeted Go and is N/A; rely on the manual npm verification recorded in RESEARCH. BEFORE installing, re-confirm against the RESEARCH table that every one of the eight is "Approved" and has no postinstall script; if any package is found flagged [ASSUMED]/[SUS]/[SLOP] at execution time, STOP and escalate to a blocking-human gate (do not auto-install).

    Install from the repo root via the pnpm workspace filter so the install lands in `web/` devDependencies and the authoritative lockfile is the repo-root `pnpm-lock.yaml` (Phase 11 invariant): `pnpm --filter web add -D` with the EXACT pinned versions vitest@4.1.8 vite@8.0.16 @vitejs/plugin-react@6.0.2 jsdom@29.1.1 vite-tsconfig-paths@6.1.1 @testing-library/react@16.3.2 @testing-library/dom@10.4.1 @playwright/test@1.57.0. Pin @playwright/test to 1.57.0 to match the installed Python playwright r1200 so no new chromium is downloaded.

    CRITICAL Phase-11 invariant (SITE-02): the install must NOT modify go.mod, go.sum, or ANY Go-module file. Verify with `git status` that only web/package.json + the repo-root pnpm-lock.yaml changed. If the install touches a Go file or triggers a build-approval prompt, stop and resolve per the Phase-11 precedent (DENY untrusted lifecycle scripts at the root) before proceeding.
  </action>
  <acceptance_criteria>
    - All eight packages confirmed "Approved" with no postinstall script in 19-RESEARCH §Package Legitimacy Audit (no [ASSUMED]/[SUS]/[SLOP] in the set).
    - web/package.json devDependencies contains all eight at the EXACT pinned versions (no `^`/`~` ranges).
    - `cd web && pnpm install --frozen-lockfile` exits 0.
    - `git diff --exit-code go.mod go.sum` shows NO change (SITE-02 invariant); `git status` shows only web/package.json + repo-root pnpm-lock.yaml changed.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm install --frozen-lockfile && git diff --exit-code ../go.mod ../go.sum</automated>
  </verify>
  <done>All eight devDependencies present in web/package.json at the exact pinned versions; `pnpm --filter web install --frozen-lockfile` succeeds; `git diff --exit-code go.mod go.sum` shows no Go-module change.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config + package.json scripts</name>
  <files>web/vitest.config.mts, web/package.json</files>
  <read_first>
    - .planning/phases/19-test-suite-ci/19-PATTERNS.md "web/vitest.config.mts" section (the exact config shape: /// reference, plugins order, globals:false, include, exclude, environmentMatchGlobs) and "web/package.json (MODIFY)" section
    - .planning/phases/19-test-suite-ci/19-RESEARCH.md §Pattern 1 + Pitfall 1 (tsconfigPaths MUST precede react) + Pitfall 6 (tsc picks up .mts → triple-slash reference)
    - web/tsconfig.json (paths: @/*→./*, collections/*→./.source/*; include has **/*.mts so tsc will type-check the config; skipLibCheck true)
    - web/package.json (current scripts block lines 6-13)
  </read_first>
  <action>
    Create `web/vitest.config.mts` per the PATTERNS shape: first line `/// <reference types="vitest/config" />` (prevents tsc --noEmit failures since tsconfig include matches **/*.mts — Pitfall 6); import defineConfig from 'vitest/config', react from '@vitejs/plugin-react', tsconfigPaths from 'vite-tsconfig-paths'; `plugins: [tsconfigPaths(), react()]` with tsconfigPaths FIRST (Pitfall 1 — required for @/ alias resolution); `test:` block with `environment: 'jsdom'`, `globals: false` (OQ-4 — no Biome test domain, explicit imports), `include: ['tests/unit/**/*.test.{ts,tsx}']`, `exclude: ['node_modules','.next','out','tests/e2e','tests/postbuild']`, and `environmentMatchGlobs: [['tests/unit/accuracy*','node'],['tests/unit/metadata*','node']]` so the pure file-walk / constant tests run in the node env (metadata.test.ts also carries the @vitest-environment node docblock — keep both mechanisms consistent).

    Add the five scripts to web/package.json exactly as named (do not rename): `"typecheck": "tsc --noEmit"`, `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:postbuild": "vitest run tests/postbuild"`, `"test:e2e": "playwright test"`. Use `vitest run` (NOT bare `vitest`) for `test` so CI never enters watch mode. Leave the existing `start`/`build`/`lint`/`format`/`dev`/`postinstall` scripts unchanged.
  </action>
  <acceptance_criteria>
    - web/vitest.config.mts line 1 is `/// <reference types="vitest/config" />`.
    - In vitest.config.mts the `plugins` array has `tsconfigPaths()` BEFORE `react()`; `globals: false`; `environment: 'jsdom'`; `include` covers `tests/unit/**/*.test.{ts,tsx}`; `exclude` lists `tests/e2e` and `tests/postbuild`.
    - web/package.json scripts contains all five keys: typecheck, test, test:watch, test:postbuild, test:e2e (test === `vitest run`, not bare `vitest`).
    - `cd web && pnpm typecheck` exits 0 (the .mts config does not break tsc).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>web/vitest.config.mts exists with tsconfigPaths() before react(), globals:false, and the triple-slash reference on line 1; the five scripts are present in package.json; `pnpm --filter web typecheck` exits 0 (the .mts config does not break tsc).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pre-build unit tests — cn(), useReducedMotion, InstallChip, metadata constants</name>
  <files>web/tests/unit/utils.test.ts, web/tests/unit/reduced-motion.test.tsx, web/tests/unit/install-chip.test.tsx, web/tests/unit/metadata.test.ts</files>
  <read_first>
    - web/lib/utils.ts (cn signature: `export function cn(...inputs: ClassValue[])` → twMerge(clsx(inputs)))
    - web/lib/reduced-motion.tsx (exports ReducedMotionProvider + useReducedMotion(): boolean; context default prefersReducedMotion:false; uses window.matchMedia("(prefers-reduced-motion: reduce)"))
    - web/components/home/install-chip.tsx (export function InstallChip() no props; INSTALL_COMMAND = "go install github.com/bantuson/beekeeper/cmd/beekeeper@latest"; copy aria-label "Copy install command to clipboard")
    - web/lib/metadata.ts (export const BASE_URL = "https://beekeeper.vercel.app"; export const SITE_NAME = "Beekeeper")
    - .planning/phases/19-test-suite-ci/19-PATTERNS.md the four matching test sections (utils.test.ts, reduced-motion.test.tsx, install-chip.test.tsx, metadata.test.ts) for the exact assertion patterns and the matchMedia mock helper
  </read_first>
  <behavior>
    - utils.test.ts: cn('foo','bar') === 'foo bar'; cn('p-2','p-4') === 'p-4' (Tailwind conflict, last wins); cn('foo', false && 'bar', null, undefined) === 'foo' (falsy filtered)
    - reduced-motion.test.tsx: with matchMedia mocked matches:false → useReducedMotion() (via renderHook wrapped in ReducedMotionProvider) returns false; with matches:true → returns true
    - install-chip.test.tsx: render(<InstallChip />) shows text matching /go install github\.com\/bantuson\/beekeeper/; a button with accessible name "Copy install command to clipboard" exists
    - metadata.test.ts: BASE_URL === 'https://beekeeper.vercel.app'; SITE_NAME === 'Beekeeper'
  </behavior>
  <action>
    Author four Vitest test files under `web/tests/unit/`. Every file uses EXPLICIT imports (no globals — OQ-4): `import { describe, expect, it } from 'vitest'` (+ `vi` where mocking, `renderHook`/`render`/`screen` from '@testing-library/react'). Import units under test via the `@/` alias (resolved by vite-tsconfig-paths): `@/lib/utils`, `@/lib/reduced-motion`, `@/components/home/install-chip`, `@/lib/metadata`.

    utils.test.ts: jsdom default env is fine (pure function). Assert the three cn() behaviors above with `.toBe(...)`.

    reduced-motion.test.tsx: jsdom env (top-level default). jsdom does NOT implement window.matchMedia — define the mockMatchMedia helper from PATTERNS (Object.defineProperty(window,'matchMedia',...) returning {matches, media, addEventListener:vi.fn(), removeEventListener:vi.fn(), ...}). Call mockMatchMedia(false)/mockMatchMedia(true) before each renderHook; wrap with `{ wrapper: ReducedMotionProvider }`; assert result.current.

    install-chip.test.tsx: jsdom env. render(<InstallChip />); assert the install command text via screen.getByText(/.../); assert the copy button via screen.getByRole('button', { name: 'Copy install command to clipboard' }). Do NOT trigger the copy (avoids needing a navigator.clipboard mock — the component's .catch() guards it). Use the exact aria-label string from install-chip.tsx (read it; do not guess).

    metadata.test.ts: add `// @vitest-environment node` as line 1 (pure constant import, no DOM). Assert BASE_URL and SITE_NAME equal the exact strings.

    Run `pnpm test` and confirm all four files pass.
  </action>
  <acceptance_criteria>
    - Four files exist under web/tests/unit/: utils.test.ts, reduced-motion.test.tsx, install-chip.test.tsx, metadata.test.ts.
    - Each file uses explicit `import { ... } from 'vitest'` (no reliance on globals).
    - Units are imported via the `@/` alias (proves vite-tsconfig-paths resolution).
    - install-chip.test.tsx asserts the copy button by the EXACT aria-label read from install-chip.tsx.
    - metadata.test.ts line 1 is `// @vitest-environment node`.
    - `cd web && pnpm test` exits 0 (all four files green; no "Cannot find module '@/...'" error).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm test</automated>
  </verify>
  <done>`pnpm --filter web test` exits 0 with the four unit files (utils, reduced-motion, install-chip, metadata) all green; the @/ alias resolves (no "Cannot find module '@/...'" errors), proving vite-tsconfig-paths is wired.</done>
</task>

<task type="auto">
  <name>Task 4: Port accuracy_spec.py → tests/unit/accuracy.test.ts (verbatim constants, no coverage loss)</name>
  <files>web/tests/unit/accuracy.test.ts</files>
  <read_first>
    - web/tests/accuracy_spec.py (FULL FILE — source of truth; copy UNENFORCED_FEATURES, UNENFORCED_LABELS, PHANTOM_COMMANDS arrays VERBATIM; reproduce AC-1 source_doc frontmatter + referenced-path-exists, AC-2 unenforced labels present, AC-3 no phantom commands)
    - .planning/phases/19-test-suite-ci/19-PATTERNS.md "web/tests/unit/accuracy.test.ts" section (the JS port pattern: __dirname via fileURLToPath, mdxFiles() recursive readdirSync, frontmatterBlock regex, the three describe blocks)
    - .planning/phases/19-test-suite-ci/19-RESEARCH.md §Porting Map "accuracy_spec.py → accuracy.test.ts" (the per-assertion JS equivalents)
    - web/content/docs/*.mdx (the files being walked — confirm source_doc frontmatter shape)
  </read_first>
  <action>
    Create `web/tests/unit/accuracy.test.ts` as a faithful port of accuracy_spec.py. Line 1: `// @vitest-environment node` (pure file I/O, no DOM). Explicit imports: `import { describe, expect, it } from 'vitest'`; `import { readdirSync, readFileSync, existsSync } from 'fs'`; `import { join, dirname } from 'path'`; `import { fileURLToPath } from 'url'`. Define `__dirname` via `dirname(fileURLToPath(import.meta.url))` (ESM — tsconfig module is esnext, __dirname is not auto-defined). Resolve WEB_DIR = join(__dirname,'../..'), REPO_ROOT = join(WEB_DIR,'..'), CONTENT_DIR = join(WEB_DIR,'content','docs') — matching accuracy_spec.py's path math.

    Copy the three constant arrays VERBATIM from accuracy_spec.py (do not edit, reorder, or add/drop entries): UNENFORCED_FEATURES = ['release_age','minimumReleaseAge','lifecycle_script_allowlist'], UNENFORCED_LABELS = ['unenforced','not enforced','<UnenforcedCallout'], PHANTOM_COMMANDS = ['beekeeper hooks status','beekeeper catalogs rebuild','beekeeper check --input','beekeeper status']. (Cross-check the literal strings against the .py file at port time — these must match byte-for-byte so retiring the .py in Wave 3 loses zero coverage.)

    Implement mdxFiles() via readdirSync(CONTENT_DIR, { recursive: true, withFileTypes: true }) filtered to .mdx files (entry.parentPath join). Implement frontmatterBlock() via `/^---\s*\n([\s\S]*?)\n---\s*\n/.exec(text)`. Reproduce the three AC describe blocks exactly: AC-1 (every .mdx has a non-empty source_doc; each comma-split ref resolves via existsSync(join(REPO_ROOT, ref))); AC-2 (every doc that mentions an UNENFORCED_FEATURES token also contains at least one UNENFORCED_LABELS marker); AC-3 (no .mdx contains any PHANTOM_COMMANDS string). Mirror the .py file's exact per-file iteration and failure semantics so the assertion count matches.

    Run `pnpm test` and confirm accuracy.test.ts passes alongside the Task-3 tests.
  </action>
  <acceptance_criteria>
    - web/tests/unit/accuracy.test.ts exists; line 1 is `// @vitest-environment node`.
    - The three constant arrays (UNENFORCED_FEATURES, UNENFORCED_LABELS, PHANTOM_COMMANDS) are byte-identical to accuracy_spec.py (verbatim — no added/dropped/reordered entries).
    - AC-1, AC-2, AC-3 are all reproduced (source_doc path-exists, unenforced labels, no phantom commands).
    - `__dirname` is derived via `fileURLToPath(import.meta.url)` (ESM-safe).
    - `cd web && pnpm test` exits 0 with all five unit files green; the .py file is NOT deleted (retirement is Wave 3).
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm test</automated>
  </verify>
  <done>web/tests/unit/accuracy.test.ts exists; the three constant arrays are byte-identical to accuracy_spec.py; AC-1/AC-2/AC-3 are all reproduced; `pnpm --filter web test` exits 0 with all five unit files (utils, reduced-motion, install-chip, metadata, accuracy) green. NOTE: the .py file is NOT deleted here — retirement happens in Wave 3 after parity is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → web/ devDependencies | New test packages cross into the workspace at install time (supply-chain surface) |
| repo source files → Vitest node process | accuracy.test.ts reads repo-local MDX + resolves source_doc paths (no network, no user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | npm install of 8 new devDependencies | mitigate | Pin EXACT versions (no ranges); install via `--frozen-lockfile`; package-legitimacy gate (Task 1) cites RESEARCH §Package Legitimacy Audit (all Approved, none has postinstall) |
| T-19-02 | Tampering | pnpm workspace install touching Go-module files | mitigate | SITE-02 invariant: Task 1 asserts `git diff --exit-code go.mod go.sum` shows no change; install is `--filter web` scoped |
| T-19-03 | Elevation of Privilege | postinstall lifecycle scripts on new deps | accept | RESEARCH verified NONE of the 8 packages has a postinstall script; Phase-11 root `allowBuilds` DENY for unvetted lifecycle scripts remains in force |
| T-19-04 | Information Disclosure | accuracy.test.ts file walk | accept | Reads only repo-local MDX + resolves repo-relative source_doc paths; no PII, no network, no secrets; pure dev-tooling |
| T-19-SC | Tampering | npm/pnpm installs (supply chain) | mitigate | package-legitimacy gate before install (Task 1); pinned versions; manual npm verification recorded in RESEARCH (slopcheck targeted Go ecosystem — N/A for npm). If any package is flagged [ASSUMED]/[SUS]/[SLOP] at execution time, escalate to a blocking-human gate before install. |
</threat_model>

<verification>
- `cd web && pnpm install --frozen-lockfile` → exit 0; `git diff --exit-code go.mod go.sum` → no change (SITE-02).
- `cd web && pnpm typecheck` → exit 0 (vitest.config.mts triple-slash reference prevents tsc failure).
- `cd web && pnpm test` → exit 0; all five unit files pass.
- `cd web && pnpm lint` → exit 0 over the new test files (globals:false means no undeclared test symbols; add a point-of-use biome-ignore only if a specific line fails).
- accuracy.test.ts constant arrays are byte-identical to accuracy_spec.py (manual diff at port time).
</verification>

<success_criteria>
- Vitest is installed + configured; `pnpm test` runs the pre-build unit suite green.
- The five pre-build unit tests exist and pass (utils, reduced-motion, install-chip, metadata, accuracy).
- accuracy.test.ts faithfully ports accuracy_spec.py with verbatim constants (parity prerequisite for Wave-3 retirement).
- package.json has the five new scripts; the eight devDependencies are pinned and installed workspace-isolated with no Go-module change.
- `pnpm typecheck` and `pnpm lint` both pass with the new files present.
</success_criteria>

<output>
Create `.planning/phases/19-test-suite-ci/19-01-SUMMARY.md` when done.
</output>
