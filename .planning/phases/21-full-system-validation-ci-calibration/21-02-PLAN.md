---
phase: 21-full-system-validation-ci-calibration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/check/deny_render_golden_test.go
  - internal/check/testdata/deny/.gitkeep
  - internal/hooks/conformance_test.go
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "Every one of the 17 harness targets has installer-config conformance: keys written, idempotent re-install (1 entry), backup-on-overwrite for file-writers; printed-config + no-file-written for the 4 gateway targets; Cline !windows error on Windows"
    - "The deny renderer emits a byte-exact golden-file contract for all 15 HarnessIDs + the unknown-harness fail-closed default"
    - "The Hermes fail-open seam (exit 0 + action:block JSON) and the Kilo/Trae UNGUARDED cases (exit 2, no stdout JSON) are first-class golden rows"
    - "A -update flag regenerates the golden files"
  artifacts:
    - path: "internal/check/deny_render_golden_test.go"
      provides: "Table-driven golden-file deny conformance over all HarnessIDs + unknown, with -update"
      contains: "golden"
    - path: "internal/check/testdata/deny"
      provides: "Committed per-harness golden files (one .golden per deny case)"
    - path: "internal/hooks/conformance_test.go"
      provides: "TestHarnessConformance covering all 17 allTargets (config/idempotent/backup or printed-config or !windows-error)"
      contains: "allTargets"
  key_links:
    - from: "internal/check/deny_render_golden_test.go"
      to: "internal/check/testdata/deny/*.golden"
      via: "byte-exact compare of RenderDeny output"
      pattern: "ReadFile|WriteFile"
    - from: "internal/hooks/conformance_test.go"
      to: "internal/hooks allTargets"
      via: "per-target install assertion loop"
      pattern: "allTargets"
---

<objective>
Build the VAL-02 local 17-harness conformance suite: golden-file deny contracts + installer-config assertions for every target, with the honesty seams as first-class cases. The deny half converts the existing substring-asserting `TestRenderDeny` into byte-exact golden files (`testdata/deny/<harness>.golden`) with a `-update` flag; the installer half asserts every one of the 17 `allTargets` writes correct config (keys, idempotent, backup) — or prints config without writing a file (the 4 gateway targets) — or errors on Windows (Cline). Hermes fail-open (exit 0 + `action:"block"`) and Kilo/Trae UNGUARDED (exit 2, no stdout JSON) are explicit golden rows.

Purpose: A local, deterministic conformance gate (no live harness) proving a hijacked agent gets the exact per-harness block contract on every supported target (D-04). The deny contract is already ~95% built (`RenderDeny` + `TestRenderDeny` cover all cases via substrings); this plan upgrades to byte-exact golden + fills the installer half to all 17.
Output: golden-file deny test + `testdata/deny/` + a unified `TestHarnessConformance` installer test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-full-system-validation-ci-calibration/21-CONTEXT.md
@.planning/phases/21-full-system-validation-ci-calibration/21-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: golden-file deny conformance with -update (VAL-02 deny half)</name>
  <files>internal/check/deny_render_golden_test.go, internal/check/testdata/deny/.gitkeep</files>
  <read_first>
    - internal/check/deny_render.go (RenderDeny — all 15 HarnessIDs, families A-H, Hermes exit-0, unknown-harness fail-closed exit-2, DenyOutput{Stdout,Stderr,ExitCode})
    - internal/check/deny_render_test.go (the existing TestRenderDeny substring asserts — the source table to convert to golden, all cases already enumerated)
    - 21-RESEARCH.md §"Pattern 2: Golden-file deny conformance with -update" + §"17-Harness Conformance" deny-contract half (the families + honesty-seam rows)
  </read_first>
  <behavior>
    - Test 1: for each HarnessID (the 15 consts) + the unknown/empty harness, RenderDeny(h, blockDecision) marshalled to a stable canonical form ({ExitCode,Stdout,Stderr}) equals the committed testdata/deny/<harness>.golden byte-for-byte.
    - Test 2 (Hermes row): the Hermes golden encodes ExitCode 0 and Stdout containing action:"block" (fail-open seam).
    - Test 3 (Kilo/Trae rows): the Kilo and Trae goldens encode ExitCode 2 and nil/empty Stdout (UNGUARDED, no stdout JSON).
    - Test 4 (-update): running with -update regenerates the goldens; re-running without -update then passes.
  </behavior>
  <action>Create `internal/check/deny_render_golden_test.go` with a table over all 15 `HarnessID` consts plus an explicit unknown/empty-harness row. For each, call `RenderDeny(h, policy.Decision{Allow:false, Reason:"<fixed canary reason>"})`, serialize the `DenyOutput` to a stable canonical form (a deterministic struct→JSON or a fixed `exit=N\nstdout=...\nstderr=...` layout — choose one and keep it stable), and compare byte-exact to `internal/check/testdata/deny/<harness>.golden`. Add `var update = flag.Bool("update", false, "regenerate golden files")`; when set, `os.WriteFile` the canonical bytes; otherwise `os.ReadFile` + assert equality (RESEARCH Pattern 2). Use a FIXED reason string so the goldens are deterministic. Name the honesty-seam rows explicitly in the table (`hermes` → assert the golden's exit is 0 and stdout contains `"action":"block"`; `kilo`/`trae` → assert exit 2 + empty stdout) so a regression in the fail-open/UNGUARDED contract fails loudly. Generate the goldens once via `go test -run TestRenderDenyGolden -update ./internal/check/` during execution and COMMIT them. Add a `.gitkeep` so the empty testdata/deny dir is tracked before generation. Keep the existing `TestRenderDeny` substring test untouched (it remains a fast smoke; the golden test is the byte-exact gate).</action>
  <verify>
    <automated>go test -run TestRenderDenyGolden ./internal/check/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run TestRenderDenyGolden ./internal/check/` exits 0 with the committed goldens (no -update).
    - `internal/check/testdata/deny/` contains one `.golden` file per HarnessID (15) plus the unknown-harness case (16 files), each non-empty except where the contract is empty-stdout (Kilo/Trae/Windsurf/OpenCode stdout is empty but the file still encodes exit=2).
    - The `hermes.golden` encodes `exit=0` and its stdout contains `"action":"block"` (grep the golden file).
    - The `kilo.golden` and `trae.golden` encode `exit=2` with empty stdout.
    - The test file declares a `-update` flag (grep `flag.Bool` in deny_render_golden_test.go).
    - `go test ./internal/check/` (full package) stays green.
  </acceptance_criteria>
  <done>The deny contract for all 15 harnesses + unknown is byte-exact golden-asserted, with the Hermes fail-open and Kilo/Trae UNGUARDED seams as explicit rows and a -update regeneration path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 17-target installer-config conformance (VAL-02 installer half)</name>
  <files>internal/hooks/conformance_test.go</files>
  <read_first>
    - internal/hooks/hooks.go (allTargets [17], gatewayTargets [continue/openclaw/kilo/trae], fileTargets [10], InstallTo, backupSettings)
    - internal/hooks/hooks_test.go (the established installer-assert pattern: readJSON / globFiles / backup-created / idempotent-1-entry / theme-preserved — TestInstallClaudeCode is the canonical shape; TestInstallGatewayTarget + TestInstallGatewayTargetKiloTraeUNGUARDED are the gateway-target analogs)
    - internal/hooks/cline_test.go + cline_windows.go (the !windows installer + Windows stub-error contract)
    - 21-RESEARCH.md §"17-Harness Conformance" installer-config half (the two-axis split: installer = 17 targets via allTargets; deny = 15 HarnessIDs)
  </read_first>
  <behavior>
    - Test 1 (file-writers): for each of the 13 file-writing targets (the 10 fileTargets + hermes + cline-non-windows + opencode), installing into a fixture writes the expected key, is idempotent (re-install yields one entry), creates a backup-on-overwrite, and preserves unrelated keys.
    - Test 2 (gateway targets): for each of the 4 gatewayTargets (continue/openclaw/kilo/trae), InstallTo prints config to the writer and writes NO settings file; Kilo/Trae output asserts the UNGUARDED honesty text.
    - Test 3 (Cline on Windows): on GOOS=windows, installing cline returns the documented "macOS/Linux only" error (build-tag-correct, asserted via the windows stub or a runtime.GOOS skip note).
  </behavior>
  <action>Create `internal/hooks/conformance_test.go` with `TestHarnessConformance` that iterates `allTargets` (the authoritative 17-target roster) and routes each to the correct assertion: file-writers get the four-assertion shape from `TestInstallClaudeCode` (key present, idempotent single entry, `*.beekeeper-backup-*` created on overwrite, unrelated keys preserved) — reuse the `copyFixture`/`readJSON`/`globFiles` helpers already in hooks_test.go (same package); gateway targets (`gatewayTargets` map) assert `InstallTo` writes to the `io.Writer` and creates NO config file (capture the buffer, assert non-empty printed guide, assert no settings file appears in the temp dir); Kilo/Trae additionally assert the UNGUARDED honesty substring (extend the existing `TestInstallGatewayTargetKiloTraeUNGUARDED` contract). For Cline, assert the `!windows` installer succeeds and the Windows path returns the explicit error — gate on `runtime.GOOS` so the test is correct on both (the windows file is `cline_windows.go`'s stub error). The goal is uniform coverage: every one of the 17 `allTargets` is asserted exactly once, with no target silently skipped. Do NOT modify any installer production code (no surfaced defect — D-03); if a target lacks a fixture, add the fixture under `internal/hooks/testdata/` only.</action>
  <verify>
    <automated>go test -run TestHarnessConformance ./internal/hooks/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run TestHarnessConformance ./internal/hooks/` exits 0 on Windows.
    - The test iterates over `allTargets` (grep `allTargets` in conformance_test.go) so all 17 are covered; a subtest exists per target (e.g. `t.Run(target, ...)`).
    - File-writer subtests assert all four contracts (key / idempotent-1-entry / backup-created / preserve).
    - Gateway-target subtests (continue/openclaw/kilo/trae) assert printed config + NO settings file written; kilo/trae assert the UNGUARDED text.
    - The Cline branch handles GOOS=windows (error) vs non-windows (install) correctly — `GOOS=linux go vet ./internal/hooks/` and native `go vet ./internal/hooks/` both clean.
    - No installer production `.go` file is modified (git diff shows only `_test.go` + optional testdata fixtures).
  </acceptance_criteria>
  <done>All 17 allTargets have uniform installer-config conformance: file-writers (config/idempotent/backup/preserve), gateway targets (printed config, no file), Cline (!windows error), with Kilo/Trae UNGUARDED honesty asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hijacked agent → harness hook | A compromised agent issues a blocked tool call; the deny renderer's per-harness contract is the boundary that must produce the correct block |
| installer → harness config file | Re-running install must not corrupt, duplicate, or silently clobber unrelated config — the backup + idempotency boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Repudiation/EoP | internal/check RenderDeny (Hermes/Windsurf/Kilo/Trae fail-open seams) | mitigate | Byte-exact golden files assert the exact exit code + JSON per harness; Hermes exit-0+action:block and Kilo/Trae exit-2+no-stdout are explicit golden rows so a fail-open regression fails the gate (VAL-02) |
| T-21-03 | Tampering | hooks installer (idempotency / backup) | mitigate | TestHarnessConformance asserts backup-on-overwrite + idempotent single-entry + preserve-unrelated-keys for every file-writer; gateway targets assert no-file-write; Cline asserts the Windows error |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed (RESEARCH: N/A); no install surface |
</threat_model>

<verification>
- `go test -run TestRenderDenyGolden ./internal/check/` green with committed goldens.
- `go test -run TestHarnessConformance ./internal/hooks/` green covering all 17 allTargets.
- `go test ./internal/check/ ./internal/hooks/` green (no regression to existing tests).
- `go vet ./internal/hooks/` + `GOOS=linux go vet ./internal/hooks/` clean (Cline build-tag branches).
</verification>

<success_criteria>
- A local, deterministic 17-harness conformance suite is green with no live harness.
- The deny contract is byte-exact golden-asserted for all 15 HarnessIDs + unknown fail-closed.
- The Hermes fail-open and Kilo/Trae UNGUARDED honesty seams are first-class, regression-protected rows.
- Every one of the 17 installer targets has uniform config/idempotent/backup (or printed-config / !windows-error) conformance.
</success_criteria>

<output>
Create `.planning/phases/21-full-system-validation-ci-calibration/21-02-SUMMARY.md` when done.
</output>
