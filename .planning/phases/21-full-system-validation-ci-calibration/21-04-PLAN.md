---
phase: 21-full-system-validation-ci-calibration
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - internal/check/e2e_test.go
  - docs/validation-register.md
  - docs/validation-posture.md
  - README.md
autonomous: false
requirements: [VAL-05, VAL-06, VAL-07]
must_haves:
  truths:
    - "A Claude Code --hook claude-code e2e canary read of ~/.ssh + ~/.aws returns exit 2 + Family-A deny JSON + audit decision:block"
    - "docs/validation-register.md enumerates all 16 non-Claude-Code harness live-block procedures + the gated-22M-model LlamaFirewall e2e, each with steps/expected/sign-off"
    - "README harness count is corrected to 17 total / 16 non-Claude-Code (matrix is the source of truth, already 17)"
    - "The Tier A/B/C validation posture + the no-test allowlist reason-code taxonomy are documented so the coverage claim is auditable"
  artifacts:
    - path: "internal/check/e2e_test.go"
      provides: "New --hook claude-code exit-2 canary sub-case alongside the existing exit-1 default-mode case"
      contains: "--hook"
    - path: "docs/validation-register.md"
      provides: "16-harness + gated-model live-block register with sign-off fields"
      contains: "sign-off"
    - path: "docs/validation-posture.md"
      provides: "Tier A/B/C model + reason-code taxonomy documentation"
      contains: "Tier A"
  key_links:
    - from: "internal/check/e2e_test.go"
      to: "beekeeper check --hook claude-code"
      via: "real-binary exit-2 + Family-A deny JSON assertion"
      pattern: "permissionDecision.*deny"
    - from: "docs/validation-register.md"
      to: "the 17-harness roster + gated-model e2e"
      via: "per-harness procedure rows"
      pattern: "Expected|sign-off"
---

<objective>
Close the human-facing validation tier: the VAL-05 Claude Code live e2e (the documented true-block reference), the VAL-06 manual register for everything irreducibly manual, and the VAL-07 honesty docs. The e2e adds the missing `--hook claude-code` exit-2 canary case (the existing e2e only proves the exit-1 default-mode block — Phase 10's hook contract is exit 2). The register documents the 16 non-Claude-Code harness live-block procedures + the gated-22M-model LlamaFirewall e2e with sign-off fields. The honesty docs correct the README count (15→17 / 14→16) and document the Tier A/B/C model + the allowlist reason-code taxonomy so the coverage claim is auditable.

Purpose: Tier C of the validation model (D-01/D-07) — the honest artifact that says "here is exactly what we verified by hand and how to reproduce it." VAL-07 (D-08) makes the coverage claim auditable rather than taken on faith. The register's gated-model sign-off may stay PENDING past phase close (the human HF-license gate — D-07/deferred), captured honestly.
Output: e2e sub-case + `docs/validation-register.md` + `docs/validation-posture.md` + README edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-full-system-validation-ci-calibration/21-CONTEXT.md
@.planning/phases/21-full-system-validation-ci-calibration/21-RESEARCH.md
@docs/harness-support-matrix.md
@docs/THREAT-MODEL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claude Code --hook exit-2 canary e2e (VAL-05)</name>
  <files>internal/check/e2e_test.go</files>
  <read_first>
    - internal/check/e2e_test.go (the existing TestE2ELiveBinary: builds the real binary, newHome/seedCatalog/runCase helpers, the SPATH exit-1 default-mode case — the exit-2 --hook case is the gap)
    - internal/check/deny_render.go (exitHookBlock = 2; Family-A hookSpecificOutput {permissionDecision:"deny"} for claude-code)
    - 21-RESEARCH.md §"VAL-05 — Claude Code Live e2e" (the exact new case: drive `beekeeper check --hook claude-code`, assert exit 2 + stdout permissionDecision:deny + audit decision:block) + Pitfall 2 (exit-1-default vs exit-2-hook)
    - cmd/beekeeper/main.go (the --hook flag wiring — confirm `beekeeper check --hook claude-code` reads stdin the same way)
  </read_first>
  <behavior>
    - Test (new sub-case under TestE2ELiveBinary or a sibling //go:build e2e test): driving the real binary `beekeeper check --hook claude-code` with a canary `~/.ssh/id_rsa` read AND a `~/.aws/credentials` read returns exit code 2 (the hook deny contract, NOT exit 1), stdout contains `"permissionDecision":"deny"` (Family A), and the audit record decision is "block".
    - The existing exit-1 default-mode SPATH case is kept (it validates the non-hook path).
  </behavior>
  <action>Extend `internal/check/e2e_test.go` (`//go:build e2e`) with a new sub-case `t.Run("SPATH_hook_claude_code_exit2", ...)` (or a sibling test reusing the same `newHome`/`seedCatalog`/`runCase` helpers — extend `runCase` to accept extra args so it can invoke `beekeeper check --hook claude-code`). Drive two canary reads — `~/.ssh/id_rsa` and `~/.aws/credentials` — and assert: exit code is 2 (NOT 1 — Pitfall 2: the exit-1 case is the default-mode block; the hook contract is exit 2 via `--hook`), stdout contains `"permissionDecision":"deny"` (Family A nested hookSpecificOutput from RenderDeny for claude-code), and the audit `decision` is `"block"`. Keep the existing exit-1 default-mode SPATH case unchanged. This makes the documented true-block reference exit-2-accurate and matches the Phase-10 live proof (canary `~/.ssh` + `~/.aws` DENIED). Do NOT modify production code — this is a test-only gap (D-03).</action>
  <verify>
    <automated>go test -tags e2e -run TestE2ELiveBinary ./internal/check/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags e2e -run TestE2ELiveBinary ./internal/check/` exits 0 on the Windows dev box (Claude Code e2e is the one locally-runnable harness — D-07).
    - The new sub-case asserts exit code == 2 (grep `!= 2` or `want 2` in the new sub-case) AND stdout contains `permissionDecision`/`deny` AND audit decision "block".
    - The existing exit-1 default-mode SPATH sub-case is still present and green (grep `want 1` still exists).
    - No production `.go` file is modified (git diff shows only internal/check/e2e_test.go).
    - The file retains `//go:build e2e` on line 1.
  </acceptance_criteria>
  <done>The --hook claude-code exit-2 canary block is proven end-to-end against the real binary; the documented true-block reference is exit-2-accurate and the exit-1 default-mode case is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: validation-register.md + validation-posture.md + README count fix (VAL-06 authoring, VAL-07)</name>
  <files>docs/validation-register.md, docs/validation-posture.md, README.md</files>
  <read_first>
    - docs/harness-support-matrix.md (the authoritative 17-target tier doc — already 17; the source of truth README and the register must match)
    - README.md lines 30-72 (the stale "15 agent harnesses" [line 32] + "other 14 harnesses" [line 66]; the Tier-1 list of 10; Tier-2 Hermes/Cline/OpenCode; Tier-3 Kilo/Trae — note Continue + OpenClaw are in allTargets [17] but absent from this summary)
    - internal/hooks/hooks.go allTargets (the 17-target roster: claude-code, cursor, codex, augment, codebuddy, qwen, copilot, antigravity, gemini, windsurf, hermes, cline, continue, opencode, openclaw, kilo, trae)
    - internal/check/deny_render.go (the per-harness deny families A-H — source the Expected column from these)
    - 21-RESEARCH.md §"VAL-06 — Register Schema" (the exact row template + the gated-model entry) + §"VAL-07 — Honesty Docs" (README is stale; matrix is correct; document Tier A/B/C + allowlist taxonomy)
    - coverage-allowlist.txt + internal/coveragegate/allowlist.go (from Plan 01 — the reason-code taxonomy to document: generated-bpf|platform-stub|type-only|exec-seam-stub|thin-delegator|gen-directive)
    - .planning/phases/20-runtime-hardening/20-02-* (the gated-22M-model LlamaFirewall e2e + the HF-license human gate — the 17th register entry, sign-off may stay PENDING)
  </read_first>
  <action>Create `docs/validation-register.md` with one section per non-Claude-Code harness — the 16 from `allTargets` minus claude-code (cursor, codex, augment, codebuddy, qwen, copilot, antigravity, gemini, windsurf, hermes, cline, continue, opencode, openclaw, kilo, trae) — each using the RESEARCH §"VAL-06 Register Schema" row template (Prereq / Install `beekeeper hooks install --target <name>` / Drive the canary `~/.aws/credentials` read / Expected exit code + deny family / Result checkboxes / Verified-by-date sign-off). Source each harness's Expected deny family from `deny_render.go` (Hermes: exit 0 + `{"action":"block"}`; Kilo/Trae: native UNGUARDED, only MCP-routed calls block; Windsurf/OpenCode: exit 2, no stdout JSON). Add the 17th entry — the gated-22M-model LlamaFirewall e2e — with its prereq (accept Llama-Prompt-Guard-2 license + `huggingface-cli login` + `beekeeper llamafirewall install` + `BEEKEEPER_LLMF_E2E=1`), run command, expected (benign→allow / injection / unsafe-code / crash→fail-closed), and a sign-off that honestly reads PENDING on the human HF-license gate (D-07/deferred). Create `docs/validation-posture.md` documenting the Tier A/B/C validation model (Tier A = locally testable, 100% gate-enforced; Tier B = platform/kernel/build-tag CI matrix; Tier C = irreducible manual register) AND the no-test allowlist mechanism with the closed reason-code taxonomy (`generated-bpf|platform-stub|type-only|exec-seam-stub|thin-delegator|gen-directive`) — keep consistent with THREAT-MODEL.md §8 honesty spine so an external reader can audit the coverage claim (D-08). Edit `README.md`: change "15 agent harnesses" → "17 agent harnesses" and "other 14 harnesses" → "other 16 harnesses"; reconcile the Tier summary table to include Continue + OpenClaw under Tier 3 (gateway-only) so the visible counts sum to 17 (matrix is authoritative). Link the new posture doc from the README verification-scope section.</action>
  <verify>
    <automated>python -c "r=open('README.md',encoding='utf-8').read(); assert '17 agent harnesses' in r and 'other 16 harnesses' in r and '15 agent harnesses' not in r; reg=open('docs/validation-register.md',encoding='utf-8').read().lower(); assert reg.count('expected')>=16 and 'llamafirewall' in reg and 'pending' in reg; pos=open('docs/validation-posture.md',encoding='utf-8').read(); assert 'Tier A' in pos and 'generated-bpf' in pos; print('VAL-06/07 docs OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The assertion command prints `VAL-06/07 docs OK`.
    - README contains "17 agent harnesses" and "other 16 harnesses"; the stale "15 agent harnesses" string is gone; the Tier summary now includes Continue + OpenClaw (17 total).
    - docs/validation-register.md has ≥16 harness procedure rows (each with an Expected + sign-off field) plus the gated-22M-model LlamaFirewall entry whose sign-off honestly reads PENDING.
    - docs/validation-posture.md documents Tier A/B/C AND the full six-code allowlist taxonomy.
    - No Go production `.go` file is modified by this task (docs + README only).
  </acceptance_criteria>
  <done>The register enumerates all 16 non-Claude-Code harnesses + the gated-model e2e with sign-off fields; the README count is corrected to 17/16; the Tier A/B/C posture + allowlist taxonomy are documented and auditable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer register sign-off + phase honesty review (VAL-06)</name>
  <files>docs/validation-register.md, docs/validation-posture.md, README.md</files>
  <what-built>
    docs/validation-register.md (16 non-Claude-Code harness live-block procedures + the gated-22M-model LlamaFirewall e2e, each with steps/expected/sign-off), docs/validation-posture.md (Tier A/B/C model + allowlist taxonomy), the corrected README harness counts, and the Claude Code --hook exit-2 e2e (Task 1, green locally).
  </what-built>
  <how-to-verify>
    1. Open docs/validation-register.md and confirm each of the 16 non-Claude-Code harness rows has accurate Prereq / Install / Drive / Expected (deny family matches deny_render.go) / sign-off fields, consistent with docs/harness-support-matrix.md.
    2. Confirm the gated-22M-model LlamaFirewall entry's sign-off honestly reads PENDING (the HF-license human gate — Claude cannot accept the Llama-Prompt-Guard-2 license; sign-off may remain pending past phase close per D-07).
    3. Confirm docs/validation-posture.md's Tier A/B/C description + the reason-code taxonomy match the actual coverage-allowlist.txt and read honestly against THREAT-MODEL.md §8.
    4. Confirm the README now reads "17 agent harnesses" / "other 16 harnesses" and the Tier table sums to 17.
    5. Optionally sign off the Claude Code register row now (it is the one live-verified harness — the Task-1 e2e is its automated proof); the other 16 + the gated model stay UNVERIFIED/PENDING until you (or a future session) install each real client and drive the canary read.
  </how-to-verify>
  <action>Pause for the maintainer to review the register/posture/README per the how-to-verify steps. The maintainer may optionally sign off the Claude Code row (its Task-1 e2e is the automated proof); the other 16 harness rows + the gated-22M-model entry are left UNVERIFIED/PENDING by design until each real client is installed and driven (Tier C is irreducibly manual — D-07). Do NOT auto-approve: this is a never-auto-approvable honesty checkpoint regardless of workflow.auto_advance.</action>
  <verify>
    <human-check>Maintainer confirms the register's 16 harness rows + the gated-model entry are accurate and honest, the posture doc matches coverage-allowlist.txt + THREAT-MODEL.md §8, and the README reads 17/16; replies "approved".</human-check>
  </verify>
  <resume-signal>Type "approved" to accept the register/posture/README as the honest Tier-C artifact (with the 16 harness rows + gated model left unsigned/PENDING by design), or describe corrections.</resume-signal>
  <done>Maintainer has reviewed and approved the Tier-C register/posture/README as honest, with the 16 harness rows + gated model left unsigned/PENDING by design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hijacked agent → Claude Code hook | The canary credential read is the adversary action; the --hook exit-2 deny is the boundary proven live |
| public coverage claim → external auditor | An external reader must be able to audit "fully validated" — the posture doc + register are the boundary between claim and proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Malicious code (V10) | beekeeper check --hook claude-code | mitigate | The live e2e proves a canary ~/.ssh + ~/.aws read is DENIED end-to-end (exit 2 + Family-A deny JSON + audit block) — the documented true-block reference (VAL-05) |
| T-21-08 | Repudiation | the public "fully validated" claim | mitigate | docs/validation-posture.md documents Tier A/B/C + the reason-coded allowlist; docs/validation-register.md captures exactly what is hand-verified vs PENDING (gated model) so the claim is auditable, not taken on faith (VAL-06/07, D-08) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed; docs + a test-only e2e change (RESEARCH: N/A) |
</threat_model>

<verification>
- `go test -tags e2e -run TestE2ELiveBinary ./internal/check/` green on Windows (exit-2 --hook case + the preserved exit-1 case).
- The VAL-06/07 docs assertion prints `VAL-06/07 docs OK`.
- README has no stale "15 agent harnesses" string; register has ≥16 harness rows + the PENDING gated-model entry; posture documents Tier A/B/C + the six-code taxonomy.
- Maintainer approves the register/posture/README as the honest Tier-C artifact (Task 3 blocking checkpoint).
</verification>

<success_criteria>
- The Claude Code live e2e denies a canary credential read end-to-end at the exit-2 hook contract (VAL-05).
- docs/validation-register.md is the honest manual register for the 16 non-Claude-Code harnesses + the gated-model e2e, with sign-off fields (VAL-06).
- The README count is corrected to 17/16 and the validation posture (Tier A/B/C + allowlist taxonomy) is documented and auditable (VAL-07).
- The gated-model sign-off honestly reads PENDING on the human HF-license gate (D-07/deferred).
</success_criteria>

<output>
Create `.planning/phases/21-full-system-validation-ci-calibration/21-04-SUMMARY.md` when done.
</output>
