---
phase: 17-seo-static-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/metadata.ts
  - web/tests/seo_spec.py
autonomous: true
requirements: [SEO-01]
must_haves:
  truths:
    - "A single BASE_URL constant exists that every canonical/OG/sitemap/robots URL derives from"
    - "seo_spec.py asserts SC-1 (title/description/canonical), SC-2 (og:image/twitter:card + OG png), SC-3 (sitemap.xml >=13 locs + robots.txt) over out/"
    - "seo_spec.py runs red (exit 1) until the implementation plans land, then green"
  artifacts:
    - path: "web/lib/metadata.ts"
      provides: "BASE_URL + SITE_NAME single-source-of-truth constants"
      contains: "https://beekeeper.vercel.app"
    - path: "web/tests/seo_spec.py"
      provides: "SC-1..3 file-walk validation harness (stdlib only, no Playwright)"
      contains: "RESULT: ALL ASSERTIONS PASSED"
  key_links:
    - from: "web/tests/seo_spec.py"
      to: "web/out/sitemap.xml + web/out/robots.txt + out/**/index.html"
      via: "pathlib.rglob file-walk after pnpm build"
      pattern: "rglob.*index\\.html"
---

<objective>
Establish the Phase 17 foundation: the single-source-of-truth base-URL constant
(`web/lib/metadata.ts`) that every downstream metadata/sitemap/robots task derives
its URLs from, and the validation harness (`web/tests/seo_spec.py`) that asserts
SC-1..3 over the built `out/` tree.

Purpose: Per D-01, all canonical/OG/`og:url`/sitemap/robots URLs must derive from
ONE constant so a future custom-domain swap is a one-line change. Per the VALIDATION
strategy, the spec is Wave 0 — it must exist (and run red) before the implementation
plans land so every later task has an `<automated>` gate to run against.
Output: `web/lib/metadata.ts` (BASE_URL/SITE_NAME consts), `web/tests/seo_spec.py`
(SC-1..3 file-walk harness, stdlib only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

EXECUTION NOTE (project-specific): All web/ work runs INLINE on `main`
(use_worktrees=false). Spawned executor subagents here have a Bash with NO
node/git/pnpm on PATH — they CANNOT run `pnpm build` or commit. The orchestrator
(inline) runs `cd web && pnpm build` and the git commit. Keep verify commands
runnable from `web/` on Windows. `python tests/seo_spec.py` runs against `out/`
and assumes a prior `pnpm build`. In THIS plan the spec is expected to exit 1 (red)
because the metadata/sitemap/robots are not implemented yet — that is the correct
Wave 0 state. Do NOT treat the red spec as a failure of this plan.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-seo-static-assets/17-CONTEXT.md
@.planning/phases/17-seo-static-assets/17-RESEARCH.md
@.planning/phases/17-seo-static-assets/17-PATTERNS.md
@.planning/phases/17-seo-static-assets/17-VALIDATION.md
</context>

<artifacts_this_phase_produces>
The full Phase-17 surface (created across plans 17-01/02/03):
- `web/lib/metadata.ts` — exports `BASE_URL = "https://beekeeper.vercel.app"` + `SITE_NAME = "Beekeeper"` (THIS plan)
- `web/tests/seo_spec.py` — SC-1..3 file-walk harness (THIS plan)
- `web/app/opengraph-image.png` — committed 1200×630 PNG (plan 17-02)
- `web/app/opengraph-image.alt.txt` — OG alt text (plan 17-02)
- `web/app/layout.tsx` (MOD) — metadataBase + title template + shared openGraph/twitter (plan 17-02)
- `web/app/page.tsx` (MOD) — home description + canonical "/" + og url (plan 17-02)
- `web/app/docs/[[...slug]]/page.tsx` (MOD) — alternates.canonical + openGraph.url (plan 17-02)
- `web/app/changelog/[[...slug]]/page.tsx` (MOD) — alternates.canonical + openGraph.url (plan 17-02)
- `web/app/sitemap.ts` — MetadataRoute.Sitemap + force-static (plan 17-03)
- `web/app/robots.ts` — MetadataRoute.Robots + force-static (plan 17-03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the BASE_URL single-source-of-truth constant (web/lib/metadata.ts)</name>
  <files>web/lib/metadata.ts</files>
  <read_first>
    - web/lib/source.ts (the `@/lib/` import convention this file must follow — minimal, side-effect-free, one concern per file)
    - web/lib/changelog-source.ts (same convention)
    - 17-PATTERNS.md "web/lib/metadata.ts (CREATE)" section — full target shape + the no-analog rationale
    - 17-CONTEXT.md D-01 (the locked host) + D-04 (honesty: this is an INTENDED host, site not deployed)
  </read_first>
  <action>
    Create `web/lib/metadata.ts` as a constants-only module (no imports, no side
    effects). Export `BASE_URL` set to the exact string `https://beekeeper.vercel.app`
    (no trailing slash on the origin) per D-01 — this is the locked Vercel project URL
    and the ONLY place the host literal appears in the codebase. Also export
    `SITE_NAME` set to `Beekeeper` so layout.tsx title template / openGraph.siteName
    derive from one place. Add a top-of-file comment naming the consumers
    (layout.tsx metadataBase, sitemap.ts, robots.ts, the docs/changelog
    generateMetadata canonical builders) and stating that a custom-domain swap is a
    one-line change here. Per D-04 do NOT add any "live"/"available now" language —
    it is a constant, not a claim. The file is imported everywhere via the `@/lib/metadata`
    alias (same pattern as `@/lib/source`).
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');const s=fs.readFileSync('lib/metadata.ts','utf8');if(!/export const BASE_URL\s*=\s*[\"']https:\/\/beekeeper\.vercel\.app[\"']/.test(s)){console.error('BASE_URL missing/incorrect');process.exit(1)};if(!/export const SITE_NAME\s*=\s*[\"']Beekeeper[\"']/.test(s)){console.error('SITE_NAME missing');process.exit(1)};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/lib/metadata.ts` exists and exports `BASE_URL === "https://beekeeper.vercel.app"` (verbatim, no trailing slash on the origin) and `SITE_NAME === "Beekeeper"`.
    - The node check above prints `OK` and exits 0.
    - File contains no imports and no runtime side effects (constants only).
    - The host literal `beekeeper.vercel.app` appears in this file and (after later plans) is NOT hardcoded again anywhere else — every other URL derives from BASE_URL.
  </acceptance_criteria>
  <done>BASE_URL + SITE_NAME constants exist in web/lib/metadata.ts; node assertion prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Create the SC-1..3 file-walk validation harness (web/tests/seo_spec.py)</name>
  <files>web/tests/seo_spec.py</files>
  <read_first>
    - web/tests/gfx_spec.py lines 1-95 (the EXACT scaffold to copy: SCRIPT_DIR/WEB_DIR/OUT_DIR path resolution, the `if not OUT_DIR.is_dir(): sys.exit(1)` guard, the `failures=[]` + `fail()`/`ok()` helpers, the `if __name__ == "__main__"` exit contract). DROP the Playwright import + http.server + SilentHandler/start_server — this spec is pure file-walk, no browser.
    - 17-PATTERNS.md "web/tests/seo_spec.py (CREATE)" — the full target file is spelled out there; use it verbatim as the reference (it is the authoritative shape).
    - 17-VALIDATION.md "Assertion detail" — the precise SC-1/SC-2/SC-3 assertions this harness must encode.
    - 17-RESEARCH.md "seo_spec.py Design" + the known 13-URL route enumeration (8 docs + home + /changelog/ + 3 versions).
  </read_first>
  <action>
    Create `web/tests/seo_spec.py` — stdlib only (`os`, `pathlib`, `re`, `sys`); NO
    Playwright, NO http.server. Resolve `OUT_DIR = <web>/out` via the gfx_spec.py
    SCRIPT_DIR/WEB_DIR pattern and guard with `sys.exit(1)` if `out/` is absent.
    Define `BASE_URL = "https://beekeeper.vercel.app"` as a module-level literal in the
    spec (the Python test cannot import the TS constant; mirror the value and add a
    comment that it MUST stay in sync with web/lib/metadata.ts). Provide a
    `content_html_files()` helper that returns every `out/**/index.html` EXCLUDING any
    path containing `404` or `_not-found` (error pages are not public content).
    Implement three check groups using the shared `fail()`/`ok()` helpers:
    - SC-1: for each content HTML file assert a non-empty `<title>...</title>`, a
      `<meta name="description" content="...">` (non-empty), and a
      `<link rel="canonical" href="https://beekeeper.vercel.app/...` whose href is
      absolute, uses the locked host, and starts with BASE_URL + "/".
    - SC-2: assert `out/opengraph-image.png` exists; for each content HTML file assert
      `<meta property="og:image"` and `<meta name="twitter:card"` are present.
    - SC-3: assert `out/sitemap.xml` exists, contains `BASE_URL + "/"`, has at least 13
      `<loc>` entries, and that EVERY `<loc>` value starts with BASE_URL (no
      private/internal/relative URL leaks — supports T-17-02). Assert `out/robots.txt`
      exists, contains `Allow: /`, and references `sitemap.xml`.
    Use the gfx_spec.py exit contract: print a header, run all three groups, then exit 1
    listing failures or exit 0 with `RESULT: ALL ASSERTIONS PASSED`. Encode the
    docstring REQUIREMENT COVERAGE block (SC-1..3) verbatim from 17-PATTERNS.md.
  </action>
  <verify>
    <automated>cd web && python tests/seo_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/seo_spec.py` exists, imports only stdlib (`os`/`pathlib`/`re`/`sys`), and contains NO `playwright` / `http.server` references (grep returns nothing).
    - Running `python tests/seo_spec.py` against the CURRENT `out/` (pre-implementation) prints FAIL lines for the missing canonical/og/sitemap/robots and exits 1 — this RED state is correct and expected for Wave 0 (no implementation has landed yet).
    - The script's `<loc>`-count gate uses `>= 13` and the per-`<loc>` host check rejects any URL not starting with `https://beekeeper.vercel.app`.
    - `content_html_files()` excludes `404` and `_not-found` paths.
  </acceptance_criteria>
  <done>seo_spec.py exists (stdlib-only file-walk) and runs red against the current out/ — exit 1 with FAIL lines naming the missing SC-1/2/3 artifacts. It will go green once plans 17-02 and 17-03 land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public web (out/) | Anything written to `out/` is world-readable once SITE-03 deploys. sitemap.xml/robots.txt enumerate routes publicly. |
| build-time constant → emitted URLs | `BASE_URL` is the only host source; it flows into every canonical/OG/sitemap URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering / Spoofing | `BASE_URL` constant in web/lib/metadata.ts | accept | It is a hardcoded compile-time string literal, never user input — no open-redirect / host-injection surface. Pinned to the locked Vercel host per D-01. |
| T-17-02 | Information Disclosure | seo_spec.py sitemap host-prefix assertion | mitigate | The harness asserts EVERY sitemap `<loc>` starts with BASE_URL — fails the build-gate if any private/internal/relative path leaks into the public sitemap (defense for the asset plan in 17-03). |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (Python stdlib only; no new npm deps — see 17-RESEARCH Package Legitimacy Audit). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `web/lib/metadata.ts` node assertion prints OK (Task 1).
- `cd web && python tests/seo_spec.py` runs (stdlib only) and exits 1 RED against the current out/ (Task 2) — the harness is wired and ready to go green once 17-02/17-03 land.
- No new npm packages added; no `pnpm install` required by this plan.
</verification>

<success_criteria>
- BASE_URL/SITE_NAME single-source-of-truth constants exist (`web/lib/metadata.ts`).
- seo_spec.py exists, is stdlib-only, encodes SC-1..3, and runs red (correct Wave 0 state).
- The harness's per-`<loc>` host-prefix check is present (T-17-02 mitigation).
</success_criteria>

<output>
Create `.planning/phases/17-seo-static-assets/17-01-SUMMARY.md` when done.
Record: the exact BASE_URL/SITE_NAME values, the seo_spec.py red-output snapshot
(which SC groups failed pre-implementation), and confirmation that no npm deps were added.
</output>
