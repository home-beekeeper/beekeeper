---
phase: 02-windows-root-resolver
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - ../pollen/VERSION
  - ../pollen/CHANGES.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "../pollen/VERSION reads 0.1.1-pollen.2"
    - "CHANGES.md has a v0.1.1-pollen.2 section recording the Windows root resolver, parity test, and the 6 flipped skips, referencing the ACTUAL files (cmd/pollen/roots_windows.go), not the PRD-draft internal/resolver/ name"
    - "The full Pollen CI matrix is green on ubuntu/macos/windows AND the differential job still green on Linux+macOS before tagging"
    - "Tag v0.1.1-pollen.2 is pushed and the release workflow produces a cosign signature + CycloneDX SBOM (reusing the Phase-1 stack, no new infra)"
  artifacts:
    - path: "../pollen/VERSION"
      provides: "single-line version 0.1.1-pollen.2"
      contains: "0.1.1-pollen.2"
    - path: "../pollen/CHANGES.md"
      provides: "v0.1.1-pollen.2 delta section"
      contains: "v0.1.1-pollen.2"
  key_links:
    - from: "../pollen/VERSION"
      to: ".goreleaser.yaml ldflags -X main.Version"
      via: "release workflow injects Version at build"
      pattern: "0.1.1-pollen.2"
---

<objective>
Cut the signed `v0.1.1-pollen.2` release closing Phase 2 (Success Criterion 4 / release gate). Bump `VERSION`, append the `CHANGES.md` delta, confirm the full 3-OS CI matrix is green (including the new Windows root-resolver and parity tests, and the still-green Linux+macOS differential), then tag and verify the Sigstore signature + SBOM via the existing Phase-1 release stack.

Purpose: Phase 2's four Success Criteria require a tagged, signed `v0.1.1-pollen.2` with Windows CI no longer skipping root-resolver tests. The signing/SBOM/SLSA infrastructure already shipped in Phase 1 (FORK-03/SDEF-02) — this plan reuses it unchanged; the only repo edits are VERSION and CHANGES.md. The tag push triggers `release.yml` (keyless OIDC cosign + CycloneDX), which is why this plan is non-autonomous: the human confirms CI is green before tagging and verifies the signature after.

Output: edited `../pollen/VERSION` + `../pollen/CHANGES.md` (committed), the `v0.1.1-pollen.2` git tag, and a verified signed GitHub release.

CROSS-REPO: All edits and the tag are in `../pollen`. This task uses explicit `git -C ../pollen` operations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-windows-root-resolver/02-CONTEXT.md
@.planning/phases/02-windows-root-resolver/02-RESEARCH.md
@.planning/phases/02-windows-root-resolver/02-01-SUMMARY.md
@.planning/phases/02-windows-root-resolver/02-02-SUMMARY.md
@.planning/phases/02-windows-root-resolver/02-03-SUMMARY.md

<interfaces>
<!-- Release stack facts extracted from ../pollen (Phase 1) — reuse unchanged. -->

../pollen/VERSION                — single line, currently `0.1.1-pollen.1`
../pollen/.goreleaser.yaml       — injects version via ldflags `-X main.Version={{.Version}}`; builds linux/darwin/windows; CycloneDX SBOM via syft (unchanged)
../pollen/.github/workflows/release.yml — cosign v3 keyless (GitHub OIDC) signs checksums.txt; slsa-github-generator@v2.1.0; triggers on tag push
../pollen/.github/workflows/ci.yml — test matrix [ubuntu-latest, macos-latest, windows-latest] runs `go test -v -race ./...` (CGO_ENABLED=1); differential job [ubuntu, macos] runs `go test ./cmd/pollen/ -run '^TestDifferential$'`
Cosign verify identity regexp (Phase-1 established, canonical casing): ^https://github\.com/home-beekeeper/pollen/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump VERSION and append the CHANGES.md v0.1.1-pollen.2 section</name>
  <files>../pollen/VERSION, ../pollen/CHANGES.md</files>
  <read_first>
    - ../pollen/VERSION — REQUIRED. Confirm it is the single line `0.1.1-pollen.1`.
    - ../pollen/CHANGES.md — REQUIRED. The existing `## v0.1.1-pollen.1` section format (Added/Modified/Renamed/Fixed headings, the pinned-SHA preamble) to mirror for the new section.
    - .planning/phases/02-windows-root-resolver/02-RESEARCH.md — REQUIRED. Q6 "CHANGES.md Entry" (the exact Added/Modified content to record) and the "WRES-01 Literal Filename Confusion" pitfall (use cmd/pollen/roots_windows.go, NOT internal/resolver/resolver_windows.go).
    - .planning/phases/02-windows-root-resolver/02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md — REQUIRED. The actual files created/modified, so CHANGES.md records reality (not the plan's intent if anything deviated).
  </read_first>
  <action>
    Edit `../pollen/VERSION` to the single line `0.1.1-pollen.2` (no trailing content beyond the existing newline). Append a new `## v0.1.1-pollen.2 (<today>) — Windows root resolver` section to `../pollen/CHANGES.md` ABOVE or directly after the v0.1.1-pollen.1 section, matching the existing heading style. Under `### Added`, list: `cmd/pollen/roots_windows.go` (//go:build windows) Windows root discovery for all 8 ecosystems (name them) using %APPDATA%/%LOCALAPPDATA%/%USERPROFILE%/%ProgramFiles% via filepath.Join + globExisting for Python*/Ruby*/.gem wildcards; `cmd/pollen/roots_windows_test.go` (//go:build windows) Windows root-resolver unit tests; `cmd/pollen/parity_test.go` PTEST-01 cross-platform parity test; `cmd/pollen/testdata/parity-fixture/` 8-ecosystem fixture. Under `### Modified`, list: `cmd/pollen/roots.go` added case "windows": delegation in baselineHomeCandidates/systemRoots/browserExtensionCandidateRoots + isBroadHomeRoot drive-root branch; `cmd/pollen/main_test.go` flipped 6 Phase-2 Windows skips and added Windows cases to TestIsBroadHomeRoot. Use the ACTUAL filename `cmd/pollen/roots_windows.go` — do NOT write `internal/resolver/resolver_windows.go` (RESEARCH pitfall: that PRD-draft path does not exist). Derive the date from the system date. Do not inline the full section text from this action — follow the existing CHANGES.md section as the literal shape.
  </action>
  <verify>
    <automated>cd ../pollen && grep -Fxq '0.1.1-pollen.2' VERSION && grep -c 'v0.1.1-pollen.2' CHANGES.md && grep -c 'internal/resolver/resolver_windows.go' CHANGES.md</automated>
  </verify>
  <acceptance_criteria>
    - `../pollen/VERSION` is exactly `0.1.1-pollen.2` (grep -Fxq matches the whole line)
    - `grep -c 'v0.1.1-pollen.2' ../pollen/CHANGES.md` >= 1 (new section present)
    - `grep -c 'cmd/pollen/roots_windows.go' ../pollen/CHANGES.md` >= 1 (actual filename recorded)
    - `grep -c 'internal/resolver/resolver_windows.go' ../pollen/CHANGES.md` == 0 (PRD-draft path NOT used)
    - CHANGES.md new section references parity_test.go, the parity-fixture, and the 6 flipped skips
  </acceptance_criteria>
  <done>VERSION reads 0.1.1-pollen.2 and CHANGES.md has an accurate v0.1.1-pollen.2 section naming the real files.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the version bump and confirm the full CI matrix is green</name>
  <files>../pollen/VERSION, ../pollen/CHANGES.md</files>
  <read_first>
    - .planning/phases/02-windows-root-resolver/02-CONTEXT.md — REQUIRED. Cross-repo model (explicit git -C ../pollen).
    - ../pollen/.github/workflows/ci.yml — REQUIRED. The test + differential job names/triggers so you know what "green" means and which checks gate the tag.
  </read_first>
  <action>
    Commit the version bump in pollen: `git -C ../pollen add VERSION CHANGES.md` then `git -C ../pollen commit -m "release(02-04): bump to v0.1.1-pollen.2 — Windows root resolver"`. Push the commit to origin (`git -C ../pollen push`) so CI runs against it. Then confirm the full CI matrix is green BEFORE tagging: poll the GitHub Actions runs for the pushed commit via `gh -R home-beekeeper/pollen run list` / `gh run watch`, requiring (a) the 3-OS test job (ubuntu/macos/windows) green — this is where TestWindowsBaselineRoots and TestParityAllEcosystems must pass on windows-latest, and (b) the differential job green on ubuntu+macos. If any job is red, STOP and surface the failure (do not tag). Local dev-host pre-check before pushing: `cd ../pollen && go build ./... && go vet ./...` and run the non-windows suite `go test ./cmd/pollen/ -count=1` to catch obvious breakage early (Windows-tagged tests only execute on windows-latest CI).
  </action>
  <verify>
    <automated>cd ../pollen && go build ./... && go vet ./... && go test ./cmd/pollen/ -run '^TestParityAllEcosystems$|^TestDifferential$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../pollen && git log -1 --format='%s'` contains `release(02-04)` and `v0.1.1-pollen.2`
    - `cd ../pollen && go build ./... && go vet ./...` exit 0 on the dev host
    - The pushed commit's GitHub Actions: the 3-OS test job (ubuntu-latest, macos-latest, windows-latest) is green AND the differential job (ubuntu, macos) is green — confirmed via `gh -R home-beekeeper/pollen run list` for the commit SHA
    - windows-latest specifically shows TestWindowsBaselineRoots and TestParityAllEcosystems passing (no skip)
  </acceptance_criteria>
  <done>Version-bump commit pushed; full 3-OS CI matrix + differential confirmed green on the commit before any tag is created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tag, sign, and verify the v0.1.1-pollen.2 release</name>
  <files>../pollen (git tag v0.1.1-pollen.2)</files>
  <action>
    Gated on a confirmed-green CI matrix from Task 2. Create and push the annotated release tag from the pollen repo: `git -C ../pollen tag -a v0.1.1-pollen.2 -m "Pollen v0.1.1-pollen.2 — Windows root resolver (WRES-01, WRES-02, PTEST-01)"` then `git -C ../pollen push origin v0.1.1-pollen.2`. The tag push triggers release.yml (keyless cosign OIDC + CycloneDX SBOM). After the release workflow completes, verify the Sigstore signature on checksums.txt and confirm the SBOM is attached. Do NOT push the tag until the human confirms (per the checkpoint below) that the 3-OS test + differential CI is green for the pushed commit.
  </action>
  <what-built>Plan 01–03 shipped the Windows root resolver, Windows unit tests, and the cross-platform parity test to ../pollen, and Task 2 confirmed the full 3-OS CI matrix is green on the v0.1.1-pollen.2 version-bump commit. The remaining step is creating and pushing the signed release tag — the tag push triggers release.yml (keyless cosign OIDC + CycloneDX SBOM), which only runs in GitHub Actions and produces signed artifacts a human should confirm.</what-built>
  <how-to-verify>
    1. Confirm CI is green for the pushed commit: `gh -R home-beekeeper/pollen run list --limit 5` — the test (3-OS) and differential workflows for the latest commit show `completed / success`.
    2. Create and push the annotated tag from ../pollen:
       `git -C ../pollen tag -a v0.1.1-pollen.2 -m "Pollen v0.1.1-pollen.2 — Windows root resolver (WRES-01, WRES-02, PTEST-01)"`
       `git -C ../pollen push origin v0.1.1-pollen.2`
    3. Watch the release workflow: `gh -R home-beekeeper/pollen run watch` until the release job completes successfully.
    4. Verify the release carries a Sigstore signature + SBOM. Download `checksums.txt` and its `.sigstore.json` bundle from the GitHub release, then run:
       `cosign verify-blob --bundle checksums.txt.sigstore.json --certificate-identity-regexp '^https://github\.com/home-beekeeper/pollen/' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' checksums.txt`
       Expected output: `Verified OK`.
    5. Confirm a CycloneDX SBOM artifact is attached to the `v0.1.1-pollen.2` GitHub release and records the upstream pinned commit.
  </how-to-verify>
  <verify>Manual: cosign verify-blob prints "Verified OK" and a CycloneDX SBOM is attached to the v0.1.1-pollen.2 GitHub release.</verify>
  <done>v0.1.1-pollen.2 is tagged, the release workflow signed checksums.txt (cosign "Verified OK"), and the CycloneDX SBOM is attached to the release.</done>
  <resume-signal>Type "approved" once cosign prints "Verified OK" and the SBOM is attached, or describe what failed (e.g. CI red, signature mismatch, missing SBOM).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git tag push → GitHub Actions release pipeline | Tag push triggers keyless OIDC signing; the workflow identity must be the canonical home-beekeeper/pollen release.yml |
| Released artifacts → downstream consumers (beekeeper) | checksums.txt signature + SBOM are the trust anchor a consumer verifies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Spoofing | Release artifact provenance | mitigate | cosign keyless verify with `--certificate-identity-regexp '^https://github\.com/home-beekeeper/pollen/'` + OIDC issuer pin (Phase-1 established canonical casing); checkpoint requires "Verified OK" before sign-off. |
| T-02-11 | Tampering | Tagging before CI is green | mitigate | Task 2 gates the tag on a confirmed-green 3-OS matrix + differential; the checkpoint re-confirms run status before pushing the tag. No tag is created on a red commit. |
| T-02-12 | Repudiation | Untracked delta in the release | accept | CHANGES.md records the exact files/behaviors; SBOM records the upstream pinned commit. Low residual risk — delta is two-file + new test files, all in the 02-0{1,2,3} commits. |
</threat_model>

<verification>
- `cd ../pollen && grep -Fxq '0.1.1-pollen.2' VERSION` — version bumped
- 3-OS CI matrix green (TestWindowsBaselineRoots + TestParityAllEcosystems pass on windows-latest) + differential green on Linux+macOS
- `cosign verify-blob ... checksums.txt` → `Verified OK`; CycloneDX SBOM attached to the v0.1.1-pollen.2 release
- CHANGES.md names cmd/pollen/roots_windows.go (not internal/resolver/...)
</verification>

<success_criteria>
- Phase-2 Success Criterion 4 met: v0.1.1-pollen.2 tagged + signed; Windows CI no longer skips root-resolver tests
- Full 3-OS matrix + differential green before tagging
- Release reuses the Phase-1 signing/SBOM stack with zero new infra
- VERSION + CHANGES.md committed as release(02-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-windows-root-resolver/02-04-SUMMARY.md`
</output>
