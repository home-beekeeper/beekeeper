---
phase: 02-windows-root-resolver
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ../pollen/cmd/pollen/roots_windows_test.go
  - ../pollen/cmd/pollen/main_test.go
autonomous: true
requirements: [WRES-01, WRES-02]

must_haves:
  truths:
    - "On a Windows host, TestWindowsBaselineRoots proves resolveRoots(baseline) returns roots for all 8 ecosystems when fake env-var dirs exist under a t.TempDir"
    - "The 6 Phase-2 t.Skip calls in main_test.go no longer carry 'arrive in Phase 2' language — Windows-relevant tests run, inherently-Unix tests keep a Unix-specific reason"
    - "TestIsBroadHomeRoot runs on Windows and asserts C:\\ and %USERPROFILE% are broad while %USERPROFILE%\\code is narrow"
    - "An empty %APPDATA% produces zero APPDATA-derived roots (no CWD-relative path leak)"
  artifacts:
    - path: "../pollen/cmd/pollen/roots_windows_test.go"
      provides: "TestWindowsBaselineRoots (8-ecosystem coverage) + empty-env-var guard test"
      contains: "//go:build windows"
      min_lines: 60
    - path: "../pollen/cmd/pollen/main_test.go"
      provides: "6 flipped skips + Windows cases in TestIsBroadHomeRoot"
  key_links:
    - from: "../pollen/cmd/pollen/roots_windows_test.go"
      to: "resolveRoots(model.ProfileBaseline, nil, rootsOpts{})"
      via: "t.Setenv(USERPROFILE/APPDATA/LOCALAPPDATA/ProgramFiles) + os.MkdirAll fixture dirs"
      pattern: "resolveRoots\\(model.ProfileBaseline"
---

<objective>
Prove the Plan-01 Windows resolver works and flip the six Phase-2 Windows skips so Windows CI actually exercises Windows root resolution. Satisfies the verification side of WRES-01 and WRES-02 and Phase-2 Success Criterion 4 ("Windows CI no longer skips root-resolver tests").

Purpose: Plan 01 added the Windows root code but nothing tests it, and `main_test.go` still has six `t.Skip("...arrive in Phase 2 (v0.1.1-pollen.2)")` calls that silently bypass Windows. This plan adds a new `//go:build windows` test file with real Windows assertions (env-var override + fake fixture dirs → resolveRoots), then removes the "Phase 2" deferral from the six skips: Windows-relevant tests get un-skipped/adapted, and tests that are inherently Unix (e.g. `.local/lib/python`) keep a skip but with a Unix-specific (non-Phase-2) reason so there is no rot.

Output: `../pollen/cmd/pollen/roots_windows_test.go` (new) + the edited `../pollen/cmd/pollen/main_test.go`, committed to the pollen repo.

CROSS-REPO: Code lives in `../pollen`. This task does its OWN `git -C ../pollen` commit. Do NOT create test code under `beekeeper/internal/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-windows-root-resolver/02-CONTEXT.md
@.planning/phases/02-windows-root-resolver/02-RESEARCH.md
@.planning/phases/02-windows-root-resolver/02-PATTERNS.md
@.planning/phases/02-windows-root-resolver/02-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from ../pollen. -->

From ../pollen/cmd/pollen/roots.go (package main — directly callable from tests):
  func resolveRoots(profile string, explicit []string, opts rootsOpts) ([]scanner.Root, []string, error)  // line 61
  type rootsOpts struct { AllUsers bool }
  func isBroadHomeRoot(path string) bool   // line 171 — now has the Windows drive-root branch from Plan 01

From ../pollen/internal/model/model.go:
  model.ProfileBaseline                    // "baseline"
  model.RootKindUserPackage / RootKindGlobalPackage

Windows env vars os.UserHomeDir reads: USERPROFILE (NOT HOME — Pitfall 5).
The six Phase-2 skip lines in main_test.go: 54–55, 121–122, 146–147, 273–274, 288–289, 300–301
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create roots_windows_test.go — 8-ecosystem Windows resolver tests</name>
  <files>../pollen/cmd/pollen/roots_windows_test.go</files>
  <read_first>
    - ../pollen/cmd/pollen/main_test.go — REQUIRED. The `t.TempDir()` + `os.MkdirAll` + `resolveRoots(...)` + assert-root-present pattern (lines 99–142, esp. TestResolveRootsProjectIncludesCodeDir 120–142). Note these use `t.Setenv("HOME", home)` — you MUST use USERPROFILE/APPDATA/LOCALAPPDATA/ProgramFiles instead (Pitfall 5).
    - ../pollen/cmd/pollen/roots_windows.go — REQUIRED (from Plan 01). The exact roots windowsBaselinePackageRoots/windowsSystemRoots emit, so your fixture dirs and assertions match.
    - .planning/phases/02-windows-root-resolver/02-PATTERNS.md — REQUIRED. "roots_windows_test.go" section: the Windows t.Setenv block, the os.MkdirAll+resolveRoots call shape, and Pitfall 5 (USERPROFILE not HOME).
    - .planning/phases/02-windows-root-resolver/02-RESEARCH.md — REQUIRED. Q5 Pitfall 1 (empty-env guard test) and Pitfall 6 (PyPI dist-info, not venv layout).
  </read_first>
  <behavior>
    - TestWindowsBaselineRoots: set USERPROFILE/APPDATA/LOCALAPPDATA/ProgramFiles to subdirs of one t.TempDir; os.MkdirAll the on-disk dirs each ecosystem root expects (e.g. APPDATA\npm\node_modules, LOCALAPPDATA\pnpm\store, LOCALAPPDATA\Yarn\Data\global, USERPROFILE\.bun\install\cache, APPDATA\Python\Python313\site-packages, USERPROFILE\go\pkg\mod, USERPROFILE\.gem\ruby\3.3.0\gems, APPDATA\Composer\vendor, ProgramFiles\nodejs\node_modules, ProgramFiles\Ruby33-x64\lib\ruby\gems); call resolveRoots(model.ProfileBaseline, nil, rootsOpts{}); assert each created dir appears in the returned roots with the expected Kind. Glob roots (Python*, .gem/ruby/*, Ruby*) must resolve via the created versioned dirs.
    - TestWindowsBaselineRootsEmptyAppdata: set APPDATA="" (and USERPROFILE to a valid temp so resolveRoots does not fail "no default roots"), assert NO returned root path is CWD-relative / under "npm\node_modules" without a drive — i.e. the empty-var guard held.
    - All tests are guarded `//go:build windows` so they compile-and-run only on a Windows host (and windows-latest CI).
  </behavior>
  <action>
    Create `../pollen/cmd/pollen/roots_windows_test.go`. First line `//go:build windows`, blank line, `package main`. Import "os", "path/filepath", "testing", and "github.com/bantuson/pollen/internal/model".

    Write `TestWindowsBaselineRoots(t *testing.T)`: `tmp := t.TempDir()`; `t.Setenv("USERPROFILE", tmp)`; `t.Setenv("APPDATA", filepath.Join(tmp, "AppData", "Roaming"))`; `t.Setenv("LOCALAPPDATA", filepath.Join(tmp, "AppData", "Local"))`; `t.Setenv("ProgramFiles", filepath.Join(tmp, "ProgramFiles"))`. `os.MkdirAll` (0o755) the directory each of the 8 ecosystems resolves to — for the three glob roots create a concrete versioned dir under the wildcard parent (e.g. `Python313` for `Python*`, `3.3.0` for `.gem\ruby\*`, `Ruby33-x64` for `Ruby*`). Call `resolveRoots(model.ProfileBaseline, nil, rootsOpts{})`; collect returned `roots` into a `map[string]string` of Path→Kind; assert every created dir is present with the expected Kind (RootKindGlobalPackage for npm-MSI + Ruby-system, RootKindUserPackage for the rest), failing with `t.Errorf` naming the missing path. Use the test naming prefix `TestWindowsBaseline` so the VALIDATION quick-run `-run '^TestWindowsBaseline'` matches.

    Write `TestWindowsBaselineRootsEmptyAppdata(t *testing.T)`: set USERPROFILE to a temp with `go\pkg\mod` created (so the baseline has a survivor), set APPDATA="", and assert that no returned root has a path lacking a volume name (`filepath.VolumeName(r.Path) == ""`) — proving the empty-APPDATA guard prevented a relative `npm\node_modules` leak. Mirror the main_test.go assertion idiom; do not inline implementations here — follow read_first.
  </action>
  <verify>
    <automated>cd ../pollen && GOOS=windows go vet ./cmd/pollen/ && grep -c '^//go:build windows' cmd/pollen/roots_windows_test.go</automated>
  </verify>
  <acceptance_criteria>
    - ../pollen/cmd/pollen/roots_windows_test.go line 1 is exactly `//go:build windows`
    - `cd ../pollen && GOOS=windows go vet ./cmd/pollen/` exits 0 (test file compiles under windows tag)
    - `grep -c 'func TestWindowsBaselineRoots' ../pollen/cmd/pollen/roots_windows_test.go` >= 1 (name matches the `-run '^TestWindowsBaseline'` quick-run)
    - `grep -c 't.Setenv("USERPROFILE"\|t.Setenv("APPDATA"\|t.Setenv("LOCALAPPDATA"\|t.Setenv("ProgramFiles"' ../pollen/cmd/pollen/roots_windows_test.go` covers all four env vars; `grep -c 't.Setenv("HOME"' ../pollen/cmd/pollen/roots_windows_test.go` == 0 (Pitfall 5)
    - `grep -c 'TestWindowsBaselineRootsEmptyAppdata' ../pollen/cmd/pollen/roots_windows_test.go` == 1 (empty-env guard test present)
    - On a Windows host: `go test ./cmd/pollen/ -run '^TestWindowsBaseline' -v` passes (PASS lines for both tests). On a non-Windows dev box this is build-tag-excluded — the GOOS=windows vet above is the local signal; the live run is windows-latest CI.
  </acceptance_criteria>
  <done>roots_windows_test.go covers all 8 ecosystems + the empty-env-var guard, uses USERPROFILE-family overrides (never HOME), and vets clean under GOOS=windows.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the 6 Phase-2 skips and add Windows cases to TestIsBroadHomeRoot</name>
  <files>../pollen/cmd/pollen/main_test.go</files>
  <read_first>
    - ../pollen/cmd/pollen/main_test.go — REQUIRED. The exact six skip sites: TestIsBroadHomeRoot (54–55), TestResolveRootsProjectIncludesCodeDir (121–122), TestResolveRootsBaselineIncludesUserLocalPython (146–147), TestResolveRootsBaselineRefusesBroadHome (273–274), TestResolveRootsProjectRefusesBroadHome (288–289), TestResolveRootsDeepAllowsBroadHome (300–301). Also read the existing `broad[]`/`narrow[]` slices in TestIsBroadHomeRoot (60–89).
    - .planning/phases/02-windows-root-resolver/02-RESEARCH.md — REQUIRED. Q4 "Exact Skips to Flip" table (which tests are inherently Unix vs Windows-relevant) and the TestIsBroadHomeRoot un-skip guidance.
    - .planning/phases/02-windows-root-resolver/02-PATTERNS.md — REQUIRED. The "Phase-2 Windows skip lines to flip" table and "TestIsBroadHomeRoot un-skip adaptation" (Windows-shaped broad[]/narrow[] cases).
  </read_first>
  <action>
    Edit `../pollen/cmd/pollen/main_test.go`. The governing rule: NO remaining `t.Skip` on Windows may say "arrive in Phase 2" or reference `v0.1.1-pollen.2` (Success Criterion 4 + PRD §1.5 no-rot). Apply per the RESEARCH Q4 table:
    - TestIsBroadHomeRoot (54–55): REMOVE the windows skip. The function works on Windows via os.UserHomeDir() + the Plan-01 drive-root branch. The existing `t.Setenv("HOME", home)` and Unix-only broad/narrow entries must be made platform-aware: gate the Unix-shaped paths behind `if runtime.GOOS != "windows"` and add a Windows-shaped set (broad: `C:\`, `os.Getenv("USERPROFILE")`; narrow: `filepath.Join(os.Getenv("USERPROFILE"), "code")`, `filepath.Join(os.Getenv("USERPROFILE"), ".vscode", "extensions")`) behind `if runtime.GOOS == "windows"`. On Windows do NOT call `t.Setenv("HOME", ...)` (it does not redirect os.UserHomeDir there).
    - TestResolveRootsProjectIncludesCodeDir (121–122), TestResolveRootsBaselineRefusesBroadHome (273–274), TestResolveRootsProjectRefusesBroadHome (288–289), TestResolveRootsDeepAllowsBroadHome (300–301): these exercise HOME-override behavior that on Windows requires USERPROFILE. Either (a) adapt them to use USERPROFILE when `runtime.GOOS == "windows"` so they run, or (b) if adaptation is non-trivial for a given test, replace the skip MESSAGE so it states the Unix-specificity WITHOUT "Phase 2" / "arrive in" / version language (e.g. "HOME-override harness is Unix-only; Windows resolver coverage lives in roots_windows_test.go"). Prefer (a) for the broad-home refusal tests since the Plan-01 drive-root branch makes them meaningful on Windows.
    - TestResolveRootsBaselineIncludesUserLocalPython (146–147): inherently Unix (`.local/lib/python` layout). KEEP a skip but rewrite the message to a Unix-specific, non-Phase-2 reason (e.g. "Unix .local/lib/python layout; Windows PyPI roots covered by TestWindowsBaselineRoots").
    Do NOT touch differential_test.go:55 (PTEST-02 Windows skip stays — different concern). Add `"runtime"` to imports only if not already present (it is, line 6). Follow read_first for the literal broad[]/narrow[] shapes; do not inline here.
  </action>
  <verify>
    <automated>cd ../pollen && go vet ./cmd/pollen/ && grep -c 'arrive in Phase 2\|v0.1.1-pollen.2' cmd/pollen/main_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' ../pollen/cmd/pollen/main_test.go | grep -c 'arrive in Phase 2'` == 0 (no remaining Phase-2 deferral language in main_test.go)
    - `grep -c 'v0.1.1-pollen.2' ../pollen/cmd/pollen/main_test.go` == 0
    - `grep -c 't.Skip' ../pollen/cmd/pollen/main_test.go` is reduced from 6 of the targeted skips: the only Windows `t.Skip` remaining (if any) is the inherently-Unix TestResolveRootsBaselineIncludesUserLocalPython and carries a Unix-specific (non-version) reason
    - `cd ../pollen && go vet ./cmd/pollen/` exits 0 AND `GOOS=windows go vet ./cmd/pollen/` exits 0
    - `cd ../pollen && go test ./cmd/pollen/ -run '^TestIsBroadHomeRoot$|^TestResolveRoots' -count=1` passes on the dev host (Linux/macOS path still green — adaptation did not break Unix)
    - The differential skip is intact: `grep -c 'PTEST-02 differential runs on Linux' ../pollen/cmd/pollen/differential_test.go` == 1 (untouched)
    - On a Windows host: `go test ./cmd/pollen/ -v` runs TestIsBroadHomeRoot and the adapted resolve-roots tests (no Phase-2 skip)
  </acceptance_criteria>
  <done>The 6 Phase-2 skips no longer reference Phase 2 / v0.1.1-pollen.2; Windows-relevant tests run on Windows; TestIsBroadHomeRoot asserts C:\ broad; Unix tests still green; differential_test.go skip untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the Windows tests + skip flips to the pollen repo</name>
  <files>../pollen/cmd/pollen/roots_windows_test.go, ../pollen/cmd/pollen/main_test.go</files>
  <read_first>
    - .planning/phases/02-windows-root-resolver/02-CONTEXT.md — REQUIRED. "Repository & cross-repo model" (explicit git -C ../pollen).
  </read_first>
  <action>
    `git -C ../pollen add cmd/pollen/roots_windows_test.go cmd/pollen/main_test.go` then `git -C ../pollen commit -m "test(02-02): Windows root-resolver tests + flip 6 Phase-2 skips (WRES-01, WRES-02)"`. Do NOT push. Do NOT stage beekeeper files. Match existing pollen commit style.
  </action>
  <verify>
    <automated>cd ../pollen && git show --stat HEAD | grep -E 'roots_windows_test.go|main_test.go'</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../pollen && git log -1 --format='%s'` contains `02-02`
    - `cd ../pollen && git show --stat HEAD` lists exactly `cmd/pollen/roots_windows_test.go` and `cmd/pollen/main_test.go`
    - `cd ../pollen && git status --porcelain cmd/pollen/roots_windows_test.go cmd/pollen/main_test.go` is empty
  </acceptance_criteria>
  <done>Pollen repo has a test(02-02) commit with exactly the two test files; nothing pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test env-var overrides → resolveRoots | t.Setenv injects USERPROFILE/APPDATA/etc.; tests must not touch real user data |
| Fixture dirs under t.TempDir → filesystem | os.MkdirAll writes only inside the auto-cleaned temp dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Test fixtures escaping into real %USERPROFILE% | mitigate | All fixture writes go under `t.TempDir()` (auto-removed); env overrides use `t.Setenv` (auto-restored). No absolute real-home path is ever constructed. Verified: tests reference only tmp-derived paths. |
| T-02-05 | Repudiation / Rot | Silent Windows skips masking missing coverage | mitigate | This plan's core deliverable: remove "Phase 2" skip language so Windows CI genuinely runs root-resolver assertions (Success Criterion 4); grep gate asserts zero residual deferral strings. |
| T-02-06 | Elevation | Empty-env-var CWD-relative path leak | mitigate | TestWindowsBaselineRootsEmptyAppdata asserts no volume-less root path is returned — proves the Plan-01 env guard (T-02-01) holds under test. |
</threat_model>

<verification>
- `cd ../pollen && GOOS=windows go vet ./cmd/pollen/` — Windows test files compile
- `cd ../pollen && go test ./cmd/pollen/ -count=1` — Unix/macOS suite still green after skip adaptation
- On windows-latest CI: `go test -race ./...` runs TestWindowsBaselineRoots + adapted tests (no Phase-2 skip)
- `grep -v '^#' main_test.go | grep -c 'arrive in Phase 2'` == 0; differential_test.go skip untouched
</verification>

<success_criteria>
- WRES-01/WRES-02 verified: TestWindowsBaselineRoots proves all 8 ecosystems resolve on Windows
- Success Criterion 4 met: no Windows root-resolver test is skipped with Phase-2/version language
- Empty-env-var guard proven; TestIsBroadHomeRoot refuses C:\ on Windows; Unix suite still green; differential skip intact
- Committed to pollen as test(02-02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-windows-root-resolver/02-02-SUMMARY.md`
</output>
