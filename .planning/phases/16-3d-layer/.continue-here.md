---
context: phase
phase: 16-3d-layer
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-09
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GSD phase-resolver can't find Phase 16 | `extractPhaseToken("16-3d-layer")` greedily eats the digit-led `3d` token → returns `16-3d`, so `phase-plan-index`, `init.execute-phase`, and any phase-resolving GSD verb report **"Phase not found"** for Phase 16. | blocking | Execute inline and **hand-manage** STATE.md / ROADMAP.md / REQUIREMENTS.md (already the project precedent). Do NOT rely on `gsd state.*` / `verify-work` tooling resolving this phase. Do NOT "fix" by renaming the dir — it diverges the slug and breaks 35 in-plan path refs. |
| Spawned subagents can't build | gsd-executor / planner subagents here have a Bash with **no node/git/pnpm** on PATH → they cannot run `pnpm build`/commit. | blocking | Run execution **inline** (orchestrator does the work via its own PowerShell/Bash). `use_worktrees=false` for this project. Do NOT spawn gsd-executor for build/commit work. |
| freeze --execute hangs on Windows | `charmbracelet/freeze --execute` relies on a PTY and hangs on Windows; blocked the real TUI screenshots. | advisory | TUI screenshots were dropped per maintainer. If revisited, render TUI panels as HTML terminal blocks or have the maintainer run `beekeeper dashboard`. |
| Em-dashes in copy | Maintainer does not want em-dashes (—) in marketing copy. | advisory | Keep copy em-dash-free (use commas/colons). All 13 home components were swept. |

<current_state>
Phase 16 (3D Layer) is functionally **done and green** but **NOT finalized**. All of GFX-01..04 pass
(`pnpm build` + `web/tests/gfx_spec.py` + `web/tests/home_spec.py`). The hero was redesigned **twice**
in response to maintainer feedback (the original UAT "approved" was for a now-replaced hive). Finalization
(16-03-SUMMARY + mark complete + reconcile tracking) is **gated on the maintainer confirming the redesigned
hive** on http://localhost:3000. Working tree is clean at HEAD `1096b75`.
</current_state>

<completed_work>
- Plan 16-01 (foundation) — `ce8eb4f`, 16-01-SUMMARY.md ✓
- Plan 16-02 (3D layer) — `426cbed`, 16-02-SUMMARY.md ✓
- Plan 16-03 T1/T2 (GFX-04 LCP proxy + no-context-leak; lighthouse skipped) — `0e801ed`
- Redesign round 1 (two-panel living-hive + multilayer architecture + Layer 1 expansion + auto-mode story) — `bfd0314`, `45e7b2a`
- TUI screenshots (Part C) — SKIPPED (freeze hangs on Windows); harness removed
- Redesign round 2 (single animated honeycomb, top-aligned/lifted, SVG fade, left-col alignment, copy icons, em-dashes stripped, extension-watcher→privileged) — `1096b75`
</completed_work>

<remaining_work>
1. **Maintainer confirms the redesigned hive** position/look (localhost:3000). If off, tweak the lift:
   `web/components/home/hero-canvas.tsx` → `Hive()` useFrame → `g.position.y = 0.45 + …` (and/or `<group scale={1.25}>`).
2. **Finalize Phase 16**: write `16-03-SUMMARY.md`, mark the phase complete, hand-reconcile
   STATE.md / ROADMAP.md / REQUIREMENTS.md (GFX-01..04 → done), stop the dev servers.
</remaining_work>

<decisions_made>
- 3D is a **single** animated honeycomb (dropped the 2nd layer + pulses) — maintainer rejected the busier design twice.
- Explicit three.js lights instead of drei `<Environment>` (no runtime HDRI CDN fetch; offline/bounded bundle).
- GFX-04 gated by the Playwright `performance` LCP proxy; `lighthouse` install skipped (Windows-unproven).
- The server SVG (`#hero-hive-svg`) fades out once the real canvas mounts (prevents double-image from the lift; SVG stays the LCP element).
- Editor-extension watcher → Opt-in/privileged tier (not Default).
</decisions_made>

<blockers>
- GSD resolver can't find Phase 16 (see anti-pattern #1) → all tracking is hand-managed.
- freeze --execute hangs on Windows → TUI screenshots dropped.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable companion to this file (full task/decision state).
2. `.planning/phases/16-3d-layer/16-RESEARCH.md` — locked R3F/static-export architecture facts.
3. `web/components/home/hero-canvas.tsx` — the current single animated honeycomb (lift/scale knobs).

## Infrastructure State
- **Dev server**: `pnpm dev` running on **http://localhost:3000** (background; hot-reloads). A static `python http.server` on :4300 may also be running (possibly stale).
- Pinned 3D deps installed in `web/` (three@0.184.0 / @react-three/fiber@9.6.1 / @react-three/drei@10.7.7 / @types/three@0.184.1). No Go core changes this session.

<context>
The hero is two-panel (copy left, hive right). The maintainer cares a lot about the 3D landing well
("not impressed" twice). The third iteration is a clean single honeycomb, lifted into the upper frame,
row top-aligned to sit by the "autonomous coding agents" headline. Content was also reworked: the
architecture is now a defense-in-depth picture (Default / Optional / Opt-in-privileged), Layer 1 covers
all three pre-hook surfaces (malicious packages / npm→pnpm-bun nudge / sensitive-path reads), and the
origin story leads with the auto-mode exposure rather than the VS Code extension.
</context>

<next_action>
Start with: ask the maintainer whether the redesigned hive on http://localhost:3000 is positioned and
looking right. If yes → finalize Phase 16 (16-03-SUMMARY.md, mark complete, hand-reconcile
STATE/ROADMAP/REQUIREMENTS for GFX-01..04, stop the dev servers). If no → adjust the hive lift/scale in
web/components/home/hero-canvas.tsx and re-show on :3000.
</next_action>
