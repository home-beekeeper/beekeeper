---
phase: 01-fork-setup-discipline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - "../pollen/go.mod"
  - "../pollen/go.sum"
  - "../pollen/cmd/pollen/main.go"
  - "../pollen/cmd/pollen/selftest.go"
  - "../pollen/cmd/pollen/version.go"
  - "../pollen/cmd/pollen/roots.go"
  - "../pollen/cmd/pollen/main_test.go"
  - "../pollen/internal/**"
autonomous: true
requirements: [FORK-01, FORK-04]
must_haves:
  truths:
    - "The pollen git repo exists at ../pollen with upstream bumblebee history at the pinned SHA"
    - "go.mod first line is module github.com/home-beekeeper/pollen"
    - "pollen binary builds from ./cmd/pollen on the dev machine and cross-compiles to windows"
    - "pollen selftest emits exactly 3 findings and exits 0"
    - "No bumblebee command/package/binary-name string survives outside attribution contexts"
  artifacts:
    - path: "../pollen/go.mod"
      provides: "Renamed Go module declaration"
      contains: "module github.com/home-beekeeper/pollen"
    - path: "../pollen/cmd/pollen/main.go"
      provides: "Renamed CLI entry point"
    - path: "../pollen/cmd/pollen/selftest.go"
      provides: "Embedded-fixture selftest with pollen-selftest- temp prefix"
      contains: "pollen-selftest-"
  key_links:
    - from: "../pollen/cmd/pollen/*.go"
      to: "github.com/home-beekeeper/pollen/internal/*"
      via: "import paths rewritten from perplexityai/bumblebee"
      pattern: "github.com/home-beekeeper/pollen/internal"
    - from: "../pollen/cmd/pollen/selftest.go"
      to: "../pollen/cmd/pollen/selftest/{catalog.json,fixtures}"
      via: "go:embed all:selftest/fixtures selftest/catalog.json"
      pattern: "go:embed all:selftest/fixtures"
---

<objective>
Create the Pollen repository as a bounded Apache-2.0 fork of upstream `perplexityai/bumblebee`, pinned at the v0.1.1 commit, with the two — and only two — structural changes this milestone permits: module path rewrite (`github.com/perplexityai/bumblebee` → `github.com/home-beekeeper/pollen`) and `cmd/bumblebee/` → `cmd/pollen/`. Apply the targeted trademark string fixes that the bulk module-path rewrite does NOT catch, then prove the rename is sound by building, cross-compiling to Windows, and running selftest.

Purpose: Every other plan in this phase depends on this repo existing with the rename done. This is Wave 0 — the hard prerequisite. Preserving upstream's directory structure (no `internal/resolver/` invention, no file moves beyond the cmd rename) keeps the §6.2 sync workflow tractable in Phase 5.

Output: A committed `../pollen` git repo with `upstream` remote, renamed module + cmd, clean `go mod tidy`, green selftest, and a green `GOOS=windows go build ./...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<cross_repo_execution_model>
CRITICAL: The executor's CWD is the beekeeper repo. Pollen is a SEPARATE git repo at `../pollen` (i.e. `C:/Users/Bantu/mzansi-agentive/pollen`), home `github.com/home-beekeeper/pollen`. It is NOT vendored into beekeeper and is OUTSIDE beekeeper's worktree.

- Every task that creates/edits pollen files MUST use explicit `../pollen/...` paths.
- Beekeeper's auto-commit will NOT capture pollen files. Each task that produces pollen artifacts MUST stage+commit them in the pollen repo itself via `git -C ../pollen add ...` then `git -C ../pollen commit ...`.
- Only this PLAN.md and the eventual SUMMARY.md live in beekeeper/.planning and are committed to beekeeper as usual.
- Network is required for this plan (git clone of upstream). This is a normal git clone; `autonomous: true` is fine.
</cross_repo_execution_model>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-setup-discipline/01-CONTEXT.md
@.planning/phases/01-fork-setup-discipline/01-RESEARCH.md
@.planning/phases/01-fork-setup-discipline/01-VALIDATION.md

<pinned_upstream_facts>
<!-- Verified against the live upstream repo (RESEARCH.md "Upstream Structure: Verified Facts"). Use these, NOT PRD assumptions. -->
- Upstream: github.com/perplexityai/bumblebee
- Pinned commit SHA (40-char): c24089804ee66ece4bec6f14638cb98985389cdb
- Pinned tag: v0.1.1, dated 2026-05-22T15:37:53Z
- Module path: github.com/perplexityai/bumblebee ; go 1.25
- External dependencies: ZERO (no go.sum at v0.1.1; pure stdlib)
- NO internal/resolver/ package — root discovery lives in cmd/bumblebee/roots.go. Do NOT create internal/resolver/.
- cmd/bumblebee/ contains: main.go, main_test.go, roots.go, selftest.go (+ selftest_test.go), sink.go, version.go, and a selftest/ subdir (catalog.json + fixtures/{npm-fixture,pypi-fixture,mcp-fixture})
- selftest.go embeds via: //go:embed all:selftest/fixtures selftest/catalog.json  (paths are relative to the .go file; they survive the cmd rename UNCHANGED)
- selftest asserts exactly 3 findings (npm, pypi, mcp)
- selftest temp dir prefix is os.MkdirTemp("", "bumblebee-selftest-*") — MUST become "pollen-selftest-*" (FORK-04)
- version.go: ldflags target is main.Version (package main, NOT a version package); fileDefault constant is "0.1.1"
- internal/ packages preserved verbatim: ecosystem, endpoint, exposure, model, normalize, output (output.go + httpsink.go), scanner, walk
</pinned_upstream_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone upstream at the pinned SHA, init the pollen repo, rewrite the module path</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "Pattern 1: Module Path Rewrite", "Upstream Structure: Verified Facts", "Pitfall 7: go mod tidy Surprises"
    - .planning/phases/01-fork-setup-discipline/01-CONTEXT.md — "Repository & Cross-Repo Model (LOCKED)", "Fork mechanics"
  </read_first>
  <files>../pollen/ (whole repo), ../pollen/go.mod, ../pollen/go.sum, ../pollen/internal/**, ../pollen/cmd/bumblebee/** (pre-rename)</files>
  <action>
    Clone `https://github.com/perplexityai/bumblebee.git` into `../pollen` PRESERVING history (so the Phase 5 sync workflow can cherry-pick), then check out the pinned commit `c24089804ee66ece4bec6f14638cb98985389cdb` and reset the default branch to it. Add the git remote `upstream` pointing at `https://github.com/perplexityai/bumblebee.git` (RESEARCH Open Question 2 recommends this now — zero cost, makes UPSTREAM.md's sync workflow immediately usable). Rename the `origin` remote or remove it so a later `gh repo create` can bind `origin` to `github.com/home-beekeeper/pollen` in plan 05.

    Rewrite EVERY occurrence of the literal `github.com/perplexityai/bumblebee` to `github.com/home-beekeeper/pollen` across all `*.go` files and `go.mod`. The import appears in `go.mod` (one line) and in `cmd/bumblebee/*.go` internal imports (main.go, selftest.go import internal/endpoint, internal/exposure, internal/model, internal/output, internal/scanner). Use the platform-appropriate bulk replace (PowerShell `Get-ChildItem -Recurse -Include '*.go','go.mod' | ForEach-Object { (Get-Content) -replace ... | Set-Content }` on Windows). Do NOT touch the `upstream:` line that will later appear in UPSTREAM.md — that file does not exist yet.

    Run `go mod tidy` in `../pollen`. Expect an empty-or-minimal `go.sum` (zero external deps). Commit everything to the pollen repo: `git -C ../pollen add -A && git -C ../pollen commit -m "fork: import upstream bumblebee @ c24089804ee66ece4bec6f14638cb98985389cdb (v0.1.1); rewrite module path -> github.com/home-beekeeper/pollen"`.
  </action>
  <verify>
    <automated>cd ../pollen && head -1 go.mod | grep -qx "module github.com/home-beekeeper/pollen" && git -C ../pollen log --oneline -1 && git -C ../pollen remote get-url upstream</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ../pollen rev-list --all | grep -q c24089804ee66ece4bec6f14638cb98985389cdb` — the pinned commit is present in history (history preserved).
    - `../pollen/go.mod` first line is exactly `module github.com/home-beekeeper/pollen`.
    - `grep -rl "github.com/perplexityai/bumblebee" ../pollen --include="*.go"` returns NOTHING (no stale import paths in Go source).
    - `git -C ../pollen remote get-url upstream` prints `https://github.com/perplexityai/bumblebee.git`.
    - `../pollen/go.sum` exists and is empty or contains only toolchain lines (zero external deps); `go mod verify` in `../pollen` succeeds.
    - The pollen repo HEAD commit message references the pinned SHA.
  </acceptance_criteria>
  <done>Pollen repo exists at ../pollen with preserved upstream history, the pinned commit reachable, module path rewritten, upstream remote configured, go mod tidy clean, and the import rewrite committed to the pollen repo.</done>
</task>

<task type="auto">
  <name>Task 2: Rename cmd/bumblebee → cmd/pollen and apply targeted trademark string fixes (FORK-04)</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "Pattern 2: cmd Directory Rename", "Pattern 3: Version Injection", "Pitfall 2: go:embed Path Breaks", "Pitfall 3: Trademark Violation in Temp Dir Prefix"
    - ../pollen/cmd/bumblebee/selftest.go (the os.MkdirTemp prefix), ../pollen/cmd/bumblebee/main.go (help/usage text), ../pollen/cmd/bumblebee/version.go (fileDefault constant)
  </read_first>
  <files>../pollen/cmd/pollen/main.go, ../pollen/cmd/pollen/selftest.go, ../pollen/cmd/pollen/version.go, ../pollen/cmd/pollen/roots.go, ../pollen/cmd/pollen/sink.go, ../pollen/cmd/pollen/main_test.go, ../pollen/cmd/pollen/selftest_test.go, ../pollen/cmd/pollen/selftest/** (moves with the directory)</files>
  <action>
    Move the directory `../pollen/cmd/bumblebee/` to `../pollen/cmd/pollen/` using `git -C ../pollen mv cmd/bumblebee cmd/pollen` (preserves history + moves the embedded `selftest/` subtree as one unit). Do NOT alter the `//go:embed all:selftest/fixtures selftest/catalog.json` directive text — it is relative to the source file and stays correct after the move (Pitfall 2).

    Apply these targeted string replacements that the module-path sed did NOT catch (these are the FORK-04 trademark-discipline fixes — "bumblebee" as a COMMAND/BINARY-NAME context, never attribution):
    1. In `cmd/pollen/selftest.go`: change the temp-dir prefix literal `bumblebee-selftest-` to `pollen-selftest-` inside the `os.MkdirTemp("", ...)` call.
    2. In `cmd/pollen/main.go` (and any sibling that sets cobra Use/Short/Long or usage/help text): replace the binary-name string `bumblebee` with `pollen` in command Use/Short/Long, usage strings, and any printed program name. Leave any code-comment that merely references upstream provenance only if it is genuinely attribution; otherwise rename.
    3. In `cmd/pollen/version.go`: change the `fileDefault` constant from `"0.1.1"` to `"0.1.1-pollen.1"`. The ldflags target remains `main.Version` (do not introduce a version package).

    Commit: `git -C ../pollen add -A && git -C ../pollen commit -m "rename: cmd/bumblebee -> cmd/pollen; trademark fixes (selftest temp prefix, help text, version default) [FORK-04]"`.
  </action>
  <verify>
    <automated>cd ../pollen && test -d cmd/pollen && test ! -d cmd/bumblebee && grep -q "pollen-selftest-" cmd/pollen/selftest.go && grep -q '0.1.1-pollen.1' cmd/pollen/version.go</automated>
  </verify>
  <acceptance_criteria>
    - `../pollen/cmd/pollen/` exists; `../pollen/cmd/bumblebee/` does NOT exist.
    - `../pollen/cmd/pollen/selftest/catalog.json` and `../pollen/cmd/pollen/selftest/fixtures/{npm-fixture,pypi-fixture,mcp-fixture}` all exist (the embedded subtree moved intact).
    - `grep -q "pollen-selftest-" ../pollen/cmd/pollen/selftest.go` and `grep -vc "bumblebee-selftest-" ../pollen/cmd/pollen/selftest.go` confirms the old prefix is gone.
    - `grep "0.1.1-pollen.1" ../pollen/cmd/pollen/version.go` matches the new fileDefault.
    - FORK-04 trademark grep gate (filtered to exclude attribution files, which do not exist yet in cmd/): `grep -ri "bumblebee" ../pollen/cmd/ --include="*.go" | grep -v '^#' | grep -vi "derived from\|upstream\|originally\|attribution\|perplexity"` returns NOTHING — no bare "bumblebee" command/binary string survives in cmd/.
    - `go build ./cmd/pollen` (run in ../pollen) compiles with no `pattern selftest/fixtures: no matching files found` error (proves go:embed survived the move).
  </acceptance_criteria>
  <done>cmd renamed to pollen, embedded fixtures intact, the three targeted trademark/version string fixes applied, the package builds, and the rename is committed to the pollen repo.</done>
</task>

<task type="auto">
  <name>Task 3: Build, cross-compile to Windows, run selftest, prove the rename is sound</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "Pitfall 6: Windows Build Includes No Resolver Logic Yet", "Open Questions" Q1 (GOOS=windows compile check), "Validation Architecture"
    - .planning/phases/01-fork-setup-discipline/01-VALIDATION.md — Per-Task Verification Map (FORK-01 build smoke + GOOS=windows + PTEST-03 selftest rows)
  </read_first>
  <files>(no new files — verification gates only; any build-tag fix to a test file lands here)</files>
  <action>
    From `../pollen`, run the verification battery that proves the Wave 0 rename is sound:
    1. `go build -trimpath -buildvcs=false -o dist/pollen ./cmd/pollen` (host build).
    2. `GOOS=windows GOARCH=amd64 go build ./...` — cross-compile to Windows to catch any Unix-only `syscall` compile-fail in upstream code (RESEARCH Open Question 1). If a NON-TEST file fails to compile on Windows, that is a real Phase-1 blocker — stop and surface it (Windows resolver logic is Phase 2, but the code must at least COMPILE on Windows now). If only a TEST file fails to compile, add a `//go:build !windows` tag to that test file with a structured comment `// Windows root-resolver tests arrive in Phase 2 (v0.1.1-pollen.2)` — never a silent skip.
    3. Run `./dist/pollen selftest` and assert it prints exactly 3 findings and exits 0.
    4. Run the inherited upstream Go suite: `go test -count=1 ./...` (race detector is a CI concern; locally CGO may be absent — `-race` is exercised in plan 04's CI, not here). Upstream's darwin/linux-specific tests `t.Skip` automatically; that is expected.

    Commit any build-tag fix to the pollen repo: `git -C ../pollen add -A && git -C ../pollen commit -m "build: cross-compile clean for windows; tag darwin/linux-only tests [FORK-01]"` (skip the commit if no file changed).
  </action>
  <verify>
    <automated>cd ../pollen && go build -trimpath -buildvcs=false -o dist/pollen ./cmd/pollen && GOOS=windows GOARCH=amd64 go build ./... && ./dist/pollen selftest && go test -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build -trimpath -buildvcs=false ./cmd/pollen` exits 0 (FORK-01 build smoke).
    - `GOOS=windows GOARCH=amd64 go build ./...` exits 0 — no Unix-only compile failures in non-test code (RESEARCH Open Question 1 resolved).
    - `./dist/pollen selftest` exits 0 AND its output reports 3 findings (PTEST-03 local smoke). Assert the count, e.g. output contains the selftest pass line for npm, pypi, and mcp fixtures.
    - `go test -count=1 ./...` passes (inherited upstream suite green on the dev host; darwin/linux-only tests skip cleanly).
    - If any test file was build-tagged for Windows, the tag carries the structured reason comment referencing Phase 2 — verify with `grep -rl "Phase 2 (v0.1.1-pollen.2)" ../pollen` if such a file exists.
  </acceptance_criteria>
  <done>The renamed module builds on the host, cross-compiles cleanly to Windows, selftest emits 3 findings and exits 0, and the inherited test suite passes — proving the Wave 0 rename did not break the fork.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream repo → pollen fork | Unreviewed upstream code is absorbed into a new dependency Beekeeper will consume. Untrusted-until-reviewed code crosses here. |
| dev machine git → pollen repo | Local commits define what later becomes a signed release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Upstream code absorbed via clone | mitigate | Clone is pinned to the explicit 40-char SHA `c24089804ee66ece4bec6f14638cb98985389cdb` (Task 1), NOT a moving branch. The 6 commits on upstream `main` since v0.1.1 are intentionally NOT absorbed (deferred to Phase 5 sync, with diff review). `go mod verify` confirms zero external deps. |
| T-01-02 | Spoofing | Module identity | mitigate | Module path rewritten to `github.com/home-beekeeper/pollen` so the fork can never be confused with upstream's import path; cmd renamed so the binary identity is `pollen`. |
| T-01-03 | Tampering | Hidden Unix-only assumptions causing silent Windows breakage later | mitigate | `GOOS=windows go build ./...` in Task 3 forces a compile-time check now; build-tagged test skips carry structured Phase-2 reasons, never silent rot. |
| T-01-04 | Information Disclosure | Trademark misuse signalling false upstream affiliation | mitigate | FORK-04 string fixes (Task 2) + grep gate ensure "bumblebee" survives only in genuine attribution, satisfying Apache-2.0 §6 (no trademark grant). |
</threat_model>

<verification>
- Pollen repo exists at ../pollen with upstream history and the pinned SHA reachable.
- `go.mod` module is `github.com/home-beekeeper/pollen`; no stale `perplexityai/bumblebee` import in any `.go` file.
- `cmd/pollen/` exists with embedded selftest fixtures intact; `cmd/bumblebee/` gone.
- Trademark grep gate passes for `cmd/`.
- Host build + `GOOS=windows go build ./...` + `pollen selftest` (3 findings) + `go test ./...` all green.
- All pollen changes are committed IN the pollen repo (`git -C ../pollen log` shows the fork/rename/build commits).
</verification>

<success_criteria>
- FORK-01: pollen module exists, forked at the pinned SHA, `cmd/pollen/` in place, builds on host and cross-compiles to Windows.
- FORK-04 (partial — cmd/ scope): no "bumblebee" command/binary/temp-prefix string survives outside attribution. (Full FORK-04 audit across attribution files completes in plan 02.)
- The repo is in a state where plans 02, 03, 04 can proceed in parallel against disjoint file sets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-setup-discipline/01-01-SUMMARY.md` (in beekeeper) recording: the exact pinned SHA imported, the resolution of Open Question 1 (did any non-test file need a Windows build fix?), whether any test file was build-tagged for Windows and why, the final `go.sum` state, and the pollen repo HEAD commit hashes for the fork/rename/build commits.
</output>
