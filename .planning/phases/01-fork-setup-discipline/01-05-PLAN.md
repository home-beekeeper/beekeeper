---
phase: 01-fork-setup-discipline
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - "../pollen/ (git remote + tag v0.1.1-pollen.1)"
autonomous: false
requirements: [FORK-03, SDEF-02]
must_haves:
  truths:
    - "The github.com/bantuson/pollen GitHub repo exists and the pollen code is pushed to it"
    - "The full CI matrix is green (ubuntu/macos/windows) and the differential test is green on Linux+macOS"
    - "Tag v0.1.1-pollen.1 is pushed, triggering the release pipeline"
    - "The release carries a cosign Sigstore signature on checksums.txt and a CycloneDX SBOM per artifact"
    - "The cosign signature verifies against the github.com/bantuson/pollen OIDC identity"
  artifacts:
    - path: "../pollen/ (remote)"
      provides: "Published v0.1.1-pollen.1 release with signature + SBOM"
      contains: "v0.1.1-pollen.1"
  key_links:
    - from: "tag v0.1.1-pollen.1"
      to: ".github/workflows/release.yml"
      via: "git push tag triggers the cosign + SLSA + SBOM pipeline"
      pattern: "v0.1.1-pollen.1"
    - from: "checksums.txt.sigstore.json"
      to: "github.com/bantuson/pollen OIDC identity"
      via: "cosign verify-blob certificate-identity-regexp"
      pattern: "bantuson/pollen"
---

<objective>
Cut the first Pollen release: create the `github.com/bantuson/pollen` GitHub repo, push the code, confirm the full CI matrix + differential are green, then tag `v0.1.1-pollen.1` to trigger the cosign-signed, SLSA-attested, SBOM-bearing release pipeline. Verify the published artifacts carry a valid Sigstore signature and a CycloneDX SBOM. This is the phase capstone — it proves fork hygiene works end-to-end with NO Windows code yet (FORK-03 final proof + SDEF-02 published-SBOM proof).

Purpose: Everything in plans 01–04 is inert until a real release runs the signing + provenance + SBOM pipeline in GitHub Actions. The first release validates the cosign OIDC identity (RESEARCH Assumption A1) and the SLSA full-semver generator. Two-account release-approval discipline applies at the tag step (same as beekeeper). A human checkpoint confirms the Actions run succeeded and the signature/SBOM are present and verifiable, because the signing pipeline only runs server-side.

Output: A published `v0.1.1-pollen.1` GitHub release with cosign signature + CycloneDX SBOM + SLSA provenance, verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<cross_repo_execution_model>
CRITICAL: CWD is beekeeper; Pollen is a SEPARATE repo at `../pollen`. All git/gh operations target the pollen repo: use `git -C ../pollen ...` and `gh ... --repo bantuson/pollen` (or run from within ../pollen). Beekeeper auto-commit does NOT apply. Only this PLAN.md / SUMMARY.md are beekeeper commits.

Depends on plans 01–04: the full repo (rename, attribution, differential harness, build/release pipeline) must be committed in ../pollen before this plan tags a release.
</cross_repo_execution_model>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fork-setup-discipline/01-CONTEXT.md
@.planning/phases/01-fork-setup-discipline/01-RESEARCH.md
@.planning/phases/01-fork-setup-discipline/01-VALIDATION.md

<release_facts>
- GitHub home: github.com/bantuson/pollen (handle bantuson). The repo must be created remotely — it does not exist yet.
- Tag: v0.1.1-pollen.1 (matches the release.yml trigger on v*). VERSION file already contains 0.1.1-pollen.1.
- The release pipeline (cosign sign-blob, syft CycloneDX SBOM, slsa-github-generator@v2.1.0) runs ONLY in GitHub Actions on the tag push — it cannot be exercised locally.
- cosign verify identity: certificate-oidc-issuer https://token.actions.githubusercontent.com ; certificate-identity-regexp anchored to ^https://github.com/bantuson/pollen/ (RESEARCH Assumption A1 — VALIDATE the exact cert identity string from the first release here).
- Two-account release-approval discipline noted for the tag step (CONTEXT.md self-defense).
</release_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the GitHub repo, push pollen, confirm the full CI matrix is green</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-CONTEXT.md — "Repository & Cross-Repo Model" (github.com/bantuson/pollen)
    - .planning/phases/01-fork-setup-discipline/01-VALIDATION.md — "Sampling Rate" (phase gate: full CI matrix green + differential green before tag)
  </read_first>
  <files>../pollen/ (origin remote + pushed branch)</files>
  <action>
    Create the remote repo with `gh repo create bantuson/pollen --public --source ../pollen --remote origin --push` (or, if `gh repo create` cannot take the existing worktree, create the empty repo then `git -C ../pollen remote add origin https://github.com/bantuson/pollen.git` and `git -C ../pollen push -u origin HEAD`). Do NOT disturb the `upstream` remote added in plan 01. Push the default branch so CI fires.

    Wait for the CI workflow run on the pushed branch and confirm it is green: use `gh run list --repo bantuson/pollen` and `gh run watch` (or `gh run view`) to confirm the 3-OS matrix (ubuntu/macos/windows), the differential job (Linux+macOS), selftest, race tests, vet, and govulncheck all pass. If CI is red, STOP and surface the failing job + logs — do not proceed to tagging on a red matrix (the whole point of the phase is green guards before code lands).

    If `gh` returns an auth error at any step, that surfaces an authentication gate — report it so the user can `gh auth login`, then retry. Do not fabricate success.
  </action>
  <verify>
    <automated>gh repo view bantuson/pollen --json nameWithOwner -q .nameWithOwner && gh run list --repo bantuson/pollen --limit 1 --json conclusion -q '.[0].conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view bantuson/pollen` succeeds — the remote repo exists with the pollen code pushed.
    - The latest CI run conclusion is `success`: `gh run list --repo bantuson/pollen --limit 1 --json conclusion -q '.[0].conclusion'` prints `success`.
    - The CI run includes passing jobs on `windows-latest`, `macos-latest`, `ubuntu-latest`, plus the differential and govulncheck jobs (inspect with `gh run view <id>`); none are skipped except the intentional, structured Windows differential skip.
    - The `upstream` remote is still present in `../pollen` (`git -C ../pollen remote get-url upstream` works) and `origin` points at `github.com/bantuson/pollen`.
  </acceptance_criteria>
  <done>The github.com/bantuson/pollen repo exists with the code pushed and the full CI matrix (including the differential guard) green — the phase gate for tagging is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Tag v0.1.1-pollen.1 and trigger the signed release pipeline</name>
  <read_first>
    - ../pollen/.github/workflows/release.yml (the v* trigger + cosign + SLSA jobs from plan 04)
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "release.yml for Pollen"
  </read_first>
  <files>../pollen/ (annotated tag v0.1.1-pollen.1, pushed)</files>
  <action>
    Confirm `../pollen/VERSION` reads `0.1.1-pollen.1` (set in plan 02) so the release version matches the tag. Create an annotated tag on the green HEAD: `git -C ../pollen tag -a v0.1.1-pollen.1 -m "Pollen v0.1.1-pollen.1 — bounded Apache-2.0 fork of perplexityai/bumblebee @ c24089804ee66ece4bec6f14638cb98985389cdb; fork hygiene proven, no Windows code yet"`. Push it: `git -C ../pollen push origin v0.1.1-pollen.1`. This triggers release.yml (goreleaser build + cosign sign-blob + syft CycloneDX SBOM + SLSA L3 provenance).

    Watch the release run with `gh run watch --repo bantuson/pollen` (the run triggered by the tag). If the release pipeline fails (e.g. cosign OIDC, syft, or the SLSA generator), STOP and surface the failing job + logs — a failed signing pipeline is a FORK-03 blocker, not something to retry blindly.
  </action>
  <verify>
    <automated>git -C ../pollen tag --list | grep -qx "v0.1.1-pollen.1" && gh release view v0.1.1-pollen.1 --repo bantuson/pollen --json tagName -q .tagName</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ../pollen tag --list` contains `v0.1.1-pollen.1`.
    - The tag is pushed and triggered a release workflow run: `gh run list --repo bantuson/pollen --workflow release.yml --limit 1 --json conclusion -q '.[0].conclusion'` prints `success`.
    - `gh release view v0.1.1-pollen.1 --repo bantuson/pollen` shows the release exists with uploaded assets.
    - The release assets include `checksums.txt`, `checksums.txt.sigstore.json` (cosign bundle), per-archive `*.cdx.json` CycloneDX SBOMs, and the SLSA provenance attestation (`*.intoto.jsonl`).
  </acceptance_criteria>
  <done>The v0.1.1-pollen.1 tag is pushed, the release pipeline ran green, and the release carries checksums + cosign signature + CycloneDX SBOMs + SLSA provenance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the published signature + SBOM (FORK-03 + SDEF-02 final proof)</name>
  <what-built>
    Plans 01–04 produced the forked, attributed, reproducibly-built pollen repo with the differential + selftest CI guards and the cosign/SLSA/SBOM release pipeline. Tasks 1–2 of this plan pushed the code, got a green 3-OS CI matrix, and cut the signed `v0.1.1-pollen.1` release. This checkpoint confirms the published artifacts actually verify — the one thing that can only be proven against the real, server-side-signed release.
  </what-built>
  <how-to-verify>
    1. Download the release artifacts: `gh release download v0.1.1-pollen.1 --repo bantuson/pollen` (gets checksums.txt, checksums.txt.sigstore.json, the *.cdx.json SBOMs, archives).
    2. Verify the cosign signature against the pollen OIDC identity (this VALIDATES RESEARCH Assumption A1 — the exact cert identity string):
       `cosign verify-blob --bundle checksums.txt.sigstore.json --certificate-oidc-issuer https://token.actions.githubusercontent.com --certificate-identity-regexp "^https://github.com/bantuson/pollen/" checksums.txt`
       Expected: `Verified OK`.
    3. Inspect a `*.cdx.json` SBOM and confirm it is valid CycloneDX JSON and records the source upstream commit (the pinned SHA `c24089804ee66ece4bec6f14638cb98985389cdb`) — SDEF-02.
    4. Confirm the SLSA provenance attestation (`*.intoto.jsonl`) is attached to the release.
    5. (Two-account discipline) Confirm the tag/release approval followed the documented two-account step.
    If cosign reports the identity did not match the regexp, capture the ACTUAL `Subject`/identity string from the cert — that is the A1 correction to fold into docs/THREAT-MODEL.md and CHANGES.md.
  </how-to-verify>
  <files>downloaded release assets: checksums.txt, checksums.txt.sigstore.json, *.cdx.json, *.intoto.jsonl</files>
  <action>Download the v0.1.1-pollen.1 release assets, run the cosign verify-blob command against the bantuson/pollen OIDC identity, inspect a *.cdx.json SBOM for the pinned upstream SHA, and confirm the SLSA provenance attestation is attached. This is a human-verified gate because the signing pipeline only runs server-side; the operator confirms the published artifacts verify.</action>
  <verify>cosign verify-blob --bundle checksums.txt.sigstore.json --certificate-oidc-issuer https://token.actions.githubusercontent.com --certificate-identity-regexp "^https://github.com/bantuson/pollen/" checksums.txt  (expected: Verified OK); and a *.cdx.json SBOM contains the pinned SHA c24089804ee66ece4bec6f14638cb98985389cdb</verify>
  <done>cosign reports Verified OK against the bantuson/pollen identity, the CycloneDX SBOM records the pinned upstream SHA, and the SLSA provenance attestation is present on the release.</done>
  <resume-signal>Type "approved" if the cosign signature verifies OK and the CycloneDX SBOM records the pinned upstream SHA; otherwise describe the failure (especially the actual cosign cert identity string if the regexp did not match).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tag push → server-side signing pipeline | The signed release identity is asserted by GitHub Actions OIDC; the operator must verify the resulting cert. |
| published release → downstream operator/beekeeper | Consumers trust the cosign signature + SBOM to confirm artifact integrity and provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Spoofing | A release signed by an unexpected identity (wrong workflow/repo) would pass naive verification | mitigate | Task 3 verifies the cosign cert identity is anchored to ^https://github.com/bantuson/pollen/ and captures the actual identity string, validating A1 before any downstream trust is placed. |
| T-01-18 | Repudiation | Releasing without provenance/SBOM | mitigate | Task 2 asserts the release carries checksums.txt.sigstore.json, *.cdx.json, and *.intoto.jsonl; Task 3 confirms the SBOM records the pinned upstream SHA (SDEF-02). |
| T-01-19 | Elevation of Privilege | A single compromised account cutting an unauthorized signed release | mitigate | Two-account release-approval discipline at the tag step (release_facts); the human checkpoint confirms it was followed. |
| T-01-20 | Tampering | Tagging on a red CI matrix (shipping unverified guards) | mitigate | Task 1 blocks tagging until the full 3-OS matrix + differential are green; Task 2 stops on a failed release run. |
</threat_model>

<verification>
- github.com/bantuson/pollen exists; code pushed; full CI matrix + differential green.
- v0.1.1-pollen.1 tag pushed; release pipeline green.
- Release carries checksums + cosign sigstore bundle + CycloneDX SBOMs + SLSA provenance.
- cosign verify-blob returns Verified OK against the bantuson/pollen identity; SBOM records the pinned upstream SHA.
</verification>

<success_criteria>
- FORK-03 (final): a reproducibly-built, cosign-signed v0.1.1-pollen.1 release exists and verifies — fork hygiene proven before any Windows code.
- SDEF-02 (published): a CycloneDX SBOM recording the upstream commit is attached to the release.
- The phase ends green: the differential + selftest guards protect every subsequent Windows-code phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-setup-discipline/01-05-SUMMARY.md` recording: the release URL, the ACTUAL cosign cert identity string (A1 resolution — note whether it matched the expected regexp), confirmation the CycloneDX SBOM records the pinned SHA, the SLSA provenance attestation name, and any correction needed to docs/THREAT-MODEL.md's identity regexp. Also note the phase is complete and the next step is `/gsd-verify-work 1`.
</output>
