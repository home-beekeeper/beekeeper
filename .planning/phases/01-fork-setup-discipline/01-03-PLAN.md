---
phase: 01-fork-setup-discipline
plan: 03
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - "../pollen/cmd/pollen/differential_test.go"
  - "../pollen/cmd/pollen/normalize_diff.go"
  - "../pollen/cmd/pollen/testdata/diff-fixture/**"
autonomous: true
requirements: [PTEST-02, PTEST-03]
must_haves:
  truths:
    - "A normalization harness strips the 7 non-deterministic NDJSON fields and sorts records by record_id"
    - "The differential guard is named exactly `func TestDifferential(` in package main under ./cmd/pollen/ — the name is LOCKED so plan 04's CI can invoke `go test ./cmd/pollen/ -run '^TestDifferential$'` deterministically"
    - "TestDifferential asserts pollen and upstream bumblebee produce identical NDJSON after normalization, on a fixed fixture"
    - "TestDifferential builds the upstream bumblebee binary from the pinned SHA, not a moving version"
    - "TestDifferential skips with a structured reason on Windows (differential is Linux+macOS only); on a CI Linux runner (CI=true) a clone/build failure is a TEST FAILURE, never a skip"
    - "pollen selftest still emits exactly 3 findings (regression guard)"
  artifacts:
    - path: "../pollen/cmd/pollen/normalize_diff.go"
      provides: "NDJSON normalization (strip non-det fields + sort by record_id)"
      contains: "record_id"
    - path: "../pollen/cmd/pollen/differential_test.go"
      provides: "TestDifferential — the load-bearing PTEST-02 guard (name LOCKED)"
      contains: "func TestDifferential("
    - path: "../pollen/cmd/pollen/testdata/diff-fixture"
      provides: "Fixed multi-ecosystem fixture for the differential comparison"
  key_links:
    - from: "../pollen/cmd/pollen/differential_test.go"
      to: "upstream bumblebee @ c24089804ee66ece4bec6f14638cb98985389cdb"
      via: "git clone + checkout pinned SHA + go build ./cmd/bumblebee in the test"
      pattern: "c24089804ee66ece4bec6f14638cb98985389cdb"
    - from: "../pollen/cmd/pollen/differential_test.go"
      to: "../pollen/cmd/pollen/normalize_diff.go"
      via: "normalize() applied to both outputs before byte comparison"
      pattern: "normalize"
---

<objective>
Build the differential test harness (PTEST-02) — the single most load-bearing guard in this milestone. Raw NDJSON comparison between `pollen` and upstream `bumblebee` ALWAYS fails because upstream emits 7 non-deterministic fields and orders package records by concurrent-worker completion. This plan implements the normalization harness (strip the 7 fields, sort by the stable `record_id` SHA-256 key) and the `TestDifferential` that runs both binaries on a fixed fixture and asserts byte-for-byte equality AFTER normalization. It also adds the selftest regression assertion (PTEST-03) so the 3-finding contract is locked.

Purpose: Every later Windows-code phase (2–4) relies on this guard to prove Pollen has not drifted from upstream behavior on Linux/macOS. If this harness is wrong, all downstream parity claims are worthless. TDD is mandatory here: write the normalization tests first (known input → known stripped+sorted output), then implement. The differential test name is LOCKED to `TestDifferential` so plan 04's CI can invoke it by exact regexp with no name-drift risk across the 01-03 → 01-04 boundary.

Output: `normalize_diff.go` (normalization), `differential_test.go` (`func TestDifferential(` + selftest regression), and a fixed `testdata/diff-fixture/` — committed to the pollen repo, green on the dev host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<cross_repo_execution_model>
CRITICAL: CWD is beekeeper; Pollen is a SEPARATE repo at `../pollen`, OUTSIDE beekeeper's worktree. Write all new files under `../pollen/cmd/pollen/`. Beekeeper auto-commit does NOT capture pollen files — commit in the pollen repo via `git -C ../pollen add ... && git -C ../pollen commit ...`. Only this PLAN.md / SUMMARY.md are beekeeper commits.

Depends on plan 01 (Wave 0): the renamed `cmd/pollen/` package must exist. Touches ONLY new files under `cmd/pollen/` (differential_test.go, normalize_diff.go, testdata/) — disjoint from plan 02 (root attribution files) so the two run in parallel. Network is required (the test clones upstream to build the comparison binary).
</cross_repo_execution_model>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fork-setup-discipline/01-CONTEXT.md
@.planning/phases/01-fork-setup-discipline/01-RESEARCH.md
@.planning/phases/01-fork-setup-discipline/01-VALIDATION.md

<normalization_contract>
<!-- LOAD-BEARING. From 01-RESEARCH.md "NDJSON Determinism Analysis" + 01-VALIDATION.md. Verified against upstream model.go / endpoint.go / main.go / scanner.go. -->
Fields to STRIP before comparison (7 total):
  per-scan (4):  run_id, scan_time, end_time, duration_ms
  per-machine endpoint sub-fields (3):  endpoint.hostname, endpoint.username, endpoint.uid
Sort key after stripping:  record_id  (upstream's StableID() — SHA-256 of record_type+ecosystem+package_name+version+source paths; same input => same hash). This is the safe, deterministic sort anchor.
Deterministic fields that MUST match (do NOT strip): record_id, record_type, schema_version (always "0.1.0"), ecosystem, package_name, version, source_file, finding fields (catalog_id, severity).

Why ordering is non-deterministic: upstream scanner spawns 4 goroutines over a 256-item buffered channel; package records emit in worker-completion order, not filesystem order. Two runs on identical input differ in record order. Sorting by record_id removes this.

Environment control for the fixture run (so root discovery + endpoint do not vary): pass an explicit `--root <fixture-dir>` (do NOT let it discover a dynamic home dir); use a profile that honors explicit root (deep/project per upstream flag semantics). If upstream exposes a device-id env override flag, pin it to a fixed value so endpoint.device_id is stable too (or strip it if it is non-deterministic in practice — verify against actual output).
</normalization_contract>

<upstream_binary_source>
<!-- Claude's Discretion per CONTEXT.md resolved to Option A (build from pinned commit), per RESEARCH "Obtaining the Upstream Bumblebee Binary in CI". -->
Build upstream from the pinned SHA inside the test (or a t.Helper):
  git clone https://github.com/perplexityai/bumblebee.git <tmp>
  git -C <tmp> checkout c24089804ee66ece4bec6f14638cb98985389cdb
  go build -o <tmp>/bumblebee-bin ./cmd/bumblebee   (run with <tmp> as dir)
Rationale: the differential test proves pollen matches the PINNED upstream commit, not any later bumblebee.

Skip-vs-fail semantics (a clone/build FAILURE must be distinguishable from "no network on a developer box"):
- Use the runner context signal `os.Getenv("CI") == "true"` to tell a GitHub-hosted runner apart from a local offline box.
- If CI != "true" (developer machine, network optional): a clone/build failure → `t.Skip("upstream clone unavailable offline; differential runs in CI")`. Acceptable to skip locally.
- If CI == "true" (GitHub-hosted ubuntu-latest, network EXPECTED): a clone/build failure → `t.Fatalf(...)` — a TEST FAILURE, never a skip. This guarantees that if upstream is moved/deleted/unreachable, the differential guard fails loudly in CI instead of silently vanishing behind a "normal offline skip".
- Document this skip-vs-fail rule in a code comment at the clone site so the asymmetry is intentional and reviewable.
ALTERNATIVE (acceptable, executor's call): cache the upstream `bumblebee` binary as testdata built at the pinned SHA, eliminating the network dependency entirely. If you take this route, the cached binary MUST be the pinned SHA `c24089804ee66ece4bec6f14638cb98985389cdb` build, and a missing/corrupt cached binary is still a CI failure (not a skip) when CI == "true".
</upstream_binary_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and unit-test the NDJSON normalization harness (normalize_diff.go)</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "NDJSON Determinism Analysis", "Differential Harness Design", "Code Examples: NDJSON non-deterministic fields"
    - .planning/phases/01-fork-setup-discipline/01-VALIDATION.md — "Differential-test normalization contract"
    - ../pollen/internal/model/model.go (the record struct + StableID/record_id field name — confirm the JSON key is `record_id`)
    - ../pollen/internal/endpoint/endpoint.go (confirm the endpoint sub-field JSON keys: hostname/username/uid)
  </read_first>
  <files>../pollen/cmd/pollen/normalize_diff.go</files>
  <behavior>
    - Test 1: normalize() on a 1-line NDJSON record removes keys run_id, scan_time, end_time, duration_ms and nested endpoint.hostname/username/uid; all other keys (record_id, record_type, ecosystem, package_name, version, schema_version) are preserved unchanged.
    - Test 2: normalize() on a 3-record NDJSON stream whose lines are in REVERSE record_id order returns records sorted ascending by record_id (proves the sort defeats worker-completion ordering).
    - Test 3: two NDJSON streams that differ ONLY in the 7 stripped fields and in record order normalize to byte-identical results (the core PTEST-02 invariant).
    - Test 4: a record MISSING a record_id (malformed) is surfaced as an error, not silently sorted to an arbitrary position (fail-closed on bad input).
  </behavior>
  <action>
    Implement `normalize(ndjson []byte) ([]byte, error)` (and any helper types) in `../pollen/cmd/pollen/normalize_diff.go`, package `main`. Parse each non-empty line as a generic JSON object (`map[string]any` is acceptable — schema-version is frozen at 0.1.0 so a struct is also fine, but a map keeps it robust to additive upstream fields). Delete the 4 top-level keys (run_id, scan_time, end_time, duration_ms) and the 3 nested keys under the `endpoint` object (hostname, username, uid). Sort the surviving records ascending by the `record_id` string value; return an error if any record lacks a `record_id`. Re-marshal with sorted map keys (Go's `json.Marshal` already sorts map keys deterministically) and join with `\n` so the output is byte-stable. Write the table-driven tests described in <behavior> in `normalize_diff.go`'s sibling — put them inside `differential_test.go` (Task 2) OR a `normalize_diff_test.go`; if separate, that file is also owned by this plan. RED first: write the failing tests, then implement until green.

    Commit: `git -C ../pollen add cmd/pollen/normalize_diff.go cmd/pollen/normalize_diff_test.go && git -C ../pollen commit -m "test(pollen): NDJSON normalization harness — strip 7 non-det fields + sort by record_id [PTEST-02]"` (RED then GREEN; squash or two commits are both fine).
  </action>
  <verify>
    <automated>cd ../pollen && go test -run 'TestNormalize' -count=1 ./cmd/pollen/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run TestNormalize -count=1 ./cmd/pollen/` passes (in ../pollen).
    - `normalize_diff.go` deletes exactly the 7 documented keys and no others — verify by grep that the strip list contains `run_id`, `scan_time`, `end_time`, `duration_ms`, `hostname`, `username`, `uid` and that the sort key is `record_id`.
    - The malformed-record (missing record_id) test asserts a non-nil error (fail-closed), not a silent pass.
    - Two streams differing only in stripped fields + order produce byte-identical normalize() output (Test 3 green).
  </acceptance_criteria>
  <done>normalize_diff.go implemented and unit-green: strips the 7 non-deterministic fields, sorts by record_id, fails closed on records missing record_id — committed to the pollen repo.</done>
</task>

<task type="auto">
  <name>Task 2: Build the fixed fixture and the LOCKED-name TestDifferential (pollen == upstream after normalization) + selftest regression</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "Obtaining the Upstream Bumblebee Binary in CI" (Option A), "Pitfall 4: Differential Test Fails Due to Record Ordering", "Differential Harness Design" (fixture env control)
    - ../pollen/cmd/pollen/selftest/fixtures/ (mirror the shape of upstream's npm/pypi/mcp fixtures for the diff fixture)
    - ../pollen/cmd/pollen/main.go (confirm the scan subcommand + --root / --profile flag names so the test invokes the binary correctly)
  </read_first>
  <files>../pollen/cmd/pollen/differential_test.go, ../pollen/cmd/pollen/testdata/diff-fixture/**</files>
  <action>
    Create a small, FIXED fixture tree under `../pollen/cmd/pollen/testdata/diff-fixture/` containing at least one npm, one pypi, and one mcp package layout (model it on the embedded selftest fixtures so both binaries detect the same packages). This fixture is committed so the comparison is reproducible across machines and CI.

    Implement the differential guard with the EXACT, LOCKED signature `func TestDifferential(t *testing.T)` in `differential_test.go` (package main). The name is NON-NEGOTIABLE — plan 04's CI invokes `go test ./cmd/pollen/ -run '^TestDifferential$'`, so do NOT rename, suffix, or parameterize it (no `TestDifferentialNDJSON`, no `TestDifferential_Linux`). Body:
    1. `if runtime.GOOS == "windows" { t.Skip("PTEST-02 differential runs on Linux+macOS only; Windows behavior arrives Phase 2 (v0.1.1-pollen.2)") }` — structured skip, never silent.
    2. Build the upstream bumblebee binary from the PINNED SHA per <upstream_binary_source> (clone → checkout c24089804ee66ece4bec6f14638cb98985389cdb → `go build ./cmd/bumblebee`). Apply the skip-vs-fail rule from <upstream_binary_source>: if `os.Getenv("CI") == "true"` a clone/build failure is `t.Fatalf` (TEST FAILURE — upstream-moved/unreachable must fail loudly in CI, never masquerade as an offline skip); only when CI != "true" (a developer's offline box) may a clone failure `t.Skip` with a clear network reason. Add a code comment at the clone site documenting this asymmetry.
    3. Build (or `go run`) the pollen binary from `./cmd/pollen`.
    4. Run BOTH against the fixed fixture with an explicit `--root <testdata/diff-fixture>` and a profile that honors explicit root, capturing stdout NDJSON. Pin any device-id env override to a fixed value if upstream supports it.
    5. `normalize()` BOTH outputs (Task 1) and assert byte-for-byte equality. On mismatch, print a unified diff of the normalized streams so failures are debuggable.

    Add `TestSelftestThreeFindings` (or assert within an existing test): run `pollen selftest` (via the built binary or the in-process selftest entry) and assert exit 0 with exactly 3 findings (npm, pypi, mcp) — the PTEST-03 regression guard. This must run on ALL OSes (selftest uses explicit temp-dir roots, not the system resolver — Pitfall 6), so do NOT Windows-skip the selftest assertion.

    Commit: `git -C ../pollen add cmd/pollen/differential_test.go cmd/pollen/testdata && git -C ../pollen commit -m "test(pollen): TestDifferential vs pinned upstream (name locked; CI clone-fail is fatal) + selftest 3-finding regression [PTEST-02][PTEST-03]"`.
  </action>
  <verify>
    <automated>cd ../pollen && grep -q "func TestDifferential(" cmd/pollen/differential_test.go && go test ./cmd/pollen/ -run '^TestDifferential$' -count=1 -v && go test -run 'TestSelftest' -count=1 -v ./cmd/pollen/</automated>
  </verify>
  <acceptance_criteria>
    - NAME LOCK (hard criterion): `grep -q "func TestDifferential(" ../pollen/cmd/pollen/differential_test.go` matches AND `go test ./cmd/pollen/ -run '^TestDifferential$'` selects exactly one test. The differential guard is named exactly `TestDifferential` in package main under `./cmd/pollen/` — no rename, no suffix, no variant. This is the name plan 04's CI invokes verbatim.
    - `go test ./cmd/pollen/ -run '^TestDifferential$' -count=1` passes on the dev host (Linux/macOS path) OR skips ONLY for a structured reason (Windows, or clone-offline with CI != "true") — never a silent pass. On the Windows dev machine it skips with the Phase-2 reason; confirm the skip message contains `v0.1.1-pollen.2`.
    - CI-FAIL-LOUD (hard criterion): the test distinguishes CI from offline-dev via `os.Getenv("CI")` — `grep -q 'os.Getenv("CI")' ../pollen/cmd/pollen/differential_test.go` matches, and on a clone/build failure with CI=="true" the code path is `t.Fatalf`/`t.Fatal` (a TEST FAILURE), NOT `t.Skip`. A code comment at the clone site documents the skip-vs-fail asymmetry. (If the cached-binary alternative is used instead, the cached binary is the pinned-SHA build and a missing/corrupt cache is `t.Fatal` when CI=="true".)
    - `grep -q "c24089804ee66ece4bec6f14638cb98985389cdb" ../pollen/cmd/pollen/differential_test.go` — the test pins upstream to the exact SHA (not a tag/branch/version).
    - `grep -q 'runtime.GOOS == "windows"' ../pollen/cmd/pollen/differential_test.go` and the skip string references Phase 2 — the structured Windows skip is present.
    - `../pollen/cmd/pollen/testdata/diff-fixture/` contains npm, pypi, and mcp package layouts (committed).
    - `go test -run TestSelftest -count=1 ./cmd/pollen/` passes on the host and asserts exactly 3 findings (PTEST-03 regression); this test is NOT Windows-skipped.
    - The test calls `normalize` on both outputs before comparison (`grep -c "normalize" ../pollen/cmd/pollen/differential_test.go` ≥ 2).
  </acceptance_criteria>
  <done>TestDifferential (name LOCKED) builds upstream from the pinned SHA, runs both binaries on a fixed fixture, normalizes both outputs, and asserts byte-equality; a clone failure is fatal in CI (CI=="true") but a skip only on an offline dev box; the selftest 3-finding regression guard runs on all OSes — all committed to the pollen repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream binary build (in-test clone) → differential assertion | The test fetches upstream code over the network to build the comparison oracle. |
| pollen output behavior → downstream parity claims | This harness is the evidence every later phase cites to prove no behavioral drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | The in-test upstream clone could pull a moving/compromised newer commit, making the oracle wrong | mitigate | The test checks out the explicit pinned SHA `c24089804ee66ece4bec6f14638cb98985389cdb` (asserted by grep gate), never a branch/tag-latest. |
| T-01-10 | Repudiation | A silently-passing or silently-vanishing differential test would mask real drift (false assurance) | mitigate | Skip paths are asymmetric: Windows + offline-dev (CI != "true") use structured `t.Skip`; in CI (CI == "true") a clone/build failure is `t.Fatal`, so upstream-moved/unreachable fails loudly instead of looking like a normal offline skip. A normalize() mismatch surfaces a unified diff. Never a silent pass. |
| T-01-11 | Tampering | Hidden non-deterministic field added by a future upstream sync would falsely fail (or falsely pass) the diff | accept | The map-based normalize tolerates additive fields; if upstream adds a NEW non-deterministic field in a Phase-5 sync, the differential test will fail loudly and the strip-list is updated then — a deliberate fail-loud, not a Phase-1 risk. |
</threat_model>

<verification>
- TestNormalize green: 7 fields stripped, sorted by record_id, fail-closed on missing record_id.
- TestDifferential (name LOCKED, selectable by `-run '^TestDifferential$'`) pins upstream to the SHA, normalizes both outputs, asserts equality; structured skips only locally — a CI clone failure is fatal.
- TestSelftest asserts exactly 3 findings on all OSes (no Windows skip).
- All new files committed in the pollen repo.
</verification>

<success_criteria>
- PTEST-02: the differential harness exists and proves pollen == pinned-upstream NDJSON (after normalization) on Linux/macOS; named exactly `TestDifferential` so plan 04's CI invokes it via `go test ./cmd/pollen/ -run '^TestDifferential$'` with zero name-drift risk; a CI clone/build failure fails the job rather than skipping.
- PTEST-03: the selftest 3-finding contract is locked by an automated regression test runnable on all 3 OSes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-setup-discipline/01-03-SUMMARY.md` recording: that the differential test name is exactly `TestDifferential` in `./cmd/pollen/` (the name plan 04's CI must invoke), the exact scan flags used to make output deterministic (--root/--profile/device-id), whether endpoint.device_id needed stripping in practice, the fixture layout, the selftest test name(s), and any upstream JSON-key surprises (e.g. if the record_id key was not literally `record_id`).
</output>
</output>
