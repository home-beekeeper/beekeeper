---
phase: 01-fork-setup-discipline
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - "../pollen/LICENSE"
  - "../pollen/NOTICE"
  - "../pollen/CHANGES.md"
  - "../pollen/UPSTREAM.md"
  - "../pollen/README.md"
  - "../pollen/VERSION"
  - "../pollen/threat_intel/README.md"
autonomous: true
requirements: [FORK-02, FORK-04]
must_haves:
  truths:
    - "LICENSE is the verbatim upstream Apache-2.0 text, unmodified"
    - "NOTICE attributes Perplexity/Bumblebee, states non-affiliation, and points general users upstream"
    - "UPSTREAM.md records the 40-char pinned SHA, tag, date, verifier, and the sync workflow"
    - "CHANGES.md documents every delta from the pinned commit under Added/Renamed/Modified/Removed"
    - "VERSION file contains 0.1.1-pollen.1"
    - "threat_intel/ ships empty except a README noting catalogs flow via beekeeper catalogs sync"
    - "The full-repo FORK-04 trademark audit passes: bumblebee appears ONLY in attribution contexts"
  artifacts:
    - path: "../pollen/UPSTREAM.md"
      provides: "Pinned-commit record + sync workflow"
      contains: "c24089804ee66ece4bec6f14638cb98985389cdb"
    - path: "../pollen/NOTICE"
      provides: "Attribution + non-affiliation statement"
    - path: "../pollen/VERSION"
      provides: "Version string"
      contains: "0.1.1-pollen.1"
    - path: "../pollen/CHANGES.md"
      provides: "Delta log from the pinned commit"
  key_links:
    - from: "../pollen/README.md"
      to: "upstream perplexityai/bumblebee"
      via: "derived-from attribution paragraph pointing general users upstream"
      pattern: "derived from|perplexityai/bumblebee"
---

<objective>
Add the legal, attribution, and version files that make Pollen a compliant Apache-2.0 derivative, and run the full-repo FORK-04 trademark audit. This plan delivers FORK-02 (LICENSE/NOTICE/CHANGES.md/UPSTREAM.md with the pinned SHA) and completes FORK-04 (the trademark discipline that plan 01 started inside `cmd/`, now verified across the WHOLE repo including the new attribution files).

Purpose: A bounded fork is only legitimate if attribution is exact and trademark use is disciplined (Apache-2.0 §6 grants no trademark rights). UPSTREAM.md is also the load-bearing record the Phase 5 sync workflow keys off — it must carry the verified 40-char SHA, tag, date, and the documented sync procedure now, even though sync EXECUTION is deferred.

Output: `LICENSE`, `NOTICE`, `CHANGES.md`, `UPSTREAM.md`, `README.md`, `VERSION`, and `threat_intel/README.md` committed to the pollen repo, with the full-repo trademark grep gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<cross_repo_execution_model>
CRITICAL: CWD is beekeeper; Pollen is a SEPARATE repo at `../pollen`, OUTSIDE beekeeper's worktree. Use explicit `../pollen/...` paths for all file writes. Beekeeper auto-commit does NOT capture pollen files — stage+commit in the pollen repo via `git -C ../pollen add ... && git -C ../pollen commit ...`. Only this PLAN.md / SUMMARY.md are beekeeper commits.

This plan depends on plan 01 (Wave 0): the pollen repo must already exist with the cmd/module rename done. It touches ONLY root attribution files + threat_intel/README.md — disjoint from plan 03 (cmd/pollen test code) so the two run in parallel.
</cross_repo_execution_model>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fork-setup-discipline/01-CONTEXT.md
@.planning/phases/01-fork-setup-discipline/01-RESEARCH.md

<canonical_source_refs>
<!-- The EXACT NOTICE text and CHANGES.md format come from the PRD, NOT improvised. -->
- beekeeper-m2-prd.md §7.2 — the exact NOTICE text (attributes Perplexity/Bumblebee, states non-affiliation, points general users to upstream, references github.com/bantuson/beekeeper). Read this section and reproduce its NOTICE verbatim.
- beekeeper-m2-prd.md §7.3 — the CHANGES.md format (Added / Renamed / Modified / Removed sections).
- beekeeper-m2-prd.md §6.2 — the upstream sync workflow that UPSTREAM.md must document (git remote update upstream, diff-review, run upstream tests, cherry-pick preserving Windows code, re-run differential, bump pinned commit + CHANGES.md, tag v0.1.1-pollen.N).
</canonical_source_refs>

<pinned_facts>
- Pinned SHA: c24089804ee66ece4bec6f14638cb98985389cdb
- Pinned tag: v0.1.1 ; pinned date: 2026-05-22 ; verified by: bantuson
- Upstream: github.com/perplexityai/bumblebee
- VERSION value: 0.1.1-pollen.1
- LICENSE: upstream's Apache-2.0 file already present in ../pollen from the clone — keep it VERBATIM, do not regenerate or reformat.
- threat_intel/ ships EMPTY except a README (catalogs flow via beekeeper catalogs sync; the --exposure-catalog CLI surface stays for compat but empty by default).
</pinned_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write UPSTREAM.md, CHANGES.md, NOTICE, and verify the verbatim LICENSE (FORK-02)</name>
  <read_first>
    - beekeeper-m2-prd.md §7.2 (exact NOTICE text), §7.3 (CHANGES.md format), §6.2 (sync workflow)
    - .planning/phases/01-fork-setup-discipline/01-RESEARCH.md — "Pattern: UPSTREAM.md content for FORK-02", "Code Examples"
    - ../pollen/LICENSE (confirm it is upstream's Apache-2.0 verbatim after the clone)
    - .planning/phases/01-fork-setup-discipline/01-01-SUMMARY.md (the deltas plan 01 made — feed these into CHANGES.md)
  </read_first>
  <files>../pollen/UPSTREAM.md, ../pollen/CHANGES.md, ../pollen/NOTICE, ../pollen/LICENSE (verify only)</files>
  <action>
    Confirm `../pollen/LICENSE` is upstream's Apache-2.0 text unmodified (it came in via the clone; do NOT regenerate — a regenerated license can differ from upstream's exact bytes). If a NOTICE file already exists upstream, replace it with the PRD §7.2 text; otherwise create it. The NOTICE MUST: name Perplexity AI / Bumblebee as the origin, state Pollen is NOT affiliated with or endorsed by Perplexity, point general users to upstream `github.com/perplexityai/bumblebee`, and reference `github.com/bantuson/beekeeper` as the consumer — reproduce the §7.2 text verbatim, do not paraphrase.

    Write `UPSTREAM.md` with the front-matter block: `upstream: github.com/perplexityai/bumblebee`, `pinned commit: c24089804ee66ece4bec6f14638cb98985389cdb`, `pinned tag: v0.1.1`, `pinned date: 2026-05-22`, `verified by: bantuson`. Then document the §6.2 sync workflow as a numbered procedure (remote update, diff-review of new files / NDJSON schema / root resolver / LICENSE+NOTICE, run upstream tests on Linux+macOS, cherry-pick preserving Windows code paths, re-run the differential test, bump pinned commit + CHANGES.md, tag a new v0.1.1-pollen.N). State explicitly that sync EXECUTION is deferred to Phase 5; this file only DOCUMENTS the workflow now.

    Write `CHANGES.md` in the §7.3 Added/Renamed/Modified/Removed format, documenting the deltas from the pinned commit: Renamed (module path → github.com/bantuson/pollen; cmd/bumblebee → cmd/pollen); Modified (selftest temp prefix → pollen-selftest-; version fileDefault → 0.1.1-pollen.1; help/usage binary name → pollen); Added (will list NOTICE/UPSTREAM.md/CHANGES.md/VERSION + the differential harness + repro-build/release pipeline as those land — list what this phase adds). Pull the exact plan-01 deltas from 01-01-SUMMARY.md so nothing is missed.

    Commit: `git -C ../pollen add LICENSE NOTICE UPSTREAM.md CHANGES.md && git -C ../pollen commit -m "docs: Apache-2.0 attribution — NOTICE, UPSTREAM.md (pinned SHA + sync workflow), CHANGES.md [FORK-02]"`.
  </action>
  <verify>
    <automated>cd ../pollen && grep -q "c24089804ee66ece4bec6f14638cb98985389cdb" UPSTREAM.md && grep -q "v0.1.1" UPSTREAM.md && grep -q "verified by: bantuson" UPSTREAM.md && test -f LICENSE && test -f NOTICE && grep -qiE "Renamed|Modified|Added" CHANGES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep c24089804ee66ece4bec6f14638cb98985389cdb ../pollen/UPSTREAM.md` matches the full 40-char SHA (FORK-02 core assertion).
    - `../pollen/UPSTREAM.md` contains `pinned tag: v0.1.1`, `pinned date: 2026-05-22`, and `verified by: bantuson`.
    - `../pollen/UPSTREAM.md` documents the numbered sync workflow including the words `git remote update` (or `git remote -v upstream` equivalent), `diff`, `differential`, and `v0.1.1-pollen` (tag bump step).
    - `../pollen/LICENSE` is byte-identical to upstream's Apache-2.0 (it was not regenerated): `git -C ../pollen diff HEAD -- LICENSE` shows no change to LICENSE content from the cloned baseline, OR the clone's LICENSE blob hash is unchanged.
    - `../pollen/NOTICE` contains the strings `Perplexity`, `Bumblebee`, a non-affiliation statement (e.g. `not affiliated` / `not endorsed`), and `github.com/perplexityai/bumblebee`.
    - `../pollen/CHANGES.md` has Added / Renamed / Modified sections and names `cmd/bumblebee` → `cmd/pollen` and the module-path rename.
  </acceptance_criteria>
  <done>LICENSE confirmed verbatim, NOTICE matches PRD §7.2, UPSTREAM.md carries the pinned SHA + tag + date + verifier + sync workflow, CHANGES.md logs the deltas — all committed to the pollen repo.</done>
</task>

<task type="auto">
  <name>Task 2: Write README, VERSION, threat_intel/README; run the full-repo FORK-04 trademark audit</name>
  <read_first>
    - .planning/phases/01-fork-setup-discipline/01-CONTEXT.md — "Trademark discipline (FORK-04)", "threat_intel/ ships empty"
    - .planning/phases/01-fork-setup-discipline/01-VALIDATION.md — FORK-04 grep-audit row (the exact gate command)
    - ../pollen/README.md (the upstream README that came in via the clone — it WILL contain "Bumblebee" in headlines that must be reframed as attribution)
  </read_first>
  <files>../pollen/README.md, ../pollen/VERSION, ../pollen/threat_intel/README.md</files>
  <action>
    Write `../pollen/VERSION` containing exactly `0.1.1-pollen.1` (single line, no trailing prose).

    Rewrite `../pollen/README.md` for Pollen: the HEADLINE and project name are "Pollen" (never "Bumblebee" in the title/headers). Include exactly ONE attribution paragraph — a "derived from" sentence naming upstream `perplexityai/bumblebee`, stating Pollen is a bounded Windows-focused Apache-2.0 fork that exists to serve `github.com/bantuson/beekeeper`, and pointing GENERAL users to upstream Bumblebee. "Bumblebee" may appear ONLY inside that attribution paragraph. Remove or rename every other "bumblebee" occurrence (install commands → `go install github.com/bantuson/pollen/cmd/pollen@...`, usage examples → `pollen ...`, badges, headers).

    Create `../pollen/threat_intel/README.md` noting the directory ships EMPTY by design: catalogs flow through beekeeper's own `catalogs sync` (PRD §6.3 "reference" decision), and the `--exposure-catalog` CLI surface is preserved for compatibility but empty by default. Ensure no catalog JSON is committed under `threat_intel/` except whatever upstream selftest fixtures already live under `cmd/pollen/selftest/` (those are NOT in threat_intel/).

    Run the FORK-04 full-repo trademark audit gate and make it pass.

    Commit: `git -C ../pollen add README.md VERSION threat_intel/README.md && git -C ../pollen commit -m "docs: Pollen README (single attribution para), VERSION 0.1.1-pollen.1, empty threat_intel/ [FORK-04]"`.
  </action>
  <verify>
    <automated>cd ../pollen && grep -qx "0.1.1-pollen.1" VERSION && test -f threat_intel/README.md && bash -c 'OUT=$(grep -rIi "bumblebee" . --include="*.go" --include="*.yaml" --include="*.yml" --include="Makefile" -l 2>/dev/null | grep -v -i "selftest" ); echo "src-files-with-bumblebee:" $OUT; [ -z "$OUT" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `../pollen/VERSION` is exactly `0.1.1-pollen.1` (one line): `grep -qx "0.1.1-pollen.1" ../pollen/VERSION`.
    - FORK-04 source-code gate: `grep -rIi "bumblebee" ../pollen --include="*.go" --include="*.yaml" --include="*.yml" --include="Makefile" | grep -vi "selftest" | grep -v '^#'` returns NOTHING (no bumblebee command/binary/config strings in code; selftest fixture filenames are upstream data, allowed).
    - FORK-04 README gate: in `../pollen/README.md`, every "Bumblebee" / "bumblebee" occurrence sits inside the single attribution paragraph. Check: no markdown header line (`^#`) contains "bumblebee" — `grep -i "^#.*bumblebee" ../pollen/README.md` returns NOTHING; and install/usage code fences reference `pollen`, not `bumblebee` — `grep -i "go install.*bumblebee\|^\s*bumblebee " ../pollen/README.md` returns NOTHING.
    - `../pollen/threat_intel/README.md` exists and references `catalogs sync` and "empty"; `find ../pollen/threat_intel -name '*.json'` returns NOTHING.
    - `../pollen/README.md` contains a derived-from attribution paragraph naming `perplexityai/bumblebee` and pointing general users upstream.
  </acceptance_criteria>
  <done>README reframed with a single attribution paragraph, VERSION set, threat_intel empty-by-design with a README, and the full-repo FORK-04 trademark gate green — all committed to the pollen repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pollen repo → public consumers | Attribution/legal files are the public-facing trust + compliance surface of the fork. |
| pollen → upstream Bumblebee identity | Trademark use distinguishes (or dangerously conflates) the fork from upstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Spoofing | Trademark misuse implying upstream affiliation/endorsement | mitigate | FORK-04 full-repo grep gate (Task 2) confines "bumblebee" to the single README attribution paragraph + NOTICE/UPSTREAM.md; NOTICE explicitly states non-affiliation per PRD §7.2 (Apache-2.0 §6 grants no trademark rights). |
| T-01-06 | Repudiation | Undocumented provenance — which upstream commit was forked | mitigate | UPSTREAM.md records the verifiable 40-char SHA + tag + date + verifier; CHANGES.md logs every delta, so the provenance chain is auditable and the Phase 5 sync has a fixed baseline. |
| T-01-07 | Tampering | License integrity (a modified Apache-2.0 could strip obligations) | mitigate | LICENSE kept byte-verbatim from the clone (Task 1 explicitly forbids regeneration); diff against the cloned baseline confirms no drift. |
| T-01-08 | Information Disclosure | Accidentally vendoring upstream catalogs into the fork (data duplication + stale-intel risk) | mitigate | threat_intel/ ships empty with a README; `find threat_intel -name '*.json'` gate ensures no catalog JSON is committed — catalogs single-source through beekeeper. |
</threat_model>

<verification>
- LICENSE verbatim; NOTICE per §7.2; UPSTREAM.md with the pinned SHA + sync workflow; CHANGES.md delta log — all present and committed.
- VERSION is `0.1.1-pollen.1`.
- threat_intel/ empty except its README; no catalog JSON committed.
- Full-repo FORK-04 trademark gate green: "bumblebee" only in attribution (NOTICE, README paragraph, UPSTREAM.md).
</verification>

<success_criteria>
- FORK-02: LICENSE/NOTICE/CHANGES.md/UPSTREAM.md complete; UPSTREAM.md carries the verified 40-char SHA + tag + date + verifier + documented sync workflow.
- FORK-04 (complete): trademark discipline holds across the WHOLE repo, not just cmd/.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-setup-discipline/01-02-SUMMARY.md` recording: confirmation the LICENSE is verbatim (with the method used to confirm), the final NOTICE non-affiliation wording, the UPSTREAM.md sync-workflow steps captured, and the result of the full-repo FORK-04 grep gate (paste the empty result).
</output>
