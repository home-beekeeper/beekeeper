---
gsd_state_version: 1.0
milestone: v1.5.0
milestone_name: Install Posture
status: planning
last_updated: "2026-06-22T10:00:00.000Z"
last_activity: 2026-06-22
progress:
  total_phases: 0
  completed_phases: 0
  total_plans: 0
  completed_plans: 0
  percent: 0
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-15 -- v1.4.0 milestone close)

**Core value:** A hijacked or off-task agent cannot successfully act on the developer's machine without Beekeeper deciding to permit it.
**Current focus:** 🔨 Milestone **v1.5.0 "Install Posture"** ACTIVE (started 2026-06-21; ships publicly as release v1.1.0). Retire the package-manager nudge; ship tool-agnostic install posture (Layer 1 default posture at the hook + Layer 2 read-only `beekeeper posture` view + Layer 3 scoped audited overrides), honest about enforcement boundaries. Scope = `beekeeper-install-posture-prd.md` AC 1-8; phases 26-31; REQ-IDs in `.planning/REQUIREMENTS.md`. Two human gates: Gate 1 (enforcement-boundary review) after Phase 27; Gate 2 (release signing) after Phase 31. **Verified arch (memory install-posture-arch.md):** release-age + lifecycle already exist as pure policy evaluators wired to scan/watch but NOT the hook (hook runs evaluateNudge@internal/check/handler.go:381); git/remote-URL rule is new; preserve internal/nudge/detect.go + scanners.go for the Layer-2 view; shim already exists → keep+repoint+document-as-roadmap (maintainer-approved). NEXT: /gsd-plan-phase 26. Carried-forward (not in this milestone): v1.4.0 overlay wiring for gateway/scan/watch + PRD §6/§7 (org push / community feed); SITE-03 live Vercel deploy; CORR-F1. (v1.1.0 Pollen remains PARKED, independent; resume via docs/release-runbook.md.)

> ⏸ **v1.1.0 "Pollen" is PARKED, not closed** — paused at the 05-05 maintainer release checkpoint. To resume the release: see `.planning/phases/05-contribution-back-milestone-close/.continue-here.md` and `docs/release-runbook.md`. The four signed-tag releases remain in the "Deferred Items" table below. Do not archive v1.1.0 until the runbook is run + 05-05 Task 3 completes.

## Current Position

Phase: 31 ✅ COMPLETE & verified. ALL 6 phases (26-31) of v1.5.0 done; GATE 1 PASSED. Release-gate reviews done (security-engineer: all 8 invariants HELD → APPROVE; gsd-code-reviewer: 0 crit / 1 high / 2 med / 5 low / 2 info — the 1 HIGH H-01 FIXED, mediums/lows deferred with rationale in `31-REVIEW-DECISION.md`). **PRs OPEN: Go `home-beekeeper/beekeeper#19`, web `Bantuson/beekeeper-web#1`.** **AWAITING GATE 2 — the maintainer merges both PRs, then cuts + signs the v1.1.0 release tag (orchestrator cannot self-sign; this is the only remaining step).**
Plan: Phase 31 done — 7 SSH-signed commits (a1b4452/44aa666/35fff2b/f87241a/96ab635/11d383b/00d9f7d).
Status: Phase 31 ✅ COMPLETE & verified 2026-06-22 — added the install-posture test/coverage/E2E bar (zero production code changed; tests/docs/CI only): release-age exactly-24h→allow boundary + all 6 lifecycle script types fire; fetch-timeout fail-soft (warn under block mode); live-`RunCheck` block-mode for lifecycle + git-remote, WITH the `...DefaultWarnsNotBlock` attribution sibling proving the block comes from the opt-up not the rule; override `enforce --warn` record + ecosystem-scoped allow (pypi NOT exempt) + config-persistence round-trip; real-binary E2E git-remote warn-by-default / block-under-opt-up (asserts exit + audit decision + reason) + honest SENTRY-009 CI-only skip; CI `e2e` job wired into `release-gate.needs`; new `docs/posture-validation.md` coverage report naming 3 deliberate gaps; REL-01 confirmed (version is ldflags-injected; web v1.1.0 changelog exists; no roadmap item documented as shipped). Orchestrator INDEPENDENTLY re-verified: read all 7 new test bodies (genuine, behavior-asserting on the live path — not false-confidence) + re-ran build/vet/test/`-tags e2e`/coverage-floor/cross-vet (linux+darwin) all green. The one transient fail was the documented load-flaky p99 `TestBenchmarkRunCheckGate` (it ran concurrently with the coverage-profile build); confirmed passing isolated x2 + full internal/check suite clean. Phase 30 (prior) ✅ COMPLETE & verified 2026-06-22 (BOTH repos). **Go core** (this repo, commits 0b0bfcf plan / 02def02 docs): retired stale docs/nudge.md → docs/install-posture.md (3 rules + warn/fail-soft default + `beekeeper posture` view + scoped overrides allow/enforce + verbatim posture.BoundaryStatement + roadmap note), documented SENTRY-009 in THREAT-MODEL §8; build+vet+full `go test ./...` green (27 pkgs). **Web/docs** (SEPARATE repo beekeeper-web, commit a9016e5 on main): home "Hardened nudge" card → "Install posture"; ~10 docs reframed (configuration/cli-reference/getting-started/audit-log/troubleshooting/security/integration/dashboard + blog); dead source_doc paths to the DELETED cmd/beekeeper/nudge.go fixed so the accuracy gate is GREEN again (it was RED on this branch); SENTRY-001..008 → 001..009; boundary statement propagated; v1.0.0 badges → v1.1.0; new v1.1.0 changelog entry + roadmap note. Independently re-verified by orchestrator: accuracy 27/27 + EVERY source_doc resolves against ../beekeeper + unit 36/36 + pnpm build exit 0 + em-dash & phantom-`beekeeper nudge` greps clean. Reqs IPBND-02 + NMIG-03 + REL-01(prep) done. **Schema-freeze DECISION (locked 2026-06-22): KEEP the typed posture_override_* fields** (audit-only, never routed to the corpus StoreSink, frozen moat schema intact; forward-useful for a future adjudication_source: operator_override; CorpusSchemaVersion 1.0→1.1 bump is a future-milestone deliberate step — do NOT re-encode).
Last activity: 2026-06-22 — Drove the GSD loop AUTONOMOUSLY end-to-end: Phases 30 + 31 executed/verified/committed; then per maintainer direction ran BOTH a security review (security-engineer: all 8 load-bearing invariants HELD, APPROVE) and a code review (gsd-code-reviewer: 0 crit / 1 high / 2 med / 5 low / 2 info) over the full milestone diff (5d1db0b..HEAD); FIXED the one ship-gate H-01 (reject `posture allow --once --rule` so a one-shot token cannot record a rule scope it does not enforce; + `TestPostureAllowOnceRejectsRule`; full gate re-verified green); deferred the mediums/lows with rationale in `31-REVIEW-DECISION.md`; pushed `feat/install-posture-foundation` and opened **Go PR #19** to main plus a companion **web docs PR #1** (`Bantuson/beekeeper-web`). **STOPPED at Gate 2** — the maintainer merges both PRs, then cuts + signs the v1.1.0 release tag (the one step the orchestrator cannot do). Nothing else pending. Resume doc `.planning/HANDOFF-install-posture.md` is stale on the phase pointer — STATE is authoritative.

## Phase Summary (v1.5.0 — Install Posture, ships as v1.1.0)

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 26 | Nudge Removal & Posture Rule Foundation | NMIG-01/02/04, IPST-04/05 | ✅ Complete & verified 2026-06-21 (26-01 new git/remote-URL pure rule by orchestrator; 26-02 nudge removal + relocate pm-config readers→internal/posture + shim repoint by gsd-executor, independently re-verified: build+vet+full suite+fuzz green, grep gates clean, handler.go merge chain + layered.go untrusted invariants intact, frozen audit schema preserved). Commits 4451a1f, 347e3cd, fe1c692 |
| 27 | Layer 1 Hook Enforcement + Sentry Observation (**Gate 1**) | IPST-01/02/03/06, IPBND-01, NMIG-04 | ✅ COMPLETE — **GATE 1 PASSED 2026-06-21**. 27-01 posture hook adapter (warn default, fail-soft, latency-bounded, never blocks) + cached lifecycle adapter; 27-02 SENTRY-009 detection-only install observation (cross-platform) + canonical posture.BoundaryStatement; 27-03 shim MADE REAL (buildShimToolCall reconstructs the install so catalog+posture fire on shim installs; was a latent no-op) + honest boundary/help. Maintainer ratified the map + boundary copy + warn/fail-soft default; **added IPOVR-03 (opt a rule up to block) to v1.0 → Phase 29**. Build+vet+full suite green; live-path tests prove fresh→warn, missing-ts→warn-not-block, catalog-block survives posture warn, shim-shape enforces. Commits 5275296, f575365, 391c4b4, 58921ff, 6387b20, 8de66fc, 93312ba + boundary tighten |
| 28 | Layer 2 `beekeeper posture` View | IPVIEW-01/02, IPBND-01 | ✅ Complete & verified 2026-06-21 (delegated to gsd-executor, independently re-verified + reviewed). Pure `internal/posture/view.go` comparison model (shared by CLI+TUI) + `beekeeper posture` CLI + read-only TUI dashboard panel; reads each pm config vs Beekeeper's enforced posture, names gaps, prints `posture.BoundaryShort/Statement`. Read-only guarantee proven by a byte-for-byte sha256 unchanged-after-run test over fixture .npmrc/pnpm-workspace.yaml/bunfig.toml (IPVIEW-02). Orchestrator fix: release-age figure DERIVED from policy.DefaultReleaseAgeConfig (no drift). Executor added an npm probe to DetectState (Rule-2; npm fields were dead). build+vet+full suite green; em-dash clean. Commits 3b1d61c, eb53632, b0b662f, 2e6f41f, cbc3ae3 |
| 29 | Layer 3 Scoped Override + Per-Rule Severity | IPOVR-01/02/03 | ✅ COMPLETE & verified 2026-06-21. **29-01** (IPOVR-03): PostureConfig per-rule action (warn|block, default warn) + tighten-only untrusted merge (both paths; refuses block→warn) + action-aware posturizeWithAction (blocks a DEFINITE violation under block mode; warnUnknown bypasses it → unknown stays fail-soft warn); handler merge order unchanged. **29-02** (IPOVR-01/02): posture-scoped allowlist (NOT package_allowlist — proven by TestRunCheckPostureAllowAlwaysDoesNotBypassCatalogBlock: a posture allow-always never downgrades a catalog malware block) + owner-only atomic allow-once store (consume-then-warn proven) + `beekeeper posture allow|enforce` CLI writing distinct posture_override audit records + display-only TUI card. Independently re-verified: build/vet/full suite green, security-distinction + allow-once + untrusted-drop-Allow tests genuine. **Schema note (flagged to maintainer):** 29-02 added 4 omitempty audit-only fields (posture_override_*) to AuditRecord; posture_override records are written via a plain audit.NewWriter and NEVER reach the corpus StoreSink, so no CorpusRecord content changes and the frozen corpus schema intent is preserved (schema_lock_test passes). Commits cd5af47/b2e316c/9debb01/86a5c9e (29-01) + 1af13d5/ead635d/6ee8185 (29-02). |
| 30 | Docs, Home Page & Boundary Statements | IPBND-02, NMIG-03, REL-01 (prep) | ✅ Complete & verified 2026-06-22 (BOTH repos). **Go** (0b0bfcf, 02def02): retired stale docs/nudge.md → docs/install-posture.md (3 rules + warn/fail-soft + posture view + scoped overrides + verbatim boundary statement) + SENTRY-009 in THREAT-MODEL §8; build/vet/test green. **Web** (SEPARATE repo beekeeper-web @ a9016e5): home bullet "Hardened nudge"→"Install posture", ~10 docs reframed, dead nudge.go source_doc paths fixed (accuracy gate RED→GREEN 27/27), SENTRY-001..008→001..009, boundary statement propagated, v1.0.0 badges→v1.1.0, new v1.1.0 changelog + roadmap note. Orchestrator independently re-verified both repos (accuracy 27/27, every source_doc resolves, unit 36/36, pnpm build, em-dash/phantom-cmd greps clean). |
| 31 | Test, Coverage & E2E + CI Matrix (**Gate 2** after) | test coverage, REL-01 (finalize) | ✅ Complete & verified 2026-06-22 (7 signed commits a1b4452..00d9f7d). Gap-fill (feature already heavily tested in 26-29): release-age 24h boundary + 6 lifecycle script types + fetch-timeout fail-soft; live-`RunCheck` block-mode for lifecycle + git-remote + the default-warns attribution sibling; override enforce-warn + ecosystem-scoped allow (pypi NOT exempt) + config persistence; real-binary E2E git-remote warn/block-under-opt-up + honest SENTRY-009 CI-only skip; CI `e2e` job → release-gate.needs; `docs/posture-validation.md` (3 named gaps); REL-01 confirmed. Orchestrator independently re-verified (read all 7 test bodies = genuine; build/vet/test/e2e/coverage-floor/cross-vet green; the lone fail was the documented load-flaky p99 latency gate, confirmed passing isolated). |

## Phase Summary (v1.4.0)

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 22 | Schema & Envelope Lock | SCHEMA-01..06, SCOPE-01..02 | ✅ Complete & verified 2026-06-13 (3/3 plans, 12 commits; verifier 8/8; full suite green 27 pkgs + zero new deps + internal/corpus pure; maintainer freeze sign-off; schema FROZEN at CorpusSchemaVersion 1.0; IPv6 normalize quirk accepted + tracked) |
| 23 | Corpus Store & Adjudication Engine | ADJ-01..07, STORE-01..05, ENV-01..03 | ✅ Complete & verified 2026-06-14 (3/3 plans, 13 commits + 2 fixes; gsd-verifier 5/5; full suite green + ENV-03 fuzz 316k/0 + BenchmarkRunCheck ~25ms + zero new deps + internal/policy pure; orchestrator-caught ADJ-01 benchmark defect fixed pre-verify) |
| 24 | First Responder Corpus Binding | FRB-01..05 | ✅ Complete & verified 2026-06-14 (3/3 plans, 3 waves sequential on main; gsd-verifier 10/10 must-haves → passed; FRB-03 red/coral TUI gate maintainer-APPROVED; full suite green 27 pkgs + synthetic Nx Console evaluator gate + zero new deps; internal/watch ⊥ internal/tui; Rule-1 config-merge fix caught a prod-blocking Corpus.Enabled=false bug) |
| 25 | Launch Readiness | LAUNCH-01..04 | ✅ Complete & verified + SECURED + CODE-REVIEWED 2026-06-15 (3/3 plans, Wave 1 sequential on main; gsd-verifier 4/4 → passed; 25-03 §13 honesty checkpoint APPROVED live; /gsd-secure-phase 8/8 T-25-* closed; /gsd-code-review 0-crit/4-warn/3-info → all 4 warnings FIXED incl. 2 confirmed false-confidence launch gates WR-01 LAUNCH-01 + WR-02 LAUNCH-03 now genuine; full suite green 27 pkgs + go vet + zero new deps + internal/policy purity; tests+docs only over 22-24 code). + COVERAGE-HARDENED 2026-06-15 (maintainer-requested 100%-unit+e2e before closure): v1.4.0 logic funcs driven to 100% (corpus 75→92%, mergeCorpus/mergeAutoQuarantine 0/57→100% regression-locking the Phase-24 bug, OperatorAdjudication/ResolveCorpusPath/salt 0→100%) + reason-coded allowlist for fault-injection/Cobra branches (25-COVERAGE.md) + NEW system-wide real-binary moat-loop e2e TestCorpusMoatLoopE2E (//go:build e2e); full validation green (default suite + e2e tag + Phase-21 canary + zero deps). NEXT: /gsd-complete-milestone v1.4.0 |

## Phase Summary (v1.3.0)

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 10 | Hook-Block Protocol Compliance & Multi-Harness Enforcement | (seed) | ✅ Shipped 2026-06-05 (v1.3.0 seed) |
| 11 | Scaffold & Toolchain Isolation | SITE-01, SITE-02 | ✅ Complete & verified 2026-06-07 (1/1 plan; 4/4 SCs) |
| 12 | Design System | DSYS-01–04 | ✅ Complete & verified 2026-06-08 (3/3 plans; verifier 8/8 must-haves, 4/4 reqs; Playwright-verified both themes) |
| 13 | Docs Content Pipeline | DOCS-01 | ✅ Complete & verified 2026-06-08 (3/3 plans; verifier 4/4 SCs PASSED; FOWT UAT passed; code review 0-crit/3-warn) |
| 14 | Changelog Pipeline | CHG-01–03 | ✅ Complete & verified 2026-06-08 (2/2 plans; verifier 7/7 must-haves + 3/3 SCs PASSED; code review 2-crit/3-warn ALL fixed inline before verify) |
| 15 | Marketing Home | HOME-01–05 (SITE-03 deferred→Vercel) | ✅ Complete & verified 2026-06-08 (3/3 plans; verifier 5/5 must-haves + SC-1..5; code review 2-crit/3-warn fixed inline; both-theme Playwright proof) |
| 16 | 3D Layer | GFX-01–04 | ✅ Complete & verified 2026-06-09 (3/3 plans; GFX-01..04 green on prod build, FCP 560ms; maintainer UAT approved after 3 hive rounds) |
| 17 | SEO & Static Assets | SEO-01 | ✅ Complete & verified 2026-06-09 (3/3 plans; seo_spec SC-1..3 + gfx + home green; maintainer-approved flat-hive OG card) |
| 18 | Full Content Authoring | DOCS-02–09 | ✅ Complete & verified 2026-06-09 (6/6 plans; accuracy_spec AC-1..3 green; maintainer AC-5 sign-off vs THREAT-MODEL.md; nudge mode:block deviation per D-04) |
| 18.1 | Docs Theme Restyle | DSYS-05 | ✅ Complete & maintainer-approved 2026-06-09 (QUICK TASK — white sidebar border killed, teal/amber accents, sidebar duplication fixed via flatten; Playwright 0 white borders, all specs green). Command-card copy split DEFERRED → backlog todo. |
| 19 | Test Suite & CI | QA-01, QA-02 | ✅ Complete & verified 2026-06-11 (3/3 plans inline on main; unit 33 + postbuild 29 + e2e 12 incl. 4 QA-02 paths green; web.yml 6-gate + ci.yml bidirectional isolation; 4 Python specs retired after parity; code review 0-crit/6-warn — WR-01 pinned-serve fix; verifier PASSED 3/3) |
| 20 | Runtime Hardening II (Tiers 1–3) | CSYNC-01..06, LLMF-01..06, SENT-01..11 | ✅ Complete 2026-06-11 (6/6 plans code-complete 2026-06-10; **20-02 HF-license human gate CLOSED 2026-06-11** — license + read-only token + gated-weights download VERIFIED on Windows via `huggingface_hub.snapshot_download` into the StateDir HF_HOME, 11 files/~279MB. Full real-sidecar e2e is **CI/Linux-only by design**: codeshield→semgrep has no native Windows build (semgrep#1330), so the install aborts on native Windows. eBPF/ETW DNS + LLMF scan e2e CI-validated). |
| 21 | Full-System Validation & CI Calibration | VAL-01..VAL-08 | ✅ Complete & verified 2026-06-11 (4/4 plans, INLINE on main; maintainer register sign-off). W1 21-01 coveragegate gate + reason-coded fail-closed allowlist (only internal/version; package-level linkage) + Tier-A gap tests (hooks/protected, tui/computeStatus 81→100%, ipc server/client/peer Tier-B; catalog.ResolveHealthy pre-existed) [VAL-01/08] ∥ 21-02 16+1 golden deny + 17-target installer conformance, Hermes/Kilo/Trae honesty seams [VAL-02]; W2 21-03 FuzzEvaluateEvent (5th fuzz target, no-panic+valid-severity) + ci.yml 3×GOOS + fuzz-sentry→release-gate [VAL-03/04]; W3 21-04 Claude-Code `--hook` exit-2 canary e2e LIVE (both ~/.ssh+~/.aws) + validation-register (16+gated PENDING) + posture + README 17/16 [VAL-05/06/07]. Full gate GREEN; Tier-B/CI build-verified (live run at first push, D-05); zero product behavior changed (D-03). |

## Phase Summary (v1.1.0)

| Phase | Name | Tag | Requirements | Status |
|-------|------|-----|--------------|--------|
| 1 | Fork Setup & Discipline | v0.1.1-pollen.1 | FORK-01–04, PTEST-02–03, SDEF-02 | ✅ Complete |
| 2 | Windows Root Resolver | v0.1.1-pollen.2 | WRES-01–02, PTEST-01 | ✅ Code complete — signed release deferred to M2 close |
| 3 | Windows Path Representation | v0.1.1-pollen.3 | WPATH-01–02 | ✅ Code complete & verified — signed release deferred to M2 close |
| 4 | Windows Extension & MCP Coverage | v0.1.1-pollen.4 | WEXT-01–03, BKINT-01, PTEST-04 | ✅ Code complete & verified — signed release deferred to M2 close |
| 5 | Contribution-Back & Milestone Close | v0.1.1-pollen.5 | SYNC-01–02, BKINT-02, PTEST-05, SDEF-01 | Not started |

## Phase Summary (v1.2.0)

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 6 | Corroboration Severity Hardening | CORR-01, CORR-02 | ✅ Complete (3/3 plans; +2 code-review fixes) |
| 7 | Sensitive-Path Runtime Enforcement | SPATH-01–04 | ✅ Complete (3/3 plans; +6 code-review fixes) |
| 8 | Package-Manager Nudge + Behavioral Test Suite | NUDGE-01–08, BTEST-01–03 | ✅ Complete & verified (8/8 plans; 5/5 SCs, 11/11 reqs; full suite + fuzz + live-binary E2E green) |
| 9 | v1.2.0 Tech-Debt Cleanup (inserted from audit) | CLEAN-01–04, HARDEN-01–03, DRIFT-01 | ✅ Complete & verified (5/5 plans + 1 fix; 9/9 must-haves; full suite + e2e + fuzz green) |

## Performance Metrics

**Velocity (v1.0.0):**

- Total plans completed: 73
- Average duration: ~10 min/plan

## Accumulated Context

### Decisions

Recent decisions from Phase 11 (v1.3.0 - Scaffold & Toolchain Isolation, plan 11-01):

- Phase 11-01: web/ scaffolded with create-next-app@16.2.7 — App Router at web/app/ (no src/), Tailwind v4 CSS-first, Biome (no ESLint), TS `@/*`→`./*`. @16 had moved past the RESEARCH `--help` check, driving the deviations below.
- Phase 11-01: packageManager pinned to `pnpm@11.1.3` by hand (--use-pnpm no longer writes it). corepack `prepare pnpm@11.5.2 --activate` blocked by EPERM on C:\Program Files\nodejs → A3 fallback to active 11.1.3.
- Phase 11-01: authoritative lockfile is the REPO-ROOT `pnpm-lock.yaml` (pnpm workspace mode), NOT web/pnpm-lock.yaml as RESEARCH assumed; stale member lockfile deleted.
- Phase 11-01: create-next-app@16 generated a web/pnpm-workspace.yaml build-approval stub; pnpm 11.1.3 ABORTS (exit 1) a non-interactive install until the gate is decided. Resolved by deleting the web file + an explicit DENY at the root (`allowBuilds: sharp: false`, `ignoredBuiltDependencies: unrs-resolver`) — no untrusted lifecycle scripts run on install.
- Phase 11-01: root `.gitignore` web entries — `web/.source` has NO trailing slash so `git check-ignore` matches before Phase 13 creates the dir; existing `*.out` is an extension rule (not the out/ dir), so `web/out/` is still required.
- Phase 11-01: Geist scaffold fonts kept in layout.tsx because globals.css (left untouched) references their CSS vars; design-system fonts arrive in Phase 12.

Recent decisions from Phase 10 (v1.3.0 - plan 10-06):

- Phase 10-06: kilo_trae.go is a dedicated file for Tier-3 no-hook harness guides; guides upgraded with "UNGUARDED" text; TestInstallGatewayTargetKiloTraeUNGUARDED enforces the honesty gate (T-10-22)
- Phase 10-06: docs/harness-support-matrix.md is the authoritative 15-harness honesty document: Tier 1 testable = Claude Code; Tier 1 documented = 9 others; Tier 2 = Hermes/Cline/OpenCode; Tier 3 = Kilo/Trae
- Phase 10-06: README.md created (project had no README); links harness-support-matrix.md; states headline honestly without overclaiming universal protection

Recent decisions from Phase 10 (v1.3.0 - plan 10-05):

- Phase 10-05: Hermes YAML patching uses bufio.Scanner line scan (3 cases: append full block / append pre_tool_call: under hooks: / insert entry under existing pre_tool_call:); no gopkg.in/yaml.v3 dep
- Phase 10-05: Cline build-tag split — cline.go (!windows) real installer with executable PreToolUse 0o755; cline_windows.go (windows) stub with "macOS/Linux only" error; both GOOS builds clean
- Phase 10-05: TargetOpenCode moved from gatewayTargets to plugin installer; printOpenCodeGuide retained as MCP-fallback reference; TargetKilo/TargetTrae added to gatewayTargets + gateway_targets.go
- Phase 10-05: T-10-20 — installCline backs up + warns on foreign PreToolUse before overwriting; uninstallCline verifies clinePreCommand marker before removing

Recent decisions from Phase 10 (v1.3.0 - plan 10-03):

- Phase 10-03: Cursor event-name fixed (preToolUse → beforeShellExecution/beforeMCPExecution/beforeReadFile); FailClosed:true retained; command updated to --hook cursor
- Phase 10-03: ensureCodexFeaturesFlag uses targeted line/section string patching without new TOML library (CLAUDE.md constraint); idempotent under all 4 entry conditions
- Phase 10-03: Augment/CodeBuddy/Qwen reuse mergeClaudeHookEntry/removeClaudeHookEntry trinity; beekeeperClaudePreEntryWith/beekeeperClaudePostEntryWith added for parametric entry construction
- Phase 10-03: installCodex backs up config.toml before calling ensureCodexFeaturesFlag

Recent decisions from Phase 10 (v1.3.0 - plan 10-01):

- Phase 10-01: RenderDeny returns ExitCode=0 on allow — harness approval flow never bypassed; permissionDecision:"allow" never emitted (CONTEXT decision 3, T-10-02)
- Phase 10-01: Hermes ExitCode=0 by design (exit codes ignored by Hermes; block carried by JSON action:"block" with guaranteed non-empty message)
- Phase 10-01: Unknown HarnessID fails closed — exit 2 + stderr, nil Stdout (never silently allows)
- Phase 10-01: claudePreCommand changed from "beekeeper check" to "beekeeper check --hook claude-code"; propagates to merge/uninstall helpers via sentinel string

Recent decisions from Phase 02 (v1.1.0 Pollen - plan 02-02):

- Phase 02-02: isBroadHomeRoot gains C:\Users and C:\Users\<name> broad detection (Rule-1 auto-fix) — mirrors /Users and /Users/<name> on Unix; test asserted C:\Users broad but implementation only had C:\ drive-root
- Phase 02-02: roots_windows_test.go uses t.Setenv(USERPROFILE/APPDATA/LOCALAPPDATA/ProgramFiles) — never HOME — for Windows test isolation (Pitfall 5 prevention)
- Phase 02-02: glob root fixtures: create concrete versioned dirs (Python313, 3.3.0, Ruby33-x64) under wildcard parent so filepath.Glob resolves (needed for PyPI/RubyGems test assertions)
- Phase 02-02: TestResolveRootsBaselineIncludesUserLocalPython keeps Windows skip; reason updated to Unix-specific (non-Phase-2) language pointing to TestWindowsBaselineRoots for Windows PyPI coverage

Recent decisions from Phase 01 (v1.1.0 Pollen - plan 01-01):

- GOOS=windows go build ./... passes clean — no non-test files needed Windows fixes (Open Question 1 resolved)
- 6 Unix-root-resolver tests in cmd/pollen/main_test.go get t.Skip with "Phase 2 (v0.1.1-pollen.2)" structured reasons (not build tags — allows other tests in the file to run)
- scanner_test.go TestEndToEndScan: path separator bug fixed via filepath.Separator (not a skip; test passes on all OSes)
- BUMBLEBEE_ env var names renamed to POLLEN_ in roots.go + main_test.go (FORK-04 trademark)
- upstream remote configured at pollen repo clone; origin binding to github.com/home-beekeeper/pollen deferred to plan 05

Recent decisions from Phase 11:

- VerifySignatureWithKey(entry, pubKey) added alongside VerifySignature — presence-only path unchanged for backward compat
- Dissent sentinels (CatalogMatch{Dissented:true}) emitted by MultiIndex.LookupAll for configured-but-no-match sources; corroborate() filters them into SourcesDissented — import cycle avoided
- scanOnDeltaFn injectable var follows runBumblebeeFn pattern for test-time mock without real scan binary
- GoReleaser before.hooks uses sh -c guard so non-Linux environments skip eBPF generate gracefully
- -buildvcs=false added to goreleaser build flags (reproducibility gap closure)

Recent decisions from Phase 7:

- go-winio import path is github.com/Microsoft/go-winio (capital M); lowercase fails at go get with module path mismatch
- PipePath is var not const to enable test-time substitution; production value unchanged
- GetCurrentProcessToken().IsElevated() replaces manual TOKEN_ELEVATION unsafe pointer dance
- ETW EnableProvider is the actual API (not AddProvider); Provider struct needs GUID value type from *MustParseGUID dereference
- TestQueryServiceWhenNotInstalled skips on non-admin (mgr.Connect returns Access Denied); covered by CI admin runners

Recent decisions from Phase 6:

- Remote sink errors are fire-and-forget (nil returned); local NDJSON write is never blocked by remote collector outage
- AuditConfig imported by audit/sink.go from internal/config — no import cycle (config imports only stdlib)
- LlamaFirewall injection detection (LLMF-02) exits 0 in hook handler — PostToolUse hooks must not block agent flow; llmf_alert is the forensic signal
- scan_code / scan_alignment are Python sidecar stubs; CodeShield model integration deferred
- Phase 06-01 (CORR-01): CatalogHealthy defaults true — escalation active by default; callers explicitly set false on confirmed catalog degradation
- Phase 06-01 (CORR-01): findSeverityOverride all-versions guard inside helper — Version=="*" returns nil, preventing wildcard mis-tagged critical entries from single-source block
- Phase 06-01 (CORR-02): validateCorroborationThresholds extended with per-severity bounds loop; fail-closed to "block" on violation (BlockAt<1, BlockAt>globalBlockAt, QuarantineAt<BlockAt)
- Phase 06-01 (CORR-01/02): escalation + sanity gate shipped atomically in one commit (STATE.md Blockers/Concerns constraint satisfied)
- [Phase ?]: critical_block_at operator configurability added to policyloader

Recent decisions from Phase 07 (v1.2.0 — Sensitive-Path Runtime Enforcement):

- runCheck ordering (CR-02 fix): ApplyPolicyOverlay runs BEFORE the sensitive-path block; the path block is merged LAST via mergeDecisions (most-restrictive-wins) so a package_allowlist allow can never downgrade a credential-read block. runCheckWithIndex (integration_test.go) mirrors the same block so tests prove wiring without a catalog match.
- canonicalizePath (internal/check/paths.go, impure adapter) order: expandWinEnvVars → expandHome → filepath.Abs → EvalSymlinks(fallback to Abs result on error so non-existent credential files still evaluate) → ToSlash. internal/policy stays pure (TestPathImportsArePure green).
- expandWinEnvVars: single-pass strings.Builder, targeted %VAR%→os.Getenv (NOT os.ExpandEnv), fail-closed on unresolved var (literal %VAR% kept, never empty). Satisfies SC2 `type %USERPROFILE%\.ssh\id_rsa` (D-01).
- extractBashCredentialPaths scans ALL read-verb occurrences (moving-offset loop, CR-01 fix) and firstShellToken skips leading flag tokens (handles `cat -n …` and `a && cat ~/.ssh/id_rsa`).
- isAllowedPath gained a basename branch (no-separator patterns match lastSegment) — required for AllowPatterns (.env.example/.test/.schema) to take effect (Pitfall 2). extractTargetPath (policyloader) reads file_path primary, path fallback (both `!= ""` guarded).
- DefaultSensitivePaths added /.cursor/, /.windsurf/, bare /.cargo/credentials (D-02). D-03: SPATH wired into `beekeeper check` ONLY (no gateway/watch/scan).
- [Phase ?]: Antigravity settings path: ~/.gemini/antigravity/hooks.json (primary); .agents/hooks.json documented as project-local alternative
- [Phase ?]: Windsurf: exit-2-only deny (no stdout JSON); installer uses runtime.GOOS branch for powershell vs command key
- [Phase ?]: Gemini hooks in settings.json top-level hooks array, not nested event map

### Open Research Flags (v1.2.0)

- **[RESOLVED in 08-RESEARCH.md, 2026-06-04]** Flag 2 → **Position B** (60s detection cache lives ONLY in the long-lived gateway/shim; the one-shot `beekeeper check` hook runs `nudge.DetectStateFn` fresh with a 2s timeout, no file-cache/hot-path I/O). Flag 4 → **EXTRACT** a new pure `internal/pkgparse/` collapsing the two existing install-parse copies (engine.go `installPrefixes` + enforce.go `installPrefixesOverlay`); pnpm/bun/yarn install verbs map to ecosystem "npm" (closes F3). discuss-phase was intentionally skipped (maintainer chose to plan directly); research committed both decisions so they did not leak into implementation.
- **During Phase 8:** Windows corepack-shimmed pnpm `cmd.exe` startup time under the 2s detection timeout — live CI timing needed.
- **Flag 5 (PRD corrections):** `minimumReleaseAge` default is 1440 minutes (not 60); Node 22 is Maintenance LTS (Node 24 is Active LTS) — apply before implementation in Phase 8.

### Blockers/Concerns

- [RESOLVED 2026-06-04] Phase 8 (NUDGE): Flag 2 + Flag 4 settled in 08-RESEARCH.md and encoded in the plans (Flag 5 PRD corrections — minimumReleaseAge weakness baseline 1440, Node 24 recommended / floor 22 — also baked in). detect.go signature locked (cache-free `DetectStateFn` seam + gateway-only Cache wrapper). No longer a blocker.
- PLCY-07 (Phase 6) self-defense: [RESOLVED in 06-01] escalation + sanity gate shipped atomically; all-versions guard + SeverityOverrides in one commit

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260611-mpe | Pollen Windows test coverage (model 100% · walk 0→84.8% · scanner 79.6% · editorext 64.6→98.5% · cmd/pollen 62.8→80.4% · VAL-01-style coveragegate gate). cmd/pollen ceiling = main()/os.Exit + platform-exclusive mac/linux roots (out of Windows scope). Code in `../pollen` repo. | 2026-06-11 | pollen `da58af7`..`c896175` | [260611-mpe-pollen-windows-test-coverage](./quick/260611-mpe-pollen-windows-test-coverage/) |
| 260612-f80 | Detection-only first-responder + reversible quarantine (safe first slice **A+C1+B**). (A) catalog sync delta now runs a READ-ONLY installed-package cross-reference (`internal/watch/crossref.go`, relocated from `scan/` to break a scan->watch->scan import cycle); (C1) `internal/quarantine` is type-aware + reversible (editor-extension + language-package dir-move; `MoveTyped`/`packages/`) with an opt-in `auto_quarantine` knob (default DISABLED + DRY-RUN, threshold default 2 / clamp [1,3], absent->2 not floor) that auto reversible-quarantines at corroboration >= threshold (path-known) or records a pending-quarantine TUI incident (path-unknown); destructive PURGE always human-gated ([P]/[R] TUI, CLI y/N). (B) catalog->Sentry `TargetList` tightens correlation on the flagged subtree (DETECTION-ONLY, 60s reload, no kill/isolate). `internal/policy` ZERO-DIFF + pure; fail-closed on move error. Verifier 10/10; `go build`+`vet`+`test ./...`+GOOS=linux/darwin vet green; web/docs/blog reconciled (web gate green). On branch `feat/first-responder-quarantine` (NOT merged to main). DEFERRED: C2 destructive uninstall, C3 browser-ext/mcp-config quarantine. | 2026-06-12 | `25c2eae`..`2cce0de` + web `9aeb8e3` | [260612-f80-detection-only-first-responder-sync-hit-](./quick/260612-f80-detection-only-first-responder-sync-hit-/) |
| 260615-ky4 | **FRB-05 enforcement fix — closes the v1.4.0 milestone-audit BLOCKER.** The local catalog overlay (`local-overlay.idx`, written by Phase 24 `AddLocalOverlayEntry`) was never loaded by any production enforcement path. `internal/check/handler.go:236` now calls `catalog.NewMultiIndexWithOverlay(..., filepath.Join(cacheDir,"local-overlay.idx"))` (was `NewMultiIndex`, no overlay), with a dedicated `defer` closing only `multiIdx.Overlay`. Honest escalation is **allow→warn** (overlay entries are unsigned per CTLG-07; block still requires 2+ signed sources — anti-poisoning), NOT allow→block. +RunCheck-level regression tests: `TestRunCheckLocalOverlayEscalates` (overlay match → warn; revert-verified to fail under plain `NewMultiIndex`) + `TestRunCheckNoOverlayUnchanged` (missing overlay → allow, fail-closed guard). Full suite green 27 pkgs + build + vet. **Scoped out (recorded):** gateway (`gateway.go:141`) = clean follow-up; daemon:113 left alone (no self-feedback loop); scan/watch separate flows. On branch `fix/frb-05-overlay-check-path` (off main, NOT merged). | 2026-06-15 | `fc9fd66`,`4f1756f` | [260615-ky4-wire-the-local-catalog-overlay-into-the-](./quick/260615-ky4-wire-the-local-catalog-overlay-into-the-/) |

## Deferred Items

**Acknowledged at v1.4.0 milestone close (2026-06-15):** the pre-close artifact audit (`audit-open`) surfaced 4 items; each cross-checked and confirmed not a real gap:

| Category | Item | Status |
|----------|------|--------|
| quick_task | `260615-ky4` (FRB-05 enforcement fix) | DONE & merged to main; "unknown" only by the SUMMARY-frontmatter heuristic (no `status: complete` field) |
| quick_task | `260612-f80` (detection-only first-responder) | DONE on branch `feat/first-responder-quarantine`; "unknown" by the same heuristic |
| todo | `corpus-behavior-sig-ipv6-normalization.md` | Accepted at the Phase 22 freeze; fix needs a `CorpusSchemaVersion` bump (already in the carried-forward table below) |
| todo | `docs-styling-polish.md` | STALE — already promoted → Phase 18.1 (DSYS-05), shipped 2026-06-09 in v1.3.0; file never moved to `done/` |

Additional v1.4.0 follow-ups carried forward (accepted at close, not blocking): overlay wiring for the MCP gateway / scan / watch surfaces (the `beekeeper check` hot path is wired); the warn-vs-block design question for confirmed-malicious overlay entries (overlay entries are unsigned per CTLG-07 → allow→warn, never block on their own); STORE-02 `audit.NewMultiSinkWithCorpus` fan-out seam unused in production (records still written via the direct path). Full audit: `milestones/v1.4.0-MILESTONE-AUDIT.md`.

**Acknowledged at v1.3.0 milestone close (2026-06-11):** the `audit-open` query surfaced 4 items; each was cross-checked against the per-phase verification record and confirmed STALE bookkeeping (not real gaps), plus the one intentional requirement deferral:

| Category | Item | Status |
|----------|------|--------|
| requirement | SITE-03 — live Vercel deploy of the v1.3.0 site | Intentional deferral (static export retained; page build-verified; carried forward) |
| uat (stale) | Phase 13 `13-UAT.md` | `passed`, 0 pending scenarios — not a gap |
| verification (stale) | Phase 11 `11-VERIFICATION.md` `human_needed` | SITE-01/02 are Complete & verified everywhere downstream — stale frontmatter marker |
| todo (stale) | `docs-styling-polish.md` pending | Already PROMOTED → Phase 18.1 (DSYS-05), shipped 2026-06-09; file never moved to `done/` |
| context (stale) | Phase 10 `10-CONTEXT.md` 3 open questions | All resolved when Phase 10 shipped 2026-06-05 |

Items acknowledged and carried forward:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Corpus schema | `behavior_signature_hash` IPv6 bare-address normalize quirk (`::1`->`::`; trailing `:digits` stripped as a port) | Accepted at the Phase 22 freeze sign-off; immaterial to the package/extension exfil threat model; fix is bracket-aware host:port parsing + a **CorpusSchemaVersion bump** (breaking). Tracked: `todos/pending/corpus-behavior-sig-ipv6-normalization.md`; pointer in `internal/corpus/schema_version.go` | Phase 22 (v1.4.0) |
| Testing | `go test -race` requires CGO/C compiler | CI-only | Phase 1 (v1.0.0) |
| Build | `make verify-release` requires make on Windows | CI-only | Phase 1 (v1.0.0) |
| Watch | `notify.Config` wired to config preferences | Future phase | Phase 3 (v1.0.0) |
| Cursor | Windows extension-dir path (Assumption A1) | Live validation in v1.1.0 Phase 4 | Phase 3 (v1.0.0) |
| Distribution | Pollen binary releases (DIST-01) | v2 requirement | v1.1.0 scoping |
| Self-catalog | Separate `pollen-self` catalog (SELF-02) | v2 requirement | v1.1.0 scoping |
| Release | **`v0.1.1-pollen.2` signed tag (Phase 2 SC4)** — VERSION+CHANGES bumped and 4 commits prepared locally in `../pollen` (HEAD `c94b271`), **unpushed, untagged**. Cut the release at M2 close: `git push origin main` → confirm 3-OS CI green → `git tag -a v0.1.1-pollen.2` + push → cosign verify. Exact commands in `.planning/phases/02-windows-root-resolver/02-04-SUMMARY.md`. | **Deferred to M2 close** (maintainer decision 2026-06-02) | Phase 2 (v1.1.0) |
| Release | **`v0.1.1-pollen.3` signed tag (Phase 3 SC4)** — VERSION bumped to `0.1.1-pollen.3` + CHANGES.md WPATH section prepared locally in `../pollen` (commits incl. `1cb3fdb`, `19695e3`), **untagged, unsigned**. Cut at M2 close together with pollen.2: confirm 3-OS CI green → `git tag -a v0.1.1-pollen.3` + push → cosign verify. Details in `.planning/phases/03-windows-path-representation/03-03-SUMMARY.md`. | **Deferred to M2 close** (D-06, maintainer decision 2026-06-02) | Phase 3 (v1.1.0) |
| Release | **`v0.1.1-pollen.4` signed tag (Phase 4 SC5)** — VERSION bumped to `0.1.1-pollen.4` + CHANGES.md WEXT section prepared locally in `../pollen` (HEAD `a9db7b3`), **untagged, unsigned**. Cut at M2 close together with pollen.2 + pollen.3: confirm 3-OS CI green → `git tag -a v0.1.1-pollen.4` + push → cosign verify. Details in `.planning/phases/04-windows-extension-mcp-coverage-beekeeper-compat-test/04-03-SUMMARY.md`. | **Deferred to M2 close** (D-06, maintainer decision 2026-06-02) | Phase 4 (v1.1.0) |
| Phase 06 P01 | 440 | 3 tasks | 5 files |
| Phase 06 P02 | 171 | 2 tasks | 4 files |
| Phase 06-corroboration-severity-hardening P03 | 15 | 3 tasks | 9 files |
| Phase 10 P04 | 7 minutes | 3 tasks | 6 files |

## Session Continuity

Last session: 2026-06-22 (RESUMED via /gsd-resume-work — clean between-phase handoff, no incomplete work). Loaded STATE + PROJECT + `.planning/HANDOFF-install-posture.md` + memory `install-posture-arch.md`. State: milestone v1.5.0 "Install Posture" (ships as release v1.1.0), Phases 26-29 ✅ COMPLETE + verified, **GATE 1 PASSED**, 5/6 phases done, ~34 SSH-signed commits on `feat/install-posture-foundation` (NOT pushed), git working tree clean. The PLAN-without-SUMMARY hits for 26-29 are a false positive (this milestone stores no separate `-SUMMARY.md`; phases tracked in STATE with commit hashes). No HANDOFF.json, no fresh .continue-here (the only one is the PARKED v1.1.0 Pollen `05-*` checkpoint — untouched). Schema-freeze decision locked: KEEP typed `posture_override_*` audit fields (audit-only, never hit the corpus StoreSink; frozen moat schema intact). **NEXT = Phase 30 (Docs, Home Page & Boundary Statements; IPBND-02/NMIG-03/REL-01 prep).** Step 1 of Phase 30 is to CONFIRM the web/docs repo location — per memory [[beekeeper-web-split-repo]] the marketing+docs site is a SEPARATE repo at `C:/Users/Bantu/mzansi-agentive/beekeeper-web` (accuracy gate resolves `source_doc` against sibling `../beekeeper`), so most home-page+docs edits likely live there. Then: swap the home "Agent safety" nudge bullet for install-posture framing + the npm v12 obsolescence note (no em-dashes, sentence case); bring install-posture docs current (3 rules + warn/fail-soft + `beekeeper posture` view + scoped override + opt-up-to-block); propagate `posture.BoundaryStatement`; document SENTRY-009; strip steer-to-pnpm/Bun copy; bump version to v1.1.0 + changelog with a roadmap note for deferred layers. Resolver is BROKEN for this summary-style milestone — hand-manage phases, pass explicit plan paths to any gsd-executor. Then Phase 31 (tests/coverage/E2E + CI matrix), then **Gate 2** (maintainer signs the v1.1.0 release tag). No blockers.

Last session: 2026-06-12 (continued → MERGED to main + web-copy reconciliation). Merged `feat/first-responder-quarantine` into `main` via an explicit `--no-ff` integration commit (`bdcf829`, 34 files: the full A+C1+B feature + F-1..F-8 security hardening + THREAT-MODEL §12). Then ANALYSED the marketing home, docs, and blog against the shipped + hardened feature and reconciled the copy (`baad3aa`, web gate GREEN: lint + typecheck + unit 33 incl. accuracy + build + postbuild 29 + e2e 13). The one real inaccuracy: `security.mdx` claimed *any* scan hit tightens the catalog-to-Sentry targeted trace, but the F-4 fix gates it at corroboration ≥ threshold (≥2) — corrected (a single-source warn no longer tightens). Also softened "signed sources" → "sources" (TM-B-02 presence-check, not crypto) in security + configuration; added a "Move-source safety" note to `security.mdx` (symlink/junction + system-root refusal, Windows-complete restore guard, audit redaction; refs §12) + extended its `source_doc`; added the Layer-1→Layer-2 targeted-trace bridge to `two-layers.tsx` + the blog so the first-responder loop reads complete (both gated ≥2). `faq.tsx` + `dashboard-demo.tsx` were already accurate (no change). **The feature is now ON MAIN; working tree clean (only pre-existing scratch untracked).** NEXT: `/gsd-new-milestone` OR a deferred item (C2 destructive uninstall, C3 browser-ext/mcp-config quarantine, docs command-card-copy split, SITE-03 Vercel deploy).

Last session: 2026-06-12 (continued → SECURITY REVIEW + full remediation of the first-responder quarantine). The built-in `/security-review` skill aborted (its preamble assumes an `origin` remote; beekeeper is local-only), so the review ran via the **security-engineer** agent (opus) against `main...HEAD`. Verdict: **0 Critical, 1 High, 3 Medium, 3 Low, 4 Info** (report: `260612-f80-SECURITY-REVIEW.md`; 13 verified-safe items incl. no command-injection in the pollen exec, no NDJSON structural injection, dry-run zero-mutation, fail-closed move, `internal/policy` purity, 0600 + owner-DACL under self-protection, no daemon-panic DoS, tightening can only *lower* thresholds). Maintainer chose **"Everything actionable"** (AskUserQuestion) → **gsd-executor(opus)** fixed F-1..F-5 + F-7 + F-8 with one atomic commit + regression test each; full gate GREEN and **independently re-verified** (go build + go vet + `go test ./...` all 26 packages `ok` + `GOOS=linux/darwin go vet`). Fixes: **F-1** (High) route both new audit writes through `audit.RedactRecord` + `DefaultRedactPatterns` (TM-D-03 redaction invariant restored) `cada681`; **F-4** gate Sentry `AddTarget` on `CorroborationCount >= threshold` so a single source no longer tightens detection `3e0c70f`; **F-7** hoist `MultiIndex` + one audit writer per pass + 1000-finding cap `06238d5`; **F-5** daemon logs + audits a corrupt `sentry-targets.json` (log-and-continue, no DoS) `2efdedf`; **F-2** reject symlink/junction move+restore sources (`Lstat` + Windows reparse-attr check + `EvalSymlinks` dest-parent re-assert) `8125814`; **F-3** Windows-complete restore `OriginalPath` canonicalization (reject `C:foo` / `\\?\` / ADS / trailing dot-space; `runtime.GOOS=="windows"`-guarded, POSIX untouched) `cff1535`; **F-8** refuse system-critical roots (drive/filesystem roots, `C:\Windows`, `Program Files`, StateDir, quarantineDir) + require absolute source `f7e5bee`; review remediation log appended `300d165`; THREAT-MODEL §12 + TOC documenting the two new trust inputs + STATE this entry (this docs commit). Reviewer deviations accepted: F-3 rejects `\\?\` outright (safer than silent normalize); F-8 skipped the optional ecosystem allow-list (false-refusal risk; corroboration + symlink + system-root controls already bound the primitive). F-6 (TOCTOU) mitigated by F-2/F-3; residual needs a StateDir breach (out of scope). **Branch `feat/first-responder-quarantine` is now ~17 commits ahead of main, still NOT merged** — maintainer review/merge pending. NEXT: review/merge the branch, then `/gsd-new-milestone` OR a deferred item (C2/C3, docs command-card-copy split, SITE-03 deploy).

Last session: 2026-06-12 (RESUMED via /gsd-resume-work → EXECUTED quick task **260612-f80** "detection-only first-responder + reversible quarantine"). The 06-11 `HANDOFF.json` + `.planning/.continue-here.md` (both 21:33) were STALE — superseded by the FRESH root `.continue-here.md` (06-12), which paused mid-flight on this quick task right after committing the web-redesign checkpoint (`4097d0d` on main) + the handoff doc (`ff60e80`). Created the isolation branch **`feat/first-responder-quarantine`** (LOCKED decision #1) and ran the full quick-task pipeline orchestrated with subagents: gsd-planner(opus) → 6-task PLAN → gsd-plan-checker (PASS, 0 blockers) → gsd-executor(sonnet, 6 atomic commits `25c2eae`..`2cce0de`) → gsd-verifier (10/10 must-haves PASS, all 5 gates exit 0, honesty invariants adversarially confirmed in code) → frontend-architect web/docs/blog reconciliation (`9aeb8e3`, full web gate green: lint/typecheck/unit 33 incl. accuracy/build/postbuild 29/e2e 13) + a stale-comment fix. Shipped the SAFE FIRST SLICE A+C1+B: (A) catalog sync delta runs a READ-ONLY installed-package cross-reference (`internal/watch/crossref.go` — relocated from `scan/` to break a scan→watch→scan import cycle); (C1) `internal/quarantine` is type-aware + reversible (`MoveTyped`/`packages/` subdir; editor-extension + language-package dir-move) with an opt-in `auto_quarantine` config knob (default DISABLED + DRY-RUN, threshold default 2 / clamp [1,3], absent→2 not floor) that auto reversible-quarantines at corroboration ≥ threshold (path-known) or records a pending-quarantine TUI incident (path-unknown); (B) catalog→Sentry `TargetList` (`internal/sentry/targets.go`, created in Task 4) tightens correlation thresholds on the flagged subtree, DETECTION-ONLY (no kill/isolate/network-cut; daemon 60s reload). **Honesty invariants HELD:** scan READ-ONLY, purge ALWAYS human-gated ([P]/[R] TUI, CLI y/N), `internal/policy` ZERO-DIFF + pure, fail-closed on move error. CI-only: live Sentry OS-tap escalation (target-list logic is unit-tested locally). DEFERRED follow-ons named (NOT shipped): **C2** destructive package-manager uninstall (lockfile rewrite); **C3** browser-extension + mcp-config quarantine (scanned, not cataloged). Consumed + deleted all 3 checkpoint artifacts (root `.continue-here.md` + the stale `.planning/.continue-here.md` + `HANDOFF.json`). **Branch `feat/first-responder-quarantine` is NOT merged to main** — left for maintainer review/merge (keeps the Go feature + its clean web-copy update isolated from the main web redesign, per LOCKED decision #1). NEXT: maintainer reviews/merges the branch, then `/gsd-new-milestone` OR a deferred item (C2/C3, the docs command-card-copy split, SITE-03 Vercel deploy).

Last session: 2026-06-11 (RESUMED via /gsd-resume-work → **CLOSED milestone v1.3.0**). Resume restored from HANDOFF.json + `.planning/.continue-here.md` (the cross-cutting "v1.3.0 milestone close PAUSED at confirmation" checkpoint after the Pollen v0.2.0 release sprint). Maintainer chose "Close v1.3.0 now" (AskUserQuestion). Ran `/gsd-complete-milestone v1.3.0` but drove the archival **BY HAND** rather than via the generic `milestone.complete` CLI — the checkpoint + this project's documented phase-number-resolver regression both warned the CLI ROADMAP rewrite could disturb the parked v1.1.0 "Pollen" section. Read-only `audit-open` was run (4 items, all confirmed stale + acknowledged). Steps executed: wrote `milestones/v1.3.0-ROADMAP.md` (full historical phase detail, Phases 10–21 + the 18.1 INSERTED marker + decisions/resolved/deferred/tech-debt summary) + `milestones/v1.3.0-REQUIREMENTS.md` (final status, SITE-03 carried forward); prepended the v1.3.0 entry to MILESTONES.md; reorganized ROADMAP.md (v1.3.0 collapsed to a `<details>` + archive link, Pollen section + its bold Progress rows preserved verbatim — there is NO `## Backlog` section to re-append); evolved PROJECT.md (Current State leads with v1.3.0, "Current Milestone" → "Shipped: v1.3.0", footer 2026-06-11); appended RETROSPECTIVE.md v1.3.0 section + cross-milestone trends; STATE frontmatter → status:shipped, progress 13/13 100%; safety commit of all archive + doc files; `git rm .planning/REQUIREMENTS.md`; `git tag -a v1.3.0` LOCAL (beekeeper has no remote — nothing to push); final commit of the deletion. Untracked working-tree files (.claude/, 16-PATTERNS.md, .understand-anything/, AGENTS.md, HTML prototypes) were NOT swept into the commits (explicit file staging). Phase directories KEPT in place (not moved to a v1.3.0-phases archive) so the parked v1.1.0 `05-*` resume artifacts stay untouched; `/gsd-cleanup` can archive the v1.3.0 phase dirs later. **NEXT: `/gsd-new-milestone`** (or independently resume parked v1.1.0 Pollen via `docs/release-runbook.md`).

Last session: 2026-06-11 (RESUMED via /gsd-resume-work → drove the **Phase-20 20-02 LlamaFirewall HF-license live gate to CLOSURE** — the last open v1.3.0 item). The HANDOFF/.continue-here said "rebuild + re-run install", and the transformers-pin fix (`61c77b9`, `transformers>=4.51.3`) WAS correct — but the real blocker was a hard PLATFORM limit, not a pin: `llamafirewall==1.0.3` → `codeshield` → `semgrep`, and **semgrep has no native Windows build** ([semgrep#1330](https://github.com/semgrep/semgrep/issues/1330)), so `beekeeper llamafirewall install` aborts on native Windows at the semgrep wheel-build step. The sidecar also constructs the CODE_SHIELD scanner at startup, so the full real-sidecar e2e is **CI/Linux-only by design** — exactly as the e2e test header + every prior STATE note ("LLMF e2e is CI-only") already said. **Resolved** (maintainer chose "verify download, mark e2e CI-only" via AskUserQuestion): (1) rebuilt BOTH binaries (repo-root `beekeeper.exe` + `~/go/bin/beekeeper.exe`) so the fixed `transformers>=4.51.3` embed re-deploys — the stale repo-root binary shadowed go/bin in `where` order and was the likely culprit for the user's "still old pin" error; (2) found + fixed a token-location gotcha — `huggingface-cli login` wrote the token to the DEFAULT `~/.cache/huggingface/token`, but the install pins `HF_HOME` to the StateDir and `snapshot_download` reads `$HF_HOME/token`, so the token was invisible → copied it to `%APPDATA%\beekeeper\llamafirewall\hf\token` (bytes only, never printed); (3) **VERIFIED the human gate** on Windows via system `huggingface_hub` 1.15.0 `snapshot_download("meta-llama/Llama-Prompt-Guard-2-22M")` with `HF_HOME` pinned to the StateDir → 11 files / ~279MB (model.safetensors + config + tokenizer + license docs) into the exact cache the install uses, proving the accepted license + read-only token authorize the gated weights; (4) `docs/validation-register.md` gated-model row PENDING → ✅ (license+token+download verified 2026-06-11 Windows; full scan e2e CI/Linux-only); (5) landed the per-operator docs clarification + a NEW native-Windows/semgrep honesty note in `web/content/docs/security.mdx` (Gated-model bullet + new posture bullet) and `cli-reference.mdx` (install note), em-dash-free per the web-style directive; web gate GREEN (`pnpm build` exit 0 / unit 33/33 incl. accuracy / postbuild SEO 29/29). HANDOFF.json consumed+deleted; phase-20 `.continue-here.md` deleted. **ALL v1.3.0 phases (10–21) DONE; the last human gate is CLOSED. NEXT: `/gsd-complete-milestone v1.3.0`.** (WSL not attempted — CLAUDE.md bars local WSL integration; CI Linux is the canonical e2e runner. A Windows-aware PromptGuard-only install that skips CodeShield is a possible future product slice, NOT done.)

Last session: 2026-06-11 (EXECUTED + COMPLETED Phase 21 — Full-System Validation & CI Calibration, VAL-01..08). Ran `/gsd-execute-phase 21` INLINE on main (Go subagents would be fine here per D-09, but inline gave control over the iterative coverage-gate enumeration + cross-GOOS vet + avoided the SDK-tracking/Windows-stdio pitfalls). 4/4 plans, 3 waves, per-task atomic commits, hand-managed tracking. **W1 21-01** (coveragegate `feat` + 2 `test` + SUMMARY): `internal/coveragegate` = go/parser filesystem walker + fail-closed reason-coded allowlist (closed 6-code taxonomy; `TestAllowlistFailsClosed` proves bare-path/unknown-code rejection); **package-level linkage NOT same-name-sibling** (Pitfall 1: sibling = 70/184 false positives); empirically only `internal/version` is zero-test → allowlisted `type-only`. Tier-A gap tests: hooks/protected (markers + home-rooting), tui/computeStatus 81→100% + recentAuditRecords bounded-tail, ipc server/client/peer Tier-B. **W2 21-02** (golden `test` + conformance `test` + `.gitattributes` build + SUMMARY): byte-exact golden deny for 15 HarnessIDs + unknown (16 goldens, `-update`), Hermes exit-0/Kilo-Trae exit-2-empty/unknown-fail-closed honesty seams; `TestHarnessConformance` over all 17 allTargets (no silent skips). **W3 21-03** (fuzz `test` + ci `ci` + SUMMARY): `FuzzEvaluateEvent` (5th fuzz target, pure EvaluateEvent, no-panic+valid-severity; 5s smoke 22.9k execs clean) + ci.yml delta (Linux-leg 3×GOOS cross-build + `fuzz-sentry`→release-gate.needs; existing eBPF/kernel/eslogger/-race jobs preserved; web/release isolation intact). **W3 21-04** (e2e `test` + docs `docs` + SUMMARY): Claude-Code `check --hook claude-code` exit-2 canary e2e proven LIVE on Windows for BOTH ~/.ssh/id_rsa + ~/.aws/credentials (exit 2 + Family-A permissionDecision:deny + audit block — NOT the exit-1 default-mode case, Pitfall 2); `docs/validation-register.md` (16 harness rows + gated-22M-model PENDING) + `docs/validation-posture.md` (Tier A/B/C + allowlist taxonomy) + README 15→17/14→16 + Tier-3 +Continue/OpenClaw; **maintainer APPROVED** the register/posture/README (blocking Task-3 checkpoint, AskUserQuestion). **Full gate GREEN:** go test ./... + coveragegate + 16 golden + 17-conformance + -tags fuzz seed + -tags e2e all 5 cases + go vet. **REUSABLE (this session):** (a) coverage gate = package-level linkage + fail-closed reason-coded allowlist, not %-threshold and not sibling-linkage; (b) `catalog.ResolveHealthy` ALREADY had a full health_test.go (RESEARCH over-scoped — verify before authoring); (c) the Windows Cline `~\Documents\Cline\…` path in HookConfigFiles is an INTENDED sentinel (Cline is macOS/Linux-only), not a defect; (d) installer idempotency = hook-marker count STABLE across re-install, NOT ==1 (Cursor installs 3 entries: beforeShellExecution/beforeMCPExecution/beforeReadFile); (e) golden fixtures need a `.gitattributes` `text eol=lf` pin or core.autocrlf=true breaks byte-exact compare on Windows checkout; (f) `go test -update` — the custom flag must FOLLOW the package path (`go test ./pkg -run X -update`), else go test defaults to `.`; (g) ipc Tier-B `//go:build linux||darwin` tests are compile-validated from Windows via `GOOS=linux/darwin go vet` (go build does NOT compile tests); anchor unix sockets under `/tmp` (macOS sun_path ~104B limit vs long t.TempDir); (h) VAL-05 hook contract is exit 2 via `--hook`, the existing e2e only proved exit-1 default-mode; (i) cross-compile CI step gated to the Linux leg where eBPF bytecode is generated. Commits: 21-CONTEXT/RESEARCH/VALIDATION/plans from the planning session, then b36b6d0… execution chain (coveragegate, gap tests, ipc, golden, conformance, .gitattributes, fuzz, ci.yml, e2e, docs) + 4 SUMMARYs + this reconciliation. STATE/ROADMAP/REQUIREMENTS hand-updated (VAL-01..08 → Complete; did NOT run phase.complete — frontmatter-regression caveat). Resolver was FINE for slug `full-system-validation-ci-calibration`. **ALL v1.3.0 phases (10–21) now done.** NEXT: the Phase-20 20-02 HF-license human gate (independent; also closes the register's gated-model row), then `/gsd-complete-milestone v1.3.0`.

Last session: 2026-06-11 (PLANNED Phase 21 — Full-System Validation & CI Calibration, the Go-core pre-ship release gate). Ran `/gsd-plan-phase 21`. Maintainer decisions (AskUserQuestion): capture context INLINE, research-first, and **scope = apply all surfaced fixes** (validation + remediation, bounded to audit-/test-traceable work — no cosmetic refactors). Pipeline, hand-managed inline on main: 21-CONTEXT.md (D-01..D-10; Tier A/B/C model; coverage gate = presence + reason-coded allowlist, NOT %-threshold) → gsd-phase-researcher (HIGH-conf, code-grounded at `04857cc` — CORRECTED several CONTEXT assumptions: only `internal/version` is a zero-test package; the 4 `sanity.go` collapse to ONE `catalog.ResolveHealthy` test; VAL-02 is ~95% built via `RenderDeny`+`TestRenderDeny`; VAL-04 fuzz entry = pure `sentry.EvaluateEvent`; **VAL-05 real gap = the Phase-10 `--hook` exit-2 contract has NO live e2e**, the existing e2e only proves exit-1 default-mode; VAL-07 = README-only count fix since harness-support-matrix is already 17) → Nyquist 21-VALIDATION.md → gsd-planner (opus, 4 plans/3 waves: W1 21-01 coverage-gate+Tier-A gaps [VAL-01/08] ∥ 21-02 17-harness golden+installer conformance [VAL-02]; W2 21-03 FuzzEvaluateEvent + ci.yml delta [VAL-03/04]; W3 21-04 `--hook` exit-2 e2e + validation-register + posture + README fix [VAL-05/06/07], 21-04 autonomous:false maintainer sign-off) → gsd-plan-checker (PASSED, 0-blocker/2-warning). W1 = the recurring Dim-11 RESEARCH `## Open Questions` annotation gap + Q3 OPEN → FIXED inline (Q3 RESOLVED: ci.yml has NO ETW/Windows-Sentry live gate — jobs are test-matrix/fuzz×3/kernel-5.4/kernel-5.15/eslogger/release-gate; the windows-latest leg's `go test ./...` IS the ETW coverage, register-noted CI-build-validated; a live-ETW job mirroring eslogger is a future Tier-B enhancement, out of D-03 scope). W2 = 21-01 scope upper-bound (3 tasks/12 files) accepted, clean split point at Task 3 (ipc Tier-B) if context runs hot. **SKIPPED the separate pattern-mapper** — RESEARCH already supplies a superior code-verified analog map (§Architecture Patterns / §Tier-A Gap Test Contracts / §17-Harness Conformance / §Sentry Fuzz Target). 17-harness roster PINNED from `internal/hooks/hooks.go allTargets` (15 HarnessIDs + 4 gatewayTargets; Hermes exit-0 fail-open + Kilo/Trae UNGUARDED). Commits: `b36b6d0` CONTEXT, `0e72dec` RESEARCH+VALIDATION, `ffde749` plans (planner reconciled the ROADMAP Phase-21 section + progress row), + the W1 RESEARCH-annotation/ROADMAP-checked/STATE update. Resolver is FINE for slug `full-system-validation-ci-calibration` (resolves correctly, unlike `16-3d-layer`); STATE/ROADMAP still hand-managed (did NOT run state.planned-phase — frontmatter-regression caveat). **NEXT: `/gsd-execute-phase 21`** (INLINE on main — Go subagents ARE fine here, unlike the web phases which needed node/pnpm; Tier-B paths eBPF/eslogger/ETW/-race/peer-cred are CI-validated only on the Windows box; the CI matrix YAML is statically authored + locally build-verified, live GitHub run at first push per D-05; 21-04 Task 3 is a never-auto-approvable maintainer register sign-off). Then the Phase-20 20-02 HF-license human gate, then `/gsd-complete-milestone v1.3.0`.

Last session: 2026-06-11 (EXECUTED + COMPLETED Phase 19 — Test Suite & CI, QA-01/02, the LAST v1.3.0 web phase). Ran `/gsd-execute-phase 19` INLINE on main (D-05; use_worktrees-style sequential, the orchestrator acted as executor). 3/3 plans, per-task atomic commits, SUMMARYs, hand-managed tracking. **W1 19-01** (`0e06fab` deps, `a4d1138` config, `8889256` unit, `0a82172` biome-drift fix, `1abb86d` accuracy port, `5709dc9` SUMMARY): Vitest 4.1.8 + jsdom + @testing-library + vite-tsconfig-paths + @playwright/test@1.57.0 (all pinned, no Go-module touch / SITE-02), vitest.config.mts, 5 scripts, 5 unit tests 33/33 (accuracy port has verbatim constants). **W2 19-02** (`d63a08b` playwright config, `69afea8` test:postbuild include fix + gitignore, `abf0691` home.spec, `c81fa99` gfx+seo, `502a828` SUMMARY): playwright.config (chromium, serve out --listen 4199), home.spec.ts (home_spec.py port + 4 QA-02 paths), gfx.spec.ts, postbuild/seo.test.ts; 12/12 e2e + 29/29 postbuild. **W3 19-03** (`aaab69c` web.yml+ci.yml, `bdc142a` e2e de-flake+timeout, `1bb1108` retire .py, `4f5dc8e` SUMMARY): web.yml 6-gate path-filtered + ci.yml bidirectional paths-ignore; 4 .py specs re-run green on the same out/ (6 constant arrays byte-identical) then `git rm`'d. **Gates:** code review `c5fbe06` (0-crit/6-warn/5-info; WR-01 unpinned `pnpm dlx serve` → `7c1e1b4` pinned serve@14.2.6 + `pnpm exec`; WR-04/05 faithful .py parity kept; WR-02/03 push-time/repo-norm); verifier `5257bf8` PASSED 3/3 SCs. STATE/ROADMAP/REQUIREMENTS hand-updated (QA-01/QA-02 → Complete). **REUSABLE (new this session, also in memory):** Vitest 4 dropped `environmentMatchGlobs` (use `// @vitest-environment node` docblock); globals:false needs explicit `afterEach(cleanup)`; Vitest CLI positional only filters within `include` + `exclude` always wins (postbuild dir must be in include; scope `test` to tests/unit); Fumadocs static-search E2E = `[data-search-full]` → `input[placeholder="Search"]` → result BUTTONS, storageKey `bk-theme`; `pnpm dlx serve` defeats `--frozen-lockfile` → pin serve; subagents DO have node/pnpm here (the old "subagents lack node/pnpm" note is stale, but inline exec was correct anyway). **NEXT: maintainer completes the Phase-20 20-02 HF-license live bootstrap (the only Phase-20 item left), then `/gsd-plan-phase 21` (Full-System Validation), then `/gsd-complete-milestone v1.3.0`.**

Last session: 2026-06-10 (RESUMED Phase 20 from HANDOFF.json via /gsd-resume-work; chose "Follow recorded intent" = finish all autonomous code, stop only at the 20-02 HF-license human gate). Phase 20 had been paused at 20-02 Task 2 with 4/6 plans done (20-01/03/04 + 20-02 Task 1). This session completed ALL remaining autonomous code, inline on main, per-task atomic commits, hand-managed tracking: **20-02 Task 2** (`b116c5b`) collapsed the LlamaFirewall IPC to one loopback-TCP transport + per-launch crypto/rand bearer token (deleted client_windows.go/named-pipe; TCP dial-retry readiness; HF_HOME + port/token via cmd.Env; persistState writes port+token; NewSupervisor 2-arg; main.go enable/disable/status use platform.ConfigPath()/StateDir() fixing the Windows $HOME bug; audit-record reads port+token from state.json; new cross-platform client_token_test.go). **20-02 Task 4** (`923c4de`) added the //go:build e2e double-gated real-sidecar test (benign/injection/codeshield/crash-fail-closed) + TestSupervisorRestartOnCrash (uses startCrashingSidecar). **20-02 Task 3 code** (`f36546d`) added `beekeeper llamafirewall install` (CPU-only venv under StateDir + pinned deps via the CPU torch index + gated 22M model pre-pull into HF_HOME, surfacing the Llama-license + huggingface-cli login requirement) + supervisor venv-interpreter auto-detect + honest home/docs (dropped AlignmentCheck + "every tool call"; opt-in/experimental/gated/local-no-cloud; security.mdx posture subsection; cli-reference install). **20-05** (`304fedc`) corrected PROJECT.md Sentry Daemon + Out-of-Scope + the stale 20-02 LlamaFirewall bullets, rewrote THREAT-MODEL §8 (removed false "SENTRY-004/005 do not fire" + "fanotify no drops"; 5 honest residual gaps), and reframed the home Sentry cards + added a honesty-callout Sentry gap; the synthetic 006/007/008+watchlist rules_tests already existed from 20-03/04 and were verified green. **20-06** (`d5719c5`, OPTIONAL stretch executed) added EventDNSQuery + Linux kprobe DNS ingestion (udp/tcp_sendmsg dport 53 bpf2go BeekeeperDNS + fail-closed loader stub, bytecode CI-only) + Windows DNS-Client ETW (provider {1C95126E-...} event 3006 QueryName) + tests. **REUSABLE:** the eBPF DNS QNAME parse + msg_iter.iov (pre-6.4 / CI kernels 5.4+5.15), the ETW DNS-Client manifest-provider enable, and the LLMF real-sidecar e2e are all CI-validated only (CI-only, like the v1.2.0 -race + the 20-04 ETW field-key flag). Every commit passed go build ./... + 3 GOOS + go test + go vet (native + GOOS=linux) + (where web touched) pnpm build + accuracy/seo/home/gfx specs. SUMMARYs written for 20-02/05/06; ROADMAP 6/6; HANDOFF.json consumed+deleted; the Phase-20 .continue-here.md rewritten to the human-gate-only state. **NEXT: maintainer completes the 20-02 HF-license live bootstrap (the only Phase-20 item left), then `/gsd-plan-phase 19` (Test Suite & CI, QA-01/02 — the last v1.3.0 phase).**

Last session: 2026-06-09 (continued → INSERTED Phase 18.1 + did the docs-restyle as a QUICK TASK). Maintainer asked to insert Phase 18.1 (Docs Theme Restyle, DSYS-05) then scoped the work down to a **quick task, not a full phase** (also floated VitePress — declined: this is Next.js 16/React, Fumadocs IS the in-stack VitePress equivalent; re-platforming to Vue/Vite would fork the stack and is a milestone decision, not a restyle). Used the installed Python Playwright to screenshot dark-mode docs + dump computed styles to diagnose 3 gripes, then fixed all 3 (commits `aa57d74` flatten, `eec08c8` theming): **(1) white sidebar/body line** — Tailwind v4's preflight `border:0 solid` resets border-color to `currentColor` (bright `--fg`) on Fumadocs' bare `border-e`; an unlayered same-property override did NOT win the cascade in practice, so pinned `aside.bg-fd-card { border-inline-end-color: var(--border) !important }` (biome-ignore, like the reduced-motion block) — Playwright re-probe confirmed **0 visible white borders**. **(2) no color/accents** — ROOT CAUSE: our shadcn slots are declared via `@theme inline` which does NOT expose them as CSS custom properties, so fumadocs' `--color-fd-*` bridge (which does `--color-fd-x: var(--color-y)`) resolved EMPTY → transparent sidebar + unset primary. Fix: bind `--color-fd-*` DIRECTLY to the raw theme-switched tokens (`--bg/--surface/--fg/--teal/--border`) in `:root` (globals.css Section 9); `--color-fd-primary: var(--teal)` → active nav/links/TOC teal. **(3) sidebar name-duplication** — each section was `folder/index.mdx` + a same-title `meta.json` → Fumadocs drew a group label AND an identical indented child; fixed by flattening all 8 to flat `content/docs/<section>.mdx` + removing per-section meta.json (routes + Orama index unchanged; slug derives from filename). Plus an amber left-accent + teal tint on the active sidebar item (Section 9c). Verified dual-theme (dark+light) via Playwright; `pnpm build` + accuracy/seo/home/gfx specs ALL green; marketing home unaffected. **REUSABLE LESSONS:** (a) Tailwind-v4 default border color = `currentColor` → Fumadocs bare-border chrome needs an explicit theme-border override (and the cascade can be stubborn → scoped `!important`); (b) `@theme inline` shadcn slots are NOT real CSS vars, so any third-party `var(--color-*)` bridge (Fumadocs) reads them empty — bind the integration tokens to the RAW theme tokens instead. **STILL PENDING (maintainer's earlier "important" note):** multi-command command-field blocks (e.g. cli-reference catalogs/audit/config-set groups, integration Tier-1 install list) render as ONE card with ONE copy button that copies ALL commands — split each independent/alternative command into its OWN card+copy button (its own fenced block). Maintainer reviewed the served build and **approved → "mark complete to move on"**, so Phase 18.1 / DSYS-05 is CLOSED (18.1-SUMMARY.md written) and the command-card split was captured to `.planning/todos/pending/docs-command-card-copy.md` (NOT dropped). Quick task, no full plan/UI-SPEC/verify ceremony per maintainer scope. Milestone 9/10 phases (90%). **NEXT: `/gsd-plan-phase 19`** (Test Suite & CI, QA-01/02 — the LAST v1.3.0 phase).

Last session: 2026-06-09 (continued → EXECUTED + COMPLETED Phase 18). Ran `/gsd-execute-phase 18` INLINE on main (use_worktrees=false; subagents lack node/pnpm — I acted as the executor). 6/6 plans, ~14 atomic commits. **W1 18-01** (`71e3851`/`f61bb07`): `web/tests/accuracy_spec.py` (pure-Python AC-1 source_doc+referenced-path-exists / AC-2 unenforced-feature labels / AC-3 no phantom commands — cloned from seo_spec.py) + `web/components/docs/unenforced-callout.tsx` (`UnenforcedCallout`, amber `var(--amber)` dual-theme clone of breaking-change-callout) registered in mdx-components.tsx. RED until content landed (correct). **W2 18-02..05** authored all 8 docs sections from the source-of-truth (read THREAT-MODEL.md whole + harness-support-matrix.md + nudge.md + config.go + main.go): getting-started, installation (EXACT cosign/SLSA/SBOM from THREAT-MODEL §7, lowercase `bantuson` source-uri), configuration, cli-reference (exhaustive H2-per-command tree), security (posture + ALL known gaps co-located on one page), integration (Hermes fail-open / Kilo-Trae UNGUARDED / Claude-Code-only / `--bind 0.0.0.0` caveats each at point-of-use), troubleshooting, audit-log. **All stub inaccuracies corrected:** `hooks install --target` (not `--hook`), removed `check --input`, `hooks status`→`diag`, `catalogs rebuild`→`catalogs sync`, audit log = single `beekeeper.ndjson` (no rotation). **W3 18-06** (`e5847fd`): full suite GREEN (`pnpm build` exit 0, all 8 sections in out/docs/; accuracy_spec AC-1..3 + seo_spec + home_spec + gfx_spec all exit 0) → maintainer reviewed the served build at localhost:3000 vs docs/THREAT-MODEL.md and **APPROVED** the AC-5 accuracy gate. **CRITICAL ACCURACY DEVIATION (reusable):** the planner/RESEARCH §7 OQ-1 AND `docs/nudge.md`'s field-ref were WRONG — they claimed `config set nudge.mode` only accepts soft|hard and that block isn't settable. I verified `internal/config/config.go`: `legalNudgeModes={soft,hard,block}`, `ValidateNudgeConfig` ACCEPTS block (explicit `ValidateNudgeConfig{Mode:"block"}`→nil test + `TestEvaluateBlockMode`); `ensureNudgeBlockDefault` sets `mode=block` on first `hooks install`; `require_hardened:true` is a SEPARATE no-hardened-PM block trigger. Documented `mode:block` accurately per D-04 (code wins) — recorded in 18-03-SUMMARY. (The plan's `config set nudge.mode block`==0 grep still passed: I show the `soft` opt-down form, never that literal.) `docs/nudge.md` mode field-ref is now stale relative to the web docs — Go-side doc fix is out of scope. Tracking hand-managed (skipped phase.complete SDK verb — frontmatter caveat); REQUIREMENTS DOCS-02..09→Complete, ROADMAP Phase-18 row→6/6 Complete, STATE reconciled (8/9 phases, 24/24 plans, 89%). Docs **styling** (stock Fumadocs theme — the "looks old/basic" concern) remains separately DEFERRED (`.planning/todos/pending/docs-styling-polish.md`). **NEXT: `/gsd-plan-phase 19`** (Test Suite & CI, QA-01/02 — the LAST v1.3.0 phase; the docs-styling-polish backlog item is also still pending).

Last session: 2026-06-09 (continued → PLANNED Phase 18). Ran `/gsd-plan-phase 18` (Full Content Authoring, DOCS-02–09). Two maintainer decisions up front via AskUserQuestion: (1) **keep docs styling SEPARATE** — Phase 18 is pure content authoring; the deferred "docs look old/basic" Fumadocs-theme redesign (`.planning/todos/pending/docs-styling-polish.md`) becomes its own future phase with its own UI-SPEC, so NO UI-SPEC here despite ROADMAP "UI hint: yes"; (2) **research-first, inline** (Phase 13–17 precedent). Pipeline, all hand-managed inline on main: 18-CONTEXT.md (D-01..D-05) → gsd-phase-researcher (sonnet, HIGH conf) → 18-VALIDATION.md → gsd-pattern-mapper (sonnet, 11/11 analogs) → gsd-planner (opus, 6 plans) → gsd-plan-checker (sonnet, PASSED all dims; 7c N/A; one non-blocking advisory on 18-01 T2's deliberate pnpm-build verify). **The research paid off the research-first choice** — it caught real inaccuracies in the existing Phase-13 stubs that the plans now actively correct: `beekeeper hooks install` flag is `--target` NOT `--hook`; `beekeeper check --input` does not exist; `beekeeper hooks status` does not exist (→`beekeeper diag`); `beekeeper catalogs rebuild` does not exist (→`catalogs sync`); audit log is a single `beekeeper.ndjson` with NO daily rotation (stub claimed rotated/compressed); the `allow_remote_gateway` config gate the `--bind` help text promises is NOT implemented. Accuracy gate (DOCS-09) = a new pure-Python `web/tests/accuracy_spec.py` (AC-1 source_doc frontmatter + referenced path exists / AC-2 unenforced-feature labels present where `release_age`/`lifecycle_script_allowlist` mentioned / AC-3 no phantom commands) cloned from seo_spec.py, PLUS a blocking maintainer review vs `docs/THREAT-MODEL.md`. New reusable component `UnenforcedCallout` (amber dual-theme clone of breaking-change-callout.tsx, `var(--amber)` raw token NOT `--color-bk-*`). Waves: W1 18-01 infra → W2 18-02/03/04/05 parallel (8 docs sections, zero file overlap) → W3 18-06 full-suite gate + AC-5 human checkpoint (autonomous:false). 5 doc commits (d945920 CONTEXT, 1ed1593 RESEARCH, 553d592 VALIDATION, 5deeeb1 PATTERNS, 03b876c plans+ROADMAP). STATE+ROADMAP hand-updated (skipped state.planned-phase — frontmatter caveat); REQUIREMENTS unchanged (DOCS-02..09 already mapped). Resolver FINE for slug `full-content-authoring`. **NEXT: `/gsd-execute-phase 18`** (INLINE on main — subagents lack node/pnpm; execute W1 before W2; 18-06 T2 is a never-auto-approvable human accuracy checkpoint).

Last session: 2026-06-09 (continued → EXECUTED + COMPLETED Phase 17). Ran `/gsd-execute-phase 17` inline on main (use_worktrees=false; subagents lack node/pnpm). 3/3 plans + atomic commits. 17-01: BASE_URL/SITE_NAME const + seo_spec.py harness (ran red Wave-0 then green). 17-02: layout metadataBase + title template + shared OG/twitter; home + docs + changelog canonicals. 17-03: sitemap.ts (13 locs) + robots.ts force-static (flat out/ files; A1 fallback unneeded). **KEY DEVIATION (reusable Next.js fact):** nested catch-all routes must set ONLY `alternates.canonical` and NOT return `openGraph` — a nested route's openGraph REPLACES the inherited one wholesale (shallow merge), stripping the root `app/opengraph-image.png` file-convention og:image from every doc/changelog page (SC-2 failed on 12 pages until openGraph.url was removed). **OG card:** 3 maintainer rounds (animated-style rejected → legibility fix → flat hero-hive.svg approved); authored as HTML + rendered to a 1200×630 PNG via the installed Playwright (no new dep); Next dev server caches the generated OG image so a `.next` clear + restart was needed for live review. **+2 maintainer hero tweaks** (committed separately, style(home) `b162e92`): removed the 'v1.3.0 shipped: self-protection + TUI policy editor' pill; moved 'Read the docs' onto the go-install chip's row. Final gate GREEN (build + seo_spec + gfx_spec + home_spec all exit 0). Docs-styling concern (maintainer: docs 'look old and basic') captured to `.planning/todos/pending/docs-styling-polish.md` (deferred, NOT a Phase-17 redesign). Tracking hand-managed (skipped state.begin-phase/phase.complete — frontmatter caveat). Resolver fine for slug `seo-static-assets`. **NEXT: Phase 18 (Full Content Authoring, DOCS-02–09).**

Last session: 2026-06-09 (continued → PLANNED Phase 17). After finalizing Phase 16, ran `/gsd-plan-phase 17` (SEO & Static Assets). Maintainer chose: skip discuss / capture inline, research-first, canonical base URL = `https://beekeeper.vercel.app` (the Vercel project URL; SITE-03 deploy still deferred). Pipeline: 17-CONTEXT.md (inline, D-01..D-05) → researcher (sonnet, HIGH conf — confirmed: static `app/opengraph-image.png` not Edge `ImageResponse`; `sitemap.ts`/`robots.ts` need `export const dynamic='force-static'`; `metadataBase` is the one missing root change; canonical/og on Fumadocs catch-all routes; pure-Python `seo_spec.py` file-walk for verification) → 17-VALIDATION.md → 17-PATTERNS.md → planner (opus) 3 plans/2 waves → plan-checker PASSED (12/12 dims; the only blocker was the recurring Dim-11 RESEARCH `## Open Questions (RESOLVED)` annotation gap — fixed inline, NO plan change, re-verified clean). Commits: 4ec560c CONTEXT, eda3615 RESEARCH, fc862a5 VALIDATION, 9d87f80 PATTERNS, d4c880f plans, + RESEARCH annotation fix. ROADMAP + STATE hand-updated (did NOT run state.planned-phase — frontmatter-corruption caveat). UI-SPEC gate false-positived (ROADMAP UI-hint:no) → proceeded as --skip-ui. **NEXT: `/gsd-execute-phase 17`** (17-02 Task 3 = blocking OG-card human checkpoint; execute INLINE on main — subagents lack node/pnpm; phase-resolver is fine for slug `seo-static-assets`, unlike `16-3d-layer`).

Last session: 2026-06-09 (resumed → FINALIZED Phase 16). Restored from HANDOFF.json + 16-3d-layer/.continue-here.md: Phase 16 was executed & green but paused at the finalization gate. Maintainer requested a hive rework (round 3) — applied: darker back depth ring + short amber center→cone light-streaks (`Spoke`) in hero-canvas.tsx, defense-in-depth cards reordered Default→Privileged→Optional (how-it-works.tsx), command-field scrollbars removed (no-scrollbar @utility in globals.css). Maintainer approved on :3000 ("Good → finalize"). Full gate re-run GREEN on the production build (pnpm build exit 0; gfx_spec.py GFX-01..04 PASS, FCP 560ms; home_spec.py PASS). 16-03-SUMMARY.md written; STATE/ROADMAP/REQUIREMENTS reconciled (GFX-01..04 done); HANDOFF.json + 16-3d-layer/.continue-here.md deleted (one-shot); dev servers stopped. Committed inline on main. **Phase 16 COMPLETE. NEXT: Phase 17 (SEO & Static Assets, SEO-01).** NOTE for future sessions: GSD phase-resolver can't find "16-3d-layer" (greedy `3d` token) → tracking is hand-managed; do NOT spawn gsd-executor (subagents lack node/pnpm).

Last session: 2026-06-07 (resumed) — restored from HANDOFF.json (2026-06-07T11:56). v1.3.0-seed work complete & committed (e1b907b/307fbad/999fa39/95a99e6 + wip e6962e6); tree clean. Proceeding to next action: formalize v1.3.0 milestone.

Last session: 2026-06-06 (resumed)
Stopped at: Big dogfood session. (1) handoff #9 scan→pollen E2E validated. (2) #13 scan audit-bloat FIXED. (3) Wired beekeeper as a LIVE Claude Code hook (~/.claude/settings.json, merged w/ GSD; backup .pre-beekeeper-20260606.bak). (4) Found pnpm-detect flaky on Windows (42-92%). (5) Built nudge `mode=block` (detection-INDEPENDENT enforcement, offers pnpm+bun) — PROVEN blocking live npm install via the hook. (6) Fixed pkgparse compound/env bypass (was defeating nudge AND catalog block). 3 commits on main: 6691585 fix(scan), 96f0a7b fix(pkgparse), 0028392 feat(nudge). Live config: %APPDATA%/beekeeper/config.json mode=block.
Resume file: memory/nudge-block-mode-and-bypass-fix.md + HANDOFF.json. Open: block-as-default decision; block-mode false-positive on quoted "&& npm install" (e.g. commit msgs → use git commit -F); optional /gsd-code-review, /gsd-new-milestone v1.3.0.

## Operator Next Steps

- **v1.2.0 (current):** ALL phases (6,7,8,9) ✅ **complete & verified**. Milestone audit (`v1.2.0-MILESTONE-AUDIT.md`, `tech_debt` — NO blockers) → maintainer scope "Everything" → **Phase 9 "v1.2.0 Tech-Debt Cleanup" EXECUTED (5/5 plans + fix `ef4ea97`) + VERIFIED passed 9/9** (`09-VERIFICATION.md`). All audit findings resolved: CLEAN-01 hermetic CORR E2E (signed non-wildcard fixture, blocks offline), CLEAN-02 LoadLayered Nudge-pointer merge (root fix), CLEAN-03 Phase-6 Nyquist COMPLIANT, CLEAN-04 handler comment, HARDEN-01/02/03 SPATH evasion edges (ancestor-symlink dual-form + Windows ADS/trailing-dot + verb word-boundary), DRIFT-01 live version_drift npm registry query (fail-open, floors never bumped). Release gates GREEN (go build + full unit suite + `-tags e2e` 15.4s + `-tags fuzz` incl. policy post-fix + TestPathImportsArePure + TestOverlayAllowCannotDowngradePathBlock). Phase 9 ran SEQUENTIALLY on main (`use_worktrees=false`); executors were told NOT to do phase-number-keyed SDK tracking writes — orchestrator updated STATE/ROADMAP/REQUIREMENTS MANUALLY. **NEXT:** re-run `/gsd-complete-milestone v1.2.0` — fold `v1.2.0-MILESTONE-AUDIT.md` + `09-VERIFICATION.md` into the archive (optionally `/gsd-verify-work 9` for conversational UAT first). (Phase 8's 08-05 executor had hit a mid-run session limit AFTER committing its 3 task commits — closed out manually by writing its SUMMARY, no re-execution.) Known INFO (now folded into Phase 9): gateway `realMetadataFetch` (drift.go) is a production stub returning empty, so `version_drift` emits nothing live until a real registry query is added (floor auto-update is Out-of-Scope; drift is informational-only). Also confirmed broken this session: `phase-plan-index 8` flattens all 8 plans into "wave 1" (ignores the `wave:` frontmatter) — execution used the authoritative PLAN-frontmatter wave order W1{01,05} W2{02,03} W3{04} W4{06} W5{07,08}. Code review done (08-REVIEW.md): 0 critical, 4 warning — ALL FIXED (WR-01 redact nudge command fields in audit log, WR-02 parseInt overflow guard, WR-03 parallelize PM probes so detection stays ~2s within the hook budget, WR-04 bound drift goroutine) + re-confirmed (build + tests + live E2E green); 5 Info deferred. Caveat: `go test -race` is CI-only (CGO disabled locally) — the CI race pass on internal/nudge is the authoritative confirmation for WR-03's concurrency change. NOTE (still applies to execute): the `gsd-sdk` init.plan-phase / state.begin-phase / phase.complete resolvers map bare phase numbers (7, 8) to ARCHIVED v1.0.0 dirs under `.planning/milestones/v1.0.0-phases/` and corrupt STATE frontmatter progress — the live v1.2.0 phases are in `.planning/phases/NN-slug/`. Pass explicit paths to agents and update STATE/ROADMAP/REQUIREMENTS tracking MANUALLY (do not trust phase-number-keyed SDK writes). `init.execute-phase`, `roadmap.get-phase`, and `init.phase-op` resolved correctly; `init.plan-phase`/`state.begin-phase` did not. CONFIRMED this session: `init.plan-phase 8` → `.planning/milestones/v1.0.0-phases/08-tui-dashboard` (wrong); plan-phase was driven with explicit live paths instead.
- **v1.1.0 (parked release):** when ready, run `docs/release-runbook.md` (push `../pollen` + cut signed tags `pollen.2/.3/.4/.5` + cosign verify + create/push `home-beekeeper/beekeeper`), then finish 05-05 Task 3 (tracking + verify) and close v1.1.0 via `/gsd-complete-milestone`. Resume context: 05-05 `.continue-here.md` + `docs/release-runbook.md`. Do NOT close v1.1.0 before this runs.
- **Still pending (from v1.0.0 close):** the beekeeper GitHub remote is created as part of the v1.1.0 runbook (Step 1: `gh repo create home-beekeeper/beekeeper`).
