---
phase: 09-v1.2.0-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/config/layered.go
  - internal/config/layered_test.go
  - cmd/beekeeper/nudge.go
  - internal/check/handler.go
  - internal/check/integration_test.go
autonomous: true
requirements: [CLEAN-02, CLEAN-04, HARDEN-01]
must_haves:
  truths:
    - "config.LoadLayered returns a non-nil cfg.Nudge populated with PRD defaults when no layer sets a nudge block"
    - "A project-layer nudge.enabled:false (or nudge.mode override) wins over the user/default layer through LoadLayered's merge — without any consumer-side helper"
    - "The handler.go decision-merge comment describes the real order: overlay → SPATH → NUDGE (most-restrictive-wins)"
    - "The SPATH block in handler.go and integration_test.go iterates BOTH canonical forms (HARDEN-01) so an ancestor-symlink credential read blocks"
  artifacts:
    - path: "internal/config/layered.go"
      provides: "Nudge *NudgeConfig pointer merge in merge() (field-level src-wins + project-disable rule)"
    - path: "internal/config/layered_test.go"
      provides: "Layered-config test: default Nudge populated; project override wins; no consumer helper"
    - path: "internal/check/handler.go"
      provides: "Corrected merge-order comment + SPATH dual-form wiring; nudge nil-guard documented as defense-in-depth"
    - path: "cmd/beekeeper/nudge.go"
      provides: "defaultNudgeConfigHelper guard documented as defense-in-depth (root cause now fixed)"
  key_links:
    - from: "internal/config/layered.go merge()"
      to: "Config.Nudge *NudgeConfig"
      via: "src-wins-if-set + per-field override merge of the Nudge block"
      pattern: "src\\.Nudge"
    - from: "internal/check/handler.go runCheck SPATH loop"
      to: "internal/check/paths.go canonicalizePathForms"
      via: "iterate both lexical + symlink-resolved forms, EvaluatePath each, merge"
      pattern: "canonicalizePathForms"
---

<objective>
Fix the CLEAN-02 root cause and two adjacent items that live in the same files:
- CLEAN-02: `config.LoadLayered`'s `merge()` never merges the `Config.Nudge *NudgeConfig` pointer. The user layer happens to carry defaults (via `Load`), but a project-layer `nudge.*` override is silently dropped, and any future LoadLayered consumer reading `cfg.Nudge` without a nil-check gets nil. Fix `merge()`; document the consumer guards as defense-in-depth.
- CLEAN-04: the `handler.go:261` comment ("sensitive-path block is merged LAST") is stale — NUDGE is the real last merge. Correct it to overlay → SPATH → NUDGE.
- HARDEN-01 wiring (depends on Plan 01): switch the two SPATH evaluation loops (handler.go runCheck + integration_test.go runCheckWithIndex) to iterate BOTH canonical forms via the `canonicalizePathForms` helper Plan 01 added, so an ancestor-symlink credential read blocks end-to-end.

Purpose: Correct layered config at its root (defense-in-depth, not consumer band-aids), keep the merge-order documentation truthful, and complete the HARDEN-01 fix through the live check pipeline.

Output: `merge()` carries the Nudge block; a layered-config test proves defaulting + project override with no consumer helper; the handler comment is accurate; the SPATH loops are dual-form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v1.2.0-tech-debt-cleanup/09-CONTEXT.md
@.planning/phases/09-v1.2.0-tech-debt-cleanup/09-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. -->

internal/config/config.go:
  type Config struct { ...; Nudge *NudgeConfig `json:"nudge,omitempty"` }
  type NudgeConfig struct { Enabled bool; Mode, Preferred string; RequireHardened, CheckSocketScanner bool;
                            MajorDriftCheck NudgeMajorDriftCheck; VersionFloors NudgeVersionFloors }
  func DefaultNudgeConfig() NudgeConfig          // PRD §5.1 defaults (Enabled:true, Mode:"soft", Preferred:"pnpm", floors 11/1.3/22, drift 168h)
  func ValidateNudgeConfig(nc NudgeConfig) error // fail-closed enum/floor/interval validation
  func Load(path string) (Config, error)         // absent file -> Nudge=&defaults; present -> validates non-nil Nudge

internal/config/layered.go:
  func LoadLayered(opts LayerOpts) (Config, error)  // baseline -> system -> user(Load) -> project -> env -> flags -> validate
  func merge(dst, src Config) Config                // src-wins-if-nonzero per field; CURRENTLY DOES NOT TOUCH src.Nudge/dst.Nudge
  func loadIfPresent(path) (Config, bool, error)    // raw parse, NO default-filling (Nudge stays nil if absent in that layer)
  func mergeLlamaFirewall(dst, src LlamaFirewallConfig) // the documented bool-merge precedent to mirror for nudge bools
  func validate(cfg Config) (Config, error)         // FailMode switch only

cmd/beekeeper/config_resolve.go:
  func resolveConfig(cmd) (Config, error)  // calls config.LoadLayered -> used by `beekeeper nudge status|check`

cmd/beekeeper/nudge.go (consumer workaround — CLEAN-02 target):
  // status (~79-83) and check (~178-182): `nc := cfg.Nudge; if nc == nil { d := defaultNudgeConfigHelper(); nc = &d }`
  func defaultNudgeConfigHelper() config.NudgeConfig  // returns config.DefaultNudgeConfig()

internal/check/handler.go (runCheck ~257-309):
  // ~259-265: STALE comment block claiming "The sensitive-path block is merged LAST"
  // ~280-288: SPATH loop — `for _, rawPath := range extractPathTargets(toolCall) { resolved := canonicalizePath(rawPath); ... EvaluatePath; mergeDecisions }`
  // ~301-304: `nudgeCfgValue := config.DefaultNudgeConfig(); if cfg.Nudge != nil { nudgeCfgValue = *cfg.Nudge }` (nil-guard, CLEAN-02 defense-in-depth)
  // ~290-309: NUDGE block — the ACTUAL last merge

internal/check/integration_test.go (runCheckWithIndex ~76-86):
  // mirrors handler SPATH loop with the same canonicalizePath call — must be updated in lockstep

internal/check/paths.go (Plan 01 deliverable — read 09-01-SUMMARY.md to confirm the exact name/signature):
  func canonicalizePathForms(raw string) []string  // returns lexical (pre-EvalSymlinks) + symlink-resolved forms, de-duplicated
  func canonicalizePath(raw string) string         // unchanged single-form
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge the Nudge pointer in LoadLayered.merge()</name>
  <files>internal/config/layered.go</files>
  <read_first>internal/config/layered.go (merge ~159-199, mergeAudit, mergeLlamaFirewall ~223-252, loadIfPresent ~116-131, validate ~336-348), internal/config/config.go (Config, NudgeConfig, DefaultNudgeConfig, ValidateNudgeConfig), internal/config/layered_test.go (existing merge tests, TestMerge_ZeroValuePreservation ~118)</read_first>
  <behavior>
    - LoadLayered with NO nudge block in any layer -> cfg.Nudge != nil and equals DefaultNudgeConfig() (defaulting at the layered root, not just via the user-layer Load side effect).
    - LoadLayered where a PROJECT layer sets `nudge.enabled:false` (and/or `nudge.mode:"hard"`) -> the merged cfg.Nudge reflects the project override (Enabled:false / Mode:"hard"), and unset project nudge sub-fields inherit the lower-layer/default values (no zeroing — Pitfall 5 consistency).
    - An invalid merged nudge block (e.g. project nudge.mode:"aggressive") is rejected by LoadLayered (fail-closed), mirroring how Load validates.
  </behavior>
  <action>
    Add Nudge handling to merge(): introduce `mergeNudge(dst, src *NudgeConfig) *NudgeConfig` and call `dst.Nudge = mergeNudge(dst.Nudge, src.Nudge)` in merge(). Semantics: if src is nil, return dst unchanged (lower layer wins / absent does not zero). If dst is nil and src non-nil, start from a copy of *src. If both non-nil, field-level override src-wins-if-set, mirroring mergeAudit/mergeLlamaFirewall conventions. Bool fields (Enabled, RequireHardened, CheckSocketScanner) share the "cannot distinguish absent from false" limitation as LlamaFirewall.Enabled — resolve it the SAME documented way as mergeLlamaFirewall: treat a present project nudge object as authoritative for Enabled (so the NUDGE-08/PRD §11 project-disable `nudge.enabled:false` wins), and apply other bools when src has any non-zero nudge field set; document the limitation and chosen rule in the mergeNudge doc comment. Strings (Mode, Preferred) and nested VersionFloors/MajorDriftCheck fields: src wins if non-empty/non-zero. After all layers merge, LoadLayered must ensure a non-nil Nudge: if cfg.Nudge == nil set it to DefaultNudgeConfig(); if non-nil run ValidateNudgeConfig and return its error on failure (fail-closed) — do this within/just before validate (extend validate or add the check in LoadLayered). Reference CLEAN-02.
    IMPORTANT: loadIfPresent does NOT default-fill, so a project file WITHOUT a nudge key yields src.Nudge==nil -> mergeNudge leaves the lower layer intact. A project file WITH `"nudge":{...}` yields a non-nil src.Nudge whose absent sub-fields are Go zero values — the field-level merge must not let those zeros clobber lower-layer non-zero values (except the documented bool/Enabled project-disable rule).
  </action>
  <verify>
    <automated>go test ./internal/config/ -run "TestMerge|TestLoadLayered" -v</automated>
  </verify>
  <done>merge() carries the Nudge block via mergeNudge; LoadLayered guarantees a non-nil, validated cfg.Nudge; absent nudge layer -> DefaultNudgeConfig; project nudge override (enabled:false / mode) wins and partial overrides do not zero lower-layer fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layered-config test proving defaulting + project override without a consumer helper; document consumer guards as defense-in-depth</name>
  <files>internal/config/layered_test.go, cmd/beekeeper/nudge.go, internal/check/handler.go</files>
  <read_first>internal/config/layered_test.go (test patterns, how LayerOpts/temp files are built), cmd/beekeeper/nudge.go (status ~79-83, check ~178-182, defaultNudgeConfigHelper ~284-286), internal/check/handler.go (nudge nil-guard ~301-304)</read_first>
  <behavior>
    - Test A: LoadLayered with a temp user file that has NO nudge block (or absent user file) and no project layer -> cfg.Nudge != nil && deep-equals DefaultNudgeConfig().
    - Test B: LoadLayered with a project file containing `{"nudge":{"enabled":false}}` over a defaulting user layer -> cfg.Nudge.Enabled == false AND the other nudge fields equal the defaults (Mode "soft", Preferred "pnpm", floors intact).
    - Test C: LoadLayered with a project file `{"nudge":{"mode":"hard"}}` -> cfg.Nudge.Mode == "hard" and Enabled stays true (inherited).
    - All three assert directly on the LoadLayered return value — NO call to defaultNudgeConfigHelper or any consumer-side nil fix.
  </behavior>
  <action>
    Add the three layered-config tests to internal/config/layered_test.go (TestLoadLayeredNudgeDefaulting, TestLoadLayeredProjectNudgeOverrideWins, TestLoadLayeredProjectNudgeModeOverride — or one table-driven test). Build temp files with t.TempDir + os.WriteFile, pass LayerOpts{UserPath, ProjectPath}. Assert on cfg.Nudge fields. Then, since the root cause is fixed, UPDATE the consumer guards to be documented defense-in-depth rather than load-bearing: in cmd/beekeeper/nudge.go (both status and check sites) KEEP the `if nc == nil` guard but change its comment to note LoadLayered now always populates Nudge (CLEAN-02) and the guard remains as belt-and-suspenders; likewise update the comment on the handler.go nudge nil-guard (~301-304). Do NOT remove the guards (they protect direct zero-Config construction in tests) — only reclassify them in comments. Update defaultNudgeConfigHelper's doc comment to reference CLEAN-02. No runtime behavior change in the consumers.
  </action>
  <verify>
    <automated>go test ./internal/config/ ./cmd/beekeeper/ -run "TestLoadLayered|Nudge" -v</automated>
  </verify>
  <done>Three layered-config assertions pass against LoadLayered output with no consumer helper; consumer nil-guards remain but are re-commented as defense-in-depth referencing CLEAN-02; cmd/beekeeper builds and its nudge tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Correct handler.go merge-order comment (CLEAN-04) and wire HARDEN-01 dual-form SPATH</name>
  <files>internal/check/handler.go, internal/check/integration_test.go</files>
  <read_first>internal/check/handler.go (runCheck ~257-309, esp. comment ~259-265 + SPATH loop ~280-288), internal/check/integration_test.go (runCheckWithIndex SPATH loop ~76-86), internal/check/paths.go (canonicalizePathForms — confirm name from 09-01-SUMMARY.md), .planning/phases/09-v1.2.0-tech-debt-cleanup/09-01-SUMMARY.md</read_first>
  <action>
    CLEAN-04: rewrite the stale comment near handler.go:261 that says "The sensitive-path block is merged LAST". Replace with an accurate description of the real order: ApplyPolicyOverlay (FIRST, on the engine/catalog result) -> SPATH block -> NUDGE block (LAST), all combined via mergeDecisions most-restrictive-wins; an overlay package_allowlist allow can never downgrade a SPATH or NUDGE block. Keep the CR-02 rationale.
    HARDEN-01 wiring: change the SPATH loop in runCheck to iterate canonicalizePathForms(rawPath) instead of the single canonicalizePath — for each rawPath, get the slice of forms, skip empties, call policy.EvaluatePath on EACH form, and mergeDecisions for each (most-restrictive-wins means a block on ANY form blocks). Apply the IDENTICAL change to runCheckWithIndex in integration_test.go so the test mirror stays faithful to production. Do not alter the overlay-before-SPATH or SPATH-before-NUDGE ordering. If 09-01-SUMMARY.md named the helper differently than canonicalizePathForms, use the actual name from that summary.
  </action>
  <verify>
    <automated>go test ./internal/check/ -v</automated>
  </verify>
  <done>handler.go comment states overlay -> SPATH -> NUDGE accurately; both runCheck and runCheckWithIndex iterate all canonical forms and block on any form; the full internal/check suite passes; the ancestor-symlink path (Plan 01's helper) now blocks through the live pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config layers (system/user/project/env/flags) → merged Config | A project-controlled `.beekeeper.json` (potentially in an agent-writable repo) sets `nudge.*`. The merge must honor a project DISABLE of the nudge (operator intent) but must never let a partial project layer silently zero a security-relevant lower-layer setting. |
| agent tool call → runCheck SPATH | Ancestor-symlink credential paths (HARDEN-01) reach the SPATH evaluator; the dual-form wiring must block on either form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering / Repudiation | LoadLayered merge of Nudge | mitigate | A future LoadLayered consumer reading cfg.Nudge without a nil-check silently gets nil (nudge silently disabled), or a partial project layer zeros lower-layer floors. Fix: mergeNudge does src-wins-if-set field merge + LoadLayered guarantees a non-nil, ValidateNudgeConfig-validated Nudge (fail-closed on invalid). Consumer nil-guards retained as defense-in-depth. |
| T-09-08 | Information Disclosure | runCheck SPATH dual-form (HARDEN-01) | mitigate | Ancestor-symlink credential read could pass the single-form SPATH check. Fix: iterate canonicalizePathForms and block on any matching form. Fail-closed preserved. |
| T-09-09 | Repudiation (doc accuracy) | handler.go merge-order comment | mitigate | A stale comment misrepresenting the merge order invites a future change that reorders SPATH/NUDGE and downgrades a block. Fix: comment now states the real overlay -> SPATH -> NUDGE most-restrictive-wins order. |

Project-layer config is treated as authoritative for an explicit nudge DISABLE (matches NUDGE-08/PRD §11), but the merge never zeros lower-layer security settings from absent project fields. No fail-open path is introduced.
</threat_model>

<verification>
- `go test ./internal/config/ ./internal/check/ ./cmd/beekeeper/ -v` green.
- Layered-config tests assert directly on LoadLayered output (no consumer helper) for defaulting + project override.
- `go test ./internal/check/ -v` includes the integration tests proving the dual-form SPATH wiring (ancestor-symlink blocks).
- `go build ./...` clean; `go vet ./internal/config/... ./internal/check/... ./cmd/beekeeper/...` clean.
- No nudge runtime behavior regression in `beekeeper nudge status|check`.
</verification>

<success_criteria>
- CLEAN-02: LoadLayered merges the Nudge pointer at its root; a layered-config test proves defaulting + project override without a consumer helper; consumer guards documented as defense-in-depth.
- CLEAN-04: handler.go decision-merge comment corrected to overlay -> SPATH -> NUDGE.
- HARDEN-01: the ancestor-symlink credential read blocks through runCheck/runCheckWithIndex via dual-form canonicalization.
</success_criteria>

<output>
After completion, create `.planning/phases/09-v1.2.0-tech-debt-cleanup/09-03-SUMMARY.md`.
</output>
