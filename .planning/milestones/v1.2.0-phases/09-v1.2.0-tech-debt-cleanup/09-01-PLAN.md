---
phase: 09-v1.2.0-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/check/paths.go
  - internal/check/paths_test.go
  - internal/policy/path.go
  - internal/policy/path_test.go
autonomous: true
requirements: [HARDEN-01, HARDEN-02, HARDEN-03]
must_haves:
  truths:
    - "A credential read whose ancestor directory is a symlink still blocks (the /.aws/ or /.ssh/ fragment survives normalization)"
    - "A Windows ADS reference (id_rsa:stream) and a trailing-dot/space basename (credentials. / 'credentials ') block fail-closed"
    - "A bash command containing a verb-substring token (e.g. ./catalog.sh) does not false-trigger credential extraction, while real `more ~/.ssh/id_rsa` still flags"
    - "internal/policy/path.go remains pure (TestPathImportsArePure stays green); all FS/OS-string normalization lives in internal/check/paths.go"
  artifacts:
    - path: "internal/check/paths.go"
      provides: "Symlink double-form helper + word-boundary verb matching (impure adapter)"
    - path: "internal/policy/path.go"
      provides: "ADS/trailing-dot-tolerant basename matching in matchesBlockPattern/isAllowedPath (pure)"
    - path: "internal/check/paths_test.go"
      provides: "Regression tests for HARDEN-01/02/03 that fail on pre-fix code"
    - path: "internal/policy/path_test.go"
      provides: "Pure basename-normalization unit tests + purity guard unchanged"
  key_links:
    - from: "internal/check/handler.go runCheck SPATH block"
      to: "canonicalizePath* + policy.EvaluatePath"
      via: "extractPathTargets → canonicalize → EvaluatePath → mergeDecisions (handler wiring lands in Plan 03)"
      pattern: "canonicalizePathForms"
---

<objective>
Close the three SPATH credential-exfil evasion edges deferred as Info findings in 07-REVIEW.md (IN-01/02/03):
- HARDEN-01: an ancestor symlink can make EvalSymlinks resolve away a sensitive fragment, dodging the blocklist.
- HARDEN-02: Windows Alternate Data Streams (`id_rsa:stream`) and trailing-dot/space basenames (`credentials.`, `credentials `) slip past basename/glob matching.
- HARDEN-03: bash read-verb extraction matches verb substrings without a left word boundary, so a token like `./catalog.sh` can be mis-handled.

Purpose: Beekeeper is a security tool; each of these is a real bypass of the credential-read block (F2). The fixes must remain fail-closed and must not regress any existing SPATH behavior proven in Phase 7.

Output: Hardened `internal/check/paths.go` (impure adapter — adds `canonicalizePathForms` + word-boundary verb matching) + ADS/trailing-dot-tolerant matching in `internal/policy/path.go` (pure), each covered by a regression test that fails on the pre-fix code. The handler/integration SPATH call sites that consume `canonicalizePathForms` are wired in Plan 03 (which owns handler.go) — this plan delivers and unit-proves the helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v1.2.0-tech-debt-cleanup/09-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. Use directly — no exploration needed. -->

internal/check/paths.go (impure adapter — FS/env/OS-string normalization lives HERE):
  func canonicalizePath(raw string) string
    // current sequence: expandWinEnvVars -> expandHome -> filepath.Abs -> filepath.EvalSymlinks (fallback to Abs on error) -> filepath.ToSlash
  var bashReadVerbs = []string{"cat ","head ","tail ","less ","more ","type ","Get-Content ","gc "}  // current: trailing-space substring match
  func extractBashCredentialPaths(cmd string) []string  // current: strings.Index moving-offset scan per verb; skips leading "-" flag tokens
  func firstShellToken(rest string) string  // strips quotes, stops at whitespace

internal/policy/path.go (PURE — imports only "strings"; TestPathImportsArePure forbids os/net/net/http/io/sync/time/context):
  func EvaluatePath(resolvedPath string, cfg SensitivePathConfig) Decision  // allowlist-first, then blocklist
  func matchesBlockPattern(resolvedPath, pattern string) bool  // fragment (contains "/" or "\\") -> Contains; else basename via lastSegment
  func isAllowedPath(resolvedPath, allow string) bool          // basename or separator-prefix match
  func lastSegment(p string) string                            // splits on "/" and "\\", returns final segment
  func normalizeSlashes(p string) string
  func DefaultSensitivePaths() SensitivePathConfig             // BlockPatterns incl. "/.ssh/","/.aws/", basename ".env",".env.*"

internal/policy/path_test.go:
  func TestPathImportsArePure(t)  // parses path.go imports; forbidden = {os,net,net/http,io,sync,time,context} — MUST stay green
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HARDEN-01 — ancestor-symlink double-form helper in paths.go</name>
  <files>internal/check/paths.go, internal/check/paths_test.go</files>
  <read_first>internal/check/paths.go (canonicalizePath ~134-163), internal/check/paths_test.go (TestCanonicalizePath ~85-151), internal/policy/path.go (EvaluatePath/matchesBlockPattern)</read_first>
  <behavior>
    - Plant an ancestor symlink: real dir T (t.TempDir), symlink L -> T, request canonicalizePathForms(L + "/.aws/credentials"). The pre-fix single-form canonicalizePath can EvalSymlinks the ancestor and yield a resolved path under T that no longer textually carries the /.aws/ fragment in a matchable shape — the regression test asserts the /.aws/ fragment IS present in at least one returned form.
    - Both forms must be returned: the lexically-cleaned (pre-EvalSymlinks, Abs+ToSlash) form AND the post-EvalSymlinks form. A downstream block on EITHER form blocks.
    - Real `~/.aws/credentials` (non-existent, EvalSymlinks errors) still yields a form containing /.aws/ (existing Pitfall-3 fallback preserved).
    - os.Symlink may fail on Windows without privilege: gate the symlink-planting sub-test with a skip when os.Symlink returns a permission error (t.Skip with a clear reason), so the suite stays green on the unprivileged Windows dev box; CI Linux/macOS exercise it.
  </behavior>
  <action>
    Add an exported-within-package helper `canonicalizePathForms(raw string) []string` in internal/check/paths.go that returns BOTH the lexically-cleaned form (expandWinEnvVars -> expandHome -> filepath.Abs -> ToSlash, WITHOUT EvalSymlinks) and the EvalSymlinks-resolved form (current canonicalizePath output), de-duplicated (drop empty, drop exact duplicates). Keep the existing `canonicalizePath` returning the EvalSymlinks-resolved form unchanged for backward compatibility of existing callers/tests. Do NOT edit handler.go or integration_test.go here — they are owned by Plan 03, which wires canonicalizePathForms into the two SPATH loops; add a doc-comment note on canonicalizePathForms pointing to Plan 03 for the consumer wiring. Prove the helper in paths_test.go: a test calls canonicalizePathForms on the ancestor-symlink path and asserts AT LEAST ONE returned form contains "/.aws/". Do NOT move EvalSymlinks or any os call into internal/policy (purity gate). Reference IN-01 in the doc comment.
  </action>
  <verify>
    <automated>go test ./internal/check/ -run TestCanonicalizePath -v</automated>
  </verify>
  <done>canonicalizePathForms exists, returns both lexical and symlink-resolved forms (de-duplicated); the ancestor-symlink regression test asserts a /.aws/ fragment survives in at least one form (skips cleanly when os.Symlink is unprivileged); canonicalizePath unchanged for existing callers; go vet clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HARDEN-02 — Windows ADS + trailing-dot/space basename normalization (pure + adapter)</name>
  <files>internal/policy/path.go, internal/policy/path_test.go, internal/check/paths_test.go</files>
  <read_first>internal/policy/path.go (matchesBlockPattern ~128-148, isAllowedPath ~159-184, lastSegment ~187-193), internal/policy/path_test.go (TestEvaluatePath table + TestPathImportsArePure ~196-229), internal/check/paths.go (canonicalizePath)</read_first>
  <behavior>
    - Pure path.go: a basename "id_rsa:fakestream" matches a basename block pattern the same as "id_rsa"; ".env:stream" matches the ".env" basename pattern; "credentials." and "credentials " (trailing dot / trailing space) match as if "credentials". The fragment match for "/.aws/credentials:stream" still blocks via the "/.aws/" fragment pattern (already works) — the new coverage is the BASENAME branch.
    - Normalization for match purposes strips a trailing `:streamname` ADS suffix and trims trailing dots/spaces from the segment under test, applied inside the basename comparison only (does not mutate the stored/returned path).
    - Allowlist must remain correct: ".env.example:stream" and ".env.example." must still be ALLOWED (allowlist normalization mirrors blocklist normalization on the segment side so an attacker cannot un-allowlist a safe file or smuggle a blocked file past the allow check).
    - Purity: path.go must NOT import os/net/etc. TestPathImportsArePure stays green.
  </behavior>
  <action>
    In internal/policy/path.go add a pure helper `normalizeBasename(seg string) string` that (1) drops a trailing NTFS ADS suffix — seg is already a last-segment with no path separators, so treat ANY ':' as an ADS separator and cut from the first ':' — and (2) trims trailing '.' and ' ' via strings.TrimRight(seg, ". "). Apply normalizeBasename to the segment inside BOTH matchesBlockPattern's basename branch AND isAllowedPath's basename branch (normalize seg before the exact/glob comparison; patterns do not carry ADS/trailing-dot forms, so normalize only the seg side). Keep fragment-pattern matching unchanged. Add the ADS/trailing-dot cases to the existing TestEvaluatePath table in path_test.go (OS-agnostic pure-string tests — no //go:build windows needed for the pure layer). Reference IN-02.
    In internal/check/paths_test.go (the impure adapter side) add Windows-gated coverage that the end-to-end canonicalize+evaluate path blocks `%USERPROFILE%\.ssh\id_rsa:fakestream` and `%USERPROFILE%\.ssh\credentials.` — use a runtime `if runtime.GOOS != "windows" { t.Skip(...) }` sub-test so the Windows dev box exercises them and other OSes skip. Do not add OS string-stripping to canonicalizePath itself unless a backslash/forward-slash issue requires it; prefer that the pure normalizeBasename in path.go does the work after ToSlash, and document any exception in canonicalizePath.
  </action>
  <verify>
    <automated>go test ./internal/policy/ -run "TestEvaluatePath|TestPathImportsArePure" -v</automated>
  </verify>
  <done>normalizeBasename strips ADS suffix + trailing dots/spaces; id_rsa:stream / credentials. / 'credentials ' / .env:stream block via the basename branch; .env.example:stream and .env.example. stay allowlisted; TestPathImportsArePure green; Windows-gated adapter test asserts the full canonicalize->evaluate path blocks the ADS/trailing-dot forms (skips on non-Windows).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HARDEN-03 — word-boundary-anchored read-verb extraction</name>
  <files>internal/check/paths.go, internal/check/paths_test.go</files>
  <read_first>internal/check/paths.go (bashReadVerbs ~173-182, extractBashCredentialPaths ~233-267, firstShellToken ~193-216), internal/check/paths_test.go (TestExtractBashCredentialPaths ~325)</read_first>
  <behavior>
    - `more ~/.ssh/id_rsa` -> extracts "~/.ssh/id_rsa" (real verb still flags).
    - `cat /tmp/a && cat ~/.ssh/id_rsa` -> extracts both (existing CR-01 behavior preserved).
    - `cat -n ~/.ssh/id_rsa` -> extracts "~/.ssh/id_rsa" (leading-flag skip preserved).
    - The real gap is a LEADING boundary: `./catalog.sh ~/.ssh/id_rsa` must NOT match the "cat " inside "catalog.sh", and `scatter ~/.ssh/id_rsa` must NOT match the "cat" inside "scatter". Assert that a verb appearing as a non-word-start substring does NOT extract a credential token.
    - Standalone verbs at start-of-string or after a shell separator (space, tab, `&&`, `||`, `;`, `|`, newline, `(`) DO match.
  </behavior>
  <action>
    Replace the bare `strings.Index(cmd[from:], verb)` substring scan with a left-boundary-anchored scan: match each verb only when the character immediately PRECEDING the match index is start-of-string OR a shell token boundary byte {space, tab, '\n', '\r', ';', '|', '&', '('}. Keep the existing trailing space in each verb token (which already enforces the right-hand boundary). Preserve the moving-offset all-occurrences loop (CR-01) and the leading-flag-skip loop (CR-01). Add a small pure helper `isShellBoundary(b byte) bool`. Update the bashReadVerbs doc comment to describe the boundary rule and reference IN-03. Extend TestExtractBashCredentialPaths with: standalone `more ~/.ssh/id_rsa` flags; `./catalog.sh ~/.ssh/id_rsa` and `scatter ~/.ssh/id_rsa` do NOT extract a credential token from the embedded "cat"; chained `cat a && cat ~/.ssh/id_rsa` still extracts both; `cat -n ~/.ssh/id_rsa` still extracts.
  </action>
  <verify>
    <automated>go test ./internal/check/ -run TestExtractBashCredentialPaths -v</automated>
  </verify>
  <done>Read-verb extraction is left-boundary-anchored via isShellBoundary; embedded-substring verbs (catalog.sh, scatter) do not false-trigger; real `more ~/.ssh/id_rsa`, chained reads, and `cat -n` paths still extract; all existing TestExtractBashCredentialPaths cases pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent tool call → `beekeeper check` | Untrusted `file_path` / Bash `command` strings crafted by a hijacked/off-task agent cross into the SPATH evaluator. An attacker controls the exact string shape to dodge the credential-read block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure / Tampering | canonicalizePath EvalSymlinks (HARDEN-01) | mitigate | Ancestor symlink can resolve a `/.aws/` or `/.ssh/` fragment out of the canonical path, exfiltrating credentials. Fix: canonicalizePathForms returns BOTH the lexically-cleaned (pre-EvalSymlinks) and post-EvalSymlinks forms; a downstream block on either form blocks. Fail-closed on EvalSymlinks error preserved (fallback to Abs). |
| T-09-02 | Information Disclosure | policy.matchesBlockPattern basename branch (HARDEN-02) | mitigate | Windows ADS (`id_rsa:stream`) reads the credential stream while the basename "id_rsa:stream" != "id_rsa"; trailing-dot/space basenames (`credentials.`) are distinct strings the FS treats as `credentials`. Fix: normalizeBasename strips ADS suffix + trailing dots/spaces before block matching; allowlist normalized identically so the allow path cannot be abused. Pure, no I/O. |
| T-09-03 | Information Disclosure / Evasion | extractBashCredentialPaths verb matching (HARDEN-03) | mitigate | Verb-substring matching can mis-handle a token like `./catalog.sh`. Fix: left-boundary-anchored verb matching (isShellBoundary) so only standalone verbs at a shell boundary trigger; real reads still flag. |
| T-09-04 | Elevation (purity break) | internal/policy/path.go | mitigate | Adding OS/FS string handling to the pure package would break the one-implementation-three-consumers contract. Fix: all FS/symlink work stays in internal/check/paths.go (impure); only pure string normalization (normalizeBasename) goes in path.go. TestPathImportsArePure is the gate. |

All four threats are mitigated (not accepted) — each closes a real bypass of the F2 credential-read block. Every fix remains fail-closed: a normalization error or symlink-resolution error must never turn a block into an allow.
</threat_model>

<verification>
- `go test ./internal/check/ ./internal/policy/ -v` green (full check + policy suites — no SPATH regression).
- `go test ./internal/policy/ -run TestPathImportsArePure` green (purity preserved).
- Each HARDEN regression test FAILS when reverted against pre-fix code (executor confirms by reasoning from the test shape / temporarily reverting the fix).
- `go vet ./internal/check/... ./internal/policy/...` clean.
- Note: `-race` is CI-only on this Windows box (CGO-gated); plain `go test` is authoritative locally, CI confirms race.
</verification>

<success_criteria>
- HARDEN-01: canonicalizePathForms returns dual forms so an ancestor-symlink credential path is matchable on at least one form; regression test fails on pre-fix single-form code (skips cleanly when os.Symlink is unprivileged). Consumer wiring lands in Plan 03.
- HARDEN-02: `id_rsa:stream`, `credentials.`, `credentials ` block fail-closed; Windows-gated adapter tests + OS-agnostic pure tests cover them; allowlist lookalikes unaffected.
- HARDEN-03: read-verb extraction left-boundary-anchored; embedded-substring verbs do not false-trigger; real `more ~/.ssh/id_rsa` still flags.
- internal/policy/path.go stays pure (TestPathImportsArePure green).
</success_criteria>

<output>
After completion, create `.planning/phases/09-v1.2.0-tech-debt-cleanup/09-01-SUMMARY.md`.
</output>
