---
phase: 07-sensitive-path-runtime-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - internal/check/handler.go
  - internal/check/handler_test.go
  - internal/check/integration_test.go
autonomous: true
requirements: [SPATH-01, SPATH-02, SPATH-03, SPATH-04]
must_haves:
  truths:
    - "RunCheck blocks a Read tool call with file_path ~/.aws/credentials (exit 1 + decision block)"
    - "RunCheck blocks traversal file_path ../../.aws/credentials (exit 1)"
    - "RunCheck blocks Windows form C:\\Users\\u\\.aws\\credentials (exit 1)"
    - "RunCheck blocks Bash cat ~/.ssh/id_rsa AND type %USERPROFILE%\\.ssh\\id_rsa (exit 1 + sensitive-path-policy rule id)"
    - "TestRunCheckBashTypeUserProfileBlocks calls t.Setenv(\"USERPROFILE\", ...) so the full D-01 chain (extractBashCredentialPaths -> canonicalizePath -> expandWinEnvVars -> EvaluatePath) is verified end-to-end, not a false pass where the raw token coincidentally fails a different check"
    - "RunCheck allows .env.example / .env.test / .env.schema (exit 0 + allow) and still blocks .env.production"
    - "The path block fires independently of catalog matching (also wired into runCheckWithIndex test path)"
    - "Each block emits an NDJSON audit record with decision:\"block\""
  artifacts:
    - path: "internal/check/handler.go"
      provides: "Path-evaluation + mergeDecisions block in runCheck (after policy.Evaluate, before ApplyPolicyOverlay) and in runCheckWithIndex"
      contains: "EvaluatePath"
    - path: "internal/check/handler_test.go"
      provides: "RunCheck integration tests for all four success criteria + audit-record assertions"
      contains: "TestRunCheckCredentialFileBlocks"
  key_links:
    - from: "internal/check/handler.go runCheck"
      to: "policy.EvaluatePath via extractPathTargets + canonicalizePath"
      via: "path block inserted after policy.Evaluate, before ApplyPolicyOverlay"
      pattern: "EvaluatePath"
    - from: "internal/check/handler.go runCheckWithIndex"
      to: "policy.EvaluatePath"
      via: "same path block so integration tests exercise wiring without a catalog match"
      pattern: "extractPathTargets"
    - from: "RunCheck block decision"
      to: "audit NDJSON record decision:block + rule_ids sensitive-path-policy"
      via: "finalizeWithAC chokepoint"
      pattern: "sensitive-path-policy"
---

<objective>
Wire the sensitive-path adapter (from 07-02) and the engine (from 07-01) into the LIVE
`beekeeper check` pipeline so credential reads actually block — closing finding F2. Insert
the path-evaluation + `mergeDecisions` block into `runCheck` (after `policy.Evaluate` at
~line 257, before `ApplyPolicyOverlay`) AND into the `runCheckWithIndex` test path so the
block fires independent of catalog matching. Then add `RunCheck` integration tests that drive
the full stdin→exit-code→audit-record path for all four success criteria (the PRIMARY signal
per Pitfall 5 — wiring proven live, not just isolated EvaluatePath correctness).

Purpose: SPATH-01/02/03/04 success criteria SC1–SC4 are observable only at the RunCheck level.
This plan is where the phase goal becomes true. D-03 scope: `beekeeper check` (runCheck) ONLY —
do NOT replicate Phase 6's gateway/watch/scan fan-out.

Output: Updated `internal/check/handler.go` (path block in both runCheck + runCheckWithIndex);
`internal/check/handler_test.go` with the nine RunCheck integration tests from 07-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sensitive-path-runtime-enforcement/07-CONTEXT.md
@.planning/phases/07-sensitive-path-runtime-enforcement/07-RESEARCH.md
@.planning/phases/07-sensitive-path-runtime-enforcement/07-VALIDATION.md

<interfaces>
<!-- Contracts produced by 07-01 and 07-02 that this plan consumes. -->

From internal/check/paths.go (produced in 07-02):
- func extractPathTargets(tc policy.ToolCall) []string
- func canonicalizePath(raw string) string
- func mergeDecisions(base, overlay policy.Decision) policy.Decision

From internal/policy/path.go (updated in 07-01):
- func EvaluatePath(resolvedPath string, cfg SensitivePathConfig) Decision  // block reason "sensitive path blocked: ..."; RuleIDs ["sensitive-path-policy"]
- func DefaultSensitivePaths() SensitivePathConfig  // now includes /.cursor/, /.windsurf/, bare /.cargo/credentials; AllowPatterns .env.example/.env.test/.env.schema

Insertion point — internal/check/handler.go runCheck (read in full; key lines):
- line 257: decision := policy.Evaluate(toolCall, multiIdx, thresholds, ac)   ← INSERT path block AFTER
- line 272-274: if len(policyFiles) > 0 { decision = policyloader.ApplyPolicyOverlay(...) }  ← path block runs BEFORE this
- line 278: return finalizeWithAC(decision, cfg, toolCall, auditPath, ac)   ← single chokepoint (exit code + audit)
- const exitAllow = 0; const exitBlock = 1 (handler.go:43-44)
- func RunCheck(ctx, stdin, cfg, indexPath, auditPath, cacheDir string) Result

Second insertion point — internal/check/integration_test.go runCheckWithIndex:
- line 66: decision := policy.Evaluate(toolCall, idx, DefaultCorroborationThresholds(), AgentContext{})  ← INSERT same path block AFTER
- NOTE: runCheckWithIndex is the path the credential-block-without-catalog-match tests use (07-CONTEXT "Specific Ideas"). It lives in integration_test.go (a _test.go file). Mirror the production path block there so the wiring is exercised hermetically.

Existing test helpers (reuse — do not recreate):
- buildTestIndex(t, dir) string                 (handler_test.go:21)
- closedConfig() config.Config                  (handler_test.go:41)
- auditPathIn(t) string                         (handler_test.go:43)
- readLastAuditRecord(t, auditPath) audit.AuditRecord  (integration_test.go:76)
- audit.AuditRecord.Decision string (json "decision"); audit.AuditRecord.RuleIDs []string (json "rule_ids")
- (no containsRuleID helper exists yet — add a small local helper or inline a range loop over rec.RuleIDs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the path-evaluation block into runCheck and runCheckWithIndex</name>
  <files>internal/check/handler.go, internal/check/integration_test.go</files>
  <read_first>
    - internal/check/handler.go (FULL runCheck, ~lines 101-279 — the file being modified; confirm line 257 policy.Evaluate, line 272 ApplyPolicyOverlay, line 278 finalizeWithAC, exit constants)
    - internal/check/integration_test.go (runCheckWithIndex, ~lines 36-73 — the second insertion point; note it has no ApplyPolicyOverlay)
    - internal/check/paths.go (the adapter from 07-02 — extractPathTargets / canonicalizePath / mergeDecisions signatures)
    - internal/policy/path.go (EvaluatePath + DefaultSensitivePaths from 07-01)
    - .planning/phases/07-sensitive-path-runtime-enforcement/07-RESEARCH.md "Code Examples" Example 1 (insertion-point block) + Q2 (wiring point, before-overlay rationale)
    - .planning/phases/07-sensitive-path-runtime-enforcement/07-CONTEXT.md D-03 (check-only scope — gateway/watch/scan OUT)
  </read_first>
  <action>
    In runCheck (handler.go), AFTER `decision := policy.Evaluate(...)` (line 257) and BEFORE the
    `ApplyPolicyOverlay` block (line 272), insert the path-evaluation loop per 07-RESEARCH.md Example 1:
    build `spathCfg := policy.DefaultSensitivePaths()`; for each rawPath in `extractPathTargets(toolCall)`,
    `resolved := canonicalizePath(rawPath)`, skip if empty, `pathDecision := policy.EvaluatePath(resolved, spathCfg)`,
    `decision = mergeDecisions(decision, pathDecision)`. Running BEFORE the overlay means a JSON policy-file
    sensitive_path rule (escape hatch) still applies on top, and a path block can be escalated but not silently
    downgraded. Do NOT touch gateway/watch/scan (D-03). Add a short comment citing SPATH-01/02/03 and D-03 scope.

    Insert the IDENTICAL path block into runCheckWithIndex (integration_test.go) AFTER its `policy.Evaluate`
    call (line 66) and before `finalize`, so the credential block fires even when the catalog index returns no
    match (07-CONTEXT "Specific Ideas"). Keep the two blocks behaviorally identical (consider a tiny shared
    unexported helper in paths.go, e.g. `applyPathPolicy(toolCall, decision) policy.Decision`, called from both —
    optional but reduces drift; if added, unit-test it is not required since both call sites are integration-tested).

    NO fenced code in the action — reference Example 1 for the exact block.
  </action>
  <verify>
    <automated>go build ./internal/check/... && go test ./internal/check/... -count=1 -timeout=60s</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/check/...` succeeds
    - internal/check/handler.go contains a call to extractPathTargets and policy.EvaluatePath between policy.Evaluate (line ~257) and policyloader.ApplyPolicyOverlay (line ~272)
    - internal/check/handler.go contains a call to mergeDecisions
    - internal/check/integration_test.go runCheckWithIndex also calls extractPathTargets + policy.EvaluatePath (path block fires without a catalog match)
    - handler.go does NOT add SPATH wiring to any gateway/watch/scan path (D-03 — grep the diff confirms only runCheck changed in production code)
    - `go test ./internal/check/... -count=1 -timeout=60s` exits 0 (existing tests still green after wiring)
  </acceptance_criteria>
  <done>The sensitive-path engine is wired into the live runCheck pipeline (and the hermetic runCheckWithIndex test path); the path block runs before the overlay and is independent of catalog matching; scope is check-only (D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RunCheck integration tests for SC1-SC4 with audit-record assertions</name>
  <files>internal/check/handler_test.go</files>
  <read_first>
    - internal/check/handler_test.go (the Phase 6 RunCheck integration tests ~lines 659-817 — the EXACT template to copy: buildTestIndex, closedConfig, auditPathIn, RunCheck call, exit-code + decision assertions)
    - internal/check/integration_test.go (readLastAuditRecord helper ~line 76; runCheckWithIndex for catalog-independent cases)
    - internal/audit/types.go (AuditRecord.Decision json "decision"; AuditRecord.RuleIDs json "rule_ids")
    - .planning/phases/07-sensitive-path-runtime-enforcement/07-VALIDATION.md "Requirement → Test Map" + "Observable Signals Per Success Criterion" (exact test names + assertion shapes)
    - .planning/phases/07-sensitive-path-runtime-enforcement/07-RESEARCH.md Q6 (test table, helper inventory) + Pitfall 5 (test the wired pipeline, not just EvaluatePath)
  </read_first>
  <behavior>
    - TestRunCheckCredentialFileBlocks: Read + file_path "~/.aws/credentials" → res.ExitCode==exitBlock, res.Decision.Level=="block", res.Decision.Allow==false, last audit record .Decision=="block" and .RuleIDs contains "sensitive-path-policy" (SC1+SC4)
    - TestRunCheckTraversalBlocks: Read + file_path "../../.aws/credentials" → exitBlock + block (SC1, SPATH-02)
    - TestRunCheckWindowsCredentialBlocks: Read + file_path "C:\\Users\\u\\.aws\\credentials" → exitBlock (SPATH-02 Windows form)
    - TestRunCheckBashCatCredentialBlocks: Bash + command "cat ~/.ssh/id_rsa" → exitBlock + block + rule id sensitive-path-policy (SC2, SPATH-03)
    - TestRunCheckBashTypeUserProfileBlocks: Bash + command "type %USERPROFILE%\\.ssh\\id_rsa" → exitBlock + block (SC2 full, D-01); use t.Setenv("USERPROFILE", ...) so the expansion resolves to a path containing /.ssh/
    - TestRunCheckEnvExampleAllowed / TestRunCheckEnvTestAllowed / TestRunCheckEnvSchemaAllowed: Read + file_path .env.example/.test/.schema → res.ExitCode==exitAllow, res.Decision.Level=="allow" (SC3, SPATH-04)
    - TestRunCheckEnvProductionBlocked: Read + file_path .env.production → exitBlock (.env.* glob intact)
  </behavior>
  <action>
    Add the nine integration tests named above to handler_test.go, copying the Phase 6 RunCheck pattern
    (handler_test.go:659-817). Each test: t.TempDir(); idxPath := buildTestIndex(t, dir) (a clean index — the
    package under test is NOT in the catalog, proving the path block fires independent of catalog matching);
    cacheDir := filepath.Join(dir,"catalogs") + os.MkdirAll(...,0700); auditPath := auditPathIn(t); stdin :=
    strings.NewReader(<raw JSON>); res := RunCheck(ctx, stdin, closedConfig(), idxPath, auditPath, cacheDir).

    Block tests assert res.ExitCode==exitBlock AND res.Decision.Level=="block" AND !res.Decision.Allow, then
    readLastAuditRecord(t, auditPath) and assert rec.Decision=="block" (SC4). For SC2 tests also assert rec.RuleIDs
    contains "sensitive-path-policy" — add a small local helper (e.g. hasRuleID(rec.RuleIDs, "sensitive-path-policy"))
    or an inline range loop since no containsRuleID helper exists. Allow tests assert res.ExitCode==exitAllow AND
    res.Decision.Level=="allow". For TestRunCheckBashTypeUserProfileBlocks call t.Setenv("USERPROFILE", t.TempDir())
    (or a fixed value whose path, after expansion, contains the credential fragment) — never set HOME on Windows
    (Phase 2 Pitfall-5 isolation decision).

    Use the exact test names from 07-VALIDATION.md so the Requirement→Test Map commands resolve.
  </action>
  <verify>
    <automated>go test ./internal/check/... -run "TestRunCheckCredentialFileBlocks|TestRunCheckTraversalBlocks|TestRunCheckWindowsCredentialBlocks|TestRunCheckBashCatCredentialBlocks|TestRunCheckBashTypeUserProfileBlocks|TestRunCheckEnvExampleAllowed|TestRunCheckEnvTestAllowed|TestRunCheckEnvSchemaAllowed|TestRunCheckEnvProductionBlocked" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/check/... -run "TestRunCheckCredentialFileBlocks|TestRunCheckTraversalBlocks|TestRunCheckWindowsCredentialBlocks|TestRunCheckBashCatCredentialBlocks|TestRunCheckBashTypeUserProfileBlocks|TestRunCheckEnvExampleAllowed|TestRunCheckEnvTestAllowed|TestRunCheckEnvSchemaAllowed|TestRunCheckEnvProductionBlocked" -count=1` exits 0
    - handler_test.go contains all nine func names listed in the verify command
    - The block tests assert BOTH res.ExitCode==exitBlock AND readLastAuditRecord(...).Decision=="block" (SC4 — live wiring, not isolated EvaluatePath)
    - The SC2 Bash tests assert rec.RuleIDs contains "sensitive-path-policy"
    - The full phase suite `go test ./internal/policy/... ./internal/check/... ./internal/policyloader/... -count=1 -timeout=60s` exits 0
  </acceptance_criteria>
  <done>All four success criteria (SC1-SC4) are proven at the RunCheck level with exit-code AND audit-record assertions; finding F2 (credential reads passing through allow) is closed; the wiring is demonstrably live (Pitfall 5 addressed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent stdin JSON → runCheck | Untrusted tool call enters the live enforcement pipeline; the path block is the new control point |
| merged decision → audit sink | Block decisions must be forensically recorded (decision:"block" NDJSON) at the finalizeWithAC chokepoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | Wiring gap — runCheck never calls EvaluatePath (the F2 failure) | mitigate | Path block inserted in runCheck + runCheckWithIndex; proven by RunCheck integration tests asserting exit code + audit record (Pitfall 5, SC4) |
| T-07-12 | Repudiation | Block not recorded in audit log | mitigate | finalizeWithAC chokepoint already writes NDJSON; tests assert readLastAuditRecord(...).Decision=="block" (SC4) |
| T-07-13 | Tampering | Policy-file overlay silently downgrades a path block to allow | mitigate | Path block runs BEFORE ApplyPolicyOverlay; sensitive_path overlay only supports block/warn (no allow branch — 07-RESEARCH Q5); engine-level AllowPatterns is the controlled escape hatch, recorded in audit reason (T-09-31 forensically visible) |
| T-07-14 | Denial of Service | Path eval slows the hot check path | accept | extractPathTargets + canonicalizePath are O(targets) string/stat ops, no network; within execTimeout budget; ctx.Err() check after Evaluate still fail-closes |

No `high`-severity unmitigated threats. T-07-11 is the central F2 closure and is mitigated + test-proven.
</threat_model>

<verification>
- `go test ./internal/policy/... ./internal/check/... ./internal/policyloader/... -count=1 -timeout=60s` is green (the 07-VALIDATION.md full-suite command).
- Quick-run from 07-VALIDATION.md passes: the nine RunCheck tests + paths_test + policy/path tests resolve and pass.
- Manual smoke (optional, redundant with Phase 8 E2E gate): `echo '{"tool_name":"Read","tool_input":{"file_path":"~/.aws/credentials"}}' | beekeeper check; echo $?` → 1.
- Diff review confirms D-03: SPATH wiring touches only runCheck (+ the runCheckWithIndex test path), not gateway/watch/scan.
</verification>

<success_criteria>
- SC1: ~/.aws/credentials AND ../../.aws/credentials block at RunCheck (exit 1 + decision block).
- SC2: Bash cat ~/.ssh/id_rsa AND type %USERPROFILE%\.ssh\id_rsa detected (exit 1 + sensitive-path-policy rule id) (D-01).
- SC3: .env.example / .env.test / .env.schema NOT blocked (exit 0 + allow); .env.production still blocks (SPATH-04).
- SC4: integration tests assert exit code AND decision:"block" audit record — wiring proven live (Pitfall 5).
- D-03 scope honored: check-only.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sensitive-path-runtime-enforcement/07-03-SUMMARY.md`
</output>
