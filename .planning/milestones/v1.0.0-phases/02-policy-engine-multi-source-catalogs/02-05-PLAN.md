---
phase: 02-policy-engine-multi-source-catalogs
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/catalog/socket.go
  - internal/catalog/socket_test.go
autonomous: true
requirements: [CTLG-03]
must_haves:
  truths:
    - "A Socket PURL query with a configured Bearer token returns matching catalog entries"
    - "With no Socket token configured, the Socket source is disabled gracefully (not an error)"
    - "A second query within 24h is served from disk cache without a network call"
    - "An HTTP 429 triggers exponential backoff honoring Retry-After, capped at 5 retries"
    - "Socket unavailability degrades to warn-only (returns degraded=true), never fail-closes the whole check"
    - "The v0/purl deprecation (removal 2026-07-30) and migration to /v0/packages is documented in code"
  artifacts:
    - path: "internal/catalog/socket.go"
      provides: "Socket PURL API adapter with Bearer auth, backoff, disk cache, deprecation TODO"
      contains: "api.socket.dev/v0/purl"
    - path: "internal/config/config.go"
      provides: "Socket api_token config field"
      contains: "SocketAPIToken"
  key_links:
    - from: "internal/catalog/socket.go"
      to: "policy.CatalogMatch"
      via: "SocketAdapter.LookupAll returns []policy.CatalogMatch with CatalogSource socket"
      pattern: "policy.CatalogMatch"
    - from: "internal/catalog/socket.go"
      to: "config.SocketAPIToken"
      via: "adapter reads token from config; empty disables the source"
      pattern: "token"
---

<objective>
Build the Socket PURL catalog source as an HTTP adapter (`POST https://api.socket.dev/v0/purl`) with Bearer-token auth, 24h disk cache, exponential backoff on 429, and graceful degradation. This is the THIRD independent signed source for PLCY-01 corroboration (three sources → block + quarantine recommendation).

Purpose: Socket adds a third corroboration source so the quarantine threshold is reachable. Unlike OSV, Socket requires a Bearer token and is on a deprecation clock (v0/purl removal 2026-07-30) — both handled here. The token field is added to the Phase 1 config (still minimal; full layered config is Phase 9).

Output: `internal/catalog/socket.go` + test, plus a small `internal/config/config.go` extension for the token. Depends on Plan 01 for `policy.CatalogMatch`. The aggregator combining all three sources is Plan 08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-policy-engine-multi-source-catalogs/02-CONTEXT.md
@.planning/phases/02-policy-engine-multi-source-catalogs/02-PATTERNS.md
@.planning/phases/02-policy-engine-multi-source-catalogs/02-01-SUMMARY.md

<interfaces>
From internal/config/config.go (Phase 1 — current state, extend it):
```go
type Config struct {
    FailMode string `json:"fail_mode"`
}
func Load(path string) (Config, error) // missing file → Config{FailMode:"closed"}, nil
func (c Config) FailClosed() bool
```
From internal/catalog/sync.go: fetch idioms, writeFileAtomic (in index.go), io.LimitReader bound.
From internal/policy/types.go (Plan 01): policy.CatalogMatch with provenance fields.
Socket API: POST https://api.socket.dev/v0/purl, header Authorization: Bearer <token>, body is a PURL or PURL list. Returns package risk data. 401 without token (confirmed). Free tier 500 quota/hour. DEPRECATED 2026-01-05, removal 2026-07-30 → migrate to POST /v0/packages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Socket token to config (extend, preserve fail_mode behavior)</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go (full file — extend Config, do not change Load/FailClosed semantics)
    - internal/config/config_test.go (existing fail_mode tests must keep passing)
  </read_first>
  <action>
    Add a nested struct field to `Config`: `Socket SocketConfig `+"`json:\"socket\"`"+`` where `type SocketConfig struct { APIToken string `+"`json:\"api_token\"`"+` }`. Add a convenience accessor `func (c Config) SocketAPIToken() string { return c.Socket.APIToken }`. Do NOT change the FailMode default logic, the missing-file behavior, or FailClosed — only add the new optional field (absent token → "" → Socket disabled). Update the package doc comment to note Socket token is the one Phase 2 addition; full layered config remains Phase 9.

    Add a test `TestSocketTokenLoads`: a config JSON with `{"socket":{"api_token":"tok_abc"}}` loads with SocketAPIToken()=="tok_abc" and FailMode defaulting to "closed". Add `TestSocketTokenAbsentIsEmpty`: a config with only fail_mode → SocketAPIToken()=="".
  </action>
  <verify>
    <automated>go test ./internal/config/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/config/... -count=1` exits 0 (existing fail_mode tests + new Socket tests)
    - `Config` has nested `Socket SocketConfig` with `APIToken string` (json "socket"/"api_token")
    - `Config.SocketAPIToken()` returns the token; absent → ""
    - missing-file and fail_mode default behavior unchanged (existing tests green)
  </acceptance_criteria>
  <done>Config carries an optional socket.api_token without disturbing Phase 1 fail-mode semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Socket PURL adapter with Bearer auth, backoff, cache, graceful degrade</name>
  <files>internal/catalog/socket.go, internal/catalog/socket_test.go</files>
  <read_first>
    - internal/catalog/sync.go (HTTP idioms)
    - internal/catalog/index.go (writeFileAtomic)
    - .planning/phases/02-policy-engine-multi-source-catalogs/02-PATTERNS.md (lines 495–558, socket.go Bearer + backoff + degraded-mode pattern)
    - .planning/phases/02-policy-engine-multi-source-catalogs/02-CONTEXT.md (Socket Public API — CTLG-03, token required, 429 backoff, deprecation, graceful degrade)
  </read_first>
  <action>
    Create `socket.go` in package catalog. Define `const socketPURLURL = "https://api.socket.dev/v0/purl"`. Add a prominent doc comment / TODO block: "DEPRECATION: v0/purl deprecated 2026-01-05, removal 2026-07-30. Migrate to POST https://api.socket.dev/v0/packages before removal. This adapter is structured so migration touches only socket.go: change socketPURLURL, the request body builder, and the response parser." Define `func purlFor(ecosystem, pkg, version string) string` building a Package-URL like `pkg:npm/<pkg>@<version>` with the ecosystem→purl-type map (npm→npm, pypi→pypi, go→golang, cargo→cargo, rubygems→gem, packagist→composer).

    Define the cache record `socketCacheEntry{ CachedAt time.Time; Entries []Entry }`, cache path `<cacheDir>/socket-cache/<ecosystem>/<pkg>/<version>.json` (version "" → "_any").

    Implement `func QuerySocket(ctx context.Context, client *http.Client, cacheDir, token, ecosystem, pkg, version string) (entries []Entry, degraded bool, err error)`:
      - token == "" → return (nil, false, nil): Socket simply disabled, NOT a failure, NOT degraded.
      - cache-first: if cache fresh (<24h) → return (cached, false, nil).
      - POST socketPURLURL with `Authorization: Bearer <token>`, Content-Type application/json, body from purlFor. On HTTP 429: read Retry-After header (seconds) else exponential backoff base 1s doubling, max 60s, up to 5 retries; honor ctx cancellation between sleeps (use a select on `time.After` and `ctx.Done()`). On exhausting retries → return (nil, true, error) [degraded=true].
      - On transport error or 5xx → return (nil, true, err): degraded to warn-only (the corroboration this source would have provided is downgraded, but the check is NOT fail-closed on Socket alone).
      - On 200: bounded read (4MB), parse the Socket response, map to []Entry with CatalogSource "socket", CatalogSignature "socket-api" (treated as signed — authoritative host over TLS). Write cache atomically. Return (entries, false, nil).

    Add `type SocketAdapter struct { Client *http.Client; CacheDir, Token string; Ctx context.Context }` with `func (a SocketAdapter) LookupAll(ecosystem, pkg string) []policy.CatalogMatch`: calls QuerySocket(version ""); on degraded or error returns nil (the aggregator records the degradation to audit, Plan 08); maps entries to policy.CatalogMatch{CatalogSource:"socket", Signed:true, CatalogVersion:"socket-api", ...}.

    Add `socket_test.go` with httptest stubs: TestQuerySocketNoTokenDisabled (token "" → nil,false,nil, no request made — assert with a server that fails the test if hit); TestQuerySocketParsesResponse (200 → entries with CatalogSource "socket"); TestQuerySocketCacheHit (second call with server closed → cached); TestQuerySocket429Backoff (server returns 429 twice then 200 → succeeds within retry budget; use a tiny backoff by injecting via a package-level var or accept the real 1s — prefer a test-injectable backoff base to keep the test fast); TestQuerySocket5xxDegrades (500 → degraded true, err non-nil, no panic); TestSocketAdapterLookupAllMapsMatches.
  </action>
  <verify>
    <automated>go test ./internal/catalog/... -run 'TestQuerySocket|TestSocketAdapter' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/catalog/... -run 'TestQuerySocket|TestSocketAdapter' -count=1` exits 0
    - TestQuerySocketNoTokenDisabled: empty token makes NO HTTP request and returns (nil,false,nil)
    - TestQuerySocket429Backoff: 429→429→200 succeeds within 5 retries (test runs in <3s via injectable backoff base)
    - TestQuerySocket5xxDegrades: 500 yields degraded=true, non-nil err, no panic, no fabricated entries
    - TestQuerySocketCacheHit proves disk cache served with server closed
    - `socket.go` contains the literal string "2026-07-30" and a migration TODO to "/v0/packages"
    - SocketAdapter.LookupAll returns []policy.CatalogMatch with CatalogSource "socket", Signed true
    - cache writes go through writeFileAtomic; cache dir 0o700
  </acceptance_criteria>
  <done>Socket adapter authenticates with Bearer token, backs off on 429, caches for 24h, degrades to warn-only on failure, and documents the 2026-07-30 deprecation/migration path in one file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket API → adapter | Remote JSON untrusted; Bearer token is a secret that must never be logged or cached to disk |
| config → adapter | api_token sourced from ~/.beekeeper/config.json (owner-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | Bearer token leakage | mitigate | Token only set as a request header; never written to cache files, audit, or stderr (mirrors GITHUB_TOKEN handling in sync.go T-02-04) |
| T-02-05-02 | Denial of Service | Socket 429 rate limiting | mitigate | Cache-first (24h TTL) is the primary defense; exponential backoff with Retry-After honored; capped at 5 retries; ctx cancellation respected |
| T-02-05-03 | Denial of Service | Socket outage fail-closing the whole check | mitigate | Socket failure degrades to warn-only (degraded=true) for the corroboration it would have provided — the check proceeds on remaining sources rather than blocking everything |
| T-02-05-04 | Tampering | deprecated endpoint silently failing post-2026-07-30 | mitigate | Migration TODO and dated comment in socket.go; adapter structured so the swap to /v0/packages touches only one file |
</threat_model>

<verification>
- `go test ./internal/config/... ./internal/catalog/... -run 'TestSocket|TestQuerySocket' -count=1` exits 0
- `go vet ./internal/catalog/... ./internal/config/...` exits 0
- `go build ./internal/catalog/... ./internal/config/...` exits 0
</verification>

<success_criteria>
- CTLG-03 Socket PURL adapter with Bearer auth, 24h cache, 429 backoff, graceful degrade
- Empty token disables Socket gracefully (Bumblebee + OSV continue)
- 2026-07-30 deprecation and /v0/packages migration documented in code
- Produces policy.CatalogMatch as the third signed corroboration source
</success_criteria>

<output>
After completion, create `.planning/phases/02-policy-engine-multi-source-catalogs/02-05-SUMMARY.md` documenting QuerySocket/SocketAdapter signatures, the degraded-vs-disabled distinction, the backoff strategy, and the deprecation migration plan.
</output>
