---
phase: 09-policy-as-code-self-defense-capstone
plan: 04
type: execute
wave: 2
depends_on: [09-03-PLAN]
files_modified:
  - internal/llamafirewall/latency.go
  - internal/llamafirewall/latency_test.go
  - internal/check/diag.go
  - internal/check/diag_windows.go
  - internal/check/diag_other.go
  - internal/check/latency_persist.go
  - internal/check/handler.go
  - internal/check/diag_test.go
autonomous: true
requirements: [CODE-06, SWIN-04]
must_haves:
  truths:
    - "beekeeper diag assembles hook latency p95/p99, sidecar inference latency p95, catalog freshness per source, and ETW EventsLost into one report"
    - "Hook latency p95/p99 accumulate across one-shot beekeeper check invocations (persisted, not reset to zero each process)"
    - "ETW EventsLost reads the real Windows atomic counter on Windows and reports 0 on non-Windows via a build-tag stub"
    - "LatencyTracker exposes P99() alongside the existing P95()"
    - "Catalog freshness in diag includes all configured sources, including beekeeper-self"
  artifacts:
    - path: "internal/check/diag.go"
      provides: "DiagReport, CatalogSourceStatus, CollectDiag(stateFile string) DiagReport"
      contains: "func CollectDiag"
    - path: "internal/check/diag_windows.go"
      provides: "eventsLost() reading windows.EventsLost (//go:build windows)"
      contains: "go:build windows"
    - path: "internal/check/diag_other.go"
      provides: "eventsLost() returning 0 (//go:build !windows)"
      contains: "go:build !windows"
    - path: "internal/llamafirewall/latency.go"
      provides: "P99() method on LatencyTracker"
      contains: "func (t *LatencyTracker) P99()"
  key_links:
    - from: "internal/check/diag.go CollectDiag"
      to: "catalog.LoadState(stateFile).Sources"
      via: "reads per-source freshness from state.json"
      pattern: "LoadState"
    - from: "internal/check/diag.go CollectDiag"
      to: "eventsLost()"
      via: "platform-dispatched ETW EventsLost (windows real, other 0)"
      pattern: "eventsLost\\(\\)"
    - from: "internal/check/handler.go runCheck"
      to: "GlobalHookTracker.Record + persisted ring file"
      via: "records elapsed latency at end of runCheck so diag can report accumulated p95/p99"
      pattern: "GlobalHookTracker"
---

<objective>
Implement `beekeeper diag` (CODE-06, SWIN-04): a single report combining hook latency p95/p99, LlamaFirewall sidecar inference latency p95, catalog freshness per source, and ETW `EventsLost`. Three gaps from RESEARCH must be filled: (1) `LatencyTracker` has no `P99()` — add it; (2) the hook handler has no persistent latency time-series — `beekeeper check` is a one-shot process, so add a small persisted ring file written at the end of `runCheck` and read by `diag`; (3) ETW `EventsLost` is Windows-only — add the build-tag pair `diag_windows.go` / `diag_other.go`.

Purpose: All health signals in one human-readable place (Phase 9 success criterion 3) so the developer can see latency, freshness, and detection-coverage degradation without separate commands.
Output: `DiagReport` + `CollectDiag` in internal/check, the build-tag ETW pair, hook latency persistence, and `LatencyTracker.P99()`. The CLI `beekeeper diag` command wiring + output formatting lands in Plan 05; this plan provides the data-assembly layer it calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/09-policy-as-code-self-defense-capstone/09-CONTEXT.md
@.planning/phases/09-policy-as-code-self-defense-capstone/09-RESEARCH.md
@.planning/phases/09-policy-as-code-self-defense-capstone/09-PATTERNS.md

<interfaces>
<!-- Verified data sources for diag. -->
From internal/llamafirewall/latency.go:
  type LatencyTracker struct { p95buf [100]int64; head int; count int; filled bool; mu ... }
  func (t *LatencyTracker) Record(ms int64)
  func (t *LatencyTracker) P95() int64   // sorted-buffer 95th index; P99 follows identically with 0.99
  var GlobalLatencyTracker  // package-level sidecar tracker (Phase 6) — expose P95 for diag
From internal/check/handler.go:
  func RunCheck(ctx, stdin, cfg, indexPath, auditPath, cacheDir string) Result  // entry; runCheck is the impl at line 92
From internal/catalog/state.go:
  func LoadState(path string) (WatchState, error)
  type SourceState struct { Hash string; Count int; Degraded bool; DegradedReason string }
  // Plan 03 adds WatchState.SelfQuarantine — diag may surface it as a source line
From internal/sentry/windows/etw.go:  var EventsLost uint64  // atomic; read via atomic.LoadUint64(&EventsLost) on Windows only

Build-tag pair idiom + DiagReport struct: 09-PATTERNS.md "diag.go" / "diag_windows.go" / "diag_other.go".
Hook latency persistence (one-shot process) resolution: 09-RESEARCH.md Open Question 2 — option (a): append sample to a small ring file under ~/.beekeeper/.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LatencyTracker.P99() + GlobalHookTracker + persisted latency ring</name>
  <read_first>
    - internal/llamafirewall/latency.go (self-reference: P95() is the exact template for P99(); reuse p95buf)
    - internal/llamafirewall/latency_test.go (analog: existing P95 test for the P99 assertion)
    - internal/check/handler.go (runCheck at line 92 — the measurement point; wrap with start := time.Now() and Record at the end)
    - internal/catalog/state.go writeFileAtomic usage / SaveState (atomic-write idiom for the latency ring file)
    - 09-PATTERNS.md "diag.go" GlobalHookTracker declaration + runCheck integration point
    - 09-RESEARCH.md Open Question 2 (one-shot process: persist samples to a ring file; diag reads the rolling window)
  </read_first>
  <behavior>
    - Test (P99): a LatencyTracker fed a known distribution returns a P99() >= P95() and matches the 0.99 index of the sorted buffer.
    - Test (TestGlobalHookTracker): after recording N simulated check latencies (via the persisted ring helper), the accumulated p95/p99 read back are non-zero and ordered p99 >= p95.
  </behavior>
  <action>
    In latency.go add `func (t *LatencyTracker) P99() int64` identical to P95() but with the 0.99 percentile index (reuse the p95buf field; no new struct field — 09-PATTERNS.md note). In internal/check, declare `var GlobalHookTracker = &llamafirewall.LatencyTracker{}` (package-level, mirroring llamafirewall.GlobalLatencyTracker). Create `internal/check/latency_persist.go` implementing a small fixed-size ring persisted to `~/.beekeeper/hook-latency.json` (last 100 samples): `appendHookLatency(ringPath string, ms int64)` (read ring, append/rotate, writeFileAtomic) and `loadHookLatency(ringPath string) []int64`. In handler.go runCheck, capture `start := time.Now()` at the top and call `appendHookLatency(<ringPath under cacheDir/state dir>, time.Since(start).Milliseconds())` just before returning the Result (do not change the Result contract or fail the check if the ring write errors — best-effort, log nothing that breaks fail-closed). Add latency_test.go P99 test and a TestGlobalHookTracker test exercising the persisted ring helpers.
  </action>
  <verify>
    <automated>cd C:/Users/Bantu/mzansi-agentive/beekeeper && go test ./internal/llamafirewall/... -run TestP99 -count=1 && go test ./internal/check/... -run TestGlobalHookTracker -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/llamafirewall/... -run TestP99 -count=1` exits 0.
    - `go test ./internal/check/... -run TestGlobalHookTracker -count=1` exits 0 (accumulated p95/p99 non-zero across simulated invocations, p99 >= p95).
    - Source assertion: `grep -c "func (t \*LatencyTracker) P99" internal/llamafirewall/latency.go` returns 1.
    - Hook ring write is best-effort: runCheck still returns a valid fail-closed Result if the ring path is unwritable (covered by an existing or added handler test that uses an invalid ring path).
  </acceptance_criteria>
  <done>P99() added; GlobalHookTracker + a persisted latency ring accumulate hook latency across one-shot check invocations; ring write is best-effort and never breaks fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: DiagReport + CollectDiag + ETW build-tag pair</name>
  <read_first>
    - internal/check/diag.go does not exist yet; analog is internal/llamafirewall/latency.go (accessor) + internal/catalog/state.go (LoadState)
    - cmd/beekeeper/protect_windows.go + protect_other.go (analog: //go:build windows / !windows pair — both files MUST exist or compile fails)
    - internal/sentry/windows/etw.go (EventsLost var) + internal/sentry/windows/daemon.go (atomic.LoadUint64(&EventsLost) read idiom)
    - internal/check/latency_persist.go (Task 1 ring loader feeds diag's hook p95/p99)
    - 09-PATTERNS.md "diag.go" + "diag_windows.go" + "diag_other.go" sections (exact struct, build tags, import path)
    - 09-RESEARCH.md Pattern 5 (DiagReport fields, data-source table, cross-platform ETW isolation)
  </read_first>
  <action>
    Create `internal/check/diag.go` with `type DiagReport struct { HookLatencyP95MS, HookLatencyP99MS, SidecarLatencyP95MS int64; CatalogSources []CatalogSourceStatus; ETWEventsLost uint64 }` and `type CatalogSourceStatus struct { Name string; Degraded bool; Count int; Hash string }`. Implement `func CollectDiag(stateFile, hookLatencyRingPath string) DiagReport`: hook p95/p99 from the Task 1 persisted ring (loadHookLatency → feed a temp LatencyTracker → P95()/P99(), or compute directly); sidecar p95 from `llamafirewall.GlobalLatencyTracker.P95()`; catalog sources from `catalog.LoadState(stateFile).Sources` mapped to CatalogSourceStatus (iterate the map deterministically — sort by name — and include beekeeper-self when present); ETW via `eventsLost()`. Create `internal/check/diag_windows.go` (`//go:build windows`) with `func eventsLost() uint64 { return atomic.LoadUint64(&windows.EventsLost) }` importing internal/sentry/windows, and `internal/check/diag_other.go` (`//go:build !windows`) with `func eventsLost() uint64 { return 0 }`. BOTH files must be committed (Critical pair requirement). Add diag_test.go: TestEventsLost_NonWindows asserts eventsLost() == 0 on the dev (non-Windows compiles) build, and a CollectDiag test that seeds a state.json fixture with two sources and asserts the report lists them sorted with correct Degraded/Count.
  </action>
  <verify>
    <automated>cd C:/Users/Bantu/mzansi-agentive/beekeeper && go build ./internal/check/... && go test ./internal/check/... -run "TestEventsLost_NonWindows|TestCollectDiag" -count=1 && GOOS=windows go build ./internal/check/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/check/... -run TestEventsLost_NonWindows -count=1` exits 0 (reports 0 on non-Windows).
    - `go build ./internal/check/...` AND `GOOS=windows go build ./internal/check/...` both succeed (the build-tag pair compiles on both targets — the Windows file references the real EventsLost var).
    - CollectDiag returns a DiagReport whose CatalogSources contains the seeded sources in sorted order (asserted in TestCollectDiag).
    - `go test ./internal/check/... -count=1` (whole package) exits 0.
  </acceptance_criteria>
  <done>DiagReport + CollectDiag assemble all four metrics; the ETW build-tag pair compiles on Windows and non-Windows with real-counter and zero-stub respectively; catalog sources surface deterministically including beekeeper-self.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted latency ring file → diag | A corrupted or attacker-written ring file crosses here; must not crash diag or runCheck |
| state.json → diag | Per-source freshness crosses here; diag is read-only and must not mutate enforcement state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Denial of Service | latency ring write failure breaks fail-closed check | mitigate | Ring write is best-effort at the END of runCheck; a write error never alters the Result or the fail-closed decision (asserted by acceptance test) |
| T-09-15 | Tampering | corrupt latency ring crashes diag | mitigate | loadHookLatency treats a corrupt/missing ring as an empty sample set (missing-file-is-OK pattern); diag reports 0 rather than panicking |
| T-09-16 | Information Disclosure | diag leaks sensitive config/secrets | accept | diag reports only latency, counts, hashes, and degraded flags — no tokens or paths with secrets; catalog Hash is a content digest, not sensitive |
| T-09-17 | Tampering | build-tag stub silently hides real ETW loss | mitigate | diag_windows.go reads the real atomic counter; diag_other.go returns 0 only because ETW genuinely does not exist off-Windows; both files committed so neither platform silently drops the field |
</threat_model>

<verification>
- `go build ./internal/check/...` and `GOOS=windows go build ./internal/check/...` both succeed.
- `go test ./internal/check/... ./internal/llamafirewall/... -count=1` green.
- `go vet ./internal/check/...` clean.
- diag report includes the four required sections (hook p95/p99, sidecar p95, per-source catalog freshness, ETW EventsLost) per CODE-06.
</verification>

<success_criteria>
Phase 9 success criterion 3: beekeeper diag displays hook latency p95/p99, sidecar inference latency, catalog freshness per source, and ETW EventsLost in a single output (data-assembly layer; CLI formatting in Plan 05).
</success_criteria>

<output>
After completion, create `.planning/phases/09-policy-as-code-self-defense-capstone/09-04-SUMMARY.md`.
</output>
