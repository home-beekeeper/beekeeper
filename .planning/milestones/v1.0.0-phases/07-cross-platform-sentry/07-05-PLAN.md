---
phase: 07-cross-platform-sentry
plan: 05
type: execute
wave: 4
depends_on: [07-03, 07-04]
files_modified:
  - .goreleaser.yaml
  - .github/workflows/release.yml
  - .github/workflows/ci.yml
  - internal/sentry/darwin/eslogger_fields_test.go
autonomous: true
requirements: [SFDF-05, SMAC-04]
must_haves:
  truths:
    - ".goreleaser.yaml has a top-level sboms section invoking syft to produce CycloneDX JSON output per archive"
    - ".github/workflows/release.yml installs syft via anchore/sbom-action/download-syft@v0 BEFORE the GoReleaser step"
    - ".github/workflows/release.yml has a new provenance job that calls slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.1.0 (FULL semver — NOT @v2; RESEARCH Pitfall 7; CLAUDE.md constraint)"
    - "The goreleaser job exports a base64-encoded hashes string via $GITHUB_OUTPUT and the provenance job consumes it as base64-subjects"
    - "The goreleaser job's permissions block grants id-token: write (for cosign + SLSA Sigstore OIDC) and contents: write (for release asset upload)"
    - "The provenance job's permissions block grants actions: read, id-token: write, contents: write (per RESEARCH §6.2)"
    - ".github/workflows/ci.yml gains a new test-eslogger-fields job on macos-latest that runs eslogger and validates parser field names against real eslogger output"
    - "release-gate job in ci.yml has the new test-eslogger-fields job added to its needs list (release blocker)"
    - "make verify-release VERSION=X.Y.Z still works on Linux (reproducible build target not broken by SBOM/provenance additions)"
    - "cosign keyless signing from Phase 1 (SFDF-02) is preserved — the signs section in goreleaser.yaml continues to work"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "sboms section using syft + cyclonedx-json format; signs and archives sections preserved from Phase 1"
      contains: "cyclonedx-json"
    - path: ".github/workflows/release.yml"
      provides: "goreleaser job with hashes output + provenance job @v2.1.0 + syft install step"
      contains: "generator_generic_slsa3.yml@v2.1.0"
    - path: ".github/workflows/ci.yml"
      provides: "test-eslogger-fields job on macos-latest validating parser against real eslogger output; release-gate updated"
      contains: "test-eslogger-fields"
    - path: "internal/sentry/darwin/eslogger_fields_test.go"
      provides: "TestEsloggerFieldValidation reads a live capture from a known env-var path and runs parseEsloggerLine over each line, asserting non-zero PID for exec/open events"
      contains: "TestEsloggerFieldValidation"
  key_links:
    - from: ".github/workflows/release.yml goreleaser job"
      to: ".goreleaser.yaml sboms section"
      via: "GoReleaser auto-invokes syft per archive; syft installed earlier in the job"
      pattern: "anchore/sbom-action/download-syft"
    - from: ".github/workflows/release.yml provenance job"
      to: "checksums.txt artifact hash output"
      via: "base64-subjects: ${{ needs.goreleaser.outputs.hashes }} feeds the SLSA generator"
      pattern: "base64-subjects"
    - from: ".github/workflows/ci.yml test-eslogger-fields"
      to: "internal/sentry/darwin/eslogger_fields_test.go"
      via: "macos-latest runner captures live eslogger output to a file; passes file path via BEEKEEPER_ESLOGGER_FIXTURE env var; test reads the file and parses each line"
      pattern: "BEEKEEPER_ESLOGGER_FIXTURE"
---

<objective>
Close out Phase 7 with SLSA Level 3 provenance + CycloneDX SBOM (SFDF-05) in the GoReleaser pipeline, AND add the macos-latest CI job that validates the eslogger parser field names against live eslogger output (closing the long-standing CLAUDE.md research note about eslogger field name uncertainty — supports SMAC-02's correctness in production). The plan also wires the macOS test into the release-gate aggregator so an eslogger field-name regression blocks releases.

Purpose: SLSA Level 3 is the v0.9.0-target self-defense deliverable. The CLAUDE.md constraint and RESEARCH §6 lock specific versions (`slsa-github-generator@v2.1.0` full semver, `syft` from anchore/sbom-action). This plan does not invent any policy — it wires the locked components into the existing GoReleaser pipeline without breaking Phase 1's reproducibility or Sigstore signing. The macos-latest field validation job is the empirical guarantee that the 07-01 parser's [ASSUMED] field paths actually match what eslogger emits on a current macOS — without it, the parser could silently produce empty SentryEvents in production.

Output: `.goreleaser.yaml` gains an sboms section; `.github/workflows/release.yml` gains a syft install step + hashes export + provenance job; `.github/workflows/ci.yml` gains the test-eslogger-fields job and updates the release-gate needs list; `internal/sentry/darwin/eslogger_fields_test.go` provides the parser validation logic that the CI job invokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-cross-platform-sentry/07-CONTEXT.md
@.planning/phases/07-cross-platform-sentry/07-RESEARCH.md
@.goreleaser.yaml
@.github/workflows/release.yml
@.github/workflows/ci.yml

<interfaces>
<!-- Current release.yml structure (existing, do not delete — only extend) -->

Current `.github/workflows/release.yml`:
```yaml
name: Release
on:
  push:
    tags:
      - 'v*'
permissions:
  contents: write
  id-token: write
jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: sigstore/cosign-installer@v3
      - uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

After this plan: the goreleaser job MUST expose `outputs.hashes` for the provenance job to consume; the provenance job MUST use `@v2.1.0` (NOT `@v2`).

Current `.goreleaser.yaml`:
```yaml
version: 2
builds: [ ... CGO_ENABLED=0, -trimpath, mod_timestamp, ldflags ... ]
checksums:
  name_template: "checksums.txt"
signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
release:
  draft: false
```

After this plan: a new top-level `sboms:` section that invokes syft to produce CycloneDX JSON per archive.

Current `.github/workflows/ci.yml` (existing — release-gate at the bottom):
```yaml
  release-gate:
    runs-on: ubuntu-22.04
    needs: [test-sentry-kernel-5-4, test-sentry-kernel-5-15, fuzz-ipc, fuzz-llamafirewall]
    steps:
      - run: echo "All release gates passed"
```

After this plan: release-gate needs list adds `test-eslogger-fields`.

SLSA Level 3 generator pattern (RESEARCH §6.2 — VERIFIED):
```yaml
provenance:
  needs: [goreleaser]
  permissions:
    actions: read       # detect GitHub Actions environment
    id-token: write     # Sigstore OIDC signing
    contents: write     # upload to release
  uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.1.0
  with:
    base64-subjects: "${{ needs.goreleaser.outputs.hashes }}"
    upload-assets: true
```

GoReleaser hashes export pattern (RESEARCH §6.2):
```yaml
- name: Generate artifact hashes for SLSA
  id: hash
  env:
    ARTIFACTS: "${{ steps.run-goreleaser.outputs.artifacts }}"
  run: |
    set -euo pipefail
    checksum_file=$(echo "$ARTIFACTS" | jq -r '.[] | select(.type=="Checksum") | .path')
    hashes=$(cat "$checksum_file" | base64 -w0)
    echo "hashes=$hashes" >> "$GITHUB_OUTPUT"
```

GoReleaser sboms section (RESEARCH §6.1 — VERIFIED goreleaser.com/customization/sbom):
```yaml
sboms:
  - id: beekeeper-sbom
    cmd: syft
    artifacts: archive
    args:
      - "$artifact"
      - "--output"
      - "cyclonedx-json=$document"
    documents:
      - "${artifact}.cdx.json"
```

eslogger field validation CI step (RESEARCH §10 Validation Architecture):
```yaml
test-eslogger-fields:
  runs-on: macos-latest
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
    - name: Capture live eslogger output
      run: |
        # eslogger requires sudo; CI macOS runners support `sudo -n`.
        # Run eslogger in background, let it stream events from natural CI activity,
        # then kill it. Save to a fixture file consumed by the Go test.
        FIXTURE=$(mktemp -t eslogger.XXXXXX.ndjson)
        sudo -n eslogger exec open create network_flow fork > "$FIXTURE" &
        ESLPID=$!
        # Generate some process activity to ensure we capture at least one exec event
        sleep 2
        for i in 1 2 3; do /bin/echo "warmup $i" > /dev/null; sleep 0.2; done
        sleep 1
        sudo kill $ESLPID 2>/dev/null || true
        wait $ESLPID 2>/dev/null || true
        echo "BEEKEEPER_ESLOGGER_FIXTURE=$FIXTURE" >> "$GITHUB_ENV"
        wc -l "$FIXTURE"
    - name: Validate parser against live eslogger output
      run: |
        go test -tags darwin -count=1 -v ./internal/sentry/darwin/ -run TestEsloggerFieldValidation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SLSA Level 3 + CycloneDX SBOM in GoReleaser pipeline (SFDF-05)</name>
  <files>.goreleaser.yaml, .github/workflows/release.yml</files>
  <read_first>
    - .goreleaser.yaml (current state — must extend, not replace; preserve builds/checksums/signs/archives/release sections intact)
    - .github/workflows/release.yml (current state — must extend; preserve cosign-installer + goreleaser-action steps; add syft install + hashes export + provenance job)
    - .planning/phases/07-cross-platform-sentry/07-RESEARCH.md §6 (full SBOM + SLSA wiring — quoted patterns are VERIFIED from official docs)
    - CLAUDE.md (constraint: `slsa-github-generator@v2.1.0` — FULL semver, NOT `@v2`)
    - .planning/phases/07-cross-platform-sentry/07-CONTEXT.md (locked: SBOM in CycloneDX format generated by syft; reproducible builds and cosign signing preserved)
    - .planning/phases/01-foundation/01-PLAN-self-defense.md if it exists in this repo's planning history (the original SFDF-01/02 plan — re-reading it confirms the existing structure we extend, but it is not strictly required since the current .goreleaser.yaml shows the live state)
  </read_first>
  <action>
    Step 1 — Modify `.goreleaser.yaml` to add an `sboms:` section.

    Read `.goreleaser.yaml`. After the `signs:` section (and before `archives:`), add a new top-level section:

    ```yaml
    # SFDF-05 (SBOM): syft-generated CycloneDX SBOM per archive. Goreleaser invokes
    # syft after the archive is built. The CycloneDX format is required per
    # CONTEXT.md locked decision; the default would be SPDX-JSON.
    sboms:
      - id: beekeeper-sbom
        cmd: syft
        artifacts: archive
        args:
          - "$artifact"
          - "--output"
          - "cyclonedx-json=$document"
        documents:
          - "${artifact}.cdx.json"
    ```

    Do NOT modify the existing `signs:`, `archives:`, `release:`, `builds:`, or `checksums:` sections — they implement SFDF-01/02 from Phase 1 and must be preserved.

    Update the file header comment block. The original says SBOM is "intentionally NOT configured in this file. Add them only in Phase 7." — change that line to:

    ```
    # SFDF-05 (Phase 7): SBOM via syft in CycloneDX format is configured below.
    # SLSA Level 3 provenance attestation is generated by the GitHub Actions
    # release workflow, not GoReleaser — see .github/workflows/release.yml.
    ```

    Step 2 — Modify `.github/workflows/release.yml` to install syft, export artifact hashes, and add the provenance job.

    The complete updated file body (replacing the current content):

    ```yaml
    # Release pipeline (SFDF-01 + SFDF-02 + SFDF-05)
    #
    # Triggered by pushing a vX.Y.Z tag. GoReleaser builds reproducible
    # multi-platform binaries, cosign v3 keylessly signs checksums.txt, syft
    # produces CycloneDX SBOMs per archive, and slsa-github-generator@v2.1.0
    # produces an SLSA Level 3 provenance attestation tied to the GitHub Actions
    # OIDC identity. No long-lived signing keys are used.
    name: Release

    on:
      push:
        tags:
          - 'v*'

    permissions:
      contents: read

    jobs:
      goreleaser:
        runs-on: ubuntu-latest
        outputs:
          hashes: ${{ steps.hash.outputs.hashes }}
        permissions:
          contents: write    # upload release artifacts
          id-token: write    # cosign keyless OIDC signing
        steps:
          - name: Checkout
            uses: actions/checkout@v4
            with:
              fetch-depth: 0

          - name: Set up Go
            uses: actions/setup-go@v5
            with:
              go-version-file: go.mod

          - name: Install cosign
            uses: sigstore/cosign-installer@v3

          - name: Install syft
            uses: anchore/sbom-action/download-syft@v0

          - name: Run GoReleaser
            id: run-goreleaser
            uses: goreleaser/goreleaser-action@v7
            with:
              distribution: goreleaser
              version: '~> v2'
              args: release --clean
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          - name: Generate artifact hashes for SLSA provenance
            id: hash
            env:
              ARTIFACTS: "${{ steps.run-goreleaser.outputs.artifacts }}"
            run: |
              set -euo pipefail
              checksum_file=$(echo "$ARTIFACTS" | jq -r '.[] | select(.type=="Checksum") | .path')
              if [ -z "$checksum_file" ] || [ ! -f "$checksum_file" ]; then
                echo "::error::checksums.txt not found in GoReleaser artifacts"
                exit 1
              fi
              hashes=$(cat "$checksum_file" | base64 -w0)
              echo "hashes=$hashes" >> "$GITHUB_OUTPUT"

      provenance:
        # SFDF-05: SLSA Level 3 provenance attestation via the slsa-github-generator
        # reusable workflow. The version MUST be pinned to a full semver per CLAUDE.md
        # and the SLSA framework's trusted-builder verification rules — never @v2.
        needs: [goreleaser]
        permissions:
          actions: read      # detect GitHub Actions environment for provenance
          id-token: write    # Sigstore OIDC signing for the attestation
          contents: write    # upload the .intoto.jsonl attestation to the release
        uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.1.0
        with:
          base64-subjects: "${{ needs.goreleaser.outputs.hashes }}"
          upload-assets: true
    ```

    KEY POINTS:
    - The top-level `permissions: contents: read` is the safe default; per-job permissions are explicit and minimal.
    - The goreleaser job adds `outputs: hashes` so the provenance job can consume it.
    - The Generate artifact hashes step fails loudly if checksums.txt is missing (rather than producing an empty base64 string).
    - The provenance job uses `@v2.1.0` exactly — the version string is the load-bearing part of SFDF-05.

    Verify after editing:
    - The file remains valid YAML (no tab characters; consistent 2-space indentation).
    - The `version: '~> v2'` for goreleaser-action is preserved (this is goreleaser-action's version, NOT SLSA's).
  </action>
  <verify>
    <automated>grep -c "sboms:" .goreleaser.yaml</automated>
    <automated>grep -c "cyclonedx-json" .goreleaser.yaml</automated>
    <automated>grep -c "generator_generic_slsa3.yml@v2.1.0" .github/workflows/release.yml</automated>
    <automated>grep -c "anchore/sbom-action/download-syft@v0" .github/workflows/release.yml</automated>
    <automated>grep -c "base64-subjects" .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sboms:" .goreleaser.yaml` reports exactly 1 (one section).
    - `grep -c "cyclonedx-json" .goreleaser.yaml` reports >= 1 (CycloneDX format per CONTEXT.md locked decision).
    - `grep -c "syft" .goreleaser.yaml` reports >= 1.
    - `grep -c "cosign" .goreleaser.yaml` reports >= 1 (Phase 1 SFDF-02 preserved).
    - `grep -c "mod_timestamp" .goreleaser.yaml` reports >= 1 (Phase 1 SFDF-01 reproducibility preserved).
    - `grep -c "trimpath" .goreleaser.yaml` reports >= 1 (Phase 1 SFDF-01 preserved).
    - `grep -c "generator_generic_slsa3.yml@v2.1.0" .github/workflows/release.yml` reports >= 1.
    - `grep -c "generator_generic_slsa3.yml@v2 " .github/workflows/release.yml` reports 0 (no truncated tag; the `v2 ` trailing-space pattern catches the `@v2` mistake that RESEARCH Pitfall 7 warns against).
    - `grep -c "generator_generic_slsa3.yml@main\|generator_generic_slsa3.yml@master" .github/workflows/release.yml` reports 0 (no floating refs).
    - `grep -c "anchore/sbom-action/download-syft@v0" .github/workflows/release.yml` reports >= 1.
    - `grep -c "base64-subjects" .github/workflows/release.yml` reports >= 1.
    - `grep -c "outputs:" .github/workflows/release.yml` reports >= 1 (goreleaser job exposes hashes).
    - `grep -c "id-token: write" .github/workflows/release.yml` reports >= 2 (both goreleaser and provenance jobs need it).
    - `grep -c "actions: read" .github/workflows/release.yml` reports >= 1 (provenance job).
    - `grep -c "sigstore/cosign-installer@v3" .github/workflows/release.yml` reports >= 1 (Phase 1 SFDF-02 step preserved).
    - `grep -c "goreleaser/goreleaser-action@v7" .github/workflows/release.yml` reports >= 1 (Phase 1 step preserved).
    - Both files parse as valid YAML (e.g., `python -c "import yaml; yaml.safe_load(open('.goreleaser.yaml'))"` exits 0; same for release.yml — if python is not available on the dev machine, use `yamllint` or `go run -tags ignore - <<< 'package main; import _ "yaml.v3"' || true` — the CI run will catch invalid YAML).
    - The existing `signs`, `archives`, `release`, `builds`, `checksums` sections of `.goreleaser.yaml` are unchanged (verify by diffing against the pre-edit state — count of lines that match `^signs:`, `^archives:`, `^release:`, `^builds:`, `^checksums:` each equals 1).
  </acceptance_criteria>
  <done>.goreleaser.yaml has CycloneDX SBOM via syft; .github/workflows/release.yml has syft installer + hashes output + provenance job using slsa-github-generator@v2.1.0 full semver; Phase 1 reproducibility and cosign signing preserved.</done>
</task>

<task type="auto">
  <name>Task 2: macos-latest eslogger field validation test + CI job + release-gate update</name>
  <files>internal/sentry/darwin/eslogger_fields_test.go, .github/workflows/ci.yml</files>
  <read_first>
    - internal/sentry/darwin/parser.go (07-01 — parseEsloggerLine; this test exercises it against live output)
    - internal/sentry/darwin/eslogger.go (07-01 — DefaultEsloggerEvents; same event subscription list as the CI capture command)
    - .planning/phases/07-cross-platform-sentry/07-RESEARCH.md §2.1 (eslogger man page — sudo required, --format json default), §10 Validation Architecture (the exact CI job pattern documented)
    - .planning/phases/07-cross-platform-sentry/07-CONTEXT.md (CLAUDE.md research note: eslogger field names partially undocumented; parser must be built against real macos-latest output)
    - .github/workflows/ci.yml (current — must extend; preserve test/fuzz/sentry-kernel-5.4/sentry-kernel-5.15 jobs; add test-eslogger-fields; update release-gate needs list)
  </read_first>
  <behavior>
    - TestEsloggerFieldValidation has build tag `//go:build darwin` and reads the path in env var BEEKEEPER_ESLOGGER_FIXTURE; if the env var is empty or the file does not exist, the test calls t.Skip with a clear reason ("BEEKEEPER_ESLOGGER_FIXTURE not set — run under CI with live eslogger capture") so local `go test` on a dev machine without sudo eslogger is not blocked
    - When the fixture is available: opens the file, reads line-by-line via bufio.Scanner, calls parseEsloggerLine for each line; tracks counts: total lines, parsed successfully, of which exec count, open count, create count, network count, and lines that had a non-zero PID
    - Asserts: total > 0 (the fixture is non-empty), parsed > 0 (parser handles at least one line), exec count > 0 (the warmup activity generated at least one exec event), and parsed events with PID > 0 / Exe != "" >= 1 (no all-zero-field events from a schema mismatch)
    - The CI job test-eslogger-fields runs on macos-latest, captures real eslogger output (sudo -n eslogger ...) for ~3 seconds while generating warmup process activity, exports the path via $GITHUB_ENV, then runs `go test -tags darwin -run TestEsloggerFieldValidation` which consumes the env var
    - test-eslogger-fields is added to release-gate's needs list so an eslogger-field-name regression blocks releases (matches the existing release-gate aggregator pattern with sentry-kernel-5.4 / sentry-kernel-5.15 / fuzz-ipc / fuzz-llamafirewall)
  </behavior>
  <action>
    Step 1 — Create `internal/sentry/darwin/eslogger_fields_test.go` with build tag `//go:build darwin` and `package darwin`.

    Imports: `bufio`, `os`, `testing`, `github.com/mzansi-agentive/beekeeper/internal/sentry`.

    Test body:
    ```
    func TestEsloggerFieldValidation(t *testing.T) {
        fixturePath := os.Getenv("BEEKEEPER_ESLOGGER_FIXTURE")
        if fixturePath == "" {
            t.Skip("BEEKEEPER_ESLOGGER_FIXTURE not set — run under macos-latest CI with live eslogger capture")
        }

        f, err := os.Open(fixturePath)
        if err != nil {
            t.Skipf("BEEKEEPER_ESLOGGER_FIXTURE=%q not openable: %v", fixturePath, err)
        }
        defer f.Close()

        scanner := bufio.NewScanner(f)
        scanner.Buffer(make([]byte, 1<<20), 1<<20) // allow 1MB lines (real eslogger events can be large)

        var total, parsed, execCount, openCount, createCount, networkCount, withPID, withExe int
        for scanner.Scan() {
            total++
            line := scanner.Bytes()
            if len(line) == 0 {
                continue
            }
            ev, perr := parseEsloggerLine(line)
            if perr != nil {
                continue
            }
            parsed++
            switch ev.Kind {
            case sentry.EventProcessCreate:
                execCount++
            case sentry.EventFileAccess:
                // distinguish open vs create by checking original event_type? Not exposed by SentryEvent.
                // Use a heuristic: if FilePath is set, count as file access (open OR create — both legitimate).
                openCount++ // counted together for the purposes of this validation
            case sentry.EventNetworkConnect:
                networkCount++
            }
            if ev.PID > 0 {
                withPID++
            }
            if ev.Exe != "" {
                withExe++
            }
        }
        if err := scanner.Err(); err != nil {
            t.Fatalf("scanner: %v", err)
        }

        t.Logf("Validation summary: total=%d parsed=%d exec=%d file_access=%d network=%d withPID=%d withExe=%d",
            total, parsed, execCount, openCount, networkCount, withPID, withExe)

        if total == 0 {
            t.Fatal("fixture is empty — no eslogger events captured (CI capture step may be misconfigured)")
        }
        if parsed == 0 {
            t.Fatal("zero events parsed successfully — parser likely has wrong field paths against current eslogger schema (CLAUDE.md research note)")
        }
        if execCount == 0 {
            t.Fatal("zero exec events found — warmup activity should have generated at least one process start; parser may be missing the .event.exec.target.executable.path field")
        }
        if withPID == 0 {
            t.Fatal("zero events had non-zero PID — parser is reading from the wrong audit_token path (RESEARCH §2.2 Assumption A6)")
        }
        if withExe == 0 {
            t.Fatal("zero events had non-empty Exe — parser is reading from the wrong executable.path field")
        }
    }
    ```

    Step 2 — Modify `.github/workflows/ci.yml` to add the test-eslogger-fields job and update release-gate.

    Read the current ci.yml. After the `test-sentry-kernel-5-15` job (and before `release-gate`), insert a new job:

    ```yaml
      test-eslogger-fields:
        # Phase 7 release gate (SMAC-02 + CLAUDE.md research note): validate that
        # the eslogger NDJSON parser's field paths match the actual schema emitted
        # by eslogger on a current macOS. Without this gate, the parser could
        # silently produce empty SentryEvents in production (Apple does not provide
        # a stable schema guarantee — fields may change across OS minor versions).
        runs-on: macos-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-go@v5
            with:
              go-version-file: go.mod
              cache: true
          - name: Capture live eslogger output
            shell: bash
            run: |
              set -euo pipefail
              FIXTURE=$(mktemp -t eslogger.XXXXXX.ndjson)
              # Background eslogger; CI runners support `sudo -n` (passwordless sudo).
              sudo -n eslogger exec open create network_flow fork > "$FIXTURE" 2>/dev/null &
              ESLPID=$!
              # Generate predictable process activity so we capture at least one exec event.
              sleep 1
              for i in 1 2 3 4 5; do
                /bin/echo "warmup $i" > /dev/null
                /usr/bin/whoami > /dev/null
                sleep 0.2
              done
              sleep 1
              sudo -n kill "$ESLPID" 2>/dev/null || true
              wait "$ESLPID" 2>/dev/null || true
              echo "BEEKEEPER_ESLOGGER_FIXTURE=$FIXTURE" >> "$GITHUB_ENV"
              echo "Captured $(wc -l < "$FIXTURE") lines to $FIXTURE"
              head -1 "$FIXTURE" | head -c 200 || true
          - name: Validate parser against live eslogger output
            run: go test -tags darwin -count=1 -v -run TestEsloggerFieldValidation ./internal/sentry/darwin/
    ```

    NOTE on `sudo -n`: macos-latest GitHub Actions runners support passwordless sudo by default (the `runner` user is in the sudoers file with NOPASSWD). If `sudo -n` fails in practice, the capture step will exit non-zero and the test step will run with an empty fixture path, causing TestEsloggerFieldValidation to t.Fatal with a clear error — which is the correct release-blocking behavior.

    Step 3 — Update the `release-gate` job in ci.yml.

    Find the existing `release-gate` job:
    ```yaml
      release-gate:
        runs-on: ubuntu-22.04
        needs: [test-sentry-kernel-5-4, test-sentry-kernel-5-15, fuzz-ipc, fuzz-llamafirewall]
    ```

    Update the `needs:` list to include `test-eslogger-fields`:
    ```yaml
      release-gate:
        runs-on: ubuntu-22.04
        needs: [test-sentry-kernel-5-4, test-sentry-kernel-5-15, fuzz-ipc, fuzz-llamafirewall, test-eslogger-fields]
        steps:
          - run: echo "All release gates passed"
    ```

    Do NOT modify the existing `test`, `fuzz`, `fuzz-ipc`, `fuzz-llamafirewall`, `test-sentry-kernel-5-4`, `test-sentry-kernel-5-15` jobs.
  </action>
  <verify>
    <automated>go test -tags darwin -count=1 -v -run TestEsloggerFieldValidation ./internal/sentry/darwin/ 2>&1 | head -20</automated>
    <automated>grep -c "test-eslogger-fields" .github/workflows/ci.yml</automated>
    <automated>grep -c "BEEKEEPER_ESLOGGER_FIXTURE" .github/workflows/ci.yml internal/sentry/darwin/eslogger_fields_test.go</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/sentry/darwin/eslogger_fields_test.go` exists and starts with `//go:build darwin`.
    - `grep -c "BEEKEEPER_ESLOGGER_FIXTURE" internal/sentry/darwin/eslogger_fields_test.go` reports >= 1.
    - `grep -c "t.Skip" internal/sentry/darwin/eslogger_fields_test.go` reports >= 1 (so local runs without the env var skip rather than fail).
    - `grep -c "parseEsloggerLine" internal/sentry/darwin/eslogger_fields_test.go` reports >= 1 (exercises the 07-01 parser).
    - `grep -c "test-eslogger-fields:" .github/workflows/ci.yml` reports >= 1 (job defined).
    - `grep -c "macos-latest" .github/workflows/ci.yml` reports >= 1 (job runs on macos-latest — the locked-in environment per CONTEXT.md and CLAUDE.md).
    - `grep -c "sudo -n eslogger" .github/workflows/ci.yml` reports >= 1.
    - `grep -c "BEEKEEPER_ESLOGGER_FIXTURE" .github/workflows/ci.yml` reports >= 1 (env var export wired between steps).
    - `grep -c "TestEsloggerFieldValidation" .github/workflows/ci.yml` reports >= 1 (the go test command invokes the test).
    - The release-gate needs list contains `test-eslogger-fields`: `grep -c "test-eslogger-fields" .github/workflows/ci.yml` reports >= 2 (one for the job definition, one for the needs list).
    - The release-gate needs list still contains the previous entries: `grep -A1 "release-gate:" .github/workflows/ci.yml | grep -c "test-sentry-kernel-5-4\|test-sentry-kernel-5-15\|fuzz-ipc\|fuzz-llamafirewall"` reports >= 1 (the previous gates are preserved — at least one is found by the multi-pattern grep).
    - On the Windows dev machine, `go test -tags darwin -count=1 -v -run TestEsloggerFieldValidation ./internal/sentry/darwin/`: since the test file has `//go:build darwin` and the Windows dev cannot run darwin-tagged code natively, the test command exits 0 with "no tests to run" or similar. Verify by running `GOOS=darwin go vet ./internal/sentry/darwin/...` which exits 0 — confirms the file compiles under darwin tag.
    - `go vet -tags darwin ./internal/sentry/darwin/...` (with GOOS=darwin if cross-vetting) exits 0.
    - On macos-latest CI runner: the test-eslogger-fields job completes successfully, capturing >0 events and parsing >0 of them with non-zero PID.
    - `go build ./...` on Windows dev machine exits 0 (the new test file does not break compilation).
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML; if python unavailable, accept failure here — the GitHub Actions parser will catch invalid YAML on push).
  </acceptance_criteria>
  <done>eslogger_fields_test.go skips locally and validates field schema on macos-latest CI; test-eslogger-fields job added to ci.yml; release-gate needs list includes the new job; existing jobs preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag push → release workflow | Only authorized users with `contents: write` on the repo can push tags; existing GitHub-side authorization is the trust anchor |
| GoReleaser → syft subprocess | syft is downloaded by anchore/sbom-action/download-syft@v0; pinned to a release of the action; should be SHA-pinned for v1.0 (deferred — current `@v0` is the documented pattern) |
| slsa-github-generator reusable workflow | Pinned to `@v2.1.0` full semver; the reusable workflow is hosted in slsa-framework's official repo |
| eslogger output → parser | macos-latest CI runner; trusted environment; eslogger's output is the schema-of-record; if it changes between OS versions, the test fails and a release is blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | SLSA generator `@v2` mutable tag replaced by attacker-controlled commit | mitigate | Full semver `@v2.1.0` per RESEARCH Pitfall 7 and CLAUDE.md; grep gate `grep -c "@v2 "` reports 0 (catches truncated form); acceptance criteria enforce the exact string match |
| T-07-05-02 | Supply Chain | syft replaced by malicious version in the SBOM step | mitigate | Pinned via `anchore/sbom-action/download-syft@v0` — anchore is the maintainer of syft; the action itself is published by anchore; for v1.0 release the action should be SHA-pinned via Renovate (deferred — current pattern is the documented baseline) |
| T-07-05-03 | Tampering | Provenance attestation signed with wrong identity (e.g., personal access token) | mitigate | The provenance job uses `id-token: write` and Sigstore OIDC which binds the GitHub Actions workflow identity into the attestation; no long-lived keys; slsa-verifier downstream confirms the issuer is github.com/slsa-framework/slsa-github-generator |
| T-07-05-04 | Information Disclosure | SBOM containing internal dependency names | accept | SBOMs are public artifacts intended for consumers; all listed dependencies are public Go modules already in go.mod; no proprietary information exposed |
| T-07-05-05 | Tampering | macOS CI runner with stale eslogger schema producing false PASS | mitigate | macos-latest is GitHub's current macOS image; updates track the latest stable; if a new macOS minor version changes the schema, the test will fail (parsed==0 or withPID==0) and block the release — exactly the intended failure mode |
| T-07-05-06 | Denial of Service | sudo -n eslogger failing on CI runner (no sudo or rate-limited) breaks the gate | mitigate | macos-latest runners support `sudo -n`; if this changes the capture step exits non-zero and the test step's TestEsloggerFieldValidation receives an unreadable BEEKEEPER_ESLOGGER_FIXTURE and Skips with a clear message — the CI step output makes the failure mode visible |
| T-07-05-07 | Tampering | base64-subjects empty string passed to SLSA generator | mitigate | The hashes step explicitly fails with an error message if checksums.txt is missing (`if [ -z "$checksum_file" ] || [ ! -f "$checksum_file" ]; then exit 1 fi`); empty base64 would also fail the SLSA generator's input validation |
</threat_model>

<verification>
- `.goreleaser.yaml` is valid YAML and contains a top-level `sboms:` section with `cyclonedx-json` output
- `.github/workflows/release.yml` is valid YAML; goreleaser job has `outputs.hashes`; provenance job uses `@v2.1.0` exactly
- `.github/workflows/ci.yml` is valid YAML; `test-eslogger-fields` job present; `release-gate.needs` includes `test-eslogger-fields`
- `grep -c "generator_generic_slsa3.yml@v2.1.0" .github/workflows/release.yml` reports >= 1
- `grep -c "generator_generic_slsa3.yml@v2 \|@v2\"" .github/workflows/release.yml` reports 0 (no truncated `@v2` form)
- `grep -c "cyclonedx-json" .goreleaser.yaml` reports >= 1
- `grep -c "anchore/sbom-action/download-syft@v0" .github/workflows/release.yml` reports >= 1
- `grep -c "test-eslogger-fields" .github/workflows/ci.yml` reports >= 2 (job def + release-gate needs)
- `grep -c "TestEsloggerFieldValidation" internal/sentry/darwin/eslogger_fields_test.go .github/workflows/ci.yml` reports >= 2
- `go test -tags darwin -count=1 ./internal/sentry/darwin/...` on macos-latest CI passes (with the env var set by the capture step); on the Windows dev machine the test file is excluded by build tag, so `go build ./...` and `go test ./...` are unaffected
- The existing Phase 1 SFDF-01 (reproducibility: mod_timestamp, -trimpath) and SFDF-02 (cosign signing) sections of `.goreleaser.yaml` are preserved unchanged
- `make verify-release VERSION=X.Y.Z` (per Phase 1's existing Makefile target) still functions on Linux — the SBOM and SLSA additions are downstream of the reproducible build, not in its critical path
</verification>

<success_criteria>
- SFDF-05 part 1 (SBOM): `.goreleaser.yaml` invokes syft via the sboms section to produce CycloneDX JSON per archive; release.yml installs syft before GoReleaser runs
- SFDF-05 part 2 (SLSA Level 3): release.yml has a provenance job using slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.1.0 (FULL semver, not @v2 — RESEARCH Pitfall 7) with base64-subjects fed by the goreleaser job's hashes output
- SMAC-02 / SMAC-04 reinforcement: eslogger field validation runs on macos-latest CI against live output; test-eslogger-fields job added to ci.yml release-gate so a parser-vs-schema regression blocks releases (closing the CLAUDE.md research note about eslogger field name uncertainty)
- Phase 1 self-defense (SFDF-01 reproducibility + SFDF-02 cosign signing) preserved — the additions in this plan extend, never replace, the existing Phase 1 GoReleaser configuration
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-platform-sentry/07-05-SUMMARY.md`. Document:
- The exact slsa-github-generator version pinned (must be v2.1.0)
- The syft installation pattern used (anchore/sbom-action/download-syft@v0)
- Whether the eslogger field validation passed against live macos-latest output on this CI run, and any deviations observed between the parsed events and the [ASSUMED] field paths in 07-RESEARCH §2.2
</output>
