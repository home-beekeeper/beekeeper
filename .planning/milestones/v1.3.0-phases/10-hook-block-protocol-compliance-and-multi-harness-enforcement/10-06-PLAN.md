---
phase: 10-hook-block-protocol-compliance-and-multi-harness-enforcement
plan: 06
type: execute
wave: 5
depends_on: ["10-01", "10-05"]
files_modified:
  - internal/hooks/kilo_trae.go
  - internal/hooks/hooks.go
  - internal/hooks/hooks_test.go
  - docs/harness-support-matrix.md
  - README.md
autonomous: true
requirements: [HPC-05]
must_haves:
  truths:
    - "hooks install --target {kilo,trae} prints an MCP-gateway routing guide (no pre-exec hook exists for these; native tools are UNGUARDED) and writes no hook file"
    - "docs/harness-support-matrix.md publishes the Tier 1/2/3 matrix honestly (Tier-1 testable = Claude Code; Tier-1 documented; Tier-2 caveats Hermes/Cline/OpenCode; Tier-3 MCP-gateway-only Kilo/Trae)"
    - "README links the support matrix and states the Windows/Cline + Kilo/Trae-native-unguarded caveats without overclaiming"
  artifacts:
    - path: "internal/hooks/kilo_trae.go"
      provides: "Kilo + Trae gateway-routing guides (printGuide, no file write)"
      contains: "MCP gateway"
    - path: "docs/harness-support-matrix.md"
      provides: "Published Tier 1/2/3 harness support matrix"
      contains: "Tier 3"
      min_lines: 40
  key_links:
    - from: "internal/hooks/hooks.go InstallTo"
      to: "printGatewayGuide (kilo, trae)"
      via: "gatewayTargets map + switch case"
      pattern: "TargetKilo"
    - from: "README.md"
      to: "docs/harness-support-matrix.md"
      via: "markdown link"
      pattern: "harness-support-matrix"
---

<objective>
Route the two no-hook harnesses (Kilo, Trae) to the MCP gateway via a printed guide — they have NO
pre-exec hook, so their native Bash/file tools are UNGUARDED and only MCP tools can be intercepted —
and publish the honest Tier 1/2/3 support matrix in docs/ + README. No overclaiming: the matrix
distinguishes locally-testable (Claude Code) from documented-unverified, names the Tier-2 caveats
(Hermes fail-open, Cline no-Windows, OpenCode subagent bypass), and the Tier-3 native-unguarded gap.

Purpose: HPC-05 — gateway routing for no-hook harnesses + the published support matrix that makes
the product's real coverage honest. Depends on Plan 05 because it shares hooks.go + hooks_test.go.
Output: kilo_trae.go gateway guides, dispatch, docs/harness-support-matrix.md, README section/link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kilo + Trae MCP-gateway routing guides + dispatch</name>
  <files>internal/hooks/kilo_trae.go, internal/hooks/hooks.go, internal/hooks/hooks_test.go</files>
  <read_first>
    - internal/hooks/gateway_targets.go (printGatewayGuide dispatch lines 13-24 + printContinueGuide/printOpenCodeGuide/printOpenClawGuide fmt.Fprintf templates lines 28-125 — the EXACT printGuide pattern to replicate; the gateway URL http://127.0.0.1:7837/mcp + the `beekeeper gateway token` instruction)
    - internal/hooks/hooks.go (gatewayTargets map lines 28-32, InstallTo case line 78, UninstallTo case line 119, error strings)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md (matrix rows 11 Kilo: ~/.config/kilo, MCP-ONLY, intercept MCP tools via mcp in kilo.json, built-in Bash/file tools UNGUARDED; row 14 Trae: ~/.trae, MCP-only, intercept via ~/.trae/mcp.json, native cmds gated only by interactive auto-run)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (kilo_trae.go section lines 564-569)
  </read_first>
  <action>
    Create internal/hooks/kilo_trae.go with printKiloGuide(out) and printTraeGuide(out) following the
    EXACT printContinueGuide pattern (a fmt.Fprintf block, no file write). Each guide: explains the harness
    has NO external pre-exec hook (Kilo: open FR #5827; Trae: no programmatic hook), so ONLY MCP tools can
    be intercepted via the MCP gateway and the harness's NATIVE Bash/file tools are UNGUARDED (state this
    plainly — no overclaiming). Show the MCP gateway config: for Kilo add the beekeeper MCP server to
    kilo.json (~/.config/kilo); for Trae add it to ~/.trae/mcp.json — both pointing at
    http://127.0.0.1:7837/mcp with the Authorization Bearer token from `beekeeper gateway token`. In
    hooks.go add TargetKilo="kilo", TargetTrae="trae"; add both to the gatewayTargets map; route them in
    the InstallTo gateway case (extend printGatewayGuide to dispatch kilo→printKiloGuide, trae→printTraeGuide)
    and in the UninstallTo gateway no-op case; append to allTargets; update the error strings. Add the two
    targets to TestInstallGatewayTarget coverage in hooks_test.go (assert a guide is printed mentioning the
    gateway URL + the UNGUARDED-native-tools caveat, and that NO file is written).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/hooks/ -run 'TestInstallGatewayTarget|TestInstallDispatch' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/hooks/kilo_trae.go contains printKiloGuide + printTraeGuide each mentioning "MCP gateway" and the native-tools-UNGUARDED caveat and the gateway URL 127.0.0.1:7837
    - internal/hooks/hooks.go contains TargetKilo + TargetTrae in the gatewayTargets map and dispatch in printGatewayGuide
    - `go test ./internal/hooks/ -run 'TestInstallGatewayTarget|TestInstallDispatch' -count=1` exits 0 and asserts no file is written for kilo/trae
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Kilo + Trae route to a printed MCP-gateway guide (no file write) that honestly states native tools are unguarded; dispatch + tests cover them.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the honest Tier 1/2/3 harness support matrix (docs + README)</name>
  <files>docs/harness-support-matrix.md, README.md</files>
  <read_first>
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md (§3 "Honest support tiers" lines 71-76 — the authoritative tier assignments; §2 the 15-harness matrix for config dirs + deny contracts + fail modes; §1 the bug context)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-LIVE-TEST-RESUME.md (the "honest scope" + CI-coverage-reality sections — only Claude Code is live-verifiable; CI cannot test harness honoring)
    - internal/hooks/gateway_targets.go (printGuide prose tone — the template/voice for the docs)
    - README.md (find the existing integrations/harness section to link from; match its heading style)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md ("No Analog Found" lines 681-684 — docs/harness-support-matrix.md is greenfield, content derived from RESEARCH §3)
  </read_first>
  <action>
    Create docs/harness-support-matrix.md. Write a markdown table of all 15 harnesses with columns:
    Harness, Config dir, Interception (HOOK / PLUGIN / MCP-gateway-only / NONE), Deny mechanism (exit 2 +
    JSON family / JSON-only / throw / gateway), Tier, Caveats, Verification status. Assign tiers EXACTLY per
    RESEARCH §3: Tier-1 testable = Claude Code (live-verified, HPC-04); Tier-1 documented (unverified
    locally) = Codex, Cursor, Augment, CodeBuddy, Qwen, Gemini CLI, Copilot, Antigravity, Windsurf; Tier-2
    caveated = Hermes (fail-open → JSON-only block), Cline (macOS/Linux ONLY — no Windows), OpenCode (plugin;
    subagent/MCP calls bypass it); Tier-3 MCP-gateway-only = Kilo, Trae (native Bash/file tools UNGUARDED;
    only MCP tools intercepted via the gateway). Add an explicit "Honesty notes" section: (a) only Claude
    Code is locally live-verified; the other 14 are implemented against documented contracts and validated by
    contract-shape unit tests, NOT a running harness; (b) CI cannot test whether a harness HONORS the hook —
    that is local + manual + Claude-Code-only; (c) the Windows caveat (Cline unsupported); (d) the Tier-3
    native-unguarded gap. Then edit README.md: add a short "Agent harness support" subsection that links to
    docs/harness-support-matrix.md and states the headline honestly — "full hook-block verified on Claude
    Code; documented contracts for 9 more; caveats for Hermes/Cline/OpenCode; Kilo/Trae are MCP-gateway-only
    (native tools unguarded)". Do NOT overclaim universal protection.
  </action>
  <verify>
    <automated>test -f docs/harness-support-matrix.md && grep -q "Tier 3" docs/harness-support-matrix.md && grep -q "harness-support-matrix" README.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/harness-support-matrix.md exists, is at least 40 lines, contains a table row for all 15 harnesses, and contains the strings "Tier 1", "Tier 2", "Tier 3", "UNGUARDED" (or "unguarded"), and "Claude Code"
    - docs/harness-support-matrix.md states that only Claude Code is locally live-verified and the other 14 are documented-contract / contract-shape-tested only
    - docs/harness-support-matrix.md notes the Cline-no-Windows and OpenCode-subagent-bypass and Hermes-fail-open caveats
    - README.md contains a link to docs/harness-support-matrix.md and does not claim universal protection
  </acceptance_criteria>
  <done>The Tier 1/2/3 support matrix is published in docs and linked from README, honestly distinguishing live-verified from documented and naming every caveat.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New in this plan: `internal/hooks/kilo_trae.go` (`printKiloGuide`/`printTraeGuide`); the consts
`TargetKilo`, `TargetTrae`; `docs/harness-support-matrix.md`; a README "Agent harness support"
subsection. Modified existing: `gatewayTargets` map (+ kilo/trae), `printGatewayGuide` dispatch,
extended `TestInstallGatewayTarget`. The full phase-wide symbol inventory lives in 10-01-PLAN.md's
`<artifacts_this_phase_produces>`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user expectation → documented coverage | The support matrix is the contract with the user about what is actually protected; overclaiming creates a false sense of security |
| Kilo/Trae native tools → no interception | These harnesses' native Bash/file tools have NO pre-exec hook; only MCP tools route through the gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-21 | Repudiation / false assurance (overclaim) | support matrix | mitigate | The matrix explicitly marks only Claude Code as live-verified and labels the other 14 documented-contract-only; README states the headline honestly — users cannot infer protection that was never tested |
| T-10-22 | Elevation (Tier-3 native bypass) | Kilo/Trae native tools | accept + document | Kilo/Trae have no pre-exec hook (upstream gap, Kilo FR #5827); their native tools are UNGUARDED. Documented plainly in the guide AND the matrix; MCP tools are still intercepted via the gateway. No code can close this without upstream hook support |
| T-10-23 | Information Disclosure (stale caveats) | docs drift | accept | The matrix is sourced directly from RESEARCH §3; the SUMMARY records the source date so future harness-contract changes (e.g. Codex PR #18385 landing) can be re-reconciled |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Docs + Go stdlib only; no package installs |
</threat_model>

<verification>
- `go build ./...` clean; `go test ./internal/hooks/ -count=1` green (Kilo/Trae gateway-guide + dispatch tests + all prior).
- docs/harness-support-matrix.md exists with all 15 harnesses + the three tiers + honesty notes.
- README links the matrix and does not overclaim.
</verification>

<success_criteria>
- Kilo + Trae route to a printed MCP-gateway guide (no file write) that states native tools are unguarded.
- The Tier 1/2/3 support matrix is published in docs/harness-support-matrix.md and linked from README, distinguishing live-verified (Claude Code) from documented-contract, and naming every Tier-2/Tier-3 caveat.
</success_criteria>

<output>
Create `.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-06-SUMMARY.md` when done.
</output>
