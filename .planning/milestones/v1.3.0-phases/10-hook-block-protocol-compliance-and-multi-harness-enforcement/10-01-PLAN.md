---
phase: 10-hook-block-protocol-compliance-and-multi-harness-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/check/deny_render.go
  - internal/check/deny_render_test.go
  - cmd/beekeeper/main.go
  - internal/hooks/claude_code.go
  - internal/hooks/hooks_test.go
autonomous: true
requirements: [HPC-01, HPC-03, HPC-06]
must_haves:
  truths:
    - "beekeeper check --hook claude-code on a block exits 2 and writes hookSpecificOutput deny JSON to stdout"
    - "beekeeper check with NO --hook flag still exits 1 on block and prints raw Decision JSON (default path unchanged)"
    - "beekeeper check --hook claude-code on an allow/warn exits 0 and emits nothing harness-specific"
    - "the Claude Code installer wires the command 'beekeeper check --hook claude-code' (not bare 'beekeeper check')"
    - "a CI-runnable regression test asserts the exact per-harness deny output + exit code for the shipped binary path"
  artifacts:
    - path: "internal/check/deny_render.go"
      provides: "Pure table-driven RenderDeny(HarnessID, policy.Decision) DenyOutput + HarnessID constants + exitHookBlock=2"
      contains: "func RenderDeny("
      min_lines: 60
    - path: "internal/check/deny_render_test.go"
      provides: "Per-harness deny-contract table test (the missing release gate)"
      contains: "func TestRenderDeny"
    - path: "cmd/beekeeper/main.go"
      provides: "--hook flag on the check command, branching only on block"
      contains: "\"hook\""
  key_links:
    - from: "cmd/beekeeper/main.go newCheckCmd"
      to: "internal/check.RenderDeny"
      via: "if hookTarget != \"\" && !result.Decision.Allow"
      pattern: "RenderDeny"
    - from: "internal/hooks/claude_code.go"
      to: "beekeeper check --hook claude-code"
      via: "claudePreCommand constant"
      pattern: "check --hook claude-code"
---

<objective>
Build the verifiable core of the hook-block fix: a pure, table-driven deny renderer
(`RenderDeny` in `internal/check`), the `--hook` flag on `beekeeper check` that emits
the per-harness deny signal ONLY on block, the corrected Claude Code installer command
string, and the regression gate that was missing — a test asserting the EXACT per-harness
deny output + exit code so an exit-1 silent-over-allow can never ship again.

Purpose: This is the slice that makes the headline security guarantee real and is the only
locally live-verifiable harness (Claude Code). Waves 2 and 3 depend on `RenderDeny` and the
table test existing.
Output: `internal/check/deny_render.go` (+ test), `--hook` flag wiring, fixed
`claudePreCommand`, Claude installer test for the new command string.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-CONTEXT.md
@.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure table-driven RenderDeny + HarnessID/DenyOutput/exitHookBlock in internal/check</name>
  <files>internal/check/deny_render.go</files>
  <read_first>
    - internal/check/handler.go (the analog: Result struct lines 78-83, exitCodeFor lines 444-449, the exitAllow=0/exitBlock=1 constants lines 49-51, finalizeWithAC lines 430-439 — RenderDeny mirrors exitCodeFor's pure-function style)
    - internal/check/paths_test.go (lines 272-299 — the cases/tc table-test idiom Task 1's test reuses)
    - internal/policy (the policy.Decision type: fields Allow bool, Level string, Reason string — used as the RenderDeny input)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-RESEARCH.md §3 (deny-contract families — the source-of-truth for each harness's exact JSON shape) and §2 (the 15-harness matrix rows)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (the deny_render.go section lines 38-157 — struct shapes per family, constant names, import list)
  </read_first>
  <action>
    Create internal/check/deny_render.go in package check. Add a new exported type `HarnessID string`
    and one const per harness whose value EXACTLY matches the `--hook` flag value and the installer
    target name: HarnessClaudeCode="claude-code", HarnessCursor="cursor", HarnessCodex="codex",
    HarnessAugment="augment", HarnessCodeBuddy="codebuddy", HarnessQwen="qwen", HarnessCopilot="copilot",
    HarnessGemini="gemini", HarnessAntigravity="antigravity", HarnessWindsurf="windsurf",
    HarnessCline="cline", HarnessHermes="hermes", HarnessOpenCode="opencode", HarnessKilo="kilo",
    HarnessTrae="trae". Add an unexported const `exitHookBlock = 2` (document: every hook-capable
    harness except Hermes honors exit 2 as deny; Hermes is fail-open on exit codes so its block path
    is JSON-only). Add the exported struct `DenyOutput` with fields Stdout []byte, Stderr []byte,
    ExitCode int (per 10-PATTERNS lines 80-86). Add the pure function
    `func RenderDeny(h HarnessID, d policy.Decision) DenyOutput` — NO I/O, NO os.Exit, NO package-level
    state; callers do the writing/exiting. Contract: if d.Allow is true RenderDeny returns
    DenyOutput{ExitCode:0} with nil Stdout and nil Stderr (allow/warn defers to the harness's own
    permission flow per CONTEXT decision 3 — NEVER emit permissionDecision:"allow"). On block
    (d.Allow==false) Stderr is ALWAYS the non-empty d.Reason (universal stderr baseline) and ExitCode
    is exitHookBlock=2 for all harnesses EXCEPT Hermes where ExitCode=0 (fail-open). Render the
    per-harness Stdout JSON by family using json.Marshal on small typed structs (NOT hand-built
    strings): Family A nested hookSpecificOutput.permissionDecision="deny" + permissionDecisionReason
    with hookEventName="PreToolUse" for claude-code/codex/codebuddy/augment/qwen; Family B flat
    {"permissionDecision":"deny","permissionDecisionReason":...} for copilot; Family C
    {"permission":"deny","user_message":...,"agent_message":...} for cursor; Family D
    {"decision":"deny","reason":...} for gemini; Family E dual-defensive
    {"decision":"deny","permissionDecision":"deny","denyReason":...} for antigravity (MED-confidence);
    Family F {"cancel":true,"errorMessage":...} for cline; Family G {"action":"block","message":...}
    with a guaranteed non-empty message for hermes (this JSON is the ONLY block path; if d.Reason is
    empty substitute "blocked by beekeeper policy"); Family H Windsurf and OpenCode emit nil Stdout
    (Windsurf is exit-2-only; opencode's deny is carried by the JS plugin throwing, so the CLI need
    only signal exit 2 + stderr). For an unknown/empty HarnessID on block, fail CLOSED: exit 2 + stderr
    reason, nil Stdout (never silently allow). Use the imports encoding/json + fmt +
    github.com/home-beekeeper/beekeeper/internal/policy. Keep godoc comment density matching handler.go.
    Do NOT touch handler.go's existing exitAllow/exitBlock/exitCodeFor — the default path is unchanged.
  </action>
  <verify>
    <automated>go build ./internal/check/ && go vet ./internal/check/</automated>
  </verify>
  <acceptance_criteria>
    - internal/check/deny_render.go contains `func RenderDeny(` and `exitHookBlock = 2`
    - internal/check/deny_render.go contains `type HarnessID string` and the const HarnessHermes = "hermes"
    - RenderDeny is pure: deny_render.go contains no `os.Exit`, no `os.Stdout`, no `os.Stderr`, no goroutines
    - `go build ./internal/check/` exits 0 and `go vet ./internal/check/` exits 0
  </acceptance_criteria>
  <behavior>
    - RenderDeny(HarnessClaudeCode, {Allow:false,Reason:"credential read"}) → ExitCode 2, Stdout contains `"permissionDecision":"deny"`, Stderr contains "credential read"
    - RenderDeny(HarnessHermes, {Allow:false,Reason:"policy"}) → ExitCode 0, Stdout contains `"action":"block"` and a non-empty message
    - RenderDeny(HarnessWindsurf, {Allow:false,Reason:"policy"}) → ExitCode 2, Stdout is nil/empty
    - RenderDeny(HarnessCursor, {Allow:false,Reason:"x"}) → Stdout contains `"permission":"deny"`
    - RenderDeny(HarnessGemini, block) → Stdout contains `"decision":"deny"`; RenderDeny(HarnessCopilot, block) → flat `"permissionDecision":"deny"` (no hookSpecificOutput wrapper)
    - RenderDeny(HarnessClaudeCode, {Allow:true}) → ExitCode 0, Stdout nil, Stderr nil
  </behavior>
  <done>RenderDeny is a pure table-driven function in internal/check covering all 15 HarnessIDs by deny-family, building, and vet-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deny-contract table test (the missing release gate) + --hook flag on beekeeper check</name>
  <files>internal/check/deny_render_test.go, cmd/beekeeper/main.go</files>
  <read_first>
    - internal/check/deny_render.go (created in Task 1 — the function under test)
    - internal/check/paths_test.go (lines 272-299 — copy the cases:=[]struct{name,...} + for/t.Run idiom exactly)
    - internal/check/handler_test.go (lines 1-17 — the package check + imports strings/testing/policy idiom)
    - cmd/beekeeper/main.go (newCheckCmd lines 245-308 — the --tool flag is the precedent: declare var, StringVar, branch in RunE; lines 301-303 are the result/os.Exit default path that MUST stay unchanged)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (deny_render_test.go section lines 161-227 and the newCheckCmd modification section lines 231-270)
  </read_first>
  <action>
    Create internal/check/deny_render_test.go in package check with `func TestRenderDeny(t *testing.T)`.
    Build a cases table (one row PER harness × deny-family) with fields name, harness HarnessID,
    decision policy.Decision, wantExit int, wantStdoutContains string, wantStderrContains string.
    Cover at minimum: claude-code/codex/codebuddy/augment/qwen → exit 2 + `"permissionDecision":"deny"`;
    copilot → exit 2 + flat `"permissionDecision":"deny"`; cursor → exit 2 + `"permission":"deny"`;
    gemini → exit 2 + `"decision":"deny"`; antigravity → exit 2 + both `"decision":"deny"` and
    `"permissionDecision":"deny"`; cline → exit 2 + `"cancel":true`; hermes → exit 0 +
    `"action":"block"` (wantStderrContains the reason too, proving non-empty message); windsurf → exit 2
    + empty wantStdoutContains (assert Stdout is empty when wantStdoutContains==""); opencode → exit 2 +
    empty Stdout; plus an allow row (HarnessClaudeCode, Allow:true) asserting exit 0 + empty Stdout +
    empty Stderr (proves non-block never over-allows). In the loop call out:=RenderDeny(tc.harness,
    tc.decision); assert out.ExitCode==tc.wantExit; when wantStdoutContains!="" assert
    strings.Contains(string(out.Stdout), wantStdoutContains) ELSE assert len(out.Stdout)==0; same shape
    for Stderr. Then modify cmd/beekeeper/main.go newCheckCmd: declare `var hookTarget string`; add
    `cmd.Flags().StringVar(&hookTarget, "hook", "", "Harness name for hook invocations (emits exit 2 + harness-specific deny JSON on block; default mode unchanged)")`.
    After `result := check.RunCheck(...)` and BEFORE `os.Exit(result.ExitCode)`, insert: if hookTarget != ""
    AND !result.Decision.Allow → out := check.RenderDeny(check.HarnessID(hookTarget), result.Decision);
    if len(out.Stdout)>0 fmt.Fprint(os.Stdout, string(out.Stdout)); if len(out.Stderr)>0
    fmt.Fprint(os.Stderr, string(out.Stderr)); os.Exit(out.ExitCode). The existing
    os.Exit(result.ExitCode) remains as the default/allow path. Do NOT alter RunCheck, the --tool branch,
    or the stdout Decision JSON that finalizeWithAC already emits (the harness ignores it; tests + shim
    rely on it on the default path).
  </action>
  <verify>
    <automated>go test ./internal/check/ -run TestRenderDeny -count=1 && go build ./cmd/beekeeper/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/check/ -run TestRenderDeny -count=1` exits 0
    - The TestRenderDeny table includes a hermes row asserting wantExit==0 and wantStdoutContains==`"action":"block"`
    - The TestRenderDeny table includes a windsurf row asserting wantExit==2 and empty Stdout
    - The TestRenderDeny table includes an allow row asserting exit 0 + empty Stdout + empty Stderr
    - cmd/beekeeper/main.go contains `"hook"` flag registration and a `check.RenderDeny(check.HarnessID(hookTarget)` call guarded by `!result.Decision.Allow`
    - `go build ./cmd/beekeeper/` exits 0
  </acceptance_criteria>
  <done>The regression gate exists and passes; the --hook flag emits the per-harness deny on block while the default path (no --hook) still exits 1 with raw JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Point the Claude Code installer at 'beekeeper check --hook claude-code' + assert it</name>
  <files>internal/hooks/claude_code.go, internal/hooks/hooks_test.go</files>
  <read_first>
    - internal/hooks/claude_code.go (claudePreCommand const line 16; mergeClaudeHookEntry/removeClaudeHookEntry/claudeEntriesContainCommand lines 53-107 use claudePreCommand as the sentinel — changing the const propagates to idempotency + targeted uninstall with no other change)
    - internal/hooks/hooks_test.go (TestInstallClaudeCode lines 59-114, TestInstallClaudeCodePreservesExistingHooks lines 413-498, TestUninstallClaudeCode lines 370-412 — the readJSON + assertion idiom)
    - .planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-PATTERNS.md (claude_code.go modification section lines 318-330)
  </read_first>
  <action>
    In internal/hooks/claude_code.go change the claudePreCommand constant from "beekeeper check" to
    "beekeeper check --hook claude-code". Do NOT change claudePostCommand ("beekeeper audit-record" —
    PostToolUse is record-only, never blocks). Confirm by reading that beekeeperClaudePreEntry,
    mergeClaudeHookEntry (install idempotency), and removeClaudeHookEntry (uninstall) all reference
    claudePreCommand so the new string propagates to detection + targeted uninstall automatically — make
    NO other logic changes. Then add a focused test to internal/hooks/hooks_test.go:
    `func TestInstallClaudeCodeWiresHookFlag(t *testing.T)` that installs into a t.TempDir() settings.json
    via installClaudeCode, reads it back (reuse the readJSON helper used by the existing Claude tests),
    and asserts the PreToolUse beekeeper entry's inner hooks command equals
    "beekeeper check --hook claude-code" (NOT bare "beekeeper check"). Re-run install once more in the
    same test to assert idempotency (no duplicate entry) under the new command string. This proves the
    installed hook now triggers the exit-2 deny adapter from Plan 01 Tasks 1-2.
  </action>
  <verify>
    <automated>go test ./internal/hooks/ -run 'TestInstallClaudeCode' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/hooks/claude_code.go contains `claudePreCommand  = "beekeeper check --hook claude-code"`
    - internal/hooks/claude_code.go still contains `claudePostCommand = "beekeeper audit-record"` unchanged
    - hooks_test.go contains `func TestInstallClaudeCodeWiresHookFlag`
    - `go test ./internal/hooks/ -run 'TestInstallClaudeCode' -count=1` exits 0 (includes the existing preserve-existing-hooks + dry-run tests, proving no regression)
  </acceptance_criteria>
  <done>The Claude Code installer wires the --hook command and a test asserts the exact installed command string + idempotency.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Newly-created symbols across ALL six plans of Phase 10 (source-grounding / drift verification must
EXCLUDE these from "missing symbol" checks — they are introduced by this phase, not pre-existing):

**internal/check (Plan 01):**
- `RenderDeny` (func), `DenyOutput` (struct: Stdout/Stderr/ExitCode), `HarnessID` (type)
- `exitHookBlock` (const = 2)
- HarnessID consts: `HarnessClaudeCode`, `HarnessCursor`, `HarnessCodex`, `HarnessAugment`, `HarnessCodeBuddy`, `HarnessQwen`, `HarnessCopilot`, `HarnessGemini`, `HarnessAntigravity`, `HarnessWindsurf`, `HarnessCline`, `HarnessHermes`, `HarnessOpenCode`, `HarnessKilo`, `HarnessTrae`
- per-family deny structs (e.g. `hookSpecificOutputDeny`, `copilotDeny`, `cursorDeny`, `geminiDeny`, `antigravityDeny`, `clineDeny`, `hermesDeny`)
- test func `TestRenderDeny`

**cmd/beekeeper (Plan 01):** the `--hook` flag on the `check` command (var `hookTarget`)

**internal/hooks — new files + symbols:**
- Plan 03: `augment.go` (`installAugment`/`uninstallAugment`/`augmentPreCommand`/`augmentSettingsPath`), `codebuddy.go` (`installCodeBuddy`/`uninstallCodeBuddy`/`codebuddyPreCommand`/`codebuddySettingsPath`), `qwen.go` (`installQwen`/`uninstallQwen`/`qwenPreCommand`/`qwenSettingsPath`); `codexConfigPath`, `ensureCodexFeaturesFlag`; consts `TargetAugment`, `TargetCodeBuddy`, `TargetQwen`
- Plan 04: `copilot.go` (`installCopilot`/`uninstallCopilot`/`copilotPreCommand`/`copilotSettingsPath`), `gemini.go` (`installGemini`/`uninstallGemini`/`geminiHooksFile`/`geminiHookEntry`/`geminiSettingsPath`/`containsGeminiHookByCommand`), `antigravity.go` (`installAntigravity`/`uninstallAntigravity`/`antigravityPreCommand`/`antigravitySettingsPath`), `windsurf.go` (`installWindsurf`/`uninstallWindsurf`/`windsurfHooksFile`/`windsurfHook`/`windsurfHooksPath`); consts `TargetCopilot`, `TargetGemini`, `TargetAntigravity`, `TargetWindsurf`
- Plan 05: `hermes.go` (`installHermes`/`uninstallHermes`/`hermesPreCommand`/`hermesConfigPath`), `cline.go` + `cline_windows.go` (`installCline`/`uninstallCline`/`clinePreCommand`/`clineHooksDir`), `opencode_plugin.go` (`installOpenCodePlugin`/`uninstallOpenCodePlugin`/`openCodePluginDir`/`openCodePluginTemplate`); consts `TargetHermes`, `TargetCline`
- Plan 06: `kilo_trae.go` (`printKiloGuide`/`printTraeGuide`); consts `TargetKilo`, `TargetTrae`

**New test funcs (Plans 03-06):** `TestInstallClaudeCodeWiresHookFlag`, `TestInstallAugment`, `TestInstallCodeBuddy`, `TestInstallQwen`, `TestInstallCopilot`, `TestInstallGemini`, `TestInstallAntigravity`, `TestInstallWindsurf`, `TestInstallHermes`, `TestInstallCline`, `TestInstallOpenCodePlugin` (+ Cursor/Codex fix subtests + extended `TestInstallGatewayTarget` / `TestInstallDispatch`)

**New docs:** `docs/harness-support-matrix.md`; a README "Agent harness support" subsection

**Modified existing symbols:** `claudePreCommand` (→ "beekeeper check --hook claude-code"), `beekeeperCodexPreToolUse` command (→ "beekeeper check --hook codex"), the Cursor `installCursor`/`uninstallCursor` event keys (preToolUse → beforeShellExecution/beforeMCPExecution/beforeReadFile + command), the `--target` usage strings in `newHooksCmd`. The default `beekeeper check` path (exitAllow/exitBlock/exitCodeFor/RunCheck) is UNCHANGED.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → beekeeper check (hook) | The agent harness invokes `beekeeper check --hook <h>` on an untrusted tool call; the deny signal it gets back is the ONLY thing standing between a hijacked agent and execution |
| beekeeper check → harness (deny output) | The exit code + stdout/stderr beekeeper emits must match the harness's documented deny contract EXACTLY or the harness fails open and runs the tool |
| installer → ~/.claude/settings.json | The installer writes the command string the harness will execute on every tool call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering (silent over-allow) | RenderDeny exit code / JSON shape | mitigate | TestRenderDeny asserts the EXACT exit code (2, or 0 for Hermes) + the exact deny JSON substring per harness; a wrong shape fails the build — this is the gate that the exit-1 bug slipped (HPC-06) |
| T-10-02 | Elevation (bypass user approval) | non-block path under --hook | mitigate | RenderDeny returns ExitCode 0 + nil Stdout on Allow; the allow-row test asserts no harness-specific output, so warn/allow never emits permissionDecision:"allow" and never bypasses the harness's own approval flow (CONTEXT decision 3) |
| T-10-03 | Denial of Service (default path regression) | newCheckCmd --hook branch | mitigate | The --hook branch is gated on `hookTarget != "" && !result.Decision.Allow`; the default os.Exit(result.ExitCode) path is untouched. Shim/gateway/tests that rely on exit 1 + raw JSON are unaffected (CONTEXT decision 2) |
| T-10-04 | Tampering (Hermes fail-open) | Family G render | mitigate | Hermes ExitCode=0 by design (it ignores exit codes); the block is carried ONLY by `{"action":"block","message":...}` with a guaranteed non-empty message (empty reason → "blocked by beekeeper policy"). The hermes test row asserts both |
| T-10-05 | Spoofing (clobber existing hooks) | claudePreCommand change | accept | The merge-not-clobber path (claude_code.go mergeClaudeHookEntry) already shipped (commit 50513ae) + is covered by TestInstallClaudeCodePreservesExistingHooks; changing only the command constant reuses that path |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan (pure Go stdlib + existing internal/policy import); no Package Legitimacy Gate needed |
</threat_model>

<verification>
- `go build ./...` clean.
- `go vet ./internal/check/ ./cmd/beekeeper/` clean.
- `go test ./internal/check/ ./internal/hooks/ -count=1` green (includes TestRenderDeny + TestInstallClaudeCodeWiresHookFlag + the existing preserve-existing-hooks regression).
- Manual sanity (optional, local): `printf '{"tool_name":"Read","tool_input":{"file_path":"./README.md"}}' | go run ./cmd/beekeeper check --hook claude-code; echo $?` → 0.
</verification>

<success_criteria>
- RenderDeny is a pure, table-driven function in internal/check mapping (HarnessID, Decision) → DenyOutput for all 15 harnesses by deny-family.
- `beekeeper check --hook claude-code` on a block exits 2 and writes hookSpecificOutput deny JSON; on allow/warn exits 0 emitting nothing harness-specific.
- `beekeeper check` with NO --hook still exits 1 on block with raw Decision JSON (default path proven unchanged).
- The Claude Code installer wires `beekeeper check --hook claude-code` and a test asserts the exact command string.
- TestRenderDeny is the CI-runnable regression gate asserting the harness deny contract (HPC-03/HPC-06).
</success_criteria>

<output>
Create `.planning/phases/10-hook-block-protocol-compliance-and-multi-harness-enforcement/10-01-SUMMARY.md` when done.
</output>
