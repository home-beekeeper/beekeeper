---
phase: 12-design-system
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - web/app/globals.css
  - web/app/layout.tsx
autonomous: false
requirements: [DSYS-01, DSYS-02, DSYS-03, DSYS-04]
user_setup: []

must_haves:
  truths:
    - "pnpm build succeeds with the canonical Beekeeper globals.css order (tailwindcss → shadcn/tailwind.css → [fumadocs commented] → @custom-variant dark → @theme inline → :root/.light → reduced-motion media query)"
    - "Tailwind utilities render Beekeeper tokens (bg-background = #0a0d12 dark, primary = amber); shadcn focus ring resolves to teal via --color-ring"
    - "layout.tsx loads Inter + JetBrains Mono via next/font/google, sets suppressHydrationWarning on <html>, includes a keyboard-reachable skip link, and wraps the app in <Providers>"
    - "A visitor can toggle light/dark with persistence and no flash-of-wrong-theme; reduced-motion disables animation site-wide; both themes pass WCAG 2.1 AA with visible focus"
  artifacts:
    - path: "web/app/globals.css"
      provides: "Canonical Beekeeper token CSS (replaces Geist scaffold) — dark default + .light overrides + reduced-motion gate + ambient gradient"
      contains: "@custom-variant dark"
    - path: "web/app/layout.tsx"
      provides: "Inter/JetBrains Mono fonts, Providers wrapper, skip link, suppressHydrationWarning"
      contains: "suppressHydrationWarning"
  key_links:
    - from: "web/app/layout.tsx"
      to: "web/app/providers.tsx"
      via: "import { Providers } from \"./providers\""
      pattern: "from \"./providers\""
    - from: "web/app/globals.css"
      to: "next/font CSS vars"
      via: "--font-sans: var(--font-inter); --font-mono: var(--font-jetbrains-mono)"
      pattern: "var\\(--font-inter\\)"
    - from: "web/app/globals.css"
      to: "shadcn --color-ring"
      via: "--color-ring: var(--color-bk-teal)"
      pattern: "--color-ring"
---

<objective>
Replace the Phase 11 Geist scaffold with the canonical Beekeeper design system: rewrite
web/app/globals.css (the 8-section token order from UI-SPEC) and web/app/layout.tsx (Inter +
JetBrains Mono fonts, suppressHydrationWarning, skip link, Providers wrapper). This is the
integration plan — it lands all four DSYS requirements and gates them with a manual smoke
checkpoint (theme toggle, reduced-motion, WCAG AA, keyboard focus).

Purpose: globals.css and layout.tsx must come AFTER shadcn init (Plan 01) — the CLI clobbers
globals.css, so this plan deliberately overwrites the CLI output with the Beekeeper tokens
(RESEARCH Pitfall 1) — AND after the providers exist (Plan 02), since layout imports Providers.
The Geist→Inter swap and the globals.css rewrite are coupled (globals.css references the font CSS
vars layout.tsx defines), so both files are rewritten together. The opsz axis for Inter has a
known build-failure risk (RESEARCH Pitfall 6 / Assumptions A1) — this plan handles the fallback.

Output: web/app/globals.css, web/app/layout.tsx. Addresses DSYS-01 (token cascade + import order),
DSYS-02 (no-FOUC theme toggle wiring), DSYS-03 (CSS reduced-motion gate + provider in tree),
DSYS-04 (focus ring, skip link, WCAG AA tokens).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-system/12-UI-SPEC.md
@.planning/phases/12-design-system/12-RESEARCH.md
@.planning/phases/12-design-system/12-PATTERNS.md
@.planning/phases/12-design-system/12-VALIDATION.md
</context>

<artifacts_produced>
New symbols / state this plan creates (convergence source-grounding allowlist):
- web/app/globals.css (replaced) — Beekeeper CSS tokens: --color-bk-* family (bg, surface, surface-2,
  surface-3, border, border-strong, fg, fg-strong, dim, dimmer, red, coral, amber, green, teal,
  teal-d, teal-hover, purple); shadcn slot remaps (--color-background/foreground/card/popover/primary/
  secondary/muted/accent/destructive/border/input/ring); --font-sans/--font-mono/--font-display;
  layout tokens --max-content-width, --header-height, --radius; raw :root / .light custom props;
  @custom-variant dark; reduced-motion @media gate; ambient gradient
- web/app/layout.tsx (replaced) — font CSS vars --font-inter, --font-jetbrains-mono; skip link to #main-content
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite globals.css with the canonical Beekeeper token order</name>
  <files>web/app/globals.css</files>
  <read_first>
    - web/app/globals.css (CURRENT state — the shadcn-CLI-clobbered OKLCH zinc output from Plan 01; this task replaces it entirely)
    - .planning/phases/12-design-system/12-UI-SPEC.md (§globals.css Import-Order Contract, lines 98–255 — VERBATIM token values, slot remaps, light overrides; §Animations reduced-motion gate, lines 593–606; §Ambient background gradient, lines 336–345; §Focus ring, lines 796–814; §Fumadocs CSS Integration Notes / Revised Phase 12 entry, lines 939–1001)
    - .planning/phases/12-design-system/12-RESEARCH.md (§Pattern 1 @theme inline, lines 186–203; §Pattern 2 canonical structure + Note on UI-SPEC vs actual shadcn output, lines 205–251; §Code Examples globals.css skeleton, lines 418–451; Pitfall 2, lines 362–370; Anti-Patterns, lines 324–332)
    - .planning/phases/12-design-system/12-PATTERNS.md (§web/app/globals.css 8-section target, lines 42–157; §@theme inline + @custom-variant shared patterns, lines 488–521)
  </read_first>
  <action>
    Replace web/app/globals.css ENTIRELY (do NOT partial-edit — Geist + Inter vars would coexist and
    conflict per Anti-Patterns). Author the canonical order exactly:
    (1) @import "tailwindcss"; (2) @import "shadcn/tailwind.css"; (3) the Fumadocs imports as
    COMMENTED stubs at the correct position — `/* @import "fumadocs-ui/css/shadcn.css"; */`,
    `/* @import "fumadocs-ui/css/preset.css"; */`, `/* @source "../node_modules/fumadocs-ui/dist/**\/*.js"; */`
    — Phase 13 uncomments them (do NOT install Fumadocs; uncommenting now breaks the build);
    (4) `@custom-variant dark (&:where(.dark, .dark *));` — the &:where form (NOT &:is(.dark *), which
    misses elements that ARE .dark); (5) the @theme inline block (inline is MANDATORY because the slot
    remaps are indirect var refs) containing: --font-sans var(--font-inter), --font-mono
    var(--font-jetbrains-mono), --font-display var(--font-inter); the full --color-bk-* token family
    verbatim from UI-SPEC; the shadcn semantic slot remaps including --color-primary var(--color-bk-amber)
    and --color-ring var(--color-bk-teal); layout tokens --max-content-width 1180px, --header-height
    60px, --radius 0.375rem; (6) the raw :root custom props (dark defaults) and the .light overrides —
    copy ALL hex values verbatim from UI-SPEC §globals.css Order Contract, including the WCAG-darkened
    light values (amber #8a6500, teal #0a6b75, coral #a04d12) and the .light shadcn slot remaps
    (--color-primary #8a6500, --color-ring #0a6b75); (7) the dark-only ambient gradient on body
    (radial-gradient amber .05 + teal .04), scoped so it does not apply under .light; (8) the
    reduced-motion @media block zeroing animation-duration/iteration-count/transition-duration and
    scroll-behavior with !important (belt-and-suspenders, active before JS hydrates). Beekeeper's
    @theme inline MUST be the last @theme block so its values win over shadcn (and the future Fumadocs
    imports). Biome noUnknownAtRules is already off, so @theme/@source/@custom-variant won't lint-error.
  </action>
  <verify>
    <automated>cd web; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm build` exits 0 (Fumadocs lines are commented, so no missing-package failure).
    - `grep -c '@import "tailwindcss"' web/app/globals.css` returns 1 AND it is the first import.
    - `grep '@import "shadcn/tailwind.css"' web/app/globals.css` matches.
    - Fumadocs imports are COMMENTED: `grep -E '^\s*/\*\s*@import "fumadocs-ui/css/shadcn.css";\s*\*/' web/app/globals.css` matches; `grep -vE '^\s*(/\*|\*)' web/app/globals.css | grep -c 'fumadocs-ui' ` returns 0 (no ACTIVE fumadocs line).
    - `grep '@custom-variant dark (&:where(.dark, .dark \*))' web/app/globals.css` matches (correct dark variant form).
    - `grep -E '@theme inline' web/app/globals.css` matches.
    - `grep -- '--color-ring' web/app/globals.css` matches in BOTH the @theme block and the .light block (teal in both themes).
    - `grep -- '--color-bk-amber:' web/app/globals.css` matches `#e3b341`; `grep -- '--color-primary:.*var(--color-bk-amber)' web/app/globals.css` matches.
    - `grep -- '#8a6500' web/app/globals.css` matches AND `grep -- '#0a6b75' web/app/globals.css` matches (WCAG-darkened light amber + teal present).
    - No Geist references remain: `grep -c 'geist' web/app/globals.css` returns 0 (case-insensitive: `grep -ic geist` returns 0).
    - `grep 'prefers-reduced-motion: reduce' web/app/globals.css` matches (CSS motion gate present).
    - `grep -- '--font-sans: var(--font-inter)' web/app/globals.css` matches.
  </acceptance_criteria>
  <done>globals.css is the canonical 8-section Beekeeper order: tailwindcss + shadcn import, Fumadocs commented, &:where dark variant, @theme inline with full token family + slot remaps (ring=teal, primary=amber), :root/.light raw props with WCAG-darkened light values, ambient gradient, reduced-motion gate. No Geist. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite layout.tsx — Inter/JetBrains fonts, skip link, Providers, opsz fallback</name>
  <files>web/app/layout.tsx</files>
  <read_first>
    - web/app/layout.tsx (CURRENT Phase 11 state — Geist-free 19-line scaffold; this task replaces it entirely)
    - web/app/providers.tsx (created in Plan 02 — layout imports Providers from "./providers")
    - .planning/phases/12-design-system/12-UI-SPEC.md (§Font Families, lines 259–307; §Theme Toggle layout.tsx replacement, lines 639–661; §Skip link, lines 828–835)
    - .planning/phases/12-design-system/12-RESEARCH.md (§Pattern 5 Font Loading, lines 296–322; §layout.tsx Phase 12 replacement code example, lines 453–499; Pitfall 4 suppressHydrationWarning, lines 382–390; Pitfall 6 opsz build failure + fallback, lines 402–410; Assumptions A1, line 594)
    - .planning/phases/12-design-system/12-PATTERNS.md (§web/app/layout.tsx, lines 160–243 — key differences from Phase 11)
  </read_first>
  <action>
    Replace web/app/layout.tsx ENTIRELY (do NOT partial-edit — leftover Phase 11 attributes would
    conflict). Import Inter and JetBrains_Mono from next/font/google, the Providers from "./providers",
    and "./globals.css". Configure Inter with subsets ["latin"], variable "--font-inter",
    display "swap", and axes ["opsz"] (the optical-size axis for display sizing). Configure
    JetBrains_Mono with subsets ["latin"], variable "--font-jetbrains-mono", display "swap". Keep the
    existing metadata (title "Beekeeper", description "Real-time safety harness for autonomous coding
    agents."). In RootLayout, set the <html> element lang="en", className to the two font variable
    classes (`${inter.variable} ${jetbrainsMono.variable}`), and suppressHydrationWarning (REQUIRED —
    next-themes writes the .dark/.light class before hydration; without it React throws a mismatch,
    Pitfall 4). The <body> gets `min-h-full flex flex-col antialiased`. Before <Providers>, insert the
    skip link: an <a href="#main-content"> with the exact sr-only / focus-visible utility chain from
    UI-SPEC §Skip link (visually hidden until focused, then absolutely positioned top-left with
    z-[100], bk-surface bg, bk-fg text, padding, rounded, border bk-border-strong) and text
    "Skip to main content". Wrap {children} in <Providers>. OPSZ FALLBACK (Pitfall 6 / A1): if
    `pnpm build` fails with a font/axis error mentioning opsz or an invalid fonts.googleapis.com URL,
    remove the `axes: ["opsz"]` prop from the Inter config and instead rely on
    `font-optical-sizing: auto` in CSS (the browser activates opsz automatically for Inter v4 without
    the next/font axis declaration) — same visual result, zero rework. If you take the fallback, add
    `font-optical-sizing: auto` to globals.css on h1, h2, h3, .display per UI-SPEC §Inter Display
    substitution and note the deviation in the SUMMARY. NOTE: page.tsx still has no element with
    id="main-content"; the skip link target is wired by Phase 15's marketing home — that is expected
    this phase; do not add page content here (out of scope per UI-SPEC §Scope Fence).
  </action>
  <verify>
    <automated>cd web; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm build` exits 0 (with opsz, OR with the documented opsz-removed fallback).
    - `grep 'suppressHydrationWarning' web/app/layout.tsx` matches on the <html> element (Pitfall 4).
    - `grep 'from "next/font/google"' web/app/layout.tsx` matches; `grep -E "Inter\(|JetBrains_Mono\(" web/app/layout.tsx` matches both.
    - `grep -- '--font-inter' web/app/layout.tsx` matches AND `grep -- '--font-jetbrains-mono' web/app/layout.tsx` matches (CSS var names align with globals.css @theme).
    - `grep 'from "./providers"' web/app/layout.tsx` matches AND `grep -E "<Providers>" web/app/layout.tsx` matches (children wrapped).
    - `grep 'href="#main-content"' web/app/layout.tsx` matches AND `grep 'Skip to main content' web/app/layout.tsx` matches (WCAG 2.4.1 skip link).
    - `grep -ic geist web/app/layout.tsx` returns 0 (no Geist remnants).
    - If opsz fallback taken: `grep 'font-optical-sizing: auto' web/app/globals.css` matches AND the SUMMARY documents the deviation.
    - `cd web && pnpm lint` (Biome) clean.
  </acceptance_criteria>
  <done>layout.tsx loads Inter + JetBrains Mono (opsz or documented fallback), sets suppressHydrationWarning on <html>, wires font CSS vars matching globals.css, includes the keyboard-reachable skip link, and wraps the app in <Providers>. No Geist. Build + lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke verification — theme toggle, reduced-motion, WCAG AA, keyboard focus</name>
  <read_first>
    - .planning/phases/12-design-system/12-VALIDATION.md (§Manual-Only Verifications, lines 72–80)
    - .planning/phases/12-design-system/12-UI-SPEC.md (§WCAG AA contrast tables, lines 381–431; §Focus / Keyboard / WCAG-AA Contract, lines 794–851)
  </read_first>
  <what-built>
    Plans 01–03 produced: shadcn foundation + four base components; ThemeProvider + ReducedMotionProvider;
    the canonical Beekeeper globals.css (dark default + .light overrides + reduced-motion gate +
    teal focus ring); and layout.tsx with Inter/JetBrains fonts, suppressHydrationWarning, and the
    skip link. The automated gate (`pnpm build` + Biome) is green. These four DSYS behaviors are
    paint-timing / a11y behaviors with NO automated runner this phase (Vitest/Playwright land in
    Phase 19), so they need a one-time human smoke check against the built output.
  </what-built>
  <how-to-verify>
    From web/: run `pnpm build`, then serve the static output (`pnpm start`, or any static server over
    web/out/). Open the page in a browser, then:
    1. THEME TOGGLE + PERSISTENCE + NO-FOUC (DSYS-02): switch the OS / DevTools theme or, since the
       in-page ThemeToggle UI is a Phase 15 deliverable, set the theme via the console
       (`localStorage.setItem('bk-theme','light')` then reload, and `'dark'` then reload). Confirm:
       the .light/.dark class lands on <html>, the choice persists across a hard reload, and there is
       NO flash of the wrong theme on load (the inline next-themes script applies the class before
       first paint).
    2. REDUCED-MOTION (DSYS-03): DevTools → Rendering → "Emulate prefers-reduced-motion: reduce".
       Confirm <html> gets data-reduced-motion="true" and any transition/animation is suppressed
       (the CSS @media gate + the provider both fire).
    3. WCAG AA CONTRAST (DSYS-04): with Chrome DevTools a11y / contrast checker, spot-check the
       body text, headings, and the teal/amber accent colors against the surfaces in BOTH themes;
       compare to the UI-SPEC §WCAG contrast tables (all pairs are pre-verified ≥ 4.5:1 for text,
       ≥ 3:1 for UI/large). Confirm no contrast regressions.
    4. KEYBOARD + FOCUS (DSYS-04): press Tab from page load — the "Skip to main content" link must be
       the first focusable element and become visible on focus; continue tabbing and confirm a visible
       teal focus ring (--color-ring) on focusable elements in both themes.
  </how-to-verify>
  <resume-signal>Type "approved" if all four behaviors pass, or describe any failure (wrong theme flash, motion not gated, a contrast pair below threshold, missing/invisible focus or skip link) for a gap-closure pass.</resume-signal>
  <acceptance_criteria>
    - DSYS-02: theme class applies, persists across reload, no flash-of-wrong-theme (human-confirmed).
    - DSYS-03: data-reduced-motion="true" appears under emulation and animation is suppressed site-wide (human-confirmed).
    - DSYS-04: both themes pass AA contrast spot-checks vs UI-SPEC tables; skip link is the first tab stop and visible on focus; teal focus ring visible in both themes (human-confirmed).
  </acceptance_criteria>
  <done>All four DSYS behavioral criteria human-verified against the built static output in both themes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → static out/ | Tailwind v4 resolves the token cascade at build; fonts emitted into out/_next/static (no runtime Google request) |
| client → DOM | next-themes inline script writes the theme class before first paint; matchMedia drives the reduced-motion attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information disclosure | next/font/google build-time fetch | accept | Fonts are downloaded at build and self-hosted in out/_next/static; no runtime third-party font request, no user data leaves the build. Static export — no server. Low/no surface (RESEARCH §Architectural Responsibility Map). |
| T-12-06 | Tampering | CSS token cascade order (shadcn/Fumadocs vs Beekeeper) | mitigate | Beekeeper @theme inline is authored LAST so its values win; Fumadocs imports stay COMMENTED until Phase 13 (Pitfall 2) — wrong order would silently render wrong colors with no build error; acceptance criteria assert order + commented-Fumadocs + token presence. |
| T-12-04 | Tampering | next-themes inline script vs CSP | accept | No CSP headers configured at static-export stage; deferred to Phase 17 (SEO & Headers) per RESEARCH §Security Domain. |
</threat_model>

<verification>
- `cd web && pnpm build` exits 0 (Tasks 1–2).
- `cd web && pnpm lint` clean.
- globals.css: correct import order; @custom-variant &:where form; @theme inline last; ring=teal both themes; primary=amber; WCAG-darkened light values; reduced-motion gate; no Geist.
- layout.tsx: Inter + JetBrains fonts (opsz or documented fallback); suppressHydrationWarning; skip link; <Providers> wrap; no Geist.
- Task 3 human-verify: DSYS-02 (toggle/persist/no-FOUC), DSYS-03 (reduced-motion), DSYS-04 (AA contrast + keyboard + focus) all pass in both themes.
</verification>

<success_criteria>
The Beekeeper design system is live and integrated: canonical globals.css token cascade (DSYS-01),
no-FOUC theme toggle wiring with persistence (DSYS-02), CSS + provider reduced-motion gate (DSYS-03),
WCAG AA tokens + teal focus ring + skip link (DSYS-04). Build + Biome green; all four DSYS behaviors
human-verified in both light and dark themes.
</success_criteria>

<output>
Create `.planning/phases/12-design-system/12-03-SUMMARY.md` when done.
</output>
