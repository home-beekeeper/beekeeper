---
phase: 13-docs-content-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/next.config.ts
  - web/next.config.mjs
  - web/source.config.ts
  - web/tsconfig.json
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "pnpm build (in web/) exits 0 after fumadocs deps are installed and the config files are wired"
    - "fumadocs-mdx generates the web/.source/ directory (server.ts/browser.ts) at build time"
    - "next.config.mjs retains output:export, trailingSlash:true, images.unoptimized, transpilePackages unchanged"
  artifacts:
    - path: "web/next.config.mjs"
      provides: "ESM Next config wrapped by createMDX, all four static-export keys preserved"
      contains: "createMDX"
    - path: "web/source.config.ts"
      provides: "fumadocs-mdx collection config pointing at content/docs"
      contains: "defineDocs"
    - path: "web/tsconfig.json"
      provides: "collections/* path alias resolving to ./.source/*"
      contains: "collections/*"
    - path: "web/package.json"
      provides: "pinned fumadocs deps + postinstall codegen script"
      contains: "fumadocs-mdx"
  key_links:
    - from: "web/next.config.mjs"
      to: "fumadocs-mdx/next"
      via: "createMDX import + withMDX(nextConfig) wrap"
      pattern: "createMDX"
    - from: "web/source.config.ts"
      to: "content/docs"
      via: "defineDocs({ dir: 'content/docs' })"
      pattern: "content/docs"
---

<objective>
Install the Fumadocs build-time pipeline (fumadocs-ui, fumadocs-core, fumadocs-mdx, @types/mdx at research-pinned versions) and wire the three config files that make `fumadocs-mdx` run inside `next build`: the `next.config.ts` → `next.config.mjs` rename (adding the ESM-only `createMDX` wrapper), the new `source.config.ts` collection config, and the `tsconfig.json` `collections/*` path alias.

Purpose: This is Wave 0 toolchain wiring (per 13-RESEARCH.md § Wave 0 Gaps). Nothing downstream — the docs route, source loader, or static-search route (plan 13-02) — can resolve or build until `.source/` is generated and `collections/server` resolves. This plan establishes that foundation and proves it with a clean `pnpm build`.

Output: Pinned deps in package.json/pnpm-lock.yaml; `web/next.config.mjs` (renamed, createMDX-wrapped); `web/source.config.ts`; `web/tsconfig.json` with the `collections/*` alias; a `postinstall: fumadocs-mdx` codegen script. A `pnpm build` that exits 0 and generates `web/.source/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-docs-content-pipeline/13-RESEARCH.md
@.planning/phases/13-docs-content-pipeline/13-PATTERNS.md
@.planning/phases/13-docs-content-pipeline/13-VALIDATION.md
@web/next.config.ts
@web/tsconfig.json
@web/package.json
@web/biome.json
</context>

<artifacts_this_phase_produces>
This plan (13-01) produces the toolchain layer of the phase artifact set:
- `web/next.config.mjs` — NEW (renamed from `web/next.config.ts`, which is DELETED); wraps the existing config with `createMDX()` from `fumadocs-mdx/next`.
- `web/source.config.ts` — NEW; `defineDocs({ dir: 'content/docs' })` + `defineConfig()`.
- `web/tsconfig.json` — MODIFIED; adds `"collections/*": ["./.source/*"]` to `compilerOptions.paths`.
- `web/package.json` — MODIFIED; adds deps `fumadocs-ui@16.9.3`, `fumadocs-core@16.9.3`, `fumadocs-mdx@15.0.11`, `@types/mdx@2.0.14` and a `postinstall: fumadocs-mdx` script.
- `web/.source/` — GENERATED at build (gitignored; not committed).

(Downstream artifacts produced by 13-02: `web/lib/source.ts`, `web/app/docs/layout.tsx`, `web/app/docs/[[...slug]]/page.tsx`, `web/app/api/search/route.ts`, the `RootProvider` wiring in `web/app/providers.tsx`, the `@source` glob restore in `web/app/globals.css`. By 13-03: `web/content/docs/**` meta.json + seed MDX.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned fumadocs deps + add postinstall codegen script</name>
  <files>web/package.json, web/pnpm-lock.yaml</files>
  <read_first>
    - web/package.json (current deps/scripts — the file being modified; `packageManager: pnpm@11.1.3`, no fumadocs deps yet)
    - 13-RESEARCH.md § Standard Stack (exact versions) and § Package Legitimacy Audit (all 4 packages APPROVED/[ASSUMED], no [SLOP]/[SUS])
    - 13-RESEARCH.md § Pattern 10 (postinstall: fumadocs-mdx rationale + that it is the project's OWN script, NOT a dependency lifecycle script, so it does not trip the pnpm allowBuilds gate)
    - 13-PATTERNS.md § web/package.json — MODIFY (exact target scripts + dependencies block)
    - .planning/STATE.md Phase 11-01 decisions (authoritative lockfile is the REPO-ROOT pnpm-lock.yaml in pnpm workspace mode; the Phase-11 build-approval gate was resolved by an explicit root DENY — do not re-open it)
  </read_first>
  <action>
    Install the four research-pinned packages into the `web` workspace member: `fumadocs-ui@16.9.3`, `fumadocs-core@16.9.3`, `fumadocs-mdx@15.0.11`, `@types/mdx@2.0.14`. Run the install non-interactively from the repo root with the env var `CI=true` set so pnpm's build-script approval prompt cannot hang the run — use `pnpm --filter web add fumadocs-ui@16.9.3 fumadocs-core@16.9.3 fumadocs-mdx@15.0.11 @types/mdx@2.0.14` (exact-version specifiers, no caret, to match research pins). Per 13-RESEARCH.md none of these three fumadocs packages declare a `postinstall` script, so no change to `pnpm-workspace.yaml` `allowBuilds`/`ignoredBuiltDependencies` is required and none should be made. After install, add a `postinstall` script `"postinstall": "fumadocs-mdx"` as the FIRST entry in `web/package.json` `scripts` (this is the package's own codegen script per Pattern 10, generating `.source/` types for fresh clones; it is NOT a dependency lifecycle script and does not require an allowBuilds entry). Confirm the four versions landed exactly (no caret-widening) in `web/package.json` `dependencies`. The authoritative lockfile updated by this install is the REPO-ROOT `pnpm-lock.yaml` (workspace mode, per Phase-11 decision) — let pnpm update it; do not hand-edit a member lockfile.
  </action>
  <verify>
    <automated>cd web && pnpm install --frozen-lockfile</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` `dependencies` contains exactly `"fumadocs-ui": "16.9.3"`, `"fumadocs-core": "16.9.3"`, `"fumadocs-mdx": "15.0.11"`, `"@types/mdx": "2.0.14"` (no `^`/`~` prefixes).
    - `web/package.json` `scripts` contains `"postinstall": "fumadocs-mdx"`.
    - `pnpm install --frozen-lockfile` (run in web/) exits 0 — proves the lockfile is consistent and the install was non-interactive.
    - `pnpm-workspace.yaml` `allowBuilds`/`ignoredBuiltDependencies` are NOT modified for any fumadocs package (no fumadocs lifecycle script was approved).
  </acceptance_criteria>
  <done>The four fumadocs deps are pinned in package.json, the postinstall codegen script is present, and a frozen-lockfile install succeeds non-interactively without touching the workspace build-approval gate.</done>
</task>

<task type="auto">
  <name>Task 2: Rename next.config.ts → next.config.mjs with createMDX wrapper</name>
  <files>web/next.config.mjs, web/next.config.ts</files>
  <read_first>
    - web/next.config.ts (the file being renamed — confirm the four keys: `output:"export"`, `trailingSlash:true`, `images.unoptimized:true`, `transpilePackages:["three","@react-three/fiber","@react-three/drei"]`)
    - 13-RESEARCH.md § Pattern 1 (next.config.mjs rename rationale: fumadocs-mdx is ESM-only; `.mjs` is the safe path on Node 22) and § Pitfall 5 (ERR_REQUIRE_ESM if left as .ts) and § Code Examples → next.config.mjs (canonical target)
    - 13-RESEARCH.md § Assumptions Log A1 (fallback: if `.mjs` rename still fails, try `.mts` or add `"type":"module"` to web/package.json — last resort) and A5 (do NOT add fumadocs-ui to transpilePackages)
    - 13-PATTERNS.md § web/next.config.mjs (exact target content; Biome covers `.mjs` via files.includes ["**"])
  </read_first>
  <action>
    Delete `web/next.config.ts` and create `web/next.config.mjs` in its place. The new file imports `createMDX` from `fumadocs-mdx/next`, declares the same NextConfig object via a JSDoc type annotation `/** @type {import('next').NextConfig} */` (since `.mjs` has no `import type` from `"next"` resolution guarantee — use the JSDoc form per research), and exports `withMDX(nextConfig)` where `const withMDX = createMDX()`. Carry over ALL FOUR existing keys verbatim and unchanged: `output: 'export'`, `trailingSlash: true`, `images: { unoptimized: true }`, `transpilePackages: ['three', '@react-three/fiber', '@react-three/drei']`. Do NOT add `fumadocs-ui` or `fumadocs-core` to `transpilePackages` (Assumption A5 — they ship pre-compiled dist). Use 2-space indent (Biome). Ensure no `web/next.config.ts` remains (Next would otherwise prefer one over the other / warn on duplicate config).
  </action>
  <verify>
    <automated>cd web && node -e "import('./next.config.mjs').then(m=>{const c=m.default;if(c.output!=='export'||c.trailingSlash!==true||!c.images.unoptimized||!Array.isArray(c.transpilePackages))throw new Error('config keys not preserved: '+JSON.stringify(c));console.log('config OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `web/next.config.ts` no longer exists; `web/next.config.mjs` exists.
    - `web/next.config.mjs` contains `import { createMDX } from 'fumadocs-mdx/next'` and `export default withMDX(nextConfig)`.
    - The resolved config object has `output === 'export'`, `trailingSlash === true`, `images.unoptimized` truthy, and `transpilePackages` is the unchanged 3-element three/r3f/drei array (proven by the node import verify above).
    - `transpilePackages` does NOT include `fumadocs-ui`/`fumadocs-core`.
  </acceptance_criteria>
  <done>next.config is now an ESM `.mjs` wrapped by createMDX, the old `.ts` is gone, and all four static-export keys are provably preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Create source.config.ts + add tsconfig collections alias, then build-verify Wave 0</name>
  <files>web/source.config.ts, web/tsconfig.json</files>
  <read_first>
    - web/tsconfig.json (the file being modified — current `paths` is only `"@/*": ["./*"]`; `include` already has `**/*.mts`)
    - 13-RESEARCH.md § Pattern 2 (source.config.ts canonical: defineDocs+defineConfig, dir 'content/docs', forward slashes) and § Pattern 3 (tsconfig collections/* alias) and § Pitfall 4 (collections/server fails TS resolution without the alias) and § Pitfall 7 (always forward slashes in dir on Windows)
    - 13-PATTERNS.md § web/source.config.ts — NEW and § web/tsconfig.json — MODIFY (exact target paths block)
    - 13-VALIDATION.md § Wave 0 Requirements + Per-Task Verification Map Wave 0 row (gate: `cd web && pnpm build` exits 0; `.source/` generated)
  </read_first>
  <action>
    Create `web/source.config.ts` per Pattern 2: import `defineDocs, defineConfig` from `fumadocs-mdx/config`; export `const docs = defineDocs({ dir: 'content/docs' })` (forward slash, relative to web/ root); `export default defineConfig()`. No `"use client"` (build-time config). In `web/tsconfig.json`, add `"collections/*": ["./.source/*"]` to `compilerOptions.paths` immediately after the existing `"@/*": ["./*"]` entry — change ONLY the `paths` object; leave `include`, `exclude`, and every other `compilerOptions` field untouched (the `**/*.mts` include already covers the renamed config). Then run `pnpm build` to confirm the Wave 0 toolchain is coherent. NOTE: at this point `content/docs/` does not exist yet (plan 13-03 creates seed MDX) and `lib/source.ts` / the docs route do not exist yet (plan 13-02) — `pnpm build` must still exit 0 here because fumadocs-mdx generates an empty-but-valid `.source/` from an absent/empty content dir and there are no docs pages to render yet. If `pnpm build` fails because fumadocs-mdx errors on a missing `content/docs` directory, create the empty directory `web/content/docs/` (with no MDX) so codegen succeeds; do NOT add seed content here (that is 13-03's scope fence). Confirm `web/.source/` is generated after the build.
  </action>
  <verify>
    <automated>cd web && pnpm build && node -e "const fs=require('fs');if(!fs.existsSync('.source'))throw new Error('.source not generated');const t=fs.readFileSync('tsconfig.json','utf8');if(!t.includes('collections/*'))throw new Error('collections alias missing');console.log('Wave 0 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/source.config.ts` exists and contains `defineDocs({ dir: 'content/docs' })` (forward slash) and `export default defineConfig()`.
    - `web/tsconfig.json` `compilerOptions.paths` contains both `"@/*": ["./*"]` and `"collections/*": ["./.source/*"]`; no other tsconfig field changed.
    - `cd web && pnpm build` exits 0 (Wave 0 minimum gate from 13-VALIDATION.md).
    - `web/.source/` directory exists after the build (fumadocs-mdx codegen ran).
  </acceptance_criteria>
  <done>source.config.ts and the tsconfig collections alias are in place, `.source/` is generated, and `pnpm build` is green — the Wave 0 toolchain boundary is proven coherent before any route/loader code is added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → web/node_modules | New fumadocs packages cross into the build toolchain (supply-chain surface) |
| pnpm install → workspace build-approval gate | Dependency lifecycle scripts could request elevated install-time execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | fumadocs-ui/core/mdx + @types/mdx install | mitigate | Pin EXACT versions (16.9.3/16.9.3/15.0.11/2.0.14) from 13-RESEARCH.md § Package Legitimacy Audit; install with `pnpm install --frozen-lockfile` so the locked tree cannot drift; all four packages are APPROVED (official Fumadocs ecosystem / DefinitelyTyped), none [SLOP]/[SUS] |
| T-13-02 | Elevation of Privilege | pnpm build-approval gate | mitigate | Research VERIFIED none of the three fumadocs packages declare a `postinstall` lifecycle script; install runs non-interactively with `CI=true`; the Phase-11 root DENY (`allowBuilds: sharp:false`, `ignoredBuiltDependencies: unrs-resolver`) stays intact and is NOT re-opened — no untrusted lifecycle script runs at install |
| T-13-SC | Tampering | npm installs (this plan's only install surface) | mitigate | slopcheck unavailable in env → packages tagged [ASSUMED] per research; they match the locked STACK.md recommendation and are well-known (>50K–1M wk downloads, github.com/fuma-nama/fumadocs / DefinitelyTyped). Maintainer may skip the human-verify checkpoint for these per research note. No [SLOP] package present. |

**Disposition note:** No threat in this plan is rated `high`. The supply-chain surface is bounded to four well-known, version-pinned packages installed under a frozen lockfile with the existing build-approval DENY intact. No blocking human checkpoint is required (research explicitly notes the maintainer may skip the legitimacy checkpoint given project context and the absence of [SLOP]/[SUS] verdicts).
</threat_model>

<verification>
Wave 0 (toolchain) gate, per 13-VALIDATION.md:
- `cd web && pnpm install --frozen-lockfile` exits 0 (deps pinned + lockfile consistent).
- `cd web && pnpm build` exits 0.
- `web/.source/` is generated by fumadocs-mdx codegen.
- `web/next.config.mjs` resolves with all four static-export keys preserved; `web/next.config.ts` is gone.
- `web/tsconfig.json` has the `collections/*` alias.
</verification>

<success_criteria>
- Fumadocs deps installed at exact research-pinned versions under a consistent root lockfile.
- `next.config.mjs` is the ESM createMDX-wrapped config; the four static-export keys (output:export, trailingSlash, images.unoptimized, transpilePackages) are provably unchanged.
- `source.config.ts` and the tsconfig `collections/*` alias exist.
- `pnpm build` exits 0 and generates `web/.source/` — the Wave 0 toolchain boundary is green before route/loader code lands in 13-02.
</success_criteria>

<output>
Create `.planning/phases/13-docs-content-pipeline/13-01-SUMMARY.md` when done.
</output>
