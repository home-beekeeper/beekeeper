---
phase: 16-3d-layer
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - web/tests/gfx_spec.py
  - web/package.json
autonomous: false
requirements: [GFX-04]
user_setup: []

must_haves:
  truths:
    - "The home page LCP is < 2500ms with the SVG as the LCP element (GFX-04)"
    - "Active WebGL canvas count stays <= 1 across a navigate-away-and-back cycle (no context leak) (GFX-04)"
    - "The full GFX suite (gfx_spec.py) and home_spec.py both exit 0 on the final build"
    - "Maintainer confirms the hive reads as a hive, matches brand teal/amber, and rotates/snaps in both themes (subjective acceptance)"
  artifacts:
    - path: "web/tests/gfx_spec.py"
      provides: "GFX-04 LCP proxy + no-context-leak assertions (final suite)"
      contains: "largest-contentful-paint"
  key_links:
    - from: "web/tests/gfx_spec.py"
      to: "out/index.html"
      via: "Playwright performance.getEntriesByType('largest-contentful-paint') LCP proxy"
      pattern: "largest-contentful-paint"
---

<objective>
Close GFX-04: prove the performance budget (LCP < 2.5s with the SVG as the LCP element) and the no-WebGL-context-leak guarantee across navigation cycles, then run the full phase gate and a maintainer visual/interaction UAT. The LCP measurement uses the Windows-safe Playwright `performance` proxy as the primary path (per RESEARCH.md Open Question 4), with the `lighthouse` CLI as an optional/CI path.

Purpose: GFX-04 is the last requirement and the phase release gate. It is also where the subjective "does the hive look and feel right" acceptance happens — the two manual-only verifications from VALIDATION.md.
Output: gfx_spec.py GFX-04 block implemented (LCP proxy + context-leak); optional lighthouse devDep; full-suite green; maintainer UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-3d-layer/16-RESEARCH.md
@.planning/phases/16-3d-layer/16-PATTERNS.md
@.planning/phases/16-3d-layer/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GFX-04 LCP-proxy + no-context-leak assertions in gfx_spec.py</name>
  <read_first>
    - web/tests/gfx_spec.py — the harness from Plans 16-01/16-02 (the labeled GFX-04 PENDING placeholder to replace; the failures[]/fail()/ok() helpers; the http.server scaffold)
    - .planning/phases/16-3d-layer/16-PATTERNS.md — § "web/tests/gfx_spec.py" → the GFX-04 row (Playwright `performance.getEntriesByType('largest-contentful-paint')` Windows-safe LCP proxy) and the context-count pattern
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § "Validation Architecture" test map (GFX-04 rows), § Open Question 4 (Lighthouse-on-Windows risk -> Playwright proxy is the documented fallback/primary), § "Code Examples" -> "Lighthouse LCP Audit" (the optional CLI path)
  </read_first>
  <action>Replace the GFX-04 PENDING placeholder in `web/tests/gfx_spec.py` with two real assertions. (1) LCP proxy: `page.goto(base_url, wait_until="networkidle")`, then read `page.evaluate("performance.getEntriesByType('largest-contentful-paint').slice(-1)[0]?.startTime ?? performance.getEntriesByType('paint').find(e => e.name === 'largest-contentful-paint')?.startTime ?? 0")`; assert the value is `> 0` and `< 2500` (ms). If the proxy returns 0/None in headless (LCP entry not emitted), fall back to the `first-contentful-paint` paint entry and assert `< 2500`, with a labeled note that LCP-vs-FCP proxy was used. (2) No-context-leak: after the first goto, capture `canvas_a = page.evaluate("document.querySelectorAll('canvas').length")`; navigate to a docs route and back (`page.goto(base_url + "docs/getting-started/", wait_until="networkidle")` then `page.goto(base_url, wait_until="networkidle")`); capture `canvas_b`; assert `canvas_b <= 1` AND `canvas_b <= max(canvas_a, 1)` (count never grows past one — proves R3F auto-dispose + single-canvas rule, no accumulation). Do not add watch-mode flags. Keep all earlier GFX-01/02/03 blocks intact.</action>
  <verify>
    <automated>cd web && pnpm build && python tests/gfx_spec.py</automated>
  </verify>
  <acceptance_criteria>
    - `python tests/gfx_spec.py` exits 0 with all four GFX blocks (01/02/03/04) implemented and green.
    - GFX-04 LCP block asserts the proxy value is `> 0` and `< 2500`, with a labeled FCP fallback path.
    - GFX-04 context-leak block navigates away and back and asserts the canvas count does not grow past 1.
    - No remaining PENDING placeholders in gfx_spec.py.
  </acceptance_criteria>
  <done>gfx_spec.py fully covers GFX-01..04; the LCP budget and no-context-leak guarantees are asserted via the Windows-safe Playwright proxy.</done>
</task>

<task type="auto">
  <name>Task 2: (Optional) wire the lighthouse CLI LCP audit as a CI/secondary path</name>
  <read_first>
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § "Code Examples" → "Lighthouse LCP Audit (CI-friendly)" (the python http.server + npx lighthouse + node LCP-threshold assertion) and § Open Question 4 (Windows CLI reliability is UNPROVEN — Playwright proxy from Task 1 is the authoritative gate)
    - .planning/phases/16-3d-layer/16-VALIDATION.md — Wave 0 item: "lighthouse devDependency install ... if CLI audit is used"
    - web/package.json — current devDependencies (pin style)
  </read_first>
  <action>Add `lighthouse@13.3.0` as a dev dependency via `pnpm add -D lighthouse@13.3.0` inside `web/` ONLY (workspace isolation) and record a documented LCP-audit command in the SUMMARY (python http.server on out/ + `npx lighthouse http://127.0.0.1:PORT/ --output json --quiet --chrome-flags="--headless"` + a node assertion that `audits['largest-contentful-paint'].numericValue < 2500`). This is the OPTIONAL/CI path: if `lighthouse` fails to run on the Windows dev machine (RESEARCH.md Open Question 4), do NOT block — the Task-1 Playwright proxy is the authoritative GFX-04 gate. Note clearly in the SUMMARY whether lighthouse ran locally or is deferred to CI. If the maintainer prefers not to add the dev dependency, skip the install and document that GFX-04 is gated solely by the Playwright proxy (acceptable per RESEARCH.md).</action>
  <verify>
    <automated>cd web && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Either: `lighthouse@13.3.0` is in `web/package.json` devDependencies (pinned, no caret) and the documented audit command is recorded in the SUMMARY; OR the install is explicitly skipped with a SUMMARY note that the Playwright proxy is the sole GFX-04 gate.
    - `cd web && pnpm build` exits 0 (dev-dep install did not break the build).
    - `git status` shows no Go-module changes.
  </acceptance_criteria>
  <done>The lighthouse CLI LCP audit is available (or explicitly deferred to CI) without becoming a hard Windows-local gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer UAT — hive visual quality + drag interaction in both themes</name>
  <read_first>
    - .planning/phases/16-3d-layer/16-VALIDATION.md — § "Manual-Only Verifications" (the two subjective acceptances: hive visual/brand fit, drag-to-rotate feel)
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § Open Question 1 (theme-aware colors) + Open Question 2 (hive complexity vs bundle)
  </read_first>
  <what-built>The interactive R3F hive centerpiece now mounts over the static SVG on the home hero, behind dynamic(ssr:false), with theme-aware teal/amber materials, PresentationControls drag-to-rotate (snap + polar/azimuth limits), frameloop="demand", and a fail-closed capability gate. All automated GFX-01..04 assertions are green.</what-built>
  <how-to-verify>
    1. Build and serve: `cd web && pnpm build` then `python -m http.server 4300 --directory out` (PowerShell-safe), open http://127.0.0.1:4300/ in a real browser (WebGL available).
    2. Confirm the hive reads as a hive (honeycomb hexagonal cluster), not an abstract blob; colors match brand teal (#39c5cf dark / #0a6b75 light) and amber accents.
    3. Toggle the theme (header toggle): confirm the hive material recolors per theme — NO dark-only freeze (the Phase-15 dual-theme trap).
    4. Drag the centerpiece: confirm PresentationControls rotate/snap/limits feel right (no runaway spin, returns toward center).
    5. Set OS/browser `prefers-reduced-motion: reduce` (or DevTools rendering emulation) and reload: confirm the static SVG shows and NO canvas mounts; page fully usable.
    6. Tab through the hero with the keyboard: confirm focus order is unaffected by the canvas (canvas is aria-hidden, not focusable) and the "Read the docs" link + install chip are reachable.
  </how-to-verify>
  <acceptance_criteria>
    - Maintainer confirms hive visual/brand fit in BOTH themes.
    - Maintainer confirms drag-to-rotate feel and reduced-motion fallback.
    - Maintainer confirms keyboard navigation is unaffected.
    - Type "approved" to complete the phase, or describe issues to feed a gap-closure plan.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side navigation → canvas mount/unmount | Each route change can allocate/free a WebGL context; accumulation is the leak/DoS risk |
| npm registry → web/node_modules (dev) | Optional lighthouse dev dependency crosses the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Denial of Service | WebGL context accumulation across navigation | mitigate | R3F auto-disposes (forceContextLoss + dispose) on `<Canvas>` unmount; single-canvas rule; GFX-04 context-leak test asserts count never grows past 1 across a nav cycle |
| T-16-08 | Tampering | optional `lighthouse@13.3.0` dev dep | mitigate | Canonical Google Chrome project (legitimacy-gated in Plan 16-01 Task 1); pinned exact version; dev-only, workspace-isolated; install scoped to web/ |
| T-16-09 | Information disclosure | LCP/perf timing exposure | accept | Standard browser performance API timings; no sensitive data; acceptable for a marketing site |
</threat_model>

<verification>
- `cd web && pnpm build` exits 0.
- `cd web && python tests/gfx_spec.py` exits 0 with GFX-01..04 all implemented and green (LCP proxy < 2500ms; canvas count <= 1 across nav).
- `cd web && python tests/home_spec.py` exits 0.
- Maintainer UAT approved (hive visual + drag feel + reduced-motion fallback + keyboard nav in both themes).
- `git status` shows no Go-module changes.
</verification>

<success_criteria>
GFX-04 is proven: the home page meets the LCP < 2.5s budget with the SVG as the LCP element and leaks no WebGL contexts across navigation; the full GFX suite is green; and the maintainer has signed off on the subjective hive visual/interaction quality in both themes — completing Phase 16.
</success_criteria>

<output>
Create `.planning/phases/16-3d-layer/16-03-SUMMARY.md` when done.
</output>
