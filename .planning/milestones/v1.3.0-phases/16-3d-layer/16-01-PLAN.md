---
phase: 16-3d-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - pnpm-lock.yaml
  - web/public/hero-hive.svg
  - web/tests/gfx_spec.py
autonomous: false
requirements: [GFX-01, GFX-03]
user_setup: []

must_haves:
  truths:
    - "three / @react-three/fiber / @react-three/drei / @types/three are installed in web/node_modules at the pinned versions"
    - "web/public/hero-hive.svg exists and is a valid hexagonal hive SVG with explicit viewBox + width/height"
    - "web/tests/gfx_spec.py exists, serves out/ via Python http.server, and exits 0/1 with a failures[] summary"
    - "pnpm build still emits a non-empty out/index.html (static export intact after install)"
  artifacts:
    - path: "web/public/hero-hive.svg"
      provides: "Static SVG LCP fallback element (honeycomb hexagon cluster, teal+amber)"
      contains: "<polygon"
    - path: "web/tests/gfx_spec.py"
      provides: "Playwright GFX harness — build-clean grep + canvas-after-hydration scaffold"
      min_lines: 80
    - path: "web/package.json"
      provides: "Pinned 3D deps in dependencies"
      contains: "@react-three/fiber"
  key_links:
    - from: "web/package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm add writes exact pinned versions to the root workspace lockfile"
      pattern: "@react-three/fiber"
---

<objective>
Lay the Phase 16 foundation: install the pinned Three.js / R3F / drei stack inside `web/` (workspace-isolated), author the static `web/public/hero-hive.svg` LCP fallback, and create the `web/tests/gfx_spec.py` Playwright harness that downstream waves assert against. This closes the two VALIDATION.md Wave 0 gaps (`gfx_spec.py`, `hero-hive.svg`) so every later requirement-bearing task has an automated signal (Nyquist Dimension 8).

Purpose: No 3D code can be written or verified until (a) the packages exist, (b) the SVG fallback that is the LCP candidate exists for `pnpm build`, and (c) the test harness exists to prove GFX-01..04. This plan delivers all three with zero R3F component code.
Output: Pinned deps in web/package.json + root pnpm-lock.yaml; web/public/hero-hive.svg; web/tests/gfx_spec.py (build-clean + canvas-post-hydration assertions wired, later-wave assertions stubbed).
</objective>

<artifacts_produced>
## Artifacts this phase (16) produces

Enumerated here once for the whole phase; later plans reference these by name.

### New files
| File | Plan | Role |
|------|------|------|
| `web/public/hero-hive.svg` | 16-01 | Static honeycomb SVG — server-rendered LCP fallback element |
| `web/tests/gfx_spec.py` | 16-01 (created), 16-02 + 16-03 (extended) | Playwright harness covering GFX-01..04 (PORT 4200) |
| `web/components/home/hero-canvas-wrapper.tsx` | 16-02 | `"use client"` boundary; `dynamic(ssr:false)` loader |
| `web/components/home/hero-canvas.tsx` | 16-02 | R3F `<Canvas>` + capability gate + hive scene + sr-only sibling |

### Modified files
| File | Plan | Change |
|------|------|--------|
| `web/components/home/hero.tsx` | 16-02 | Add server-rendered SVG `<img>` LCP element + `<HeroCanvasWrapper />` slot (stays a Server Component) |
| `web/package.json` + `pnpm-lock.yaml` | 16-01 (+ optional 16-03) | Pinned 3D deps (+ optional `lighthouse` dev dep) |
| `web/next.config.mjs` | 16-01 | VERIFY ONLY — `transpilePackages` already lists all three; no edit |

### Exported symbols / components
| Symbol | File | Kind |
|--------|------|------|
| `HeroCanvasWrapper` | hero-canvas-wrapper.tsx | named export (client boundary component) |
| `HeroCanvas` (default) | hero-canvas.tsx | default export (R3F canvas component) |
| `useCanMount3D` | hero-canvas.tsx | capability-gate hook (reduced-motion + WebGL probe + saveData) |
| `HiveMesh`, `HexCell` | hero-canvas.tsx | internal R3F scene components (hexagonal honeycomb) |

### New dependencies
| Package | Version | Plan | Scope |
|---------|---------|------|-------|
| `three` | 0.184.0 | 16-01 | dependency |
| `@react-three/fiber` | 9.6.1 | 16-01 | dependency |
| `@react-three/drei` | 10.7.7 | 16-01 | dependency |
| `@types/three` | 0.184.1 | 16-01 | dependency (types) |
| `lighthouse` | 13.3.0 | 16-03 | devDependency (OPTIONAL / CI path) |
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-3d-layer/16-RESEARCH.md
@.planning/phases/16-3d-layer/16-PATTERNS.md
@.planning/phases/16-3d-layer/16-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for the 3D stack (blocking, not auto-approvable)</name>
  <read_first>
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § "Package Legitimacy Audit" (the five [ASSUMED OK] rows) and § "Standard Stack" → "Version verification (confirmed 2026-06-08)"
  </read_first>
  <what-built>Nothing yet — this is the pre-install legitimacy verification. RESEARCH.md ran a manual provenance check (slopcheck was blocked by Beekeeper's own sandbox) and tagged all five packages `[ASSUMED OK]`. Per the Package Legitimacy Gate, `[ASSUMED]` packages require a blocking human-verify checkpoint before install.</what-built>
  <how-to-verify>
    1. Open each registry page and confirm the package is the canonical one (correct GitHub source repo, healthy download counts, recent maintenance):
       - https://npmjs.com/package/three  → source github.com/mrdoob/three.js
       - https://npmjs.com/package/@react-three/fiber  → source github.com/pmndrs/react-three-fiber
       - https://npmjs.com/package/@react-three/drei  → source github.com/pmndrs/drei
       - https://npmjs.com/package/@types/three  → DefinitelyTyped
       - (optional, Plan 16-03) https://npmjs.com/package/lighthouse  → github.com/GoogleChrome/lighthouse
    2. Confirm the exact versions exist: three@0.184.0, @react-three/fiber@9.6.1, @react-three/drei@10.7.7, @types/three@0.184.1.
    3. Confirm none introduce an install-time postinstall script needing approval (RESEARCH.md states none do).
  </how-to-verify>
  <acceptance_criteria>
    - Maintainer types "approved" after confirming all four packages are the canonical pmndrs/mrdoob/DefinitelyTyped packages at the pinned versions.
    - If any package looks wrong/typosquatted, maintainer halts and the plan does not proceed to Task 2.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or name the suspect package to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the pinned 3D stack inside web/ (workspace-isolated)</name>
  <read_first>
    - web/package.json — current dependencies/devDependencies and packageManager pin (pnpm@11.1.3)
    - web/next.config.mjs — line 27 already declares transpilePackages: ["three", "@react-three/fiber", "@react-three/drei"] (VERIFY, do not re-add)
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § "Standard Stack" → "Installation" and "pnpm-workspace.yaml allowBuilds additions required" (none needed for these three)
    - CLAUDE.md — "web/ Node toolchain is isolated" + "pnpm workspace" + "Windows primary dev machine"
  </read_first>
  <action>Run `pnpm add three@0.184.0 @react-three/fiber@9.6.1 @react-three/drei@10.7.7 @types/three@0.184.1` from inside the `web/` directory only (workspace isolation — never the repo root, never the Go module). Use the exact pinned versions (no caret) so the root pnpm-lock.yaml records exact resolutions (supply-chain mitigation T-16-SC). None of these packages have install-time postinstall scripts, so no new `allowBuilds` / build-approval entry is required — if pnpm prompts for a build-script approval for any of these three, DENY it (fail-closed) and note it in the SUMMARY. After install, verify `web/next.config.mjs` transpilePackages already lists all three names and matches the installed package names exactly — do NOT duplicate or re-add the array. Confirm `go.mod`, `go.sum`, and any Go file are unchanged by `git status`.</action>
  <verify>
    <automated>cd web && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` `dependencies` contains `three`, `@react-three/fiber`, `@react-three/drei`, and `@types/three`, all at the exact pinned versions (no caret).
    - Root `pnpm-lock.yaml` resolves `@react-three/fiber@9.6.1`, `@react-three/drei@10.7.7`, `three@0.184.0`.
    - `cd web && pnpm build` exits 0 and emits a non-empty `web/out/index.html`.
    - `git status` shows no modification to `go.mod`, `go.sum`, or any `*.go` file.
  </acceptance_criteria>
  <done>The three R3F packages + @types/three are installed at pinned versions, the static build still passes, and the Go module is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Author the static hero-hive.svg LCP fallback</name>
  <read_first>
    - .planning/phases/16-3d-layer/16-PATTERNS.md — § "web/public/hero-hive.svg (static asset)" (constraints: viewBox + width/height 400×400, honeycomb of 7 hexagons, teal #39c5cf cells + amber #e3b341 accent nodes, behind aria-hidden so no internal a11y text needed)
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § "Code Examples" → "Hive Geometry" (the hexagonal-cluster visual reference) and § Pitfall 4 (explicit width/height/viewBox prevents layout recalculation that breaks LCP)
    - web/app/globals.css — Section 6a dark tokens (--teal: #39c5cf, --amber: #e3b341) for the SVG fill colors
  </read_first>
  <action>Create `web/public/hero-hive.svg` as a valid standalone SVG file (served via `<img src="/hero-hive.svg">` in a later wave — never `dangerouslySetInnerHTML`, so no script/foreignObject). Use `viewBox="0 0 400 400"` plus explicit `width="400" height="400"`. Draw a honeycomb cluster of 7 flat-topped hexagons (1 center + 6 surrounding) using `<polygon points="...">` elements. Fill the hex cells with the dark-theme brand teal `#39c5cf` (the canonical brand color; the SVG is the static fallback shown under reduced-motion/no-WebGL in either theme — a neutral brand color is acceptable per PATTERNS.md). Use amber `#e3b341` for 2–3 accent nodes/strokes to read as the "mediation" highlight. Keep it lightweight (no embedded raster, no external refs). This is the LCP candidate — it must be self-contained and paint instantly.</action>
  <verify>
    <automated>cd web && python -c "import xml.dom.minidom as m; d=m.parse('public/hero-hive.svg'); s=d.getElementsByTagName('svg')[0]; assert s.getAttribute('viewBox') and s.getAttribute('width') and s.getAttribute('height'); assert len(d.getElementsByTagName('polygon'))>=7; print('OK hive svg')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/public/hero-hive.svg` parses as valid XML (no parse error).
    - Root `<svg>` has non-empty `viewBox`, `width`, and `height` attributes.
    - Contains at least 7 `<polygon>` elements (honeycomb cluster).
    - Contains both the teal `#39c5cf` and amber `#e3b341` hex strings.
    - No `<script>`, `<foreignObject>`, or external `href`/`xlink:href` to a remote resource.
  </acceptance_criteria>
  <done>A self-contained 400×400 honeycomb SVG exists in public/, ready to be the server-rendered LCP element.</done>
</task>

<task type="auto">
  <name>Task 4: Create the gfx_spec.py Playwright harness (build-clean + canvas-post-hydration)</name>
  <read_first>
    - web/tests/home_spec.py — the exact analog: copy the http.server SilentHandler block (lines 24–64), the failures[]/fail()/ok()/run_tests()/__main__ exit-code scaffold (lines 94–250). gfx_spec.py mirrors it on PORT 4200 (home_spec uses 4199).
    - .planning/phases/16-3d-layer/16-PATTERNS.md — § "web/tests/gfx_spec.py (test, NEW)" (server-launch boilerplate, the GFX assertion table, the reduced_motion new_context pattern for a later wave)
    - .planning/phases/16-3d-layer/16-RESEARCH.md — § "Validation Architecture" → "Phase Requirements → Test Map" (the per-GFX automated commands)
  </read_first>
  <action>Create `web/tests/gfx_spec.py` mirroring `home_spec.py` structure exactly: the `OUT_DIR` guard, the `SilentHandler(directory=OUT_DIR)` http.server, `PORT = 4200`, the `failures[]` / `fail()` / `ok()` helpers, and the `if __name__ == "__main__"` exit-0/exit-1 summary. Implement the GFX-01 assertions now: (a) read `out/index.html` as a string and assert `"<canvas"` is NOT in it AND `"WebGLRenderingContext"` and `"@react-three"` are NOT in it (server HTML is clean — GFX-01); (b) `page.goto(base_url, wait_until="networkidle")` then `page.evaluate("document.querySelectorAll('canvas').length")` and assert the count is `<= 1` (canvas only appears after hydration; accept 0 when headless Chromium lacks WebGL — GFX-01/GFX-02). Stub the GFX-03 (reduced-motion + aria) and GFX-04 (LCP + context-leak) blocks as clearly-labeled `# TODO Wave 2/3` placeholder functions that currently print a visible "PENDING — implemented in Plan 16-02/16-03" line via ok() (so the harness runs green against the pre-3D build and the later waves fill them in — do NOT leave them as silent passes). Use `browser.new_context(reduced_motion="reduce")` documented in the PATTERNS.md excerpt as the shape the Wave-2 GFX-03 block will take. No watch-mode flags.</action>
  <verify>
    <automated>cd web && pnpm build && python tests/gfx_spec.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && python tests/gfx_spec.py` exits 0 against the current (pre-3D) build.
    - The script reads `out/index.html` and asserts absence of `<canvas`, `WebGLRenderingContext`, `@react-three` — and these are genuinely absent now (server HTML clean).
    - The script launches the Python http.server on port 4200 and drives `sync_playwright` chromium (no port collision with home_spec's 4199).
    - GFX-03 and GFX-04 blocks are present as labeled placeholders that print a visible "PENDING" line (not silent no-ops).
    - Running `python tests/home_spec.py` immediately after still exits 0 (no shared-port interference).
  </acceptance_criteria>
  <done>gfx_spec.py exists, exits 0 on the pre-3D build, enforces GFX-01 server-HTML cleanliness now, and reserves labeled slots for the Wave-2/3 assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → web/node_modules | Untrusted third-party 3D packages cross into the build at install time |
| public/hero-hive.svg → browser `<img>` | Static asset rendered to the DOM (later wave) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering | `pnpm add three/@react-three/* @types/three` | mitigate | Blocking human-verify legitimacy gate (Task 1, never auto-approved); exact pinned versions written to root pnpm-lock.yaml; canonical pmndrs/mrdoob/DefinitelyTyped sources confirmed; no install-time postinstall approved |
| T-16-01 | Tampering | `web/public/hero-hive.svg` | mitigate | Static `public/` file; later served via `<img>` only (never `dangerouslySetInnerHTML`); Task 3 acceptance forbids `<script>`/`<foreignObject>`/remote refs |
| T-16-02 | Denial of Service | install corrupting Go module | mitigate | Install scoped to `web/` only; Task 2 acceptance asserts go.mod/go.sum/*.go unchanged |
</threat_model>

<verification>
- `cd web && pnpm build` exits 0; `out/index.html` non-empty and free of `<canvas`/`@react-three`/`WebGLRenderingContext`.
- `cd web && python tests/gfx_spec.py` exits 0 (GFX-01 server-HTML clean; canvas count <= 1 post-hydration).
- `web/public/hero-hive.svg` parses, has viewBox+width+height, ≥7 polygons, teal+amber fills.
- `git status` shows no Go-module changes; pinned versions in package.json + pnpm-lock.yaml.
</verification>

<success_criteria>
The 3D toolchain is installed (pinned, workspace-isolated, legitimacy-gated), the static SVG LCP fallback exists, and the GFX Playwright harness exists and is green against the pre-3D build — so Plan 16-02 can implement R3F components with an assertion target already in place.
</success_criteria>

<output>
Create `.planning/phases/16-3d-layer/16-01-SUMMARY.md` when done.
</output>
