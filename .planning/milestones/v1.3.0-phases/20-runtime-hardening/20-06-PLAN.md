---
phase: 20-runtime-hardening
plan: 06
type: execute
wave: 4
depends_on: ["20-04", "20-05"]
files_modified:
  - internal/sentry/types.go
  - internal/sentry/rules.go
  - internal/sentry/rules_test.go
  - internal/sentry/linux/gen.go
  - internal/sentry/linux/bpf/dns_tracer.bpf.c
  - internal/sentry/linux/ebpf.go
  - internal/sentry/windows/parser.go
  - internal/sentry/windows/parser_test.go
  - internal/sentry/windows/daemon.go
autonomous: true
requirements: [SENT-11]
optional: true
must_haves:
  truths:
    - "STRETCH/OPTIONAL — droppable if Tier 3 scope fills. A new EventDNSQuery event kind carries QNAME for DNS-tunneling detection."
    - "Linux DNS ingestion: a kprobe on udp_sendmsg/tcp_sendmsg filtered to dport 53 parses the QNAME -> EventDNSQuery (new bpf2go target, embedded bytecode, never compiled at runtime)."
    - "Windows DNS ingestion: the Microsoft-Windows-DNS-Client ETW provider (event ID 3006) QueryName -> EventDNSQuery."
    - "macOS DNS is explicitly OUT (NetworkExtension, v2)."
  artifacts:
    - path: internal/sentry/types.go
      provides: "EventDNSQuery EventKind"
      contains: "EventDNSQuery"
    - path: internal/sentry/linux/bpf/dns_tracer.bpf.c
      provides: "kprobe udp_sendmsg/tcp_sendmsg dport-53 QNAME parse"
      contains: "udp_sendmsg"
    - path: internal/sentry/windows/parser.go
      provides: "DNS-Client provider ID 3006 -> EventDNSQuery"
      contains: "3006"
  key_links:
    - from: internal/sentry/linux/gen.go
      to: dns_tracer.bpf.c
      via: "new bpf2go target (CI-generated bytecode, never at runtime)"
      pattern: "bpf2go"
    - from: internal/sentry/windows/daemon.go
      to: Microsoft-Windows-DNS-Client provider
      via: "etw.EnableProvider on the existing session"
      pattern: "EnableProvider"
---

<objective>
Tier 3 W4 — DNS ingestion (STRETCH, OPTIONAL). This plan is explicitly DROPPABLE if
Tier 3 scope fills (blueprint W4). It adds an `EventDNSQuery` event kind and DNS
QNAME ingestion on Linux (eBPF kprobe) and Windows (ETW DNS-Client), closing the
DNS-TXT exfiltration gap (analysis F1) that TCP-connect ingestion cannot see. macOS
DNS stays OUT (NetworkExtension, v2).

Purpose: DNS-tunneling is a real 2026 exfil channel (analysis F1) invisible to the
current TCP-connect-only network source. This is the cheapest of the remaining
new-event-source gaps (analysis §4 priority order).

Output: EventDNSQuery kind; Linux kprobe DNS bpf2go target (embedded bytecode);
Windows DNS-Client ETW ingestion; synthetic tests. macOS DNS explicitly deferred.

NOTE TO EXECUTOR: If the orchestrator or maintainer signals Tier 3 scope is full,
SKIP this plan entirely — it is marked `optional: true` and gated behind 20-04/20-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-runtime-hardening/20-BLUEPRINT.md
@.planning/phases/20-runtime-hardening/20-RESEARCH.md
@analysis/sentry-coverage-2026-06.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `sentry.EventDNSQuery EventKind` appended to the iota (after EventFileWrite) — carries the QNAME in FilePath/a QName field.
- Linux: new `bpf/dns_tracer.bpf.c` + a 3rd bpf2go target in gen.go (`BeekeeperDNS`) + generated `bpf_beekeeper_dns_bpfel.go` (CI-generated, NEVER committed, NEVER compiled at runtime — per CLAUDE.md eBPF constraint) + ebpf.go reader emitting EventDNSQuery.
- Windows: DNS-Client provider GUID `{1C95126E-7EEA-49A9-A3FE-A378B03DDB4D}` enabled on the existing ETW session + parser case for event ID 3006 QueryName -> EventDNSQuery.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventDNSQuery kind + Linux kprobe DNS bpf2go target (build-check + synthetic test)</name>
  <files>internal/sentry/types.go, internal/sentry/rules.go, internal/sentry/rules_test.go, internal/sentry/linux/gen.go, internal/sentry/linux/bpf/dns_tracer.bpf.c, internal/sentry/linux/ebpf.go</files>
  <read_first>
    - internal/sentry/types.go — the EventKind iota (now incl. EventFileWrite from plan 20-04). Append EventDNSQuery.
    - internal/sentry/linux/gen.go (:5-6) — the existing two `go:generate go tool bpf2go ... BeekeeperExec / BeekeeperNet` directives + the `-I./bpf/headers` include. Add a 3rd `BeekeeperDNS` target for dns_tracer.bpf.c.
    - internal/sentry/linux/bpf/network_tracer.bpf.c — the existing kprobe C source to model dns_tracer.bpf.c on (CO-RE, ringbuf, the NetworkEvent struct shape).
    - internal/sentry/linux/bpf_beekeeper_net_bpfel.go (:40) — the loader-stub fail-closed pattern ("run go generate on Linux" error when bytecode absent). The DNS generated file must follow the same fail-closed stub discipline (CLAUDE.md: loader stubs fail closed when bytecode absent; bytecode is CI-generated, never committed, never compiled at runtime).
    - internal/sentry/linux/ebpf.go (:57 StartEBPFReaders) — where readers attach + emit events.
    - RESEARCH Tier-3 DNS: Linux kprobe `udp_sendmsg`/`tcp_sendmsg` filtered to dport 53 + in-kernel QNAME parse -> EventDNSQuery (uprobe getaddrinfo REJECTED — Go bypasses libc + 10-20x overhead).
  </read_first>
  <behavior>
    - EventDNSQuery is a distinct kind carrying the queried domain (QNAME).
    - The dns_tracer.bpf.c kprobe filters to destination port 53 and extracts the QNAME from the DNS payload.
    - The loader stub for the DNS program fails CLOSED (clear error) when bytecode is absent (Windows/local builds), matching the existing net/exec stubs — never compiles at runtime.
    - A synthetic EventDNSQuery flows through EvaluateEvent without panicking (a rule consuming it can be a no-op for now or feed a future DNS-exfil rule — keep it minimal; the rule layer stays pure).
  </behavior>
  <action>
    Append `EventDNSQuery` to the EventKind iota in types.go (after EventFileWrite). Add `bpf/dns_tracer.bpf.c` modeled on network_tracer.bpf.c: a kprobe on `udp_sendmsg` and `tcp_sendmsg` filtered to dport 53 that parses the QNAME from the DNS query payload and pushes a DNS event over the ringbuf. Add a 3rd bpf2go directive (`BeekeeperDNS`) to gen.go mirroring the existing two (same `-I./bpf/headers`). Add the generated-loader expectations + a fail-closed stub matching bpf_beekeeper_net_bpfel.go (the real bytecode is CI-generated, NEVER committed, NEVER compiled at runtime — CLAUDE.md eBPF constraint). Wire ebpf.go (StartEBPFReaders) to attach the DNS reader and emit `sentry.EventDNSQuery`. Add a minimal synthetic rules_test asserting an EventDNSQuery passes through EvaluateEvent cleanly. This is build-check-only locally; real eBPF capture is CI-validated (Ubuntu kernel matrix). Do NOT compile bytecode at runtime; do NOT commit generated bytecode.
  </action>
  <verify>
    <automated>go build ./... && GOOS=linux go build ./internal/sentry/linux/ && go test ./internal/sentry/ -run "DNSQuery" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "EventDNSQuery" internal/sentry/types.go` returns a match in the const block.
    - SOURCE: `grep -n "BeekeeperDNS" internal/sentry/linux/gen.go` returns a match; `internal/sentry/linux/bpf/dns_tracer.bpf.c` exists and contains `udp_sendmsg`.
    - SOURCE: the DNS loader stub fails closed when bytecode is absent (mirrors bpf_beekeeper_net_bpfel.go:40); no runtime compilation; no committed bytecode (`*_bpfel.go` real bytecode is .gitignore'd / CI-only per existing convention).
    - BUILD: `go build ./...` and `GOOS=linux go build ./internal/sentry/linux/` exit 0.
    - TEST: `go test ./internal/sentry/ -run "DNSQuery" -count=1` exits 0.
    - NOTE (CI-gated): real kprobe DNS capture validated in the Ubuntu eBPF CI matrix.
  </acceptance_criteria>
  <done>EventDNSQuery exists; dns_tracer.bpf.c + BeekeeperDNS bpf2go target + fail-closed loader stub added; ebpf reader emits EventDNSQuery; synthetic test + Linux build green; no runtime/committed bytecode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Windows DNS-Client ETW ingestion (provider 3006 QueryName -> EventDNSQuery)</name>
  <files>internal/sentry/windows/parser.go, internal/sentry/windows/parser_test.go, internal/sentry/windows/daemon.go</files>
  <read_first>
    - internal/sentry/windows/daemon.go (:146-159) — the provider-enable loop: `etw.MustParseGUID(p.guid)` -> `etw.Provider{...}` -> `sess.EnableProvider(prov)` with an access-denied-continue fallback. Add the DNS-Client provider to the provider list it iterates.
    - internal/sentry/windows/parser.go — `guidKernelNetwork` (:20) + the `case guidKernelNetwork:` branch (:150). Add a `guidDNSClient` const + a parser case for event ID 3006 reading `QueryName` from EventData -> EventDNSQuery.
    - internal/sentry/windows/parser_test.go — the ETW summary parse test style.
    - RESEARCH Tier-3 DNS: Windows ETW `Microsoft-Windows-DNS-Client` `{1C95126E-7EEA-49A9-A3FE-A378B03DDB4D}` ID 3006 `QueryName`. RESEARCH open-q: confirm tekert/golang-etw can enable a MANIFEST provider (DNS-Client) on the existing session — record the result in SUMMARY.
  </read_first>
  <behavior>
    - The DNS-Client provider GUID is added to the ETW session's enabled providers (with the same access-denied-continue fallback as the existing providers).
    - A DNS-Client event ID 3006 with a QueryName parses to sentry.EventDNSQuery carrying the queried domain.
    - Non-3006 DNS-Client events are ignored (ErrUnknownEvent), like the other providers' non-matching IDs.
  </behavior>
  <action>
    Add a `guidDNSClient = "1c95126e-7eea-49a9-a3fe-a378b03ddb4d"` const (normalized to the codebase's GUID format) to parser.go and a parser case for the DNS-Client provider event ID 3006 reading `QueryName` from EventData -> `sentry.EventDNSQuery`. Register the DNS-Client provider in the daemon.go provider list so `EnableProvider` enables it on the existing session (RESEARCH open-q: verify golang-etw can enable a manifest provider on the running session — if it cannot, document the limitation + fail gracefully like the access-denied path; do NOT crash). Add parser tests for ID 3006 -> EventDNSQuery and a non-3006 ID -> ErrUnknownEvent. Record the verified field key + manifest-provider-enable result in the SUMMARY (CLAUDE.md ETW research flag).
  </action>
  <verify>
    <automated>GOOS=windows go build ./internal/sentry/windows/ && go test ./internal/sentry/windows/ -run "Parse|DNS" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "3006" internal/sentry/windows/parser.go` returns a match mapping to EventDNSQuery; `grep -ni "1c95126e" internal/sentry/windows/parser.go` returns the DNS-Client GUID.
    - SOURCE: daemon.go's provider list includes the DNS-Client provider (verify by reading the provider slice).
    - BEHAVIOR: a parser test asserts ID 3006 with QueryName -> EventDNSQuery and a non-3006 ID -> ErrUnknownEvent.
    - BUILD: `GOOS=windows go build ./internal/sentry/windows/` exits 0 (native on the dev machine).
    - TEST: `go test ./internal/sentry/windows/ -run "Parse|DNS" -count=1` exits 0.
    - NOTE (CI-gated): real DNS-Client capture validated on windows-latest CI.
  </acceptance_criteria>
  <done>DNS-Client provider enabled + ID 3006 QueryName parsed to EventDNSQuery; non-3006 ignored; windows build + parser tests green; manifest-provider-enable result recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DNS query payload -> Sentry rules | Untrusted QNAMEs (possible TXT-tunnel exfil) crossing into detection. |
| kernel/ETW DNS source -> EventDNSQuery | New event source normalized into the engine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-18 | Info Disclosure | DNS-TXT tunneling exfil invisible to TCP-connect ingestion | mitigate | EventDNSQuery via Linux kprobe + Windows DNS-Client closes analysis F1 (Linux/Windows; macOS deferred to v2). |
| T-20-19 | Tampering (supply chain) | runtime-compiled or committed eBPF bytecode | mitigate | Bytecode CI-generated only, never committed, never compiled at runtime; loader stub fails closed when absent (CLAUDE.md eBPF constraint). |
| T-20-20 | Availability | golang-etw cannot enable a manifest provider on the live session | accept (documented) | Fail gracefully (access-denied-continue pattern); document the limitation in SUMMARY; do not crash the daemon. |
</threat_model>

<verification>
- `go build ./...` + `GOOS=linux|windows go build ./internal/sentry/...` exit 0.
- `go test ./internal/sentry/... -count=1` and `go vet ./internal/sentry/...` exit 0.
- No eBPF bytecode committed; no runtime compilation; loader stubs fail closed.
- macOS DNS explicitly OUT (recorded, not built).
</verification>

<success_criteria>
- SENT-11 (DNS stretch) satisfied IF executed: EventDNSQuery + Linux kprobe DNS bpf2go target + Windows DNS-Client ingestion; macOS DNS deferred.
- This plan is OPTIONAL — dropping it does not block phase completion (the ROADMAP SCs are satisfied by plans 20-01..20-05; DNS is the explicit stretch).
</success_criteria>

<output>
Create `.planning/phases/20-runtime-hardening/20-06-SUMMARY.md` when done (or a one-line SKIPPED note if Tier 3 scope filled and the stretch was dropped).
</output>
