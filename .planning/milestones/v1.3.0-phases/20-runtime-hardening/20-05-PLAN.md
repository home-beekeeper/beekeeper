---
phase: 20-runtime-hardening
plan: 05
type: execute
wave: 3
depends_on: ["20-03", "20-04"]
files_modified:
  - .planning/PROJECT.md
  - docs/THREAT-MODEL.md
  - web/components/home/how-it-works.tsx
  - web/components/home/feature-cards.tsx
  - web/components/home/honesty-callout.tsx
  - internal/sentry/rules_test.go
autonomous: true
requirements: [SENT-09, SENT-10]
must_haves:
  truths:
    - "PROJECT.md and THREAT-MODEL.md §8 no longer overstate Sentry — they state editor+agent scope, detection-only, and the no-DNS/no-memory/no-write(pre-008) gaps, and remove the now-false SENTRY-004/005-don't-fire and fanotify-no-drops claims."
    - "The marketing home no longer implies Sentry catches 'novel campaigns the catalog has never seen' / 'fires regardless of catalog knowledge' without the editor-extension + detection-only caveat, and the honesty callout lists the Sentry gap."
    - "Synthetic rules_test cases cover SENTRY-006, SENTRY-007, SENTRY-008, and the watchlist expansion; go test ./internal/sentry/... + go vet are green."
  artifacts:
    - path: docs/THREAT-MODEL.md
      provides: "§8 updated per analysis §6.3 (stale-fix + 5 honest gaps)"
      contains: "detection-only"
    - path: web/components/home/honesty-callout.tsx
      provides: "added Sentry-scope gap entry"
      contains: "Sentry"
    - path: internal/sentry/rules_test.go
      provides: "synthetic 006/007/008 + watchlist test cases"
      contains: "SENTRY-008"
  key_links:
    - from: web/components/home/how-it-works.tsx
      to: "honest Sentry framing"
      via: "editor-extension-scoped + before-catalog wording (analysis §6.4)"
      pattern: "editor"
    - from: docs/THREAT-MODEL.md §8
      to: "code reality"
      via: "remove SENTRY-004/005-don't-fire + fanotify-no-drops staleness"
      pattern: "InventoryStore"
---

<objective>
Tier 3 W3 — honesty edits + synthetic rule tests (SENT). Make the docs and home
page match what the Sentry engine actually does. The analysis (§6) found PROJECT.md
and THREAT-MODEL.md §8 both stale (they still say SENTRY-004/005 "do not fire" and
"fanotify does not count drops" — both now false in code) AND the home page
overstates Sentry ("catches novel campaigns the catalog has never seen", "fires
regardless of catalog knowledge") without the editor-extension + detection-only
caveat. This plan applies the §6 before/after diffs and adds synthetic test cases
for the new rules from plans 20-03/20-04.

Purpose: a security product's honesty is part of its threat model — overstated
detection lulls users into false confidence. This closes the documentation gap.

Output: PROJECT.md + THREAT-MODEL.md §8 corrections; how-it-works/feature-cards/
honesty-callout honesty edits; synthetic rules_test cases for 006/007/008 + watchlist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-runtime-hardening/20-BLUEPRINT.md
@analysis/sentry-coverage-2026-06.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
No new exported symbols. This plan updates documentation/marketing text and adds
test cases only. The rule symbols (SENTRY-006/007/008, isMonitoredDescendant,
isExternalDest, isPersistencePath, EventFileWrite) are produced by plans 20-03/20-04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: PROJECT.md + THREAT-MODEL.md §8 honesty corrections (apply analysis §6.1-6.3)</name>
  <files>.planning/PROJECT.md, docs/THREAT-MODEL.md</files>
  <read_first>
    - analysis/sentry-coverage-2026-06.md §6.1 (PROJECT.md `#### Sentry Daemon` ~line 115 diff: SENTRY-001..005 each editor-descendant-gated, detection-only, editor-extension-trojan scope, agents/CI NOT seen in v1), §6.2 (PROJECT.md `### Out of Scope` ~line 152: add 4 bullets — non-editor contexts, persistence-write detection, DNS/memory, legit-endpoint exfil), §6.3 (THREAT-MODEL.md §8 ~line 658: replace the stale SENTRY-004/005-don't-fire + fanotify-no-drops text with the 5 honest gaps).
    - .planning/PROJECT.md `#### Sentry Daemon` section + `### Out of Scope` section (find current text).
    - docs/THREAT-MODEL.md §8 `#### Detection-Completeness Gaps in the Behavioral Sentry`.
    - analysis §0 stale-doc correction: all three daemons build a live InventoryStore + pass Snapshot(now); fanotify counts drops (EventsDropped) — so the "don't fire" / "no drops" claims are NOW FALSE and must be removed.
    - NOTE: plans 20-03/20-04 add SENTRY-006/007/008 + agent scope + file-write — reflect those as NOW-LANDING (not future) where appropriate, but keep the residual gaps (DNS/memory/legit-endpoint) honest as still-open.
  </read_first>
  <action>
    Apply the analysis §6.1 diff to PROJECT.md `#### Sentry Daemon`: name SENTRY-001..005, state editor-descendant ancestry, mark detection-only (audit records, no quarantine/kill — extension quarantine lives in the watch/scan layer), and scope it to the editor-extension-trojan family. Update to note that THIS phase adds SENTRY-006/007/008 + agent ancestry + file-write ingestion (so v1.x agent/persistence coverage now exists), while keeping the §6.2 Out-of-Scope bullets that remain true (DNS-tunneling, process-memory-scrape, exfil over legitimate endpoints). Apply the §6.3 diff to THREAT-MODEL.md §8: REMOVE the stale "SENTRY-004/005 do not fire in production" and "fanotify does not count drops" claims (both false per analysis §0); add the honest gaps that remain after this phase (residual: DNS, process-memory, legit-endpoint exfil; and the Windows file/network no-parent-PID attribution weakness). Keep the edits faithful to the §6 diffs and to the code reality post-20-03/20-04. Do not introduce new claims the code doesn't back.
  </action>
  <verify>
    <automated>git diff --name-only -- .planning/PROJECT.md docs/THREAT-MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "SENTRY-004/005" docs/THREAT-MODEL.md` shows the stale "do not fire" phrasing is gone (the §8 text no longer says those rules don't fire); `grep -n "InventoryStore" docs/THREAT-MODEL.md` reflects the corrected reality (they DO fire).
    - SOURCE: `grep -n "detection-only\|detection only" docs/THREAT-MODEL.md` returns a match in §8.
    - SOURCE: PROJECT.md `### Out of Scope` contains the residual-gap bullets (DNS/memory/legit-endpoint) — verify by reading the section.
    - BEHAVIOR: the §8 text matches analysis §6.3 intent (no overstatement; residual gaps stated) — confirmed by reading the diff.
  </acceptance_criteria>
  <done>PROJECT.md Sentry Daemon + Out-of-Scope and THREAT-MODEL §8 corrected: stale don't-fire/no-drops claims removed; editor+agent scope + detection-only + residual gaps stated.</done>
</task>

<task type="auto">
  <name>Task 2: Home honesty edits (how-it-works / feature-cards / honesty-callout) — apply analysis §6.4</name>
  <files>web/components/home/how-it-works.tsx, web/components/home/feature-cards.tsx, web/components/home/honesty-callout.tsx</files>
  <read_first>
    - analysis/sentry-coverage-2026-06.md §6.4 table: how-it-works.tsx (~:177-178 "catching novel campaigns the catalog has never seen" -> editor-extension-host scope + "before the catalog has flagged them"), feature-cards.tsx (~:52-54 "Fires regardless of catalog knowledge" -> editor-extension activity + "Catalog-independent, detection-only (alerts/audits; does not auto-contain)"), honesty-callout.tsx (omission: ADD a Sentry gap line — "editor-extension-scoped and detection-only — does not watch standalone-terminal agents, CI runners, persistence writes, DNS, or process memory").
    - web/components/home/how-it-works.tsx around line 177; web/components/home/feature-cards.tsx around line 52; web/components/home/honesty-callout.tsx (the existing gap-list structure to append to).
    - NOTE on hero.tsx: analysis §6.4 also flags hero.tsx, but the prior Phase-17 maintainer hero edits already reshaped it — read hero.tsx and only adjust if it still makes the blanket "cannot act without permission" Sentry claim; otherwise leave hero alone (record the decision in SUMMARY). The dual-theme tokens must stay `var(--amber)`/`var(--teal)` raw tokens, NOT `--color-bk-*` (MEMORY dual-theme lesson).
  </read_first>
  <action>
    Apply the analysis §6.4 honest-wording edits: how-it-works.tsx — scope the correlation claim to editor-extension hosts and "before the catalog has flagged them" (drop "novel campaigns the catalog has never seen"). feature-cards.tsx — scope to editor-extension activity + "Catalog-independent, detection-only (alerts/audits; does not auto-contain)" (drop "Fires regardless of catalog knowledge"). honesty-callout.tsx — append a Sentry-scope gap entry matching the existing callout structure (editor-extension-scoped + detection-only; no standalone-terminal agents/CI/persistence/DNS/memory). Note: plans 20-03/20-04 ADD agent + persistence coverage, so phrase the gap to reflect what remains scoped/detection-only (DNS, process memory, legit-endpoint exfil; detection-only enforcement) rather than claiming agents are entirely unseen. Keep all color tokens as raw theme tokens (var(--amber)/var(--teal)) for dual-theme correctness. Run pnpm build to confirm the JSX compiles (pnpm required per execution-environment notes).
  </action>
  <verify>
    <automated>cd web; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "novel campaigns the catalog has never seen" web/components/home/how-it-works.tsx` == 0; `grep -c "regardless of catalog knowledge" web/components/home/feature-cards.tsx` == 0 (the overstatements are removed).
    - SOURCE: `grep -ci "detection-only\|detection only" web/components/home/feature-cards.tsx` >= 1; `grep -ci "Sentry" web/components/home/honesty-callout.tsx` >= 1 (the gap entry exists).
    - SOURCE: no `--color-bk-` token introduced in the edited components (`grep -c "color-bk-" web/components/home/honesty-callout.tsx` unchanged from baseline; new accents use var(--amber)/var(--teal)).
    - BEHAVIOR: `web/` `pnpm build` exits 0.
  </acceptance_criteria>
  <done>how-it-works + feature-cards no longer overstate Sentry; honesty-callout lists the Sentry scope/detection-only gap; dual-theme tokens preserved; pnpm build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synthetic rules_test cases for SENTRY-006/007/008 + watchlist; full sentry suite + web specs gate</name>
  <files>internal/sentry/rules_test.go</files>
  <read_first>
    - internal/sentry/rules.go (post-20-03/20-04): evalSENTRY006, evalSENTRY007, evalSENTRY008, isMonitoredDescendant, isExternalDest, isPersistencePath, the expanded defaultSensitivePaths, EventFileWrite dispatch.
    - internal/sentry/rules_test.go — the existing synthetic-event test harness (how it builds a ProcessNode tree + SentryEvent + RuleState + calls EvaluateEvent and asserts on returned alerts).
    - blueprint T3.10: synthetic rules_test cases for 006/007/008 + watchlist; `go test ./internal/sentry/...` + `go vet`.
    - web/tests/seo_spec.py, web/tests/home_spec.py, web/tests/gfx_spec.py, web/tests/accuracy_spec.py — the existing pure-Python web spec harnesses gated by the honesty edits (Task 2).
  </read_first>
  <behavior>
    - SENTRY-006: a synthetic agent-descended PID reading 2 sensitive paths in 60s fires SENTRY-006; an editor integrated terminal does not double-fire.
    - SENTRY-007: a synthetic monitored-descendant cred-read followed by an external outbound fires SENTRY-007; loopback/RFC1918 does not.
    - SENTRY-008: a synthetic monitored-descendant EventFileWrite to a persistence path fires SENTRY-008; a benign path does not.
    - Watchlist: a 2-cloud-cred read (.aws + .config/gcloud) fires SENTRY-001.
  </behavior>
  <action>
    Add synthetic-event table-driven test cases to rules_test.go covering SENTRY-006 (agent-bare-terminal 2-read + integrated-terminal no-double-fire), SENTRY-007 (cred-read+external-outbound fire; loopback/RFC1918 no-fire; warn-first in baseline), SENTRY-008 (persistence-write fire; benign-path no-fire; non-monitored-writer no-fire), and the watchlist expansion (2-cloud-cred -> SENTRY-001). These consolidate / complement the per-task tests from plans 20-03/20-04 into one suite-level proof. Then run the full sentry suite + go vet, and the web specs (Task 2 gate) to confirm the honesty edits don't regress the web build. No fenced code — follow the existing rules_test harness.
  </action>
  <verify>
    <automated>go test ./internal/sentry/... -count=1 && go vet ./internal/sentry/...</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "SENTRY-006" internal/sentry/rules_test.go` >= 1 AND `grep -c "SENTRY-007" internal/sentry/rules_test.go` >= 1 AND `grep -c "SENTRY-008" internal/sentry/rules_test.go` >= 1.
    - BEHAVIOR: the test cases assert the fire/no-fire behaviors in <behavior> (incl. integrated-terminal no-double-fire and loopback/RFC1918 no-fire).
    - TEST: `go test ./internal/sentry/... -count=1` and `go vet ./internal/sentry/...` exit 0.
    - WEB: `web/` `pnpm build` exits 0 and `python web/tests/seo_spec.py`, `home_spec.py`, `gfx_spec.py`, `accuracy_spec.py` all exit 0 (honesty edits don't regress the web gates; pnpm/python required per execution-environment notes).
  </acceptance_criteria>
  <done>Synthetic 006/007/008 + watchlist cases exist and pass; full sentry suite + go vet green; web build + all four web specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation/marketing claims -> user trust | Overstated detection claims cross into user risk assessment (false confidence). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-16 | Repudiation/honesty | home + threat-model overstating Sentry coverage | mitigate | Apply analysis §6 diffs — editor+agent scope, detection-only, residual gaps stated; remove the stale don't-fire/no-drops claims. This is the detection-only honesty self-defense for Tier 3. |
| T-20-17 | Tampering (test gap) | new rules shipping untested | mitigate | Synthetic 006/007/008 + watchlist test cases lock the behaviors; full suite + go vet gate. |
</threat_model>

<verification>
- `go test ./internal/sentry/... -count=1` + `go vet ./internal/sentry/...` exit 0.
- `web/` `pnpm build` exits 0; seo_spec / home_spec / gfx_spec / accuracy_spec all exit 0.
- PROJECT.md + THREAT-MODEL §8 + home no longer overstate Sentry/LlamaFirewall (cross-checked against analysis §6 and code reality).
</verification>

<success_criteria>
- SENT-09..11 satisfied: PROJECT/THREAT-MODEL honesty corrections; home honesty edits; synthetic 006/007/008 + watchlist tests.
- ROADMAP Phase-20 SC-4 holds: THREAT-MODEL §8 + home no longer overstate Sentry/LlamaFirewall; go test + go vet + TestRulesImportsArePure green; pnpm build + web specs green.
</success_criteria>

<output>
Create `.planning/phases/20-runtime-hardening/20-05-SUMMARY.md` when done.
</output>
