# Phase 18.1 Summary — Docs Theme Restyle (DSYS-05)

**Mode:** Quick task (inserted phase, executed inline — NOT full plan/UI-SPEC/verify ceremony, per maintainer scope)
**Status:** ✅ Complete & maintainer-approved · **Date:** 2026-06-09
**Requirement:** DSYS-05

## What was delivered

Restyled the stock Fumadocs docs chrome to the Beekeeper (Phase-12) brand, fixing the three maintainer gripes from the Phase-17 review. Diagnosed with the installed Python Playwright (dark-mode screenshots + computed-style dumps), fixed, and re-verified.

| Gripe | Root cause | Fix |
|-------|-----------|-----|
| White sidebar/body line (dark) | Tailwind v4 preflight resets border-color to `currentColor` (bright `--fg`); Fumadocs' bare `border-e` sidebar divider inherited it | `aside.bg-fd-card { border-inline-end-color: var(--border) !important }` (biome-ignore). Playwright re-probe: **0 visible white borders** |
| "Basic — no color/accents" | shadcn slots are `@theme inline` (not exposed as CSS vars) → Fumadocs' `--color-fd-*` bridge resolved EMPTY (transparent sidebar, unset primary) | Bound `--color-fd-*` directly to the raw theme-switched tokens (`--bg/--surface/--fg/--teal/--border`) in globals.css §9; `--color-fd-primary: var(--teal)` → teal active nav/links/TOC |
| Sidebar name-duplication | each section = `folder/index.mdx` + same-title `meta.json` → group label + identical indented child | Flattened all 8 to `content/docs/<section>.mdx`, removed per-section meta.json (routes + Orama index unchanged) |

Plus an amber left-accent + teal tint on the active sidebar item (globals.css §9c).

## Verification

- `pnpm build` exit 0; `accuracy_spec` + `seo_spec` + `home_spec` + `gfx_spec` all exit 0 (no regression).
- Playwright: 0 visible white-border elements; dual-theme (dark + light) confirmed; marketing home unaffected.
- Biome clean (the single scoped `!important` carries a biome-ignore).
- Maintainer reviewed the served build and **approved**.

## Decisions

- **VitePress declined** — this is Next.js 16/React; Fumadocs is the in-stack equivalent. Re-platforming to Vue/Vite is a milestone decision, not a restyle (would fork the stack, drop shared tokens/components/SEO, blow up Phase 19 CI).
- Executed as a **quick task** (no UI-SPEC/plan/ui-review) per maintainer scope.

## Deferred (captured)

- One-command-per-card copy for multi-command blocks → `.planning/todos/pending/docs-command-card-copy.md` (maintainer-flagged; closed 18.1 on the visual gripes per "mark complete to move on").

## Commits

- `01302fd` insert phase · `aa57d74` flatten (duplication) · `eec08c8` theming (borders/accents) · `73bff9f` tracking
