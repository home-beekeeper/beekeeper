---
phase: 21-full-system-validation-ci-calibration
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - internal/sentry/fuzz_test.go
  - .github/workflows/ci.yml
autonomous: true
requirements: [VAL-03, VAL-04]
must_haves:
  truths:
    - "FuzzEvaluateEvent fuzzes the pure sentry.EvaluateEvent entry point: it never panics and only ever returns critical/high severities"
    - "A blocking fuzz-sentry CI job exists and is in release-gate.needs (a discovered panic blocks release)"
    - "The ci.yml matrix gains a 3xGOOS cross-compile build step (build-only)"
    - "The matrix runs the new ipc Tier-B tests on the Linux/macOS legs (peer-cred validation)"
    - "web.yml and release.yml triggers are NOT touched; ci.yml paths-ignore web isolation is preserved"
  artifacts:
    - path: "internal/sentry/fuzz_test.go"
      provides: "FuzzEvaluateEvent (//go:build fuzz) — Sentry rule evaluator fuzz target"
      contains: "FuzzEvaluateEvent"
    - path: ".github/workflows/ci.yml"
      provides: "3xGOOS cross-build step + fuzz-sentry job + release-gate.needs gains fuzz-sentry"
      contains: "fuzz-sentry"
  key_links:
    - from: "internal/sentry/fuzz_test.go"
      to: "sentry.EvaluateEvent"
      via: "fuzz-driven SentryEvent into the pure evaluator"
      pattern: "EvaluateEvent"
    - from: ".github/workflows/ci.yml release-gate.needs"
      to: "fuzz-sentry job"
      via: "blocking release gate dependency"
      pattern: "fuzz-sentry"
---

<objective>
Add the VAL-04 Sentry-rule-evaluator fuzz target (the only missing fuzz surface) and wire it as a blocking CI release gate; extend the VAL-03 CI matrix with the delta that is genuinely new (3×GOOS cross-compile build, the new ipc Tier-B tests on the Unix legs) without re-adding the matrix jobs that already exist. The fuzz target fuzzes the pure `sentry.EvaluateEvent` entry point — it must never panic and only ever return `critical`/`high` severities. The `fuzz-sentry` job is added to `release-gate.needs` so a discovered panic blocks release (VAL-08). The matrix `test` job gains one `for os in linux darwin windows` cross-build step. `web.yml`/`release.yml` triggers are untouched; the `ci.yml` web `paths-ignore` isolation is preserved (Pitfall 4).

Purpose: Close the last fuzz gap (D-06: 7 targets exist, Sentry is the 8th) and finalize the cross-platform matrix that validates everything platform-bound (D-05). The eBPF/eslogger/ETW/`-race`/2-kernel jobs ALREADY exist in ci.yml — this plan ADDS only the delta, it does not re-author them.
Output: `internal/sentry/fuzz_test.go` + targeted `ci.yml` edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-full-system-validation-ci-calibration/21-CONTEXT.md
@.planning/phases/21-full-system-validation-ci-calibration/21-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FuzzEvaluateEvent — Sentry rule evaluator fuzz target (VAL-04)</name>
  <files>internal/sentry/fuzz_test.go</files>
  <read_first>
    - internal/sentry/rules.go (EvaluateEvent signature at line 345: event, *RuleState, tree map[uint32]ProcessNode, InventorySnapshot, RuleConfig, BaselineState, now time.Time → []SentryAlert; pure, no I/O)
    - internal/sentry/types.go (SentryEvent / EventKind iota / ProcessNode / RuleState / NewRuleState / RuleConfig / InventorySnapshot / SentryAlert.Severity = "critical"|"high")
    - internal/sentry/baseline.go (BaselineState — zero value DurationDays==0 → IsBaselineActive false; safe to pass {} per RESEARCH A1)
    - internal/policy/fuzz_test.go (the FuzzEvaluate pattern to clone: //go:build fuzz, f.Add seeds, severity-validity assertion)
    - internal/sentry/imports_test.go (TestRulesImportsArePure — confirms EvaluateEvent is pure/fuzzable)
    - 21-RESEARCH.md §"Sentry Fuzz Target" (the FuzzEvaluateEvent signature + seed corpus + CI wiring)
  </read_first>
  <behavior>
    - Test (FuzzEvaluateEvent): feeding fuzz bytes into the SentryEvent's kind/pid/ppid/exe/path fields, with NewRuleState() + a small synthetic process tree + default RuleConfig + zero BaselineState + time.Now(), EvaluateEvent never panics and every returned alert has Severity in {"critical","high"}.
    - Seed corpus: a FileAccess cred read, a ProcessCreate cred-CLI, a NetworkConnect, a FileWrite persistence path, and an out-of-range kind (255)/zero event.
  </behavior>
  <action>Create `internal/sentry/fuzz_test.go` under `//go:build fuzz`, package `sentry`, modeled on `internal/policy/fuzz_test.go::FuzzEvaluate`. Seed with the five corpus rows from RESEARCH §"Sentry Fuzz Target" (FileAccess cred read `/home/u/.aws/credentials`; ProcessCreate `gh`; NetworkConnect `claude`; FileWrite `.config/systemd/user/x.service`; out-of-range kind 255). In `f.Fuzz`, build a `SentryEvent{Kind: EventKind(kind), PID: pid, PPID: ppid, Exe: exe, FilePath: path}`, a one-node `tree`, call `EvaluateEvent(ev, NewRuleState(), tree, InventorySnapshot{}, RuleConfig{}, BaselineState{}, time.Now())`, and assert every returned alert's `Severity` is `"critical"` or `"high"` (anything else `t.Errorf`). The contract: no panic + only valid severities (a panic is a fail-open DoS vector per the threat register; an invalid severity breaks audit/SIEM). Confirm `BaselineState{}` zero-value is accepted (A1 — if `applyDefaults`/`IsBaselineActive` needs a non-zero baseline, seed via the baseline_test helper, but RESEARCH says zero is safe). Smoke locally: `go test -tags fuzz -run FuzzEvaluateEvent -fuzz FuzzEvaluateEvent -fuzztime 5s ./internal/sentry/`.</action>
  <verify>
    <automated>go test -tags fuzz -run FuzzEvaluateEvent ./internal/sentry/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags fuzz -run FuzzEvaluateEvent ./internal/sentry/` exits 0 (seed corpus runs without panic).
    - `go test -tags fuzz -run FuzzEvaluateEvent -fuzz FuzzEvaluateEvent -fuzztime 5s ./internal/sentry/` exits 0 (5s local fuzz smoke finds no crash).
    - The file declares `//go:build fuzz` on line 1 and defines `func FuzzEvaluateEvent(f *testing.F)`.
    - `go build -tags fuzz ./internal/sentry/` is clean.
    - No sentry production `.go` file is modified (the evaluator is pure and unchanged — D-03).
  </acceptance_criteria>
  <done>The Sentry rule evaluator has a fuzz target that proves no-panic + valid-severity-only over fuzzed events; the local 5s smoke is clean.</done>
</task>

<task type="auto">
  <name>Task 2: ci.yml matrix delta — 3xGOOS cross-build + fuzz-sentry blocking job (VAL-03/VAL-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the WHOLE file: matrix `test` job [ubuntu/macos/windows] already does go mod verify / build / `go test -race CGO=1` / vet / tidy + Linux eBPF generate; test-sentry-kernel-5-4 + 5-15 little-vm-helper jobs; test-eslogger-fields; fuzz / fuzz-ipc / fuzz-llamafirewall; release-gate.needs)
    - 21-RESEARCH.md §"CI Matrix Decomposition" (what is ALREADY present — do NOT re-add — vs the NEW delta) + §"YAML shape recommendation" + Pitfall 3 (2-kernel = little-vm-helper VM images, NOT runner OS) + Pitfall 4 (do not touch web.yml / paths-ignore) + Pitfall 5 (eBPF never committed)
    - CLAUDE.md (eBPF bytecode CI-only/never-committed; eBPF CI matrix = explicit Ubuntu 20.04/22.04 — satisfied by the existing 5.4/5.15 little-vm-helper images per RESEARCH A3)
  </read_first>
  <action>Edit `.github/workflows/ci.yml` ONLY (never `web.yml` or `release.yml`; never remove the `paths-ignore: web/**` triggers — Pitfall 4). Add exactly the RESEARCH-identified delta and nothing already present: (1) a "Cross-compile (3 GOOS, build-only)" step in the existing matrix `test` job running `for os in linux darwin windows; do GOOS=$os go build -trimpath -buildvcs=false ./...; done` (`shell: bash`) so cross-compilation of all 3 GOOS is gated. (2) a new top-level `fuzz-sentry` job (runs-on `ubuntu-22.04`, checkout + setup-go, one step `go test -tags fuzz -run FuzzEvaluateEvent -fuzz FuzzEvaluateEvent -fuzztime 30s ./internal/sentry/`) modeled on the existing `fuzz-ipc` job. (3) add `fuzz-sentry` to `release-gate.needs` (VAL-04 blocking — a discovered Sentry panic blocks release, VAL-08). Do NOT re-author the eBPF generate step, the kernel-5.4/5.15 little-vm-helper jobs, the eslogger job, the `-race`/CGO test step, or the existing fuzz jobs — they already exist and are correct (RESEARCH: confirm-don't-duplicate). The new ipc Tier-B server/client/peer tests from Plan 01 already run on the Linux/macOS matrix legs via the existing `go test -race ./...` (they are `//go:build linux||darwin`), so no new ipc job is needed — verify this during execution by confirming the matrix `test` step's `go test ./...` includes `./internal/ipc/...`. Keep all edits minimal and additive; the YAML is statically authored + locally build-verified (D-05 — the live GitHub run happens at first push, the repo is unpushed).</action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']; assert 'fuzz-sentry' in j, 'fuzz-sentry job missing'; assert 'fuzz-sentry' in j['release-gate']['needs'], 'fuzz-sentry not in release-gate.needs'; print('ci.yml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parses and the assertion command prints `ci.yml OK` (fuzz-sentry job exists AND is in release-gate.needs).
    - `ci.yml` contains a cross-compile step running `GOOS=$os go build ... ./...` for linux/darwin/windows (grep `GOOS=` in the test job).
    - The `paths-ignore` web isolation on BOTH the `pull_request` and `push` triggers is unchanged (grep `web/**` appears twice; `web.yml` is untouched — `git diff --name-only` lists only `.github/workflows/ci.yml`).
    - The existing eBPF generate step, the two little-vm-helper kernel jobs, the eslogger job, and the `-race`/CGO test step are all still present (not removed) — `git diff` shows additions only, no deletions of those blocks.
    - `release.yml` is NOT modified (git diff does not list it).
  </acceptance_criteria>
  <done>ci.yml gains the 3xGOOS cross-build step + a blocking fuzz-sentry release gate; all pre-existing matrix jobs are preserved; web/release isolation is intact. (CI-validated: the live green run is at first push per D-05 — local acceptance is YAML-valid + build-verified.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile kernel event → Sentry evaluator | A crafted/garbage SentryEvent reaches the pure EvaluateEvent; a panic here is a fail-open DoS for the runtime monitor |
| commit → release gate | A change that introduces a Sentry parser panic must be blocked before a release tag is cut |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | DoS / fail-open | sentry.EvaluateEvent | mitigate | FuzzEvaluateEvent (native fuzzing) proves no-panic + valid-severity-only; added to release-gate.needs as a blocking CI gate so a discovered crash blocks release (VAL-04/VAL-08, D-06) |
| T-21-06 | Tampering (config/build) | ci.yml matrix integrity | mitigate | Cross-platform matrix gates build (native+3 GOOS), vet, `-race`/CGO, eBPF gen+load (CI-only bytecode, never committed — Pitfall 5), eslogger, ETW, peer-cred; web/release isolation preserved (Pitfall 4) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed; no new `uses:` action beyond those already pinned (RESEARCH: N/A) |
</threat_model>

<verification>
- `go test -tags fuzz -run FuzzEvaluateEvent ./internal/sentry/` green; 5s fuzz smoke clean.
- ci.yml parses; `fuzz-sentry` job present and in `release-gate.needs`; 3×GOOS cross-build step present.
- `web.yml` + `release.yml` untouched; `paths-ignore: web/**` preserved on both ci.yml triggers.
- `go test ./...` still green on Windows (no regression from the fuzz file under default build).
</verification>

<success_criteria>
- The Sentry rule evaluator is fuzzed as a blocking CI release gate (VAL-04 complete — 5 required targets: policy, IPC-proto, catalog, MCP=gateway/parser, Sentry).
- The CI matrix gates build (native+3 GOOS), vet, test, `-race`, eBPF gen+load, eslogger, ETW, peer-cred — the delta added without re-authoring existing jobs (VAL-03).
- Go/web CI isolation and the eBPF-never-committed constraint are intact.
</success_criteria>

<output>
Create `.planning/phases/21-full-system-validation-ci-calibration/21-03-SUMMARY.md` when done.
</output>
