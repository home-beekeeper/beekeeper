---
phase: 21-full-system-validation-ci-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/coveragegate/coveragegate.go
  - internal/coveragegate/coveragegate_test.go
  - internal/coveragegate/allowlist.go
  - internal/coveragegate/allowlist_test.go
  - coverage-allowlist.txt
  - internal/catalog/health_test.go
  - internal/ipc/server_test.go
  - internal/ipc/client_test.go
  - internal/ipc/peer_linux_test.go
  - internal/ipc/peer_darwin_test.go
  - internal/hooks/protected_test.go
  - internal/tui/model_coverage_test.go
autonomous: true
requirements: [VAL-01, VAL-08]
must_haves:
  truths:
    - "go test ./internal/coveragegate/ is green: every Go prod file under internal/+cmd/ is package-tested OR reason-coded allowlisted, zero UNACCOUNTED"
    - "The allowlist parser fails closed: a bare path (no reason code) and an out-of-taxonomy reason code are both rejected as UNACCOUNTED"
    - "catalog.ResolveHealthy fail-open-on-read behavior is tested (covers the 4 sanity.go delegators)"
    - "internal/ipc server/client/peer tests exist and compile under //go:build linux||darwin (Tier-B, CI-validated)"
    - "hooks.protected.go markers + home-rooted HookConfigFiles are tested; tui model status/audit-tail logic is tested"
  artifacts:
    - path: "internal/coveragegate/coveragegate.go"
      provides: "Prod-file walker (go/build) + ACCOUNTED/UNACCOUNTED classifier + TestCoverageManifest"
      contains: "func"
    - path: "internal/coveragegate/allowlist.go"
      provides: "Fail-closed reason-coded allowlist parser with closed reason-code taxonomy"
      contains: "reason"
    - path: "coverage-allowlist.txt"
      provides: "The reason-coded no-test allowlist (path<TAB># reason: <code> lines)"
      contains: "reason:"
    - path: "internal/catalog/health_test.go"
      provides: "ResolveHealthy fail-open + degraded + first-run coverage"
      contains: "ResolveHealthy"
  key_links:
    - from: "internal/coveragegate/coveragegate_test.go"
      to: "internal/+cmd/ tree"
      via: "go/build walk of every non-_test.go file"
      pattern: "build\\.|parser\\."
    - from: "internal/coveragegate/allowlist.go"
      to: "coverage-allowlist.txt"
      via: "reason-coded line parse"
      pattern: "reason"
---

<objective>
Build the VAL-01/VAL-08 coverage gate and close the surfaced Tier-A test gaps. The gate walks every Go production file under `internal/` and `cmd/`, classifies each as package-tested or reason-coded-allowlisted, and fails on any UNACCOUNTED file. The allowlist parser fails closed (a bare path or an unknown reason code breaks the gate — the phase's self-defense surface). Then add real tests for the genuinely test-less files: `catalog.ResolveHealthy` (covers the four `*/sanity.go` delegators), the `internal/ipc` server/client/peer (Tier-B), `hooks/protected.go`, and the uncovered `tui/model.go` functions.

Purpose: This is the spine of the phase — "the gate tests the gate." Per D-02 the gate is a presence + accountability check (NOT a coverage-% threshold); per D-10 it is tamper-evident. The Tier-A gap tests are the remediation half of D-03 (mostly missing TESTS, not behavior changes — RESEARCH found no defects in these files).
Output: `internal/coveragegate` package, `coverage-allowlist.txt`, and 7 new/extended `*_test.go` files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-full-system-validation-ci-calibration/21-CONTEXT.md
@.planning/phases/21-full-system-validation-ci-calibration/21-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coveragegate package — prod-file walker + fail-closed reason-coded allowlist (VAL-01/VAL-08)</name>
  <files>internal/coveragegate/coveragegate.go, internal/coveragegate/coveragegate_test.go, internal/coveragegate/allowlist.go, internal/coveragegate/allowlist_test.go, coverage-allowlist.txt</files>
  <read_first>
    - internal/sentry/imports_test.go (the existing go/parser tree-walk analog — RESEARCH §"Don't Hand-Roll" names this as the linkage-walk pattern to clone)
    - 21-RESEARCH.md §"Pattern 1: Package-level coverage linkage + file-level reason-coded allowlist" and §"VAL-08 — Self-Defense" (the reason-code taxonomy + fail-closed contract)
    - internal/version/version.go (the lone zero-test package per RESEARCH OQ-1 — must be an allowlist entry)
    - internal/editorinit/lookup.go (the exec-seam-stub allowlist candidate)
  </read_first>
  <behavior>
    - Test 1 (TestCoverageManifest): walking internal/+cmd/ classifies every non-_test.go prod file; with the shipped coverage-allowlist.txt present, zero files are UNACCOUNTED — the test passes.
    - Test 2 (TestCoverageManifest negative, table/helper): a synthetic prod file in a synthetic test-less package with no allowlist entry is reported UNACCOUNTED.
    - Test 3 (TestAllowlistFailsClosed): the parser rejects a bare path line (no reason code) and a path with a reason code OUTSIDE the closed taxonomy — both classified UNACCOUNTED, never silently accounted.
    - Test 4 (TestAllowlistReasonTaxonomy): each of the six closed reason codes parses to an accounted entry; an empty/whitespace reason fails.
  </behavior>
  <action>Create package `internal/coveragegate`. In `coveragegate.go`, implement a walker using `go/build` (`build.Context.MatchFile` / `go/parser`) — NOT a regex `//go:build` scan (RESEARCH Don't-Hand-Roll) — that enumerates every non-`_test.go` `.go` file under `internal/` and `cmd/` and classifies each as `package-tested` (its directory contains ≥1 `_test.go`), `allowlisted` (path present in the parsed allowlist with a valid reason code), or `UNACCOUNTED`. Expose `Walk(roots ...string) ([]FileStatus, error)` and a `TestCoverageManifest` in `coveragegate_test.go` that fails listing every UNACCOUNTED path. Use package-level linkage, NOT same-name-sibling linkage (Pitfall 1: same-name flags 70/184 false positives). In `allowlist.go`, implement a fail-closed parser of `coverage-allowlist.txt`: each line is `path<TAB># reason: <code>`; a bare path with no reason code OR a code outside the closed taxonomy `generated-bpf|platform-stub|type-only|exec-seam-stub|thin-delegator|gen-directive` is treated as UNACCOUNTED (D-10). Author `coverage-allowlist.txt` at repo root with reason-coded entries for the genuine no-test files: `internal/version/version.go` (type-only), the generated `internal/sentry/linux/bpf_*_bpfel.go` (generated-bpf), `internal/sentry/linux/gen.go` (gen-directive), the `*_other.go`/`*_windows.go`/`*_unix.go` fail-closed shims (platform-stub), `internal/editorinit/lookup.go` (exec-seam-stub), and any pure type/const files (type-only) the walk surfaces. Run the walk during execution to enumerate the actual UNACCOUNTED set and reason-code each entry — do NOT guess; every entry must be a real file the walk flags. Add `TestAllowlistFailsClosed` + `TestAllowlistReasonTaxonomy` in `allowlist_test.go` per the behavior block.</action>
  <verify>
    <automated>go test ./internal/coveragegate/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/coveragegate/` exits 0 on the Windows dev box.
    - `go test -run TestAllowlistFailsClosed ./internal/coveragegate/` exits 0 (the meta-test proves bare-path + unknown-reason rejection).
    - `coverage-allowlist.txt` exists at repo root and every line matches `path<TAB># reason: <code>` with `<code>` in the closed six-code taxonomy (grep: no line in the file has a path without a `# reason:` token — `grep -v '^#' coverage-allowlist.txt | grep -cv 'reason:'` returns 0).
    - The walker uses `go/build` or `go/parser` (no regex `//go:build` scan) — grep `coveragegate.go` for `go/build` or `go/parser` import.
    - `go build ./internal/coveragegate/` is clean.
  </acceptance_criteria>
  <done>The coverage gate passes with zero UNACCOUNTED files, the allowlist is reason-coded and fails closed, and the meta-test proves the bar cannot be silently lowered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: catalog.ResolveHealthy + hooks.protected + tui model gap tests (VAL-01, local Tier-A)</name>
  <files>internal/catalog/health_test.go, internal/hooks/protected_test.go, internal/tui/model_coverage_test.go</files>
  <read_first>
    - internal/catalog/health.go (the ResolveHealthy logic under test — note the documented fail-OPEN-on-read at lines 53-65; the four check/watch/scan/gateway sanity.go delegate to it)
    - internal/hooks/protected.go (BeekeeperHookMarkers + HookConfigFiles — pure metadata under test)
    - internal/tui/model.go (computeStatus / recentAuditRecords / runPaletteSelection — RESEARCH A2: run `go test -cover ./internal/tui/` to pin the exact uncovered funcs before writing)
    - internal/tui/model_test.go (the existing model test — extend, do not duplicate covered cases)
    - 21-RESEARCH.md §"Tier-A Gap Test Contracts" (the per-gap must-assert table)
  </read_first>
  <behavior>
    - Test 1 (catalog ResolveHealthy): empty cacheDir → true; missing state.json → true (fail-open-on-read by design); bumblebee source Degraded:true → false; bumblebee absent (first run) → true.
    - Test 2 (hooks protected): BeekeeperHookMarkers contains "beekeeper check" and "beekeeper audit-record"; every path returned by HookConfigFiles(home) is rooted under home (no empty/absolute-escape).
    - Test 3 (tui model): computeStatus derives the right status from a synthetic HealthState + a recent-critical audit record; recentAuditRecords reads only the bounded tail (does not parse the whole file).
  </behavior>
  <action>Write `internal/catalog/health_test.go` covering `ResolveHealthy` per the four cases in the behavior block — this single test covers the four `internal/{check,watch,scan,gateway}/sanity.go` delegators (RESEARCH: they are 4-line identical delegators to `catalog.ResolveHealthy`; testing the real logic once is preferred over four ceremony tests, OQ-1). Seed a temp state.json with `state.Sources[bumblebeeSource].Degraded` via the existing `catalog.SaveState`/`BuildIndex` helpers (read state.go during execution for the exact constructor). Write `internal/hooks/protected_test.go` asserting the markers and that `HookConfigFiles(home)` returns one path per supported harness, each `strings.HasPrefix(path, home)` (no empty, no absolute-escape). Write `internal/tui/model_coverage_test.go` targeting the specific uncovered model functions — run `go test -cover ./internal/tui/` first to confirm which of `computeStatus`/`recentAuditRecords`/`runPaletteSelection` are uncovered (A2), then assert `computeStatus` against a synthetic `HealthState` + recent-critical record and that `recentAuditRecords` reads only the bounded tail (the v1.3.0-seed de-mock work). Do NOT modify production code — these files have no surfaced defect (D-03 guardrail: tests only, no cosmetic refactor).</action>
  <verify>
    <automated>go test ./internal/catalog/ ./internal/hooks/ ./internal/tui/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/catalog/ ./internal/hooks/ ./internal/tui/` exits 0 on Windows.
    - `go test -run TestResolveHealthy ./internal/catalog/` exits 0 and exercises all four branches (empty/missing/degraded/first-run).
    - `internal/hooks/protected_test.go` asserts both markers AND home-rooting of every HookConfigFiles path.
    - No production `.go` file (non-`_test.go`) is modified by this task (git diff shows only `_test.go` additions).
    - The four `*/sanity.go` files are now ACCOUNTED by the Task-1 gate via package-tested status (their package contains tests).
  </acceptance_criteria>
  <done>catalog.ResolveHealthy, hooks.protected, and the tui model gap functions have real tests; the four sanity.go delegators are accounted; zero production behavior changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IPC server/client/peer Tier-B tests (VAL-01, CI-validated)</name>
  <files>internal/ipc/server_test.go, internal/ipc/client_test.go, internal/ipc/peer_linux_test.go, internal/ipc/peer_darwin_test.go</files>
  <read_first>
    - internal/ipc/server.go (NewServer / Serve / Close — 0600 socket, Lchown ownerUID, unauth peer closed pre-handler)
    - internal/ipc/client.go (Connect / SendCommand / ReadResponse — write/read deadlines)
    - internal/ipc/peer_linux.go + peer_darwin.go (verifyPeerUID via SO_PEERCRED / LOCAL_PEERCRED)
    - internal/ipc/pipe_windows_test.go (the existing Windows-half peer test — the assertion-shape analog)
    - internal/ipc/proto_test.go (the same-package encode/decode round-trip helpers to reuse)
    - 21-RESEARCH.md §"Tier-A Gap Test Contracts" rows for ipc server/client/peer + Pitfall 6 (build-tagged tests skipped on Windows)
  </read_first>
  <behavior>
    - Test 1 (server): NewServer creates the socket at 0600 with ownerUID; a pre-existing socket at the path is removed; Serve returns on ctx cancel; an unauthenticated peer connection is closed before the handler runs (fail-closed peer-auth).
    - Test 2 (client): Connect→SendCommand→ReadResponse round-trips against a real NewServer; a write/read deadline timeout returns an error (does not hang).
    - Test 3 (peer_linux/peer_darwin): same-UID connection passes verifyPeerUID; a mismatched expected-UID is rejected.
  </behavior>
  <action>Write `internal/ipc/server_test.go` and `internal/ipc/client_test.go` under `//go:build linux || darwin` (matching server.go/client.go build tags) using a real `net.Listen("unix")` server (RESEARCH Don't-Hand-Roll: the production path IS the test, no fake socket). Assert the server contract from the behavior block: socket mode 0600 (os.Stat), pre-existing socket removed, `Serve` returns `ctx.Err()` on cancel, and the unauthenticated-peer-closed-before-handler path. For the client, round-trip an `IPCCommand`/`IPCResponse` through `Connect`/`SendCommand`/`ReadResponse` against a live `NewServer`, and assert a too-short timeout produces an error rather than hanging. Write `internal/ipc/peer_linux_test.go` (`//go:build linux`) and `internal/ipc/peer_darwin_test.go` (`//go:build darwin`) asserting `verifyPeerUID` passes for the current process UID (`os.Getuid()`) and rejects a deliberately wrong expected-UID. These are Tier-B: they compile-check on Windows but only RUN in the CI Linux/macOS matrix legs — they will be picked up by `go test ./internal/ipc/...` on those OSes (Plan 03 confirms the matrix runs them). Do NOT modify ipc production code (no surfaced defect — D-03).</action>
  <verify>
    <automated>go vet ./internal/ipc/</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/ipc/` is clean (the new test files compile under their build tags; on Windows they are excluded by the linux||darwin tag, so vet validates the build-tag correctness).
    - `GOOS=linux go build ./internal/ipc/` and `GOOS=darwin go build ./internal/ipc/` succeed (the production files build cross-platform; tests are tag-guarded).
    - Each new test file carries the correct `//go:build` line (server/client = `linux || darwin`; peer_linux = `linux`; peer_darwin = `darwin`) — grep the first line of each file.
    - acceptance note: these tests are CI-validated, NOT run-green-locally — the Windows dev box cannot run `linux||darwin` tests (Pitfall 6). Local acceptance is "builds + vet clean + correct build tags"; the green RUN is the CI matrix (Plan 03 VAL-03).
    - No production `.go` file under internal/ipc is modified (git diff shows only `_test.go` additions).
  </acceptance_criteria>
  <done>IPC server/client/peer have Tier-B tests with correct build tags; they compile on Windows and run green in the CI Linux/macOS matrix; ipc files are now package-tested (ACCOUNTED by the gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit → coverage gate | An agent or careless commit adds a prod file or weakens the allowlist; the gate is the trust boundary that must fail closed |
| client → IPC Unix socket | An unprivileged local process attempts to connect to the Sentry daemon socket; peer-UID auth is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | coverage-allowlist.txt / the gate | mitigate | Allowlist parser fails closed on bare-path + unknown-reason-code lines (TestAllowlistFailsClosed); a new untested prod file with no reason-coded entry breaks `go test ./internal/coveragegate/` (VAL-08, D-10) |
| T-21-04 | Spoofing/EoP | internal/ipc Unix socket | mitigate | Socket created 0600 owner-only + Lchown ownerUID; verifyPeerUID (SO_PEERCRED/LOCAL_PEERCRED) rejects mismatched-UID peers before the handler runs — asserted by the new server + peer tests (CI-validated Tier-B) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Phase 21 installs ZERO external packages (RESEARCH Package Legitimacy Audit: N/A — all stdlib + already-pinned actions); no install surface to gate |
</threat_model>

<verification>
- `go test ./internal/coveragegate/` green (gate + fail-closed meta-test).
- `go test ./internal/catalog/ ./internal/hooks/ ./internal/tui/` green (Tier-A gap tests).
- `go vet ./internal/ipc/` clean + `GOOS=linux`/`GOOS=darwin go build ./internal/ipc/` clean (Tier-B tests compile; run green in CI).
- `go test ./...` green on the Windows dev box (no regression).
- `coverage-allowlist.txt` reason-coded, fails closed.
</verification>

<success_criteria>
- The coverage gate reports zero UNACCOUNTED prod files across internal/+cmd/.
- The allowlist is tamper-evident (reason-coded, fails closed — VAL-08).
- All eight surfaced Tier-A gaps are closed: the four sanity.go (via catalog.ResolveHealthy), ipc server/client + peer, hooks/protected.go, tui model logic — each either package-tested or (editorinit/lookup.go) honestly allowlisted.
- Zero production behavior changed (D-03 guardrail: RESEARCH found no defects in the gap files).
</success_criteria>

<output>
Create `.planning/phases/21-full-system-validation-ci-calibration/21-01-SUMMARY.md` when done.
</output>
