---
phase: 19-test-suite-ci
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - .github/workflows/web.yml
  - .github/workflows/ci.yml
  - web/tests/home_spec.py
  - web/tests/gfx_spec.py
  - web/tests/seo_spec.py
  - web/tests/accuracy_spec.py
autonomous: true
requirements: [QA-01, QA-02]
user_setup: []

must_haves:
  truths:
    - ".github/workflows/web.yml triggers ONLY on web/**, pnpm-workspace.yaml, and itself (paths: include filter)"
    - ".github/workflows/web.yml runs the 5 ordered gates: lint → typecheck → unit → build → postbuild → e2e (QA-01 step order, SEO post-build per OQ-1)"
    - ".github/workflows/ci.yml gains paths-ignore (web/**, pnpm-workspace.yaml, .github/workflows/web.yml) on BOTH push and pull_request (bidirectional isolation, SC-1)"
    - "the four Python specs re-run green on the production out/ build immediately before deletion (JS parity proven, no coverage regression)"
    - "the four web/tests/*.py files are deleted after parity; the full JS suite (lint→typecheck→unit→build→postbuild→e2e) is green"
  artifacts:
    - path: ".github/workflows/web.yml"
      provides: "path-filtered web CI job with the 5 ordered gates (statically verified, not a live run — D-03)"
      contains: "paths:"
    - path: ".github/workflows/ci.yml"
      provides: "Go CI with paths-ignore web isolation"
      contains: "paths-ignore"
  key_links:
    - from: ".github/workflows/web.yml on.paths"
      to: "web/** + pnpm-workspace.yaml + itself"
      via: "GitHub Actions paths include filter"
      pattern: "web/\\*\\*"
    - from: ".github/workflows/ci.yml on.paths-ignore"
      to: "web/** + pnpm-workspace.yaml + web.yml"
      via: "GitHub Actions paths-ignore exclude filter"
      pattern: "paths-ignore"
---

<objective>
Create the path-filtered web CI workflow, isolate Go CI bidirectionally, then prove JS parity against the four Python specs and retire them. Author `.github/workflows/web.yml` (the 5 ordered gates: Biome lint → tsc → Vitest unit → pnpm build → SEO postbuild → Playwright E2E, with pinned first-party actions + least-privilege perms), add a `paths-ignore` block to BOTH triggers of the existing Go `.github/workflows/ci.yml`, re-run the four `web/tests/*.py` specs green on the production `out/` to prove no coverage regression, then DELETE the four `.py` files, and run the full JS suite green as the phase gate.

Purpose: QA-01 requires a path-filtered web CI job separate from Go CI, gating merges with the named gates; SC-1 requires the isolation to be bidirectional. D-01 requires the `.py` specs to be retired ONLY after JS parity is proven. D-03 requires the workflow correctness to be verified by STATIC inspection (the repo is unpushed — no live GitHub run).

Output: `.github/workflows/web.yml` (new), the `paths-ignore` edit on `ci.yml`, the deletion of the four `web/tests/*.py` files, and a green full JS suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

NOTE: D-05 — execution runs INLINE on main (subagents lack node/pnpm/playwright). The orchestrator is the executor; per-task atomic commits; tracking is hand-managed.
DEPENDS ON 19-01 + 19-02: the full JS suite (vitest config, unit tests, playwright config, e2e specs, postbuild seo) must already be green before web.yml is authored and before the .py specs are retired.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-test-suite-ci/19-CONTEXT.md
@.planning/phases/19-test-suite-ci/19-RESEARCH.md
@.planning/phases/19-test-suite-ci/19-PATTERNS.md
@.planning/phases/19-test-suite-ci/19-VALIDATION.md

# CI files modified — read the CURRENT on-block before editing:
@.github/workflows/ci.yml

# Python specs being retired (must re-run green before deletion):
@web/tests/home_spec.py
@web/tests/gfx_spec.py
@web/tests/seo_spec.py
@web/tests/accuracy_spec.py

# Wave-1/2 artifacts the workflow gates run against:
@web/package.json
</context>

<artifacts_this_phase_produces>
This Wave-3 plan introduces / changes:

NEW file:
- `.github/workflows/web.yml` — path-filtered web CI job (ubuntu-latest, working-directory: web, 5 ordered gates + Playwright browser install + report artifact on failure; least-privilege `permissions: { contents: read }`)

MODIFIED:
- `.github/workflows/ci.yml` — adds `paths-ignore` (web/**, pnpm-workspace.yaml, .github/workflows/web.yml) to BOTH `push` and `pull_request`. This is the ONLY change to ci.yml; all jobs (test, fuzz, fuzz-ipc, fuzz-llamafirewall, test-sentry-kernel-5-4, test-sentry-kernel-5-15, test-eslogger-fields, release-gate) are unchanged.

DELETED (after JS parity proven):
- `web/tests/home_spec.py`, `web/tests/gfx_spec.py`, `web/tests/seo_spec.py`, `web/tests/accuracy_spec.py`

No new package.json scripts/deps (all in 19-01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/web.yml + add paths-ignore to ci.yml (bidirectional isolation)</name>
  <files>.github/workflows/web.yml, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the CURRENT on-block lines 3-6: `on: { pull_request:, push: { branches: [main] } }` — NO path filter; and the actions-version style to mirror: actions/checkout@v4, actions/setup-go@v5)
    - .planning/phases/19-test-suite-ci/19-PATTERNS.md ".github/workflows/web.yml (NEW)" section (the full workflow) and ".github/workflows/ci.yml (MODIFY — add paths-ignore)" section (the exact replacement on-block) — these are the canonical bodies; do NOT inline them here, read them there
    - .planning/phases/19-test-suite-ci/19-RESEARCH.md §Pattern 3 (web.yml: defaults.run.working-directory web, pnpm/action-setup@v6 version 11.1.3, actions/setup-node@v4 node 22 cache:pnpm, playwright install chromium --with-deps, the 5 ordered gates, upload-artifact@v4 on failure) + §Pattern 4 (ci.yml paths-ignore on BOTH triggers) + §Security Domain (default read permissions sufficient; first-party actions only)
    - .planning/phases/19-test-suite-ci/19-CONTEXT.md D-03 (verify by static inspection, NOT a live run) + D-04 (bidirectional isolation specifics)
  </read_first>
  <action>
    Create `.github/workflows/web.yml` per PATTERNS §web.yml. Key requirements: `name: Web CI`; `on:` with BOTH `push: { branches: [main], paths: [...] }` and `pull_request: { paths: [...] }` where paths = ['web/**','pnpm-workspace.yaml','.github/workflows/web.yml'] (the include filter — D-04). One job `web` on `ubuntu-latest` with `defaults: { run: { working-directory: web } }`. Add least-privilege `permissions: { contents: read }` at the job (or top) level (Security Domain — no id-token/contents:write needed). Steps in EXACT order, each a pinned first-party action or a pnpm script: (1) actions/checkout@v4; (2) pnpm/action-setup@v6 with version 11.1.3 (matches packageManager); (3) actions/setup-node@v4 with node-version '22' + cache 'pnpm'; (4) `pnpm install --frozen-lockfile`; (5) `pnpm exec playwright install chromium --with-deps` (NO browser cache — official PW guidance; --with-deps for Ubuntu system libs); (6) `pnpm lint`; (7) `pnpm typecheck`; (8) `pnpm test`; (9) `pnpm build`; (10) `pnpm test:postbuild`; (11) `pnpm test:e2e`; (12) actions/upload-artifact@v4 `if: failure()` uploading web/playwright-results/. This enforces QA-01's lint → typecheck → unit → build → postbuild(SEO, OQ-1) → e2e ordering.

    Modify `.github/workflows/ci.yml`: replace ONLY the `on:` block (lines 3-6) so BOTH `pull_request` and `push` carry `paths-ignore: ['web/**','pnpm-workspace.yaml','.github/workflows/web.yml']` (push keeps `branches: [main]`). Do NOT touch any job. The same-file paths/paths-ignore mutual-exclusion rule does not apply across separate workflow files (web.yml uses paths:, ci.yml uses paths-ignore:) — RESEARCH Pattern 4.

    Verify by STATIC inspection only (D-03 — no live GitHub run): the YAML is well-formed, web.yml has the path filter + the 6 ordered gate steps (lint/typecheck/test/build/test:postbuild/test:e2e) in order, and ci.yml's on-block has paths-ignore with web/** on both triggers. Use a YAML parser (the inline `python -c` check in <verify>) plus a read-through; do NOT attempt to run the workflow.
  </action>
  <acceptance_criteria>
    - .github/workflows/web.yml exists and parses as valid YAML.
    - web.yml `on.push.paths` and `on.pull_request.paths` both include `web/**`, `pnpm-workspace.yaml`, `.github/workflows/web.yml` (push also keeps `branches: [main]`).
    - web.yml has least-privilege `permissions: { contents: read }`; every `uses:` is a first-party action (checkout@v4, action-setup@v6, setup-node@v4, upload-artifact@v4).
    - web.yml gate `run:` steps appear in the order pnpm lint → pnpm typecheck → pnpm test → pnpm build → pnpm test:postbuild → pnpm test:e2e (SEO post-build, OQ-1).
    - .github/workflows/ci.yml `on.push` and `on.pull_request` both carry `paths-ignore` including `web/**`; all ci.yml jobs unchanged.
    - The `<verify>` static-inspection script exits 0 (no live workflow run — D-03).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml,sys; w=yaml.safe_load(open('.github/workflows/web.yml')); c=yaml.safe_load(open('.github/workflows/ci.yml')); on_w=w[True] if True in w else w['on']; on_c=c[True] if True in c else c['on']; assert 'web/**' in on_w['push']['paths'] and 'web/**' in on_w['pull_request']['paths'], 'web.yml missing path filter'; assert 'web/**' in on_c['push']['paths-ignore'] and 'web/**' in on_c['pull_request']['paths-ignore'], 'ci.yml missing paths-ignore'; steps=[s.get('run','') for s in w['jobs']['web']['steps']]; joined=' '.join(steps); assert joined.find('pnpm lint')<joined.find('pnpm typecheck')<joined.find('pnpm test')<joined.find('pnpm build')<joined.find('pnpm test:postbuild')<joined.find('pnpm test:e2e'), 'gate order wrong'; print('web.yml + ci.yml OK')"</automated>
  </verify>
  <done>.github/workflows/web.yml exists, parses as valid YAML, has the path-include filter on both triggers, the least-privilege permissions, and the 6 gate steps in the lint→typecheck→test→build→test:postbuild→test:e2e order; .github/workflows/ci.yml has paths-ignore (incl. web/**) on BOTH push and pull_request with all jobs unchanged; the static-inspection check passes (no live run, D-03).</done>
</task>

<task type="auto">
  <name>Task 2: Prove JS parity (re-run the 4 .py specs green), retire them, then full-suite gate</name>
  <files>web/tests/home_spec.py, web/tests/gfx_spec.py, web/tests/seo_spec.py, web/tests/accuracy_spec.py</files>
  <read_first>
    - web/tests/home_spec.py, web/tests/gfx_spec.py, web/tests/seo_spec.py, web/tests/accuracy_spec.py (the four specs being retired — confirm how each is invoked: a __main__ runner that serves out/ on a port and drives the Python playwright, or a plain file-walk)
    - .planning/phases/19-test-suite-ci/19-CONTEXT.md D-01 (retire the .py files ONLY after the JS equivalents pass on the same out/ build — no coverage regression) + 19-VALIDATION.md "Before /gsd-verify-work" (the 4 .py specs re-run green immediately before deletion)
    - .planning/phases/19-test-suite-ci/19-RESEARCH.md §State of the Art "Deprecated/outdated" (the four .py specs retained until JS parity proven, then deleted)
  </read_first>
  <action>
    First build the production out/ once: `pnpm build` (the shared input for both the .py and the JS specs). Then prove parity in BOTH directions on that SAME out/:
    1. Re-run all four Python specs green against the freshly-built out/ (invoke each per its current runner — e.g. `python web/tests/seo_spec.py`, `python web/tests/accuracy_spec.py`, `python web/tests/home_spec.py`, `python web/tests/gfx_spec.py`; the browser specs use the local Python playwright r1200). Confirm all four exit 0. This is the regression baseline.
    2. Confirm the JS suite is green on the same out/: `pnpm test` (unit incl. accuracy) + `pnpm test:postbuild` (seo) + `pnpm test:e2e` (home + gfx). Confirm the verbatim-copied constant arrays (HARNESS_NAMES, KNOWN_GAP_MARKERS, FORBIDDEN_SERVER_SYMBOLS, UNENFORCED_FEATURES/LABELS, PHANTOM_COMMANDS) and the ≥13-loc sitemap threshold match the .py sources (spot-diff if any doubt). Parity = the JS specs assert everything the .py specs assert, with no weakened/removed checks.

    Only after BOTH are green: DELETE the four files `web/tests/home_spec.py`, `web/tests/gfx_spec.py`, `web/tests/seo_spec.py`, `web/tests/accuracy_spec.py` (git rm). Do NOT delete before parity is proven (D-01).

    Then run the FULL JS suite as the phase gate, in the exact CI order: `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:postbuild && pnpm test:e2e` — all exit 0. Confirm `git ls-files web/tests/*.py` returns nothing (the .py specs are gone) and that the JS suite still covers every behavior they did.
  </action>
  <acceptance_criteria>
    - All four Python specs ran green on the freshly-built production out/ BEFORE any deletion (parity baseline recorded in the SUMMARY).
    - The JS suite (pnpm test + pnpm test:postbuild + pnpm test:e2e) is green on the SAME out/; no JS assertion is weaker than its .py source (verbatim constants + ≥13-loc threshold preserved).
    - The four web/tests/*.py files are deleted (git rm) only AFTER both directions are green.
    - `cd web && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:postbuild && pnpm test:e2e` exits 0.
    - `git ls-files web/tests/*.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:postbuild && pnpm test:e2e && test -z "$(git ls-files 'web/tests/*.py')"</automated>
  </verify>
  <done>All four Python specs ran green on the production out/ build (parity baseline); the full JS suite (lint→typecheck→unit→build→postbuild→e2e) is green on the same out/; the four web/tests/*.py files are deleted (`git ls-files web/tests/*.py` is empty); no JS assertion is weaker than its .py source (verbatim constants + thresholds preserved). QA-01 (path-filtered web CI gating with the named gates) and QA-02 (the four E2E critical paths) are both fully covered by the JS suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo + npm registry | web.yml checks out the repo + installs deps on a CI worker (supply-chain + permissions surface) |
| workflow triggers → CI compute | path filters decide which workflow spends CI minutes on a given change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Elevation of Privilege | web.yml job token scope | mitigate | Least-privilege `permissions: { contents: read }` — no id-token:write, no contents:write (no signing/release in this job); first-party actions only (checkout@v4, action-setup@v6, setup-node@v4, upload-artifact@v4) |
| T-19-10 | Tampering | CI dependency install | mitigate | `pnpm install --frozen-lockfile` (deterministic, lockfile-pinned); pinned action major versions; Playwright browser fetched by PW's own `install` tool (correct revision) |
| T-19-11 | Spoofing/Tampering | third-party GitHub Actions | mitigate | No third-party actions — every `uses:` is a GitHub/Microsoft/pnpm-maintained first-party action (RESEARCH §Security Domain) |
| T-19-12 | Denial of Service | unfiltered Go CI on web commits (and vice-versa) | mitigate | Bidirectional path filters: web.yml `paths:` include + ci.yml `paths-ignore:` exclude (SC-1) so neither matrix spins on the other's changes |
| T-19-SC | Tampering | npm/pnpm + Playwright installs in CI | mitigate | Frozen lockfile install; package-legitimacy already gated at 19-01 Task 1; no new tracked deps in this plan; no postinstall scripts on the test deps |
</threat_model>

<verification>
- `python -c "import yaml; ..."` static check: web.yml has the path-include filter on both triggers + the 6 ordered gate steps; ci.yml has paths-ignore (web/**) on both triggers (D-03 — no live run).
- All four web/tests/*.py specs ran green on the production out/ build (parity baseline) BEFORE deletion (D-01).
- `cd web && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:postbuild && pnpm test:e2e` → exit 0 (full JS suite, CI order).
- `git ls-files web/tests/*.py` → empty (the four .py specs retired).
- ci.yml jobs (test, fuzz*, sentry-kernel*, eslogger, release-gate) unchanged (only the on-block edited).
</verification>

<success_criteria>
- web.yml is a valid, path-filtered web CI job running the 5+1 named gates in the QA-01 order (SEO post-build per OQ-1); statically verified (D-03).
- ci.yml is bidirectionally isolated from web changes via paths-ignore on both triggers (SC-1).
- The four Python specs proved green on the production out/ before retirement; the JS suite covers every behavior with verbatim constants/thresholds (no coverage regression, D-01).
- The four web/tests/*.py files are deleted; the full JS suite is green.
- QA-01 and QA-02 are both fully satisfied by the JS toolchain.
</success_criteria>

<output>
Create `.planning/phases/19-test-suite-ci/19-03-SUMMARY.md` when done.
</output>
