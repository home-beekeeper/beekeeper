---
phase: 18-full-content-authoring
plan: 06
type: execute
wave: 3
depends_on: ["18-02", "18-03", "18-04", "18-05"]
files_modified: []
autonomous: false
requirements: [DOCS-09]
must_haves:
  truths:
    - "The full accuracy gate is green: accuracy_spec.py AC-1/AC-2/AC-3 pass and pnpm build emits all 8 docs sections"
    - "The maintainer has read the security-relevant pages side-by-side with docs/THREAT-MODEL.md and confirmed no overclaim (AC-5)"
    - "The served docs render the expanded content with working sidebar/TOC/search (DOCS-01 regression)"
  artifacts: []
  key_links:
    - from: "web/content/docs/**/*.mdx"
      to: "docs/THREAT-MODEL.md"
      via: "human accuracy review — no claim stronger than the threat model asserts"
      pattern: "source_doc:"
---

<objective>
Close Phase 18 with the DOCS-09 human accuracy review (AC-5) — the gate that cannot be automated. The maintainer reads the security-relevant docs pages side-by-side with `docs/THREAT-MODEL.md` and confirms no claim is stronger than the threat model asserts, then signs off on the served-docs render.

Purpose: DOCS-09 / SC-5 require that all content is reviewed against `docs/THREAT-MODEL.md` before publish (D-02 — accuracy is THE gate). The accuracy_spec.py harness catches missing source_doc, missing unenforced labels, and phantom commands — but it CANNOT judge whether a prose claim overclaims relative to the threat model. That is a semantic judgment reserved for the maintainer (18-VALIDATION.md AC-5 / Manual-Only Verifications).
Output: a green full-suite run + maintainer sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Execution runs INLINE on main. Wave 3 — depends on all four content plans (18-02..18-05) being complete so the full docs set exists. This plan is NOT autonomous: it contains a blocking human checkpoint.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-full-content-authoring/18-CONTEXT.md
@.planning/phases/18-full-content-authoring/18-RESEARCH.md
@.planning/phases/18-full-content-authoring/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full accuracy + build gate green</name>
  <files></files>
  <read_first>
    - 18-VALIDATION.md "Test Infrastructure" + "Sampling Rate" (the full-suite command + the "before /gsd-verify-work" gate)
    - web/tests/accuracy_spec.py (the AC-1/AC-2/AC-3 harness from 18-01)
  </read_first>
  <action>
    Run the full Phase-18 gate in order from web/: `pnpm build` (proves all 8 expanded MDX sections + the UnenforcedCallout component compile under the Next.js 16 static export and emit out/docs/<section>/index.html for every section), then `python tests/accuracy_spec.py` (AC-1 source_doc on all 8 + referenced paths exist; AC-2 unenforced labels; AC-3 no phantom commands), then the regression specs `python tests/seo_spec.py && python tests/home_spec.py && python tests/gfx_spec.py` (prove the new content did not regress SEO/home/gfx). If any spec is RED, do NOT proceed to the human checkpoint — fix the offending section (this is the executor's responsibility on main) and re-run until green.
  </action>
  <verify>
    <automated>cd web && pnpm build && python tests/accuracy_spec.py && python tests/seo_spec.py && python tests/home_spec.py && python tests/gfx_spec.py; echo "suite=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm build` exits 0 and out/docs/ contains an index.html for all 8 sections (getting-started, installation, configuration, integration, security, cli-reference, audit-log, troubleshooting)
    - `python tests/accuracy_spec.py` exits 0 (AC-1/AC-2/AC-3 all green across all 8 MDX files)
    - `python tests/seo_spec.py`, `python tests/home_spec.py`, and `python tests/gfx_spec.py` each exit 0 (no regression)
  </acceptance_criteria>
  <done>The full suite (pnpm build + accuracy_spec + seo_spec + home_spec + gfx_spec) is green; all 8 docs sections build and pass the accuracy gate; no SEO/home/gfx regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer accuracy review against docs/THREAT-MODEL.md (AC-5)</name>
  <files></files>
  <action>
    PAUSE for the maintainer. Do NOT auto-approve this checkpoint (it is the DOCS-09 semantic-accuracy gate — never auto-advance). Present the four security-relevant pages (security, integration, configuration, getting-started) for side-by-side review against docs/THREAT-MODEL.md, start a static server for the built docs, and wait for the maintainer's explicit "approved" before finalizing. If the maintainer flags an overclaim or render issue, fix it on main, re-run the Task-1 full suite green, and re-present.
  </action>
  <what-built>
    All 8 docs sections authored and accurate-gated: getting-started, installation, configuration, security, integration, cli-reference, troubleshooting, audit-log. The mechanical gate (accuracy_spec.py AC-1/AC-2/AC-3) is green and `pnpm build` emits the full docs set. The security-relevant pages (security, integration, configuration, getting-started) carry source_doc: frontmatter pointing at docs/THREAT-MODEL.md / harness-support-matrix.md and co-locate posture with known gaps.
  </what-built>
  <how-to-verify>
    1. Read each security-relevant page side-by-side with `docs/THREAT-MODEL.md`:
       - web/content/docs/security/index.mdx — confirm posture claims (corroboration thresholds, fail-closed, SPATH, self-protection, build hardening) are NOT stronger than the threat model asserts, and that all known gaps are co-located on the same page (Hermes fail-open, Tier-3 UNGUARDED, release_age/lifecycle unenforced, --bind 0.0.0.0 + missing allow_remote_gateway gate).
       - web/content/docs/integration/index.mdx — confirm the Hermes fail-open caveat is IN the Hermes section, Kilo/Trae UNGUARDED is IN their sections, the Claude-Code-only-live-verified caveat is in the intro, and the gateway --bind 0.0.0.0 caveat is in the gateway section.
       - web/content/docs/configuration/index.mdx — confirm release_age / lifecycle_script_allowlist are labeled unenforced (UnenforcedCallout), and nudge blocking is described via require_hardened (NOT mode:"block" as a config set value).
       - web/content/docs/getting-started/index.mdx — confirm the Claude-Code-only caveat and that no step references unshipped behavior.
    2. Serve the built docs and do a quick render look: `cd web && npx serve out` (or the project's usual static-serve), open http://localhost:3000/docs/, and confirm the sidebar lists all 8 sections, the TOC renders, search returns a result, and the UnenforcedCallout renders amber in both light and dark themes.
    3. Confirm no claim anywhere overclaims universal protection or implies an unenforced feature is enforced.
  </how-to-verify>
  <verify>
    <human-check>Maintainer confirms no security-relevant page makes a claim stronger than docs/THREAT-MODEL.md asserts (AC-5), and the served docs render correctly (sidebar/TOC/search; UnenforcedCallout amber in both themes).</human-check>
  </verify>
  <acceptance_criteria>
    - Maintainer confirms no security-relevant page makes a claim stronger than docs/THREAT-MODEL.md asserts (AC-5)
    - Maintainer confirms the served docs render correctly (sidebar/TOC/search work; UnenforcedCallout amber in both themes)
    - Any overclaim or render issue the maintainer flags is fixed on main and the full suite re-run green before sign-off
  </acceptance_criteria>
  <resume-signal>Type "approved" to finalize Phase 18, or describe the overclaim/render issues to fix.</resume-signal>
  <done>The maintainer has reviewed the security-relevant pages against docs/THREAT-MODEL.md, confirmed no overclaim, confirmed the served docs render, and approved Phase 18.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no new files; it is the DOCS-09 sign-off gate:
- A green full-suite run (pnpm build + accuracy_spec + seo/home/gfx regression)
- The maintainer AC-5 accuracy review sign-off recorded in 18-06-SUMMARY.md
This closes DOCS-09 (all content reviewed against docs/THREAT-MODEL.md before publish).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → reader | The final human gate that no docs claim leads a reader to a false-trust security decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-18 | Information Disclosure (semantic overclaim) | all security-relevant pages | mitigate | The AC-5 human review against docs/THREAT-MODEL.md is the only control that catches a prose claim stronger than the model — the mechanical AC-1/AC-2/AC-3 cannot judge semantic overclaim. Blocking human checkpoint, never auto-approvable. |
| T-18-19 | Tampering (build regression) | full suite | mitigate | The full-suite gate (build + accuracy + seo/home/gfx) must be green before the human review, proving the expanded content compiles and did not regress prior phases |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. The `npx serve` in the verify step is an ephemeral static server, not a project dependency add; no legitimacy gate required. |
</threat_model>

<verification>
- `cd web && pnpm build && python tests/accuracy_spec.py && python tests/seo_spec.py && python tests/home_spec.py && python tests/gfx_spec.py` exits 0.
- Maintainer AC-5 sign-off recorded ("approved").
</verification>

<success_criteria>
- Full suite green: build emits all 8 docs sections; accuracy_spec AC-1/AC-2/AC-3 green; no SEO/home/gfx regression.
- Maintainer confirms no overclaim vs docs/THREAT-MODEL.md and approves the served docs render.
</success_criteria>

<output>
Create `.planning/phases/18-full-content-authoring/18-06-SUMMARY.md` when done.
</output>
