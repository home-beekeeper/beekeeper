---
phase: 18-full-content-authoring
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - web/content/docs/getting-started/index.mdx
  - web/content/docs/installation/index.mdx
autonomous: true
requirements: [DOCS-02, DOCS-03, DOCS-09]
must_haves:
  truths:
    - "A new user can follow getting-started from zero to a real `beekeeper check` invocation with no unshipped steps (SC-1)"
    - "Installation docs cover go install, GitHub Releases download, and cosign + SLSA verification with copyable commands (SC-2)"
    - "Both pages carry a source_doc: frontmatter field and pass AC-1/AC-3"
  artifacts:
    - path: "web/content/docs/getting-started/index.mdx"
      provides: "Quickstart: prerequisites → go install → init → catalogs sync → hooks install --target → manual beekeeper check verification"
      contains: "source_doc:"
      min_lines: 60
    - path: "web/content/docs/installation/index.mdx"
      provides: "go install, GitHub Releases binary, cosign verify, SLSA verify, SBOM, build-from-source, state-dir locations"
      contains: "source_doc:"
      min_lines: 60
  key_links:
    - from: "web/content/docs/getting-started/index.mdx"
      to: "beekeeper hooks install --target claude-code"
      via: "copyable quickstart command (NOT --hook)"
      pattern: "hooks install --target"
---

<objective>
Author the Getting Started quickstart (DOCS-02) and Installation (DOCS-03) docs sections, accurate to the shipped binary, with `source_doc:` frontmatter (DOCS-09).

Purpose: SC-1 (zero → working `beekeeper check`, no unshipped steps) and SC-2 (go install + Releases + cosign/SLSA, copyable commands). These are the two "onboarding" sections; they must use the EXACT shipped flags (`hooks install --target`, not `--hook`) and the EXACT cosign/SLSA commands from docs/THREAT-MODEL.md §7 (not the pollen-scoped release-runbook). Per D-04, commands are authored from the real source, not from memory.
Output: expanded `getting-started/index.mdx` and `installation/index.mdx` with `source_doc:` frontmatter.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Execution runs INLINE on main. Wave 2 — depends on 18-01 (accuracy_spec.py + UnenforcedCallout). 18-02/18-03/18-04/18-05 share NO files and may run in parallel.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-full-content-authoring/18-CONTEXT.md
@.planning/phases/18-full-content-authoring/18-RESEARCH.md
@.planning/phases/18-full-content-authoring/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Getting Started quickstart (DOCS-02)</name>
  <files>web/content/docs/getting-started/index.mdx</files>
  <read_first>
    - web/content/docs/getting-started/index.mdx (current 28-line stub — preserve title/description frontmatter keys; FIX the `--hook` → `--target` error per 18-RESEARCH.md §8.1)
    - README.md (the honest headline framing + quick start — primary source_doc)
    - cmd/beekeeper/main.go (init + hooks install wiring: `--yes`/`--no-editors` on init; `--target` on hooks install; `ensureNudgeBlockDefault` sets supply-chain enforcement on first install — read to confirm before writing)
    - docs/harness-support-matrix.md (the "only Claude Code is live-verified" honesty note for the point-of-use caveat)
    - 18-RESEARCH.md §1 Section 1 (Getting Started content outline + key accurate facts + mandatory caveat)
    - web/content/changelog/v1.3.0/index.mdx (the shipped MDX authoring voice + fenced-code + frontmatter analog)
  </read_first>
  <action>
    Expand getting-started/index.mdx body per 18-RESEARCH.md §1 Section 1 outline: (1) Prerequisites (Go 1.25+, supported OS); (2) Install via the `go install github.com/home-beekeeper/beekeeper/cmd/beekeeper@latest` one-liner; (3) First run `beekeeper init` (creates state dir + detects editors; mention `--yes` auto-consent and `--no-editors` for scripted installs); (4) `beekeeper catalogs sync` (builds the mmap index — required before first check); (5) Install the hook with `beekeeper hooks install --target claude-code` (use `--target`, NOT `--hook` — the stub is WRONG; note this first install sets supply-chain (nudge) enforcement); (6) Verify with a manual `beekeeper check` invocation reading a tool-call JSON from stdin — use the real form `echo '{"tool_name":"Bash","tool_input":{"command":"cat ~/.ssh/id_rsa"}}' | beekeeper check --hook claude-code` (note: `check` uses `--hook`, `hooks install` uses `--target` — different commands, different flags); (7) "What to expect" — allow/warn/block decisions land in the audit log.
    Add the point-of-use caveat verbatim-in-substance from §4.3: only Claude Code is live-verified; other harnesses are contract-shape tested — link to /docs/integration/.
    Frontmatter: keep title + description (you may tighten the description), ADD `source_doc: "README.md, docs/harness-support-matrix.md"`. Use the changelog v1.3.0 MDX voice (honest, fenced `bash` code blocks, no H1 in body — title frontmatter renders the H1).
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - getting-started/index.mdx frontmatter contains `source_doc:` and both referenced paths (README.md, docs/harness-support-matrix.md) exist on disk
    - `grep -c "hooks install --target" web/content/docs/getting-started/index.mdx` returns >= 1 and `grep -c "hooks install --hook" web/content/docs/getting-started/index.mdx` returns 0 (stub error corrected)
    - `grep -c "beekeeper check" web/content/docs/getting-started/index.mdx` returns >= 1 (a real, runnable check invocation is shown)
    - The page contains the "only Claude Code is live-verified" caveat (case-insensitive grep for "live-verified" returns >= 1) and links to /docs/integration
    - accuracy_spec.py AC-3 finds no phantom command in this file (`grep -c "beekeeper check --input" web/content/docs/getting-started/index.mdx` returns 0)
  </acceptance_criteria>
  <done>getting-started/index.mdx walks a user zero → real `beekeeper check`, uses `hooks install --target`, carries valid `source_doc:`, and includes the Claude-Code-only live-verified caveat; AC-1/AC-3 pass for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Author Installation docs (DOCS-03)</name>
  <files>web/content/docs/installation/index.mdx</files>
  <read_first>
    - web/content/docs/installation/index.mdx (current 22-line stub — preserve title/description)
    - docs/THREAT-MODEL.md §2 and §7 (the AUTHORITATIVE cosign + SLSA + SBOM verification commands — copy the EXACT commands; note the lowercase `bantuson` in the SLSA source-uri vs the workflow-URL identity for cosign, per 18-RESEARCH.md §1 Section 2)
    - docs/release-runbook.md (release/signing flow context — but the THREAT-MODEL.md §7 commands win where they differ; release-runbook is pollen-scoped)
    - CLAUDE.md (build flags `-trimpath -buildvcs=false -mod=readonly`; SLSA tag `slsa-github-generator@v2.1.0` full semver; Windows state dir `%APPDATA%\beekeeper\`)
    - 18-RESEARCH.md §1 Section 2 (Installation outline + key accurate facts + the exact copyable commands)
  </read_first>
  <action>
    Expand installation/index.mdx body per 18-RESEARCH.md §1 Section 2 outline: (1) `go install github.com/home-beekeeper/beekeeper/cmd/beekeeper@latest`; (2) Pre-built binaries from GitHub Releases (download URL pattern); (3) Cosign verification — copy the EXACT command from docs/THREAT-MODEL.md §7 (`cosign verify --certificate-identity=https://github.com/home-beekeeper/beekeeper/.github/workflows/release.yml@refs/tags/v<version> --certificate-oidc-issuer=https://token.actions.githubusercontent.com beekeeper`); (4) SLSA Level 3 provenance — copy the EXACT command (`slsa-verifier verify-artifact beekeeper --provenance-path beekeeper.intoto.jsonl --source-uri github.com/home-beekeeper/beekeeper` — NOTE lowercase `bantuson` in source-uri); (5) CycloneDX SBOM verification; (6) Build from source (`make build`, build flags `-trimpath -buildvcs=false -mod=readonly`); (7) State directory location: `~/.beekeeper/` on Linux/macOS, `%APPDATA%\beekeeper\` on Windows. Use the THREAT-MODEL.md §7 commands verbatim — do NOT paraphrase signing commands. Multi-line commands use `\` continuation exactly as in the source.
    Frontmatter: keep title + description, ADD `source_doc: "docs/THREAT-MODEL.md, docs/release-runbook.md, CLAUDE.md"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - installation/index.mdx frontmatter contains `source_doc:` and all three referenced paths (docs/THREAT-MODEL.md, docs/release-runbook.md, CLAUDE.md) exist on disk
    - `grep -c "cosign verify" web/content/docs/installation/index.mdx` returns >= 1 and `grep -c "slsa-verifier verify-artifact" web/content/docs/installation/index.mdx` returns >= 1
    - `grep -c "go install github.com/home-beekeeper/beekeeper" web/content/docs/installation/index.mdx` returns >= 1
    - SLSA source-uri uses lowercase bantuson: `grep -c "source-uri github.com/home-beekeeper/beekeeper" web/content/docs/installation/index.mdx` returns >= 1
    - The Windows state-dir note is present: case-insensitive grep for "%APPDATA" returns >= 1
  </acceptance_criteria>
  <done>installation/index.mdx covers go install + Releases + the exact cosign/SLSA/SBOM commands from THREAT-MODEL.md §7 + build-from-source + state-dir locations, with valid `source_doc:`; AC-1/AC-3 pass for this file.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes two expanded docs sections to the Phase 18 set:
- `web/content/docs/getting-started/index.mdx` (DOCS-02) with `source_doc:`
- `web/content/docs/installation/index.mdx` (DOCS-03) with `source_doc:`
Both establish the `source_doc:` frontmatter convention (DOCS-09) on their respective pages.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → reader | A reader follows the quickstart/install verbatim and trusts the commands to be real and safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Spoofing (wrong flag) | getting-started quickstart | mitigate | Use `hooks install --target` not `--hook`; AC-3 + the acceptance grep enforce no `--hook`/`--input` phantom usage |
| T-18-05 | Tampering (signature-verify drift) | installation cosign/SLSA commands | mitigate | Copy the EXACT verification commands from docs/THREAT-MODEL.md §7 (authoritative); a wrong identity/source-uri would let a reader "verify" against the wrong provenance. source_doc anchors the claim (AC-1) |
| T-18-06 | Information Disclosure (false trust) | getting-started harness caveat | mitigate | The Claude-Code-only live-verified caveat at point-of-use prevents a reader assuming universal enforcement |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (prose only). No legitimacy gate required. |
</threat_model>

<verification>
- `cd web && python tests/accuracy_spec.py` passes AC-1/AC-3 for both files (AC-2 N/A — neither page mentions release_age/lifecycle).
- Wave gate: `cd web && pnpm build` exits 0 with the expanded MDX (run at wave boundary across 18-02..18-05).
</verification>

<success_criteria>
- getting-started leads zero → real `beekeeper check` with `hooks install --target` and the Claude-Code-only caveat.
- installation has copyable go install + the exact cosign/SLSA/SBOM commands + state-dir locations.
- Both carry valid `source_doc:`; accuracy_spec.py AC-1/AC-3 green for these files.
</success_criteria>

<output>
Create `.planning/phases/18-full-content-authoring/18-02-SUMMARY.md` when done.
</output>
