---
phase: 18-full-content-authoring
plan: 05
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - web/content/docs/troubleshooting/index.mdx
  - web/content/docs/audit-log/index.mdx
autonomous: true
requirements: [DOCS-08, DOCS-09]
must_haves:
  truths:
    - "Troubleshooting uses only real commands (beekeeper diag, beekeeper version) — no phantom hooks status / catalogs rebuild / beekeeper status (SC-4 support)"
    - "Audit-log corrects the stub's false 'rotated daily and compressed' claim to a single beekeeper.ndjson file with no rotation"
    - "Both pages carry source_doc: frontmatter and pass AC-1/AC-3"
  artifacts:
    - path: "web/content/docs/troubleshooting/index.mdx"
      provides: "Common-issue triage using real commands; brief exit-1→2 note linking the changelog"
      contains: "source_doc:"
      min_lines: 55
    - path: "web/content/docs/audit-log/index.mdx"
      provides: "Audit log location/format, audit tail/query/export, nudge audit, redaction caveat, corrected single-ndjson-no-rotation claim"
      contains: "source_doc:"
      min_lines: 55
  key_links:
    - from: "web/content/docs/troubleshooting/index.mdx"
      to: "beekeeper diag"
      via: "primary diagnostic command (replaces phantom hooks status)"
      pattern: "beekeeper diag"
---

<objective>
Author the Troubleshooting (DOCS-08) and Audit Log (DOCS-08-adjacent / DOCS-09 corrections) docs sections, accurate to the shipped binary, with `source_doc:` frontmatter (DOCS-09).

Purpose: SC-4 support (real commands only) and the mandatory stub corrections (§8.1). The current stubs are the WORST offenders: troubleshooting references `beekeeper hooks status` / `beekeeper status` (both phantom — use `beekeeper diag` / `beekeeper version`), and audit-log claims "rotated daily and compressed" with a dated-file query path (both FALSE — the shipped code writes a single `beekeeper.ndjson` with no rotation). Per D-04, the binary is documented as shipped; the docs are corrected to match the code.
Output: rewritten `troubleshooting/index.mdx` and `audit-log/index.mdx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Execution runs INLINE on main. Wave 2 — depends on 18-01. Shares no files with 18-02/18-03/18-04; may run in parallel.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-full-content-authoring/18-CONTEXT.md
@.planning/phases/18-full-content-authoring/18-RESEARCH.md
@.planning/phases/18-full-content-authoring/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Troubleshooting docs (DOCS-08) — correct phantom commands</name>
  <files>web/content/docs/troubleshooting/index.mdx</files>
  <read_first>
    - web/content/docs/troubleshooting/index.mdx (current 24-line stub — preserve title/description; it references the phantom `beekeeper hooks status`, `beekeeper catalogs rebuild`, and `beekeeper status` — ALL must be removed/corrected per §8.1)
    - docs/THREAT-MODEL.md §7 and §8 (self-quarantine runnable commands; known-gap behaviors that surface as troubleshooting symptoms)
    - docs/nudge.md (security notes — pnpm detection 2s fail-open timeout)
    - cmd/beekeeper/diag.go and cmd/beekeeper/main.go (confirm `beekeeper diag` is the real diagnostic command and `beekeeper status` does NOT exist — OQ-4)
    - 18-RESEARCH.md §1 Section 7 (Troubleshooting outline + corrections vs stub), §4.5 (exit-1→2 brief note + link), §8.1 (the phantom-command corrections)
  </read_first>
  <action>
    Rewrite troubleshooting/index.mdx per 18-RESEARCH.md §1 Section 7 outline, using ONLY real commands: (1) Hook not firing / tool still runs after block; (2) Catalog sync failures (use `beekeeper catalogs sync` — NOT the phantom `catalogs rebuild`); (3) High latency on `beekeeper check`; (4) Self-quarantine event — what it means + how to investigate (note `beekeeper version`/`beekeeper diag`/`beekeeper selftest`/`beekeeper policy validate` remain runnable during self-quarantine, §7); (5) Policy file rejected by `beekeeper policy validate`; (6) Nudge not detecting pnpm/bun (detection is fail-open with a 2s timeout, not configurable); (7) Gateway not starting / upstream unreachable; (8) Sentry not installed (Linux: requires root + systemd); (9) LlamaFirewall sidecar unreachable; (10) `beekeeper diag` as the PRIMARY diagnostic tool (replaces the phantom `hooks status`); (11) Windows gotcha: state dir is `%APPDATA%\beekeeper\`, not `~/.beekeeper/`.
    MANDATORY corrections (§8.1): remove every `beekeeper hooks status` (→ `beekeeper diag`), remove every `beekeeper catalogs rebuild` (→ `beekeeper catalogs sync`), and the "getting help" section must use `beekeeper version` + `beekeeper diag` (NOT the phantom `beekeeper status`).
    Add the brief exit-1→2 note from §4.5 (if installed from a pre-v1.3.0 binary, tools may still run despite block — upgrade + re-run `beekeeper hooks install --target <harness>`) and LINK to /changelog/v1.3.0/ rather than duplicating.
    Frontmatter: keep title + description, ADD `source_doc: "docs/THREAT-MODEL.md, docs/nudge.md, cmd/beekeeper/diag.go"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - troubleshooting/index.mdx frontmatter contains `source_doc:` and all three referenced paths exist on disk
    - All phantom commands removed: `grep -c "beekeeper hooks status" web/content/docs/troubleshooting/index.mdx` returns 0; `grep -c "beekeeper catalogs rebuild" web/content/docs/troubleshooting/index.mdx` returns 0; `grep -c "beekeeper status" web/content/docs/troubleshooting/index.mdx` returns 0
    - Real diagnostic command present: `grep -c "beekeeper diag" web/content/docs/troubleshooting/index.mdx` returns >= 1
    - exit-1→2 note links the changelog: grep for "/changelog/v1.3.0" returns >= 1
    - accuracy_spec.py AC-3 passes for this file (the whole reason this file was the worst offender)
  </acceptance_criteria>
  <done>troubleshooting/index.mdx uses only real commands (beekeeper diag/version, catalogs sync), removes all three phantom commands, links the exit-1→2 history to the changelog, and carries valid source_doc:; AC-3 passes for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Author Audit Log docs — correct the rotation claim</name>
  <files>web/content/docs/audit-log/index.mdx</files>
  <read_first>
    - web/content/docs/audit-log/index.mdx (current 24-line stub — preserve title/description; it FALSELY claims "rotated daily and compressed" and a `cat ~/.beekeeper/audit/$(date +%Y-%m-%d).ndjson` dated-file query — BOTH must be corrected per §8.1)
    - docs/THREAT-MODEL.md §8 (audit redaction is field-scoped, not content-scanning; Sentry-derived fields written verbatim; behavioral-watch path does NOT route through RedactRecord — the point-of-use caveat)
    - cmd/beekeeper/main.go (audit subcommands wiring; confirm there is NO rotation logic and the file is a single beekeeper.ndjson) and cmd/beekeeper/nudge.go (nudge audit record fields)
    - 18-RESEARCH.md §1 Section 8 (Audit Log outline + corrections vs stub + the mandatory redaction caveat), §2 (audit tail/query/export flag surface)
  </read_first>
  <action>
    Rewrite audit-log/index.mdx per 18-RESEARCH.md §1 Section 8 outline: (1) Location — `~/.beekeeper/audit/beekeeper.ndjson`, a SINGLE NDJSON file (Windows `%APPDATA%\beekeeper\audit\beekeeper.ndjson`); explicitly correct the stub's false "rotated daily and compressed" claim — there is NO rotation logic in the shipped code; (2) Record types and key fields; (3) `beekeeper audit tail [--no-follow]` — stream live; (4) `beekeeper audit query --since --agent --tool --decision --limit` — filter (this REPLACES the stub's wrong `cat ...$(date +%Y-%m-%d).ndjson` dated-file path); (5) `beekeeper audit export --format ndjson|csv|otlp`; (6) `beekeeper nudge audit [--since]` — nudge records use record_type:"nudge" with nudge_action/original_command/rewritten_command/reason_code/pm_state; (7) Record examples (allow, warn, block, nudge, config_change, quarantine_restore); (8) Remote sinks (OTLP/HTTPS/syslog fan-out) + the redaction caveat.
    MANDATORY corrections (§8.1): remove "rotated daily and compressed"; remove the dated-file `cat` query (use `beekeeper audit query`/`tail`). Add the §1 Section 8 point-of-use caveat that redaction is field-scoped — the Reason field and raw PM commands are redacted before remote sinks, but Sentry-derived fields (file paths, network destinations, process exe paths) are written verbatim and the behavioral-watch path does not route through RedactRecord; the local file is owner-only (0600).
    Frontmatter: keep title + description, ADD `source_doc: "docs/THREAT-MODEL.md, cmd/beekeeper/main.go, cmd/beekeeper/nudge.go"`.
  </action>
  <verify>
    <automated>cd web && python tests/accuracy_spec.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - audit-log/index.mdx frontmatter contains `source_doc:` and all three referenced paths exist on disk
    - The false rotation claim is gone: case-insensitive grep for "rotated daily" returns 0
    - The single-file location is documented: `grep -c "beekeeper.ndjson" web/content/docs/audit-log/index.mdx` returns >= 1
    - Real query surface present (not the dated-file cat): `grep -c "beekeeper audit query" web/content/docs/audit-log/index.mdx` returns >= 1; `grep -c "date +%Y-%m-%d" web/content/docs/audit-log/index.mdx` returns 0
    - The field-scoped redaction caveat is present: case-insensitive grep for "redact" returns >= 1
    - accuracy_spec.py AC-1/AC-3 pass for this file
  </acceptance_criteria>
  <done>audit-log/index.mdx corrects the rotation/dated-file errors to a single beekeeper.ndjson with no rotation, documents audit tail/query/export + nudge audit, includes the field-scoped redaction caveat, and carries valid source_doc:.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes the final two docs sections and the heaviest stub corrections:
- `web/content/docs/troubleshooting/index.mdx` (DOCS-08) — phantom commands removed
- `web/content/docs/audit-log/index.mdx` (DOCS-08-adjacent / DOCS-09) — rotation claim corrected
Both carry `source_doc:` frontmatter (DOCS-09). With this plan, all 8 docs sections carry `source_doc:` and the accuracy_spec.py harness goes fully GREEN.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs author → reader | A reader debugging / auditing trusts the doc's commands and the audit-log behavior description |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-15 | Spoofing (phantom command) | troubleshooting | mitigate | AC-3 + acceptance greps remove `hooks status` / `catalogs rebuild` / `beekeeper status` — a reader debugging would otherwise run a dead command and lose trust |
| T-18-16 | Repudiation/false trust (audit behavior) | audit-log rotation claim | mitigate | Correct the false "rotated daily" claim to the real single-file/no-rotation behavior; a reader relying on rotation for retention/forensics would be misled |
| T-18-17 | Information Disclosure (sink leak) | audit-log redaction caveat | mitigate | Field-scoped redaction caveat: Sentry-derived fields (paths, network dests, exe paths) reach remote sinks verbatim — a reader must know before fanning out to OTLP/syslog (THREAT-MODEL §8) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (prose only). No legitimacy gate required. |
</threat_model>

<verification>
- `cd web && python tests/accuracy_spec.py` passes AC-1/AC-3 for both files AND (with all 8 sections now authored) the whole harness goes GREEN.
- Wave gate: `cd web && pnpm build` exits 0 with all 8 expanded sections.
</verification>

<success_criteria>
- troubleshooting uses only real commands; all three phantom commands removed.
- audit-log corrects the rotation/dated-file errors and includes the redaction caveat.
- Both carry valid source_doc:; accuracy_spec.py goes fully green once all 8 sections are authored.
</success_criteria>

<output>
Create `.planning/phases/18-full-content-authoring/18-05-SUMMARY.md` when done.
</output>
