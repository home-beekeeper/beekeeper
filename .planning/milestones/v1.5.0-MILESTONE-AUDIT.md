---
milestone: v1.5.0
audited: 2026-06-22
status: tech_debt
methodology: evidence-based (hand-managed milestone — no per-phase VERIFICATION.md/SUMMARY.md; verified against merged main + release-gate security/code reviews + integration checker + green cross-platform CI)
scores:
  requirements: 18/18 satisfied (REL-01 satisfied modulo the deliberate Gate-2 signature)
  phases: 6/6 complete
  integration: 6/6 wiring areas WIRED
  flows: 4/4 E2E flows WIRED
gaps:
  requirements: []   # zero unsatisfied
  integration: []    # zero broken seams
  flows: []          # zero broken flows
tech_debt:
  - phase: 31-test-coverage-e2e-ci
    source: .planning/phases/31-test-coverage-e2e-ci/31-REVIEW-DECISION.md
    items:
      - "M-01 (MED): PostureIncidentModel (internal/tui/posture_incident.go) reads pkg/eco from CatalogMatches (empty for posture) and is not wired into the live TUI feed — latent display-only; touches no E2E path. Fix when the incident card is wired in (thread package into the hook posture decision record)."
      - "M-02 (MED): alertToAuditRecord keys the install-observed mapping on Severity=='info' not RuleID=='SENTRY-009'. Currently correct (only SENTRY-009 emits info); fragility fix across 3 daemons deferred."
      - "L-01: allow-once read-then-write TOCTOU (benign double-consume under concurrent same-package installs; documented in posture_allowonce.go)."
      - "L-02: allow-once token consumed even when the install was already clean (minor UX)."
      - "L-03: SENTRY-009 misses `npm ci`; anywhere-in-cmdline verb scan (detection-completeness only)."
      - "L-04: Node/Bun version floors configured/detected but unused (dead config carried from the nudge)."
      - "L-05: pkgparse `go get` prefix lacks a trailing space (nit)."
      - "I-01: scoped-npm cache-key collision in lifecycle_cache.go (mirrors pre-existing ageCachePath)."
      - "I-02: second time.Now() read in handler.go (harmless)."
      - "Security-Low: experimental Unix shim fmt.Sprintf template would mis-quote a pathological realBin/tool path (pre-existing, non-exploitable with controlled inputs)."
  - phase: cross-milestone (carried, not v1.5.0)
    items:
      - "corpus-behavior-sig-ipv6-normalization.md — known IPv6 normalize quirk accepted in v1.4.0."
  - phase: doc-style (non-critical, integration checker note)
    items:
      - "docs/THREAT-MODEL.md paraphrases the boundary statement rather than embedding the literal posture.BoundaryStatement constant (semantics accurate + cross-referenced; not a failing REQ)."
stale_acknowledged:   # open-artifact audit items, all done/deferred from prior milestones
  - "quick_task 260612-f80 (first-responder quarantine) — DONE, merged to main; tracking file never marked complete."
  - "quick_task 260615-ky4 (FRB-05 overlay wiring) — DONE same-day in v1.4.0; tracking file never marked complete."
  - "todo docs-styling-polish.md — self-marked PROMOTED → Phase 18.1 (shipped v1.3.0)."
human_gate_pending:
  - "Gate 2 (REL-01): the maintainer cuts + signs the v1.1.0 release tag. By design the agent does not self-sign. No v1.1.0 tag exists yet (local or origin)."
---

# Milestone Audit — v1.5.0 "Install Posture" (ships as release v1.1.0)

**Status: `tech_debt`** — milestone definition of done is achieved (all 18 requirements satisfied, full cross-phase integration, all E2E flows wired), zero blockers; a set of minor, already-documented deferred items is tracked for a future milestone. One human gate remains by design: the maintainer's v1.1.0 tag signature (Gate 2).

## Audit methodology (hand-managed milestone)

This milestone follows the project's hand-managed convention (like v1.3.0/v1.4.0): no per-phase `VERIFICATION.md`/`SUMMARY.md` files. Mechanically requiring them would falsely flag all phases "unverified." Instead each requirement was verified against **merged `main`** (commit `4e92130`) using: the orchestrator's per-phase independent re-verifications, the release-gate **security review** (all 8 load-bearing invariants HELD → APPROVE), the **code review** (1 HIGH fixed pre-merge, mediums/lows deferred), the **integration checker** subagent (this audit), and **green cross-platform CI** on the merged PR (3-OS test matrix + e2e + fuzz×4 + coverage-floor + zizmor).

## Requirements coverage — 18/18 satisfied

| REQ-ID | Requirement | Phase | Evidence | Status |
|--------|-------------|-------|----------|--------|
| IPST-01 | Default posture at the hook via the policy engine | 27 | `posture_adapter.evaluatePosture` ← `handler.go:373-374` (most-restrictive merge) | ✅ |
| IPST-02 | Release-age <24h catches + reason | 27 | `policy/release_age.go` + adapter; `TestRunCheckPostureFreshPackageWarns` | ✅ |
| IPST-03 | Lifecycle-script warn, raisable to block | 27/29 | `policy/lifecycle.go` + IPOVR-03 opt-up | ✅ |
| IPST-04 | Git/remote-URL detection + warn | 26 | `pkgparse.RemoteSource` → `policy/remote_source.go` | ✅ |
| IPST-05 | Posture rules as pure policy inputs | 26 | pure evaluators (I/O-free) consumed by the adapter | ✅ |
| IPST-06 | Sentry observes installs, detection-labeled | 27 | `install_observe.go` (SENTRY-009) → `sentry_install_observed` in 3 daemons | ✅ |
| IPBND-01 | Boundary statement everywhere | 27/30 | `boundary.go` → help/docs/web | ✅ |
| IPBND-02 | Boundaries documented exactly | 30 | `docs/install-posture.md` + web docs + threat model | ✅ |
| IPVIEW-01 | `beekeeper posture` read-only side-by-side (CLI+TUI) | 28 | shared `view.BuildComparison` (CLI + TUI panel) | ✅ |
| IPVIEW-02 | View writes no config (test-asserted) | 28 | `TestPostureCmd_ReadOnlyGuarantee` (sha256 byte-for-byte) | ✅ |
| IPOVR-01 | Graduated scoped responses on warn | 29 | `posture allow --once/--always` | ✅ |
| IPOVR-02 | Distinct posture_override record; posture-scoped allowlist (T-09-31) | 29 | `posture_override.go` + `TestRunCheckPostureAllowAlwaysDoesNotBypassCatalogBlock` | ✅ |
| IPOVR-03 | Per-rule warn→block opt-up; tighten-only; fail-soft unknown | 29 | `PostureConfig` + `mergePostureUntrusted` + `posturizeWithAction` | ✅ |
| NMIG-01 | Nudge removed (CLI/config/package/audit) | 26 | `cmd/beekeeper/nudge.go` + `internal/nudge/` deleted; no settable nudge keys | ✅ |
| NMIG-02 | Release-age preserved as posture rule | 26 | `EvaluateReleaseAge` wired into the hook | ✅ |
| NMIG-03 | Home bullet reframed + npm v12 note | 30 | web `feature-cards.tsx` "Install posture" | ✅ |
| NMIG-04 | Shim kept + repointed + documented roadmap | 27/30 | `shim.go` + `buildShimToolCall` → `beekeeper check`; honest docs | ✅ |
| REL-01 | Version v1.1.0 + changelog + signed-commit PRs to main | 30/31 | web v1.1.0 changelog + badges; **PRs #19 + web #1 merged to main** | ✅ * |

\* **REL-01 caveat (not a gap):** version/changelog/PRs are done and merged. The **final signed `v1.1.0` tag is the maintainer's Gate-2 action** — the requirement itself states "the agent does not sign the release." No `v1.1.0` tag exists yet; this is the expected pre-Gate-2 state, intentionally not self-certified.

## Cross-phase integration — all WIRED (integration checker, merged main)

| Area | Verdict | Key evidence |
|------|---------|--------------|
| Pure rules → adapter → handler merge | WIRED | `handler.go:373-374`; live-path proof posture cannot lift a catalog/SPATH block |
| Per-rule severity + scoped override → config + audit | WIRED | `posture_override.go`; posture-scoped allowlist never touches `package_allowlist` (T-09-31) |
| Read-only view (CLI+TUI shared model) | WIRED | `BuildComparison`; IPVIEW-02 read-only test |
| SENTRY-009 in all 3 daemons | WIRED | `rules.go:398` → linux/darwin/windows `alertToAuditRecord` |
| Boundary statement propagation | WIRED | `boundary.go` → help/docs/web (THREAT-MODEL paraphrases — non-critical) |
| Nudge removal + shim repoint | WIRED | nudge code gone; `buildShimToolCall` → `beekeeper check` |

**E2E flows (`internal/check/e2e_test.go`, `-tags e2e`):** agent-install-warn/block-at-hook ✅, human-install-observe-only ✅ (daemon taps CI-only by design; observe-never-block covered by unit + 3 daemon mappings), `beekeeper posture` view ✅, allow-always-honored-never-bypasses-catalog ✅.

## Tech debt (zero blockers — see frontmatter for the full list)

10 tracked items, all minor and already captured in `31-REVIEW-DECISION.md`: M-01 (latent display-only incident card), M-02 (daemon severity-vs-RuleID fragility, currently correct), 5 Low, 2 Info, the pre-existing non-exploitable Unix-shim quoting note, plus the carried v1.4.0 IPv6-normalization quirk and the THREAT-MODEL boundary-paraphrase style note. None affects a requirement, an integration seam, an E2E flow, or a security invariant.

## Open-artifact audit — acknowledged

The pre-close audit surfaced 4 items, all stale or carried from prior milestones (2 done-but-unmarked quick tasks, 1 already-promoted todo, 1 known-deferred IPv6 todo). Acknowledged and recorded; none are v1.5.0 gaps.

## Verdict

Milestone v1.5.0 achieved its definition of done with zero blockers. Proceed to `/gsd-complete-milestone v1.5.0` (accept the tracked tech debt). The single remaining real-world action is the maintainer's Gate-2 v1.1.0 tag signature.
