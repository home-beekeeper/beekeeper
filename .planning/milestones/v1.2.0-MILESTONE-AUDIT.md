---
milestone: v1.2.0
milestone_name: "Runtime Behavioral Hardening"
audited: 2026-06-04T16:19:37Z
auditor: Claude (gsd-audit-milestone)
status: tech_debt
scope_phases: [6, 7, 8]
scores:
  requirements: 17/17
  phases: 3/3
  integration: 4/4   # cross-phase claims A,B,C,D all WIRED
  flows: 3/3          # F1/F2/F3 complete end-to-end through single runCheck
gaps:                 # critical blockers — none
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 06-corroboration-severity-hardening
    items:
      - "Nyquist PARTIAL: 06-VALIDATION.md frontmatter is stale (status: draft, nyquist_compliant: false, wave_0_complete: false) even though 06-VERIFICATION.md is passed 5/5. Cosmetic — run /gsd-validate-phase 6 to reconcile, or accept."
  - phase: 08-package-manager-nudge-behavioral-test-suite
    items:
      - "BTEST-03 release-gate fragility (NEW, integration check): TestE2ELiveBinary/CORR_aifigure_critical_block depends on live OSV reachability. The seeded local catalog entry is unsigned + Versions:[\"*\"] → 'warn' alone; the block only fires because the compiled binary queries osv.dev and gets a signed match. Offline / rate-limited / egress-blocked CI would FAIL the release gate (exit 0 vs 1). Hermetic unit tests (TestRunCheckAiFigureBlocks et al.) cover CORR offline, so the requirement is met; the E2E case is not network-independent."
      - "realMetadataFetch (internal/gateway/drift.go:57-63) returns an empty map → production version_drift check emits no record until a real registry query is added. Gateway-scheduler-only; the `beekeeper check` hook path does not call checkDrift. Wiring + schema + IsMajorDrift + async behavior fully tested via injected metadataFetchFn. INFO-1, documented in 08-06 'Known Stubs'. Floor auto-update is Out-of-Scope."
      - "config.LoadLayered does not merge the Config.Nudge *NudgeConfig pointer field. Mitigated at the consumer layer (defaultNudgeConfigHelper in cmd/beekeeper/nudge.go + cfg.Nudge nil-guard in handler.go:302-304); root cause in the LoadLayered merge() is unfixed. Any future LoadLayered consumer reading cfg.Nudge without a nil-check silently gets nil instead of defaults. Deferred (IN-05 category)."
      - "Stale comment handler.go:261 (\"sensitive-path block is merged LAST\") — accurate at Phase 7, now wrong: NUDGE (lines 290-309) is the actual last merge. Documentation only; ordering is functionally correct."
  - phase: 07-sensitive-path-runtime-enforcement
    items:
      - "Deferred Info findings accepted in 07-REVIEW.md: IN-01 EvalSymlinks resolves away a sensitive fragment via an ancestor symlink; IN-02 Windows ADS / trailing-dot basename evasion; IN-03 'more'/'less' verb false-positive without word-boundary. Accepted as deferred at phase close."
  - phase: cross-cutting
    items:
      - "TestHookHandlerAllow (handler_test.go) can intermittently time out under parallel load — its 5s execTimeout is tight vs live OSV latency. Pre-existing, NOT a v1.2.0 regression (WR-03's parallel PM probes did not touch the OSV timeout)."
nyquist:
  compliant_phases: [7, 8]
  partial_phases: [6]
  missing_phases: []
  overall: partial
notes:
  - "gsd-sdk phase-number resolvers map bare 6/7/8 to ARCHIVED v1.0.0 phase dirs; this audit used explicit live paths under .planning/phases/NN-slug/ throughout (per STATE.md operator warning)."
  - "go test -race is CGO-gated and CI-only on this Windows dev box; the authoritative race confirmation is the CI job, not local."
---

# Milestone v1.2.0 "Runtime Behavioral Hardening" — Audit Report

**Audited:** 2026-06-04 · **Status:** `tech_debt` (achieved; no blockers; accumulated deferred items warrant a review decision)
**Scope:** Phases 6 (CORR / F1), 7 (SPATH / F2), 8 (NUDGE + BTEST / F3 + behavioral test suite) — the live `.planning/phases/06|07|08-*` dirs (NOT the archived v1.0.0 phases of the same number).

## Definition of Done (from ROADMAP.md / REQUIREMENTS.md)

Close the three runtime-enforcement gaps that live `beekeeper check` validation exposed — a critical-severity malware package that warned instead of blocking (F1), credential files that returned ALLOW to agent reads (F2), and pnpm/bun installs that bypassed catalog matching entirely (F3) — each locked in by a behavioral test suite proving the wiring is live.

**Verdict: definition of done achieved.** All three gaps are closed and proven live; the behavioral test suite (table-driven §10, RunCheck integration, live-binary E2E, fuzz) is the green release gate.

## Requirements Coverage — 17/17 satisfied (3-source cross-reference)

Sources: (1) phase VERIFICATION.md status + per-requirement evidence, (2) REQUIREMENTS.md traceability `[x]`/Complete, (3) SUMMARY.md REQ-ID tags. All three corroborate for every requirement; **no orphans**, **no unsatisfied**.

| REQ | Phase | VERIFICATION | Traceability | SUMMARY tag | Final |
|-----|-------|--------------|--------------|-------------|-------|
| CORR-01 | 6 | passed / SATISFIED | `[x]` Complete | `CORR-01` (06-01) | **satisfied** |
| CORR-02 | 6 | passed / SATISFIED | `[x]` Complete | `CORR-02` (06-01) | **satisfied** |
| SPATH-01 | 7 | passed / SATISFIED | `[x]` Complete | `spath, sc1` (07-03) | **satisfied** |
| SPATH-02 | 7 | passed / SATISFIED | `[x]` Complete | `spath, sc2` (07-03) | **satisfied** |
| SPATH-03 | 7 | passed / SATISFIED | `[x]` Complete | `spath, sc3` (07-03) | **satisfied** |
| SPATH-04 | 7 | passed / SATISFIED | `[x]` Complete | `spath, sc4` (07-03) | **satisfied** |
| NUDGE-01 | 8 | passed / SATISFIED | `[x]` Complete | `nudge, pkgparse` (08-01/02) | **satisfied** |
| NUDGE-02 | 8 | passed / SATISFIED | `[x]` Complete | `nudge` (08-03/04) | **satisfied** |
| NUDGE-03 | 8 | passed / SATISFIED | `[x]` Complete | `nudge` (08-03/06) | **satisfied** |
| NUDGE-04 | 8 | passed / SATISFIED | `[x]` Complete | `nudge` (08-03/06) | **satisfied** |
| NUDGE-05 | 8 | passed / SATISFIED | `[x]` Complete | `nudge, pkgparse` (08-01/03) | **satisfied** |
| NUDGE-06 | 8 | passed / SATISFIED | `[x]` Complete | `nudge, audit` (08-03/05/06) | **satisfied** |
| NUDGE-07 | 8 | passed / SATISFIED | `[x]` Complete | `nudge, cli` (08-08) | **satisfied** |
| NUDGE-08 | 8 | passed / SATISFIED | `[x]` Complete | `nudge` (08-03/06/07/08) | **satisfied** |
| BTEST-01 | 8 | passed / SATISFIED | `[x]` Complete | `behavioral` (multi) | **satisfied** |
| BTEST-02 | 8 | passed / SATISFIED | `[x]` Complete | `BTEST-02` (08-07) | **satisfied** |
| BTEST-03 | 8 | passed / SATISFIED | `[x]` Complete | `BTEST-03, e2e, release-gate` (08-07) | **satisfied** (release-gate fragility noted in Tech Debt) |

## Phase Verification — 3/3 passed

| Phase | VERIFICATION status | Score | Notes |
|-------|---------------------|-------|-------|
| 6 — Corroboration Severity Hardening | passed | 5/5 must-haves | All 4 enforcement consumers wire `resolveCatalogHealthy`; purity enforced |
| 7 — Sensitive-Path Runtime Enforcement | passed | 9/9 truths | CR-01/CR-02 + WR-01..04 all RESOLVED; IN-01/02/03 deferred |
| 8 — Package-Manager Nudge + Behavioral Test Suite | passed | 11/11 reqs, 5/5 SCs | E2E + fuzz release gates green; INFO-1 (drift stub) documented |

## Cross-Phase Integration — 4/4 WIRED (gsd-integration-checker)

The crux of v1.2.0 is that all three decisions converge in the single `runCheck` chokepoint with most-restrictive-wins ordering. Confirmed in code:

| # | Claim | Verdict | Evidence |
|---|-------|---------|----------|
| A | Decision-merge ordering: `ApplyPolicyOverlay` → SPATH → NUDGE, most-restrictive-wins; overlay allow cannot downgrade a path/catalog block | **WIRED** | handler.go:246-309 (overlay@269, SPATH@280-288, NUDGE@290-309); `mergeDecisions` most-restrictive |
| B | `resolveCatalogHealthy(cacheDir)` called BEFORE `policy.Evaluate` in runCheck | **WIRED** | handler.go:252 then :257; same adapter in gateway/watch/scan sanity.go |
| C | F3 fix end-to-end: `pnpm add` → `pkgparse.Parse` → ecosystem "npm" → `LookupAll("npm",pkg)` → corroboration; duplicate parsers removed | **WIRED** | engine.go:224-233 + enforce.go:243; `installPrefixes`/`installPrefixesOverlay` deleted (commits 47770ce, 12b15c3) |
| D | Live-binary E2E exercises SPATH + CORR + NUDGE against the compiled binary, asserting exit codes + audit records | **WIRED** | e2e_test.go `TestE2ELiveBinary`; SPATH/CORR/NUDGE-pnpm PASS, NUDGE-bun SKIP (bun absent). ⚠ CORR sub-case network-dependent — see Tech Debt |

**Integration gaps: none.** 18 cross-phase exports connected, 0 orphaned, 0 missing. F1/F2/F3 → single-runCheck flow completes end-to-end; all decisions converge at `finalizeWithAC` (handler.go:319) which writes the audit record and returns the exit code.

## Nyquist Coverage — partial

| Phase | VALIDATION.md | nyquist_compliant | Status | Action |
|-------|---------------|-------------------|--------|--------|
| 6 | exists | false (status: draft) | **PARTIAL** | Optional: `/gsd-validate-phase 6` to reconcile stale frontmatter (VERIFICATION is passed 5/5) |
| 7 | exists | true (wave_0_complete: true) | COMPLIANT | — |
| 8 | exists | true | COMPLIANT | — |

Phase 6's PARTIAL is a stale-frontmatter artifact: the VALIDATION.md was drafted pre-execution and never flipped to compliant, even though the phase verified passed. Discovery only — not a blocker.

## Tech Debt & Robustness Notes (no blockers)

1. **BTEST-03 CORR E2E is not hermetic (NEW).** `TestE2ELiveBinary/CORR_aifigure_critical_block` blocks only because the live binary reaches OSV; offline/rate-limited CI would flake the release gate. *Recommended:* seed a signed local OSV-equivalent entry (as the hermetic unit tests do) so the E2E CORR case blocks without network — or explicitly gate/skip it when OSV is unreachable so a network outage never red-lights a release.
2. **realMetadataFetch stub** (gateway/drift.go) — gateway-scheduler-only; check hook unaffected; floor auto-update is Out-of-Scope. Informational.
3. **LoadLayered nil-Nudge root cause** unresolved; mitigated at consumer layer. Fix `merge()` in config.LoadLayered to merge the `Nudge` pointer so future consumers don't silently get nil.
4. **Stale comment** handler.go:261 — trivial doc fix.
5. **Phase-7 deferred Info** IN-01/02/03 (symlink ancestor, Windows ADS/trailing-dot, more/less word-boundary) — accepted as deferred in 07-REVIEW.md.
6. **TestHookHandlerAllow** intermittent timeout under parallel load — pre-existing, not a v1.2.0 regression.

## Conclusion

v1.2.0 **achieved its definition of done**: F1/F2/F3 closed, all 17 requirements satisfied across three independent sources, all three phases verified passed, cross-phase integration fully WIRED end-to-end, behavioral test suite + live-binary E2E + fuzz release gates green. **No critical blockers.** The `tech_debt` status reflects one newly-surfaced release-gate robustness concern (CORR E2E network dependency) plus a handful of already-documented deferred items — none of which block the milestone. Safe to complete; the CORR E2E hermeticity item is the one worth deciding on (accept as debt vs. quick pre-tag fix).

---
_Report: .planning/v1.2.0-MILESTONE-AUDIT.md_
